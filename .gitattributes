# All text files use LF (Unix-first repo; WSL/Linux/macOS execution requires LF)
* text=auto eol=lf
docs/history.md merge=union
CHANGELOG.md merge=union

# PowerShell scripts work fine with LF — no override needed
