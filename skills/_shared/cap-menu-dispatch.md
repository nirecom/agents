# Cap-menu Dispatch — RETIRED (issue #1248)

This protocol is retired. Cap decisions (auto-extend / silent land / risk escalation)
are now handled by `bin/review-loop-verdict` inside `bin/run-codex-review-loop`.

For the current exit-code contract, see `skills/_shared/codex-review-loop.md`.
