# Python bytecode and test caches
__pycache__/
*.pyc
.pytest_cache/

# Chromium/Electron writes this into the CWD when logging is enabled
debug.log

testResults.xml
node_modules/
settings.local.json
settings-extension.json
CLAUDE.local.md
.env
.private-info-blocklist
.private-info-allowlist
.offensive-content-blocklist
.backfill-appended.txt

# Local backups written by /worktree-end (may contain gitignored .env files)
.worktree-backup/

# Installer-created directory symlink: /wf-init alias for /workflow-init (#1743)
skills/wf-init

# CodeGraph's local index — machine-local build artifact, never shared
.codegraph/
