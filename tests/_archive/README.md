# tests/_archive/

Tests archived here target deleted behaviors or non-existent files.
They are not executed by `tests/run-all.sh`.
Kept for historical reference only — `git log` traces the full story.
