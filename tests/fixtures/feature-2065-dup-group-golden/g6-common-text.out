# audit-tests-common.sh report — <DATE>
# Scope: top-level tests/*.{sh,Tests.ps1},test_*.py excluding feature-<N>-*
# Criteria: every '# Tests:' target is missing — the filename's issue reference gates deletion only
# Cutoff: <DATE> (stale-months: 3)
# Mode: OFFLINE (orphans are still reported; deletion of issue-referencing files is held)

ORPHAN: tests/common-missing.sh
  Tests: bin/gone2.sh
  Missing paths: bin/gone2.sh
  Deletion unit: tests/common-missing.sh

  (dry-run; nothing was written — omit --dry-run to apply)
