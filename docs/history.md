# History (agents)

## Archived
- [2026](1782532607-20300-pr1157-history-staging/2026.md) — 94 entries

### FEATURE: PR #1089 — feature/lang-conv-fix (2026-06-25, afda5f873dbe85eb3c03d94378d89f5aca9c8263, #1089)

Background: fix(#917,#1078,#1079): CONV_LANG/PLAN_LANG compliance — fallback headers, orchestrator localization, UUID artifact fix

Changes: #1078: Added conv-lang-fallback:v1 header and CONV_LANG read directive (MOP-7/MDP-7 prose) to make-outline-plan and make-detail-plan SKILL.md files so orchestrators read CONV_LANG and respond in the configured language; bin/review-loop-cap-menu and bin/review-loop-summarize-concerns remain English-only (public repo — no hardcoded non-English strings); added --round N to review-loop-cap-menu so cap menus show "Round N cap reached for LABEL" and expose round_number in JSON; updated .env.example CONV_LANG comment to reflect planning dialog coverage;#917: Added conv-lang-fallback:v1 header to 12 worker agent files that were omitted from the PR #918 sweep: commit-push-worker, test-runner, web-researcher, doc-append-worker, issue-close-finalize-worker, issue-close-stage-worker, worktree-backup-worker, issue-create-survey-worker, issue-reconcile-worker, session-close-worker, worktree-copy-worker, refactor-prompts-judge;#1079: Extracted hooks/lib/is-plan-artifact.js with isPlanArtifact() covering both legacy timestamp (YYYYMMDD-HHMMSS) and current UUID session ID formats; updated hooks/check-plan-lang.js to use isPlanArtifact() in place of the inline ARTIFACT_RE that only matched the legacy format, fixing PLAN_LANG enforcement for all current UUID-format sessions <!-- compose-doc-append-sentinel: branch=feature/lang-conv-fix pr=#1089 -->

### FEATURE: PR #1092 — feature/1067-supervisor-alert-audit (2026-06-25, 85531bc3253d077a6323fef3210eeb131537bb5f, #1092)

Background: feat(#1067): EM Supervisor L2/L3 → alert/audit two-mode merge

Changes: FEATURE #1067 — EM Supervisor L2/L3 → alert/audit two-mode merge (2026-06-25): renamed l2_*/l3_* state fields and CLI tools to alert_*/audit_* (supervisor-write-alert, supervisor-write-audit, supervisor-write-audit-verdict); unified agent dispatch — alert mode (agents/supervisor.md, Sonnet, C1/C2/C3 triggers, narrow current-turn scope) and audit mode (agents/supervisor-audit.md, Opus, stage-boundary/severity-threshold triggers, broad all-stages scope); deleted agents/supervisor-layer3.md; C3 bugfix extends WORKFLOW_OFF detection alongside existing WORKTREE_OFF and writes alert_cause; severity reclassification (reportBlock error, reportFallback warning); arming threshold raised to ≥warning (notice-only sessions no longer arm); Final Report skeleton gains SUPERVISOR_ALERT_SUMMARY / SUPERVISOR_AUDIT_SUMMARY placeholders with SC-6 substitution instructions; docs/architecture/claude-code.md EM Supervisor section rewritten for alert/audit two-mode design. <!-- compose-doc-append-sentinel: branch=feature/1067-supervisor-alert-audit pr=#1092 -->

### BUGFIX: session-id resolvers returned a foreign concurrent session (#1082) (2026-06-25, 1ad76df)

Background: Six session-id resolvers resolved the wrong session in concurrent environments. In the Bash-tool subprocess path CLAUDE_ENV_FILE is not propagated (Anthropic bug #27987) and the repo-manufactured CLAUDE_SESSION_ID is absent, so each resolver fell through to a JSONL mtime scan that returns the most-recently-active OTHER session, never the caller's own. A 3-session reproduction returned a foreign session 3/3 times — systematic contamination, not a race.

Changes: Read the CC-native, per-session-distinct CLAUDE_CODE_SESSION_ID before the JSONL scan in all six resolvers (hooks/lib/workflow-state/session-id.js resolveSessionId, hooks/lib/resolve-workflow-session-id.js existence-guarded, bin/github-issues/wip-state/session-id.sh, bin/lib/codex-core.sh, bin/lib/gemini-core.sh); cc-session-title deleted its inline duplicate resolver and now delegates to the shared resolveSessionId (SSOT). Existing CLAUDE_SESSION_ID/CLAUDE_ENV_FILE/JSONL fallbacks preserved for headless/CI. Scope expanded 3->6 after a pre-implementation class-member sweep. CLAUDE_SESSION_ID relay removal deferred to #1091.

Test gap: no concurrent-session test existed for the session-id resolvers; additionally test setup/teardown did not unset ambient CLAUDE_CODE_SESSION_ID, which would have masked the JSONL-fallback path once the fix landed.

### FEATURE: PR #1090 — feature/wf-plan-impl (2026-06-25, da2d98e12c5504ed0d17d5ba8c5599397faa8a06, #1090)

Background: feat(#721): add WF-PLAN workflow type for meta-label issues

Changes: feat(#721): add WF-META workflow type for meta-label issues — meta issues don't require implementation; existing WF-CODE-N forces 14 steps including tests, branches, and security review that don't apply. Added `workflow_type` field to session state JSON (`"wf-code"` default + migration shim; old `"wf-plan"` value migrated to `"wf-meta"` on read). New `bin/workflow/set-workflow-type` CLI sets the field. Oracle (`bin/workflow/next-step`) auto-skips 8 non-applicable steps (branching_complete, write_tests, review_tests, run_tests, review_security, docs, user_verification, cleanup) and renders `[-]` in `--list` output. `workflow-init` detects `meta` label and routes to Path META (PM1-PM4). `clarify-intent` now includes a CI-3a decomposition probe: evaluates D1-D5 signals after scope is agreed, proposes WF-META to user via AskUserQuestion when ≥2 signals fire (never auto-switches). `CLAUDE.md` updated: WF-META-N documented. Session-id validation tightened to `^[A-Za-z0-9_-]+$` allowlist in both `set-workflow-type` and `next-step` (path-traversal defense-in-depth, closes security scanner F1/F2). <!-- compose-doc-append-sentinel: branch=feature/wf-plan-impl pr=#1090 -->

### FEATURE: PR #1098 — fix/1067-migration (2026-06-25, d631c5dc0e3317238d633c5503c6588b81229768, #1098)

Background: fix(#1067): add migrateLegacyState() to handle pre-#1092 state files

Changes: BUGFIX #1067: Added `migrateLegacyState()` to `readStateOrInit` in `hooks/lib/supervisor-state-writer.js`. Pre-#1092 state files that had `layer2`/`layer3` keys caused `validate()` to fail with "alert must be an object; audit must be an object". The migration transparently renames `layer2`→`alert` (with `l2_*`→`alert_*` field mapping) and `layer3`→`audit` (with `l3_*`→`audit_*` field mapping) on first read, and backfills `created_at`/`last_updated` when absent. SA4b–SA4f tests cover idempotency, layer2-only, layer3-only, validate(), and appendFinding() end-to-end. <!-- compose-doc-append-sentinel: branch=fix/1067-migration pr=#1098 -->

### FEATURE: PR #1106 — fix/721-detail-skip (2026-06-26, 3d09cd14194b841e46d950f73de7b11a34a2e452, #1106)

Background: fix(#721): add "detail" to WF_META_AUTO_SKIP — WF-META sessions no longer invoke make-detail-plan

Changes: fix(#721): add "detail" to WF_META_AUTO_SKIP in bin/workflow/next-step — WF-META sessions were invoking make-detail-plan because "detail" was absent from the auto-skip set (PR #1090 omitted it). Added "detail" as the 9th member of WF_META_AUTO_SKIP. Added regression test Case 30 to tests/bin-workflow-next-step.sh confirming the oracle routes to pre_final_report_gate when outline is complete and detail is pending. Added one-line clarifications to workflow-init PM4 and clarify-intent CI-3a noting that detail is auto-skipped in WF-META. <!-- compose-doc-append-sentinel: branch=fix/721-detail-skip pr=#1106 -->

### FEATURE: PR #1111 — feat/1094-evidence-resolver (2026-06-26, ae6c8e9d32ca79528bb8e033c5dc43d08905fc64, #1111)

Background: fix(#1094,#1064): evidence-based step completion for clarify_intent and docs — gate and oracle auto-repair

Changes: fix(#1094): `hooks/lib/workflow-state/evidence-resolver.js` — new SSOT module that owns the step→evidence predicate mapping. `hasCompletionEvidence(step, sessionId, opts)` returns true when (a) `clarify_intent`: `<PLANS_DIR>/<sessionId>-intent.md` exists; (b) `docs`: staged docs changes in any linked worktree (`hasStagedDocChanges`) OR `WORKTREE_NOTES.md` doc bullets present (`hasWorktreeNotesDocEvidence`). `workflow-gate.js` EARLY_GATE Tier 2 now calls this before blocking — if clarify_intent evidence found, auto-repairs state via `markStep` and clears the gate so Edit/Write proceed immediately. `bin/workflow/next-step` oracle likewise calls `hasCompletionEvidence("clarify_intent")` before returning `pending`, then auto-repairs and re-runs verdict (max 1 recurse). `assertValidSessionId()` added to `state-io.js` as SSOT path-traversal defense (`/^[A-Za-z0-9_-]+$/`); `getStatePath` throws on invalid; `hasCompletionEvidence` fails-open (returns false). `bin/workflow/reconcile-state` added: manual recovery CLI that reads state, checks evidence for clarify_intent and docs, and marks pending→complete when evidence found. `hooks/workflow-mark/enforce-override-handlers.js` WORKFLOW_ON handler emits a reconcile hint when any of these steps is stale after WORKFLOW_ON restore.;fix(#1064): `bin/workflow/next-step` oracle now checks docs evidence before returning `docs=pending`. When `hasStagedDocChanges` (staged changes under `docs/` or any `.md`) or `hasWorktreeNotesDocEvidence` (non-`(none)` bullet in `WORKTREE_NOTES.md ## History Notes / ## Changelog Notes`) is satisfied in any linked worktree, oracle auto-repairs state to `complete` and recurses (max 1×). Closes the gap where oracle kept instructing `/update-docs` re-runs despite staged doc changes being present. Incident reference: session fa2b4c00 / #1053 required manual `state.docs.status='complete'` write. `tests/bin-workflow-next-step.sh` and `tests/main-workflow-evidence.sh` split into `<name>/` dispatcher pattern (exceeded 500-line HARD file-split limit after additions). <!-- compose-doc-append-sentinel: branch=feat/1094-evidence-resolver pr=#1111 -->

### FEATURE: PR #1110 — feature/block-memory-1097 (2026-06-26, 8e22c3efbfd0fb88ab0809cddb1831eb6bb2b609, #1110)

Background: feat(#1097): add block-memory-direct.js PreToolUse hook to intercept agents-repo memory writes

Changes: feat(#1097): New `hooks/block-memory-direct.js` PreToolUse hook intercepts Write/Edit/MultiEdit/editFiles and Bash shell-write-redirects (redirect, tee, PowerShell cmdlets, cp/mv) targeting the memory directory (~/.claude/projects/c--git-agents/memory/). Unconditional block with a prompt asking the user to choose: create a GitHub issue via /issue-create (recommended), allow the write via a one-shot marker file ($PLANS_DIR/<sid>.memory-write-allow.tmp), or cancel. WORKFLOW_OFF bypasses the block. Fail-open on JSON parse error; fail-closed when session ID is unresolvable or marker deletion fails (EISDIR/EACCES). Settings.json entry added with matcher Edit|Write|MultiEdit|editFiles|Bash|runInTerminal|runCommands. Removed dead prose guidance from docs/agents-repo-dev.md ("Behavioral Issues Go to GitHub Issues, Not Memory") and deleted memory/feedback_nirecom_issues_over_memory.md — policy now enforced mechanically. <!-- compose-doc-append-sentinel: branch=feature/block-memory-1097 pr=#1110 -->

### FEATURE: PR #1118 — fix/subagent-sentinel-guard (2026-06-26, 3255312, #1118)

Background: fix(#1103): block subagent sentinel pollution via PreToolUse hook + workflow-mark backstop

Changes: BUGFIX #1103: subagents can no longer drive the workflow state machine via shared session ID. Added hooks/lib/subagent-detect.js (isSubagentCall predicate based on empirically-confirmed agent_id presence), hooks/block-subagent-sentinels.js (PreToolUse: blocks WORKFLOW sentinel echoes from subagents before ask dialogs materialize), and a PostToolUse backstop in hooks/workflow-mark.js (subagent calls skip sentinel dispatch while push/merge reset detection still runs). Registered block-subagent-sentinels.js in settings.json before workflow-gate.js. Two L2 test files cover 29 cases: 21 for the PreToolUse hook (8 predicate + 13 decision; covers variable-expansion and comment false-positive, malicious agent_id security case) and 8 for the PostToolUse backstop (C1 regression guard for push/squash/rebase merge; non-zero exit-code). <!-- compose-doc-append-sentinel: branch=fix/subagent-sentinel-guard pr=#1118 -->

### FEATURE: PR #1116 — fix/1105-run-tests-segment (2026-06-26, 0358a6fdd0e24761f298433c3041a99fb8b4528c, #1116)

Background: fix(#1105,#703): per-segment quote-aware test-command detection + harness path

Changes: #1105: workflow-run-tests.js — GIT_NON_EXEC_RE now recognizes the `git -C <path>` prefix form, and isTestCommand applies exclusion/detection per shell segment via a new quote-aware splitTopLevelSegments (splits on top-level &&, ||, |, ;, newline). Compound or diagnostic commands that merely reference a tests/ path no longer mark run_tests complete prematurely. Dead firstToken variable removed. Background: exclusion regexes checked only the first token while TEST_PATH_RE matched anywhere, so `git -C ... add tests/` and `node x && wc -l tests/foo` produced false positives that tripped oracle "inconsistent state" aborts. Test gap: no per-segment or git -C coverage existed; added ED8 expectation flip plus ED9-ED15 (bare/quoted git -C, &&/||/| compound, quote-aware no-split-in-quotes).;#703: tests/main-workflow-run-tests.sh — fixed stale hook path (dropped the claude-global/ prefix) so the eight state-expecting cases (N1/N2/N3/E1/E1b/E2/I1/I2) actually invoke the hook. Background: the harness referenced a pre-move path that no longer exists, so node silently failed and no state was written. Test gap: the harness path itself was the defect; fix validated by the eight revived cases turning green. <!-- compose-doc-append-sentinel: branch=fix/1105-run-tests-segment pr=#1116 -->

### FEATURE: PR #1125 — feature/1114-write-tests (2026-06-26, 74c35ca, #1125)

Background: fix(#1114): surface per-finding detail in Final Report Supervisor Findings section (#1125)

Changes: Added `### Supervisor Findings` section to `final-report-schema.js` SECTIONS and `renderSkeleton()` placeholders, with placeholder `<SUPERVISOR_FINDINGS_DETAIL>`;Extended `formatLayer2Findings()` in `supervisor-findings-render.js` with `forFinalReport` option: when true, applies ~120-char truncation with newline normalization and `<`→`‹` (U+2039) escaping to prevent guard false-blocks; reporter field added to per-finding output in both modes;Added `<SUPERVISOR_FINDINGS_DETAIL>` substitution rule to SC-6 in `skills/session-close/SKILL.md` calling `formatLayer2Findings({ forFinalReport: true })`; updated heading count 12→13;Updated stale guard comment counts ("10 section headings"→"13") in `stop-final-report-guard.js`;Fixed pre-existing stale test fixtures: `full_canonical_report_text()` in helpers.sh now includes all 13 headings; G7/G17/G18/G19/G26 inline fixtures updated; K1/K3 assertions updated; guard comment counts updated <!-- compose-doc-append-sentinel: branch=feature/1114-write-tests pr=#1125 -->

### FEATURE: PR #1126 — fix/fix-1095 (2026-06-26, 0fbe4e931ff37454da9db403d59da5961d27e28c, #1126)

Background: fix(#1095,#1024,#982,#286,#234): enforce-worktree false-positive fixes + migration cleanup

Changes: #1095: fix git-merge WRITE_PATTERN (bash-write-patterns.js): `merge(?!-)` was too broad — excluded all merge-* subcommands including git merge-file, which writes back to its first argument; narrowed to `merge(?!-base\b|-tree\b)` (excludes only read-only plumbing: merge-base and merge-tree). rm extractor's tokenizeRmArgs now resolves simple $VAR/$VAR/literal forms from process.env; fails closed on complex forms ($VAR expansion, command substitution). ff-only dispatch confirmed correct (no change).;#1024: fix git-stash-write WRITE_PATTERN (bash-write-patterns.js): `drop` and `clear` were classified as write but are ref-only operations (no working-tree mutation); they were blocked during active parallel work because isAllowedMainWorktreeCleanup allows stash from main only when no linked worktree remains. Regex narrowed to `stash\s+(?:push|pop|apply)` — drop/clear now classify as read; push/pop/apply (working-tree writes) remain gated.;#982: fix isAllowedWorktreeCommand (standard.js): sanctioned worktree lifecycle commands chained with `&&cd` were blocked by hasShellChaining before the allow predicate could match. Fixed by splitting at the first unquoted `&&` before chaining guards; tail validated as bare `cd` with no shell-meta ($VAR, backtick, semicolon, further chaining).;#838: fix git -C <main-path> worktree remove/prune from linked worktree blocked. Resolved transitively by the #982 &&cd fix — the -C form now correctly reaches the mainCheckout!==false block in isAllowedWorktreeCommand.;#286: fix findRepoRootForBash (git-repo-detection.js): used process.cwd() as the fallback start directory, so subagents launched from a linked worktree (inheriting main-worktree CWD from the hook process) misidentified their repo root and were blocked. Added toolCwd parameter (sourced from input.cwd in enforce-worktree.js) as fallback before process.cwd().;#234: remove AGENT_AUTO_BRANCH / AGENT_DEFAULT_BRANCHES migration shims from hooks/enforce-worktree/config.js (isEnforceWorktreeOn, getProtectedBranches) and both BEGIN/END migration blocks from hooks/pre-commit. All machines confirmed migrated to the ENFORCE_WORKTREE / DEFAULT_BRANCHES naming. <!-- compose-doc-append-sentinel: branch=fix/fix-1095 pr=#1126 -->

### FEATURE: PR #1121 — fix/299-null-title (2026-06-26, 121339a72aaba6a9edbf583cd8ea3caae27aa568, #1121)

Background: fix(#299): write ⏳ sentinel for untitled sessions; let issue # override sentinel

Changes: Fix #299 null-title sentinel: writeWaiting now writes "⏳" sentinel when no prior title exists; writeClearWaiting unsets it with ""; writeSetIssue skip guard allows overwriting sentinel so issue # appears after workflow-init even when a prompt was submitted first <!-- compose-doc-append-sentinel: branch=fix/299-null-title pr=#1121 -->

### FEATURE: PR #1130 — fix/fix-write-tests-evidence (2026-06-26, c12c2295ba5c7ef6d57aa9ebd1d87aec71c105d3, #1130)

Background: fix(#1107,#1085,#1083): write_tests evidence auto-repair, abort hint cleanup, RESET_FROM error

Changes: #1107: oracle write_tests evidence-based auto-repair — when staged test files exist and write_tests is pending, the oracle now auto-repairs write_tests to complete and recurses (guarded by _didAutoRepair). Placement is BEFORE the inconsistency scan so that "review_tests/run_tests complete while write_tests pending" is resolved rather than aborted. evidence-resolver.js gains a write_tests case using hasStagedTestChanges; reconcile-state EVIDENCE_STEPS extended to include write_tests.;#703: tests/main-workflow-run-tests.sh comment-only update — test failures were resolved in PR #1116 (fix #1105,#703 per-segment quote-aware detection); this session confirms the fix and updates inline documentation.;#1085: oracle abort NEXT_HINT no longer exposes the WORKFLOW_RESET_FROM_ recipe — removed the 3-bracket-form echo from the stale-state hint; recovery guidance now reads "/workflow-init" only, preventing user reliance on the unsafe triple-bracket form.;#1083: RESET_FROM unknown step name produces actionable error — reset-handler.js now emits "ERROR — unknown step X for RESET_FROM; state NOT changed. Valid steps: <list>" instead of the previous silent "ignored" message. <!-- compose-doc-append-sentinel: branch=fix/fix-write-tests-evidence pr=#1130 -->

### FEATURE: Early WIP claim, companion WIP/parent filter, primary candidate filter (#1117,#1081,#1005) (2026-06-26, d7b0584)

Background: Three related issues: #1117 (wip-set-resume NEEDS_CLARIFY branch exited before claiming WIP, leaving session unclaimed on resume), #1081 (find-companion-issues.sh included parent-of-primary and WIP-claimed-by-other issues as candidates, causing invalid companion suggestions), #1005 (WI-3 had no filter for >=2 primary candidates, leading to ambiguous workflow-init target selection).

Changes: wip-set-resume.sh now early-claims WIP before exit 1 in the NEEDS_CLARIFY branch so the session is claimed on resume; find-companion-issues.sh filters out parent-of-primary and WIP-claimed-by-other candidates; new filter-primary-candidates.sh script handles the WI-3 >=2 candidate case; clarify-intent CI-2b calls wip-set-single on companion accept.

### BUGFIX: PR #1136 — fix/1123-vscode-open (2026-06-27, 7397512, #1136)

Background: fix(#1123): exclude extensionHost from isVsCode() CLAUDE_CODE_ENTRYPOINT clause

Changes: Narrowed isVsCode() CLAUDE_CODE_ENTRYPOINT clause to exclude VSCODE_CRASH_REPORTER_PROCESS_TYPE=extensionHost; added T3b test; documented exclusion in docs/architecture/claude-code/settings.md <!-- compose-doc-append-sentinel: branch=fix/1123-vscode-open pr=#1136 -->

### FEATURE: PR #1143 — fix/1138-cross-repo (2026-06-27, dd2f4a815070e903d654fcb6569614ae0b811808, #1143)

Background: fix(#1138,#1112): cross-repo bypass and cleanup exemption in workflow-gate

Changes: #1138: workflow-gate.js cross-repo bypass — `git -C <path> commit` targeting a repo other than the agents session repo now approves without checking agents workflow state. Identity check uses `git rev-parse --git-common-dir` comparison via new `isAgentsSessionRepo()` in `hooks/workflow-gate/repo-resolution.js`. Fail-closed: any git error or missing path enforces rather than bypasses. Fixes spurious blocks when Claude commits to ai-specs or other companion repos during an agents session.;#1112: workflow-gate.js cleanup exemption — `cleanup` step is now skipped by the gate in linked-worktree context (mirrors the existing `user_verification` exemption). Cleanup is deferred to the `/worktree-end` boundary; blocking intermediate worktree commits on a pending `cleanup` step caused spurious blocks during normal session work. Main-worktree context (ENFORCE_WORKTREE=off) still enforces cleanup. <!-- compose-doc-append-sentinel: branch=fix/1138-cross-repo pr=#1143 -->

### FEATURE: PR #1140 — fix/927-inline-skill-re (2026-06-27, 8340bd3e9e28daab67fccbeb2c02e2530cef1f5b, #1140)

Background: fix(#927): replace ISSUE_CLOSE_SKILL=1 inline form with close-completed.sh subprocess in ICF-H

Changes: #927 BUGFIX: enforce-issue-close bypass — replace broken `ISSUE_CLOSE_SKILL=1` inline form with `close-completed.sh` subprocess. Background: ICF-H in `issue-close-finalize-worker.md` used `ISSUE_CLOSE_SKILL=1 gh issue close`, which cannot reach the hook's `process.env` (set at session-launch time; Bash-tool subprocess env prefixes are invisible to the Node.js hook process). All ICF-H calls were silently blocked. Root-cause confirmed by `process.env` scope analysis — every Bash tool invocation is an independent shell. Changes: new `bin/github-issues/close-completed.sh` (follows `close-not-planned.sh` subprocess-invisible pattern; accepts optional `--repo OWNER/REPO`); ICF-H in worker updated to call it with `--repo "$owner_repo"`; `enforce-issue-close.js` header comment corrected; `rules/github-issues.md` bypass documentation updated (removed "Sole sanctioned bypass form: env export" — env export cannot reach hook process). <!-- compose-doc-append-sentinel: branch=fix/927-inline-skill-re pr=#1140 -->

### FEATURE: PR #1144 — fix/fix-1137 (2026-06-27, 995e9f08d89a9c45d1c277c7059df55aa1e85958, #1144)

Background: fix(#1137): prohibit caller re-summary after deep-research DR-3 output

Changes: Background: #1137: /deep-research DR-3 presented findings to the user, but the calling orchestrator also re-narrated the same findings immediately after the Skill tool returned — producing visible double output.;Changes: Added caller-prohibition directive to DR-3 in skills/deep-research/SKILL.md: "The caller must not re-summarize or paraphrase these findings — DR-3 output is the complete user-facing surface." <!-- compose-doc-append-sentinel: branch=fix/fix-1137 pr=#1144 -->

### FEATURE: PR #1157 — fix/fix-enforce-worktree-false-blocks (2026-06-27, 34e28b6f7fb26a2d20245719af0f0a9a973ee005, #1157)

Background: fix(#1115,#982,#923,#838,#959): enforce-worktree false-block fixes — fd-dup redirect chaining misread, -C flag validation, worker script paths

Changes: issue:#1115 category:BUGFIX subject:"fix(enforce-worktree): POSIX fd-dup redirects (2>&1, N>&1, N>&-) false-block sanctioned git commands via hasShellChaining" background:"hasShellChaining() and rejectInterpreterAndChaining() in shared-cmd-utils.js used the character class [|;&] which matches a lone & character. POSIX fd-dup redirects (2>&1, >&1, N>&-) contain an unquoted & that is not a chaining operator, causing sanctioned git commands with trailing redirects (e.g. git worktree add <path> <branch> 2>&1) to be false-blocked." changes:"Added /\d*>&\d+|\d*>&-/g fd-dup stripping in hasShellChaining() and rejectInterpreterAndChaining() before the chaining character test. &> and &>> redirect-both forms are intentionally not stripped so write targets remain visible to collectBashWriteTargets. New test cases in tests/fix-enforce-worktree-merge-gate.sh and tests/fix-595-bypass-e2e.sh.";issue:#982 category:BUGFIX subject:"fix(enforce-worktree): isAllowedWorktreeCommand unconditional-allow path blocked by hasShellChaining fd-dup false positive (recurrence of #881)" background:"The unconditional-allow path in isAllowedWorktreeCommand for git worktree remove/prune/add was guarded by hasShellChaining(). The [|;&] character class in hasShellChaining() matched lone & in POSIX fd-dup redirects, blocking sanctioned worktree lifecycle commands when the command string contained a redirect like 2>&1." changes:"Covered by the same hasShellChaining/rejectInterpreterAndChaining fd-dup stripping fix as #1115 (shared-cmd-utils.js). No additional files changed specifically for #982.";issue:#923 category:BUGFIX subject:"fix(enforce-worktree): git -C <path> worktree remove/prune false-blocked from main-wt CWD — isAllowedWorktreeCommand did not recognize the -C flag form" background:"isAllowedWorktreeCommand() matched git worktree subcommands by position assuming git was the first token. When git -C <path> worktree remove was issued from the main-worktree CWD, the -C flag shifted the subcommand position and the regex did not match, causing the predicate to return false and block the command." changes:"Added -C path validation to isAllowedWorktreeCommand() in main-worktree-allows/standard.js: rejects multiple -C flags; when -C is present, the argument must resolve case-insensitively to repoRoot. New test cases T923-3 through T923-7 in tests/fix-923-enforce-worktree-c-flag-main-worktree.sh and E-WT-CFLAG cases in tests/fix-595-bypass-e2e.sh.";issue:#838 category:BUGFIX subject:"fix(enforce-worktree): git -C <main-worktree> worktree remove/prune blocked from linked-wt CWD — -C form not recognized by isAllowedWorktreeCommand" background:"When a session ran git -C <main-worktree-path> worktree remove from a linked worktree CWD, isAllowedWorktreeCommand did not validate the -C argument and failed-closed on -C forms it did not recognize, blocking the sanctioned worktree lifecycle command." changes:"Covered by the same isAllowedWorktreeCommand -C validation fix as #923 (main-worktree-allows/standard.js). The -C path is resolved and compared to repoRoot case-insensitively. Tests in tests/fix-923-enforce-worktree-c-flag-main-worktree.sh.";issue:#959 category:BUGFIX subject:"fix(enforce-worktree): sanctioned worker scripts with linked-wt path arguments false-blocked from main-wt CWD — add isAllowedWorkerScriptInvocation predicate" background:"Sanctioned worker scripts (issue-close-stage-worker, commit-push-worker etc.) are launched via bash \"<AGENTS_CONFIG_DIR>/bin/<script>\" with linked-worktree absolute paths as log-redirect targets. When called from the main-worktree CWD, the enforce-worktree main-worktree write guard treated the linked-wt path arguments as main-wt write targets and blocked the invocation." changes:"Added isAllowedWorkerScriptInvocation(cmd, repoRoot) predicate to hooks/enforce-worktree/main-worktree-allows/standard.js. Identity gate: cmd must match bash \"<double-quoted-sanctioned-script>\" against a 5-entry SANCTIONED list. argTail gate: rejects chaining operators and bare & background operator (using /&(?!>)/ to distinguish background from redirect forms). Write target gate: all targets must resolve inside registered linked worktrees (git -C repoRoot worktree list --porcelain); fail-closed on any parse failure or out-of-registry path. Wired into enforce-worktree.js main-worktree allow chain. New tests in tests/fix-959-enforce-worktree-worker-path-arg.sh (8 cases including a security pin for bare & backgrounding)." <!-- compose-doc-append-sentinel: branch=fix/fix-enforce-worktree-false-blocks pr=#1157 -->

### FEATURE: PR #1159 — fix/1120-c2-session-scope (2026-06-27, 987ab6b, #1159)
Background: fix(#1120): return null from resolveWorkflowSessionId when no ccBucket=0 owner identified among same-day candidates
Changes: #1120: resolveWorkflowSessionId() Priority 4 null-return gate — when multiple same-day session candidates exist but none has ccBucket=0 (no context.md contains the CC UUID), returns null instead of guessing by depth/mtime. Callers handle null as UNAVAILABLE, preventing supervisor C2 from reporting findings for unrelated parallel sessions. supervisor-audit.md gained a symmetric UNAVAILABLE fallback section. Tests: R22 added (gate stays dormant when ccBucket=0 present); G49 updated to expect rc=0 (null path, guard uses CC UUID, no state found). <!-- compose-doc-append-sentinel: branch=fix/1120-c2-session-scope pr=#1159 -->

### FEATURE: PR #1158 — feature/issue-485 (2026-06-27, 117382ae0271b72b9e2d142ad36009852b3c9dbc, #1158)
Background: feat(#485): advisory plan-skip hints (SKIP_HINT) at outline/detail for trivial changes
Changes: FEATURE (#485): next-step oracle now emits an optional advisory `SKIP_HINT` line (`WORKFLOW_OUTLINE_NOT_NEEDED` / `WORKFLOW_DETAIL_NOT_NEEDED`) at the outline/detail steps when `intent.md` reads as trivial, surfacing a plan-skip suggestion the model may act on (ask-gated) or ignore. Triviality is judged by a new read-only, fail-closed `hooks/lib/workflow-state/skip-signal-resolver.js` (`isTrivial`: mechanical-change keyword present, no broad-change / new-API-surface signal); `parse-oracle-output.js` accepts the optional key (4-line contract preserved). The originally-scoped second goal — skipping the security-scanner for changes that "don't touch an input boundary" — was **dropped by explicit user decision** after analysis showed it has no sound, value-adding form: any sound form (skip only provably-safe diffs) is redundant with the existing docs-only short-circuit, and any value-adding form (skip on absence of a known-dangerous pattern, or skip test-only changes) reintroduces security false-negatives (an unrecognized sink, or a commit that erodes a security test, would silently skip the scan). The review-tests-runs-in-main-worktree finding surfaced during this work was folded into #882. <!-- compose-doc-append-sentinel: branch=feature/issue-485 pr=#1158 -->

### FEATURE: PR #1165 — fix/fix-run-tests-guard (2026-06-27, 225d77f, #1165)
Background: fix(#1139,#1133): guard run_tests hook against write-tests validation runs; add oracle --reset recovery
Changes: closes #1139: hooks/workflow-run-tests.js prematurely marked run_tests complete when write-tests subagent ran tests for validation; added write_tests completion guard in the exit-0 branch (fail-open when state absent);closes #1133: context compaction left run_tests=complete with write_tests=pending; added --reset <step> flag to bin/workflow/next-step and a scoped oracle abort hint pointing at the recovery command for this specific pattern <!-- compose-doc-append-sentinel: branch=fix/fix-run-tests-guard pr=#1165 -->

### FEATURE: PR #1167 — fix/fix-299-session-title (2026-06-27, e4dd61480dee390f9d7e2a8cc2adad011ebaadbc, #1167)
Background: fix(#299): clarify-intent Path B set-issue + remove dead ⏳ code
Changes: #299: session title Path B fix + dead ⏳ code removal — `clarify-intent` Completion now calls `cc-session-title set-issue` at CI-C1a (after intent.md is written), so Path B sessions (where `clarify-intent` runs instead of `workflow-init`) rewrite the VS Code session tab title to `#N <issue title>` at the same point Path A does. This closes Bug 1 (issue number missing in Path B) and Bug 2 (first rewrite never happened in Path B), and transitively Bug 3 (⏳ sentinel persisted because `set-issue` never ran in Path B — its existing skip guard already overwrites the ⏳ form). Dead ⏳ lifecycle code removed: `writeWaiting`/`writeClearWaiting` functions deleted from `hooks/lib/session-title.js` and `bin/cc-session-title`; three orphan hook files (`stop-session-title-waiting.js`, `user-prompt-clear-waiting.js`, `pre-askuserquestion-clear-waiting.js`) deleted (they were removed from `settings.json` in PR #1142 but left on disk). NON_GITHUB / empty-closes-issues guard respected by CI-C1a — call is suppressed when no issue number is available. <!-- compose-doc-append-sentinel: branch=fix/fix-299-session-title pr=#1167 -->

### FEATURE: PR #1170 — fix/1024-stash-pop-test (2026-06-27, 9399667688506ce3e589e916de6411c5aec678b9, #1170)
Background: fix(#1024): add missing stash pop test assertion and correct WE-20 doc note
Changes: Add missing test assertion (S27b) for `WORKTREE_END_SKILL=1 git stash pop` at wtCount=2 → allow, symmetric to existing S27 (`stash push`); correct WE-20 note in cleanup-cascade.sh:28 from push-only to push/pop/drop shapes (#1024) <!-- compose-doc-append-sentinel: branch=fix/1024-stash-pop-test pr=#1170 -->

### FEATURE: PR #1171 — fix/c3-guard-fix (2026-06-27, 3fc8e11895420989115abf369c73c9cd5e0858d4, #1171)
Background: fix(supervisor-guard): symmetric C3 done/frozen guard + scope detectOffProposal to Bash tool_use
Changes: #1163: supervisor-guard.js C3 branch now skips block when alert_phase=done/frozen — added alertPhase !== "done" && alertPhase !== "frozen" entry guard symmetric with branches (2) and (3); previously writeAlertState(done→pending) silently returned false (validated against supervisor-state-writer.js:148 done→pending block) but block was issued unconditionally regardless of writer result, dropping all supervisor adjudication in rounds 2+ of a session.;#1162: detectOffProposal now scans only Bash tool_use items (item.name === "Bash") — removed assistant-prose text substring scan branch; when supervisor prose mentioned bypass keywords (e.g. explaining <<WORKFLOW_ENFORCE_WORKTREE_OFF>>), the text scan triggered C3 false-positives in subsequent rounds creating a self-reinforcing loop; real bypass sentinels are emitted exclusively from Bash tool_use echo commands which the anchored regex already matches correctly. <!-- compose-doc-append-sentinel: branch=fix/c3-guard-fix pr=#1171 -->

### FEATURE: PR #1173 — feature/issue-1155 (2026-06-27, 8998d38, #1173)
Background: feat(#1155): bulk-sub-of verdict for /issue-create + --skip-survey flag + WF-META PM4 mandate
Changes: FEATURE #1155: bulk-sub-of verdict in issue-create-dispatch + --skip-survey flag + workflow-init PM4 bulk-create mandate — added `bulk-sub-of` to `issue-create-dispatch.sh` (TSV manifest loop: create+attach each row, N URLs stdout, partial-failure recovery with orphaned-URL capture and retry hint); added `--skip-survey` to issue-create SKILL.md Phase 2 for pre-deduped callers; added PM4 bulk sub-issue creation mandate to workflow-init SKILL.md PM path; stdout contract remains backward-compatible (single-verdict callers use tail -n 1, bulk callers loop all trailing URL lines) <!-- compose-doc-append-sentinel: branch=feature/issue-1155 pr=#1173 -->

### FEATURE: PR #1178 — fix/issue-create-artifact-dir (2026-06-27, 9be2f7e85a914c787ed1b7a12adf09cdfb68ebb0, #1178)
Background: fix(#1108): unify artifact_dir to PLANS_DIR across issue-create, issue-reconcile, worktree-start, session-close
Changes: fix(#1108): unified `artifact_dir` resolution to `PLANS_DIR` across four skills (`/issue-create`, `/issue-reconcile`, `/worktree-start`, `/session-close`) and their worker agents; replaced `$AGENTS_CONFIG_DIR/artifacts/ or a temp dir` pattern with a direct `bin/workflow-plans-dir` call at each callsite so write and read sides always agree on the same deterministic path <!-- compose-doc-append-sentinel: branch=fix/issue-create-artifact-dir pr=#1178 -->

### FEATURE: PR #1179 — fix/fix-1109-heredoc-plans-dir (2026-06-27, c1182cf93a1a05570d3c898bc7d6e89afb5f1e19, #1179)
Background: fix(#1109,#983,#1025,#1040): allow heredoc and env-var-expanded writes to WORKFLOW_PLANS_DIR
Changes: fix(#1109): enforce-worktree false-blocked heredoc writes targeting WORKFLOW_PLANS_DIR when the heredoc body contained `;`/`&&`/`||`. Root cause: `hasCommandSequencing` tested the raw command including heredoc body text. Fix: new `hasCommandSequencingOutsideHeredoc()` helper strips the heredoc body before the sequencing test; parallel plans-dir allow path in `enforce-worktree.js` and `universal-target-allow.js` fires when sequencing is body-only and all redirect targets resolve under plans-dir.;fix(#983): `areAllBashTargetsUnderPlansDir` `isUnder` closure resolved targets via `path.resolve` without first expanding shell tokens, so `$HOME/.workflow-plans/foo`, `$WORKFLOW_PLANS_DIR/foo`, and `~/x` were unresolvable and fail-closed to block. Fix: call `expandStaticShellTokens` in `isUnder` before `path.resolve`; export `expandStaticShellTokens` from `redirect.js` for shared use.;fix(#1025): `extractCpMvDestination` returned null for `mv "$PLANS_DIR/foo.tmp" "$PLANS_DIR/foo"` because env-prefix substitution left `$PLANS_DIR` unresolved and the old code failed closed. Fix: added `tryResolveEnvUnderPlansDir` fallback in `cp-mv.js` (mirrors the existing `tee.js` pattern); destination now resolves correctly when the env var value and final path are both under plans-dir.;fix(#1040): same root cause as #983 — CI-C0 tracking-issue guard used `echo ... > "$COUNTER_FILE"` where `$COUNTER_FILE` pointed under `~/.workflow-plans/`; the unresolved variable was fail-closed to block. Fixed transitively by the `isUnder` `expandStaticShellTokens` expansion in bash-write-scope.js (shared fix with #983). <!-- compose-doc-append-sentinel: branch=fix/fix-1109-heredoc-plans-dir pr=#1179 -->

### FEATURE: PR #1183 — fix/fix-1176 (2026-06-27, d5eb37a8940d281bc63aa9bb9b911433243c81de, #1183)
Background: fix(#1176): restore VS Code auto-open + add clickable vscode:// breadcrumb links in plan confirmation flow
Changes: fix(#1176): restore VS Code auto-open and add clickable vscode:// breadcrumb links for plan confirmation flow — PR #1136 (7397512) added an extensionHost guard to isVsCode() that over-excluded extension-host users; guard removed, toVsCodeFileUri() added, show-plan-link.js and confirm-checkpoint.js updated to emit [filename](vscode://file/...) markdown links in systemMessages under VS Code; SHOW_PLAN_LINK_NO_AUTO_OPEN=1 remains the opt-out for all users <!-- compose-doc-append-sentinel: branch=fix/fix-1176 pr=#1183 -->

### BUGFIX: PR #1136 — fix/1123-vscode-open (2026-06-27, 7397512, #1136)
Background: fix(#1123): exclude extensionHost from isVsCode() CLAUDE_CODE_ENTRYPOINT clause
Changes: Narrowed isVsCode() CLAUDE_CODE_ENTRYPOINT clause to exclude VSCODE_CRASH_REPORTER_PROCESS_TYPE=extensionHost; added T3b test; documented exclusion in docs/architecture/claude-code/settings.md <!-- compose-doc-append-sentinel: branch=fix/1123-vscode-open pr=#1136 -->

### BUGFIX: #299: VS Code extension temp form ⏳<ai-title> blocked issue # title (2026-06-27, 2804337)
Background: PR #1121 wrote a bare ⏳ sentinel to mark 'no title yet'. The VS Code extension rewrites ⏳ to ⏳<ai-title> (no space). The prior writeSetIssue skip guard (existing !== '⏳') treated this as a real title and skipped the issue write, so session titles were never shown.
Changes: session-title.js: skip guard widened with /^⏳\S/ to also overwrite the extension's temp form; session-start.js: writeClearWaiting call removed (⏳ lifecycle retired — UserPromptSubmit fires after submission, making the clear-waiting pattern impossible); settings.json: 3 hooks unregistered (stop-session-title-waiting.js, user-prompt-clear-waiting.js, pre-askuserquestion-clear-waiting.js); tests: T18a/T18b added for real session-start.js execution, T12-T17 removed (retired lifecycle), T26/T28/T29 cover skip guard edge cases.

### FEATURE: PR #1182 — fix/1099-resolve-session-id-p7-guard (2026-06-28, 975411c4866f260e8f8f507ba2f666f393730cad, #1182)
Background: fix(#1099): guard resolveSessionId P7 against cross-repo CWD
Changes: #1099: resolveSessionId() Priority 7 (JSONL mtime scan) now guards each CWD-derived candidate (CLAUDE_PROJECT_DIR, process.cwd(), realpath) with a git-common-dir cross-repo check, so a foreign-repo working directory no longer returns another session's id. Background: in multi-repo concurrent sessions, P7's last-resort fallback could resolve to the most-recently-active OTHER session. Added shared helper hooks/lib/git-common-dir.js (getGitCommonDir / isSameGitRepo; fail-open when git is unavailable, preserving headless/CI behavior) and migrated hooks/workflow-gate/repo-resolution.js onto it (removed the inline duplicate). Scope deliberately limited to P7 safety hardening; sibling resolvers and P1-P6 left unchanged (Accepted Tradeoffs). Tests: JS-11/JS-12 cross-repo block, JS-13/JS-14 same-repo + linked-worktree positive, JS-17 AGENTS_CONFIG_DIR-unset fallback anchor; the oversized test file was split into tests/feature-resolve-session-id-js/section-*.sh to satisfy the 500-line limit. <!-- compose-doc-append-sentinel: branch=fix/1099-resolve-session-id-p7-guard pr=#1182 -->

### FEATURE: PR #1189 — fix/issue-1181 (2026-06-28, 99c2558, #1189)
Background: fix(#1181): guard WI-8 against WF-META when meta issue has open sub-issues (#1189)
Changes: BUGFIX #1181: guard WI-8 against WF-META when meta issue already has open sub-issues — new helper `skills/workflow-init/scripts/list-open-sub-issues.sh` queries the REST sub_issues endpoint (`repos/$REPO/issues/$N/sub_issues --paginate`, matching `parent-all-closed-check.sh` sibling pattern) with validated `<owner/repo>` and `<N>` args. WI-8 now loops over `ISSUES[@]` and, when any meta issue has open sub-issues, presents them via `AskUserQuestion`; the selected sub-issue replaces `ISSUES` and falls through to standard Path A/B routing. NO_OPEN for all issues proceeds to WF-META (PM1–PM5) unchanged. Security fix in same PR: initial write-code pass used a GraphQL query with hardcoded `owner: "placeholder"`, causing the guard to silently fail open; switched to REST with properly passed owner/repo arg before any test run. Background: PR #1173 introduced the PM4 bulk-sub-of mandate; without this guard, specifying an existing meta issue with open sub-issues sent the session into WF-META and PM4 attempted duplicate sub-issue creation. <!-- compose-doc-append-sentinel: branch=fix/issue-1181 pr=#1189 -->

### FEATURE: PR #1194 — feature/1100-1101-cross-repo-wire-format (2026-06-28, a29e8025bbc98804d5ee86c108442a7ed1533aee, #1194)
Background: feat(#1100,#1101): cross-repo wire format + parse-closes-issues.js extension + close flow --repo support
Changes: #1100 FEATURE: cross-repo wire format + parse-closes-issues.js extension — extended `hooks/lib/parse-closes-issues.js collectNums` to accept three forms (`#N`, `repo#N`, `owner/repo#N`); changed return type from `Number[]` to `Array<{number: number, repo?: string}>`; updated all consumers: `bin/parse-closes-issues` CLI, `check-closes-issues-nonempty.sh` inline node-e, `issue-close-write-outcome.js --fallback`; extended `clarify-intent` CI-4 `## Issues` schema and `workflow-init` WI-3 regex to detect cross-repo forms; backward-compatible (`repo` field is optional, `#N` entries continue to work);#1101 FEATURE: cross-repo close flow — added `--repo <repo|owner/repo>` flag to `find-pr-by-marker.sh`, `issue-state-check.sh`, `wip-set-single.sh`, `ensure-board-card.sh`; short-form `repo` auto-resolved to `owner/repo` via `gh repo view` (fail-closed: exit 2 on resolution failure); `issue-close-finalize` ICF-B and `issue-close-stage` worker JSON schema propagate `issue_repo` through the delegation chain; security hardening: `REPO_ARG` validated against `^[A-Za-z0-9_.-]+(/[A-Za-z0-9_.-]+)?$` before any `gh` call to prevent argument injection <!-- compose-doc-append-sentinel: branch=feature/1100-1101-cross-repo-wire-format pr=#1194 -->

### FEATURE: PR #1193 — fix/1192-fail-close (2026-06-28, 1b830232c58f3e70ef8cefffa68837b4066a9899, #1193)
Background: fix(#1192): fail-close stop-final-report-guard when worktree done; SC-6 CONV_LANG
Changes: BUGFIX #1192: stop-final-report-guard line 144 fail-open → fail-close; SC-6 CONV_LANG enforcement | PR #1193 | 1b83023 | 2026-06-28 | background: `stop-final-report-guard.js` line 144 exited 0 (fail-open) when `final-report-env.json` existed but `## Final Report — <sid>` heading was absent from transcript — LLM completion summaries after `/worktree-end` bypassed the guard silently; also `skills/session-close/SKILL.md` SC-6 had no CONV_LANG language directive so Final Report body was always English | changes: replaced `process.exit(0)` at line 144 with gate-file check (`session-close-gate.json` `gate_action:yield` bypasses, otherwise `decision:block` + CONV_LANG suffix in reason); added CONV_LANG body-language directive to SC-6; added G30/G31/G32 tests; updated G3/G20/G23b fixtures with yield-gate <!-- compose-doc-append-sentinel: branch=fix/1192-fail-close pr=#1193 -->

### FEATURE: PR #1186 — fix/1147-t0-bugfix-gate (2026-06-28, #1186)
Background: feat(#1147 T0): BUGFIX session gate — fail-before-fix enforcement
Changes: feat(#1147 T0): BUGFIX session gate — write_tests/review_tests cannot be skipped on fix/* branches; WRITE_TESTS_NOT_NEEDED rejected by workflow-mark D1 defense; review_tests=skipped blocked by workflow-gate D2 defense; getSkippableSteps SSOT excludes both steps for BUGFIX; isBugfixSession/isBugfixBranch module added as detection SSOT; doc-append.py blocks BUGFIX entries to history.md without --test-gap; compose-doc-append-entry and doc-append-worker propagate --test-gap; fail-before-fix policy documented in skills/_shared/test-design.md <!-- compose-doc-append-sentinel: branch=fix/1147-t0-bugfix-gate pr=#1186 -->

### FEATURE: PR #1206 — refactor/supervisor-guard-split (2026-06-28, 96a1256, #1206)
Background: refactor(#1202): split hooks/supervisor-guard.js into supervisor-guard/ sibling folder
Changes: REFACTOR #1202: Split hooks/supervisor-guard.js (528→358 lines) into hooks/supervisor-guard/detect.js (176 lines). Extracted 4 entrypoint-private detection functions (detectSentinelHang, detectAskUserQuestionTurn, parseTranscriptForAudit, detectOffProposal) per rules/coding/file-split.md Pattern A HARD limit. Dispatch file requires ./supervisor-guard/detect; readStdin and tryIncrementFrozen remain inline. Hook registration in settings.json unchanged; no behavior change. <!-- compose-doc-append-sentinel: branch=refactor/supervisor-guard-split pr=#1206 -->

### FEATURE: PR #1211 — fix/1205-session-mtime (2026-06-28, 0c7dcc9, #1211)
Background: fix(#1205): session-sync reset mtime wrong when JSONL tail is metadata-only
Changes: BUGFIX: session-sync reset mtime regression when JSONL tail is metadata-only — `tail -1` returned a no-timestamp line (ai-title/mode/pr-link appended by session-title.js after VS Code restart); bash fell back to `head -1` (session-start, wrong), PS1 silently skipped. Fixed `session-sync.sh` + `session-sync.ps1` to `grep '"timestamp"' | tail -1` scanning all lines. Added `bin/cc-session-mtime` (bash) + `bin/cc-session-mtime.ps1` (PS1) for standalone mtime repair without git sync. Added `tests/cc-session-mtime.sh` (10 cases). `docs/ops.md` repair runbook added. <!-- compose-doc-append-sentinel: branch=fix/1205-session-mtime pr=#1211 -->

### REFACTOR: PR #1217 — refactor/1216-test-split (2026-06-28, d559451)
Background: refactor(#1216): tests/fix-1133-oracle-mark-outline-detail.sh (680 lines) exceeded file-split.md HARD limit (>500). Split required per rules/coding/file-split.md.
Changes: REFACTOR #1216: Split tests/fix-1133-oracle-mark-outline-detail.sh (680 lines) into dispatcher (306 lines: shared setup, helpers, state fixtures, source calls) + 5 section files in tests/fix-1133-oracle-mark-outline-detail/: mark.sh (M1-M6), idempotency-security.sh (I1/S1-S2), auto-repair.sh (A1-A2), hint.sh (H1-H2/B1-B2), reconcile.sh (G1). All 36 tests pass unchanged.

### FEATURE: PR #1213 — fix/1133-oracle-mark (2026-06-29, 4bfa2cd610116ffb7ba984a989b8b668799642b5, #1213)
Background: fix(#1133): oracle --mark CLI + outline/detail evidence auto-repair + scoped hint
Changes: BUGFIX fix(#1133): oracle --mark CLI + outline/detail evidence-based auto-repair + scoped hint | Background: context compaction mid-workflow leaves outline=pending + detail=complete; oracle aborts with "inconsistent" and no actionable recovery guidance existed for this pair. | Changes: (1) evidence-resolver.js: outline/detail predicates (plans-dir artifact existence check); (2) next-step: --mark <step> complete CLI (2-token, session-global, run from any directory), outline/detail oracle auto-repair before inconsistency scan, outline/detail scoped abort hint, generic hint bifurcated by hasCompletionEvidence (artifact exists → --mark guidance; no artifact → /workflow-init guidance); (3) reconcile-state: EVIDENCE_STEPS extended to include outline and detail; (4) workflow.md: evidence-based auto-repair paragraph, --mark recovery step added to priority list. | Test gap: no test covered oracle auto-repair for outline/detail pair or --mark CLI before this PR <!-- compose-doc-append-sentinel: branch=fix/1133-oracle-mark pr=#1213 -->

### BUGFIX: update-docs UD-8g/UD-9e emitted premature WORKFLOW_USER_VERIFIED (2026-06-29, 5ba9d13)
Background: The #1053 oracle refactor (a770075) introduced the dedicated user_verification oracle step and removed 'and invoke commit-push' from update-docs UD-8g, but left UD-8g (Path A) and UD-9e (Path B) instructing emission of <<WORKFLOW_USER_VERIFIED>>. Under ENFORCE_WORKTREE=on this contradicted the oracle's user_verification hint (next-step) and skills/_shared/user-verified.md (the PR must exist first); CC tried to emit the sentinel on the worktree side before any PR existed.
Changes: Deleted UD-8g and UD-9e. UD-8f/UD-9d now carry an explicit guard ('do NOT emit WORKFLOW_USER_VERIFIED here; the oracle owns user_verification'). The oracle is the sole owner of user_verification (Path A defers to /worktree-end; Path B emits just before /commit-push).
Test gap: no lint asserts update-docs Path A/B terminate at docs_complete without a USER_VERIFIED emit

### BUGFIX: workflow stopped to ask the user at every stage boundary (oracle invoke not auto-proceeding) (2026-06-29, 5ba9d13)
Background: The #1053 oracle refactor replaced inter-skill 'Invoke <next-skill>' chaining (8 SKILL.md files) and the PostToolUse nextStepHint with an oracle-consult-between-every-step model, adding a return-to-main checkpoint after every step. CLAUDE.md said 'invoke means run the named skill' but never instructed the orchestrator to proceed without asking, so it stopped and asked at every stage. #1122 reported this for worktree-start.
Changes: CLAUDE.md: ACTION=invoke now means run the named skill immediately without pausing to ask the user; confirmation is reserved for CONFIRM_* gates, the USER_VERIFIED sentinel, and in-skill AskUserQuestion. Generalizes the worktree-start-specific auto-proceed fix to all skills.
Test gap: no test asserts the invoke-verdict auto-proceed directive is present in CLAUDE.md

### FEATURE: PR #1220 — feature/t1-test-quality (2026-06-29, 69bc6f8, #1220)
Background: feat(#1149,#1001,#1208): T1 test quality — table-driven mandatory, mutation probe, false-green detection
Changes: feat(#1149 T1-D/T1-F): table-driven mandatory + false-green detection — added Table-Driven Tests section to `skills/_shared/test-design.md` (required for parser/regex/allowlist changes); new `bin/check-table-driven.sh` enforcement checker; new `bin/check-false-green.sh` same-literal false-green detection; refactored `tests/main-workflow-gate-regex.sh` as table-driven prototype; propagated requirements to `skills/write-tests/SKILL.md` and `skills/review-tests/SKILL.md`;feat(#1001): security fix test patterns in test-design.md — added security/protective-fix test design patterns covering negative assertion requirements, attack-scenario structure, and paired test-gap documentation;feat(#1208 T1-E1): lightweight mutation probe — new `bin/mutation-probe.sh` mutates single-line `const NAME = /regex/;` declarations one at a time and verifies the test suite FAILs; mutation score threshold 80%; auto-detects test file from `# Tests:` header; partial-coverage warnings for multi-line forms and WRITE_PATTERNS arrays; security-hardened (array-based execution for auto-detect, identifier validation before sed) <!-- compose-doc-append-sentinel: branch=feature/t1-test-quality pr=#1220 -->

### FEATURE: PR #1227 — fix/1225-profile-snippet-guards (2026-06-30, 9999e4bb8d7f3244662ea51a3fb6fafb76daf3f9, #1227)
Background: fix(#1225): profile-snippet.sh fetch guards + idempotency
Changes: subject: fix(#1225): profile-snippet.sh — session-sync fetch guards + idempotency; category: BUGFIX; background: On iTerm startup, session-sync git fetch ran in background without GIT_TERMINAL_PROMPT=0. When SSH passphrase was not cached, git blocked on tty passphrase prompt, suspending the zsh job ([N] + suspended (tty input)). ctrl+C recovery left zsh prompt color lost and `codes .` failing with "command not found". Secondary root cause: no idempotency guard — .profile_common:13 and .zshrc:115 both source profile-snippet.sh, running the fetch block twice.; changes: Added _AGENTS_PROFILE_LOADED sentinel guard at file top (POSIX [ ], return 0 2>/dev/null). Extracted fetch block into _session_sync_fetch() helper: GIT_TERMINAL_PROMPT=0 scoped to fetch subshell, 2>/dev/null on git fetch, setopt LOCAL_OPTIONS NO_MONITOR (zsh-only, inside function for auto-revert via LOCAL_OPTIONS), unset -f after use.; test-gap: No L2 broad-integration test existed for profile-snippet.sh; the new test tests/fix-1225-profile-snippet-guards.sh would have caught the missing GIT_TERMINAL_PROMPT=0 guard and idempotency sentinel. <!-- compose-doc-append-sentinel: branch=fix/1225-profile-snippet-guards pr=#1227 -->

### FEATURE: PR #1227 — fix/1225-profile-snippet-guards (2026-06-30, 9999e4bb8d7f3244662ea51a3fb6fafb76daf3f9, #1227)
Background: fix(#1225): profile-snippet.sh fetch guards + idempotency
Changes: subject: fix(#1225): profile-snippet.sh — session-sync fetch guards + idempotency; category: BUGFIX; background: On iTerm startup, session-sync git fetch ran in background without GIT_TERMINAL_PROMPT=0. When SSH passphrase was not cached, git blocked on tty passphrase prompt, suspending the zsh job ([N] + suspended (tty input)). ctrl+C recovery left zsh prompt color lost and `codes .` failing with "command not found". Secondary root cause: no idempotency guard — .profile_common:13 and .zshrc:115 both source profile-snippet.sh, running the fetch block twice.; changes: Added _AGENTS_PROFILE_LOADED sentinel guard at file top (POSIX [ ], return 0 2>/dev/null). Extracted fetch block into _session_sync_fetch() helper: GIT_TERMINAL_PROMPT=0 scoped to fetch subshell, 2>/dev/null on git fetch, setopt LOCAL_OPTIONS NO_MONITOR (zsh-only, inside function for auto-revert via LOCAL_OPTIONS), unset -f after use.; test-gap: No L2 broad-integration test existed for profile-snippet.sh; the new test tests/fix-1225-profile-snippet-guards.sh would have caught the missing GIT_TERMINAL_PROMPT=0 guard and idempotency sentinel. <!-- compose-doc-append-sentinel: branch=fix/1225-profile-snippet-guards pr=#1227 -->

### FEATURE: PR #1221 — feature/1102-multi-repo-intent (2026-06-30, 7acb054238db7ebc7aba1e9a7f85a753bc073828, #1221)
Background: feat(#1102,#1104): multi-repo worktree sessions — sibling intent schema + worktree-end fan-out
Changes: #1102 (Sub-D): added `## worktrees` schema to intent.md and CI-3b multi-repo probe to `clarify-intent/SKILL.md`; sibling parsing extracted to canonical `hooks/lib/parse-worktrees.js` + `bin/parse-worktrees` (mirrors `parse-closes-issues`), so `worktree-copy-worker.md` Step 3b is a one-line CLI call; Step 3 now uses `worktree-copy-include.js` argv form (`--main-root`/`--worktree-path`) instead of an inline `node -e` — removes inline code from the prompt (rules/prompt.md §1.3/§1.5);#1104 (Sub-E): added `## SiblingWorktrees` section to WORKTREE_NOTES.md (via `hooks/lib/worktree-notes.js`); `capture-env.sh` resolves per-sibling PR/SHA and now serializes `SIBLING_REPOS_JSON` via `sibling-repos-json.js` (JSON.stringify escaping) instead of hand-built shell strings (security Finding 2 follow-up); `cleanup-cascade.sh` converted to `cleanup-cascade.md` (Read-as-spec, matching `_shared/*.md`; it only ever `cat`-ed a prompt) and WE-21 fans out doc-append-worker to each sibling repo; `commit-push` CP-2 sibling uncommitted/unpushed warning extracted to `bin/check-sibling-uncommitted.sh`; security hardening: repo whitelist, metachar rejection, path normalization <!-- compose-doc-append-sentinel: branch=feature/1102-multi-repo-intent pr=#1221 -->

### FEATURE: PR #1238 — feature/env-os-blocks (2026-07-02, ab7f495b4de3ac5f722fa865a0d4869e11733b8f, #1238)
Background: feat(#1226): OS-conditional .env blocks (#@if windows/posix) across all 3 loaders
Changes: [#1226] FEATURE — .env now supports OS-conditional blocks via comment-style markers `#@if windows` / `#@if posix` / `#@endif`. On win32 the `#@if windows` blocks are kept and `#@if posix` blocks dropped; on macOS/Linux the reverse; marker lines are stripped; a flat .env with no markers parses byte-identically (fully backward compatible). Background: path-format vars (WORKTREE_BASE_DIR, ENFORCE_WORKTREE_EXTRA_REPOS, ENFORCE_WORKTREE_EXCLUDE, WORKFLOW_PLANS_DIR) carrying Windows drive-letter/backslash values were silently dropped on macOS, but splitting .env per-OS would break SSOT for shared secrets (API keys / WIP IDs). A single conditional-block .env keeps SSOT while letting the path vars diverge per OS. Scope grew during review from "load-env.js only" to the full .env-loader class: all THREE independent loaders now interpret the markers equally — hooks/lib/load-env.js (new pure `filterOsBlocks(text, platform)`), hooks/pre-commit `_load_env_file`, and bin/github-issues/wip-state.sh `load_env_file`. The review-security pass flagged that a load-env.js-only fix would leave the two bash loaders reading the raw (unfiltered) .env, reintroducing the silent-drop for anything they consume — reversing the original single-file plan. The two bash loaders route through a new SSOT glue executable bin/env-os-filter (calls `filterOsBlocks` via node; falls back to raw passthrough with a stderr warning when node/load-env.js is unavailable, so behavior never regresses). The OS-filter algorithm lives in exactly one non-test location (`filterOsBlocks`); env-os-filter is pure glue with no filtering logic of its own. Supporting changes: .env.example restructured to demonstrate the `#@if` block format as a migration template for the 4 path vars (all default to empty); bin/review-env-example skips marker lines so `#@if`/`#@endif` are exempt from the per-variable comment-line cap; rules/docs/env-example.md gained a one-line OS-conditional-markers carve-out. env-os-filter passes .env paths to node via env vars (not string-interpolated into `-e`) to close CWE-78/CWE-22; the stdin path uses a mktemp temp file with an EXIT-trap cleanup. <!-- compose-doc-append-sentinel: branch=feature/env-os-blocks pr=#1238 -->

### FEATURE: PR #1239 — fix/migrate-repo-identity-guard (2026-07-02, d249791, #1239)
Background: fix(#1234,#1232): /migrate-repo self-repo identity guard + canary-1 label sync
Changes: #1234 (BUGFIX): /migrate-repo lacked a guard against migrating the agents repo into itself when REPO_PATH was misidentified from a path mentioned only as documentation/reference. Added defense-in-depth self-repo identity detection at three independent sites: SKILL.md MR-1 (explicit target confirmation + AskUserQuestion, never silently infer REPO_PATH), preview-and-capture.sh (early dry-run detection emitting MIGRATE_SELF_REPO_DETECTED sentinel for MR-2 to confirm via AskUserQuestion), and orchestrate.sh pre-flight (structural exit-1 block on live REPO_DIR == AGENTS_CONFIG_DIR unless MIGRATE_ACK_EXISTING_ISSUES=1). Existing-issues WARNING strengthened to flag possible misidentification. Test gap: no regression test asserted that a self-repo live run aborts or that the dry-run path still emits its sentinels.;#1232 (BUGFIX): /migrate-repo MR-4 invoked canary-1 with --from-step 2, skipping the idempotent Step 1 label sync, so history canary-1 failed with "label not found" in fresh repos lacking type:*/status:* labels. Changed MR-4 to --from-step 1 so bootstrap-labels.sh runs before the first issue is created. MR-5/MR-6 keep --from-step 2 (Step 1 already ran). Test gap: no test verified Step 1 ran before issue creation in the canary-1 stage. <!-- compose-doc-append-sentinel: branch=fix/migrate-repo-identity-guard pr=#1239 -->

### FEATURE: PR #1260 — feature/multirepo-safety (2026-07-03, 2059b189370c1ab93faf5f0114b73bf7eb3048b5, #1260)
Background: feat(#1236,#457,#1235): target-visibility leak guard + Universality First + exclude-repos
Changes: #1236 (FEATURE/SECURITY): Outbound-content scanning to public forges now resolves the *target* repo's visibility instead of inferring it from the current directory. `scan-outbound.js` gained forge-write target extraction (`extractRepoFlag`), a `shouldScanAsPublicTarget` gate (fail-CLOSED: unknown/error → scan as public), and a dynamic private-repo-name WARN sourced from `gh repo list --visibility private --limit 1000` (fail-OPEN: error → no extra names). A soft-prompt counterpart was added to `rules/github-issues.md` (## Multi-repo leak prevention). Background: pushing content to a public repo from inside a private working directory previously escaped the private-info scan because visibility was inferred from cwd, so private-repo names could leak into public issues/PRs. Security hardening during implementation: closed a scan-suppression vector where a `--repo owner/name` flag smuggled inside a quoted `--body`/`--title` value forced a public target to resolve as private and suppressed the scan — fixed by stripping only `--body`/`--title` VALUES (`stripInlineBodyArg`) before target extraction, so a real `--repo "owner/repo"` still resolves while a smuggled one is neutralized; also removed a dead predictable-path tmpdir cache of private-repo names (`os.tmpdir()/scan-private-repos-cache.json`, written-but-never-read → info-leak surface). Test gap: no regression case proved a `--repo` smuggled mid-`--body` (followed by more text before the closing quote) resolves to null, nor that a legitimate quoted `--repo` alongside a smuggle-containing body still extracts the real target — both directions now covered in `tests/feature-1236-scan-outbound-target/part-a.sh`.;#457 (REFACTOR): Added the "Universality First" core-principle to `rules/core-principles.md` (§7) — agents-repo skills/CLI/hooks must be repo-agnostic and environment-agnostic from the start, not Windows- or self-repo-specific; unavoidable special cases must be named and bounded, never branched on implicitly. Applied it to enforcement path matching: `enforce-worktree/config.js` `isRepoExcluded` and the shell `hooks/pre-commit` parity now case-fold paths only on Windows (`process.platform === 'win32'` / `uname -s` MINGW*|MSYS*|CYGWIN*) and compare case-sensitively on POSIX. Background: path comparison had lowercased unconditionally, which is correct on Windows but wrong on case-sensitive POSIX filesystems — a special case leaking into the general path.;#1235 (FEATURE/CONFIG): New `ENFORCE_WORKTREE_EXCLUDE_REPOS` env var exempts specific repo root paths from worktree enforcement so they can be edited and committed directly from any branch, without disabling enforcement globally. Honored by both the JS `enforce-worktree.js` hook (via a new `config.js` `isCommandRepoExcluded(input, cwd)` helper — extracted so `enforce-worktree.js` stays under the 500-line HARD split limit) and the shell `hooks/pre-commit`. Background: a global `core.hooksPath` made the workflow's commit guard fire inside unrelated repos, blocking automated commits there; the allowlist scopes the guard to workflow-managed repos while all other repos stay enforced. <!-- compose-doc-append-sentinel: branch=feature/multirepo-safety pr=#1260 -->

### FEATURE: PR #1266 — fix/1231-1204-symlink-crossrepo (2026-07-03, ea54f2d, #1266)
Background: fix(#1231,#1204): migrate-repo symlink docs/ cross-repo commit + find-pr-by-marker --repo fallback
Changes: BUGFIX #1204: find-pr-by-marker.sh fallback --repo routing. Background: fallback `gh pr list` (lines 90-94) ran unconditionally even when `--repo` was set; for a cross-repo issue where the primary returned empty, it searched the current repo and could match a PR carrying the same `<!-- issue-close-pr-of: N -->` marker, yielding a wrong MERGE_COMMIT in the "Resolved by commit" comment (core-principles §5 integrity gap). Fix: wrapped fallback in `if [ -z "$REPO_ARG" ]; then ... fi`; cross-repo PR is definitively in the named repo so fallback is skipped when `--repo` is given. Updated comment at line 20. Added F11 regression guard; rewrote F9/F10 from stale-comment fallback tests to primary-path tests. Test gap: no test existed for fallback behavior when `--repo` is set; F11 would have caught this.;BUGFIX #1231: commit-migration-artifacts.sh symlink docs/ cross-repo commit. Background: `git -C "$REPO_DIR" add -- docs/todo.md` exits 128 ("pathspec beyond a symbolic link") when `docs/` is a symlink to an external git repo (e.g. ai-specs). Triggered on fornix-agent migration (2026-06-30); required manual WORKFLOW_OFF workaround. All sibling repos with `docs/` symlinked to ai-specs hit the same failure (core-principles §3/§5). Fix: added `resolve_external_docs_repo()` helper (bash port of `hooks/workflow-gate/staged-evidence.js:resolveExternalDocsRepo()`); splits staging loop to skip `docs/` entries when external docs-repo detected; commits docs ALLOWLIST entries to the external repo at the correct `git rev-parse --show-prefix` relative path in a separate commit and push; dry-run reports the detected docs-repo. SKILL.md MR-10 note updated. Test gap: no test for the cross-repo symlink scenario; C13 (two real git repos + real symlink + show-prefix path assertion) would have caught this. <!-- compose-doc-append-sentinel: branch=fix/1231-1204-symlink-crossrepo pr=#1266 -->

### FEATURE: PR #1258 — refactor/next-step-rename (2026-07-03, 9aa9c8380604964f327deeb7a8816216d796e870, #1258)
Background: refactor(#1243): rename workflow "oracle" concept to next-step
Changes: REFACTOR (#1243): Renamed the workflow "oracle" concept to "next-step". Background: the deterministic next-step advisory script `bin/workflow/next-step` had been called "the oracle" across code, tests, and docs since #1053 / PR #1056 (2026-06-23) with no recorded etymology or rationale; the label over-dignified a simple next-step lookup and clashed with the altitude of `supervisor` (an actual LLM agent) while conflicting with the binary's own self-descriptive name. The user chose to drop the metaphor entirely and unify on `next-step` (core-principles §6 Name Reflects Substance, §2 SSOT). Changes: renamed code identifiers (`oracleBin`/`oracleAction`/`oracleHint` → `nextStep*` in hooks/session-start.js; `bin/workflow/lib/parse-oracle-output.js` → `parse-next-step-output.js` via git mv, with `parseOracleOutput` → `parseNextStepOutput` and the REASON string `oracle-output-malformed` → `next-step-output-malformed`; comments in `bin/workflow/next-step`, evidence-resolver.js, mark-step-handler.js); test artifacts (git mv of 6 files + 1 sibling dir; `run_oracle`/`run_oracle_rc` → `run_next_step`/`run_next_step_rc`; `ORACLE_AGENTS_DIR` → `NEXT_STEP_AGENTS_DIR`; `oracle` tag values → `next-step`); and all active / forward-looking docs (CLAUDE.md, README.md, docs/architecture/claude-code/workflow.md + settings.md, update-docs / workflow-init / clarify-intent SKILL.md). Binary path `bin/workflow/next-step` unchanged; history.md / CHANGELOG.md historical entries left as-is (append-only records — user decision); landed as a single atomic PR. <!-- compose-doc-append-sentinel: branch=refactor/next-step-rename pr=#1258 -->

### FEATURE: PR #1276 — feature/reset-from-reason (2026-07-03, cfeb06d0eb596297fec733ea7ed3144df0bfea22, #1276)
Background: feat(#1263): require mandatory reason on WORKFLOW_RESET_FROM sentinel
Changes: #1263 (FEATURE): WORKFLOW_RESET_FROM sentinel now requires a mandatory reason field (`<<WORKFLOW_RESET_FROM_{step}: {reason}>>`). Background: the bare form was the only ask-series sentinel without a reason and was reflexively abused to clear workflow gates (INCIDENT #23, #1085). Changes: `RESET_FROM_RE_DQ` in `hooks/lib/sentinel-patterns.js` now captures `: ([^>]+)`; new `RESET_FROM_LOOKSLIKE_RE` catches bare/malformed forms in `isSentinel()` only (not `isStrictSentinel()`); `hooks/workflow-mark/reset-handler.js` intercepts LOOKSLIKE matches with corrective guidance and validates the captured reason via shared `validateSkipReason()` (>=3 non-space chars, no `>`, non-placeholder) before any state mutation, keeping the #526 non-fatal null-session contract; `settings.json` ask glob narrowed to the reason-bearing form; hint strings updated in `workflow-gate.js`, `workflow-mark.js`, `CLAUDE.md`, `docs/architecture/claude-code/{workflow,settings}.md`, and `skills/clarify-intent/SKILL.md`. RESET_FROM template placeholders switched from `<angle>` to `{brace}` notation (the `<reason>` + `>>` closer produced miscopied `>>>` runs); the `<< >>` frame selection rationale (no strong positive reason — permission-glob-matchable fixed literal, collision avoidance with unrelated echoes) is now recorded in `docs/architecture/claude-code/workflow.md ## Sentinel notation`. Breaking: the bare form is a hard error (no compatibility shim). Post-merge activation: regenerate `~/.claude/settings.json` via `install/assemble-settings.js` and restart Claude Code. <!-- compose-doc-append-sentinel: branch=feature/reset-from-reason pr=#1276 -->

### FEATURE: PR #1265 — fix/run-tests-integrity (2026-07-03, e3ecc1a, #1265)
Background: fix(#1215,#1242): run_tests evidence split + contract-trust completion
Changes: #1215 (BUGFIX): Split `run_tests` out of the combined `write_tests || run_tests` evidence branch in `hooks/lib/workflow-state/evidence-resolver.js` — `run_tests` is now sentinel-only, with no staged-test evidence predicate. Background: staged test files alone falsely satisfied `run_tests`, letting the commit gate unblock without a real test run. Changes: `run_tests` falls through to `return false`; `describeEvidence("run_tests")` returns `[]`; completion is owned solely by `workflow-run-tests.js` (contract-trust) or the `run_tests` sentinel. Test gap: no evidence-resolver case asserted `run_tests` + staged tests → evidence=false (now WS-EV-19 in `tests/main-workflow-evidence/evidence-resolver.sh`; B1 lockstep-flipped in `hint.sh`).;#1242 (BUGFIX): Replaced exit-code-based `run_tests` completion in `hooks/workflow-run-tests.js` with Approach C′ unified contract-trust. Background: a pipe/compound command's masking trailing exit-0, and no-match `executed=0` runs, both produced a false-green `run_tests=complete`, so a failed test run could wrongly unblock the commit gate. Changes: `tests/run-all.sh` now emits a machine-readable `RUN_CONTRACT: PASS=n FAIL=n SKIP=n EXECUTED=n` line; the PostToolUse hook trusts ONLY that contract (run-all.sh provenance via `isRunAllSh` + exactly-one-contract rule via `parseContract` + `executed>0` & `fail==0`), and actively demotes any test command without a valid contract to `run_tests=pending` (fail-safe). Provenance is a string match (self-gaming boundary tracked for #1241 follow-up). Also swept stale exit-code guidance falsified by the change: `hooks/workflow-gate.js` `run_tests` hint, `workflow-run-tests.js` header comment, `evidence-resolver.js` `run_tests` comment, and `docs/architecture/claude-code/{workflow,settings}.md`. Test gap: no test asserted that a pipe/compound command with a masking trailing exit-0 leaves `run_tests` pending (now covered by the C-group + contract-mandatory cases in `tests/main-workflow-run-tests.sh`). <!-- compose-doc-append-sentinel: branch=fix/run-tests-integrity pr=#1265 -->

### FEATURE: PR #1272 — refactor/1251-sid-resolver (2026-07-03, 01f5841, #1272)
Background: refactor(#1251): consolidate session-id resolvers into canonical chain + bin/resolve-session-id bridge
Changes: #1251: Consolidated 8 divergent session-id resolver implementations across hooks/, bin/, and skills/ into the single canonical `hooks/lib/workflow-state/session-id.js` 7-step chain (frozen SSOT). Added `bin/resolve-session-id` bash bridge (stdout=sid, exit 2 when unresolvable); all bash callers (`bin/github-issues/wip-state/session-id.sh`, `bin/lib/codex-core.sh`, `bin/lib/gemini-core.sh`, `skills/workflow-init/scripts/aggregate-wip-check.sh`, `wip-set-resume.sh`) locate the bridge script-relative (`BASH_SOURCE`), and `bin/issue-close-write-outcome.js` requires the canonical resolver `__dirname`-relative — never via `$AGENTS_CONFIG_DIR`, so each checkout uses its own resolver. Legacy `bin/lib/resolve-session-id.sh` deleted. Behavior change: `CLAUDE_CODE_SESSION_ID` now outranks `CLAUDE_ENV_FILE`/`CLAUDE_SESSION_ID` in every bash caller, and the `isSameGitRepo` cross-repo JSONL guard applies universally — eliminating the concurrent-session misattribution class (#1082 lineage). Bridge hardened against path injection (path travels via `BRIDGE_DIR` env, not string interpolation). Residual P3/P7 gaps (charset validation, newest-unsafe JSONL masking) filed as #1267. <!-- compose-doc-append-sentinel: branch=refactor/1251-sid-resolver pr=#1272 -->
Changes: #1215 (BUGFIX): Split `run_tests` out of the combined `write_tests || run_tests` evidence branch in `hooks/lib/workflow-state/evidence-resolver.js` — `run_tests` is now sentinel-only, with no staged-test evidence predicate. Background: staged test files alone falsely satisfied `run_tests`, letting the commit gate unblock without a real test run. Changes: `run_tests` falls through to `return false`; `describeEvidence("run_tests")` returns `[]`; completion is owned solely by `workflow-run-tests.js` (contract-trust) or the `run_tests` sentinel. Test gap: no evidence-resolver case asserted `run_tests` + staged tests → evidence=false (now WS-EV-19 in `tests/main-workflow-evidence/evidence-resolver.sh`; B1 lockstep-flipped in `hint.sh`).;#1242 (BUGFIX): Replaced exit-code-based `run_tests` completion in `hooks/workflow-run-tests.js` with Approach C′ unified contract-trust. Background: a pipe/compound command's masking trailing exit-0, and no-match `executed=0` runs, both produced a false-green `run_tests=complete`, so a failed test run could wrongly unblock the commit gate. Changes: `tests/run-all.sh` now emits a machine-readable `RUN_CONTRACT: PASS=n FAIL=n SKIP=n EXECUTED=n` line; the PostToolUse hook trusts ONLY that contract (run-all.sh provenance via `isRunAllSh` + exactly-one-contract rule via `parseContract` + `executed>0` & `fail==0`), and actively demotes any test command without a valid contract to `run_tests=pending` (fail-safe). Provenance is a string match (self-gaming boundary tracked for #1241 follow-up). Also swept stale exit-code guidance falsified by the change: `hooks/workflow-gate.js` `run_tests` hint, `workflow-run-tests.js` header comment, `evidence-resolver.js` `run_tests` comment, and `docs/architecture/claude-code/{workflow,settings}.md`. Test gap: no test asserted that a pipe/compound command with a masking trailing exit-0 leaves `run_tests` pending (now covered by the C-group + contract-mandatory cases in `tests/main-workflow-run-tests.sh`). <!-- compose-doc-append-sentinel: branch=fix/run-tests-integrity pr=#1265 -->

### FEATURE: PR #1281 — refactor/split-test-files (2026-07-03, 811d25d, #1281)
Background: refactor(#1275): split 3 oversized test files into dispatcher + fragments
Changes: Split 3 oversized test files that exceeded the 500-line HARD limit (rules/coding/file-split.md Pattern A): tests/main-workflow-run-tests.sh (862→57 lines, 5 fragments), tests/main-workflow-state-machine.sh (1049→74 lines, 7 fragments), tests/fix-416-classify-sentinel-reason-text.sh (520→61 lines, 4 fragments). Each dispatcher dot-sources fragments from a sibling tests/<name>/ directory. Mechanical refactor — no test behavior changed, all suites pass identically. Closes #1275. <!-- compose-doc-append-sentinel: branch=refactor/split-test-files pr=#1281 -->

### FEATURE: PR #1282 — fix/companion-precheck (2026-07-03, 5212f22, #1282)
Background: feat(#513,#1198,#1048,#1237,#1096): companion pre-check rework + guard CLIs
Changes: #513: Extracted CI-C0 tracking-issue guard and GH reconcile from clarify-intent inline bash to bin/ CLIs. Added clarify-guard-loop.sh (GUARD_ATTEMPT counter, plans-dir hard-validation, 4 stdout tokens: PROCEED/NEED_ISSUE/RETRY_EXHAUSTED/CLOSED_ENTRY) and clarify-commit-scope.sh (Path C new issue creation, Path B label/WIP/board loop with CLOSED_ENTRY pre-scan). SKILL.md inline bash blocks replaced with single CLI invocation lines; side effects deferred until after CI-5 scope confirmation.;#1198: Fixed enforce-worktree false-blocking clarify-intent CI-C0 guard (clarify-guard-loop.sh) from main worktree. Added isAllowedClarifyGuardLoop predicate to standard.js (script-path identity check, argTail chaining/redirect rejection, zero write-targets confirmation) and wired it into enforce-worktree.js import, allow block, and re-exports.;#1048: Improved companion reason quality. Added companion_pass_file_overlap (issue body path basename intersection -> file:<basename> tag) and companion_pass_keyword_density (title token overlap n>=2 -> kw:<n> tag) to companion-passes.sh; wired into find-companion-issues.sh reason column and tag_count.;#1237: Reworked clarify-intent CI-2b/CI-3a to batch pre-check + single presentation design. Added precheck-companions.sh (companion search, purity filter, baseline/full-set/per-candidate decomposition trials, JSON snapshot output) and reference/companion-batch-presentation.md (single multiSelect batch procedure). Added provenance annotations [origin: seed]/[origin: companion #N]/(companion-driven) and companion-driven-only wf-meta routing to judge-decomposition.md. Removed per-candidate sequential AskUserQuestion from CI-2b.;#1096: Moved CI-3a decomposition proposal from AskUserQuestion body to main conversation display. Integrated into #1237 rework: precheck-companions.sh trial results are annotated in main conversation during CI-2b batch presentation. <!-- compose-doc-append-sentinel: branch=fix/companion-precheck pr=#1282 -->

### FEATURE: PR #1288 — feature/1248-cap-auto-extend (2026-07-04, 339daff1365f75470113fa45ab02cdf8805bea1f, #1288)
Background: feat(#1248): collapse review-cap decisions into review-loop-verdict
Changes: FEATURE: Collapse planner-review cap decisions into review-loop-verdict (#1248, 339daff) <!-- compose-doc-append-sentinel: branch=feature/1248-cap-auto-extend pr=#1288 -->

### FEATURE: PR #1291 — feature/1287-outline-planner-intent-lock (2026-07-04, 674f769a454ab70ed7dd368d19b4dd8f29bd87ec, #1291)
Background: feat(#1287): outline-planner detects intent-lock in Accepted Tradeoffs and emits SINGLE_APPROACH_JUSTIFIED
Changes: Added intent-lock collapse rule to outline-planner: detects non-empty `## Accepted Tradeoffs` locking a design direction and emits `SINGLE_APPROACH_JUSTIFIED` instead of fabricating straw alternatives; outline-reviewer updated to not flag missing alternatives when direction is locked via accepted tradeoffs <!-- compose-doc-append-sentinel: branch=feature/1287-outline-planner-intent-lock pr=#1291 -->

### FEATURE: PR #1298 — feature/issue-1259-tests (2026-07-04, a10b54a87f7314d52ab24e0c8fc53ef76f49b770, #1298)
Background: feat(#1259): replace outline/detail isTrivial regex with orchestrator judgment
Changes: #1259: Replace outline/detail keyword-regex trivial detection with LLM semantic judgment. Background: `isTrivial()` regex could not detect semantic obviousness (single approach, zero design questions) without reading content; skip decisions required explicit user or LLM trigger even for clearly trivial steps. Changes: added `ACTION=judgment_needed` in `bin/workflow/next-step` at outline/detail steps; `NEXT_HINT` points to intent.md (outline) or outline.md (detail; falls back to intent.md when outline was skipped — C1 path); `gate-plan-skip-sentinel.js` unconditionally auto-approves `judgment=true`-tagged sentinels before CONFIRM_* checks; `isTrivial()` retained as secondary SKIP_HINT advisory; CLAUDE.md and `docs/architecture/claude-code/workflow.md` updated. <!-- compose-doc-append-sentinel: branch=feature/issue-1259-tests pr=#1298 -->

### FEATURE: PR #1299 — docs/glossary (2026-07-04, f506e2f684c419a13813903c5a7568a31d59dece, #1299)
Background: docs(#1297): add agents terminology glossary (docs/glossary.md)
Changes: Added docs/glossary.md — an index of agents-repo terminology (workflow stage names and abbreviations), written in English (public repo). Terms are grouped by category (Workflow / Workflow steps / Miscellaneous) and ordered from general concept to specific / chronologically within each group, rather than alphabetically. Each entry carries the full name, a 1–2 line gloss, and a link to its canonical source; definitions are not duplicated (index + link only, per SSOT). Initial 9 terms: workflow, sentinel, meta, WF-META, WF-CODE, intent, outline, detail, IR. Motivated by #1253, where IR (Intermediate Representation) naming ambiguity surfaced the need to centralize term definitions. SSOT was deliberately excluded (owned by rules/core-principles.md §2; duplicating it would itself violate SSOT), and the file is not registered in the rules/docs.md Standard Files table to preserve the docs-only short-circuit. (#1297) <!-- compose-doc-append-sentinel: branch=docs/glossary pr=#1299 -->

### FEATURE: PR #1304 — feature/1257-wf-meta-tests (2026-07-04, 6f1ed6b046c806797dd153eecc4e51954c081d5e, #1304)
Background: feat(#1257): add SC-2C path for WF-META session-close
Changes: #1257 (FEATURE): session-close: add SC-2C path for WF-META sessions. Background: WF-META sessions (workflow_type: "wf-meta") had no env-JSON generation path in /session-close — SC-2A requires worktree-end's env JSON (WF-META skips worktree-end), and SC-2B requires an existing PR (WF-META has no PR). Both paths exited non-zero, crashing Final Report generation before it could write. Reproduced during session e9a7fea5 and 368c4e36 (2026-07-03/04, #1253). Changes: added SC-1a detection step to session-close SKILL.md reading workflow_type from session state JSON (checked before ENFORCE_WORKTREE, dispatches to SC-2C on "wf-meta"); added SC-2C path calling session-close-build-env.js --wf-meta to write empty-PR-fields env JSON (all fields empty strings, no new keys, consistent with existing schema); added --wf-meta flag to bin/session-close-build-env.js; added --wf-meta mode to bin/issue-close-write-outcome.js (writes state: "skipped_wf_meta" + all subfields "skipped" per issue, patterned after --non-github); added SC-3 WF-META bypass (top of SC-3, before is-github-dotcom-remote check) that writes outcome directly and skips /issue-close-finalize; added "kept open (planning session)" display mapping for state: "skipped_wf_meta" in SC-6 Closed Issue Outcomes renderer. <!-- compose-doc-append-sentinel: branch=feature/1257-wf-meta-tests pr=#1304 -->

### FEATURE: PR #1312 — feature/1292-command-ir (2026-07-04, e24e0e5, #1312)
Background: feat(#1292): add command-ir.js IR + classify() false-positive fixes (#876, #1223)
Changes: [#1292 FEATURE] New `hooks/lib/command-ir.js` module: `parse(cmd)→IR` (structured intermediate representation with segments, cmd0, argv, redirects, kind, rawText, parseFailure) and `isOsTempPath()` SSOT predicate. Split `bash-write-patterns.js` (457 lines) into `bash-write-patterns/{patterns,classify}.js` sibling directory per file-split rule; entrypoint becomes 7-line dispatch+re-export. Added `ATTACHED_REDIRECT_RE` export to `command-parser.js`. IR enables position-aware false-positive suppression in `classify()` and a CWE-22-guarded temp-path redirect gate. parseFailure===true forces "write" (fail-closed). Security review fixed three HIGH findings (path-traversal bypass in isOsTempPath, premature return-read in Out-File and git-reset pre-checks) before ship.;[#876 BUGFIX] `classify()` false-positive: `--out-file` CLI flags (`pwsh script.ps1 --out-file result.txt`) matched `\bOut-File\b` and were blocked as writes. IR position check now verifies Out-File is in command position (seg.cmd0) before the pattern fires; CLI flag position is demoted only, not short-circuited. Test gap: no regression test existed for Out-File in flag position vs command position.;[#1223 BUGFIX] `classify()` false-positive: `git log path/to/reset/module.js` and similar commands where `reset` appeared in a path argument matched `\bgit\b.*\breset\b` and were blocked. IR subcommand position check now verifies `reset` is the first non-flag argv token after `git` before the pattern fires. Test gap: no test existed for git commands where `reset` appears in a path component rather than as the git subcommand. <!-- compose-doc-append-sentinel: branch=feature/1292-command-ir pr=#1312 -->

### FEATURE: PR #1313 — feature/1306-cross-repo-routing (2026-07-04, 40d07558bb8e447d04092eb7ebf356055b6a9c15, #1313)
Background: feat(#1306): add cross-repo issue routing to workflow-init (L1 explicit tokens + L2 prose detection)
Changes: Add cross-repo issue routing to workflow-init (issue #1306). Layer 1: explicit `repo#N` / `owner/repo#N` tokens route each issue's `gh` calls to the correct repo via a `--repo` flag threaded through all workflow-init scripts (filter-primary-candidates, wip-set-resume, closed-detection, path-a-label-and-board, aggregate-wip-check, clarify-commit-scope) and leaf helpers (wip-state, wip-set-single, board-card, cmd-set). Layer 2: `clarify-intent` detects cross-repo prose and proposes a mapping for user confirmation. Added `bin/parse-issue-tokens` CLI as SSOT for all token parsing (wraps `parseIssueToken` in parse-closes-issues.js), `BOARD_CARD_REPO_OVERRIDE` env var in board-card.sh for repo override without CWD change, and `--repo-map IDX:owner/repo` arg protocol for per-issue routing across script boundaries. Short-form `repo#N` is normalized to `owner/repo#N` once at the WI-3 orchestration boundary. Security: `wip-state.sh` and `wip-set-single.sh` both validate the `--repo` slug. <!-- compose-doc-append-sentinel: branch=feature/1306-cross-repo-routing pr=#1313 -->

### FEATURE: PR #1314 — feature/proactive-lang-injection (2026-07-04, fb7c10da707b148218a4fea320d0e0b6e09fe9f4, #1314)
Background: feat(#1303): proactive language-directive injection
Changes: #1303 (FEATURE): Proactive language-directive injection. Background: CONV_LANG was injected only at four boundary events (session-start / post-compact / subagent-start / stop), so long sessions drifted back to English (lost-in-the-middle, Liu et al. TACL 2023); PLAN_LANG was only corrected reactively by check-plan-lang.js after a plan had already been written in the wrong language. Changes: added hooks/lang-inject.js (UserPromptSubmit) that re-injects the CONV_LANG directive every turn and proactively injects PLAN_LANG when isPlanning() is true (any of clarify_intent/outline/detail not complete/skipped); strengthened getConvLangInjection wording to cover narration between tool calls; added getPlanLangInjection() as a shared SSOT (hooks/lib/lang-config.js) consumed by both lang-inject.js and subagent-start.js, with PLAN_LANG limited to the outline/detail planner+reviewer agent whitelist to avoid noise; control-char/newline guard neutralizes directive-smuggling on both CONV_LANG and PLAN_LANG paths; check-plan-lang.js retained as the reactive backstop. Adopted Approach C (scoped proactive injection) over always-on (noise) and surface-split (main-conversation coverage gap). <!-- compose-doc-append-sentinel: branch=feature/proactive-lang-injection pr=#1314 -->

### FEATURE: PR #1302 — feature/1286-judgment-skip (2026-07-04, dcbbecb, #1302)
Background: feat: authoritative recorded-verdict skip for outline/detail stages
Changes: **#1302** feat: authoritative recorded-verdict skip for outline/detail — the orchestrator now records a validated `skip_judgment` (`judgment_source: orchestrator`, all rubric conditions met) in workflow state before emitting the OUTLINE/DETAIL_NOT_NEEDED path; `next-step` marks the stage `skipped` only when the recorded verdict passes per-target validation, and the advisory SKIP_HINT fifth line is demoted to a weak supplementary hint. Why: #1298's inline `judgment=true` was unvalidated (the gate checked only for the literal string, never the rubric conditions) and left no auditable record.;**#1300** recorded-verdict migration + must-fix hardening (Sub-1 of Group #1309): reverted PR #1298 (commit a10b54a) and rebased the recorded-verdict core onto it, then bundled six must-fix items in the same atomic PR — #5 gate-plan-skip-sentinel.js passes `sessionIdFromInput: input.session_id` (the skip was otherwise inert); #2 `hasValidSkipJudgment` validates per-target CONDITION_SCHEMAS (outline so_c1/so_c2, detail sd_c1/sd_c2/sd_c3) with strict key/boolean checks via the newly exported `isRecordedVerdictValid`; #1 `record-skip-judgment` reads back the written record and exits non-zero on mismatch (no false success); #3+#7 `next-step` extracts a shared `applyRecordedVerdictSkip` helper that re-attaches `skip_judgment` to markStep extraFields (audit preservation across the full-object step replacement) and fails open on any error; #6 `clarify-intent` CI-C1b records only the outline skip, deferring detail to make-outline-plan MOP-C1 (detail conditions cannot be evaluated before outline.md exists). Reworked the feature-1286 recorded-verdict test suite. <!-- compose-doc-append-sentinel: branch=feature/1286-judgment-skip pr=#1302 -->

### FEATURE: PR #1322 — feature/1293-canary2-ir (2026-07-04, f0b65398284e9d12fff5acf92bf623af144ec7a9, #1322)
Background: feat(#1293): canary-2 — splitSegments fd-dup fix + shared-cmd-utils IR migration
Changes: category: FEATURE | subject: feat(#1293): canary-2 — splitSegments fd-dup fix + shared-cmd-utils.js IR migration | background: #1293 is part of #1253 (Phase 2 IR migration series). The fd-dup misread class (2>&1 bare & misread as segment separator by splitSegments) caused 3 recurrences (#982, #838, #959), fixed at regex level in #1157. Canary-1 (#1292/PR #1312) added command-ir.js as the IR foundation. This canary builds on that: fixing splitSegments structurally and migrating three detection functions to IR-based logic. A security review during implementation caught a guard-weakening regression (segment-count signal missed leading/trailing separators); fixed to use separator-count signal (ir.separators.length > 0) for the two broad gates, matching the behavior of hasCommandSequencing which already used ir.separators. | changes: (1) command-parser.js: added splitSegmentsWithSeparators() with fd-dup lookahead (N>&M, N>&-, >&M, >&-) and unconditional separator recording; splitSegments() delegates to it (SSOT — eliminates duplicate state machine). (2) command-ir.js: extended parse() with additive separators: string[] field; all return paths including parseFailure paths get separators: []. (3) shared-cmd-utils.js: migrated hasShellChaining (uses ir.separators.length > 0), rejectInterpreterAndChaining (uses ir.separators.length > 0), and hasCommandSequencing (uses ir.separators.some(s => ";", "&&", "||")) to IR-based detection; fd-dup regex strips removed; fail-closed on parseFailure maintained across all three. <!-- compose-doc-append-sentinel: branch=feature/1293-canary2-ir pr=#1322 -->

### FEATURE: PR #1320 — feature/issue-1308-codex-allowlist (2026-07-04, 88e1e0725c85898c992371520d47f566f6e2eefe, #1320)
Background: feat(#1308): make review-plan-security and review-tests Codex-primary
Changes: Made `review-plan-security` and `review-tests` Codex-primary (Generator-Critic split): both skills now run the security-plan / test-review Codex review as the primary reviewer, with a CC subagent fallback (`plan-security-reviewer`, `test-reviewer`) launched only on Codex-unavailable (exit 3). Saves CC tokens and adds an independent adversarial perspective. Added `security-plan` / `test-review` to the FORMAT allowlists in `bin/run-codex-review-loop` and `bin/review-plan-codex`; both formats are single-round terminal (CAP=1, MAX_EXTENSIONS=0), so exit 1 is TERMINAL (no re-loop). Follow-up fix in the same change: the two new wrapper scripts' `cleanup_counter` now clears the round-number counter file on exit 1 too (`0|2|4)` → `0|1|2|4)`); the persist-on-exit-1 behavior was copied from the multi-round detail/outline wrappers and is wrong for a terminal single-round format — a stale counter would otherwise survive a terminal run.;Fixed a parallel-worktree stale-token block in `bin/compute-staged-tests-token.js` (staged-tests fingerprint emitter for the review_tests gate, #833). `findRepoDirWithStagedTests()` returned the first LINKED worktree with any staged tests, so when several concurrent worktrees had staged tests it fingerprinted the wrong one — permanently mismatching the pre-commit gate, which always fingerprints the commit-target worktree. Selection is now: explicit `argv[2]` worktree path → `process.cwd()`'s worktree when it has staged tests → legacy scan fallback. Surfaced live: `/review-tests` stored a sibling worktree's token, blocking `/commit-push` here forever. Test gap: no test exercised multi-worktree selection — added a fixture-based test asserting explicit-arg/cwd priority. <!-- compose-doc-append-sentinel: branch=feature/issue-1308-codex-allowlist pr=#1320 -->

### FEATURE: PR #1326 — refactor/1294-tests (2026-07-05, 24cb686, #1326)
Background: feat(#1294): canary-3 — bash-write-scope.js IR consumption + dispatch parse-once
Changes: canary-3 (#1294): migrate bash-write-scope.js and enforce-worktree.js to IR consumption — classify(ir|string) shim; isGhWriteIR(ir) predicate; collectBashWriteTargets/isGhWriteCommand/isEverySegmentExcluded consume IR segments; parse(cmd) called once at dispatch entry. mi/ci added to WRITE_PATTERNS. Follow-up: effectiveCmd0 single-level env-assignment gap and env VAR=gh argv ambiguity tracked for later canaries. <!-- compose-doc-append-sentinel: branch=refactor/1294-tests pr=#1326 -->

### FEATURE: PR #1332 — refactor/1327-resolve-effective-command (2026-07-05, a549e55, #1332)
Background: feat(#1327,#1328): add resolveEffectiveCommand helper; fix multi-level VAR=val + env-gh false positive
Changes: #1327: add resolveEffectiveCommand/resolveEffectiveArgv helpers to segment-utils.js; fix multi-level VAR=val mis-resolution in bash-write-scope.js and patterns.js; fix mutation-probe.sh grep BRE→ERE; add C6 regression tests (151 pass);#1328: fix env-prefix branch in isGhWriteIR (patterns.js) to skip leading VAR=val tokens via findIndex instead of indexOf("gh"), preventing false-positive match on env EDITOR=gh vim; add C5.9b regression test <!-- compose-doc-append-sentinel: branch=refactor/1327-resolve-effective-command pr=#1332 -->

### FEATURE: PR #1334 — feature/stale-guard-tests (2026-07-05, 67dc8cc9492b0a752a996f23e47027b9d07444a5, #1334)
Background: feat(#1310): hasValidSkipJudgment stale-guard — bind recorded verdict to artifact mtime
Changes: FEATURE: hasValidSkipJudgment stale-guard — bind recorded verdict to artifact mtime (#1310) <!-- compose-doc-append-sentinel: branch=feature/stale-guard-tests pr=#1334 -->

### FEATURE: PR #1338 — feature/workflow-off-skill (2026-07-06, fba692f, #1338)
Background: feat(#1337): add /workflow-off /workflow-on skills and pre-commit human bypass
Changes: Added `/workflow-off` and `/workflow-on` skills to expose `WORKFLOW_ENFORCE_WORKFLOW_OFF` / `ON` sentinels as invocable skills. Added `user-invocable: false` to `make-outline-plan` and `make-detail-plan` frontmatter.;Added human terminal bypass to `hooks/pre-commit` ENFORCE_WORKTREE section: TTY detection (`[ -t 1 ]`) combined with `CLAUDECODE` unset check. CC Bash tool is always non-TTY so the bypass cannot be defeated by unsetting CLAUDECODE. <!-- compose-doc-append-sentinel: branch=feature/workflow-off-skill pr=#1338 -->

### REFACTOR: Rename /workflow-on /workflow-off skills to /enforce-workflow-* (2026-07-09, aad1cf4)
Background: The high-frequency /workflow-init entry point ranked below the low-frequency /workflow-on and /workflow-off skills in the slash-command fuzzy autocomplete: on the shared 'workflow-' prefix the shorter names score higher, so typing '/workf' surfaced on/off before init.
Changes: Renamed the workflow-on / workflow-off skills to enforce-workflow-on / enforce-workflow-off (directory rename + frontmatter name + the /enforce-workflow-on self-reference in enforce-workflow-off/SKILL.md). /workflow-init now ranks first at the '/workf' prefix. Marker files (.workflow-off), the WORKFLOW_ENFORCE_WORKFLOW_ON/OFF sentinels, and rules/workflow-off.md (the enforcement-mechanism SSOT) are intentionally unchanged, so hook behavior is unaffected. ~/.claude/skills is a directory symlink, so the rename takes effect on the next skill scan with no reinstall.

### FEATURE: PR #1341 — feature/1340-issue-setup (2026-07-09, bebbdba6ce39f83e3b407271f38ae1cf237e2dc4, #1341)
Background: feat(#1340): add /issue-setup skill + issue-create auto-init for uninitialized repos
Changes: FEATURE (#1340): Added `/issue-setup` skill as the single entry point for initializing a fresh repo (e.g. `ds4-ops`) that lacks labels and a Projects v2 board. Background: running `issue-create` from another repo against an uninitialized repo hard-failed — `gh issue create --label "type:task"` returned HTTP 422 on missing labels, and `wip-state.sh set` cascaded failures on a missing board. The building blocks (`sync-labels.sh`, `create-project.sh`, `wip-state.sh setup`) existed but had no single initialization entry point. Changes: (a) new `skills/issue-setup` (label-sync + Projects v2 board create/reuse) wrapping `sync-labels.sh --repo` and a new `lib/ensure-project-ready.sh` (project create/reuse + Status field with Todo/In Progress/Done options + session-fingerprint field, extracted migration-state-free from `create-project.sh`); (b) `issue-create` now independently auto-detects gaps — Phase 0a in `issue-create.sh` auto-runs `sync-labels.sh` on missing labels, Phase 0b in `skills/issue-create/SKILL.md` prompts (AskUserQuestion) to create a missing board (project check is re-run every time; a single "no" never suppresses future checks, since a board is a heavy permanent resource on the target repo while a missing label is a hard 422 failure worth auto-repairing); (c) WIP field IDs migrated off `.env` `WIP_STATE_*` (structurally broken — `.env` is blocked by `block-dotenv.js` in the agents repo itself, and the flat layout cannot represent multi-repo field IDs) to on-demand resolution via `lib/resolve-project.sh`'s TSV cache, extended 5→10 columns (+Status field id, +Todo/In Progress/Done option ids, +fingerprint field id) with a schema-version guard treating any non-10-column row as a cache miss (self-healing overwrite); `wip-state.sh set/check/clear` resolve field IDs from the cache with a temporary-migration fallback, and the `setup` verb is deprecated (warn+exit 0, no `.env` write); (d) `sync-labels.sh` gains `--repo OWNER/REPO` for cross-repo sync; (e) `docs/ops/migration-from-todo.md` Step 1 now references `/issue-setup` instead of raw `sync-labels.sh`; (f) split the 702-line `tests/feature-resolve-project.sh` into a `tests/feature-resolve-project/` folder (61-line dispatcher + `_lib.sh` + resolver-core/cache/errors group files, each <500 lines) to satisfy the file-split HARD>500 limit, preserving 19/0 coverage. Every entry point validates the owner/repo argument against an anchored regex before any `gh` call. <!-- compose-doc-append-sentinel: branch=feature/1340-issue-setup pr=#1341 -->

### FEATURE: PR #1348 — fix/workflow-run-tests-1330 (2026-07-10, 65ced94072592bafa04dbdc6379c88484081e431, #1348)
Background: fix(#1330): shared-IR test detection + harden git non-exec + review coverage
Changes: #1330: Unified `hooks/workflow-run-tests.js` test-command detection onto the shared command IR. Removed its private `splitTopLevelSegments` + surface regex (`TEST_PATH_RE`) + segment-head read-only exclusion, which mis-fired on compound structures (`for f in tests/*.sh; do head -n 10 "$f"; done` matched `tests/` in the loop header and falsely demoted `run_tests`). Added `resolveEffectiveSegment()` to `hooks/lib/command-ir.js` to penetrate control-structure keywords (for/select/case headers and done/fi/esac terminators resolve to null; if/elif/while/until conditions and do/then/else bodies strip the keyword) and compose env-prefix stripping, so detection reads the real effective command per segment. Also: `resolveGitSubcommand()` skips leading git global options (`-C`/`-c`/`--git-dir`/`--no-pager` …) before the read-only subcommand check; sentinel-echo single-quote prefix fix; null-guards on `stripEnvPrefix`/`resolveEffectiveSegment` mirroring the sibling `resolveEffectiveCommand` in `hooks/lib/bash-write-patterns/segment-utils.js`. Test gap: no case exercised read-only commands nested inside compound control structures against the run-tests detection path — the surface regex matched the loop-header path and demoted `run_tests`. <!-- compose-doc-append-sentinel: branch=fix/workflow-run-tests-1330 pr=#1348 -->

### FEATURE: PR #1356 — refactor/enforce-worktree-config (2026-07-10, 0dc2a6364421e42b6b96266252b4536290f68991, #1356)
Background: refactor(#1343): unify ENFORCE_WORKTREE_EXCLUDE into path-coverage model; rename EXTRA_REPOS→ADDITIONAL_REPOS
Changes: Unified `ENFORCE_WORKTREE_EXCLUDE_REPOS` and `ENFORCE_WORKTREE_EXCLUDE` into a single `ENFORCE_WORKTREE_EXCLUDE` using a path-coverage model: glob entries (containing `*`) match via micromatch; plain path entries match via path-boundary prefix. New `hooks/lib/path-coverage-match.js` is the single source of truth for both repo-granularity (`config.js`) and file-granularity (`shared-cmd-utils.js`, `precommit-exclude-check.js`) exemption checks. `ENFORCE_WORKTREE_ADDITIONAL_REPOS` replaces `ENFORCE_WORKTREE_EXTRA_REPOS` (deprecated alias with one-time stderr warning). Deprecated `ENFORCE_WORKTREE_EXCLUDE_REPOS` also kept as an alias. Updated `.env.example`, `docs/architecture/claude-code/settings.md`, and `docs/parallel-sessions.md` to reflect the unified model. <!-- compose-doc-append-sentinel: branch=refactor/enforce-worktree-config pr=#1356 -->

### FEATURE: PR #1355 — feature/code-lang-check (2026-07-10, 05f247685eb9e91fcf446e2272f05accab0453ec, #1355)
Background: feat(#1180,#1278): add CODE_LANG commit-time language policy check + tests
Changes: feat(#1180): add CODE_LANG commit-time language policy check. Background: staged text files were not checked for language policy at commit time; CODE_LANG enforced plan artifacts but had no code-surface coverage. Changes: new hooks/lib/lint-commit-lang.js (90 lines) reads staged file list via git diff --cached -z (NUL-separated, handles special-char filenames), reads each blob via git show :path, scans for CJK (english policy) or English-only runs (japanese policy) per classifyPolicy(); violation snippets capped at 80 chars; fail-open (blob errors skipped, catch exits 0). CODE_LANG block added to hooks/pre-commit (43 lines, guarded by if [ -n "${CODE_LANG:-}" ], noop when unset). CODE_LANG documented in .env.example.;test(#1278): add 24-case test suite for CODE_LANG language policy check. Background: hooks/lib/lint-commit-lang.js had no test coverage. Changes: 43-line dispatcher tests/feature-1180-commit-lang-check.sh + 5 sibling files (lib.sh, group-u.sh, group-i.sh, group-u-postfix.sh, group-i-postfix.sh) with 24 PASS cases covering noop, english/japanese policy enforcement, file:line reporting, MAX_LINE=80 cap, secret-after-position-80 non-leak, multi-file scan, space-in-filename staged path, .env file CODE_LANG delivery, blob-fetch fail-open. 1 SKIP (tab-in-filename: Linux-only fixture per L3 gap). <!-- compose-doc-append-sentinel: branch=feature/code-lang-check pr=#1355 -->

### FEATURE: PR #1359 — feature/test-1113-1058-coverage-gate (2026-07-11, 530820a1814d35e7e82a5ea14c1ea0bccad37e88, #1359)
Background: feat(#1113,#1058): add Issues→Class-members coverage gate + structural detail existence gate
Changes: #1113: add `bin/check-issues-class-coverage` outline gate — block assembly when ## Issues count exceeds ## Class members count. Background: issues listed in intent ## Issues but absent from ## Class members produce no detail Step and ship uncovered; CONFIRM_DETAIL=off makes the gap undetectable without a structural gate. Integrated into `assemble-mandatory.sh` (fires on all `*-outline.md` output regardless of --source-kind) and `skills/make-detail-plan/SKILL.md` MDP-7 (detail existence gate). Also extended `outline-reviewer.md`, `detail-reviewer.md`, `supervisor-audit.md`, and `detail-planner.md` with symmetric soft-check directives.;#1058: extend `agents/supervisor-audit.md` and `agents/detail-planner.md` with Issues→Class-members gap as a named cross-stage coherence check and pre-draft self-check respectively. Background: #1058 identified that supervisor-audit and write-code pre-checks lacked systematic cross-stage coverage of the Issues→Class-members mapping; both agents now surface the gap explicitly before planning proceeds. <!-- compose-doc-append-sentinel: branch=feature/test-1113-1058-coverage-gate pr=#1359 -->

### FEATURE: PR #1360 — refactor/supervisor-rework (2026-07-11, 7128964, #1360)
Background: refactor(supervisor): compact output, CONV_LANG, alert pre-catch, Codex audit
Changes: #1342: Reworked EM Supervisor output for daily usability. Removed the `warning`/`notice` cumulative-severity advisory from the PostToolUse hook; only `error`-severity findings now emit `additionalContext`, cutting session noise. Added CONV_LANG support to `supervisor-findings-render.js` so block-reason text inherits the configured conversation language. Removed the C2 arm path from `supervisor-trigger.js` (OFF-sentinel PostToolUse events no longer arm `alert_armed_at`). Added `supervisor-off-proposal-shim.js` — a new PreToolUse hook that intercepts and blocks OFF-sentinel emit commands before execution, registered in `settings.json` under `Bash|runInTerminal|runCommands`. Deduplicated the Stop-hook audit trigger so consecutive Stop events in the same workflow step do not re-arm the audit cycle.;#1058: Added `checkSupervisorPreMerge()` to `hooks/workflow-gate.js`: supervisor state is now evaluated at the pre-merge (commit-push) boundary in addition to Stop-hook boundaries, so write-code phase intent violations that accumulated findings without triggering a Stop review are caught before the PR is created.;#1256: Lowered `AUDIT_SEVERITY_THRESHOLD` from `"error"` to `"warning"` in supervisor configuration; the audit mode now triggers on `warning`-severity cumulative findings, catching potential scope drift earlier in the workflow rather than only on confirmed error-severity events. <!-- compose-doc-append-sentinel: branch=refactor/supervisor-rework pr=#1360 -->

### FEATURE: PR #1362 — fix/1358-session-title-encodecwd-fix (2026-07-11, 4ce9732, #1362)
Background: fix(#1358): _encodeCwd ignores CLAUDE_PROJECT_DIR, uses cwd argument only
Changes: BUGFIX: fix(session-title): `_encodeCwd` ignores `CLAUDE_PROJECT_DIR`, uses cwd argument only — Background: concurrent CC sessions (e.g. agents + another repo) caused Bash subprocesses to inherit `CLAUDE_PROJECT_DIR` from the wrong session; `cc-session-title set-issue "$(pwd)"` passed the correct cwd explicitly but `_encodeCwd` prioritized `CLAUDE_PROJECT_DIR` over the argument, writing `custom-title` records to the wrong `~/.claude/projects/<dir>/` directory so a foreign session appeared in the workspace sessions list; Changes: removed `process.env.CLAUDE_PROJECT_DIR ||` from `_encodeCwd` — CLI always passes correct cwd via `$(pwd)`, linked-worktree correctness already handled by `CLAUDE_SESSION_JSONL_PATH`; added 9 test functions covering writeSetIssue/writeAddPr/writeMarkComplete × library/CLI + idempotency + special-char encoding; Test gap: no test verified that `CLAUDE_PROJECT_DIR` is ignored when an explicit `cwd` argument is passed. <!-- compose-doc-append-sentinel: branch=fix/1358-session-title-encodecwd-fix pr=#1362 -->

### FEATURE: PR #1363 — feature/wip-state-abandon (2026-07-11, 6f58b6e511fd921cb5e7e045f027d5eefa62d371, #1363)
Background: feat(#1336): add wip-state abandon verb + issue-state-check.sh
Changes: feat(wip-state): add `abandon` verb — Status=Todo + fingerprint clear + lock delete on OPEN issues; OPEN-only guard via new `issue-state-check.sh`; also hardens `validate_n` (rejects 0), `--repo` regex (leading-alnum required), `--repo=` empty-value; fixes `teardown_mock` contamination; 38 tests split into `tests/feature-wip-state/abandon/` subdir <!-- compose-doc-append-sentinel: branch=feature/wip-state-abandon pr=#1363 -->

### FEATURE: PR #1370 — feature/speculative-skip-a2 (2026-07-11, 2ef3c54cfc721a53d1396b9cd910061145e6ee32, #1370)
Background: feat(#1350): complexity evaluation SSOT — evaluate once at CI-C1b, persist to state.complexity_evaluation, read in MDP-3/WCD-3/WT-5
Changes: feat(#1350): S1–S6 complexity signals now evaluated once at clarify-intent Completion (CI-C1b) and persisted as `state.complexity_evaluation` top-level field. `make-detail-plan` MDP-3, `write-code` WCD-3, and `write-tests` WT-5 read the persisted verdict via `read-complexity-evaluation` CLI and fall back to direct evaluation only when the field is absent (fail-open for pre-existing sessions). Added `recordComplexityEvaluation` / `readComplexityEvaluation` / `hasComplexityEvaluation` API to `workflow-state` barrel, plus two new CLIs (`record-complexity-evaluation`, `read-complexity-evaluation`). <!-- compose-doc-append-sentinel: branch=feature/speculative-skip-a2 pr=#1370 -->

### FEATURE: PR #1366 — refactor/1364-cpr-principles (2026-07-11, c3eff4d925585e6c955a979caf3d484c055dc8ca, #1366)
Background: refactor(#1364): rename core-principles to CPR-N scheme + add CPR-3 Separate the Concerns
Changes: BUGFIX: stop-askuserquestion-required.js deleted — Background: hook was authored for #545 to enforce AskUserQuestion before USER_VERIFIED, but user-verified.md now uses Bash Allow/Deny dialog as the approval mechanism; AskUserQuestion is only called when check-verification-gate.sh returns a risk-category match. Hook blocked legitimate ENFORCE_WORKTREE=on merge flows. Line 62 also broke on first USER_VERIFIED in oldest-first tail scan causing false blocks. The obsolescence was demonstrated live during this session's /worktree-end: the hook — still loaded from session-start in the running process even after its deletion merged in PR #1366 — fired a Stop-hook block demanding an AskUserQuestion before a post-web-merge USER_VERIFIED sentinel, despite check-verification-gate.sh returning zero risk categories, forcing a spurious confirmation on an already-merged, already-closed cleanup. Changes: deleted hook file, removed settings.json Stop registration, deleted feature-545 test, removed row from feature-973 fixture table. Test gap: no test asserted that a USER_VERIFIED emit with zero risk-category matches must NOT require a preceding AskUserQuestion — the stale-hook Stop requirement was never tied by a regression guard to check-verification-gate.sh output.;REFACTOR #1364: core-principles.md CPR-N renaming + CPR-3 "Separate the Concerns" — Background: core-principles.md headings lacked stable addressable identifiers; cross-references used informal §N notation that became ambiguous on renumbering. Changes: renamed all 7 principle headings from `## N. Title` to `## CPR-N Title` scheme; added CPR-3 "Separate the Concerns" (5W1H partition reasoning — separate multiple phenomena/failure-conditions before reasoning) between CPR-2 (SSOT) and CPR-4 (Elevate Perspective); updated all live §N cross-references in agents/, rules/, skills/, bin/, install/ atomically (10 files). <!-- compose-doc-append-sentinel: branch=refactor/1364-cpr-principles pr=#1366 -->

### FEATURE: PR #1373 — fix/fix-1323 (2026-07-11, bc47fbc5277e8ce5e61deb4ca9407318d8773089, #1373)
Background: fix(#1323): narrow UNSAFE_REASON_CHARS to allow bare  in sentinel reason
Changes: BUGFIX | fix(#1323): narrow UNSAFE_REASON_CHARS to allow bare $WORD in sentinel reason text | Background: UNSAFE_REASON_CHARS = /[$`"]/ matched any $ regardless of context, causing isSentinelEchoSafe() to false-block sentinel echoes whose reason field contained literal $VAR tokens (e.g. $UNSAFE_REASON_CHARS as a label). Session 1e8a1aab saw 25+ false-blocks and 9 supervisor audit cycles from this. | Changes: Narrowed regex to /\$[({[]|[`"]/ — bare $WORD/$IDENTIFIER now yields "read" while $( ${ $[ backtick and double-quote remain blocked. Updated T3.37/T3.38 expected values (write→read). Added table-driven Group D2 regression tests T3.42–T3.46d. | Test gap: T3.37/T3.38 existed but expected "write" (masking the false-block); no test asserted that bare $WORD in reason yields "read" <!-- compose-doc-append-sentinel: branch=fix/fix-1323 pr=#1373 -->

### FEATURE: PR #1379 — feature/speculative-skip-a3 (2026-07-11, b5dc9719819844ab16c17755f4c0a354d62da118, #1379)
Background: feat(#1351): add resolveSkipConditionsFromComplexity — auto-satisfy outline/detail skip conditions for 0-signal-sonnet sessions
Changes: feat(#1351): speculative-skip A-3 — added `resolveSkipConditionsFromComplexity(sessionId, targetStep)` to `skip-signal-resolver.js` and wired it into CI-C1c (clarify-intent), MOP-1d, and MOP-C1 (make-outline-plan). For 0-signal-sonnet sessions the predicate auto-satisfies outline conditions (so_c1/so_c2) and detail conditions (sd_c1/sd_c2/sd_c3) without manual orchestrator evaluation. Requires verdict=sonnet AND signals=[] (verdict guard is load-bearing per SC-8). Returns null on any mismatch or error (fail-open), fully preserving the manual fallback path. Added 25 L1 unit tests. <!-- compose-doc-append-sentinel: branch=feature/speculative-skip-a3 pr=#1379 -->

### FEATURE: PR #1386 — feature/1382-complexity-level (2026-07-12, 087a699, #1386)
Background: refactor(#1382): complexity_evaluation stores level (high|low) instead of model name (opus|sonnet)
Changes: FEATURE #1382: rename `complexity_evaluation.verdict` → `.level`; stores `high|low` instead of model names `opus|sonnet`. Added backward-compat read-time shim in `readComplexityEvaluation` (maps legacy `opus→high / sonnet→low`). Consumer skills (write-code WCD-3, write-tests WT-5, make-detail-plan MDP-3) derive model inline (`level === "high" ? "opus" : "sonnet"`). CLI flag `--verdict` name preserved; accepted values changed from `opus|sonnet` to `high|low`. `judge-task-complexity.md` output format: `VERDICT: opus|sonnet` → `LEVEL: high|low`. Fixes CPR-7 (Name Reflects Substance) and CPR-2 (SSOT) violations where the stored field name implied a model-routing decision rather than the abstract complexity level it encodes. <!-- compose-doc-append-sentinel: branch=feature/1382-complexity-level pr=#1386 -->

### FEATURE: PR #1388 — fix/unblock-branch-d-redirect (2026-07-12, d28345f30e1cf8deb09750a50ca77abe88206705, #1388)
Background: fix(#1380): strip trailing shell redirects before enforce-worktree force-delete match (#1172)
Changes: BUGFIX #1380: the enforce-worktree branch-delete guard blocked authorized `/worktree-end` WE-19 and `/sweep-branches` force-deletes when the Bash tool appended a trailing shell redirect (`2>&1`, `>/dev/null`, `2>/dev/null`). `isWorktreeEndSkillForceDelete` / `isSweepBranchesSkillForceDelete` anchored their regex on `[ \t]*$`, which rejects any trailing redirect. Added `stripTrailingRedirects(cmd)` to `hooks/lib/command-parser.js` (strips fd-dup + unquoted file-redirect suffixes, attached & spaced, stacked; loop-until-stable; does not call `splitSegments`, does not fail-closed on chaining) and both predicates strip trailing redirects before the unchanged anchored match. CPR-5 symmetric; branch-name validation unchanged; classify() layer (#1253) untouched. Test gap: no test exercised either force-delete predicate with a trailing redirect suffix (`2>&1` / `>/dev/null`).;BUGFIX #1172: `WORKTREE_END_SKILL=1 git -C <path> branch -D <branch>` was not sanctioned when the `-C` path was quoted and contained spaces — `parseBranchDeleteTarget`'s pre-`branch` flag-value sub-pattern could not span a space inside a quoted `-C` value, returned `null`, and blocked before the force-delete predicate ran (the plan's "`-C <path>` already supported" held only for space-free paths). Widened the sub-pattern to accept quoted flag values (`"[^"]*"|'[^']*'|bare`); parse-only change, force-delete authorization unchanged (security-reviewed F4). Also added a `hasShellChaining(cmd)` defense-in-depth guard to `isWorktreeEndSkillForceDelete` for CPR-5 symmetry with `isSweepBranchesSkillForceDelete` (F3). Test gap: no test covered a quoted `-C "<path with space>"`. <!-- compose-doc-append-sentinel: branch=fix/unblock-branch-d-redirect pr=#1388 -->

### FEATURE: PR #1398 — fix/fix-1395-triage-cascade (2026-07-12, 4fb2e46c7c34453869983ac6468c0ecf0acb0319, #1398)
Background: fix(#1395,#377,#417,#437,#695,#827): issue-close-finalize triage cascade, sub-issue guards, and outcome coverage
Changes: fix(#1395): issue-close-finalize-triage resume_j and stuck_sentinel_only routes now include Step G (ICF-D parent-cascade) in NEXT_STEPS; without G the cascade loop never fired on already-closed meta parents;fix(#377): auto_close_path now warns to stderr when the already-CLOSED parent still has open sub-issues so orphaned children surface; pipeline proceeds regardless (warn, not gate);fix(#417): OPEN non-meta issue with open sub-issues now routes to skipped_open_sub_issues (exit 0 + warning) instead of falling through to the Phase-1-not-run error;fix(#437): regression test confirming compose-doc-append-entry idempotency guard suppresses duplicate history appends; no source change needed;fix(#695): issue-close-write-outcome.js --session-id mode now reads closes_issues siblings from intent.md and records subsumed entries for every N, not only the primary;fix(#827): already-closed issue re-run on resume_j path can now reach finalize_terminal once #1395 adds Step G; security: added session-id format validation in --session-id mode <!-- compose-doc-append-sentinel: branch=fix/fix-1395-triage-cascade pr=#1398 -->

### FEATURE: PR #1397 — feature/speculative-skip-complete (2026-07-12, 4245b87, #1397)
Background: feat(#1392,#1352,#544,#1353): speculative-skip engine, scope-change detection, judgment rename, settings auto-allow
Changes: #1392: Rename skip-condition evaluation path token 'manual' → 'judgment' in CI-C1c and MOP-1d/MOP-C1. Background: 'manual' was misread as user interaction; the path describes LLM orchestrator judgment evaluation. Applied to resolveSkipConditionsFromComplexity fallback output and to the corresponding prompt directives.;#1352: Add A-4 speculative-skip engine. Outline/detail skip sentinels now attach a pending skip_verdict to workflow state. A parallel skip-verifier subagent evaluates the skip and records confirm or veto via record-skip-verdict CLI. next-step gate at all-done state blocks on pending verdict, aborts on veto, and invokes write-tests on confirm or legacy no-verdict (fail-open). New API: recordSkipVerdict/readSkipVerdict/hasSpeculativeSkipPending in state-io.js; applyRecordedVerdictSkip call moved before inconsistency scan so a pending-with-valid-judgment step isn't incorrectly aborted. Adds agents/skip-verifier.md and bin/workflow/record-skip-verdict CLI.;#544: Add A-5 scope change detection in make-detail-plan. detect-scope-change.sh checks for class-member disposition changes, unexpected phase splits, and approach pivots relative to outline.md. MDP-7 gate invokes it before CONFIRM_DETAIL and surfaces a warning/block on significant drift.;#1353: Move WORKFLOW_OUTLINE_NOT_NEEDED and WORKFLOW_DETAIL_NOT_NEEDED from permissions.ask to permissions.allow in settings.json. A-4 skip-verifier + next-step gate replace the up-front approval prompt as the compensating control. <!-- compose-doc-append-sentinel: branch=feature/speculative-skip-complete pr=#1397 -->

### FEATURE: PR #1403 — fix/1396-labels-severity-model (2026-07-12, 9764a851b169791de77e141046bbd57652a88859, #1403)
Background: feat(#1396): add severity/model classification labels; /issue-create auto-attaches model:* from system-prompt injection
Changes: feat #1396: Add severity:high/low + model:* classification labels; /issue-create Phase 1 auto-classifies severity from issue content (fatal→high, cosmetic→low, else no label); Phase 4 auto-detects current model from system-prompt injection ("You are powered by the model named X") and attaches matching model:* label; unknown models get model:others; injection absent → model label skipped <!-- compose-doc-append-sentinel: branch=fix/1396-labels-severity-model pr=#1403 -->

### FEATURE: PR #1406 — fix/1391-gh-retire (2026-07-12, 0918854, #1406)
Background: fix(#1391): remove classify() temp-path redirect gate; retire kind:"gh" WRITE_PATTERNS group (#1296 partial)
Changes: BUGFIX #1391: Removed the enforce-worktree `classify()` IR temp-path redirect gate (`hooks/lib/bash-write-patterns/classify.js`). The gate demoted redirects whose write targets were all OS-temp paths to "read" via `isOsTempPath()`; because `isOsTempPath` matches ANY `/tmp/`, `/var/tmp/`, `/dev/shm/` (and Windows temp) path, a redirect into a repository *located under* `/tmp/` was misclassified as "read" and fast-allowed past main-worktree enforcement (root cause of the pre-existing R-8/R-19/R-20 failures). `classify()`'s contract is local-write detection, so temp-path redirects now correctly classify as "write" and the downstream IR scope pipeline decides allow/block. Bundled #1296 canary progress (partial retire): retired the `kind:"gh"` WRITE_PATTERNS surface-regex group so `isGhWriteIR` (IR-based) is the sole gh-write SSOT. This changes `classify(gh)` from "write" to "read" — semantically correct because gh commands write to GitHub, not the local worktree (they are gated separately at the enforce-worktree gh session-scope gate); existing tests asserting the old contract were corrected. Hardened `isGhWriteIR` to (a) skip leading gh global flags via `resolveGhSubArgv` so `gh -R owner/repo pr merge` stays gated (fixes an out-of-session gh-mutation bypass found in security review) and (b) cover the `-X=METHOD` api form. Added the L202 fast-allow `!isGhWriteCommand(ir)` exception so gh writes still reach the gh gate. Test gap: no test covered `classify()` for redirects into repos located under `/tmp/`, nor gh global-flag-before-subcommand detection in `isGhWriteIR`. <!-- compose-doc-append-sentinel: branch=fix/1391-gh-retire pr=#1406 -->

### FEATURE: PR #1409 — feature/1405-run-e2e-gate (2026-07-12, ab045dac74c95ab868bd4e19f73e9ae9eafdef51, #1409)
Background: feat(#1405): gate verification-gate ask behind RUN_E2E (off→skip / on→unchanged)
Changes: category:FEATURE subject:"#1405 — RUN_E2E-gated verification-gate ask" background:"The #833 verification-gate AskUserQuestion fired on every risk-category change before commit/merge, independent of E2E readiness. Under ENFORCE_WORKTREE=on there is no way to run the manual E2E check in place and no automated E2E environment exists, so the ask was pure friction — worsened by PR #1387 widening the fire path to the empty-staged branch-vs-main fallback." changes:"Added a RUN_E2E guard at the top of the Preflight section in skills/_shared/user-verified.md using the sanctioned bin/confirm-off RUN_E2E off reader (not raw get-config-var --is-off, preserving feature-confirm-off T17). RUN_E2E=off (default, unset included) suppresses all asks — the classifier still runs to log matched categories to WORKTREE_NOTES.md ## Unverified Categories with the distinct annotation 'skipped: RUN_E2E=off (E2E env not ready)'; on classifier exit 2/3 the log is skipped and only a stderr warning is emitted, so no ask can ever occur when off (CPR-8 totality). RUN_E2E=on runs the preflight unchanged; ERROR fail-safes to the ask. The single edit to the shared protocol is inherited by all USER_VERIFIED emission paths (worktree-end WE-4b/WE-7/WE-8, commit-push). bin/check-verification-gate.sh kept a pure classifier (no RUN_E2E awareness). Updated .env.example (dual responsibility, 'before commit or merge' wording), docs/ops.md, and rules/test.md for SSOT consistency. Reused existing RUN_E2E (#941); no new flag. Added tests/feature-1405-run-e2e-verification-gate.sh (14 structural cases including T17 non-regression and classifier purity)." <!-- compose-doc-append-sentinel: branch=feature/1405-run-e2e-gate pr=#1409 -->

### FEATURE: PR #1416 — fix/1166-closed-phase (2026-07-13, d16443005f6ceb3281bdc8e65346d4ad20bb577f, #1416)
Background: fix(#1166): rename alert_phase frozen→paused; introduce closed as permanent terminal phase
Changes: BUGFIX #1166: Rename alert_phase "frozen"→"paused"; introduce "closed" as permanent terminal phase set by SC-6 session-close. Root cause: dual semantics of "frozen" meant SC-6's session-close stop and #967's retry-exhaustion pause used the same value, so `ensureAlertScheduled` could not distinguish them and C2 review re-fired after Final Report. Fix: `paused` preserves #967 re-arm; `closed` blocks all transitions, short-circuits `ensureAlertScheduled`/`incrementAlertRetryCount`, and clears `alert_eligible_phase`. Legacy "frozen" aliased in terminal checks. Test gap: no test for alert_phase=closed preventing re-arm after SC-6 fires (now covered by T1/T2 in tests/fix-1166-closed-terminal-no-rearm.sh). <!-- compose-doc-append-sentinel: branch=fix/1166-closed-phase pr=#1416 -->

### BUGFIX: PR #1417 — fix/fix-882 (2026-07-13, c429926, #1417)
Background: fix(#882): extract session-bound worktree resolution into SSOT helper; wire RT-1/RT-4a to it
Changes: Extracted session-bound worktree resolution into `hooks/lib/workflow-state/resolve-worktree-path.js`: `isMainWorktree(dir)` (fail-close, execFileSync argv form) and `resolveSessionWorktreePath()` (fail-safe, reads state.cwd, rejects main worktree).;Added `bin/resolve-worktree-path` CLI wrapper: no-sid→empty, no-state→"NOSTATE", linked-wt→path, main/error→empty.;Added `skills/review-tests/scripts/select-staged-files.sh`: WORKTREE path→`git -C $WORKTREE diff --cached`; NOSTATE→cwd fallback; empty→exit 3.;Wired SKILL.md RT-0/RT-1/RT-4a to the SSOT: RT-0 resolves WORKTREE via CLI; RT-1 delegates file selection to select-staged-files.sh; RT-4a passes `${WORKTREE:-}` as argv[2] to compute-staged-tests-token.js.;Removed duplicate `isMainWorktree()` and execSync from `bin/compute-staged-tests-token.js`; wired to SSOT resolver. <!-- compose-doc-append-sentinel: branch=fix/fix-882 pr=#1417 -->
Test gap: no L2 test for RT-1 staged-file selection via session state before this fix

### BUGFIX: PR #1418 — fix/fix-1415 (2026-07-13, e2f3f783b48c366dc2211a27ac4d81ca648e71f4, #1418)
Background: PR #1418 merged on 2026-07-13.
Changes: fix(#1415): add record_type enum field to supervisor findings; supervisor-off-proposal-shim excludes escape_hatch_event from blockingFindings, resolving cycle where WORKTREE_OFF/WORKFLOW_OFF sentinel audit records blocked subsequent OFF sentinel emits in the same session <!-- compose-doc-append-sentinel: branch=fix/fix-1415 pr=#1418 -->

### FEATURE: PR #1420 — feature/canary5-6git (2026-07-13, 79547ed278cb7d8836b8db76c85a602af628fa12, #1420)
Background: feat(#1400,#1401): WRITE_PATTERNS→IR migration — retire green-group + git, typed write-target contract, fail-closed security convergence
Changes: FEATURE #1400: canary-5 — retire 12 "green-group" WRITE_PATTERNS surface-regex entries (posix-redir ×2, pwsh ×7, file-op rm/cp/mv ×3) from `hooks/lib/bash-write-patterns/patterns.js` and `STRIP_KINDS`. Write-detection for these groups is now performed exclusively by the IR-based predicates `isPosixRedirWriteIR`, `isPwshWriteIR`, and `isFileOpWriteIR` in `bash-write-targets.js`. Added typed write-target contract `{resolveVia:"ancestor"|"self", path}` at the `collectWriteTargetsFromSegments` collector layer; all downstream consumers (`bash-write-scope.js`, block hooks) updated to use `.path`. Migrate sibling test suites (feature-parallel-sessions-worktree-bash-patterns, fix-1391, fix-416, refactor-1294, fix-876, fix-fd-redirect) to assert new `classify()=="read"` + IR predicate contract.;FEATURE #1401: canary-6-git — retire 18 git WRITE_PATTERNS surface-regex entries. Introduce `isGitWriteIR` (read-allowlist + unknown→write fail-closed, basename-aware for /usr/bin/git/git.exe) in new `hooks/lib/bash-write-patterns/git-write-ir.js`. Add `extractGitWriteTargets` in `hooks/lib/bash-write-targets/git.js` returning `{resolveVia:"self", path:repoRoot}` self-target. Redesign `findRepoRootForBash` to resolve scope per write-segment's own IR argv (segment-aware, quote-aware; cross-segment/quoted --work-tree mis-scope → fail-closed to in-session). Add `resolveGitSubArgv` (skips git global flags via parse-git-args.js SSOT). Add `peelWrappers`/`scanWrappedVerb` with basename lookup so `/usr/bin/env git commit`, `/usr/bin/nice git commit` are caught. Add `isEverySegmentExcluded` git/gh per-segment write predicate (prevents `cp … && git commit` allow bypass). Add `isCommandSubstWriteIR` (writes in `$(…)`/backtick), `isNewlineInjectedWriteIR` (newline-injected writes), `isExoticExecWriteIR` (eval/xargs/find-exec/process-subst/sh-c/pwsh-Command). All exotic predicates wired into fast-allow gate, `isEverySegmentExcluded`, and `isReadOnlyInterpreterC`. Security: all fail-closed (unknown subcommand→write, ambiguous scope→in-session block, dynamic eval body→write). Canary suite 326 cases green. <!-- compose-doc-append-sentinel: branch=feature/canary5-6git pr=#1420 -->

### FEATURE: PR #1429 — fix/fix-supervisor-1342-1381-1374 (2026-07-13, 5f02c2f, #1429)
Background: PR #1429 merged on 2026-07-13.
Changes: BUGFIX #1342: SC-6 Final Report `findingsRender` call used `forFinalReport: true` (full per-finding detail dump); changed to `summaryOnly: true` to emit a count+severity summary line only, suppressing the verbose dump in session-close output.;BUGFIX #1381: SC-6 did not clear `audit_phase` / `audit_armed_at` after Final Report; added `--clear-audit-phase` flag to `bin/supervisor-write-audit` and wired it into the SC-6 completion block so audit state is reset on session close, eliminating the infinite audit re-arm loop that fired after every Final Report.;BUGFIX #1374: `checkSupervisorPreMerge()` blocked on any non-null `audit_verdict` including stale WARN verdicts (regression from PR #1360); fixed to skip the warning-flush block when a fresh non-BLOCK audit verdict exists (isAuditVerdictFresh: audit_last_run_at >= newest finding timestamp); only BLOCK verdict always gates; stale WARNs and fresh WARN/CONTINUE verdicts no longer block merges. Test gap: no test asserted that a WARN verdict with a timestamp older than the newest finding bypasses the pre-merge gate. <!-- compose-doc-append-sentinel: branch=fix/fix-supervisor-1342-1381-1374 pr=#1429 -->

### FEATURE: PR #1430 — feature/1145-we15-adaptive (2026-07-13, d278f895c2a858bb62c45c5679779b3112668658, #1430)
Background: feat(#1145,#1404): WE-15 adaptive block message + cleanup-cascade/SKILL.md guardrails
Changes: FEATURE #1145 #1404: Added adaptive OFF-block message in `supervisor-off-proposal-shim.js` that detects worktree-end phase via `isWorktreeEndEnv()` (new `hooks/lib/worktree-end-env-anchor.js`) and surfaces `/sweep-worktrees` + WE-16 fallback guidance instead of generic text. Strengthened `cleanup-cascade.md` WE-15/WE-16 and `SKILL.md` with explicit prohibition of `WORKTREE_OFF` and `--force`, and sweep-auto-reclaim guidance. <!-- compose-doc-append-sentinel: branch=feature/1145-we15-adaptive pr=#1430 -->

### FEATURE: PR #1433 — feature/test-design-split-1431 (2026-07-13, 251243173b74c74fa70e249cf35e84df805e4a28, #1433)
Background: feat(#1431): test-design.md progressive-disclosure split + Pattern 4 classifier coverage
Changes: feat(#1431): progressive-disclosure split of `skills/_shared/test-design.md` (288L → 180L entrypoint + `test-design/parser-regex-tests.md` + `test-design/protection-fix-tests.md`); added Classifier/guard cases Pattern 4 bullet to Test Case Categories (CPR-5 both-direction coverage, origin: #1425); updated `write-tests` WT-1 and `review-tests` RT-0a to read `rules/core-principles.md` first; wired `review-tests/scripts/run-codex-review-loop.sh` and `agents/test-reviewer.md` to load detail files conditionally; updated test comment pointers in feature-1308 and feature-1340 test helpers <!-- compose-doc-append-sentinel: branch=feature/test-design-split-1431 pr=#1433 -->

### FEATURE: PR #1434 — docs/issue-1428 (2026-07-13, 4fc6a17, #1434)
Background: feat(#1428): rename review-skill-size → review-prompt-size; extend to rules/*.md, agents/*.md, skills/_shared/*.md
Changes: Renamed `bin/review-skill-size` → `bin/review-prompt-size` and extended diff-mode and `--all` scan to cover `rules/*.md`, `agents/*.md`, and `skills/_shared/*.md` in addition to `SKILL.md`. Updated `rules/coding/file-split.md` Pattern B and `rules/prompt.md` scope definition. Updated all references in `README.md`, installer scripts, and `run-quality-gates.sh`. Closes #1428. <!-- compose-doc-append-sentinel: branch=docs/issue-1428 pr=#1434 -->

### FEATURE: PR #1437 — fix/1435-precommit-env (2026-07-14, 12bea8c, #1437)
Background: fix(#1435): block modified .env files in pre-commit (--diff-filter=AM)
Changes: BUGFIX: pre-commit `--diff-filter=A` → `AM` — block modified .env files at commit time (#1435). Test gap: no test verified that modifying an existing tracked .env was blocked; Test 7 expected rc=0 (gap). Added Test 7 rc=1 and Test 8 (gh api failure → treat as public). <!-- compose-doc-append-sentinel: branch=fix/1435-precommit-env pr=#1437 -->

### FEATURE: PR #1438 — fix/1436-fd-dup (2026-07-14, b7c4f91416c105fd1ccfd23a4f02105222c1f948, #1438)
Background: fix(#1436): exclude FD-to-FD redirects from isPosixRedirWriteIR write detection
Changes: Added `&& !/^&\d/.test(r.targetRaw)` to `writeRedirs` filter in `isPosixRedirWriteIR` (bash-write-targets.js:93) to exclude FD-to-FD redirects (`2>&1`, `>&2`) from write-target detection. Root cause: PR #1420 (2026-07-12) retired surface-regex entries with FD-to-FD negative-lookahead (from BUGFIX #243, PR #304) without porting the guard to `isPosixRedirWriteIR`. Four regression rows added to commit2-green-retire.sh POSIX_TABLE: PR-BUG-FD2/FD3 (Red→Green) and PR-BUG-FDQ/FDQ2 (always-true guard against `r.target`-based regression). Test gap: no `isPosixRedirWriteIR` regression test for FD-to-FD redirects (`2>&1`) as a non-write case. <!-- compose-doc-append-sentinel: branch=fix/1436-fd-dup pr=#1438 -->

### FEATURE: PR #1439 — fix/meta-filter-complexity-1427 (2026-07-14, f6c9163, #1439)
Background: fix(workflow-init): meta-label exclusion, Path A complexity recording, record-complexity-and-skip
Changes: BUGFIX #1427/#1394/#1413: Added meta-label exclusion axis to filter-primary-candidates.sh (extended Pass A to --json parent,labels; Pass B now drops meta candidates when at least one non-meta candidate survives). Added complexity evaluation + outline skip substeps to workflow-init Path A. Extracted the 3-step node sequence from clarify-intent CI-C1b/C1c into shared script bin/workflow/record-complexity-and-skip; rewrote both SKILL.md callers as thin wrappers. Fixed shell-variable interpolation into node -e double-quoted body (CWE-78): switched to process.env pattern. Split tests/feature-1351-skip-conditions-from-complexity.sh into _lib.sh + behavioral.sh to resolve HARD line-limit violation. Test gap: FP-10/FP-11/FP-12 tests for the meta-label exclusion axis were absent before this fix <!-- compose-doc-append-sentinel: branch=fix/meta-filter-complexity-1427 pr=#1439 -->

### FEATURE: PR #1451 — fix/1443-1442-worktree-context (2026-07-17, 9af4f07855de1502288e7e04aa2e776790830145, #1451)
Background: fix(#1443,#1442): WE-7/WE-8 linked-worktree CWD directives + CWD-independent session-ID fallback
Changes: #1443: WE-7/WE-8 in worktree-end SKILL.md now direct the model to keep CWD in the linked worktree until WE-13 (step text + Rules bullet, CPR-5 symmetric). New single-responsibility PostToolUse hook `hooks/detect-worktree-conflict.js` (registered in settings.json, matcher `Bash|runInTerminal|runCommands`) emits one non-blocking `additionalContext` guidance message when a failed command's stderr matches `fatal: '<branch>' is already used by worktree`. Deliberately one pattern + one message; generalization deferred to #1447. Test gap: no test asserted WE-7/WE-8 CWD directives or any guidance path for worktree-conflict git errors.;#1442: session-ID resolution no longer depends on CWD. Both `resolveWorkflowSessionId()` (new Priority 1d) and `resolveSessionId()` (new Priority 6c) gained a symmetric sibling-worktree scan that reads WORKTREE_NOTES.md Session-ID from linked worktrees after all env-var priorities fail: own worktree (CWD or ancestor) wins, a single sibling resolves, multiple distinct sibling IDs return null (fail-safe, no fall-through to depth-score/JSONL scans). Codex security review found and this session fixed a HIGH own-worktree false-ambiguity bug (subdirectory CWD collected own root as a sibling) via fail-before-fix tests. Test gap: no test called either resolver from a main-worktree or subdirectory CWD with session env vars absent. <!-- compose-doc-append-sentinel: branch=fix/1443-1442-worktree-context pr=#1451 -->

### FEATURE: PR #1458 — feature/workflow-init-driver (2026-07-17, cc0a35bea927848ba09fb97d0ce02be29cdadf92, #1458)
Background: feat(workflow-init): consolidate WI-2..WI-9 into resumable Node.js driver
Changes: #1446: Consolidated workflow-init's mechanical steps WI-2..WI-9 (issue-token detection, per-N `gh issue view` fetch, aggregate WIP check, CLOSED detection, label extraction, route decision, context.md write) into a resumable Node.js state-machine driver `bin/workflow/workflow-init-driver` + `bin/workflow/lib/workflow-init/` (7 phase modules, checkpoint.js, directive.js, spawn-env.js) emitting next-step-style `ACTION=` directives (invoke/done/blocked/ask_user/emit_sentinel) with checkpoint `--resume` support; SKILL.md rewritten as a driver loop (120→92 lines). Background: ds4-model sessions deviated from prose-step interpretation (WI-3 ambiguity, wrong ENFORCE_WORKTREE-block recovery). Includes the aggregate-WIP ALL_NONE unreachable-branch fix (evaluation order now error → any_other → all_none → all_same → mixed) and three security-review fixes (sentinel stripping restored in context.md write, CWE-77; CLAUDE_SESSION_ID validated against `/^[A-Za-z0-9_-]+$/` before path use, CWE-22; OPTIONS_DISPLAY percent-encoded symmetrically with QUESTION). Retired 4 scripts (aggregate-wip-check.sh, wip-set-resume.sh, closed-detection.sh, list-open-sub-issues.sh); new tests/feature-workflow-init-driver/ (103 assertions) + top-level dispatcher.;#795: Abolished the remaining "primary issue" concept — `filter-primary-candidates.sh` renamed to `filter-init-candidates.sh`; `path-a-label-and-board.sh` internal names PRIMARY/RELATED renamed to FIRST_N/SIBLINGS; all closes_issues entries are processed symmetrically in the driver's route decision.;#996: Removed all WORKFLOW_ABORTED_* sentinels (6 occurrences across workflow-init and clarify-intent SKILL.md) — audit found no consumer; abort paths now surface as driver `ACTION=blocked` or a plain stop. <!-- compose-doc-append-sentinel: branch=feature/workflow-init-driver pr=#1458 -->

### FEATURE: PR #1459 — fix/enforce-worktree-ir-regression (2026-07-17, ff050931, #1459)
Background: fix(enforce-worktree): restore scratchpad-redirect and New-Item-Directory allow paths lost in PR #1420 IR-signature migration
Changes: #1109: Investigated the 8 reported main-worktree false-blocks attributed to PR #1420's `collectBashWriteTargets` IR-signature migration (`(cmd)` → `(ir, repoRoot)`). The plans-dir heredoc / `node -e` writes were already green (PR #1179); the residual scratchpad-redirect variants were the regression. Fixed by teaching `expandStaticShellTokens` to expand `$SCRATCHPAD`/`${SCRATCHPAD}` only when they resolve under `<os-tmpdir>/claude/`, and adding the `areAllBashTargetsUnderClaude` allow predicate (fail-closed: allowed only when ALL targets resolve under the session scratchpad, tightened to `$SCRATCHPAD` when set). New SSOT `hooks/lib/claude-scratchpad-base.js` guards against TEMP/TMP poisoning by excluding paths that resolve inside the repo.;#1441: Restored the `New-Item -ItemType Directory` allow path that PR #1420 dropped when pwsh WRITE_PATTERNS were retired (on Windows, `findRepoRoot` on a non-existent external path fell back to process CWD → main repo root → block). Extracted the logic into `hooks/enforce-worktree/main-worktree-allows/new-item.js` with head-anchored dispatch, argv0 verification, and PowerShell single-quote literal semantics (`'$SCRATCHPAD/x'` stays literal → in-repo → block). The supervisor scratchpad stdout-redirect gap is covered by the same `areAllBashTargetsUnderClaude` predicate.;#1290: Same New-Item root cause as #1441 — `New-Item -ItemType Directory` issued during `/worktree-start` WS-5 (sanctioned parent-dir creation) is allowed again through the restored branch. Kept fail-closed for `-ItemType File`, in-repo targets, and chained commands.;#923: Confirmed `git worktree remove` (and add/prune) from the main worktree is allowed after restructuring `isAllowedWorktreeCommand` into a head-first dispatch (git-worktree vs New-Item) and splitting the 600-line module to satisfy the 500-line HARD limit (`worktree-command.js`, `worker-script.js`, `new-item.js`). Added a permanent regression canary (`tests/fix-1441-new-item-scratchpad-allow.sh`) so the restored allows cannot silently regress again. <!-- compose-doc-append-sentinel: branch=fix/enforce-worktree-ir-regression pr=#1459 -->
