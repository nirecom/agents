# History (agents)

## Archived
- [2026](history/2026.md) — 103 entries

## Archived
- [2026](history/2026.md) — 105 entries

## Archived
- [2026](history/2026.md) — 103 entries

### FEATURE: PR #814 — docs/claude-md-trim (2026-06-08, 8c64ac5f2aa70263e010502c050cbfa51a05e24f, #814)

Background: docs: trim CLAUDE.md verbose sections + add prompt brevity criteria

Changes: Trimmed CLAUDE.md WF-CODE-5/7/8/9/10/11/12 by removing skill-internals descriptions, historical see-issue pointers, and PreToolUse-hook explanations that the skill names already imply.;Extended `rules/prompt.md` with three new prompt-quality criteria, integrated into existing sections (Approach B): §1.4 "Every token counts" (Form), §2.3 "See issue only for complex background" and §2.4 "No post-invocation skill explanations" (Examples discipline). Standalone Brevity section avoided; SSOT remains §3.;Rewrote the supervisor-report reminder as a plain Notes bullet (not a blockquote callout). Per /deep-research findings (Anthropic 4.x guidance + Liu et al. position research), promoting a fallback item with decoration inverts the intended priority hierarchy (core-principles > workflow > supervisor-report-as-fallback). Changed "observe" → "encounter" and added scope qualifier "that core-principles + workflow don't resolve". <!-- compose-doc-append-sentinel: branch=docs/claude-md-trim pr=#814 -->

### FEATURE: PR #816 — fix/fix-802 (2026-06-08, e5c2e55297995e1123391303e367371648fd8458, #816)

Background: fix(#802): block interpreter-wrapper bypass of isAllowedWorktreeCommand

Changes: BUGFIX (#802): `bash -c 'git worktree remove X && rm -rf /'` from the main worktree was incorrectly allowed by `isAllowedWorktreeCommand`. Root cause: `hasShellChaining(cmd)` invokes `stripQuotedArgs(cmd)` first, collapsing the single-quoted body of `bash -c '...'` to `''`. The chaining detector then saw `bash -c ''` (no operators) and returned false, after which the predicate matched `git ... worktree remove` substrings inside the SQ literal on the raw cmd and returned true. Fix: added two guards to `isAllowedWorktreeCommand` in `hooks/enforce-worktree/main-worktree-allows.js`. Guard 1 is an interpreter-prefix reject placed as the first statement in the function — matches `^\s*(?:[A-Z_][A-Z0-9_]*=\S*\s+)*\b(bash|sh|zsh|dash|pwsh|powershell|cmd|node|python|perl|ruby)\b` and returns false, blocking direct interpreter wrappers plus uppercase env-prefix variants like `WORKTREE_END_SKILL=1 bash -c '...'`. Guard 2 is a belt-and-braces complete-shape gate placed after the existing git/worktree match — runs `stripQuotedArgs(cmd)` and rejects any residual `|`, `;`, `&`, `$(`, or backtick to document intent and guard against future `hasShellChaining` refactors. Test coverage: 4 new E2E cases (E-WT-REMOVE-4..7 in `tests/fix-595-bypass-e2e.sh`) and 4 new unit cases (S45..S48 in `tests/fix-enforce-worktree-main-cleanup.sh`); direct `git worktree remove "$path"` with DQ-quoted paths remains allowed (regression pin). Sibling allow predicates (`isAllowedFastForwardMerge`, `isAllowedMainWorktreeCleanup`, `isAllowedPushAllExcluded`) share the same vulnerability shape and are deferred to a follow-up issue per Accepted Tradeoffs. <!-- compose-doc-append-sentinel: branch=fix/fix-802 pr=#816 -->

### FEATURE: PR #817 — feature/808-sweep-enhancements (2026-06-08, 6d432af62f6d488678c914f1ae163beab395d244, #817)

Background: feat(#808,#809,#810): sweep series enhancements — --delete-no-pr, empty-parent sweep, /sweep-plans

Changes: #808: feat(sweep-branches): tri-state PR classification (open/merged/none/unknown) — open-first ordering prevents branch-name-reuse false positives; new --delete-no-pr flag age-gated by SWEEP_AGE_DAYS (default 30); reachability gate moved to classification pass so dry-run accurately predicts --apply output; new JSON counters: no_pr_candidates, no_pr_deleted, no_pr_skipped_young, no_pr_skipped_unreachable, pr_state_unknown, unmerged_pr_skipped; test file split into tests/feature-sweep-branches/{core,no-pr,remote,pr-state}.sh + _lib.sh dispatcher;#809: feat(sweep-worktrees): empty task-name parent directory sweep — new bin/sweep-worktrees/empty-parent.sh sourced module; depth-1 scan under WORKTREE_BASE_DIR; 4-gate safety pipeline (registered-worktree check via cross-repo git worktree list discovery + .git pointer safety net, any-children check for files AND subdirs, age gate); new counters: empty_parents_candidates, empty_parents_removed, empty_parents_skipped_{young,nonempty,registered}; function-form trap for tempfile cleanup in sweep-worktrees.sh; test file split into tests/feature-sweep-worktrees/{registry,orphan,gh-stub,empty-parent,validation}.sh + _lib.sh dispatcher;#810: feat(sweep-plans): new /sweep-plans sub-skill reclaims stale ~/.workflow-plans/ session artifacts — new bin/sweep-plans.sh with YYYYMMDD-HHMMSS + lowercase-UUID prefix grouping, SWEEP_AGE_DAYS age gate (default 30), TOCTOU re-check before deletion (prevents deleting revived sessions), errors schema as array (consistent with sweep-branches/worktrees), groups_skipped_revived counter; new skills/sweep-plans/SKILL.md wrapper; skills/sweep/SKILL.md updated to dispatch /sweep-plans; 10 test cases in tests/feature-sweep-plans.sh <!-- compose-doc-append-sentinel: branch=feature/808-sweep-enhancements pr=#817 -->

### FEATURE: PR #824 — fix/restore-confirm-plan-ux (2026-06-09, 8262cc2fbc3889941afec23d64ebda615fbab05c, #824)

Background: fix(#815): restore confirm-plan UX hooks omitted from PR #767 squash-merge

Changes: fix(#815): restore confirm-plan UX hooks omitted from PR #767 squash-merge. PR #767 (2026-06-05) landed only test files; 3 hook files (confirm-checkpoint.js, pr-created-open.js, vscode-open.js) + settings.json + 5 skill files remained untracked in source worktree. Restored via 3-way semantic port: Class A (3 new files) verbatim with two worktree-bug corrections (WORKFLOW_CONFIRM_* in permissions.ask not allow; vscode-open.js single-spawn fix from 1bed969); Class B (7 existing files) applied turn-marker.js peekTurnMarkers export, settings.json hook registrations, confirm-plan.md sentinel-emission protocol, and minimal sentinel-literal additions to 4 orchestrator SKILL.md files. <!-- compose-doc-append-sentinel: branch=fix/restore-confirm-plan-ux pr=#824 -->

### FEATURE: PR #835 — refactor/migrate-repo-askuser-gates (2026-06-10, #835)

Background: feat(#679): migrate-repo AskUserQuestion canary gates + tty-bypass-resistant pre-flight ack

Changes: FEATURE | migrate-repo: AskUserQuestion canary gates + tty-bypass-resistant pre-flight ack (#679) | Background: /migrate-repo used "STOP + get explicit user approval" directives that Claude could bypass by chaining Skill invocations; orchestrate.sh confirm() read stdin and was bypassable with `yes y |`; the existing-issues pre-flight check was guarded by FROM_STEP<=1 so resume invocations skipped it; `gh issue list` failure was silently treated as "no issues" (fails-open). Changes: replaced per-stage re-invocation with intra-skill AskUserQuestion gates at each canary transition; removed stdin-based confirm() and replaced with tty-bypass-resistant env-var gate (MIGRATE_ACK_EXISTING_ISSUES=1 required for live mode when existing issues are detected); pre-flight check moved before state_init and runs on every orchestrate.sh invocation; fail-fast on gh issue list non-zero exit; added review-code-size launcher to dotfileslink.sh and dotfileslink.ps1 (shim was missing from PATH despite binary existing in bin/). <!-- compose-doc-append-sentinel: branch=refactor/migrate-repo-askuser-gates pr=#835 -->

### FEATURE: PR #837 — fix/826-layer2-trigger (2026-06-10, c8252890b524437a8a772766013783f29c446556, #837)

Background: fix(#826): init layer2.next_check_at in appendFinding() + add supervisor-report directives to hotspot skills

Changes: BUGFIX | #826: EM Supervisor S-2 — fix Layer 2 trigger gap + improve L1 reporting coverage | Background: `appendFinding()` in `hooks/lib/supervisor-state-writer.js` never initialized `layer2.next_check_at`, so the supervisor-guard.js Stop hook never fired `ScheduleWakeup` even when Layer 1 findings existed. Additionally no SKILL.md referenced `supervisor-report`, creating a reporting coverage gap for hotspot skills. | Changes: added `ensureLayer2Scheduled()` helper invoked on both the normal-append and dedup-hit paths in `appendFinding()` (sets `layer2.next_check_at` to current ISO timestamp when null; guarded with `== null` to cover null and undefined; conditional write on dedup path to preserve no-op semantics); added one-line `supervisor-report` reference directive to 5 hotspot SKILL.md files (worktree-end, issue-close-finalize, make-detail-plan, write-code, run-tests); added regression tests in `tests/fix-826-layer2-trigger.sh` (A1-A4) <!-- compose-doc-append-sentinel: branch=fix/826-layer2-trigger pr=#837 -->

### FEATURE: PR #836 — fix/hardening-820 (2026-06-10, 39e8698265e8d74bd81c6777fb6367edab23abb9, #836)

Background: fix(#820,#822,#821,#823): enforce-worktree sibling-predicate hardening — interpreter-wrapper + RCE-flag guards

Changes: #820: Extracted `rejectInterpreterAndChaining()` and `rejectRceGitFlags()` helpers into `shared-cmd-utils.js` and wired them into all sibling allow predicates (`isAllowedFastForwardMerge`, `isAllowedPushAllExcluded`, `isAllowedMainWorktreeCleanup`). Fixed `isAllowedWorktreeCommand` C2 bug: `\bgit\b` + `\bworktree\b` checks now run on the STRIPPED command so SQ-body content cannot match. Added `rejectInterpreterAndChaining` guard to `isAllowedNewItemDirectory` (C3 fix). Extended `bash-write-patterns.js` `interpreter-c` regex for path-qualified forms and added `git-c-config-flag` WRITE pattern. Patched `isReadOnlyInterpreterC` to refuse single-segment `git <verb>` wrappers so the hook reaches the allow-predicate layer.;#822: `rejectRceGitFlags()` blocks `-c <key>=<val>`, `--upload-pack`, and `--receive-pack` in `isAllowedFastForwardMerge` and `isAllowedPushAllExcluded`. These flags enable command execution via the git transport layer (e.g. `core.sshCommand`, transport hooks).;#821: `rejectInterpreterAndChaining()` `startRe` covers path-qualified interpreter forms (`/bin/bash`, `\\UNC\path\bash`), repeated launcher chains (`env sudo bash`, `command exec bash`), and zero-or-more lowercase env-var prefixes before the interpreter name. `sepPathRe` catches path-qualified interpreters following shell separators.;#823: Stripped-gate in `rejectInterpreterAndChaining()` extended to detect literal newlines (`\n`), process-substitution input (`<(`), and process-substitution output (`>(`) in addition to the existing pipe/semicolon/ampersand/`$(`/backtick set. <!-- compose-doc-append-sentinel: branch=fix/hardening-820 pr=#836 -->

### FEATURE: PR #840 — fix/708-meta-parent-body-guard (2026-06-11, 17af2a8566bc4a3880f3d0d97642ef599e54801f, #840)

Background: feat(#708): add meta-label guard to parent-body-update.sh

Changes: Added meta-label guard to parent-body-update.sh: when a parent issue carries the `meta` label, the checkbox-flip step is skipped without calling `gh issue edit` (fail-safe: API failure also treated as meta). Added policy to rules/github-issues.md: meta parent bodies must not carry per-sub-issue completion state; GitHub native sub-issue progress UI is the SSOT. Removed the manually-maintained completion-state column from #708 body to eliminate the existing drift source. <!-- compose-doc-append-sentinel: branch=fix/708-meta-parent-body-guard pr=#840 -->

### FEATURE: PR #843 — fix/sweep-plans-orphan-prefixes (2026-06-12, 20c40bb, #843)

Background: fix(#829): widen sweep-plans regex for epoch-PID and empty-SID prefixes; add compose-doc-append-entry cleanup trap

Changes: fix(#829): widen `sweep-plans.sh` prefix regex to also match epoch-PID (`<10digits>-<pid>-`) and empty-SID (`-` prefix) orphan staging files; use EMPTY_PREFIX_KEY sentinel for bash associative array compatibility; add `cleanup_staging()` trap (EXIT HUP INT TERM) to `compose-doc-append-entry` so staging files are removed on abnormal exit; retain end-of-script cleanup as belt-and-suspenders; add `--repo`, `--history`, `--changelog` flags with mutual-exclusivity and format validation; anchor `ROTATE_DIR` rm -rf to STAGING_DIR <!-- compose-doc-append-sentinel: branch=fix/sweep-plans-orphan-prefixes pr=#843 -->

### FEATURE: PR #844 — fix/fix-825-compose-doc-append (2026-06-12, 218b423, #844)

Background: fix(#825): allow compose-doc-append-entry invocation from main worktree

Changes: #825 (BUGFIX): fix(worktree-end WE-20): allow compose-doc-append-entry bash-script invocation from main worktree. Root cause: doc-append-worker subagent was using `| tee` / `>` redirect for log capture in compose mode, causing `classify()` to return "write" and enforce-worktree.js to block the command. Root fix: hardened `agents/doc-append-worker.md` — step 3 now mandates Write tool for log capture; prohibited all shell operators (`| tee`, `>`, `>>`, `2>`, `2>&1`) in the compose dispatch Bash command. Defense-in-depth: added `isAllowedComposeDocAppend()` predicate to `hooks/enforce-worktree/main-worktree-allows.js` that explicitly pins the allow contract for the canonical `bash "$AGENTS_CONFIG_DIR/bin/compose-doc-append-entry" ...` double-quoted-path shape; raw argTail scan (no stripQuotedArgs) catches all injection vectors including `>`, `<`, `$()` inside double-quoted values; wired into dispatch chain in `hooks/enforce-worktree.js` after `isAllowedMainWorktreeCleanup`. 23 test cases in `tests/fix-825-enforce-worktree-compose-doc-append.sh`. <!-- compose-doc-append-sentinel: branch=fix/fix-825-compose-doc-append pr=#844 -->

### FEATURE: PR #845 — feature/833-e2e-qa-gap (2026-06-12, 080b3a46a66aa2c3352d68046590240a36aff52a, #845)

Background: feat(#833): E2E QA gap — verification gate + L1/L2/L3 policy + Test gap: field

Changes: FEATURE | #833: E2E QA gap — generalized test-env/user-env divergence prevention framework | Background: agents repo lacked structural enforcement to prevent test-env/user-env divergence regressions (bash tests passing while pwsh fails; mock-based hook tests passing while real-session behavior differs). Three root gaps: (1) skills/_shared/test-design.md §Mandatory integration was prompt-only with no machine-checkable gate; (2) the only real `claude -p` test was hidden behind RUN_E2E=1 and not visible in the default suite; (3) history.md regression records had no field capturing "what test was missing". | Changes: added `bin/check-verification-gate.sh` — standalone, testable bash classifier (4 categories: pwsh-required, hook-registration, skill-orchestration, installer) with structured TAB-separated stdout (CATEGORY/QUESTION per line) and 22-case test suite; wired as preflight in `skills/_shared/user-verified.md` shared protocol so every `<<WORKFLOW_USER_VERIFIED>>` emission path inherits the gate (AskUserQuestion fires only when staged files match a risk category); added L1/L2/L3 policy section to `rules/test.md` with closest-to-action verification principle and L3 aspiration (docker/VM, real device); added L2 fallback gap-documentation template (`# L3 gap` block) and pwsh-required/pwsh-not-required tag docs to `skills/_shared/test-design.md`; added `Test gap:` field to BUGFIX history format in `rules/docs/history.md`; added `--test-gap TEXT` flag to `bin/doc-append.py` with structural BUGFIX stderr warning when omitted.;FEATURE | #833: review_tests structural gate — sentinel-driven AND-gate with stale-token detection | Background: The /review-tests skill had no workflow-level enforcement; the commit gate did not check whether a review had run, so /write-code could follow /write-tests with no structural guard. The E2E gap: staged tests could be committed without any coverage verification step ever blocking. | Changes: added `review_tests` to VALID_STEPS (after write_tests) with dedicated `WORKFLOW_REVIEW_TESTS_COMPLETE: token=<hex>` sentinel; `computeStagedTestsToken()` in `hooks/workflow-gate/review-tests-evidence.js` fingerprints staged tests/ via SHA-256 so the gate re-blocks when tests are re-edited after a passing review (stale-token detection); `review-tests-handler.js` intercepts the sentinel and records token + optional warnings_summary; unresolved warnings always block regardless of staged token state; SKILL.md updated to emit the token-bearing sentinel and compute it via the evidence helper; WRITE_TESTS_NOT_NEEDED symmetrically marks review_tests skipped; evidence bypass symmetry: when write_tests is satisfied by staged files, review_tests shares the same evidence for single-commit workflows; CLAUDE.md WF-CODE-4 updated; rules/test.md AND-gate section added; 23 new tests added (gate scenarios A1–D6 + C9 regression guard, sentinel SSOT T1–T12 including T8b/T8c for COMPLETE_LOOKSLIKE). <!-- compose-doc-append-sentinel: branch=feature/833-e2e-qa-gap pr=#845 -->

### FEATURE: PR #849 — fix/842-confirm-coemission (2026-06-12, 6f8418bdafccf844376753e7172a7284772ea663, #849)

Background: fix(#842): co-emission directive + workflow-mark.js structural fallback + .env-aware CONFIRM_*

Changes: Background: PR #824 (2026-06-09) introduced the CONFIRM-series sentinel gate. Two follow-up defects were observed: (1) when a CONFIRM sentinel was the last tool call in an assistant turn, Allow returned control but no subsequent tool call was queued, stalling the workflow; (2) confirm-checkpoint.js read process.env[flagName] without loading .env, so CONFIRM_*=off set only in $AGENTS_CONFIG_DIR/.env was invisible to the hook and the dialog fired regardless of the setting.;Changes: (a) Added co-emission requirement directive to confirm-plan.md Step 3 ON path (SSOT) — the CONFIRM sentinel echo must share the assistant response with the subsequent tool calls. (b) One-line reminders added at the three caller emit sites: clarify-intent Step 5 (CONFIRM_INTENT), make-outline-plan Step 8 (CONFIRM_OUTLINE — names all three required co-emits: MARK_STEP_outline_complete + OUTLINE_PLAN_COMPLETE + make-detail-plan invocation), make-detail-plan MDP-7 (CONFIRM_DETAIL). (c) confirm-checkpoint.js now calls loadDefaultEnv() at the start of its main block, mirroring show-diff.js. (d) New tests/feature-842-confirm-coemission-static.sh locks the prompt-file additions; T12 in tests/feature-confirm-checkpoint.sh proves .env-only CONFIRM_* flags are honored.;Background (follow-up within the same PR): the prompt-only co-emission directive relies on the LLM following the rule — a single "forgot to co-emit" still stalls the workflow. End-to-end integrity requires a structural recovery path independent of the LLM's compliance.;Changes: (e) New hooks/workflow-mark/confirm-next-step-handler.js recognizes CONFIRM_{INTENT,OUTLINE,DETAIL} sentinel echoes in PostToolUse Bash output and injects an equivalent next-step hint via additionalContext — same mechanism the MARK_STEP_* sentinels already use. (f) Added CONFIRM_{INTENT,OUTLINE,DETAIL}_RE_DQ + LOOKSLIKE regexes to hooks/lib/sentinel-patterns.js and registered them in isSentinel() / isStrictSentinel(). (g) Added CONFIRM_NEXT_STEP_HINT table + confirmNextStepHint() helper to hooks/lib/workflow-state.js. (h) Wired the new handler into hooks/workflow-mark.js dispatch chain (after notNeededHandlers, before clarifyIntentCompleteHandler). (i) New tests/feature-842-confirm-next-step-hint.sh exercises the handler end-to-end (14 assertions across 6 test cases); section 6 added to tests/feature-842-confirm-coemission-static.sh asserts the handler/dispatch/regex/hint-table wiring.;Background (final simplification within the same PR): with the structural fallback in place, the co-emission directive is no longer a correctness requirement — it is at most a perf optimization (saves ~1 inference round per CONFIRM stage). Per rules/prompt.md §1.4 ("Every token counts"), the directive was removed to keep confirm-plan.md minimal and to avoid teaching the LLM a forced-call pattern that could misfire when the next step is not yet in context.;Changes: (j) Removed the "Co-emission (preferred path)" bullet and "Structural fallback" wording from confirm-plan.md Step 3; replaced with a single sentence describing the workflow-mark.js automatic next-step injection. (k) Removed "(co-emit Completion tool calls — see confirm-plan.md Step 3)" parentheticals from clarify-intent Step 5 / make-outline-plan Step 8 / make-detail-plan MDP-7 — kept generic "per protocol Step 3" wording. (l) Static test refactored: section 1 now asserts confirm-plan.md cites workflow-mark.js + additionalContext (instead of "Co-emission (preferred path)"); section 5 now asserts each caller invokes skills/_shared/confirm-plan.md (instead of the literal "confirm-plan.md Step 3" pointer); section 2 (co-emit reference in clarify-intent) removed. <!-- compose-doc-append-sentinel: branch=fix/842-confirm-coemission pr=#849 -->

### FEATURE: PR #851 — fix/846-settings-drift (2026-06-12, 9d16a2469ce98666a805b7f71bb746f0132ae0d3, #851)

Background: feat(#846): add layered settings.json drift prevention (post-merge/checkout hooks + session-start backstop)

Changes: #846: Add layered settings.json drift prevention. `hooks/post-merge` and `hooks/post-checkout` auto-reassemble `~/.claude/settings.json` when `settings.json` or `settings-extension.json` changes during a merge or branch switch. `hooks/session-start.js` detects missing entries at session start and injects a WARNING into `additionalContext` as a backstop. Drift detection (`hooks/lib/settings-drift.js`) uses multiset counting (Map) for hook matchers to handle the many-to-one matcher pattern. Architecture doc updated with layered defense summary. <!-- compose-doc-append-sentinel: branch=fix/846-settings-drift pr=#851 -->

### BUGFIX: PR #850 — fix/834-toctou-option-gamma (2026-06-12, #850)

Background: fix(#834): migrate-repo TOCTOU pre-flight gate — Option γ Layer P/C + preview-and-capture.sh

Changes: #834 — BUGFIX: migrate-repo: TOCTOU in MIGRATE_ACK_EXISTING_ISSUES acknowledgement gate. Background: PR #835 added MIGRATE_ACK_EXISTING_ISSUES=1 as a tty-bypass-resistant pre-flight gate, but the skill set it unconditionally in Steps 4-10 regardless of dry-run output. External issues created between dry-run and canary-1 silently bypassed the gate, losing the "early issue numbers = history chronology" invariant. Changes: Added Option γ two-layer gate to orchestrate.sh. Layer P validates presence and integer format of MIGRATE_ACK_UP_TO_ISSUE_N and MIGRATE_ACK_SELF_COUNT_AT_ACK. Layer C compares current highest issue number against the dry-run snapshot plus self-issue delta, allowing for migration's own canary issues without false positives. New helper skills/migrate-repo/scripts/preview-and-capture.sh runs dry-run, mirrors output to stderr, and exports the snapshot pair for eval. SKILL.md Step 2 updated to use the helper; Steps 4-10 propagate both env vars. New test file tests/feat-migrate-repo-preflight-gamma.sh adds PF5-PF12 covering Layer P/C scenarios. gh-mock.sh updated to support --jq for issue list and MOCK_HIGHEST_ISSUE_N fixture control. <!-- compose-doc-append-sentinel: branch=fix/834-toctou-option-gamma pr=#850 -->

### FEATURE: PR #841 — feature/831-supervisor-l1-coverage (2026-06-13, c4ccfdd, #841)

Background: feat(#831): L1 reporting coverage — supervisor-emit facade + hook auto-report + sid auto-resolve

Changes: feat(#831): L1 reporting coverage — new hooks/lib/supervisor-emit.js fail-open facade (reportBlock/reportFallback/reportSentinel/reportRetrospective); bin/supervisor-report --session-id now optional with 4-level auto-resolve chain (CLAUDE_SESSION_ID → CWD WORKTREE_NOTES.md → git common-dir WORKTREE_NOTES.md → resolveSessionId() JSONL scan); enforce-worktree.js + workflow-gate.js + enforce-issue-close.js call reportBlock() on every block exit via _reportContext pattern; enforce-override-handlers.js calls reportSentinel() for WORKTREE_OFF/WORKFLOW_OFF events (OFF only, no ON); skills/session-close Step 3.5 retrospective pass before Final Report; 12 SKILL.md files gain supervisor-reporting.md reminder in Rules section; 3 new test files (supervisor-emit, sid-fallback, auto-report-integration) <!-- compose-doc-append-sentinel: branch=feature/831-supervisor-l1-coverage pr=#841 -->

### FEATURE: PR #854 — fix/issue-739-enforce-worktree-sequenced-exclude (2026-06-13, fab5373d4d48c0edbb5901dddef7b1fcc2bdc2f4, #854)

Background: fix(#739): generalize enforce-worktree exclusion model for sequenced commands

Changes: BUGFIX | #739: enforce-worktree sequenced-command write-target exclusion (fix-of-fix) | Background: PR #754 (2026-06-03) added BUILTIN_EXCLUDE_PATTERNS and a parseFailure cp bypass to allow /worktree-end Step 5 backup writes. Two gaps remained and caused a regression in PR #844 (2026-06-11): (1) when the backup worker chained `mkdir -p && cp` the `hasCommandSequencing()` gate skipped both fast-paths unconditionally, falling through to BLOCK; (2) bare `cp` with `$VAR` tokens in the destination and no `WORKTREE_END_SKILL=1` prefix hit parseFailure but missed the bypass (prefix required). | Changes: added `isEverySegmentExcluded()` to `hooks/enforce-worktree/bash-write-scope.js` — splits at unquoted `&&`/`||`/`;` via a new SSOT `hooks/lib/shell-segments.js`, classifies each segment (read=transparent, write=all targets must be excluded), and allows only when every write-classified segment targets exclusively excluded paths; wired into `enforce-worktree.js` as an `else if` arm on the `hasCommandSequencing` branch; added env-prefix literal resolver in `extractCpMvDestination` (`hooks/lib/bash-write-targets.js`) that substitutes `$KEY` tokens using same-command `KEY=VALUE` env-prefix tokens (process.env not consulted; traversal-bearing values rejected); refactored `hooks/lib/merge-detect.js` to consume `splitShellCommands` from the new SSOT. Test gap: no test for `mkdir -p ... && cp ... .worktree-backup/...` (the hasCommandSequencing gate was never exercised with excluded-only targets across all segments). <!-- compose-doc-append-sentinel: branch=fix/issue-739-enforce-worktree-sequenced-exclude pr=#854 -->

### FEATURE: PR #859 — fix/842-confirm-hint (2026-06-13, dbe201b09e8f8bcaee3de016e50020f38e7518e4, #859)

Background: fix(#842): simplify CONFIRM_NEXT_STEP_HINT table to single-sentence directives

Changes: BUGFIX | #842: CONFIRM_NEXT_STEP_HINT table simplified to single-sentence directives (re-fix) | Background: PR #849 added the structural fallback (confirm-next-step-handler.js injects next-step hint via additionalContext after CONFIRM Allow). Transcript evidence from session 20260613-014611 confirmed `additionalContext` was delivered correctly but the LLM stalled — root cause was hint verbosity: the 5-step multi-tool instructions in CONFIRM_NEXT_STEP_HINT were interpreted as information rather than imperatives to execute. USER_VERIFIED hint (one verb, one sentence) never stalls. | Changes: replaced all three CONFIRM_NEXT_STEP_HINT entries (intent/outline/detail) with single-sentence directives matching USER_VERIFIED brevity; updated tests/feature-842-confirm-next-step-hint.sh assertions to drop T1b/T2b/T2c/T3b (verbose-sentinel checks no longer applicable) and add T1-new (GitHub reconciliation ping). | Test gap: no test checked hint brevity/complexity; existing assertions only verified string containment, not that the hint was short enough to trigger execution rather than analysis. <!-- compose-doc-append-sentinel: branch=fix/842-confirm-hint pr=#859 -->

### FEATURE: PR #863 — fix/conv-lang-inject (2026-06-15, b0204513c8520cca0df09bd37d59526e3acfa1e4, #863)

Background: fix(#861): inject CONV_LANG into session-start + post-compact additionalContext

Changes: FEATURE | #861: CONV_LANG injection into session-start + post-compact additionalContext | Background: CONV_LANG=japanese in .env was a no-op — env.example documented it as "Prompt-only (no hook enforcement)" and no injection mechanism was ever implemented. Prior behavior came from rules/language.md (a dotfiles-private symlink, now being removed) and check-ask-lang.js (deleted in #645 due to CJK detection failures), leaving Claude with no language directive from any hook or context. | Changes: new hooks/lib/conv-lang.js helper reads CONV_LANG via loadDefaultEnv(), normalizes (trim + toLowerCase), returns null for unset/empty/english/control-char values, otherwise returns "Respond to the user in <lang>."; session-start.js and post-compact.js both require the helper and push the injection string into their additionalContext lines array when non-null; both injection sites wrapped in fail-open try/catch so a helper failure never crashes the hook; .env.example CONV_LANG comment updated with what/can't/format per rules/docs/env-example.md. <!-- compose-doc-append-sentinel: branch=fix/conv-lang-inject pr=#863 -->

### FEATURE: PR #864 — fix/fix-416-unsafe-reason-chars (2026-06-15, 5e0b3094f9797b4498a1fafd37260895e04a6141, #864)

Background: fix(#416): narrow UNSAFE_REASON_CHARS to 3-char DQ expansion set

Changes: BUGFIX | #416: bash-write-patterns: narrow UNSAFE_REASON_CHARS to 3-char DQ expansion set (fix-of-fix for PR #787) | Background: PR #787 (2026-06-06) introduced a regression: UNSAFE_REASON_CHARS = /[$`|;&()<>\\"]/ included shell metacharacters (|, ;, &, (, ), <, >, \) that are literal inside bash double-quoted strings. Canonical WORKFLOW_BRANCHING_COMPLETE (| separators + Windows \ paths) and WORKFLOW_PREMISE_FAIL (parens in reason) were falsely classified as write and blocked by enforce-worktree.js. | Changes: narrowed UNSAFE_REASON_CHARS from 11-char to 3-char set /[$`"]/ (only bash DQ expansion triggers: dollar, backtick, quote). Added 5 positive fixtures T3.13d-h (BRANCHING_COMPLETE |/\ forms, PREMISE_FAIL parens, USER_VERIFIED ;/\) and 5 negative T3.37-41 ($VAR, \$, backtick, \", ${VAR}); flipped T3.24/T3.34/T3.35 write to read. Comment block updated with DQ-context rationale and bare-\ safety argument. | Test gap: no positive fixture for BRANCHING_COMPLETE canonical |/\ form or PREMISE_FAIL parens before this PR — the two sentinel types most affected by the regression. <!-- compose-doc-append-sentinel: branch=fix/fix-416-unsafe-reason-chars pr=#864 -->

### FEATURE: PR #862 — fix/fix-573-rm-quote-tokenizer (2026-06-15, 87657d2edb38234099f58758e6bac2fe63ad121d, #862)

Background: fix(#573): upgrade extractRmTargets to quote-aware tokenizer

Changes: BUGFIX | #573: extractRmTargets quote-aware tokenizer (enforce-worktree re-fix) | Background: PR #584 introduced extractRmTargets but left all quoted rm paths fail-closed, blocking rm of non-repo paths like the memory directory while Write tool was unaffected. | Changes: Upgraded extractRmTargets to quote-aware tokenizer (tokenizeRmArgs) in hooks/lib/bash-write-targets/rm.js; added ENOENT walk-up in findRepoRoot (git-repo-detection.js); split hooks/lib/bash-write-targets.js into bash-write-targets/ subfolder (redirect, tee, pwsh, cp-mv, rm, staged) per 500-line HARD limit. | Test gap: no test for quoted rm paths existed after PR #584, allowing the quote fail-closed case to survive undetected. <!-- compose-doc-append-sentinel: branch=fix/fix-573-rm-quote-tokenizer pr=#862 -->

### FEATURE: PR #865 — fix/853-fix-stale-assertions (2026-06-16, ff1d34f79799e22c850e4d37fc6a69b70dc8cbd4, #865)
Background: fix(#853): fix 11 stale assertions across 3 test files
Changes: BUGFIX #853: tests: fix 11 stale assertions across 3 test files — feature-confirm-flags-static.sh (4 fixes: exclude make-detail-plan from get-config-var loop after delegation to confirm-plan.md; update outline/detail needle texts to match current SKILL.md); feature-workflow-transition-hint.sh (4 fixes: add workflow_init step to STATE_CLARIFY_COMPLETE + ALL_COMPLETE_STATE helpers per PR #273 VALID_STEPS change; update SS-1c assertion from "clarify-intent" to "workflow-init"; advance fixture dates past cleanupZombies 7-day TTL); main-workflow-skip-sentinels.sh (3 fixes: add EMPTY_TRANSCRIPT_DIR mock for WS-SK-NO-SID tests to block PR #532 JSONL-scan fallback from resolving real session IDs in dev env) <!-- compose-doc-append-sentinel: branch=fix/853-fix-stale-assertions pr=#865 -->

### FEATURE: PR #869 — fix/856-issue-close-skill-docs (2026-06-16, 3a1d15e7d601fc9a12eee208c86d17025b10b198, #869)
Background: fix(#856): document exact ISSUE_CLOSE_SKILL=1 inline bypass form
Changes: Fix misleading ISSUE_CLOSE_SKILL=1 bypass documentation in rules/github-issues.md — Environment bullet conflated the strict inline-form bypass (INLINE_SKILL_RE) with the broader env-export bypass and incorrectly listed `gh issue comment` as gated by enforce-issue-close.js, causing a bypass attempt with `--comment` to fail in session 20260613-014611. Rewrote the bullet to distinguish the two bypass mechanisms, state the exact accepted inline shape verbatim (`ISSUE_CLOSE_SKILL=1 gh issue close <N> --reason completed`), drop the `gh issue comment` mention, and add an SSOT cross-reference to INLINE_SKILL_RE in hooks/enforce-issue-close.js. <!-- compose-doc-append-sentinel: branch=fix/856-issue-close-skill-docs pr=#869 -->

### FEATURE: PR #873 — fix/857-supervisor-l2-invocation (2026-06-16, 06f85a0af5c2610f43236730fb2df0e52dbd9b91, #873)
Background: fix(#857): convert EM Supervisor Layer 2 to Stop-hook block
Changes: #857: EM Supervisor S-2.5 — convert Layer 2 to Stop-hook block (close advisory-only gap) | Background: Layer 2 never executed in any session (all 15 samples showed last_run_at: null). Root cause: ScheduleWakeup advisory in supervisor-guard.js is a no-op in normal interactive sessions (only works in /loop dynamic mode); 5-min timer in supervisor-trigger.js had no valid exit path. | Changes: supervisor-trigger.js — abolished 5-min wall-clock timer; added C2 detection (ENFORCE_*_OFF sentinel commands set next_check_at). supervisor-guard.js — removed ScheduleWakeup advisory; added detectSentinelHang() (reads last assistant turn's content array from transcript JSONL, returns true when a MARK_STEP Bash tool_use is last with no following tool_use); replaced advisory block with 5-way branch where path (3) detectSentinelHang||nextCheck emits decision:block (exit 2), triggering inline L2 review. agents/supervisor.md — updated role to Stop-hook-block invocation context; added output steps 4-5 (first-aid guidance + recommend /issue-create, non-auto). <!-- compose-doc-append-sentinel: branch=fix/857-supervisor-l2-invocation pr=#873 -->

### FEATURE: PR #874 — fix/832-auto-accept-class-members (2026-06-16, e8a18b08ce239a7411f0c7d7906aad7895c7533f, #874)
Background: feat(#832): auto-accept class member triage, add MUST→OPTIONAL→NA sort
Changes: category=FEATURE subject="#832: class-members-proposal: auto-accept triage, MUST→OPTIONAL→NA sort" background="At the intent stage users lack sufficient context to meaningfully override heuristic triage values. The Phase B AskUserQuestion gate added an interview round but produced no actionable signal; decision: auto-accept the Phase A proposal and defer disposition refinement to outline/detail planners." changes="Removed Phase B (AskUserQuestion 'Accept / Modify?') and Phase C (Modify free-text parse + Round budget) from class-members-proposal.md. Phase A renamed to auto-accept variant; MUST→OPTIONAL→NA sort directive added. Removed 2-line stale Phase C reference from clarify-intent SKILL.md. Removed N11b-lib and N14 assertions from feature-clarify-intent.sh; added N17/N18/N19 regression guards (auto-accept, sort directive, Phase C removal)." <!-- compose-doc-append-sentinel: branch=fix/832-auto-accept-class-members pr=#874 -->

### FEATURE: PR #870 — fix/867-split-tests (2026-06-16, b0f8366f364cd34a7fc8f5f5bec1c1ffe7f6af85, #870)
Background: PR #870 merged on 2026-06-16.
Changes: REFACTOR | #867: split tests/main-workflow-skip-sentinels.sh into tests/main-workflow-skip-sentinels/ sibling folder to resolve 500-line HARD limit (1307 → 219-line dispatcher + 5 sub-scripts) <!-- compose-doc-append-sentinel: branch=fix/867-split-tests pr=#870 -->

### FEATURE: PR #875 — fix/842-stop-layer2 (2026-06-16, ed5f58aadb056178a2a19fb483491cc28024e0d4, #875)
Background: fix(#842): enforce CONFIRM continuation via Stop hook Layer 2
Changes: BUGFIX | #842: CONFIRM sentinel continuation enforced via Stop hook Layer 2 | Background: CONFIRM_{INTENT,OUTLINE,DETAIL} sentinels at turn end caused stall after Allow click — PostToolUse additionalContext (PR #849, #859) is a user-turn channel, not a model-turn restart mechanism. Root cause confirmed 2026-06-15 regression (session 2bb7a911). | Changes: Added Layer 2 to stop-confirm-plan-guard.js — linearly scans latest assistant turn content array for first CONFIRM_<STAGE>_RE_DQ Bash tool_use, then checks for stage-valid follow-up AFTER it (CONFIRM_INTENT→make-outline-plan Skill; CONFIRM_OUTLINE→make-detail-plan Skill; CONFIRM_DETAIL→write-tests Skill or WORKFLOW_BRANCHING_COMPLETE Bash); if absent, returns decision:block + CONFIRM_NEXT_STEP_HINT reason + exit 2 to force model turn restart. Retired confirm-next-step-handler.js (PostToolUse additionalContext path) and removed dispatch from workflow-mark.js. Updated sentinel-patterns.js and workflow-state.js comments; skills/_shared/confirm-plan.md Step 3 description updated. | Test gap: Prior tests asserted advisory systemMessage + exit 0 (old pre-#842 contract); new L2a-L2g assert decision:block + exit 2 with order-aware detection and stage-specific follow-up validation. <!-- compose-doc-append-sentinel: branch=fix/842-stop-layer2 pr=#875 -->

### FEATURE: PR #877 — fix/fix-852-enforce-worktree-abort (2026-06-16, c50f25db5cb0be0f8dd578f5052958fdd887192a, #877)
Background: fix(#852): allow git merge/rebase/cherry-pick --abort/--continue/--skip from main worktree
Changes: BUGFIX | #852: enforce-worktree: git merge/rebase/cherry-pick --abort/--continue/--skip from the main worktree CWD without -C was blocked as a write operation; added isAllowedMidOperationAbort(cmd, repoRoot) to hooks/enforce-worktree/main-worktree-allows.js covering merge|rebase|cherry-pick × abort|continue|skip from main worktree; no linked-worktree-count gate (abort/continue/skip only mutate in-progress state files — .git/MERGE_HEAD, .git/rebase-merge/ — not tracked files in linked worktrees); security guards: rejectRceGitFlags (blocks -c core.editor=evil merge --continue RCE) + ^\s*git\b anchor (blocks GIT_EDITOR=evil git rebase --continue env-var prefix); wired in enforce-worktree.js dispatch between isAllowedPushAllExcluded and isAllowedMainWorktreeCleanup; 23 tests in tests/fix-852-enforce-worktree-abort.sh (S1–S19 direct predicate + L2.A–D integration) <!-- compose-doc-append-sentinel: branch=fix/fix-852-enforce-worktree-abort pr=#877 -->

### FEATURE: PR #880 — fix/issue-868-survey-improve (2026-06-16, 8094b582c5c875c4deb377292850c799977e7192, #880)
Background: fix(#868): enhance issue-create Phase 2 survey — parallel symptom-token search, Pass 3 widened, inspect cap 25, Verdict Rubric, regression row
Changes: fix(#868): enhance issue-create Phase 2 survey — parallel symptom-token search across all 3 passes (always-on, not fallback-only), Pass 3 widened to --limit 50, inspect cap raised to 25 unique candidates with closed-first priority, Verdict Rubric (5 rules: symptom-match, scope-overlap, age-as-tiebreak, tie-break-order, no-match→none), regression/recurrence row added to verdict table (reopen regardless of closure reason); test coverage added in tests/fix-issue-868-survey-improve.sh <!-- compose-doc-append-sentinel: branch=fix/issue-868-survey-improve pr=#880 -->

### FEATURE: PR #894 — refactor/871-dead-sentinel-removal (2026-06-16, 39bbb9803c8015b107d6a8b8dd83f68c7ae97c22, #894)
Background: refactor(#871): remove dead-code WORKFLOW_OUTLINE_PLAN_COMPLETE sentinel from make-outline-plan
Changes: REFACTOR | #871: make-outline-plan: remove dead-code WORKFLOW_OUTLINE_PLAN_COMPLETE sentinel (never registered in isSentinel(); allAreSentinels guard returned done() immediately; Phase 1 removes literal from 3 sites in SKILL.md + assertion block in static test) <!-- compose-doc-append-sentinel: branch=refactor/871-dead-sentinel-removal pr=#894 -->

### FEATURE: PR #895 — fix/789-skip-step8-confirm-on-pass-all (2026-06-16, 8fbd4bd9359d6e2e8f75b8f631863c220debd992, #895)
Background: PR #895 merged on 2026-06-16.
Changes: BUGFIX #789: make-outline-plan Step 8 unconditionally fired the CONFIRM_OUTLINE sentinel dialog even when the user had already selected "Pass all approaches to make-detail-plan without selecting" in Step 7. Added a Logical-OR bypass using a CHOSEN_APPROACH variable: when CONFIRM_OUTLINE=off OR CHOSEN_APPROACH equals the pass-all selection, confirm-plan Steps 1+2 (artifact write + breadcrumb) execute without emitting the <<WORKFLOW_CONFIRM_OUTLINE>> sentinel. <!-- compose-doc-append-sentinel: branch=fix/789-skip-step8-confirm-on-pass-all pr=#895 -->

### BUGFIX: PR #896 — fix/fix-884-supervisor-workflow-init (2026-06-17, cc859ef, #896)
Background: fix(#884): add workflow-init prohibition to supervisor post-diagnosis output
Changes: BUGFIX #884: supervisor post-diagnosis workflow-init prompt. Background: After L2 diagnosis completed the model incorrectly proposed invoking /workflow-init, disrupting the active session. Root cause: agents/supervisor.md Output protocol had no /workflow-init prohibition analogous to the existing /issue-create non-auto-invoke directive; Constraints section lacked a post-diagnosis return-control directive. Changes: added Output step 6 "Do NOT auto-invoke /workflow-init — the session continues after diagnosis"; added Constraints directives "After providing first-aid guidance, return control to the user" and "Do NOT propose or invoke /workflow-init — the session continues after diagnosis". <!-- compose-doc-append-sentinel: branch=fix/fix-884-supervisor-workflow-init pr=#896 -->

### FEATURE: PR #899 — fix/891-l2-phase (2026-06-17, f574abb80d18eba4c67ef0bb89df7c9a98795a95, #899)
Background: fix(#891): add l2_phase lifecycle enum to supervisor state; gate-yield before Final Report
Changes: category:BUGFIX subject:#891 — EM Supervisor: L2 lifecycle `l2_phase` enum + gate-yield before Final Report background:Two UX regressions after PR #873 converted L2 to Stop-hook block: (1) `appendFinding()` re-armed `next_check_at` post-session causing repeated L2 blocks; (2) L2 ran after Final Report pushing it off-screen. changes:Added `l2_phase` lifecycle enum (null/pending/done/frozen) to supervisor state as SSOT; `ensureLayer2Scheduled()` and `writeLayer2State()` guard on done/frozen; `supervisor-guard.js` Path(3) skips L2 when done/frozen; `detectSentinelHang()` exempts `final_report` and `pre_final_report_gate`; session-close Step 3.6 gate-yields before Final Report when l2_phase=pending; Final Report sets phase to frozen; `supervisor-write-layer2 --set-l2-phase` CLI flag added; `VALID_STEPS` gains `pre_final_report_gate`; `workflow-state.js` split into session-id/state-io submodules <!-- compose-doc-append-sentinel: branch=fix/891-l2-phase pr=#899 -->

### FEATURE: PR #900 — fix/fix-883-wsid (2026-06-17, 2261b7dd873fb685845e4dd565a35543b862ae4f, #900)
Background: fix(#883): supervisor dual-identifier model — separate CC UUID (sid) from workflow session ID (wsid)
Changes: BUGFIX | #883: supervisor Layer 2 read wrong plan artifacts when CC UUID ≠ workflow session ID | Background: supervisor-guard.js passed CC UUID as `<sid>` to supervisor.md, which read `<plans-dir>/<sid>-intent.md`. When $CLAUDE_ENV_FILE was absent at workflow-init time, plan artifacts were named with a YYYYMMDD-HHMMSS date-fallback prefix — creating a namespace mismatch. In PR #875 session, CC UUID happened to match a prior session's intent.md (PR #859), causing Layer 2 to report on the wrong 2-file scope instead of the actual 9-file PR scope. | Fix: dual-identifier model — `<sid>` (CC UUID) for supervisor-state.json keying; `<wsid>` (workflow session ID) for plan artifact lookup. New `hooks/lib/resolve-workflow-session-id.js` implements a two-stage priority chain: (1) CLAUDE_ENV_FILE → CLAUDE_SESSION_ID value with charset validation + intent.md existence check; (2) same-day `*-context.md` mtime scan with charset validator and date-sanity guard (prefix YYYYMMDD must equal today's local date). supervisor-guard.js injects both identifiers into block-reason text; when wsid resolves to null, `Workflow session ID: UNAVAILABLE` is injected and supervisor degrades to transcript-only review with a `category=env, severity=warning` finding. supervisor.md vocabulary updated to distinguish `<sid>` vs `<wsid>` with explicit UNAVAILABLE fallback procedure. <!-- compose-doc-append-sentinel: branch=fix/fix-883-wsid pr=#900 -->

### FEATURE: PR #904 — fix/fix-confirm-stall (2026-06-17, aa37d3a28452e3664da956f51673c08864bba37e, #904)
Background: fix(#842): extend Layer 2 to CONFIRM_PR_CREATED; fix marker early-exit; SSOT PR URL regex
Changes: BUGFIX | #842: CONFIRM Layer 2 extended to CONFIRM_PR_CREATED; marker early-exit bug fixed; Layer 1 co-emit directives added; SSOT PR URL regex | Background: After PR #875 added Layer 2 for CONFIRM_{INTENT,OUTLINE,DETAIL}, CONFIRM_PR_CREATED (emitted by commit-push) remained uncovered: (1) no regex for PR_CREATED in Layer 2 scan; (2) markers.length===0 early-exit bypassed Layer 2 entirely for commit-push turns (no plan artifact written). Layer 1 also lacked model-facing co-emit imperatives. | Changes: stop-confirm-plan-guard.js — replaced markers.length===0 early-exit with hasMarker flag; Layer 2 scan runs unconditionally; added CONFIRM_PR_CREATED detection with Skill(worktree-end) and Bash(WORKFLOW_USER_VERIFIED) as valid follow-ups; added pr-created to CONFIRM_NEXT_STEP_HINT (workflow-state.js). sentinel-patterns.js — added CONFIRM_PR_CREATED_RE_DQ, CONFIRM_PR_CREATED_LOOKSLIKE_RE, CONFIRM_PR_CREATED_BODY_RE (SSOT for PR URL regex); confirm-checkpoint.js — replaced inline ad-hoc regex with CONFIRM_PR_CREATED_BODY_RE import. confirm-plan.md — rewrote Step 3 ON-path as model-facing SAME-response co-emit imperative; 4 SKILL.md call sites gained per-site co-emit reminders; commit-push Step 6.5 adds ENFORCE_WORKTREE mode-specific CONFIRM_PR_CREATED emission. | Test gap: feature-stop-guard-layer2.sh covered L2a-L2g (INTENT/OUTLINE/DETAIL) only; CONFIRM_PR_CREATED and marker-absent activation were untested; now covered by L2h-L2n and new feature-842-confirm-pr-created-ssot.sh. <!-- compose-doc-append-sentinel: branch=fix/fix-confirm-stall pr=#904 -->

### FEATURE: PR #906 — fix/supervisor-c2-label-891-892 (2026-06-17, 5cdbcf88e45af0843b5231006b81542a94dd5303, #906)
Background: fix(#879,#892,#891): rename C2 label, add post-Final-Report L2 guard, Phase 4 dispatch detection
Changes: #879: Renamed C2 trigger label from "C2 escape-hatch use" to "C2 scheduled-review" in supervisor-guard.js (cause ternary at L163) and agents/supervisor.md (description + Role). The old label was misleading because C2 fires on any non-null next_check_at, not only on escape-hatch commands. The supervisor subagent was misdiagnosing the trigger from the block-reason text.;#892: Added 6th JD checklist item to agents/supervisor.md for /issue-create Phase 4 dispatch detection. When ISSUE_CREATE_SKILL=1 ... issue-create-dispatch.sh appears in the transcript, the supervisor now verifies that all three Phase 1-3 evidence markers precede it: (a) gh issue list --state all --search, (b) symptom-token search, (c) gh issue view <N> --json inspection. All three present classifies as legitimate Phase 4; any absent may be Phase 1-3 bypass.;#891: Added sessionId param to ensureLayer2Scheduled(state, sessionId) with a filesystem-only guard: if <PLANS_DIR>/<sessionId>-final-report-env.json exists (written by session-close Step 2A), the function returns early and does not set next_check_at. Fail-open on null/invalid sessionId or any I/O error. Both appendFinding call sites thread sessionId. Fixed session-close SKILL.md Step 3.5 wording to document that retrospective findings are audit-only and do not arm a new L2 cycle after the env anchor is established. This prevents the next session's Stop hook from firing an L2 review on an already-closed session. <!-- compose-doc-append-sentinel: branch=fix/supervisor-c2-label-891-892 pr=#906 -->

### FEATURE: PR #910 — fix/726-assemble-show-plan-link (2026-06-17, b71396fa5a0eb85ad83299a449fc6f09e9a67f12, #910)
Background: fix(#726): assemble-mandatory.sh Bash hook not firing when called via SKILL.md wrapper scripts
Changes: #726: Updated make-outline-plan and make-detail-plan SKILL.md to call `_shared/assemble-mandatory.sh` directly with positional args instead of thin wrapper scripts that were invoked with env-vars only (no positionals); deleted dead wrapper scripts; dedup'd 6 inline VS Code helpers from show-plan-link.js into require('./lib/vscode-open'); split feature-show-plan-link.sh (568 lines → dispatcher + 3 sub-files); added T11/T12 parser regression tests and T-BASH-9/T-VSCODE-LIB-3 integration assertions <!-- compose-doc-append-sentinel: branch=fix/726-assemble-show-plan-link pr=#910 -->

### FEATURE: PR #916 — refactor/902-rename-next-check-at (2026-06-17, 394afb2, #916)
Background: refactor(#902): rename supervisor layer2 field next_check_at → l2_armed_at
Changes: REFACTOR #902: rename supervisor layer2 field `next_check_at` → `l2_armed_at` across all 20 files. Background: `next_check_at` was a polling-timer naming holdover from before PR #873 abolished the 5-min wall-clock timer; since PR #899 added `l2_phase` as lifecycle SSOT, the field functions only as a null/non-null armed signal — the ISO timestamp value is never compared against the current time. New name adds `l2_` namespace prefix (matching `l2_phase`) and `_at` suffix (signals timestamp type). CLI flags renamed `--next-check-at` → `--l2-armed-at` and `--clear-next-check-at` → `--clear-l2-armed-at`. Changes: schema, writer, guard, trigger, CLI bin, architecture doc, supervisor agent prompt, session-close skill, and all 10 affected test files updated atomically to prevent trigger/guard key mismatch. <!-- compose-doc-append-sentinel: branch=refactor/902-rename-next-check-at pr=#916 -->

### FEATURE: PR #918 — fix/fix-897-conv-lang (2026-06-17, 9ae174da38c8037fdbe9a525f26b3ccb72119c04, #918)
Background: fix(#897): CONV_LANG subagent compliance — settings.json language field + SubagentStart hook + per-agent dynamic fallback
Changes: category:BUGFIX subject:"#897 — CONV_LANG subagent compliance: settings.json language field + SubagentStart hook + per-agent dynamic fallback" background:"supervisor subagent and planning agents (survey-*, planner, reviewer, security-scanner) emitted English despite CONV_LANG=japanese. Root cause: hooks/session-start.js and post-compact.js correctly inject 'Respond to the user in japanese.' into additionalContext, but (1) additionalContext has insufficient authority — Claude downgrades it relative to the system prompt, causing the main conversation to ignore it; (2) no SubagentStart hook existed, so subagents received no injection at all. Approach A (settings.json language field) was tested as Phase 1; Approach D (SubagentStart hook + per-agent dynamic fallback) as Phase 2a." changes:"Phase 1: added \"language\": \"japanese\" top-level field to settings.json (framework-native first-class language enforcement mechanism). Phase 2a: new hooks/subagent-start.js mirrors post-compact.js pattern — reads CONV_LANG via getConvLangInjection() in fail-open try/catch, emits {additionalContext: 'Respond to the user in <lang>.'} when set (or {} for unset/english). Registered as SubagentStart hook in settings.json. Each of 8 planning agents (supervisor, survey-history, survey-code, detail-planner, outline-planner, security-scanner, detail-reviewer, outline-reviewer) received a generic dynamic fallback directive (<!-- conv-lang-fallback:v1 --> marker) that obeys hook-injected language directive without hardcoding any specific language name, preserving CONV_LANG SSOT." test_gap:"no L3 test for live subagent spawn language behavior — only hook-shape verified; mitigated by manual verification gate (Probe A: main conversation, Probe B-supervisor: supervisor via Stop-hook block, Probe B-planner: planning subagent via /make-outline-plan)" <!-- compose-doc-append-sentinel: branch=fix/fix-897-conv-lang pr=#918 -->

### FEATURE: PR #919 — feature/885-supervisor-accuracy-axis-a (2026-06-17, #919)
Background: feat(#885): supervisor accuracy (Axis A) — Layer 1 finding schema: env context + co-block correlation
Changes: feat: supervisor accuracy (Axis A) — Layer 1 finding schema extended with three optional fields: `reason` (discriminates `cwd_no_git_root` from policy-violation blocks), `context` (`cwd` + `git_root_resolved`, populated by enforce-worktree and workflow-gate), and `co_blocked_by` (supervisor-state-writer back-annotates bidirectionally within a 10-second / 5-finding recency window when two hooks block the same command). `INLINE_SKILL_RE` extracted from enforce-issue-close.js to `hooks/lib/block-predicates.js` as SSOT. `isMainCheckout()` trivalue-converted (true/false/null) — null on git-rev-parse non-zero exit — fail-closed (null → block) at all four call sites in enforce-worktree.js including the gh-issue-create gate. `buildExtras()` helper extracted to `hooks/enforce-worktree/block-extras.js`. <!-- compose-doc-append-sentinel: branch=feature/885-supervisor-accuracy-axis-a pr=#919 -->

### REFACTOR: PR #907 — refactor/rename-ci-mop-sc-steps (2026-06-17, 92b368a, #907)
Background: refactor(#898): rename CI/MOP/SC skill step labels to prefixed format
Changes: REFACTOR | #898: rename CI/MOP/SC skill step labels to prefixed format (CI-N, MOP-N, SC-N) | Background: PR #779 renamed WI/WE/ICF/MDP step labels to prefixed format; three remaining skills (clarify-intent, make-outline-plan, session-close) still used plain numeric headings; make-outline-plan had an explicit TODO at agents/outline-planner.md awaiting this rename | Changes: renamed CI Procedure 1.–5. → CI-1.–CI-5. and Completion 0.–3. → CI-C0.–CI-C3.; renamed MOP Procedure 0.–8. → MOP-0.–MOP-8. and Completion 1.–2. → MOP-C1.–MOP-C2.; renamed SC H2 headings Step 0–4 → Step SC-0–SC-4; removed ### Step 0 H3 from CI and MOP (folded into intro prose); swept 10 cross-reference files (cap-menu-dispatch, codex-review-loop, outline-planner, survey-history, survey-code, worktree-end, workflow.md, claude-code.md, survey-artifact-valid); extended bin/verify-renumber-sweep.sh with scoped CI/MOP/SC legacy-pattern checks; added R7/R8/R9 to tests/feature-skill-shrink-renumber-sweep.sh; updated PG-STEP0-DOC in tests/feature-premise-gate.sh to search for MOP-0 <!-- compose-doc-append-sentinel: branch=refactor/rename-ci-mop-sc-steps pr=#907 -->

### FEATURE: PR #934 — fix/remove-confirm-pr-created (2026-06-17, a2b54d256f8180bd377b4cde7c358c46304c38a0, #934)
Background: fix(#911): remove WORKFLOW_CONFIRM_PR_CREATED sentinel
Changes: #911: Remove WORKFLOW_CONFIRM_PR_CREATED sentinel — deleted Step 6.5 from commit-push/SKILL.md, removed from settings.json ask list, confirm-checkpoint.js PR-created branch, stop-confirm-plan-guard.js Layer 2 (CONFIRM_PR_CREATED case + hasMarker wrapper that blocked the markers.length===0 early-exit), sentinel-patterns.js (CONFIRM_PR_CREATED_RE_DQ / LOOKSLIKE / BODY_RE constants and isSentinel/isStrictSentinel references), and state-io.js CONFIRM_NEXT_STEP_HINT["pr-created"]. Restored markers.length===0 early-exit (reverts PR #904 hasMarker logic). Added recurrence-prevention note to confirm-plan.md. Updated CLAUDE.md WF-CODE-10 for on/off-mode PR URL visibility. Background: the sentinel surfaced as a raw echo command in the Claude Code permission dialog after every gh pr create — confusing and noisy. The merge gate (WE-7 in /worktree-end) already covers the irreversible action; pr-created-open.js already shows the PR URL as a systemMessage. <!-- compose-doc-append-sentinel: branch=fix/remove-confirm-pr-created pr=#934 -->

### FEATURE: PR #935 — feature/companion-issues (2026-06-18, #935)
Background: feat(#920): auto-detect companion issues for co-session fix
Changes: feat(#920): auto-detect companion issues for co-session fix — Background: each bugfix session required a manual prompt to find co-located issues worth bundling into one session. Automated this by running a 2-pass keyword search (`gh issue list --search`) at two insertion points: WI-4.5 (immediately after primary issue fetch in workflow-init) and CI-2a.5 (after the intent interview in clarify-intent, with richer context). Accepted companions join `closes_issues` and flow through all existing multi-issue machinery (WIP, label parity, board-card). CONFIRM_COMPANION_ISSUES flag controls per-candidate confirmation (default on). — Changes: new `bin/github-issues/find-companion-issues.sh` (NON_GITHUB gate, token extraction ≥3-char, stopword filter, 2-pass dedup+meta-drop+rank, TSV stdout contract, exit 0/1/2); WI-4.5 step in workflow-init SKILL.md + B1 prefill amendment for Path B propagation; CI-2a.5 step in clarify-intent SKILL.md; `CONFIRM_COMPANION_ISSUES=on` block in .env.example; 19 structural/L2 tests (A: CLI unit, B: prose contract, C: prefill propagation, D: .env.example). <!-- compose-doc-append-sentinel: branch=feature/companion-issues pr=#935 -->

### FEATURE: PR #938 — feature/env-example-guidelines (2026-06-18, c83160166708cfb165e91908ce977072c62c7e29, #938)
Background: feat(#925): .env.example comment guideline — rule extension + static checker + full rewrite
Changes: category:FEATURE subject:"#925 — .env.example comment guideline: rule extension + static checker + full rewrite" background:".env.example comments had no consistent style rules — variable-name headings, issue references, internal hook/script names, architecture prose, and blocks up to 22 lines all mixed together. `rules/docs/env-example.md` lacked a line cap, prohibited-content list, and category grouping rules. New variables added since #814 (SWEEP_AGE_DAYS, CONV_LANG) continued the same drift." changes:"(1) Extended `rules/docs/env-example.md` with 5 new directives: 1–5 line cap per variable, prohibited-content list (variable-name heading repeats, issue refs, internal hook/script/path names, architecture rationale, command references, redundant Example: lines), category heading format (`# --- Category ---`), enforcement pointer to `bin/review-env-example`, and a judgment note for WARN-level checks. (2) New `bin/review-env-example` static checker (340 lines): 5 HARD machine-decidable checks (variable-name heading, issue ref, internal implementation detail path|.js|hook-event|protocol-term, redundant `Example:` line, >5 comment lines per variable) + 2 WARN judgment checks (architecture rationale phrase, command reference); diff mode exits 1 on any HARD finding; `--all` mode always exits 0 (audit); CRLF-tolerant block parser; excludes `_archived/` and `node_modules/`. (3) Rewrote all ~23 `.env.example` entries: removed all banned patterns, added `# --- Worktree enforcement ---` category heading for contiguous ENFORCE_WORKTREE_* block, all comment blocks ≤ 5 lines; `bin/review-env-example --all` confirms 0 findings. (4) Installer updated: `review-env-example` symlink added to both `install/linux/dotfileslink.sh` and `install/win/dotfileslink.ps1`. (5) `CLAUDE.md` WF-CODE-6 updated with `review-env-example --base <merge-base>` parallel step. 17-case test suite in `tests/feature-review-env-example.sh`." <!-- compose-doc-append-sentinel: branch=feature/env-example-guidelines pr=#938 -->

### FEATURE: PR #936 — fix/913-905-supervisor-dual-session-id (2026-06-18, 35bdb354743f133b4a35e38398120ca329eeac37, #936)
Background: fix(#913,#905): ensureLayer2Scheduled dual-ID guard + writeLayer2State terminal l2_armed_at null-clear
Changes: #913: ensureLayer2Scheduled dual-ID guard — `ensureLayer2Scheduled` checked `${CC_UUID}-final-report-env.json` but `capture-env.sh` (worktree-end WE-11) writes `${workflow_SID}-final-report-env.json`. Because CC UUID ≠ workflow SID, the final-report guard was permanently bypassed: every post-Final-Report appendFinding call re-armed `l2_armed_at`, causing the next session's Stop hook to trigger a spurious Layer 2 review on an already-closed session. Fix: import `resolveWorkflowSessionId` in supervisor-state-writer.js; `ensureLayer2Scheduled` now builds a candidate Set of both the passed sessionId (CC UUID) and the resolved workflow SID, and returns early when any candidate's `final-report-env.json` exists in plans dir. Test gap: no test for the cross-ID scenario (CC UUID state file + workflow SID env file) — now covered by G8 in fix-supervisor-c2-label-891-892-guard.sh.;#905: writeLayer2State terminal-phase l2_armed_at stale-value fix — `session-close` sets `l2_phase=frozen` via `writeLayer2State`, but the function did not clear `l2_armed_at`. A non-null `l2_armed_at` on a terminal state caused the next session's supervisor-guard C2 check to see a scheduled review and trigger a false-positive Layer 2 block. Fix: added unconditional null-clear of `layer2.l2_armed_at` in `writeLayer2State` whenever `effectivePhase` is `done` or `frozen`, including re-patching of already-terminal states with stale values from before the fix. Test gap: no test for terminal-transition l2_armed_at clearing — now covered by G10/G11/G12/G13 in fix-supervisor-c2-label-891-892-guard.sh. <!-- compose-doc-append-sentinel: branch=fix/913-905-supervisor-dual-session-id pr=#936 -->

### FEATURE: PR #945 — feature/feat-928 (2026-06-18, fd24c4aab1ebc610e5ccca8c4069322ab4c1453d, #945)
Background: feat(#928): L2 supervisor report — standardized display format
Changes: category=FEATURE subject="feat(#708/S-2): L2 supervisor report — standardized display format (#928)" background="supervisor-guard.js emitted a single-line block reason via Stop hook. With multiple findings accumulated by the L2 supervisor, users saw only the last finding's detail with no category context and no clear state-file path for recovery." changes="Added hooks/lib/supervisor-report-format.js (pure-function module, no side effects) exporting formatCumSevErrorReason for branch(2) cumSev=error and formatL2ArmedReason for branch(3) l2ArmedAt/hang. Branch(2) now emits aggregated Categories: line (across all findings), per-finding list with severity/categories/detail, last Detail:, session IDs, and Recommended action:. Branch(3) now leads with human-readable To resume / Clear: instructions and an explicit File: path (resolved via getStatePath), followed by an Equivalent one-liner: reference; the previous embedded raw node require path was replaced with a path.resolve(__dirname) absolute reference. supervisor-guard.js imports getStatePath from supervisor-state-writer and the two formatter functions; branch string-building logic removed from guard. 19 tests in tests/feature-feat-928-supervisor-report-format.sh (12 formatter unit + 7 guard integration including G2/G19/G20/G21/G22 regression pins)." <!-- compose-doc-append-sentinel: branch=feature/feat-928 pr=#945 -->

### FEATURE: PR #946 — feature/test-cleanup-944 (2026-06-18, 17c9724ec0f990fdc57b9d40cb91334f5cdb697a, #946)
Background: feat(#944): add test governance, audit-tests.sh, review-code-size fix
Changes: category:FEATURE subject:"#944 — tests cleanup governance: scope classification, size limits, audit-tests.sh" background:"tests/ had grown to 322 .sh files (93,613 lines) causing context window pressure on run-all.sh. First cleanup (PR #704) archived 17 files; this PR establishes governance infrastructure to drive the second cleanup and prevent re-accumulation." changes:"(1) skills/_shared/test-design.md: added ## Scope Classification (feature-NNN-* = scope:issue-specific, others = scope:common; # Tags: scope:issue-specific | scope:common values defined; backfill not required — filename alone is machine-sufficient) and ## Size Limits (300-line WARN / 500-line HARD, same thresholds as code; split via tests/<name>/ sibling-folder; _archive/ excluded from scan). (2) bin/review-code-size: fixed _archived/ → _archive/ exclusion pattern bug present in both --all scan mode and diff mode — previous pattern excluded the non-existent tests/_archived/ instead of the actual tests/_archive/ directory. (3) bin/audit-tests.sh (new): staleness checker for issue-specific test files; scans tests/feature-[0-9]*-*.sh; checks issue CLOSED state via gh api with case-insensitive normalization; computes MAX last-commit date across dispatcher + sibling folder; reports deletion candidates in text or JSON format; --offline mode is conservative (no candidates emitted); --stale-months configurable (default 3); exit 0/1/2 contract." <!-- compose-doc-append-sentinel: branch=feature/test-cleanup-944 pr=#946 -->

### FEATURE: PR #952 — feature/888-933-supervisor-axis-b (2026-06-18, 28365b0, #952)
Background: feat(#888,#933): supervisor accuracy (Axis B) — Layer 2 pre-processing + plans-dir exemption
Changes: #888: feat(#888): supervisor accuracy (Axis B) — Layer 2 pre-processing + §5 causality chain — Added `## Layer 2 pre-processing` section to agents/supervisor.md: groups Layer 1 findings by co_blocked_by field (10-second/5-finding back-annotation window) and independently clusters by 60-second timestamp window; clusters treated as one composite item rather than one per blocked hook. Extended §5 with causality-chain tracing directive: for cascade failures, trace to single most-upstream root cause, file one root-cause finding with downstream findings referencing it rather than duplicating the cause.;#933: fix(#933): enforce-worktree plans-dir exemption — isAllowedWorkflowPlansDirWrite — Added isAllowedWorkflowPlansDirWrite predicate: allows writes whose every redirect/tee target resolves under WORKFLOW_PLANS_DIR; uses only extractRedirectTargets+extractTeeTargets (no rm/mv/cp extractors), fail-closed on parseFailure, handles bash -c inner-command parsing and node -e writeFileSync scanning. Split hooks/enforce-worktree/main-worktree-allows.js (was 593 lines, HARD limit violation) into main-worktree-allows/ sibling folder: standard.js (8 existing predicates, 452 lines) + plans-dir.js (new predicate, 137 lines); dispatcher reduced to 6 lines. <!-- compose-doc-append-sentinel: branch=feature/888-933-supervisor-axis-b pr=#952 -->

### FEATURE: PR #947 — feature/941-run-e2e-env-key (2026-06-18, be6d190e3f9270dab2ccd47bb2d93dc4c1263465, #947)
Background: feat(#941): RUN_E2E migrated to .env key via bin/get-config-var
Changes: category:FEATURE subject:"#941 — claude -p E2E: RUN_E2E migrated to .env key via bin/get-config-var" background:"Two claude -p E2E tests (feature-robust-workflow.sh E1 block and phase5-main-transcript-no-delegated-output.sh) used ad-hoc guards not routed through bin/get-config-var, making it impossible to disable Anthropic-billable E2E runs via .env. feature-robust-workflow.sh used raw ${RUN_E2E:-0}=1; phase5 had no guard at all. Sub-issue of #937 (claude -p E2E test implementation roadmap)." changes:"Added RUN_E2E=off to .env.example under new # --- E2E testing --- category (3-part user-visible comment: what-you-can-do / what-you-can-t-do / format). Migrated feature-robust-workflow.sh E1 block guard from ${RUN_E2E:-0}=1 to bin/get-config-var --is-off RUN_E2E off (block-scope skip, script continues on off). Added RUN_E2E guard to phase5-main-transcript-no-delegated-output.sh inserted after FEATURE_644_PHASE>=5 gate and before claude availability check, with early AGENTS_DIR resolution; exits 77 when off. Removed duplicate AGENTS_DIR= assignment. 8-case test suite in tests/feature-941-run-e2e-toggle.sh covering .env.example compliance, phase5 exit-77, phase5 pass with stub claude, FEATURE_644_PHASE<5 precedence, RUN_E2E=0 semantics, and static guard pattern grep." <!-- compose-doc-append-sentinel: branch=feature/941-run-e2e-env-key pr=#947 -->

### FEATURE: PR #948 — feature/929-supervisor-codex-review (2026-06-18, 468bedd7014de3978b07d485612bd017c2656d66, #948)
Background: feat(#929): L2 supervisor Codex adversarial review layer
Changes: category:FEATURE subject:"#929 — L2 supervisor accuracy: Codex adversarial review layer" background:"L2 supervisor (Claude Opus) ran as a single-engine evaluator, producing ~40% false-positive findings due to sycophancy bias. The Generator-Critic pattern (Silicon Mirror arXiv:2604.00478) reduces sycophancy via adversarial second-opinion from a different model identity." changes:"(1) New hooks/lib/supervisor-finding-status.js: pure mutators appendDraftFinding (max-idx assignment prevents post-drop duplicate idx), confirmFinding, dropFindings (descending sort for splice safety), promotePendingDraftsToConfirmed. (2) New hooks/lib/codex-review-parse.js: parseCodexFindings extracts JSON Lines verdicts from <!-- begin-codex-output --> markers; malformed → ok:false; invalid verdict → AGREE (fail-open, never drops findings unilaterally). (3) New bin/supervisor-review-codex: one-shot Codex adversarial review of draft L2 findings; skips silently (exit 0) when Codex unavailable, l2_phase != pending, or no draft findings; SID regex-validated against path traversal. (4) bin/supervisor-write-layer2 extended with --finding-status draft|confirmed, --confirm-finding-ids csv, --drop-finding-ids csv (descending sort). (5) hooks/lib/supervisor-state-writer.js: imports finding-status.js; exports confirmFinding, dropFindings, promotePendingDraftsToConfirmed as atomic wrappers. (6) hooks/lib/supervisor-state-schema.js: validateFinding accepts optional status (draft|confirmed) and idx fields. (7) agents/supervisor.md: 3-phase output protocol (Draft → Adversarial review → Adjudicate+finalize); fallback when Codex unavailable treats all drafts as AGREE. (8) docs/architecture/claude-code.md: L2 three-phase protocol documented; findings[] status/idx fields noted. Security hardening: SID path-traversal validation; Codex prompt data-isolation instruction." <!-- compose-doc-append-sentinel: branch=feature/929-supervisor-codex-review pr=#948 -->

### FEATURE: PR #956 — feature/903-c3-worktree-off-guard (2026-06-18, 9071c69a024e349711ea927809771c8153199808, #956)
Background: feat(#903,#389,#923): supervisor-guard C3 trigger + load-env realpath fallback + enforce-worktree -C form guard
Changes: #903: feat(#903): supervisor-guard C3 trigger — WORKTREE_OFF/WORKFLOW_OFF proposal detection in Stop hook. Background: supervisor-guard.js only detected C1 sentinel hang and C2 scheduled-review; a Claude turn emitting <<WORKFLOW_ENFORCE_WORKTREE_OFF>> or <<WORKFLOW_ENFORCE_WORKFLOW_OFF>> was not caught by the Stop hook until the next turn. Added detectWorktreeOffProposal() scanning the last 100 JSONL lines for either off-proposal substring in {type:"text"} content items; WORKFLOW_OFF takes precedence. When found and l2_phase is not terminal, calls ensureLayer2Scheduled via read→mutate→validate→writeAtomic; c3Armed boolean guards against spurious blocks when arming is skipped by terminal/final-report guard. l2_cause field added to supervisor-state-schema.js (null/string) and to writeLayer2State with co-clear when l2_armed_at is nulled; formatL2ArmedReason extended with C3-specific block wording; supervisor.md JD checklist gains item 6 for WORKTREE_OFF/WORKFLOW_OFF proposal validity. 3 new tests (G-C3a/b/c) in tests/feature-719-supervisor-guard-hook/g-c3.sh; 2 formatter tests (F13/F14) in feature-feat-928-supervisor-report-format/formatter-unit.sh. Test file split: feature-719-supervisor-guard-hook.sh (519 lines) split into dispatcher + _lib.sh + g1-g19.sh + g-c3.sh.;#389: fix(#389): load-env realpath fallback — .env not found when hooks/ is a symlink. Background: dotfiles setups symlink hooks/ into the agents config dir; __dirname resolves to the symlink target, not the config dir, so loadDefaultEnv() could not locate .env via __dirname/../... Added 3-level fallback chain: (1) AGENTS_CONFIG_DIR env var, (2) __dirname/../.., (3) fs.realpathSync(__filename)/../.. with AGENTS_HOOK_DEBUG=1 stderr diagnostic on complete miss. 8 new unit tests in tests/fix-389-load-env-default-fallback.sh.;#923: fix(#923): isAllowedWorktreeCommand -C form — transitive fix via #389. Background: suspected regression for git -C <path> worktree remove/prune commands was investigated via reproduction-first approach; all T923 tests GREEN without source changes — the existing /\bworktree\s+(?:remove|prune)\b/ regex already handles -C form. Root cause identified as #389 (WORKTREE_BASE_DIR not loaded from .env when hooks/ is symlinked). No source change in main-worktree-allows.js; retained 3 regression tests in tests/fix-923-enforce-worktree-c-flag-main-worktree.sh as permanent guard. <!-- compose-doc-append-sentinel: branch=feature/903-c3-worktree-off-guard pr=#956 -->

### CONFIG: Remove language field from settings.json (2026-06-18, d0d5a79)
Background: The language field added to settings.json in #918 is a shared repo config; personal language preference does not belong there.
Changes: Removed "language: japanese" from settings.json. Migration to the user's private dotfiles-private settings.json is tracked separately.
### FEATURE: PR #960 — fix/912-supervisor-api-error-handling (2026-06-18, 679ffeced330c536de5b40c6fee23e708231facf, #960)
Background: feat(#912,#914,#903,#372,#545): supervisor L2 API-error handling, C3 WORKTREE_OFF pre-detection, AskUserQuestion gate, WORKTREE_ON warn hook, USER_VERIFIED enforce hook
Changes: doc-append docs/history.md --category BUGFIX --subject "#912 — supervisor L2 API-error handling: fallback recipe + l2_retry_count auto-freeze" --background "When the supervisor subagent invoked from the Stop-hook block-path encountered a transient API error, there was no mechanism to escape the retry loop: the guard kept blocking, l2_armed_at was never cleared, and the session was permanently stalled. Also had no retry counter, so even legitimate repeated failures had no terminal path short of manual state-file surgery." --changes "#912: (A) Added recipeBlock() SSOT helper to hooks/lib/supervisor-report-format.js — emits a bin/supervisor-write-layer2 --clear-l2-armed-at --set-l2-phase frozen recipe at the bottom of every L2 block-reason text (C2 scheduled-review, cumSev=error, and C3 WORKTREE_OFF). (B) Added l2_retry_count integer field to supervisor state schema (createEmptyState default 0; validate check; LAYER2_PATCH_KEYS) and L2_RETRY_THRESHOLD=2 constant. incrementL2RetryCount() increments the counter, short-circuits on both frozen and done terminal states, and transitions to frozen when count reaches threshold. writeLayer2State short-circuits l2_armed_at on done/frozen and resets l2_retry_count=0 on done at writer SSOT (C-HIGH-3 fix). supervisor-guard.js tryIncrementFrozen() helper calls incrementL2RetryCount before every blocking branch: if frozen returns true the guard exits 0 (fail-open) instead of blocking. bin/supervisor-write-layer2 --increment-l2-retry-count flag added (mutex with other mutations).";doc-append docs/history.md --category FEATURE --subject "#903 — supervisor-guard C3: WORKTREE_OFF proposal pre-detection" --background "WORKTREE_OFF proposals required L2 review to catch unauthorized worktree disabling, but the guard only triggered after approval. A proposed sentinel that hadn't been approved yet was invisible to the Layer 2 gate." --changes "#903: Added detectWorktreeOffProposal(transcriptPath) to supervisor-guard.js; walks the full transcript (aligned with stop-enforce-worktree-on-warn.js scope for sibling consistency) tracking OFF/ON Bash commands via ENFORCE_WORKTREE_OFF_RE_DQ and ENFORCE_WORKTREE_ON_RE_DQ sentinel-patterns; returns true when the last OFF follows the last ON. New branch (C3) slotted between stop_hook_active (1) and cumSev=error (2); calls tryIncrementFrozen() and emits formatWorktreeOffProposalReason block when triggered and AskUserQuestion gate does not suppress it.";doc-append docs/history.md --category FEATURE --subject "#914 — supervisor-guard AskUserQuestion gate: suppress L2 blocks during user dialog" --background "When the supervisor-guard Stop hook triggered an L2 block at the same turn the agent had emitted an AskUserQuestion, two concurrent prompts appeared to the user — the AskUserQuestion dialog and the Stop-hook block message. This was confusing and the block was spurious (the agent was already paused awaiting a user answer)." --changes "#914: Added detectAskUserQuestionTurn(transcriptPath) to supervisor-guard.js; checks whether the last tool_use in the last assistant turn is an AskUserQuestion call. When true, branches (C3), (2), and (3) are all suppressed — the guard exits 0. Branch (4) advisory and branch (5) silent are unaffected (they exit 0 regardless). The detection is fail-open: any read/parse error returns false.";doc-append docs/history.md --category FEATURE --subject "#372 — Stop hook advisory: warn on unmatched WORKTREE_OFF sentinel" --background "When a WORKTREE_OFF sentinel appeared in the transcript without a matching WORKTREE_ON, the session could continue in off-mode inadvertently. No advisory was shown to the user or model." --changes "#372: New hooks/stop-enforce-worktree-on-warn.js Stop hook (exit 0 always, advisory only). Reads the full transcript, tracks WORKTREE_OFF / WORKTREE_ON sentinel order across all assistant turns, and emits additionalContext warning when OFF is more recent than ON. Registered in settings.json with 5-second timeout.";doc-append docs/history.md --category FEATURE --subject "#545 — Stop hook: enforce AskUserQuestion before WORKFLOW_USER_VERIFIED sentinel" --background "Per skills/_shared/user-verified.md, an AskUserQuestion must precede WORKFLOW_USER_VERIFIED so the user can record an explicit answer. This was a documented requirement but not machine-enforced at the Stop-hook layer." --changes "#545: New hooks/stop-askuserquestion-required.js Stop hook. Scans the last 50 transcript entries across all assistant turns; sets askUserQuestionSeen=true when any AskUserQuestion tool_use appears before the USER_VERIFIED Bash sentinel across the multi-turn window (canonical flow: AskUserQuestion turn N, user answer turn N+1, USER_VERIFIED turn N+2 — same-turn search would incorrectly block this flow). Emits decision:block with guidance when USER_VERIFIED is found without a preceding AskUserQuestion. Registered in settings.json with 5-second timeout." <!-- compose-doc-append-sentinel: branch=fix/912-supervisor-api-error-handling pr=#960 -->
### CONFIG: Remove language field from settings.json (2026-06-18, d0d5a79)
Background: The language field added to settings.json in #918 is a shared repo config; personal language preference does not belong there.
Changes: Removed "language: japanese" from settings.json. Migration to the user's private dotfiles-private settings.json is tracked separately.

### FEATURE: PR #964 — fix/953-split-robust-workflow (2026-06-18, c6690fbd7c21fa43fe17324b02fd31b4f1a19f46, #964)
Background: feat(#953,#882,#922): split robust-workflow tests + fix review-tests staged-token
Changes: #953 (FEATURE): split tests/feature-robust-workflow.sh (1225 lines) into thin dispatcher + 6 sibling-folder sub-files (tests/feature-robust-workflow/helpers.sh, workflow-gate.sh, session-start.sh, workflow-mark.sh, settings-e2e.sh, workflow-state.sh); dispatcher shrunk to 49 lines, eliminating the HARD size-limit violation that blocked review-code-size on every PR touching workflow tests;#882 (BUGFIX): fix /review-tests stale staged-tests token mismatch when skill runs from main-conversation context with an active linked worktree; added bin/compute-staged-tests-token.js (worktree enumeration CLI) and updated SKILL.md Step 4a to call it instead of hardcoded process.cwd() one-liner;#922 (FEATURE): add T-IDEM-1 (idempotency) and T-ABSENT-RESPONSE (absent success/exit_code fields resolve to exitCode=0) tests to tests/feature-show-plan-link/write-tool.sh; remove trailing-period duplicate entry from # Tests: frontmatter in tests/feature-show-plan-link.sh <!-- compose-doc-append-sentinel: branch=fix/953-split-robust-workflow pr=#964 -->

### FEATURE: PR #972 — feature/942-e2e-policy (2026-06-19, 09cda2a605f967324caa4e86a8ec3f55d9f5d0ae, #972)
Background: feat(#942,#697): Claude Code E2E test policy SSOT + dotfileslink transactional safety
Changes: #942: Claude Code E2E test policy SSOT. Background: hook coverage at L2 only — workflow-mark.js, stop-confirm-plan-guard.js, etc. have no L3 contract; ad-hoc E2E patterns drift across testers. Changes: added rules/test/claude-e2e.md "Acceptance Criteria for `claude -p` E2E Tests" (skip-gate / process hygiene / fixture hygiene / timeout / output capture / session ID / frontmatter / L3-gap block). Added hook audit table classifying every hook as P1/P2/P3/OUT for #943. Added docs/ops.md "Claude Code E2E Tests" runbook (RUN_E2E config flag, `command -v claude` skip-gate, 180s run_with_timeout wrapper, WSL-via-Windows caveat). Refined rules/test.md "L3 aspiration" to point at the canonical contract instead of duplicating it. Test gap (n/a): pure doc change; no executable contract regressed.;#697: dotfileslink.sh / dotfileslink.ps1 _link_one transactional safety + watchlist sibling sweep + behavioral test coverage. Background: prior _link_one (#685) deleted existing files before linking; symlink failure left no rollback. Watchlist `_agentSymlinks` covered only CLAUDE.md, missing skills/rules/agents siblings. Codex adversarial review surfaced HIGH-1 (`New-Item SymbolicLink` non-terminating-error → empty catch) and HIGH-2 (final `.bak` rename failure → both old and new backups lost). Changes: install/linux/dotfileslink.sh + install/win/dotfileslink.ps1 _link_one now stages original to `.bak.tmp.$PID` before `ln`/`New-Item`; on failure, restores; on success, promotes via `.bak.old.$PID` transactional pattern. Added `-ErrorAction Stop` to `New-Item SymbolicLink` (HIGH-1). profile-snippet.sh + .ps1 watchlist extended to all four sibling paths (CLAUDE.md, skills, rules, agents). Tests: bash _link_one rollback (7 cases) + Pester static regex suite (14 cases including HIGH-1/HIGH-2 assertions). Test gap: dynamic Pester rollback test deferred to #969 (depends on #965 DOTFILESLINK_HOME_OVERRIDE). <!-- compose-doc-append-sentinel: branch=feature/942-e2e-policy pr=#972 -->

### FEATURE: PR #974 — fix/913-supervisor-guard-dual-id (2026-06-19, b4195df87f387e82b2e95e1a3ee4e3d97ec22c37, #974)
Background: fix(#913): supervisor-guard dual-ID readState fallback + stray-} syntax fix
Changes: BUGFIX #913: supervisor-guard.js dual-ID readState fallback — guard was silently passing when supervisor state was written under workflow session ID (Anthropic bug #27987 propagation gap) but Stop hook read under CC UUID. Added effectiveSupervisorStateSessionId resolver (readState-null check, not existsSync); fixed stray } syntax error (PR #960 regression); extended formatter signatures with stateSessionId param routing recovery commands through effective state key; added G23 regression test. <!-- compose-doc-append-sentinel: branch=fix/913-supervisor-guard-dual-id pr=#974 -->

### FEATURE: PR #976 — feature/971-step-number-rule-lint (2026-06-19, a16e7b0e6467e53c198aec61cfa82707310ea5fb, #976)
Background: feat(#971): step-number rule (rules/prompt.md §4) + bin/review-step-numbers + WE/SC renumber
Changes: #971: Added rules/prompt.md §4 (decimal step labels PROHIBITED, letter-suffix permitted, renumber-the-tail invariant); created bin/review-step-numbers HARD lint replacing bin/verify-renumber-sweep.sh, wired into WF-CODE-6; retroactively renumbered WE-2.5→WE-3 (+WE-3..WE-21 shifted to WE-4..WE-22), SC-3.5→SC-4, SC-3.6→SC-5, SC-4→SC-6, "Step 6.5"→"Step WI-12"; swept downstream cross-refs in worktree-end/SKILL.md, cleanup-cascade.sh, session-close/SKILL.md, hooks/enforce-worktree.js, rules/github-issues.md, architecture docs, and 10 further files. <!-- compose-doc-append-sentinel: branch=feature/971-step-number-rule-lint pr=#976 -->

### FEATURE: PR #978 — feature/cap-menu-summary-811 (2026-06-19, 63c2d52, #978)
Background: feat(#811): surface concern summary before cap-menu AskUserQuestion dialog
Changes: category:FEATURE subject:"#811 — cap-menu: concern summary before AskUserQuestion dialog" background:"When MOP-6 (make-outline-plan) or MDP-6 (make-detail-plan) reached the codex review loop's round cap, the orchestrator presented only the Land/Adjust/Extend AskUserQuestion dialog with no visibility into which concerns remained unresolved. The concern ledger and prior-round RAW_FILE were already on disk at cap-reach time but not surfaced to the user." changes:"(1) Added bin/review-loop-summarize-concerns (new, mode 0755): reads concern ledger (cap-snapshot.txt) + ROUND_NUMBER-1 RAW_FILE, emits structured Markdown summary (concern ID, severity, body, resolution status, budget remaining) to stdout; supports degraded LEDGER and RAW modes; sanitises workflow sentinels; sorts HIGH→MEDIUM→LOW. (2) bin/run-codex-review-loop: at exit 2 (cap-reach), copies ledger to *-cap-snapshot.txt before rm so the summariser has a stable input. (3) skills/_shared/cap-menu-dispatch.md: added step c.5 (invoke review-loop-summarize-concerns before AskUserQuestion), added LEDGER_FILE parameter row, revised RAW_FILE row to ROUND_NUMBER-1 semantics, added Notes bullets. (4) skills/make-outline-plan/SKILL.md MOP-6: added LEDGER_FILE param + round_number-1 rationale sub-bullet + Rules exemption (d) for cap-reach summary output. (5) skills/make-detail-plan/SKILL.md MDP-6: symmetric changes." <!-- compose-doc-append-sentinel: branch=feature/cap-menu-summary-811 pr=#978 -->
