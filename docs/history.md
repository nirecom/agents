# History (agents)

## Archived
- [2026](1784244559-505473-pr1475-history-staging/2026.md) — 93 entries

### BUGFIX: PR #1417 — fix/fix-882 (2026-07-13, c429926, #1417)

Background: fix(#882): extract session-bound worktree resolution into SSOT helper; wire RT-1/RT-4a to it

Changes: Extracted session-bound worktree resolution into `hooks/lib/workflow-state/resolve-worktree-path.js`: `isMainWorktree(dir)` (fail-close, execFileSync argv form) and `resolveSessionWorktreePath()` (fail-safe, reads state.cwd, rejects main worktree).;Added `bin/resolve-worktree-path` CLI wrapper: no-sid→empty, no-state→"NOSTATE", linked-wt→path, main/error→empty.;Added `skills/review-tests/scripts/select-staged-files.sh`: WORKTREE path→`git -C $WORKTREE diff --cached`; NOSTATE→cwd fallback; empty→exit 3.;Wired SKILL.md RT-0/RT-1/RT-4a to the SSOT: RT-0 resolves WORKTREE via CLI; RT-1 delegates file selection to select-staged-files.sh; RT-4a passes `${WORKTREE:-}` as argv[2] to compute-staged-tests-token.js.;Removed duplicate `isMainWorktree()` and execSync from `bin/compute-staged-tests-token.js`; wired to SSOT resolver. <!-- compose-doc-append-sentinel: branch=fix/fix-882 pr=#1417 -->

Test gap: no L2 test for RT-1 staged-file selection via session state before this fix

### BUGFIX: PR #1418 — fix/fix-1415 (2026-07-13, e2f3f783b48c366dc2211a27ac4d81ca648e71f4, #1418)

Background: PR #1418 merged on 2026-07-13.

Changes: fix(#1415): add record_type enum field to supervisor findings; supervisor-off-proposal-shim excludes escape_hatch_event from blockingFindings, resolving cycle where WORKTREE_OFF/WORKFLOW_OFF sentinel audit records blocked subsequent OFF sentinel emits in the same session <!-- compose-doc-append-sentinel: branch=fix/fix-1415 pr=#1418 -->

### FEATURE: PR #1420 — feature/canary5-6git (2026-07-13, 79547ed278cb7d8836b8db76c85a602af628fa12, #1420)

Background: feat(#1400,#1401): WRITE_PATTERNS→IR migration — retire green-group + git, typed write-target contract, fail-closed security convergence

Changes: FEATURE #1400: canary-5 — retire 12 "green-group" WRITE_PATTERNS surface-regex entries (posix-redir ×2, pwsh ×7, file-op rm/cp/mv ×3) from `hooks/lib/bash-write-patterns/patterns.js` and `STRIP_KINDS`. Write-detection for these groups is now performed exclusively by the IR-based predicates `isPosixRedirWriteIR`, `isPwshWriteIR`, and `isFileOpWriteIR` in `bash-write-targets.js`. Added typed write-target contract `{resolveVia:"ancestor"|"self", path}` at the `collectWriteTargetsFromSegments` collector layer; all downstream consumers (`bash-write-scope.js`, block hooks) updated to use `.path`. Migrate sibling test suites (feature-parallel-sessions-worktree-bash-patterns, fix-1391, fix-416, refactor-1294, fix-876, fix-fd-redirect) to assert new `classify()=="read"` + IR predicate contract.;FEATURE #1401: canary-6-git — retire 18 git WRITE_PATTERNS surface-regex entries. Introduce `isGitWriteIR` (read-allowlist + unknown→write fail-closed, basename-aware for /usr/bin/git/git.exe) in new `hooks/lib/bash-write-patterns/git-write-ir.js`. Add `extractGitWriteTargets` in `hooks/lib/bash-write-targets/git.js` returning `{resolveVia:"self", path:repoRoot}` self-target. Redesign `findRepoRootForBash` to resolve scope per write-segment's own IR argv (segment-aware, quote-aware; cross-segment/quoted --work-tree mis-scope → fail-closed to in-session). Add `resolveGitSubArgv` (skips git global flags via parse-git-args.js SSOT). Add `peelWrappers`/`scanWrappedVerb` with basename lookup so `/usr/bin/env git commit`, `/usr/bin/nice git commit` are caught. Add `isEverySegmentExcluded` git/gh per-segment write predicate (prevents `cp … && git commit` allow bypass). Add `isCommandSubstWriteIR` (writes in `$(…)`/backtick), `isNewlineInjectedWriteIR` (newline-injected writes), `isExoticExecWriteIR` (eval/xargs/find-exec/process-subst/sh-c/pwsh-Command). All exotic predicates wired into fast-allow gate, `isEverySegmentExcluded`, and `isReadOnlyInterpreterC`. Security: all fail-closed (unknown subcommand→write, ambiguous scope→in-session block, dynamic eval body→write). Canary suite 326 cases green. <!-- compose-doc-append-sentinel: branch=feature/canary5-6git pr=#1420 -->

### FEATURE: PR #1429 — fix/fix-supervisor-1342-1381-1374 (2026-07-13, 5f02c2f, #1429)

Background: PR #1429 merged on 2026-07-13.

Changes: BUGFIX #1342: SC-6 Final Report `findingsRender` call used `forFinalReport: true` (full per-finding detail dump); changed to `summaryOnly: true` to emit a count+severity summary line only, suppressing the verbose dump in session-close output.;BUGFIX #1381: SC-6 did not clear `audit_phase` / `audit_armed_at` after Final Report; added `--clear-audit-phase` flag to `bin/supervisor-write-audit` and wired it into the SC-6 completion block so audit state is reset on session close, eliminating the infinite audit re-arm loop that fired after every Final Report.;BUGFIX #1374: `checkSupervisorPreMerge()` blocked on any non-null `audit_verdict` including stale WARN verdicts (regression from PR #1360); fixed to skip the warning-flush block when a fresh non-BLOCK audit verdict exists (isAuditVerdictFresh: audit_last_run_at >= newest finding timestamp); only BLOCK verdict always gates; stale WARNs and fresh WARN/CONTINUE verdicts no longer block merges. Test gap: no test asserted that a WARN verdict with a timestamp older than the newest finding bypasses the pre-merge gate. <!-- compose-doc-append-sentinel: branch=fix/fix-supervisor-1342-1381-1374 pr=#1429 -->

### FEATURE: PR #1430 — feature/1145-we15-adaptive (2026-07-13, d278f895c2a858bb62c45c5679779b3112668658, #1430)

Background: feat(#1145,#1404): WE-15 adaptive block message + cleanup-cascade/SKILL.md guardrails

Changes: FEATURE #1145 #1404: Added adaptive OFF-block message in `supervisor-off-proposal-shim.js` that detects worktree-end phase via `isWorktreeEndEnv()` (new `hooks/lib/worktree-end-env-anchor.js`) and surfaces `/sweep-worktrees` + WE-16 fallback guidance instead of generic text. Strengthened `cleanup-cascade.md` WE-15/WE-16 and `SKILL.md` with explicit prohibition of `WORKTREE_OFF` and `--force`, and sweep-auto-reclaim guidance. <!-- compose-doc-append-sentinel: branch=feature/1145-we15-adaptive pr=#1430 -->

### FEATURE: PR #1433 — feature/test-design-split-1431 (2026-07-13, 251243173b74c74fa70e249cf35e84df805e4a28, #1433)

Background: feat(#1431): test-design.md progressive-disclosure split + Pattern 4 classifier coverage

Changes: feat(#1431): progressive-disclosure split of `skills/_shared/test-design.md` (288L → 180L entrypoint + `test-design/parser-regex-tests.md` + `test-design/protection-fix-tests.md`); added Classifier/guard cases Pattern 4 bullet to Test Case Categories (CPR-5 both-direction coverage, origin: #1425); updated `write-tests` WT-1 and `review-tests` RT-0a to read `rules/core-principles.md` first; wired `review-tests/scripts/run-codex-review-loop.sh` and `agents/test-reviewer.md` to load detail files conditionally; updated test comment pointers in feature-1308 and feature-1340 test helpers <!-- compose-doc-append-sentinel: branch=feature/test-design-split-1431 pr=#1433 -->

### FEATURE: PR #1434 — docs/issue-1428 (2026-07-13, 4fc6a17, #1434)

Background: feat(#1428): rename review-skill-size → review-prompt-size; extend to rules/*.md, agents/*.md, skills/_shared/*.md

Changes: Renamed `bin/review-skill-size` → `bin/review-prompt-size` and extended diff-mode and `--all` scan to cover `rules/*.md`, `agents/*.md`, and `skills/_shared/*.md` in addition to `SKILL.md`. Updated `rules/coding/file-split.md` Pattern B and `rules/prompt.md` scope definition. Updated all references in `README.md`, installer scripts, and `run-quality-gates.sh`. Closes #1428. <!-- compose-doc-append-sentinel: branch=docs/issue-1428 pr=#1434 -->

### FEATURE: PR #1437 — fix/1435-precommit-env (2026-07-14, 12bea8c, #1437)

Background: fix(#1435): block modified .env files in pre-commit (--diff-filter=AM)

Changes: BUGFIX: pre-commit `--diff-filter=A` → `AM` — block modified .env files at commit time (#1435). Test gap: no test verified that modifying an existing tracked .env was blocked; Test 7 expected rc=0 (gap). Added Test 7 rc=1 and Test 8 (gh api failure → treat as public). <!-- compose-doc-append-sentinel: branch=fix/1435-precommit-env pr=#1437 -->

### FEATURE: PR #1438 — fix/1436-fd-dup (2026-07-14, b7c4f91416c105fd1ccfd23a4f02105222c1f948, #1438)

Background: fix(#1436): exclude FD-to-FD redirects from isPosixRedirWriteIR write detection

Changes: Added `&& !/^&\d/.test(r.targetRaw)` to `writeRedirs` filter in `isPosixRedirWriteIR` (bash-write-targets.js:93) to exclude FD-to-FD redirects (`2>&1`, `>&2`) from write-target detection. Root cause: PR #1420 (2026-07-12) retired surface-regex entries with FD-to-FD negative-lookahead (from BUGFIX #243, PR #304) without porting the guard to `isPosixRedirWriteIR`. Four regression rows added to commit2-green-retire.sh POSIX_TABLE: PR-BUG-FD2/FD3 (Red→Green) and PR-BUG-FDQ/FDQ2 (always-true guard against `r.target`-based regression). Test gap: no `isPosixRedirWriteIR` regression test for FD-to-FD redirects (`2>&1`) as a non-write case. <!-- compose-doc-append-sentinel: branch=fix/1436-fd-dup pr=#1438 -->

### FEATURE: PR #1439 — fix/meta-filter-complexity-1427 (2026-07-14, f6c9163, #1439)

Background: fix(workflow-init): meta-label exclusion, Path A complexity recording, record-complexity-and-skip

Changes: BUGFIX #1427/#1394/#1413: Added meta-label exclusion axis to filter-primary-candidates.sh (extended Pass A to --json parent,labels; Pass B now drops meta candidates when at least one non-meta candidate survives). Added complexity evaluation + outline skip substeps to workflow-init Path A. Extracted the 3-step node sequence from clarify-intent CI-C1b/C1c into shared script bin/workflow/record-complexity-and-skip; rewrote both SKILL.md callers as thin wrappers. Fixed shell-variable interpolation into node -e double-quoted body (CWE-78): switched to process.env pattern. Split tests/feature-1351-skip-conditions-from-complexity.sh into _lib.sh + behavioral.sh to resolve HARD line-limit violation. Test gap: FP-10/FP-11/FP-12 tests for the meta-label exclusion axis were absent before this fix <!-- compose-doc-append-sentinel: branch=fix/meta-filter-complexity-1427 pr=#1439 -->

### FEATURE: PR #1451 — fix/1443-1442-worktree-context (2026-07-17, 9af4f07855de1502288e7e04aa2e776790830145, #1451)

Background: fix(#1443,#1442): WE-7/WE-8 linked-worktree CWD directives + CWD-independent session-ID fallback

Changes: #1443: WE-7/WE-8 in worktree-end SKILL.md now direct the model to keep CWD in the linked worktree until WE-13 (step text + Rules bullet, CPR-5 symmetric). New single-responsibility PostToolUse hook `hooks/detect-worktree-conflict.js` (registered in settings.json, matcher `Bash|runInTerminal|runCommands`) emits one non-blocking `additionalContext` guidance message when a failed command's stderr matches `fatal: '<branch>' is already used by worktree`. Deliberately one pattern + one message; generalization deferred to #1447. Test gap: no test asserted WE-7/WE-8 CWD directives or any guidance path for worktree-conflict git errors.;#1442: session-ID resolution no longer depends on CWD. Both `resolveWorkflowSessionId()` (new Priority 1d) and `resolveSessionId()` (new Priority 6c) gained a symmetric sibling-worktree scan that reads WORKTREE_NOTES.md Session-ID from linked worktrees after all env-var priorities fail: own worktree (CWD or ancestor) wins, a single sibling resolves, multiple distinct sibling IDs return null (fail-safe, no fall-through to depth-score/JSONL scans). Codex security review found and this session fixed a HIGH own-worktree false-ambiguity bug (subdirectory CWD collected own root as a sibling) via fail-before-fix tests. Test gap: no test called either resolver from a main-worktree or subdirectory CWD with session env vars absent. <!-- compose-doc-append-sentinel: branch=fix/1443-1442-worktree-context pr=#1451 -->

### FEATURE: PR #1458 — feature/workflow-init-driver (2026-07-17, cc0a35bea927848ba09fb97d0ce02be29cdadf92, #1458)

Background: feat(workflow-init): consolidate WI-2..WI-9 into resumable Node.js driver

Changes: #1446: Consolidated workflow-init's mechanical steps WI-2..WI-9 (issue-token detection, per-N `gh issue view` fetch, aggregate WIP check, CLOSED detection, label extraction, route decision, context.md write) into a resumable Node.js state-machine driver `bin/workflow/workflow-init-driver` + `bin/workflow/lib/workflow-init/` (7 phase modules, checkpoint.js, directive.js, spawn-env.js) emitting next-step-style `ACTION=` directives (invoke/done/blocked/ask_user/emit_sentinel) with checkpoint `--resume` support; SKILL.md rewritten as a driver loop (120→92 lines). Background: ds4-model sessions deviated from prose-step interpretation (WI-3 ambiguity, wrong ENFORCE_WORKTREE-block recovery). Includes the aggregate-WIP ALL_NONE unreachable-branch fix (evaluation order now error → any_other → all_none → all_same → mixed) and three security-review fixes (sentinel stripping restored in context.md write, CWE-77; CLAUDE_SESSION_ID validated against `/^[A-Za-z0-9_-]+$/` before path use, CWE-22; OPTIONS_DISPLAY percent-encoded symmetrically with QUESTION). Retired 4 scripts (aggregate-wip-check.sh, wip-set-resume.sh, closed-detection.sh, list-open-sub-issues.sh); new tests/feature-workflow-init-driver/ (103 assertions) + top-level dispatcher.;#795: Abolished the remaining "primary issue" concept — `filter-primary-candidates.sh` renamed to `filter-init-candidates.sh`; `path-a-label-and-board.sh` internal names PRIMARY/RELATED renamed to FIRST_N/SIBLINGS; all closes_issues entries are processed symmetrically in the driver's route decision.;#996: Removed all WORKFLOW_ABORTED_* sentinels (6 occurrences across workflow-init and clarify-intent SKILL.md) — audit found no consumer; abort paths now surface as driver `ACTION=blocked` or a plain stop. <!-- compose-doc-append-sentinel: branch=feature/workflow-init-driver pr=#1458 -->

### FEATURE: PR #1459 — fix/enforce-worktree-ir-regression (2026-07-17, ff050931, #1459)

Background: fix(enforce-worktree): restore scratchpad-redirect and New-Item-Directory allow paths lost in PR #1420 IR-signature migration

Changes: #1109: Investigated the 8 reported main-worktree false-blocks attributed to PR #1420's `collectBashWriteTargets` IR-signature migration (`(cmd)` → `(ir, repoRoot)`). The plans-dir heredoc / `node -e` writes were already green (PR #1179); the residual scratchpad-redirect variants were the regression. Fixed by teaching `expandStaticShellTokens` to expand `$SCRATCHPAD`/`${SCRATCHPAD}` only when they resolve under `<os-tmpdir>/claude/`, and adding the `areAllBashTargetsUnderClaude` allow predicate (fail-closed: allowed only when ALL targets resolve under the session scratchpad, tightened to `$SCRATCHPAD` when set). New SSOT `hooks/lib/claude-scratchpad-base.js` guards against TEMP/TMP poisoning by excluding paths that resolve inside the repo.;#1441: Restored the `New-Item -ItemType Directory` allow path that PR #1420 dropped when pwsh WRITE_PATTERNS were retired (on Windows, `findRepoRoot` on a non-existent external path fell back to process CWD → main repo root → block). Extracted the logic into `hooks/enforce-worktree/main-worktree-allows/new-item.js` with head-anchored dispatch, argv0 verification, and PowerShell single-quote literal semantics (`'$SCRATCHPAD/x'` stays literal → in-repo → block). The supervisor scratchpad stdout-redirect gap is covered by the same `areAllBashTargetsUnderClaude` predicate.;#1290: Same New-Item root cause as #1441 — `New-Item -ItemType Directory` issued during `/worktree-start` WS-5 (sanctioned parent-dir creation) is allowed again through the restored branch. Kept fail-closed for `-ItemType File`, in-repo targets, and chained commands.;#923: Confirmed `git worktree remove` (and add/prune) from the main worktree is allowed after restructuring `isAllowedWorktreeCommand` into a head-first dispatch (git-worktree vs New-Item) and splitting the 600-line module to satisfy the 500-line HARD limit (`worktree-command.js`, `worker-script.js`, `new-item.js`). Added a permanent regression canary (`tests/fix-1441-new-item-scratchpad-allow.sh`) so the restored allows cannot silently regress again. <!-- compose-doc-append-sentinel: branch=fix/enforce-worktree-ir-regression pr=#1459 -->

### FEATURE: PR #1460 — feature/labels-ssot-propagation (2026-07-17, 8a9364a72c66a279eba3841f5a7abe7d4066d29a, #1460)

Background: feat(#1261): labels.yml SSOT propagation to sibling repos via CI

Changes: FEATURE #1261: labels.yml SSOT propagation to sibling repos — new `bin/github-issues/propagate-labels.sh` (PAT-gated, independent-sibling loop, security: SIBLING regex validation, path-traversal guard on CANONICAL_LABELS_FILE, remote-URL PAT strip after clone, GIT_WORK_DIR cleanup trap); `propagate` job added to `.github/workflows/sync-labels.yml` with `needs: sync` and job-env `HAS_PAT` boolean gate (PAT not in job-level `if:`); `docs/ops.md` PAT creation / repo-scope / first-run runbook; test suite 13 cases (L2 mock git/gh). <!-- compose-doc-append-sentinel: branch=feature/labels-ssot-propagation pr=#1460 -->

### FEATURE: Supervisor alert actionable-only output (#1450, PR #1461) (2026-07-17, 7907234)

Background: EM Supervisor alert mode injected all findings including notice-severity into the main agent context window on every review, causing unnecessary context window pollution with non-actionable information. Approach D selected: Stop hook renders actionable summary deterministically from the state file, making subagent prose output irrelevant to the main context.

Changes: Added actionableOnly mode to formatLayer2Findings (severity>=warning filter, /issue-create hint for actionable categories). Wired actionableOnly:true in stop-l2-findings-display.js (Stop hook hard guarantee). Constrained supervisor.md Reporting back section to fixed one-line ack. Added bin/supervisor-render-alert CLI for manual debugging. Security: applied escapeTokens+newline-collapse in actionableOnly branch (sibling parity with forFinalReport); added SESSION_ID_RE validation to CLI.

### FEATURE: PR #1471 — feature/1255-supervisor (2026-07-17, 6afb230, #1471)

Background: fix(#1255): supervisor reportBlock severity notice + block finding class dedup

Changes: fix(#1255): supervisor `reportBlock()` severity changed "error" → "notice" so hook blocks alone do not arm alert mode (notice short-circuit in `ensureAlertScheduled`); `appendFinding()` extended with session-wide class dedup (reporter + command key) that collapses repeated block findings from the same hook into a `class_dedup_count` field, reducing state file churn <!-- compose-doc-append-sentinel: branch=feature/1255-supervisor pr=#1471 -->

### FEATURE: PR #1472 — feature/canary-6a (2026-07-17, 15da2e27404d616a9cfce45594ecdb3398abefe4, #1472)

Background: feat(#1411): pkg-mgr × 7 + interpreter-c IR migration + retire (canary-6a Phase 2)

Changes: FEATURE #1411 (canary-6a Phase 2): Retired 8 surface-regex WRITE_PATTERNS entries — npm-write, pnpm-write, yarn-write, pip-write, uv-write, cargo-write, go-write, interpreter-c — replacing them with IR-based predicates isPkgMgrWriteIR and isInterpreterCWriteIR. New hooks/lib/bash-write-targets/pkg-mgr.js: read-allowlist fail-closed classifier (npm/pnpm/yarn/pip/pip3/uv/cargo/go) with extractPkgMgrWriteTargets ({resolveVia:"self"} self-target). bash-write-targets.js extended with isInterpreterCWriteIR + hasCFlag helper (case-insensitive PowerShell flags, combined POSIX flags like -lc). Both predicates wired into fast-allow gate (enforce-worktree.js), isEverySegmentExcluded + collectBashWriteTargets (bash-write-scope.js), innerSegIsWrite (classify.js). Security review found and fixed 4 bugs: (1) HIGH — isInterpreterCWriteIR early-return false-allow on multi-segment commands (bash -c 'read-only'; sh -c 'rm f'); (2) pwsh -command (lowercase) not recognized as -c flag; (3) bash -lc (combined POSIX flag) not recognized; (4) npm ci false-positive from ci-alias pwsh-alias pattern — fixed by suppressedPatterns in classify.js. <!-- compose-doc-append-sentinel: branch=feature/canary-6a pr=#1472 -->

### FEATURE: PR #1473 — refactor/scriptify-mop-skill-1464 (2026-07-17, 0325115, #1473)

Background: refactor(#1464): extract inline node -e from make-outline-plan/SKILL.md into scripts

Changes: REFACTOR #1464: Extract inline `node -e` calls from `skills/make-outline-plan/SKILL.md` (MOP-1d, MOP-C1) into `skills/make-outline-plan/scripts/check-outline-skip.sh` and `check-detail-skip.sh`; update tests that grep those patterns <!-- compose-doc-append-sentinel: branch=refactor/scriptify-mop-skill-1464 pr=#1473 -->

### FEATURE: PR #1474 — chore/1467-write-tests (2026-07-17, a94bcb2a360e85f60921bfe343bf7cbed7201220, #1474)

Background: refactor(#1467): scriptify issue-create Phase 4-5 dispatch

Changes: REFACTOR #1467: scriptify issue-create Phase 4-5 dispatch — extracted Phase 4 bulk-sub-of TSV manifest write + dispatch call and Phase 5 worktree-notes-append loop from skills/issue-create/SKILL.md (175 lines, WARN >100) into skills/issue-create/scripts/run-bulk-dispatch.sh and skills/issue-create/scripts/run-phase5-record.sh; SKILL.md Phase 4/5 sections replaced with single-line script invocation directives, resolving rules/prompt.md §1.3 violation <!-- compose-doc-append-sentinel: branch=chore/1467-write-tests pr=#1474 -->

### FEATURE: PR #1475 — feature/1465-scriptify-clarify-intent (2026-07-17, b027a0d6116d4bca70c8f36a7d0cf690bd4dc31a, #1475)

Background: feat(#1465): scriptify clarify-intent/SKILL.md completion and outline-skip dispatch

Changes: FEATURE: scriptify clarify-intent/SKILL.md completion and outline-skip dispatch (#1465) — Background: rules/prompt.md §1.3 requires inline 3+ step procedures to move to CLI; SKILL.md was at 160 lines (WARN exceeded). Two multi-step completion procedures remained inline. Changes: added `skills/clarify-intent/scripts/run-completion.sh` (orchestrates NON_GITHUB gate, closes_issues parsing via parse-closes-issues.js, clarify-commit-scope.sh + clarify-guard-loop.sh; returns single stdout token PROCEED|CREATED:N|CLOSED:N|RC2|NEED_ISSUE|RETRY_EXHAUSTED|CLOSED_ENTRY); added `skills/clarify-intent/scripts/check-complexity-skip.sh` (SKIP_MODE=auto/judgment dispatch, record-skip-judgment call, sentinel echo + SENTINEL_EMITTED/NO_SENTINEL protocol). SKILL.md Completion section and CI-C1c now delegate to these scripts; file reduced from 160 to 135 lines. <!-- compose-doc-append-sentinel: branch=feature/1465-scriptify-clarify-intent pr=#1475 -->

### FEATURE: PR #1476 — feature/1466-detect-non-github (2026-07-17, 5b0e771, #1476)
Background: refactor(#1466): extract NON_GITHUB detection into shared bin/detect-non-github.sh
Changes: refactor(#1466): extract NON_GITHUB detection into shared bin/detect-non-github.sh. Background: Both commit-push/SKILL.md and issue-close-stage/SKILL.md contained a verbatim ~10-line inline bash case block for NON_GITHUB detection (from PR #373, 2026-05-18); the duplicate violated CPR-2 (SSOT). Changes: Created bin/detect-non-github.sh, a shared wrapper around bin/is-github-dotcom-remote that emits the context-specific skip message to stdout and normalizes the 3-value exit-code contract (0=GitHub or fail-open, 1=non-GitHub). Replaced the inline blocks in both SKILL.md files with a single-line invocation. Updated non-github-remote-gate.md to document the Shared detection wrapper tier and annotate each consumer. <!-- compose-doc-append-sentinel: branch=feature/1466-detect-non-github pr=#1476 -->

### FEATURE: PR #1477 — feature/issue-1470 (2026-07-17, 60572a88bba2c2b3b37fa85e03ec99806a53dc9a, #1477)
Background: feat(#1470): add check-inline-procedures quality gate to WF-CODE-6
Changes: Add `bin/check-inline-procedures` quality gate to WF-CODE-6: detects inline numbered procedure blocks (≥3 consecutive `N. ` lines at column 0) in `skills/*/SKILL.md`, `agents/*.md`, and `skills/_shared/*.md`; emits advisory WARN findings. Integrated into `run-quality-gates.sh` alongside `review-prompt-size`. Always exits 0. Closes #1470. <!-- compose-doc-append-sentinel: branch=feature/issue-1470 pr=#1477 -->

### FEATURE: PR #1478 — feature/1468-scriptify-survey-history (2026-07-17, 3913c52, #1478)
Background: feat(#1468): scriptify survey-history/SKILL.md — extract 4 inline violations to scripts
Changes: #1468: scriptify survey-history/SKILL.md — extracted 4 rules/prompt.md §1.3/§1.5 violations to scripts/; reduces SKILL.md from 132 lines to 88 lines (WARN resolved) <!-- compose-doc-append-sentinel: branch=feature/1468-scriptify-survey-history pr=#1478 -->

### FEATURE: PR #1479 — refactor/scriptify-agent-files-1469 (2026-07-17, 4ed0aa4d287b4337224d42448dbc555f9a79489a, #1479)
Background: refactor(#1469): scriptify 5 agent .md files — extract inline procedures to sibling dirs
Changes: Extracted inline procedure blocks from 5 agent .md files to sibling directories (agents/<name>/); created agents/lib/planner-review-loop-protocol.md for the shared Risk-Signal File protocol (outline/detail planners); new sibling files: agents/outline-planner/output-format.md, agents/detail-planner/procedure.md, agents/detail-planner/supplementary-rules.md, agents/issue-close-finalize-worker/state-schema.md, agents/outline-reviewer/concern-identifiers.md, agents/detail-reviewer/concern-identifiers.md; line counts reduced from 108–173 to 76–111; contract sections (Verdict Format, Required response trailer, Output contract, Procedure verdict) kept inline per CPR-6 orchestrator integrity requirement. <!-- compose-doc-append-sentinel: branch=refactor/scriptify-agent-files-1469 pr=#1479 -->

### FEATURE: PR #1481 — feature/canary-7 (2026-07-17, c842367, #1481)
Background: feat(#1402,#1296): canary-7 pwsh-alias/pwsh-encoded/here/file-op IR retire + canary-final
Changes: FEATURE #1402: canary-7 — Retired 23 surface-regex WRITE_PATTERNS entries (pwsh-alias: sc/ac/ni/ri/mi/ci × 6; pwsh-encoded: encoded-command/ps-stop-parsing × 2; here-doc/here-string × 4; file-op: sed-inplace/perl-inplace/patch/touch/chmod/dd/rsync/tar-extract/unzip/gunzip/bunzip2 × 11) into IR predicates. New modules: hooks/lib/bash-write-targets/here.js (isHereWriteIR), encoded.js (isEncodedCommandWriteIR), file-op.js (isExtendedFileOpWriteIR + extractFileOpTargets). All three wired into fast-allow gate (enforce-worktree.js), isEverySegmentExcluded + collectBashWriteTargets (bash-write-scope.js), and innerSegIsWrite (classify.js C4). isPwshWriteIR extended with mi/ci. WRITE_PATTERNS reduced to 4 here-doc entries; STRIP_KINDS emptied. classify() returns "read" for retired verbs (IR predicates handle detection at hook level).;FEATURE #1296: canary-final — AT-DP2 bridge condition removed (WRITE_PATTERNS no longer drives the block decision for retired entries); block-extras.js inlined into enforce-worktree.js; Phase 2 WRITE_PATTERNS retirement complete. <!-- compose-doc-append-sentinel: branch=feature/canary-7 pr=#1481 -->

### FEATURE: PR #1480 — feature/scriptify-session-close (2026-07-17, cd9cb305c9fd1da801cdec7434cf33d7382f22d9, #1480)
Background: refactor(#1463): scriptify session-close/SKILL.md
Changes: REFACTOR #1463: scriptify session-close/SKILL.md — extracted SC-1a WF-META detection (`bin/session-close-detect-wf-meta.js`), SC-6 Final Report rendering (`bin/render-final-report.js`), and SC-7 findings render (`bin/session-close-render-sc7.js`); added `renderFinalReport()` + `buildPostMergeLines()` to `hooks/lib/final-report-schema.js` (SSOT move from stop-final-report-guard); SKILL.md reduced from 222 to 196 lines (HARD limit resolved). <!-- compose-doc-append-sentinel: branch=feature/scriptify-session-close pr=#1480 -->

### FEATURE: PR #1490 — feature/1423-write-detector-test (2026-07-17, 64ce86f7b001fa4617dabdfd464ae4d57e67155d, #1490)
Background: feat(enforce-worktree): report write predicate name in block reason
Changes: Background: `enforce-worktree.js` block responses were opaque — when a Bash write command was blocked, the reason message did not identify which write-detection predicate fired, making it difficult to understand and debug unexpected blocks.;Changes: Added `hooks/enforce-worktree/write-detector.js` (`detectWritePredicate(ir)`) — checks all 13 write predicates in order and returns the first match as `{name, detail}`; wired `_writeDetector` variable in the `Bash` branch of `enforce-worktree.js`; appended `\nDetected by: <detail> (<name>)` suffix to all 5 block-site reasons (3 generic + 2 gh-specific). Added L2 test `tests/feature-1423-enforce-worktree-reject-reason.sh` covering all 13 predicate families (14 assertions, all green). <!-- compose-doc-append-sentinel: branch=feature/1423-write-detector-test pr=#1490 -->

### FEATURE: PR #1489 — fix/fix-1483-issue-create-allowlist (2026-07-17, 90c5537, #1489)
Background: fix(#1483): add missing scripts to enforce-worktree SANCTIONED allowlist
Changes: fix(#1483): Add missing scripts to enforce-worktree SANCTIONED allowlist — `bin/github-issues/issue-create-dispatch.sh`, `skills/issue-create/scripts/run-bulk-dispatch.sh`, and `skills/issue-create/scripts/run-phase5-record.sh` were absent from the SANCTIONED array in `hooks/enforce-worktree/main-worktree-allows/worker-script.js`. The `ISSUE_CREATE_SKILL=1` prefix only bypasses `enforce-issue-close.js`, not `enforce-worktree.js`, so calling these scripts from the main worktree was blocked. The false claim in `skills/issue-create/SKILL.md` ("callable from main worktree") was replaced with accurate guidance requiring a linked worktree when `ENFORCE_WORKTREE=on`. Repeated blocks accumulated supervisor warnings and sealed the WORKTREE_OFF escape hatch, causing a two-level deadlock that required manual intervention. <!-- compose-doc-append-sentinel: branch=fix/fix-1483-issue-create-allowlist pr=#1489 -->

### FEATURE: PR #1491 — fix/1145-cleanup-window (2026-07-17, 9d1c1e5fab84e46c5b5a5186487cccbea4c248e7, #1491)
Background: fix(#1145): narrow isWorktreeEndEnv() to WE-15..WE-22 cleanup window
Changes: BUGFIX #1145: isWorktreeEndEnv() post-WE-22 false-positive fixed — new worktree-cleanup-marker.js CLI creates <sid>-wt-cleanup-active at WE-14b and deletes at WE-22a; isWorktreeEndEnv() now checks marker existence (not final-report-env.json content); anchor and shim tests updated with marker-file fixtures; T-anchor-6, T4j (KEY REGRESSION) and feature-1145-cleanup-marker.sh added. Test gap: no test for post-WE-22 scenario (env-json present + marker absent → false). <!-- compose-doc-append-sentinel: branch=fix/1145-cleanup-window pr=#1491 -->

### FEATURE: PR #1494 — fix/write-detector-1424-1425-1448 (2026-07-17, 36d0102, #1494)
Background: fix(enforce-worktree): write-detector false-negatives/false-positives (#1424 #1425 #1448)
Changes: fix(#1424): isGhWriteIR first-segment-only false-negative — `isGhWriteIR` looped over IR segments with `break` after finding the first `gh` segment, causing it to evaluate only that segment. A sequenced command like `gh status && gh pr merge 123` returned false (first segment is read-only). Fixed by removing `break` and evaluating each segment immediately; mirrors `isGitWriteIR` per-segment pattern (CPR-5 symmetry).;fix(#1425): isNewlineInjectedWriteIR backslash-continuation false-positive — `isNewlineInjectedWriteIR` split lines on `[\r\n]+` before folding `\`+newline continuations, causing continued lines to be passed as independent commands to `innerCommandIsWrite`. `isInterpreterCWriteIR` then matched `bash -c` inside quoted argument values on those stripped continuation lines, triggering false-positive blocks on sanctioned multi-line supervisor-report calls. Fixed by inserting `stripped.replace(/\\[ \t]*\r?\n/g, ' ')` (fold) before split, preserving quoting context.;fix(#1448): write-detector false-positives on non-repo redirect and trailing read-only suffix — (A) Sequenced commands with a trailing read-only segment (e.g. `git log > /tmp/out; echo RC=$?`) fell through to `abstain` because `hasCommandSequencingOutsideHeredoc` returned early before any target scope check. Fixed by adding `areAllWriteSegmentsOutsideSessionScope` (new function in `bash-write-scope.js`) that classifies each segment as read / target-write / targetless-write and allows when all write-segments' targets are outside session scope; targetless-write (gh, interpreter, exotic) remains fail-closed. (B) From a non-git CWD, single-command redirects to paths outside any git repo (e.g. `/tmp`) were blocked because `repoRoot` was null and the Bug 2 allow path required `repoRoot` to be truthy. Fixed by adding `targets.every(t => findRepoRoot(t.path) === null)` as an additional disjunct, allowing writes to provably non-git paths while preserving the existing block for targets that resolve inside any git repo. <!-- compose-doc-append-sentinel: branch=fix/write-detector-1424-1425-1448 pr=#1494 -->

### FEATURE: PR #1493 — feature/1482-post-compact-progress (2026-07-17, 2100697093f8898e77e474a79a6185e22bc9ef20, #1493)
Background: fix(#1482,#861,#1161): post-compact workflow progress + conv-lang any no-op + pr-merge reset_reason
Changes: BUGFIX #861: conv-lang.js — `CONV_LANG=any` was not in the no-op list, causing spurious "Respond to the user in any." injection. Added `any` (case-insensitive after normalize) alongside `english` and empty-string. Test gap: no test for `CONV_LANG=any` return value in existing unit-helper.sh.;BUGFIX #1161: workflow-mark.js — `gh pr merge` detection reset `user_verification` to pending without recording why. Added `{ reset_reason: "post-merge" }` as extraFields to `markStep()`, so post-compact can annotate the step as "(reset after pr merge — expected)" rather than leaving the reader to wonder whether the reset was intentional. Test gap: no integration test for the `reset_reason` field on `user_verification` after pr-merge detection.;FEATURE #1482: post-compact.js — resumed assistants had only the lossy LLM-generated compaction summary to infer workflow progress. Added an inline 10-step workflow progress block (WORKFLOW_STEPS constant, direct `readState()` call) to the PostCompact `additionalContext` injection. Also surfaces `cwd`/`git_branch` from state and annotates `user_verification: pending` with "(reset after pr merge — expected)" when `reset_reason === "post-merge"` (pairs with #1161). Fail-open: missing/corrupt state file emits "(no state file found — run /workflow-init)". <!-- compose-doc-append-sentinel: branch=feature/1482-post-compact-progress pr=#1493 -->

### FEATURE: PR #1495 — fix/1484-pre-flight-eval-allow (2026-07-17, e2a52ff, #1495)
Background: fix(#1484): allow eval-wrapped pre-flight.sh in isAllowedWorkerScriptInvocation
Changes: Fix #1484: `isAllowedWorkerScriptInvocation` in `worker-script.js` falsely blocked `eval "$(bash "$AGENTS_CONFIG_DIR/skills/issue-close-finalize/scripts/pre-flight.sh")" || exit 0` from the main worktree under ENFORCE_WORKTREE=on. Added eval-unwrap secondary regex branch and `$AGENTS_CONFIG_DIR` literal prefix normalization; added `skills/issue-close-finalize/scripts/pre-flight.sh` to SANCTIONED list. Closes #1484. <!-- compose-doc-append-sentinel: branch=fix/1484-pre-flight-eval-allow pr=#1495 -->

### FEATURE: PR #1496 — feature/issue-1492-label-taxonomy (2026-07-17, 8600877, #1496)
Background: feat(#1492): rename model:* labels to reporter-model:*, add model-scope:* taxonomy
Changes: Renamed `model:*` label series to `reporter-model:*` (fable/opus/sonnet/ds4) to distinguish "which model reported this issue" (reliability signal) from "which model exhibits this behavior". Added `model-scope:*` series (ds4/devstral/qwen-coder/claude) as an optional label for issues scoped to a specific model's behavior. Added `reporter-model:devstral` and `reporter-model:qwen-coder` for local LLMs running via llama-swap. Created `bin/github-issues/migrate-model-labels.sh` to automate the GitHub-native label rename (RENAME → CREATE → model:others cleanup → #1488 title fix), with dry-run mode and input validation matching the sibling sync-labels.sh. Background: `model:*` conflated two concepts; `[ds4]` title prefix workaround was unergonomic. <!-- compose-doc-append-sentinel: branch=feature/issue-1492-label-taxonomy pr=#1496 -->

### FEATURE: PR #1497 — feature/issue-1487-gh-scope-warn (2026-07-17, d298e401901a6cc2d731abc4ebebf5d2b380a6b6, #1497)
Background: feat(#1487): add soft gh auth 'project' scope check to install.sh + install.ps1
Changes: FEATURE feat(#1487): add soft gh auth 'project' scope check to install.sh + install.ps1 | Background: New users who had not run 'gh auth refresh -s project' got an opaque GraphQL error when using /issue-create or Projects v2; no onboarding guidance existed in install.sh or install.ps1. | Changes: install.sh and install.ps1 each gained a 3-stage soft check (gh present → auth status exit-0 → scope grep); if 'project' scope is absent, a yellow notice on stderr suggests 'gh auth refresh -s project' or '/issue-setup'. Non-blocking (install exits 0 regardless). New L2 test: tests/feature-1487-install-gh-scope-warn.sh (4 cases covering absent-gh / not-authed / scope-present / scope-absent; install.ps1 PowerShell path tracked as L3 gap in issue #1487). <!-- compose-doc-append-sentinel: branch=feature/issue-1487-gh-scope-warn pr=#1497 -->

### FEATURE: PR #1502 — fix/fix-1485-execute-bit (2026-07-17, 4e6ff0d5e2a397b193e2350a180c4780368d298a, #1502)
Background: fix(#1485): add bash/node prefix to direct call sites + chmod+x sweep on bin/
Changes: BUGFIX | Fix execute bit for bin/ scripts: call-site fixes + git update-index sweep (#1485) | Background: 148/172 bin/ scripts were mode 100644 in git due to core.fileMode=false; two confirmed breakage cases (bin/confirm-off:45 direct call to get-config-var without bash prefix, skills/clarify-intent/scripts/check-complexity-skip.sh:42 direct call to bin/workflow/record-skip-judgment without node prefix) caused Permission denied on Unix/macOS/WSL | Changes: Added bash prefix to bin/confirm-off:45; added node prefix to check-complexity-skip.sh:42; ran git update-index --chmod=+x sweep on ~108 .sh + extensionless scripts in bin/, bringing all to mode 100755 | Test gap: No test verified git-recorded file mode (100644 vs 100755) for bin/ scripts, or confirmed that prefix-less direct invocations succeed on a Unix fresh clone <!-- compose-doc-append-sentinel: branch=fix/fix-1485-execute-bit pr=#1502 -->

### FEATURE: PR #1505 — fix/1499-parse-token-hash-guard (2026-07-17, 5ce05b2a404773302792ac6c0460ece523d1c0b3, #1505)
Background: fix(#1499): add ISSUE_TOKEN_CLI_GUARD_RE to parseIssueToken — reject bare digits
Changes: BUGFIX #1499: `parseIssueToken` in `hooks/lib/parse-closes-issues.js` accepted bare digit strings (e.g. `"3.2"`) as issue numbers because `ISSUE_TOKEN_BODY_RE` made the `#` prefix optional. Added `ISSUE_TOKEN_CLI_GUARD_RE` constant requiring `#` immediately before digits; added guard as first check in `parseIssueToken`; exported constant. Added complementary `#`-presence filter in `bin/parse-issue-tokens` for defense-in-depth. `collectEntries()` (bare-number legacy path) unchanged. Test gap: no regression test for version-string token extraction before this fix. <!-- compose-doc-append-sentinel: branch=fix/1499-parse-token-hash-guard pr=#1505 -->

### FEATURE: PR #1507 — feature/1492-label-sync-delete (2026-07-17, 1e99674551dc0d81bc9cda791bc700d5cd6515c2, #1507)
Background: feat(#1492): sync-labels DELETE propagation + migrate pre-delete guard
Changes: feat(#1492): sync-labels.sh DELETE propagation + migrate pre-delete guard — Background: Two related fixes: (1) migrate-model-labels.sh Phase 1 was blocked when CI pre-created empty reporter-model:* labels before the rename script ran, losing issue associations for all 46 model:* issues; (2) sync-labels.sh and propagate-labels.sh had no mechanism to delete labels removed from labels.yml, leaving orphaned labels across sibling repos. Changes: Added pre-delete guard to migrate-model-labels.sh Phase 1 loop (checks for zero-issue target label, deletes it, then proceeds with native gh label edit --name rename). Extended sync-labels.sh from three-way diff (CREATE/UPDATE/SKIP) to four-way diff with DELETE via 3-pass awk; added --dry-run flag gating all mutations (CREATE, UPDATE, DELETE). propagate-labels.sh inherits DELETE automatically with no code change. <!-- compose-doc-append-sentinel: branch=feature/1492-label-sync-delete pr=#1507 -->

### BUGFIX: PR #1512 — feature/1488-malformed-tokens (2026-07-18, 19cd50b82b90df5eb85bcb9c8c19500f6ca021b6, #1512)
Background: fix(#1488): validate workflow-init-driver raw-token args — reject newline/malformed tokens with NEXT_HINT
Changes: Added `validateTokens()` to `bin/workflow/workflow-init-driver` with 3-stage guard: LF/CR → `invalid_token_newline`; no `#\d` prefix or trailing non-digit junk (`!/^[^#]*#\d+$/`) → `invalid_token_format`; length > 64 → `invalid_token_format`. Guard fires in `main()` before `resolveSessionId()` and emits `ACTION=blocked REASON=... NEXT_HINT=...` to stdout (stderr is suppressed by caller via `2>/dev/null`). Catches ds4 multi-line prompt injection at the entry point before `isNewlineInjectedWriteIR` can false-positive on ENFORCE_WORKTREE. `ISSUE_TOKEN_CLI_GUARD_RE` required from `hooks/lib/parse-closes-issues.js` (CPR-2 — not copied). Extended `emitBlocked()` in `directive.js` with optional `NEXT_HINT=` third parameter. Added `<raw-tokens>` explanation line in `SKILL.md` WI-2. New test file `tests/feature-workflow-init-driver/driver-malformed-tokens.sh` (49 assertions, 9 cases M1-M9). <!-- compose-doc-append-sentinel: branch=feature/1488-malformed-tokens pr=#1512 -->
Test gap: no L3 test for real claude -p + non-native LLM session driving workflow-init; L2 driver-malformed-tokens.sh covers validateTokens() guard fully

### FEATURE: PR #1513 — fix/1511-plan-lang-directive (2026-07-18, b5490b54af826dad3091fd62b9b5485ca30009be, #1513)
Background: fix(#1511): add PLAN_LANG directive to outline-planner, detail-planner, and clarify-intent
Changes: fix(#1511): add explicit PLAN_LANG directive to outline-planner, detail-planner, and clarify-intent CI-4 — agents now write plan artifacts in the configured language from the first write, eliminating the reactive re-write cycle triggered by check-plan-lang.js when PLAN_LANG=japanese <!-- compose-doc-append-sentinel: branch=fix/1511-plan-lang-directive pr=#1513 -->

### FEATURE: PR #1515 — fix/923-enforce-worktree-early-exit (2026-07-18, 5e2c5efe3c94a40e4afebcd614d409e531992694, #1515)
Background: fix(#923): enforce-worktree early-exit for git worktree remove/prune
Changes: fix(#923): add early-exit in enforce-worktree.js that evaluates isAllowedWorktreeCommand (for git worktree remove/prune) before the session-scope pipeline; uses CWD-anchored isMainCheckout to block linked-CWD + -C <main> and main-CWD + -C <other-repo> misuse; adds 6 L2 integration tests (T923-L2.A–F) covering allow/block dispatch at the full hook boundary <!-- compose-doc-append-sentinel: branch=fix/923-enforce-worktree-early-exit pr=#1515 -->

### FEATURE: PR #1516 — fix/950-session-worktree (2026-07-18, 96bfb7fcd4f1e45beca9b29edc0bc458dca9c26a, #1516)
Background: fix(#950): resolveSessionWorktreePath falls back to state.session_worktree for mid-session /worktree-start
Changes: BUGFIX #950: `resolveSessionWorktreePath` now falls back to a new `state.session_worktree` field when `state.cwd` points to the main worktree (mid-session `/worktree-start` case). `branching-handler.js` writes the field via `recordSessionWorktree()` on `WORKFLOW_BRANCHING_COMPLETE`. Fixes exit-code-3 failure in `select-staged-files.sh` / `/review-tests` when the session was started from the main worktree. <!-- compose-doc-append-sentinel: branch=fix/950-session-worktree pr=#1516 -->

### FEATURE: PR #1514 — refactor/1508-supervisor-ccuuid (2026-07-18, 62f76e801b9926b606baf2f18045710f88ef459b, #1514)
Background: refactor(#1508): unify supervisor state key on CC UUID
Changes: REFACTOR #1508: Unify supervisor state key on CC UUID — removed wsid-primary routing and dual-store mirror-write from bin/supervisor-report; removed wsid fallback shims and mirror-clear blocks from hooks/supervisor-guard.js and hooks/stop-l2-findings-display.js; split hooks/lib/supervisor-state-writer.js (507 lines → 5 files: shared.js/append.js/alert.js/audit.js) to satisfy HARD file-split limit and remove resolveWorkflowSessionId dependency from ensureAlertScheduled(). CC UUID (input.session_id / CLAUDE_CODE_SESSION_ID) is now the sole auto-resolve path in all contexts. Background: supervisor state files were dual-keyed on CC UUID and wsid due to historical #1021 (findings split) → #1052 (wsid-preferred + dual-store mirror-write workaround). Since #1082 fixed CLAUDE_CODE_SESSION_ID propagation in Bash tool subprocesses, the original motivation for wsid-primary no longer applies. <!-- compose-doc-append-sentinel: branch=refactor/1508-supervisor-ccuuid pr=#1514 -->

### FEATURE: PR #1518 — fix/fix-enforce-worktree-false-blocks (2026-07-18, 314e449, #1518)
Background: fix(enforce-worktree): remove wtCount upper-bound for skill-prefixed stash (#1024)
Changes: issue:#1115 category:BUGFIX subject:"fix(enforce-worktree): POSIX fd-dup redirects (2>&1, N>&1, N>&-) false-block sanctioned git commands via hasShellChaining" background:"hasShellChaining() and rejectInterpreterAndChaining() in shared-cmd-utils.js used the character class [|;&] which matches a lone & character. POSIX fd-dup redirects (2>&1, >&1, N>&-) contain an unquoted & that is not a chaining operator, causing sanctioned git commands with trailing redirects (e.g. git worktree add <path> <branch> 2>&1) to be false-blocked." changes:"Added /\d*>&\d+|\d*>&-/g fd-dup stripping in hasShellChaining() and rejectInterpreterAndChaining() before the chaining character test. &> and &>> redirect-both forms are intentionally not stripped so write targets remain visible to collectBashWriteTargets. New test cases in tests/fix-enforce-worktree-merge-gate.sh and tests/fix-595-bypass-e2e.sh.";issue:#982 category:BUGFIX subject:"fix(enforce-worktree): isAllowedWorktreeCommand unconditional-allow path blocked by hasShellChaining fd-dup false positive (recurrence of #881)" background:"The unconditional-allow path in isAllowedWorktreeCommand for git worktree remove/prune/add was guarded by hasShellChaining(). The [|;&] character class in hasShellChaining() matched lone & in POSIX fd-dup redirects, blocking sanctioned worktree lifecycle commands when the command string contained a redirect like 2>&1." changes:"Covered by the same hasShellChaining/rejectInterpreterAndChaining fd-dup stripping fix as #1115 (shared-cmd-utils.js). No additional files changed specifically for #982.";issue:#923 category:BUGFIX subject:"fix(enforce-worktree): git -C <path> worktree remove/prune false-blocked from main-wt CWD — isAllowedWorktreeCommand did not recognize the -C flag form" background:"isAllowedWorktreeCommand() matched git worktree subcommands by position assuming git was the first token. When git -C <path> worktree remove was issued from the main-worktree CWD, the -C flag shifted the subcommand position and the regex did not match, causing the predicate to return false and block the command." changes:"Added -C path validation to isAllowedWorktreeCommand() in main-worktree-allows/standard.js: rejects multiple -C flags; when -C is present, the argument must resolve case-insensitively to repoRoot. New test cases T923-3 through T923-7 in tests/fix-923-enforce-worktree-c-flag-main-worktree.sh and E-WT-CFLAG cases in tests/fix-595-bypass-e2e.sh.";issue:#838 category:BUGFIX subject:"fix(enforce-worktree): git -C <main-worktree> worktree remove/prune blocked from linked-wt CWD — -C form not recognized by isAllowedWorktreeCommand" background:"When a session ran git -C <main-worktree-path> worktree remove from a linked worktree CWD, isAllowedWorktreeCommand did not validate the -C argument and failed-closed on -C forms it did not recognize, blocking the sanctioned worktree lifecycle command." changes:"Covered by the same isAllowedWorktreeCommand -C validation fix as #923 (main-worktree-allows/standard.js). The -C path is resolved and compared to repoRoot case-insensitively. Tests in tests/fix-923-enforce-worktree-c-flag-main-worktree.sh.";issue:#959 category:BUGFIX subject:"fix(enforce-worktree): sanctioned worker scripts with linked-wt path arguments false-blocked from main-wt CWD — add isAllowedWorkerScriptInvocation predicate" background:"Sanctioned worker scripts (issue-close-stage-worker, commit-push-worker etc.) are launched via bash \"<AGENTS_CONFIG_DIR>/bin/<script>\" with linked-worktree absolute paths as log-redirect targets. When called from the main-worktree CWD, the enforce-worktree main-worktree write guard treated the linked-wt path arguments as main-wt write targets and blocked the invocation." changes:"Added isAllowedWorkerScriptInvocation(cmd, repoRoot) predicate to hooks/enforce-worktree/main-worktree-allows/standard.js. Identity gate: cmd must match bash \"<double-quoted-sanctioned-script>\" against a 5-entry SANCTIONED list. argTail gate: rejects chaining operators and bare & background operator (using /&(?!>)/ to distinguish background from redirect forms). Write target gate: all targets must resolve inside registered linked worktrees (git -C repoRoot worktree list --porcelain); fail-closed on any parse failure or out-of-registry path. Wired into enforce-worktree.js main-worktree allow chain. New tests in tests/fix-959-enforce-worktree-worker-path-arg.sh (8 cases including a security pin for bare & backgrounding).";issue:#1024 category:BUGFIX subject:"fix(enforce-worktree): isAllowedMainWorktreeCleanup false-blocks WORKTREE_END_SKILL=1 git stash pop when zombie worktrees inflate wtCount beyond cap" background:"isAllowedMainWorktreeCleanup in standard.js used a wtCount upper-bound check (wtCount <= maxCount, maxCount = skillPrefixed ? 2 : 1). In repos accumulating zombie worktrees (e.g. 54 registered entries), wtCount > maxCount caused WORKTREE_END_SKILL=1 git -C <main> stash pop to be blocked during /worktree-end WE-20 with 'write blocked. Reason: main worktree'. PR #1126 (2026-06-26) had already added pop/drop/clear to ALLOWED_STASH but did not address the wtCount cap." changes:"Removed the wtCount upper-bound for skill-prefixed stash subcommands only: when skillPrefixed=true and sub==='stash', the function returns true after the wtCount>=1 check, bypassing the maxCount cap. restore and checkout retain the existing cap (wtCount<=2 for skill-prefixed). wtCount<1 early-return preserves fail-closed behavior. JSDoc updated to reflect the new per-subcommand upper-bound policy. New tests S34b (wtCount=4 zombie scenario stash → allow), S34c (no-prefix stash unchanged → block), S34d (skill-prefixed restore wtCount=3 → block), S34e (skill-prefixed checkout wtCount=3 → block). S34 expectation changed from assert_block to assert_allow." <!-- compose-doc-append-sentinel: branch=fix/fix-enforce-worktree-false-blocks pr=#1518 -->

### FEATURE: PR #1506 — feature/1498-stop-premature-stop-guard (2026-07-18, ab9553f, #1506)
Background: feat(#1498): add C4 premature-stop guard Stop hook
Changes: FEATURE #1498: Add C4 premature-stop guard Stop hook — stop-premature-stop-guard.js detects ACTION=invoke from next-step after Claude stops and outputs decision:block to auto-resume; uses CC SID directly (simplified after #1508 unified supervisor state keying to CC UUID); wired in settings.json Stop hooks array; T1-T16 integration tests cover all paths including fail-open edge cases <!-- compose-doc-append-sentinel: branch=feature/1498-stop-premature-stop-guard pr=#1506 -->

### FEATURE: PR #1523 — fix/fix-1509-close-not-planned (2026-07-19, 9e5e4f2d1cc999aa358f8e598203e2cba6bd5e5c, #1523)
Background: fix(#1509): close-not-planned.sh --reason flag value and isNotPlanned detection
Changes: #1509: fix close-not-planned.sh --reason flag: used `not_planned` (underscore) but gh CLI requires `"not planned"` (space); issues silently stayed OPEN. Expanded enforce-issue-close.js isNotPlanned detection to 3 forms; aligned docs/ops.md prose and skills/issue-close-migrated/SKILL.md. Tests P8/P8a/P8b/P9/P9a added. <!-- compose-doc-append-sentinel: branch=fix/fix-1509-close-not-planned pr=#1523 -->

### FEATURE: PR #1525 — fix/scan-outbound-fixes (2026-07-19, 9c789cb4ba8b382183d20f68d9d586d581760890, #1525)
Background: fix: scan-outbound trailing-newline guard, gh api write scanning, test stub regression check
Changes: fix(#1520): `bin/scan-outbound.sh` loadAllowlist/loadBlocklist loops (`while IFS= read -r line`) dropped the last line of allowlist/blocklist files that lacked a trailing newline; added `|| [ -n "$line" ]` guard after each loop (lines 41 and 54) to flush the final partial read — matching the existing correct form at line 307.;fix(#714): `hooks/lib/forge-write-extract.js` `isForgeScanTarget()` did not recognize `gh api -X METHOD` / `gh api --method METHOD` write commands (POST/PATCH/PUT/DELETE) as forge-scan targets; added `GH_API_WRITE_REGEX`, `extractApiFieldTexts()` (handles `-f`/`-F`/`--field`/`--input @file`), and updated `extractTexts()` dispatch to cover `gh api` write payloads.;fix(#1035): `tests/feature-990-scan-outbound-offensive-integration.sh` stub at `tests/feature-990-scan-outbound-offensive-integration/scan-offensive` was a bash script incompatible with the hook's `spawnSync('node', ...)` invocation; added a regression assertion (T0) at the top of the test to verify the stub carries `#!/usr/bin/env node` shebang so this class of breakage is caught early. <!-- compose-doc-append-sentinel: branch=fix/scan-outbound-fixes pr=#1525 -->

### FEATURE: PR #1529 — feature/outline-friction (2026-07-19, 5b8cf21e6d2496edb28687829546ee95ed6b9e49, #1529)
Background: feat(#1384,#1522,#478,#1124,#1146,#1245): reduce outline planning friction — frontrunner-collapse, abolish MOP-7 dialog, PLAN_LANG directives, VS Code text visibility
Changes: #1384: outline-planner gains frontrunner-collapse rule — when intent leaves implementation choice open but one approach clearly dominates on trade-offs, risk, cost, and existing-code consistency, outline-planner emits SINGLE_APPROACH_JUSTIFIED with frontrunner-collapse reason, suppressing the approach menu without requiring an intent-lock;#1522: MOP-7 AskUserQuestion abolished from make-outline-plan — orchestrator now auto-selects the recommended approach and emits a turn-final prose rationale; confirmation reduced from two interactions (MOP-7 dialog + MOP-8 sentinel) to one (MOP-8 sentinel only) on CONFIRM_OUTLINE=on; "Pass all approaches" bypass option removed;#478: outline stage Accepted Tradeoffs constraint documented in make-outline-plan SKILL.md — outline-planner cannot add new ## Accepted Tradeoffs entries because assemble-mandatory.sh carries intent.md tradeoffs verbatim; planners directed to use ## Confirmed non-goals or ## Constraints for new design decisions;#1124: clarify-intent Remove-and-continue branch now syncs plan artifacts — when an issue is removed from closes_issues, the corresponding - #N: title line is removed from ## Issues in intent.md (and outline.md if present), preventing stale entries in downstream steps;#1146: make-outline-plan and make-detail-plan gain explicit PLAN_LANG draft directives — orchestrator-authored outline.md and detail.md bodies must be written in the configured PLAN_LANG from the first draft, eliminating the reactive re-write cycle triggered by check-plan-lang.js;#1245: user-facing analysis summaries in clarify-intent, make-outline-plan, and make-detail-plan must be delivered as turn-final assistant text or AskUserQuestion preview/description — mid-turn text between tool calls is invisible in the VS Code extension <!-- compose-doc-append-sentinel: branch=feature/outline-friction pr=#1529 -->

### FEATURE: PR #1530 — fix/fix-write-tests-evidence (2026-07-19, e712ab0d041ae102953870ddfc7fef72b71f8628, #1530)
Background: fix(#1521, #1196): write_tests evidence fallback + mark-step evidence gate
Changes: fix(#1196): hasCompletionEvidence("write_tests") now falls back to committed-diff check (git diff origin/default...HEAD -- tests/) when staged area is empty; resolves oracle abort in /worktree-end after PR merge where staging area is cleared but test files are already committed. Used execFileSync argv form (not shell string concatenation) per security review finding F1.;fix(#1521): replaced unconditional WORKFLOW_MARK_STEP_write_tests_complete rejection in mark-step-handler with evidence gate (hasCompletionEvidence); added repoCwd to workflow-mark.js ctx via resolveRepoCwd SSOT; added scoped --reset review_tests recovery hint in next-step inconsistency scan when review_tests=complete but write_tests=pending. <!-- compose-doc-append-sentinel: branch=fix/fix-write-tests-evidence pr=#1530 -->

### FEATURE: PR #1535 — fix/fix-1528-shim-closed-phase (2026-07-19, d389fa959af8e605951bbeb733ec881b85c98361, #1535)
Background: fix(#1528): add TERMINAL_ALERT_PHASES constant; supervisor shim/guard closed+paused bypass
Changes: issue:#1528 category:BUGFIX subject:"fix(#1528): supervisor-off-proposal-shim alert_phase=closed/paused not bypassing OFF-sentinel block" background:"The shim's early-return condition only checked alertPhase === \"done\", leaving \"closed\" (set by session-close SC-6) and \"paused\" (retry-exhaustion freeze) uncovered. When layer1.findings contained warning-or-higher entries after session-close, WORKFLOW_ENFORCE_WORKTREE_OFF was incorrectly blocked. #1426 added the \"done\" case but left \"closed\" and \"paused\" unhandled. Also fixed a pre-existing S9 test assertion stale since PR #1360 changed AUDIT_SEVERITY_THRESHOLD from \"error\" to \"warning\" without updating the test." changes:"Added TERMINAL_ALERT_PHASES = new Set([\"done\",\"paused\",\"closed\"]) to hooks/lib/supervisor-state-schema.js and exported it alongside ALERT_PHASE_VALUES. Replaced alertPhase === \"done\" in supervisor-off-proposal-shim.js with TERMINAL_ALERT_PHASES.has(alertPhase). Replaced 4 inline alertPhase !== \"done\" && alertPhase !== \"paused\" && alertPhase !== \"closed\" chains in supervisor-guard.js with !TERMINAL_ALERT_PHASES.has(alertPhase) (CPR-5 Orthogonality refactor). Fixed S9 assertion in feature-228-supervisor-state-schema.sh: \"error\" → \"warning\"." test_gap:"no test for alert_phase=closed and alert_phase=paused bypass in feature-supervisor-shim-injection.sh" <!-- compose-doc-append-sentinel: branch=fix/fix-1528-shim-closed-phase pr=#1535 -->

### FEATURE: PR #1538 — fix/fix-1526-scan-outbound-sandbox (2026-07-19, ad61f81a6a7d8c8aeb9ac230d49d8b81399c143a, #1538)
Background: fix(#1526): add hooks/workflow-gate/ copy and bin/scan-offensive stub to test sandboxes
Changes: Fix test sandbox missing deps for scan-outbound offensive integration: added `hooks/workflow-gate/` copy and `bin/scan-offensive` Node.js stub to `build_sandbox*` functions in feature-990, feature-blocklist-warn-mode-pretooluse, and feature-forge-write-scan-pretooluse test files. Root cause: MODULE_NOT_FOUND from missing workflow-gate caused fail-open approve before scan logic ran. CPR-5: all three sibling tests fixed in the same change. (closes #1526) <!-- compose-doc-append-sentinel: branch=fix/fix-1526-scan-outbound-sandbox pr=#1538 -->

### FEATURE: PR #1539 — fix/1492-propagate-labels-path-format (2026-07-19, d17515bfce9593510c082d5598f10c255a510889, #1539)
Background: fix(#1492): PROPAGATE_LABELS_REPOS format — semicolon-separated absolute paths
Changes: category=BUGFIX subject="PROPAGATE_LABELS_REPOS format: semicolon-separated absolute paths" background="#1492 was reopened after PR #1507 (DELETE propagation) for a CPR-5 Orthogonality violation: PROPAGATE_LABELS_REPOS used space-separated owner/repo strings while ENFORCE_WORKTREE_ADDITIONAL_REPOS used semicolon-separated absolute paths. Both variables hold repository lists; different formats violated CPR-5." changes="Changed PROPAGATE_LABELS_REPOS to semicolon-separated absolute directory paths. propagate-labels.sh now resolves owner/repo at runtime via git -C <path> remote get-url origin, supporting both HTTPS and SSH GitHub remote URL formats. Validation changed from grep-qE (no anchor) to bash [[ =~ ^...$ ]] (full-string match, F-A3-2 security fix). Updated .env.example comment block. Added 4 new tests for whitespace trimming, no-remote failure, SSH URL parsing, and trailing-semicolon skip." test_gap="No test asserted full-string anchor on SIBLING validation; F-A3-2 was caught by post-implementation security scan rather than tests" <!-- compose-doc-append-sentinel: branch=fix/1492-propagate-labels-path-format pr=#1539 -->

### FEATURE: PR #1549 — fix/1542-sync-labels-ci-propagation (2026-07-19, 6160311c8357fff83d191c68d7131cac83f7bc66, #1549)
Background: fix(#1542): add CI fallback in propagate-labels.sh; pass PROPAGATE_LABELS_REPOS in sync-labels.yml
Changes: fix(#1542): add 2-stage resolver in propagate-labels.sh — stage 1 uses `[ -d ]` guard to run existing `git -C path remote get-url`; stage 2 falls back to `basename + AGENTS_WORKSPACE owner` for absolute paths absent in CI (ubuntu-latest). sync-labels.yml propagate job now receives `PROPAGATE_LABELS_REPOS` via `vars.PROPAGATE_LABELS_REPOS`. Also fixed sync-labels.sh invocation to use `$SCRIPT_DIR` instead of `$AGENTS_WORKSPACE/bin/github-issues/` for self-contained path resolution. <!-- compose-doc-append-sentinel: branch=fix/1542-sync-labels-ci-propagation pr=#1549 -->

### FEATURE: PR #1547 — fix/fix-923 (2026-07-19, 6cc55b4, #1547)
Background: fix(#923): normalize POSIX paths in isMainCheckout to fix worktree-remove block
Changes: category: BUGFIX subject: "fix(#923): isMainCheckout POSIX path normalization — git worktree remove blocked from main worktree" background: "`isMainCheckout(repoCwd)` in `hooks/enforce-worktree/git-repo-detection.js` passed `repoCwd` to `spawnSync` without normalizing POSIX-form paths (e.g., `/path/to/repo` from Git Bash on Windows). spawnSync returned ENOENT → `isMainCheckout` returned `false` → early-exit block's else branch fired → sanctioned `git worktree remove` blocked from main worktree. Third recurrence: PR #1157 (`5c58bb3`) and PR #1515 (`89ca051`) fixed symptoms at call-site / early-exit level without normalizing inside `isMainCheckout` itself, leaving the POSIX-path input class unhandled across all callers." changes: "Added `toWindowsPath` normalization guard at the start of `isMainCheckout` (symmetric with `findRepoRootForBash` lines 201-203; CPR-5 Orthogonality). `toWindowsPath` is a no-op on POSIX/macOS (CPR-8 Universality). All 4 callers of `isMainCheckout` in `enforce-worktree.js` now automatically protected. Added regression test `tests/fix-fix-923-posix-cwd.sh` that injects POSIX-form `toolInput.cwd` via `MSYS_NO_PATHCONV=1`, confirming the bug exists before the fix (T923-POSIX.A/B FAIL) and the security boundary is preserved (T923-POSIX.C PASS)." test_gap: "no test for POSIX-form `toolInput.cwd` in `fix-923-enforce-worktree-early-exit.sh` — the gap that allowed prior fixes to appear green while the Windows-specific regression persisted" <!-- compose-doc-append-sentinel: branch=fix/fix-923 pr=#1547 -->

### FEATURE: PR #1551 — fix/1521-review-tests-backfill (2026-07-19, 8e59bf3825d6fa9cdb845df8857fea74c74d4228, #1551)
Background: fix(#1521): resolveRepoCwd linked-worktree priority + review-tests write_tests backfill
Changes: #1521: Added Priority 0 guard to resolveRepoCwd (hooks/lib/path-normalize.js): when input.cwd differs from CLAUDE_PROJECT_DIR, return input.cwd so hasCompletionEvidence checks the linked worktree's git index instead of the main worktree's. Extended normalizeCwd to normalize C:/path to C:\path on Windows. Added backfillWriteTests to review-tests-handler.js COMPLETE path: auto-marks write_tests complete when staged test evidence exists, preventing ordering mismatch where review_tests completes before write_tests. Documented sentinel usage and linked-worktree CWD requirement in skills/write-tests/SKILL.md. <!-- compose-doc-append-sentinel: branch=fix/1521-review-tests-backfill pr=#1551 -->

### FEATURE: PR #1555 — feature/post-compact-resume-hint (2026-07-19, a16d370, #1555)
Background: feat(#1537): add /resume-session hint to PostCompact notification
Changes: #1537: FEATURE: feat(#1537): add /resume-session hint to PostCompact notification | background: PostCompact hook (hooks/post-compact.js) shows workflow progress in additionalContext after context compaction. When pending steps existed, users had no indication that /resume-session was available to recover the session. | changes: Added pendingCount tracking inside the WORKFLOW_STEPS loop; append "→ Workflow is in progress. Run /resume-session to resume from the current step." to additionalContext when pendingCount > 0. Exclusion: user_verification=pending+reset_reason=post-merge is not counted (expected state after PR merge, mirroring existing annotation at lines 65-67). Added tests T37-T41 in integration-post-compact.sh covering hint-present, all-complete, post-merge-exclusion, fail-open, and user_verification-without-reset_reason cases. <!-- compose-doc-append-sentinel: branch=feature/post-compact-resume-hint pr=#1555 -->

### FEATURE: PR #1556 — feature/issue-1544 (2026-07-20, 1552c05, #1556)
Background: feat(#1544): /issue-create reopen verdict — idempotent 3-point update
Changes: #1544: `/issue-create` reopen verdict now delegates to `bin/github-issues/reopen-with-update.sh`, which executes a 3-point idempotent sequence: (1) refresh a `<!-- BEGIN reopen-banner -->` block in the issue body with reopen count, UTC timestamp, and an 8-char SHA-256 of the session ID; (2) maintain a single `<!-- reopen-log -->` marker comment via REST PATCH (never stacking new comments); (3) apply `status:regressed` label (non-fatal on failure). Prevents a chronic misread where a reopen issue appeared "done" because only its body/first comments were reviewed. Session IDs are hashed before writing to the public issue body (CWE-200 prevention). Added `status:regressed` label to `.github/labels.yml`. Added `rules/github-issues.md` label table entry for `status:regressed`. Tests: DV2/DV2-b/DV2-c added to `tests/feature-issue-create-skill/section-dispatch-core.sh`; mock arms added to `section-mock.sh`. <!-- compose-doc-append-sentinel: branch=feature/issue-1544 pr=#1556 -->

### FEATURE: PR #1550 — feature/943-e2e-per-hook-seam-l3 (2026-07-20, 79adf7acd3dcac7055f4652363c00862c3333218, #1550)
Background: feat(#943): add per-hook seam L3 tests for 6 workflow hooks; split test taxonomy to L1-L4
Changes: FEATURE #943: Add per-hook seam TL3 tests for 6 workflow hooks (workflow-mark, session-start, stop-confirm-plan-guard, stop-final-report-guard, subagent-start, post-compact) under permanent `tests/TL3-hook-<name>` names (17 files). Each test spawns a real `claude -p` mini-session to fire one hook and asserts side-effect files; subagent-start and post-compact land as TL3 gap entries (real event unfireable in short sessions). Fixed predictable temp-dir name (mkdtempSync) in 4 helpers.sh files.;REFACTOR (rules/test.md): split test-layer taxonomy to TL1–TL4, separating substrate-fidelity from pipeline-scope, and prefixed the layer tokens `TL` (Test Layer) so they no longer grep-collide with the local `L1:`/`L2:`/`L3:` case labels used inside individual test scripts. TL3 = single-component/seam real-environment (where #943 lands); TL4 = whole-pipeline end-to-end (the only tier named "E2E", tracked by #1543). Added filename convention: TL3/TL4 tests carry a layer prefix and must not embed an issue number (retire policies key on `feature-<N>` names and would delete permanent coverage). Renamed #943 tests to `TL3-hook-*`; propagated the TL tokens through `skills/_shared/test-design.md`, `rules/test/claude-e2e.md`, and `docs/architecture/claude-code/e2e-testing.md`. <!-- compose-doc-append-sentinel: branch=feature/943-e2e-per-hook-seam-l3 pr=#1550 -->

### FEATURE: PR #1559 — fix/sync-labels-no-delete (2026-07-20, 4cebe820b2db8884cce152e6c06cd53e0b8c9a8c, #1559)
Background: feat(#1552): add --no-delete flag and protected: labels key to sync-labels.sh
Changes: Added `--no-delete` flag to `bin/github-issues/sync-labels.sh`: runs CREATE and UPDATE only, skipping every DELETE (logged `[NO-DELETE] Skipped delete`), mirroring the existing `--dry-run` guard in the DELETE dispatch case. Added a `protected:` top-level key to `.github/labels.yml` (parsed by a parameter-expansion-only bash loop into a CSV, passed to the awk diff engine via `-v protected_csv`) that permanently excludes the nine GitHub default labels (bug, enhancement, documentation, good first issue, invalid, duplicate, question, wontfix, help wanted) from DELETE even in a normal sync — sibling repos may still carry those labels on existing issues. `propagate-labels.sh` threads `--no-delete` to each sibling when `PROPAGATE_LABELS_NO_DELETE` is set (non-empty). `--no-delete` (temporary, full DELETE suppression at the bash dispatch layer) and `protected:` (permanent, per-label exclusion at the awk layer) are orthogonal mechanisms. Auth stays in the `gh` auth store (`gh auth token` fallback) — no token added to `.env.example`. <!-- compose-doc-append-sentinel: branch=fix/sync-labels-no-delete pr=#1559 -->

### FEATURE: PR #1562 — fix/1557-audit-tests-retire (2026-07-20, b01aa976c05aa620ee4c442c8a35feaf580878a0, #1562)
Background: fix(#1557): audit-tests staleness via closed_at; add common orphan detector and scope tag enforcement
Changes: #1557: `bin/audit-tests.sh` staleness detection was broken since PR #689 (2026-06-01) reset all feature-NNN test committer dates via a frontmatter backfill, causing the tool to always return zero candidates. Switched the staleness criterion from last-commit date to the GitHub Issues API `closed_at` field. Also added `bin/audit-tests-common.sh` (orphan detection for scope:common tests whose `# Tests:` paths all no longer exist), `bin/check-test-scope-tag.sh` (scope tag enforcement with `--staged` pre-commit and `--all` manual sweep modes), two nightly CI steps in `sweep.yml`, a new `/sweep-tests` sub-skill, and wired it into the `/sweep` hub as SW-4. <!-- compose-doc-append-sentinel: branch=fix/1557-audit-tests-retire pr=#1562 -->

### FEATURE: PR #1570 — feature/install-gh-jq-self-sufficient (2026-07-20, 9a0592ecbe63e2944fc5d8dcccb9e0feedfe1a54, #1570)
Background: feat(#1567,#295,#1566): self-sufficient gh + jq installer; add Prerequisites docs
Changes: #1567: agents/install: add self-sufficient gh CLI setup — new install/win/gh.ps1 and install/linux/gh.sh handle winget/brew/apt install, interactive-guard auth login, and gh auth refresh -s project; replaces the scope-warn advisory in install.ps1/install.sh.;#295: document gh CLI installation and Projects v2 scope setup — README.md gains ## Prerequisites table; rules/github-issues.md ## Environment bullet extended with installer reference.;#1566: install: jq installer added — new install/win/jq.ps1 (winget) and install/linux/jq.sh (brew/apt) with idempotency check; install.ps1 and install.sh call them at the end of the install sequence, fixing WE-12 and WE-21 failures on fresh machines. <!-- compose-doc-append-sentinel: branch=feature/install-gh-jq-self-sufficient pr=#1570 -->

### FEATURE: PR #1577 — fix/1568-enforce-worktree-false-positives (2026-07-20, 82816a9, #1577)
Background: fix(#1568 #1533 #1457 #1449 #1385 #1191): fix 6 false positives in enforce-worktree hook
Changes: #1568: `isNewlineInjectedWriteIR` false-positive on `gh issue create --body "$(multiline)"` from main — DQ body with literal newlines was split as separate commands; fixed by applying `stripDqPreservingCmdSubst` before the newline split in `bash-write-targets.js`, folding DQ literal content to `""` while preserving `$()` regions.;#1533: `ISSUE_CREATE_SKILL=1 bash dispatch.sh --body "$(multiline)"` blocked from main — argTail guard's `\n` scan fired on literal newlines inside DQ body arg; fixed by applying `foldDqNewlines` helper in `worker-script.js` to collapse DQ-internal newlines to spaces before the argTail regex scan, while leaving `$()` unchanged (fail-closed preserved).;#1457: `gh issue create --body $'it'\''s fine'` blocked — `hasUnclosedQuote` in `command-ir.js` didn't recognize ANSI-C `$'...'` spans or backslash-escape in unquoted context (`\'`), causing `parseFailure:true` and write classification; fixed by adding an ANSI-C branch and a `\x`-skip in the unquoted else branch.;#1449: `bash skills/review-code-security/scripts/run-quality-gates.sh` blocked from main — the SANCTIONED allow-list in `worker-script.js` did not include this path; fixed by adding it alongside the other existing sanctioned scripts.;#1385: `bash -c 'node bin/workflow/read-complexity-evaluation --session sid'` blocked from main — no predicate allowed read-only `bash -c '...'` workflow CLI calls; fixed by adding `isAllowedReadOnlyWorkflowCli` in `standard.js` (delegates to `isReadOnlyInterpreterC`) and wiring it into the allow-chain in `enforce-worktree.js`.;#1191: `ISSUE_CREATE_SKILL=1 bash "dispatch.sh"` blocked from main when prefixed with a VAR=val env assignment — the identity regex in `worker-script.js` required the command to start with `bash`; extended the regex to accept one or more `VAR=val` prefixes before `bash`. <!-- compose-doc-append-sentinel: branch=fix/1568-enforce-worktree-false-positives pr=#1577 -->

### FEATURE: PR #1578 — fix/propagate-labels-fixes (2026-07-20, 6ef66755002cfa1b23659a23e3095605142e37a2, #1578)
Background: fix(#1560,#1546,#1262,#1545,#1548,#1565): propagate-labels PAT fallback, depth-1 scan, hooksPath clear, asset copy, docs, fixture fixes
Changes: #1560: fixed T-propagate-delete-inherit fixture — bare-slug PROPAGATE_LABELS_REPOS never entered the -d branch; replaced with a directory-path entry driven by a git mock that resolves to myorg/myrepo;#1546: propagate-labels.sh now sets core.hooksPath="" on each cloned sibling before committing, preventing a blocking global pre-commit hook (enforce-worktree) from aborting local propagation runs;#1262: corrected docs/ops/migration-from-todo.md Step 1 to say "all labels defined in .github/labels.yml" instead of enumerating a stale label list (priority:* was never in labels.yml);#1545: PROPAGATE_LABELS_PAT is now optional — if unset, the script falls back to "gh auth token"; exits 0 gracefully if both are absent; .env.example updated to reflect the relaxed requirement;#1548: PROPAGATE_LABELS_REPOS now accepts a parent directory: if it is not itself a git repo, the script scans one level of subdirectories and propagates to every git repo found; empty parent skips gracefully (exit 0);#1565: propagate-labels.sh now also copies bin/github-issues/sync-labels.sh, .github/ISSUE_TEMPLATE/task.yml, .github/ISSUE_TEMPLATE/incident.yml, and .github/workflows/sync-labels.yml into each sibling before committing <!-- compose-doc-append-sentinel: branch=fix/propagate-labels-fixes pr=#1578 -->

### FEATURE: PR #1580 — fix/fix-gh-refresh-idempotency (2026-07-20, 6c9e38e888c6812545ccdc5b1de50067b7da31cf, #1580)
Background: fix(#1573): gh auth refresh -s project — add idempotency scope check (#1580)
Changes: fix(#1573): gh auth refresh -s project was unconditionally called on every installer re-run, triggering interactive device-auth prompts even when project scope was already granted. Added gh auth status idempotency check in both gh.ps1 and gh.sh — skips the refresh when "project" already appears in auth status output. <!-- compose-doc-append-sentinel: branch=fix/fix-gh-refresh-idempotency pr=#1580 -->

### FEATURE: PR #1585 — refactor/1581-run-tl3 (2026-07-20, 81d6dfd62d1139342f3a24af449ff8fdc098faa3, #1585)
Background: refactor(#1581): rename RUN_E2E to RUN_TL3; fix TL3 test selection
Changes: REFACTOR #1581: Rename RUN_E2E → RUN_TL3 across all 26+ call-sites (.env.example, 6 TL3-hook-*.sh skip-gates, rules/test.md, rules/test/claude-e2e.md, skills/_shared/user-verified.md, docs/ops.md, docs/architecture/claude-code/e2e-testing.md, bin/review-e2e-coverage, and comment-only test file references). "E2E" is now reserved exclusively for TL4 (full pipeline); TL3 = single-seam real-environment per CPR-7. Also fixed two pre-existing test bugs in tests/feature-941-run-e2e-toggle.sh (Case 5: RUN_TL3=0 was never a recognized off value; Case 6a: grep targeted the dispatcher instead of the sourced settings-e2e.sh). FEATURE: bin/select-tests.sh now auto-appends tests/TL3-*.sh when RUN_TL3=on so TL3 tests are actually selected; the prior early-exit on empty stems was removed and a seen-map dedup prevents duplicates when a stem-match already covers a TL3 file. <!-- compose-doc-append-sentinel: branch=refactor/1581-run-tl3 pr=#1585 -->

### FEATURE: PR #1587 — fix/1576-test-frontmatter (2026-07-20, a2bd55b, #1587)
Background: fix(#1576): audit-tests parser hardening + sweep-tests --fix-headers/--apply
Changes: #1576: Hardened test frontmatter validation. Renamed `bin/check-test-scope-tag.sh` to `bin/check-test-frontmatter.sh` and extended it to validate the `# Tests:` header (presence, non-empty value, per-token `^[A-Za-z0-9._/-]+$` regex) in addition to the existing `# Tags:` scope check; `--staged` mode now reads the staged blob via `git show`. Added `bin/lib/test-frontmatter-constants.sh` (SSOT regex) and `bin/lib/test-frontmatter-fix.sh` (shared A/B/C token classifier, `normalize_token`, and atomic exec-bit-preserving header rewrite). Extended `bin/audit-tests.sh` and `bin/audit-tests-common.sh` with `--fix-headers` (report and optionally rewrite malformed `# Tests:` headers) and `--apply` on audit-tests.sh (git-rm stale issue-specific test files when all tokens are format-OK C-class and the issue is closed past the staleness cutoff); both scripts wrap the `gh api` call in `run-with-timeout.sh` via `GH_TIMEOUT`. Updated `hooks/pre-commit` to call `check-test-frontmatter.sh`, `skills/sweep-tests/SKILL.md`, `skills/sweep/SKILL.md` SW-4, and `skills/_shared/test-design.md`. <!-- compose-doc-append-sentinel: branch=fix/1576-test-frontmatter pr=#1587 -->
Changes: REFACTOR #1581: Rename RUN_E2E → RUN_TL3 across all 26+ call-sites (.env.example, 6 TL3-hook-*.sh skip-gates, rules/test.md, rules/test/claude-e2e.md, skills/_shared/user-verified.md, docs/ops.md, docs/architecture/claude-code/e2e-testing.md, bin/review-e2e-coverage, and comment-only test file references). "E2E" is now reserved exclusively for TL4 (full pipeline); TL3 = single-seam real-environment per CPR-7. Also fixed two pre-existing test bugs in tests/feature-941-run-e2e-toggle.sh (Case 5: RUN_TL3=0 was never a recognized off value; Case 6a: grep targeted the dispatcher instead of the sourced settings-e2e.sh). FEATURE: bin/select-tests.sh now auto-appends tests/TL3-*.sh when RUN_TL3=on so TL3 tests are actually selected; the prior early-exit on empty stems was removed and a seen-map dedup prevents duplicates when a stem-match already covers a TL3 file. <!-- compose-doc-append-sentinel: branch=refactor/1581-run-tl3 pr=#1585 -->
### BUGFIX: fix(#1579): issue-create label drift — reporter-model script mapping + keyword scan (2026-07-20, 0eb3b18)
Background: PR #1496 renamed model:* labels to reporter-model:* in labels.yml but left SKILL.md pointing at the old names, so the LLM kept adding labels that no longer existed on GitHub.
Changes: Rewrote SKILL.md label policy to the reporter-model:* scheme, narrowing the LLM role to extracting the raw model name and passing it via --reporter-model to the script (which now owns the name→label mapping via a case statement). Added an inline keyword scan in issue-create.sh (abort/hang/security/leak → force severity:high, word-boundary -w, no -i). Clarified the type:task and severity:high definitions in SKILL.md.
Test gap: No test covering the model→label mapping path in issue-create.sh before the fix

### FEATURE: PR #1612 — fix/issue-1600-sanctioned-allowlist (2026-07-24, 8b197f02b16dbf63a506cbfdd7006bd3911cfc48, #1612)
Background: fix(#1600,#1590,#1501,#1307): finalize-worker overlay for SANCTIONED allowlist
Changes: fix(#1600): issue-close-finalize Phase 2 was unrunnable because `worker-script.js`'s SANCTIONED allowlist only listed `pre-flight.sh` — the other three finalize-worker scripts (`run-initial.sh`, `run-loop-step.js`, `run-finalize-terminal.sh`) had no path to bypass the main-worktree write guard. Added `hooks/enforce-worktree/main-worktree-allows/finalize-worker-overlay.js`, a HARD-validating overlay (identity + env VALUES + per-position argument shape, including per-token unquoted-metacharacter rejection) that gates all three scripts before deferring to the shared write-scope tail. A confirmed HIGH-severity gap found by `/review-code-security` (unquoted `id`-position args allowed bare `|`/`<`/`>`, enabling command injection via pipe and arbitrary file write via redirect invisible to the write-target scanner) was fixed in the same session by validating quoted-vs-unquoted token provenance and rejecting shell metacharacters on unquoted tokens; the legacy SANCTIONED structural scan in `worker-script.js` received the symmetric bare-`|` rejection for CPR-5 lockstep.;fix(#1590): `run-finalize-terminal.sh` is now covered by the same `finalize-worker-overlay.js` HARD-validation registry as the other finalize scripts, closing the SANCTIONED-allowlist gap for the terminal step of the finalize chain.;fix(#1501): ICF-F's meta-label fast path previously parsed `- [ ]` checkboxes in the issue body to infer sub-issue completion, which misread prose mentioning checkbox-like text as incomplete state. Reworked to use `parent-all-closed-check.sh`'s RC=0 (all sub-issues closed) as the completion signal instead of body-text parsing.;test(#1307): `feature-1257-session-close-wf-meta.sh`'s S3 assertion grep-anchored to `/issue-close-finalize`, which could false-match SKILL.md prose referencing the skill by name rather than the actual invocation. Tightened the assertion to anchor on the actual invocation shape. <!-- compose-doc-append-sentinel: branch=fix/issue-1600-sanctioned-allowlist pr=#1612 -->

### FEATURE: PR #1618 — feature/supervisor-off-gate (2026-07-25, ef5bba2232eb081823fbef9d823b37905debdcde, #1618)
Background: feat(off-gate): reason-vetting OFF gate, emergency sentinels, next-step pause
Changes: #1608: Reworked the OFF-sentinel gate from severity-accumulation to reason-vetting (token-first). hooks/supervisor-off-proposal-shim.js decides block/pass-through by strict-DQ emergency detection → isWorkflowOff/isWorktreeOff (target-aware) → a reason-bound single-use clearance token (evaluateOffClearance in hooks/lib/session-markers.js: unexpired + target match + reason-contains-category); corrupt/missing token fails CLOSED. New bin/request-off-clearance (Phase-1 examiner + token mint), hooks/block-off-clearance-write.js (anti-cheat on the token path), enforce-override-handlers/off-clearance.js (single-use consume with wsid fallback), four *_EMERGENCY_* sentinels in hooks/lib/sentinel-patterns.js. Emergency OFF bypasses the Phase-1 examiner and is human-only + audited. Why: fixes the deadlock where accumulated errors/warnings blocked the very OFF switch needed to escape them (egg-or-chicken); the gate now measures reason legitimacy, not severity. Trust model: not a cryptographic hard gate — the real gate is Phase-2 human approval of the ask sentinel plus the audit trail; unforgeability is an explicit non-goal.;#1607: next-step pause/resume. NEXT_STEP_PAUSE/RESUME sentinels + hooks/workflow-mark/enforce-override-handlers/next-step-pause.js; next-step emits ACTION=paused (REASON=next-step-paused / workflow-off-quiet) so it stops re-announcing while a deliberate OFF/pause is in effect; CLAUDE.md documents the paused action. Why: distinguishes intentional workflow departure from a procedural mistake so the departure can be used quietly.;#1606: honest OFF block message. The shim's block message is sourced from actual supervisor state instead of showing "Active supervisor findings exist" on a fresh session whose state file is absent (ENOENT). Why: ENOENT (audit-not-yet-run) and real blocking findings were shown identically, using severity/audit-presence as a proxy — the message now tells the truth about whether findings exist.;#924: wsid-scoped warnings_summary. hooks/workflow-gate/review-tests-checker.js warnings_summary check is now tagged with wsid so a stale value from a prior wsid no longer falsely blocks a commit in long-running CC sessions. Why: token/fingerprint paths were wsid-scoped in PR #963 (commit cc8035bb) but warnings_summary was missed — same class of bleed, one remaining member.;#1132: supervisor checklist item 6 no longer misclassifies --wip mode. agents/supervisor.md + skills/_shared/off-legitimacy-rubric.md recognize `git -c workflow.wip=1 commit` as sanctioned rather than a C3 improvised bypass. Why: item 6 is the OFF-proposal validity classifier (same subsystem as the Phase-1 rubric) — a sibling bug fixed alongside the rubric work.;#1361: review-loop terminal guard. skills/review-tests/scripts/run-codex-review-loop.sh + review-tests-checker/handler terminal-marker handling (state-io.js clearReviewTestsTerminalMarker) structurally prevent a terminal exit (e.g. exit 2) from deleting the round-counter and re-enabling `--cap 1` on re-invocation. Why: a terminal exit followed by re-invocation silently reset the loop cap. <!-- compose-doc-append-sentinel: branch=feature/supervisor-off-gate pr=#1618 -->

### FEATURE: PR #1628 — feature/ds4-prompt-hardening (2026-07-25, e241c33b70554bd08f2eb567b23870c2e7e4f83a, #1628)
Background: feat(#1611): model-conditional prompt hardening for low-gap-filling backends
Changes: FEATURE: model-conditional prompt hardening for low-gap-filling backends. Background: the agents prompt-set is tuned for Opus/Sonnet, which fill rule gaps implicitly; a backend such as ds4 fills fewer gaps and takes goal-achieving shortcuts that skip "should" rules. Rather than a rules/*.md that would burden Opus/Sonnet context unconditionally, the running model is detected from the "You are powered by the model named X" self-report matched against a VERBOSE_PROMPT_MODELS allowlist; on a match a hooks/lib provider injects the hardening text as additionalContext at SessionStart and PostCompact (tolerant of repeated injection), so flag-off sessions pay zero context. Also in this issue: model detection consolidated into one matcher (hooks/lib/model-match.js, reused by issue-create reporter-model labelling); append-only doc protection extended to rotated archive files (docs/history/*.md, changelog/*.md) with the block-history-direct guard now registered for the Bash tool as well as Edit/Write; and the final-report Stop-guard trigger decoupled from the .env file so it fires on the workflow's pre-final-report gate directly. <!-- compose-doc-append-sentinel: branch=feature/ds4-prompt-hardening pr=#1628 -->

### FEATURE: PR #1635 — fix/remove-session-model-id (2026-07-25, 3539488, #1635)
Background: fix: remove unauthorized SESSION_MODEL_ID env override
Changes: REFACTOR: Removed the SESSION_MODEL_ID env-var override and the model self-report bootstrap (bin/record-session-model.js, verbose-prompt.js self-report request, hooks/session-start.js self-report block) that had been added to the #1611 model-conditional prompt hardening feature without user request — the feature is narrowed back to the originally approved hook-payload auto-detection plus VERBOSE_PROMPT_MODELS allowlist. Also fixed two stale comments left over from the removed layers and a vacuous test assertion (J01) found during the security review. Note: this corrects the design description recorded in the existing docs/history.md #1628 entry, which still describes the self-report mechanism as shipped. <!-- compose-doc-append-sentinel: branch=fix/remove-session-model-id pr=#1635 -->

### FEATURE: PR #1634 — feature/worktree-transition-1610 (2026-07-25, ac174f90e04b626fe6c05bf68e1d4d95487c015b, #1634)
Background: fix(#1610): worktree transition guard — ExitWorktree reminder + outside-worktree write gate
Changes: #1610 P0 probe SETTLED (observed 2026-07-24, Claude Code 2.1.136 reported by `claude --version`). Probe fully removed: live settings byte-restored from backup, backup deleted, both temporary scripts deleted.;Verdict A- : a `permissions.allow` entry for `EnterWorktree` does NOT suppress the upstream consent dialog. Measured with both entries live in `~/.claude/settings.json`; the dialog still appeared on every EnterWorktree call. The two tracked-`settings.json` allow entries were therefore reverted to zero diff.;`ExitWorktree` has no consent gate at all — PreToolUse to PostToolUse latency was 0.22s with no dialog, versus 15.0s (human response) for EnterWorktree. Its allow entry is a no-op either way.;Verdict P+ : both `PreToolUse` and `PostToolUse` fire for `EnterWorktree` and `ExitWorktree`. PreToolUse fires even when the user denies the call. The `cwd` field carries the post-transition directory in PostToolUse.;Unplanned finding 1 (WITHDRAWN — was wrong): the claim that a session is bound at launch without `EnterWorktree` was refuted by the transcript. An explicit call exists at 2026-07-24T13:07:50Z with `cwd=C:\git\agents`, made right after `/worktree-start`. Transcript entry evidence is therefore reliable and the Stop-hook predicate in the detail plan stands unchanged.;Unplanned finding 2: `EnterWorktree` refuses a path equal to the current working directory ("Cannot enter worktree: ... is the current working directory"). Re-entering the session's own worktree is impossible while already inside it.;Unplanned finding 3: `EnterWorktree` MOVES the session transcript from `~/.claude/projects/<launch-dir>/<sid>.jsonl` to `~/.claude/projects/<worktree-dir>/<sid>.jsonl`. It is a move, not a copy — nothing remains at the original path. This is the mechanism behind the reported usability problem where a session vanishes from the thread list of the directory it was started in.;Consequence: a session started with the worktree as its workspace directory never calls `EnterWorktree`, so it never sees the dialog, never relocates its transcript, and holds no binding to release.;Mechanism (measured 2026-07-25): `enforce-worktree` judges Edit/Write by the write-target path, not by CWD — writing to a worktree absolute path from a main CWD is already allowed. The only real effect of `EnterWorktree` is relocating the session CWD, the single anchor every Bash call resets to (`cd` inside a call never persists). So `EnterWorktree` is not isolation; it only moves the base tree for relative-path execution.;Entry-route decision SETTLED (2026-07-25): keep `EnterWorktree` as the primary entry route. The 2-window / session-disappearance cost is mitigated by a workaround — from the main-worktree window's terminal, `claude --resume [<session-id>]` lists worktree-side sessions (CLI defaults includeWorktrees=true; only the VS Code extension hardcodes false, extension-side fix is the real upstream fix). The approved detail plan stands unchanged: Step 8 (exit-forgetting Stop warning + PostToolUse recorder) and the Tier 3 entry gate are implemented as specified.;#1610 implementation: added an advisory Stop hook (`hooks/stop-exit-worktree-warn.js`) that warns when `EnterWorktree` was used but `ExitWorktree` was never called, using union evidence from workflow state and the session transcript; it fails open and never emits a Stop-time decision key (a Stop-time block would deadlock the session). Added a PostToolUse recorder (`hooks/postuse-native-worktree-record.js`) that stamps `worktree_entered_at` / `worktree_exited_at` on EnterWorktree/ExitWorktree. Added a Tier 3 `hooks/workflow-gate.js` entry predicate (`hooks/workflow-gate/worktree-entry-gate.js`) that blocks EARLY_GATE_TOOLS writes once `branching_complete` is recorded and the session's cwd is outside its linked worktree — the destination path is deliberately excluded from the decision because writing to a worktree-internal absolute path establishes no session binding; subagent/headless calls downgrade to advisory and every predicate/error path fails open to dormant. Added a shared remedy builder (`hooks/enforce-worktree/worktree-remedy.js`) surfaced in enforce-worktree block reasons. `worktree-start` WS-8 now calls EnterWorktree and `worktree-end` WE-13a calls ExitWorktree per the shared protocol `skills/_shared/worktree-transition.md`. <!-- compose-doc-append-sentinel: branch=feature/worktree-transition-1610 pr=#1634 -->

### FEATURE: PR #1650 — fix/rules-paths-frontmatter (2026-07-25, da0b6d98d7be1339f107d5423efb76bc58b66986, #1650)
Background: fix(#1641): use paths: frontmatter key for conditional rules
Changes: BUGFIX | Fix conditional rules loading: rename frontmatter key `globs:` to `paths:` (#1641) | Background: Claude Code's rules parser reads the `paths:` frontmatter key, but this repo wrote `globs:`, a deliberate 2026-05-12 reverse-migration that worked around upstream Issue #21858 (user-scope `paths:` silently ignored). That upstream bug is fixed, leaving the repo stranded on the obsolete workaround: files without `paths:` fall back to unconditional loading, so 15 rules intended to be conditional were injected in full into every session. | Changes: renamed the key to `paths:` and converted values from comma-separated single-line strings to YAML lists across 15 `rules/*.md` files in one diff (key-only replacement would have been parsed as zero patterns and stayed unconditional); reworded the `## Sub-rules` headings in `rules/coding.md` and `rules/docs.md` that claimed "path-scoped via globs"; deleted the one-line relocation stub `rules/file-investigation.md` (the real file is `skills/_shared/file-investigation.md`); rewrote `tests/refactor-rules-progressive-disclosure.sh` as a fail-closed dispatcher over five group files under `tests/refactor-rules-progressive-disclosure/`, pinning the exact ordered pattern strings per file, and updated checks k. and l. in `tests/feature-write-code-skill-static.sh` from `globs:` to `paths:`. | Test gap: no test asserted that rules frontmatter used the `paths:` key that Claude Code's loader actually reads — the suite only checked that a `globs:` key was present. <!-- compose-doc-append-sentinel: branch=fix/rules-paths-frontmatter pr=#1650 -->

### FEATURE: PR #1656 — feature/prune-stub-sessions (2026-07-26, 1f4f325844cb655973dad5f8c713dc1066ecc5ea, #1656)
Background: feat(#1655): prune stray title-only stub session .jsonl files
Changes: #1655: vscode-cc-repair can now detect and delete duplicate title-only stub session .jsonl files left behind when the VS Code Claude Code extension races session creation across worktrees — re-verifies stub identity and counterpart coverage against disk immediately before unlinking. Also refactored bin/vscode-cc-repair from 507 lines down to 43 (dispatch-only) with logic moved into bin/lib/vscode-cc-repair/ (patch.js, prune.js, primitives.js). <!-- compose-doc-append-sentinel: branch=feature/prune-stub-sessions pr=#1656 -->

### FEATURE: PR #1664 — refactor/1569-quote-spans (2026-07-28, 59d60563e62fbe34920ea7be4e1bf2a14eae86e4, #1664)
Background: refactor(#1569, #1630): shared quote-span scanner + env-independent AGENTS_CONFIG_DIR anchor
Changes: #1569: Introduced `hooks/lib/quote-spans.js` (`quote-spans/{scan,query,transform}.js`) as the single quote- and expansion-span scanner for enforce-worktree write detection, replacing the per-consumer ad-hoc quote walkers in `hooks/lib/{strip-quoted-args,bash-write-targets,command-ir}.js`, `main-worktree-allows/worker-script.js`, and the newly extracted `hooks/enforce-worktree/arg-tail-guard.js`. Background: each quoting-form defect had been point-fixed in one predicate at a time, leaving the symmetric siblings broken (CPR-8). Fail-closed contract: unparseable nesting or nesting past `MAX_SPAN_DEPTH` returns `ok:false` with a fail reason and every consumer maps that to the write/block side; the transform boundaries return their input unchanged instead of throwing, so a pathological command cannot crash the hook into emitting no verdict (a crash is fail-open). Security review found and fixed 8 defects before merge, the most serious being `case`…`esac` pattern terminators consumed as command-substitution closers (fail-open in both the scanner and `extractCommandSubstitutions`), literal `$(`/backtick accepted in unquoted arg tails, an unbounded recursion depth, and an O(n²) per-character rescan in the arg-tail guard (replaced by a single memoized offset-map sweep).;#1630: Added `hooks/lib/agents-config-dir.js`, an env-independent trust anchor for the agents config dir. Background: predicates that identify a sanctioned script by `<config-dir>/<rel-path>` read `process.env.AGENTS_CONFIG_DIR` directly — absent in subagent- and Bash-tool-spawned hook processes (the finalize-worker overlay therefore false-BLOCKed sanctioned eval) and attacker-supplied in the hostile case. Resolution now falls through env → module anchor (`__dirname`) → realpath, accepting a candidate only when it carries both the `hooks/enforce-worktree.js` and `bin/` markers; a single-marker match is ambiguous and rejected. `hooks/lib/load-env.js` keeps env-only resolution deliberately (CPR-3) — an explicit `AGENTS_CONFIG_DIR` must remain the sole settings source, or an alternate config dir would silently be injected with the real repo's `.env`; only the candidate enumeration is shared. Also hardened the overlay's argument validation (every token passes `isSimpleArgValue` before spec dispatch; unknown specs fail closed) and made absolute-path detection platform-conditional (win32 accepts `/` and `\`, POSIX only `/`). <!-- compose-doc-append-sentinel: branch=refactor/1569-quote-spans pr=#1664 -->

### FEATURE: PR #1649 — feature/1627-vscode-patch-reapply (2026-07-28, 3f2d4c9, #1649)
Background: feat(#1627): add bin/vscode-patch-include-worktrees
Changes: Added `bin/vscode-patch-include-worktrees`, which re-applies the `includeWorktrees:!1` -> `!0` flip in the installed `anthropic.claude-code` VS Code extension bundle. The extension hardcodes the falsy literal in its session-list call, so sessions whose cwd is a linked worktree never appear in the list, and every extension auto-upgrade reverts a hand-edit. The tool classifies every `includeWorktrees:` site rather than blind-replacing: an unrecognized value or more than one boolean-literal site refuses before any write, the baseline and the result are both validated with `node --check`, the edit is byte-length-preserving, and all I/O round-trips through latin1 buffers so invalid UTF-8 bytes elsewhere in the minified bundle survive untouched. Writes go to an exclusive-create temp file and are renamed into place; `extension.js.bak` is created only when absent. Discovery covers the four home-relative extension roots (stable/insiders, local/server), dedups by realpath, and accepts repeated `--extensions-dir` overrides.;Removed the dead `rules/file-investigation.md` stub. The file had been reduced to a single `Moved to skills/_shared/file-investigation.md.` pointer by the #1188 rules cleanup, but nothing referenced the old path any more — the sole live consumer, `skills/survey-history/scripts/history-docs-search.sh`, already points at the new location. As a `rules/*.md` file the stub was still being injected into every session's context, so the only thing loaded by default was a pointer, while the content it pointed at was not loaded at all. <!-- compose-doc-append-sentinel: branch=feature/1627-vscode-patch-reapply pr=#1649 -->

### FEATURE: PR #1646 — feature/gh-outbound-guard (2026-07-28, 050b6d7, #1646)
Background: fix(#1591): fail-closed outbound scan guard for gh issue/PR writes
Changes: gh_outbound_guard fail-closed scan guard wired into intent-derived issue creation (clarify-commit-scope.sh Path C), issue-create.sh, reopen-with-update.sh, and the GitHub Contents/Git-Data write helpers — outbound issue/PR title+body content is scanned for private info before submission and blocked when the scanner can't be resolved or a match is found (#1591) <!-- compose-doc-append-sentinel: branch=feature/gh-outbound-guard pr=#1646 -->

### FEATURE: PR #1677 — refactor/1655-vscode-cc-repair-relocate (2026-07-28, f61181f, #1677)
Background: refactor(#1655): relocate vscode-cc-repair out of bin/lib/ and document apply-by-default
Changes: Moved the 10 `vscode-cc-repair` modules out of `bin/lib/` into `bin/vscode-cc-repair/`, and turned the same-named entrypoint file into `bin/vscode-cc-repair/index.js` so the tool follows the existing `bin/<tool-name>/` layout used by `bin/github-issues/`, `bin/refactor-prompts/`, and `bin/workflow/`. `bin/lib/` now holds only libraries genuinely shared by two or more entrypoints. Mechanical move via `git mv` with no logic change: internal requires were already relative, the entrypoint's three `./lib/vscode-cc-repair/*` requires collapsed to `./cli` / `./patch` / `./prune`, and index mode 100755 was re-recorded. Both suites stay green (565 + 616 cases); the structural packaging guard now exempts `index.js` from the no-shebang / 100644 library assertions, which G01/G02 already assert in the opposite direction for the entrypoint, and its destructive-call-site source grep now covers the entrypoint as well. README.md also gained an explicit statement that omitting `--dry-run` applies for real — `--prune-stub-sessions` renames matched stubs to `.bak` with no confirmation prompt — because the previous wording only mentioned that `--dry-run` exists. The apply-by-default behaviour itself was left unchanged: a survey of primary sources (git clean, terraform, kubectl, rsync, ansible, helm, docker system prune, certbot, cargo publish, aws-cli) found no major CLI that defaults to dry-run, and a default no-op silently exits 0 when called from a script. <!-- compose-doc-append-sentinel: branch=refactor/1655-vscode-cc-repair-relocate pr=#1677 -->

### FEATURE: PR #1676 — fix/1133-1148-approval-gate (2026-07-28, 20b0a94118f7c0761553dd4d907a5f123e04485a, #1676)
Background: fix(#1133,#1148): env-file-only gate for CONFIRM_OUTLINE/CONFIRM_DETAIL approval bypass
Changes: TL3 (real end-to-end) verification of the outline/detail approval gate was not performed before merge — only TL1/TL2 coverage exists (15 sub-tests in tests/fix-1133-1148-approval-gate/). Accepted as risk per user decision on 2026-07-28; full clarify-intent -> make-outline-plan -> make-detail-plan E2E run through a real session was not executed.;run_tests marked complete with 12/26 selected tests failing. All 12 failures reproduced identically on a clean pre-branch baseline worktree (detached at HEAD 8b197f02, before any #1133/#1148 changes) — confirmed not a regression from this branch. Root causes already tracked: #1619, #1620, #1629, #1632, #1633. <!-- compose-doc-append-sentinel: branch=fix/1133-1148-approval-gate pr=#1676 -->

### FEATURE: PR #1675 — feature/issue-1672-backdate (2026-07-28, bc602fbca7961a4a7465cf38a03c35d5c261b2f8, #1675)
Background: feat(#1672): add --allow-backdate to doc-append and batch backfill script
Changes: Security review of the backfill path found two defects in the new code, both fixed before merge. issue-to-history.sh cd's into AGENTS_CONFIG_DIR, so its duplicate-entry grep resolved docs/history.md there while the append honored --target; since the batch path always passes --target, a re-run appended a duplicate into an append-only record, and --allow-backdate had removed the ascending-date guard that was catching this by accident. The check now unions the canonical pair with the target and its sibling archive. Separately, backfill-batch.sh resolved sort-history.py / doc-rotate.py from the caller-supplied --repo-dir while the append step pinned AGENTS_CONFIG_DIR; both now run from the config dir with the absolute target path.;doc-append rejected every /issue-reconcile backfill entry: its ascending-date guard blocks any entry dated more than 7 days before the stream tail, and a backfill entry is older than the tail by definition. Added `--allow-backdate` to lift that guard alone, forwarded it plus `--no-auto-rotate` through issue-to-history.sh, and added skills/issue-reconcile/scripts/backfill-batch.sh so a batch appends once and then sorts and rotates exactly once rather than rotating per entry. doc-append re-sorts pre-existing entries but writes each new one at the tail, so the trailing sort-history.py run is what actually places backdated entries. <!-- compose-doc-append-sentinel: branch=feature/issue-1672-backdate pr=#1675 -->

### FEATURE: PR #1683 — feature/1640-measurement-infra (2026-07-29, 3798cc1, #1683)
Background: feat(#1640): lightweight measurement infrastructure
Changes: #1640: added lightweight measurement infrastructure covering exactly three metrics, with no LLM calls on any path. `bin/measure-norm-docs` reports normative-doc size against the `rules/coding/file-split.md` thresholds, `bin/count-subagents` counts subagent invocations from the session JSONL, and a new `hooks/workflow-state/step-timestamps.js` module records a per-step `started_at` in workflow state, wired into `state-io.js` `markStep` and `hooks/workflow-mark/reset-handler.js`. Until now these three quantities could only be estimated by hand, so prompt growth, subagent fan-out, and step duration were invisible when deciding whether a split or a refactor was warranted. The timestamp recorder is gated behind `RECORD_STEP_TIMESTAMPS` (default `off`) so existing sessions pay nothing for it, and both CLIs are read-only. During review the repo owner asked for `hooks/lib/workflow-state{.js,/}` to be relocated to `hooks/workflow-state{.js,/}` so the barrel plus sibling-folder pair matches the existing `hooks/workflow-mark.js` + `hooks/workflow-mark/` shape; that repo-wide move landed in the same PR, and the pre-merge audit's objection that the shared-barrel case is not structurally symmetric with the entrypoint-private cases was recorded as #1696 rather than resolved here. <!-- compose-doc-append-sentinel: branch=feature/1640-measurement-infra pr=#1683 -->

### FEATURE: PR #1700 — refactor/recover-runtl4-decoupling (2026-07-29, e3d19884d10eb2342aa34b8aba95182e8870ff7a, #1700)
Background: refactor(#1586): decouple verification-gate ask from RUN_TL3; add RUN_TL4
Changes: #1586: Split the two responsibilities RUN_TL3 had been carrying. It gated both TL3 test execution (via bin/select-tests.sh) and the manual verification-gate AskUserQuestion raised during the WORKFLOW_USER_VERIFIED preflight. The ask was originally wired to RUN_E2E as a placeholder and was carried forward unchanged when #1581 renamed RUN_E2E to RUN_TL3. The risk categories the dialog asks about (hook-registration, pwsh-required, installer, skill-orchestration) require whole-pipeline real-environment verification, which is TL4 territory, so the ask now reads a dedicated RUN_TL4 toggle (default off) in skills/_shared/user-verified.md. RUN_TL3 keeps test execution and selection only; bin/check-verification-gate.sh stays a pure classifier with no env-var reads, and bin/select-tests.sh is untouched. TL4 automated tests remain unimplemented — this wires the flag to the manual dialog only. tests/feature-1405-run-e2e-verification-gate.sh was renamed to tests/feat-verification-gate-toggles.sh so the permanent coverage is no longer exposed to the feature-<N> retire policy, and reworked to 20 cases asserting both toggles orthogonally. The feat- prefix follows the TL1/TL2 subsystem naming convention in rules/test.md; feature- is the legacy issue-numbered form. .env.example states that automated TL4 tests do not exist yet, so enabling RUN_TL4 runs no tests and only restores the manual confirmation prompt. <!-- compose-doc-append-sentinel: branch=refactor/recover-runtl4-decoupling pr=#1700 -->

### FEATURE: PR #1704 — fix/fix-1679 (2026-07-29, 59e2b59, #1704)
Background: fix(#1679): enforce-worktree write-detection false-positive fixes
Changes: FIX(#1679): Four enforce-worktree write-detection false positives that blocked sanctioned non-local-write commands from the main worktree. Fixed `isExoticExecWriteIR` (eval/$() unconditional flag), `isInterpreterCWriteIR` (bash -c wrapper false-positive on quoted prose containing shell syntax), `isCommandSubstWriteIR` ($(...) in gh --body), and heredoc WRITE_PATTERNS (raw-regex scan matching <<'EOF' prose in quoted args). Replaced each with quote-span-aware IR-based checks; `cat <<EOF` with a safe body now classifies as "read"; `stripDoubleQuotedContent` is now SQ-aware (prevents phantom DQ spans from `"` inside `'...'` spans) while still stripping SQ body content so `<<EOF` inside bash -c body args is not mistaken for a real heredoc opener. `finalize-worker-overlay.js` run-initial.sh arg contract updated to accept 2-or-3 args with repo-slug type. <!-- compose-doc-append-sentinel: branch=fix/fix-1679 pr=#1704 -->

### FEATURE: PR #1703 — feature/issue-1701 (2026-07-29, 202b363, #1703)
Background: feat(#1701): block HARD file-size limit in workflow-gate.js (Gate 2)
Changes: Added Gate 2 to `hooks/workflow-gate.js`: staged code files exceeding the 500-line HARD limit now block commits at pre-commit time. `bin/review-code-size --staged` (new `--staged` mode) is the SSOT for index-blob line counting; the gate module `hooks/workflow-gate/code-size-gate.js` maps exit codes to block/ok verdicts. Fail-closed on infrastructure errors; fail-open on timeout only. Resolves silent bypass of `rules/coding/file-split.md` Pattern A. <!-- compose-doc-append-sentinel: branch=feature/issue-1701 pr=#1703 -->

### FEATURE: PR #1702 — fix/wf-state-derivation (2026-07-29, dfdb0faed201e66ca9dcbb8443cde7c447ea61b1, #1702)
Background: fix(#1305,#1681,#1091,#1619,#1648,#1674): Approach B read-time state derivation — veto de-skip, stale-state guard, early-gate security hardening
Changes: #1305 (FEATURE): session-start.js — new session no longer inherits stale state from an abandoned prior session; inheritance scan now skips sessions where every step is pending (never did real work), collecting all session IDs from SessionStart+PostCompact transcript entries before selecting.;#1681 (FEATURE): effective-state.js — reconcileEffectiveState de-skips outline/detail at read time when skip_verdict.verdict==="veto", preventing the step from being permanently pinned to "skipped" with no way to reach make-detail-plan.;#1619 (FEATURE): session-start.js — additionalContext now reliably injects the "Current workflow session_id: <sid>" line; TL3-hook-session-start.sh SS-E2 test passes.;#1648 (FEATURE): companion to #1619 — same session-start additionalContext fix; test/main-workflow-state-machine/state-inheritance.sh covers the cross-session inheritance contract.;#1674 (FEATURE): workflow-gate.js — early gate (Tier 1/Tier 2) reads effective state via reconcileEffectiveState instead of raw state; fail-closed: snapshot error falls back to raw state instead of treating all steps as complete; removed resolveAll+repoDir to prevent unvalidated git exec in early tier.;#1091 (FEATURE): introduced hooks/lib/workflow-state/effective-state.js (reconcileEffectiveState) as the single SSOT for derived step status; Approach B replaces the #1094 write-back self-repair pattern — derivation is always read-only, state file remains the canonical raw record. <!-- compose-doc-append-sentinel: branch=fix/wf-state-derivation pr=#1702 -->

### FEATURE: PR #1707 — fix/fix-1161 (2026-07-29, 39eb3b17cba3031fa010060b28ea3feaef5a81bd, #1707)
Background: fix(#1161): next-step post-merge guard — skip user_verification when reset_reason=post-merge
Changes: BUGFIX #1161: next-step (bin/workflow/next-step) — after gh pr merge, workflow-mark.js resets user_verification to pending with reset_reason: "post-merge" via PostToolUse. The currentStep walk loop only reads snapStatus() which lacks reset_reason, so next-step misidentified user_verification as the current step and re-prompted /issue-close-stage after WE-8 was already complete. Added a post-merge guard in the walk loop (mirroring post-compact.js lines 66-70) to skip user_verification when reset_reason=post-merge, treating it as effectively complete. Also appended a clarifying note to STEP_HINT.user_verification explaining that re-emitting the sentinel is unnecessary after gh pr merge. <!-- compose-doc-append-sentinel: branch=fix/fix-1161 pr=#1707 -->

### FEATURE: PR #1708 — fix/issue-1706-pr-window-open-twice (2026-07-29, 9e985c4, #1708)
Background: fix(#1706): remove duplicate openInBrowser call from show-user-verified-context.js
Changes: fix(#1706): Removed `openInBrowser(prUrl)` call from `hooks/show-user-verified-context.js` — `hooks/pr-created-open.js` is now the sole owner of browser-open responsibility, preventing the PR URL from opening twice in the browser before the USER_VERIFIED permission dialog. The "Open PR: <url>" text line in the context dialog is preserved. Updated `tests/feature-show-user-verified.sh` (U14a/b/c: inverted browser-spawn assertion, added whitebox source guard; removed vacuous U15/U16) and `tests/feature-pr-created-open.sh` (T6: NO_BROWSER opt-out, T7: non-http URL rejection, T8: positive-path regression anchor confirming pr-created-open.js remains the sole opener). <!-- compose-doc-append-sentinel: branch=fix/issue-1706-pr-window-open-twice pr=#1708 -->

### FEATURE: PR #1711 — feature/worker-dispatch (2026-07-30, a29ad7889d11aea3d9b33acba678d3eb0f049bce, #1711)
Background: feat(#1643): replace six LLM worker subagents with a deterministic dispatcher
Changes: #1643 FEATURE — Background: six worker subagents (test-runner, worktree-copy, worktree-backup, doc-append, issue-reconcile, session-close-gate) carried no judgement; every dispatch spent an LLM context on mechanical work. Changes: added `bin/worker-dispatch.js` as the single entrypoint with per-worker modules under `bin/worker-dispatch/`, `hooks/lib/worker-dispatch-registry.js` as the worker/capability SSOT, and `bin/worker-dispatch-paths` to emit the three absolute-path literals the guard's canonical form needs. `hooks/enforce-worktree/main-worktree-allows/worker-dispatch-overlay.js` sanctions exactly one command shape (5 tokens, bare `node`, quoted literals, no `$VAR`/chaining/redirect) under three locks: script-derived root == marker-validated `AGENTS_CONFIG_DIR`, argv main-root == the repo under judgement, and that root ∈ trusted main worktrees. `bin/worker-dispatch/emit.js` is the sole stdout writer and rescans every render for WORKFLOW sentinels before writing. The six caller skills now dispatch via `skills/_shared/worker-dispatch.md`, and all six `agents/*-worker.md` files were deleted. Delivered as one PR in two stages, test-runner first as canary.;#1643 follow-up within the same change: the new dispatcher's own defects were fixed before landing — the YAML renderer emitted `status: failed`, which matches no branch of the documented `pass|fail|timeout|runner-error` contract (now `runner-error`); a worker that *returned* a degenerate value instead of throwing crashed the dispatcher with an uncaught TypeError and a stack trace carrying absolute host paths (now rendered as a failure at exit 0); and `session-close-gate` read an absent and a corrupt supervisor-state file identically, so an unreadable state file silently produced `gate_action: proceed` — fail-open on the gate whose whole purpose is to withhold the Final Report while a review is outstanding (now fails closed with `yield` plus a warning finding).;#1643 follow-up: the scriptified `test-runner` was verifying the wrong tree. Its `runAll` script was anchored to `main-root`, and `tests/run-all.sh` derives its test directory from `BASH_SOURCE` rather than from the process CWD — so the payload's `cwd` had no effect on which suite ran, and a dispatch from a linked worktree silently ran main's tests while reporting success. The agent-based worker it replaced ran in the given cwd, so this was a regression introduced by the scriptification. Fixed by adding a third script anchor, `family-worktree`, which resolves against the payload's target worktree; the widening stays bounded because that root is the path already validated by `assertCwdInFamily`, never an arbitrary payload path. `run()` now validates `cwd` before constructing argv, so a family-anchored script can never resolve against an unproven root. Separately, the `timeout_seconds` ceiling was raised 3600 → 21600: 3600 was below the real cost of a full `--all` sweep with `RUN_TL3=on`, so the only way to request one was a request the dispatcher would kill.;#1643 follow-up: the post-implementation security review found one class of defect in the new dispatcher — a payload value that becomes a path or an argv element was not always bounded by the scope it was supposed to stay inside — and it was fixed as a class rather than case by case. (a) `branch` was validated by charset alone, so `../../../x` survived; `derived-backup-dir` joins that onto `<main-root>/.worktree-backup`, `path.join` normalized the traversal away, and the result became an authorized fsguard write scope for the worker that copies gitignored files — `.env` included. `branch` now also passes a structural check (no `.`/`..` segment, no leading or trailing `/`, no `//`, no `-`-initial segment, all of which git ref rules already forbid), and the join result is asserted to be under the backup root regardless. (b) `test_args` was typed `text[]`, but the elements become argv for `tests/run-all.sh`, which word-splits each one and runs whatever `.sh` it lands on — so an absolute path or a `../` climb selected a script outside the validated family worktree. It is now `rel-path-arg[]`, a new capability type that proves containment structurally for values that cannot be resolved in advance (a glob may match nothing yet). (c) `GH_TOKEN`/`GITHUB_TOKEN` sat in the global child-env allowlist and so reached every worker's children — including, through the family-worktree script anchor, unreviewed code from the branch under review; they now belong to `issue-reconcile`, the only worker that authenticates. (d) sentinel neutralization covered stdout but not the artifact file the calling skill reads back, so a GitHub issue title could carry a live `<<WORKFLOW_...>>` into the transcript by the second route; `fsguard.writeFile` now applies the same redaction, reusing emit.js's function so the two cannot drift. (e) `.worktree-backup/` was excluded only via `.git/info/exclude`, which is machine-local — any other clone would stage the backed-up `.env` files on `git add -A`; it is now in the tracked `.gitignore`. <!-- compose-doc-append-sentinel: branch=feature/worker-dispatch pr=#1711 -->
