# History (agents)

## Archived
- [2026](history/2026.md) — 137 entries

### FEATURE: bin/compute-staged-tests-token.js resolves the wrong worktree (first-with-staged-tests, not caller's) (2026-07-25, #1317)
Background: `bin/compute-staged-tests-token.js` is worktree-ambiguous. Its `findRepoDirWithStagedTests()` returns the **first** linked worktree in `git worktree list` order that has any staged `tests/`, **not the caller's own worktree**. Observed in session `7c740fa6` (#1303) with ~5 concurrent linked worktrees holding staged tests: it resolved to `1292-command-ir` and returned that worktree's token `fd0f7557d54baa68` instead of the caller's true token `91786c677b52638f` (computed via the `review-tests-evidence` lib with an explicit repoDir). The `WORKFLOW_REVIEW_TESTS_COMPLETE` sentinel was therefore emitted with the wrong token, which would have stale-token-blocked the commit (`hooks/workflow-gate/review-tests-checker.js` computes the token per-repoDir correctly, so the recorded vs live tokens mismatch). Related to #1315 (same worktree-resolution class; sibling `/review-tests` fork bug filed separately).
Changes: Resolve the **caller's** worktree (honor `-C` / cwd, or a session-worktree env the orchestrator passes) instead of "first linked worktree with staged tests". Add a test: ≥2 worktrees each holding staged `tests/` → the token must match the caller's worktree, not the first one enumerated. Related to #1315

### FEATURE: ds4 backend detection and conditional rules loading mechanism (2026-07-25, #1346)
Background: To avoid loading the ds4-specific guardrails (sub-issue A) under native and save tokens, a per-backend conditional loading mechanism is needed. Source measurement: session 4e2b420d / #1330. Parent: `Group: ds4 countermeasure harness implementation`.
Changes: Implement a mechanism that detects the running backend (ds4 or native) and additionally loads the sub-issue A guardrails (`rules/backend/ds4.md`) only when running ds4, saving tokens by not loading them under native. - Candidate detection methods: an env/config flag (e.g. `CLAUDE_BACKEND=ds4`), or inference from the model ID. - Candidate load points: conditional injection at SessionStart / workflow-init. - Dependency: sub-issue A (the guardrail definitions to be loaded must exist first).

### FEATURE: fix(file-split): split supervisor-state-writer.js (545-line HARD violation) + add wsid fallback to stop-l2-findings-display.js (2026-07-25, #1504)
Background: hooks/lib/supervisor-state-writer.js is 545 lines, exceeding the 500-line HARD file-split limit (rules/coding/file-split.md). This violation existed before PR #1498 (the file was ~515 lines prior), but PR #1498 added resolveEffectiveSupervisorStateSessionId (~30 lines) making it worse. Files over 500 lines MUST be split into a sibling `<name>/` directory. Additionally, stop-l2-findings-display.js lacks the wsid dual-ID fallback that supervisor-guard.js now has — flagged as sibling sweep MUST during PR #1498 security review.
Changes: 1. Split hooks/lib/supervisor-state-writer.js into a hooks/lib/supervisor-state-writer/ sibling directory to bring the main file under 500 lines. 2. Add resolveEffectiveSupervisorStateSessionId (wsid dual-ID fallback) to stop-l2-findings-display.js to match supervisor-guard.js symmetry.

### FEATURE: issue-create SKILL.md: model label table uses model:* prefix but repo labels are reporter-model:* (2026-07-25, #1572)
Background: The label policy table in `skills/issue-create/SKILL.md` (and the underlying dispatch code) instructs adding `model:sonnet`, `model:opus`, etc. However, when `#1492` migrated labels from `model:*` to `reporter-model:*`, the SKILL.md table and `issue-create.sh` were not updated to match. When `/issue-create` tries to add `--label "model:sonnet"` via `gh issue create`, it fails with `could not add label: 'model:sonnet' not found`. The issue is still created but without the reporter-model label, requiring a manual `gh issue edit --add-label reporter-model:sonnet` workaround.
Changes: - Update the model label table in `skills/issue-create/SKILL.md` to use `reporter-model:*` prefix (reporter-model:fable, reporter-model:opus, reporter-model:sonnet, reporter-model:ds4) - Update the corresponding dispatch code in `bin/github-issues/issue-create.sh` (or wherever the `--label model:*` flag is generated) to use the correct prefix - Add a test that verifies the generated label name matches an existing label Related to #1492

### FEATURE: post-merge verification: confirm on the ground that conditional rules are actually conditionally loaded after the paths: conversion (2026-07-25, #1653)
Background: #1641 / PR #1650 converted the frontmatter key of 15 rules files from `globs:` to `paths:`, but the actual load behavior can only be confirmed after merge. `~/.claude/rules` points at the main worktree (`C:/git/agents/rules`), so the state on the branch cannot be observed before merge. intent.md had agreed on "run existing tests + an in-session live check by Claude Code itself" as the verification method, but at the user_verification gate the user decided to defer the live check to the next session after merge. Because the record of that deferral exists only in the gitignored `WORKTREE_NOTES.md` and would be lost when the worktree is removed, this issue is filed as the receiving vessel for it.
Changes: - In a new session after merge, confirm that the 15 converted files are no longer injected into the context of sessions that do not touch the target paths. - Also confirm that they are correctly injected in sessions that do touch the target paths. - The important failure direction is omission, not over-loading. If `paths:` fails to match at user scope, the rules silently disappear from every session — far harder to notice than over-loading. - If an omission is confirmed, revert or re-fix. Related to #1641. Related to #1641

### FEATURE: PR #1656 — feature/prune-stub-sessions (2026-07-26, 1f4f325844cb655973dad5f8c713dc1066ecc5ea, #1656)
Background: feat(#1655): prune stray title-only stub session .jsonl files
Changes: #1655: vscode-cc-repair can now detect and delete duplicate title-only stub session .jsonl files left behind when the VS Code Claude Code extension races session creation across worktrees — re-verifies stub identity and counterpart coverage against disk immediately before unlinking. Also refactored bin/vscode-cc-repair from 507 lines down to 43 (dispatch-only) with logic moved into bin/lib/vscode-cc-repair/ (patch.js, prune.js, primitives.js). <!-- compose-doc-append-sentinel: branch=feature/prune-stub-sessions pr=#1656 -->

### FEATURE: PR #1664 — refactor/1569-quote-spans (2026-07-28, 59d60563e62fbe34920ea7be4e1bf2a14eae86e4, #1664)
Background: refactor(#1569, #1630): shared quote-span scanner + env-independent AGENTS_CONFIG_DIR anchor
Changes: #1569: Introduced `hooks/lib/quote-spans.js` (`quote-spans/{scan,query,transform}.js`) as the single quote- and expansion-span scanner for enforce-worktree write detection, replacing the per-consumer ad-hoc quote walkers in `hooks/lib/{strip-quoted-args,bash-write-targets,command-ir}.js`, `main-worktree-allows/worker-script.js`, and the newly extracted `hooks/enforce-worktree/arg-tail-guard.js`. Background: each quoting-form defect had been point-fixed in one predicate at a time, leaving the symmetric siblings broken (CPR-8). Fail-closed contract: unparseable nesting or nesting past `MAX_SPAN_DEPTH` returns `ok:false` with a fail reason and every consumer maps that to the write/block side; the transform boundaries return their input unchanged instead of throwing, so a pathological command cannot crash the hook into emitting no verdict (a crash is fail-open). Security review found and fixed 8 defects before merge, the most serious being `case`…`esac` pattern terminators consumed as command-substitution closers (fail-open in both the scanner and `extractCommandSubstitutions`), literal `$(`/backtick accepted in unquoted arg tails, an unbounded recursion depth, and an O(n²) per-character rescan in the arg-tail guard (replaced by a single memoized offset-map sweep).;#1630: Added `hooks/lib/agents-config-dir.js`, an env-independent trust anchor for the agents config dir. Background: predicates that identify a sanctioned script by `<config-dir>/<rel-path>` read `process.env.AGENTS_CONFIG_DIR` directly — absent in subagent- and Bash-tool-spawned hook processes (the finalize-worker overlay therefore false-BLOCKed sanctioned eval) and attacker-supplied in the hostile case. Resolution now falls through env → module anchor (`__dirname`) → realpath, accepting a candidate only when it carries both the `hooks/enforce-worktree.js` and `bin/` markers; a single-marker match is ambiguous and rejected. `hooks/lib/load-env.js` keeps env-only resolution deliberately (CPR-3) — an explicit `AGENTS_CONFIG_DIR` must remain the sole settings source, or an alternate config dir would silently be injected with the real repo's `.env`; only the candidate enumeration is shared. Also hardened the overlay's argument validation (every token passes `isSimpleArgValue` before spec dispatch; unknown specs fail closed) and made absolute-path detection platform-conditional (win32 accepts `/` and `\`, POSIX only `/`). <!-- compose-doc-append-sentinel: branch=refactor/1569-quote-spans pr=#1664 -->

### FEATURE: PR #1649 — feature/1627-vscode-patch-reapply (2026-07-28, 3f2d4c9, #1649)
Background: feat(#1627): add bin/vscode-patch-include-worktrees
Changes: Added `bin/vscode-patch-include-worktrees`, which re-applies the `includeWorktrees:!1` -> `!0` flip in the installed `anthropic.claude-code` VS Code extension bundle. The extension hardcodes the falsy literal in its session-list call, so sessions whose cwd is a linked worktree never appear in the list, and every extension auto-upgrade reverts a hand-edit. The tool classifies every `includeWorktrees:` site rather than blind-replacing: an unrecognized value or more than one boolean-literal site refuses before any write, the baseline and the result are both validated with `node --check`, the edit is byte-length-preserving, and all I/O round-trips through latin1 buffers so invalid UTF-8 bytes elsewhere in the minified bundle survive untouched. Writes go to an exclusive-create temp file and are renamed into place; `extension.js.bak` is created only when absent. Discovery covers the four home-relative extension roots (stable/insiders, local/server), dedups by realpath, and accepts repeated `--extensions-dir` overrides.;Removed the dead `rules/file-investigation.md` stub. The file had been reduced to a single `Moved to skills/_shared/file-investigation.md.` pointer by the #1188 rules cleanup, but nothing referenced the old path any more — the sole live consumer, `skills/survey-history/scripts/history-docs-search.sh`, already points at the new location. As a `rules/*.md` file the stub was still being injected into every session's context, so the only thing loaded by default was a pointer, while the content it pointed at was not loaded at all. <!-- compose-doc-append-sentinel: branch=feature/1627-vscode-patch-reapply pr=#1649 -->

### FEATURE: PR #1646 — feature/gh-outbound-guard (2026-07-28, 050b6d7, #1646)
Background: fix(#1591): fail-closed outbound scan guard for gh issue/PR writes
Changes: gh_outbound_guard fail-closed scan guard wired into intent-derived issue creation (clarify-commit-scope.sh Path C), issue-create.sh, reopen-with-update.sh, and the GitHub Contents/Git-Data write helpers — outbound issue/PR title+body content is scanned for private info before submission and blocked when the scanner can't be resolved or a match is found (#1591) <!-- compose-doc-append-sentinel: branch=feature/gh-outbound-guard pr=#1646 -->

### FEATURE: PR #1677 — refactor/1655-vscode-cc-repair-relocate (2026-07-28, f61181f, #1677)
Background: refactor(#1655): relocate vscode-cc-repair out of bin/lib/ and document apply-by-default
Changes: Moved the 10 `vscode-cc-repair` modules out of `bin/lib/` into `bin/vscode-cc-repair/`, and turned the same-named entrypoint file into `bin/vscode-cc-repair/index.js` so the tool follows the existing `bin/<tool-name>/` layout used by `bin/github-issues/`, `bin/refactor-prompts/`, and `bin/workflow/`. `bin/lib/` now holds only libraries genuinely shared by two or more entrypoints. Mechanical move via `git mv` with no logic change: internal requires were already relative, the entrypoint's three `./lib/vscode-cc-repair/*` requires collapsed to `./cli` / `./patch` / `./prune`, and index mode 100755 was re-recorded. Both suites stay green (565 + 616 cases); the structural packaging guard now exempts `index.js` from the no-shebang / 100644 library assertions, which G01/G02 already assert in the opposite direction for the entrypoint, and its destructive-call-site source grep now covers the entrypoint as well. README.md also gained an explicit statement that omitting `--dry-run` applies for real — `--prune-stub-sessions` renames matched stubs to `.bak` with no confirmation prompt — because the previous wording only mentioned that `--dry-run` exists. The apply-by-default behaviour itself was left unchanged: a survey of primary sources (git clean, terraform, kubectl, rsync, ansible, helm, docker system prune, certbot, cargo publish, aws-cli) found no major CLI that defaults to dry-run, and a default no-op silently exits 0 when called from a script. <!-- compose-doc-append-sentinel: branch=refactor/1655-vscode-cc-repair-relocate pr=#1677 -->

### FEATURE: PR #1676 — fix/1133-1148-approval-gate (2026-07-28, 20b0a94118f7c0761553dd4d907a5f123e04485a, #1676)
Background: fix(#1133,#1148): env-file-only gate for CONFIRM_OUTLINE/CONFIRM_DETAIL approval bypass
Changes: TL3 (real end-to-end) verification of the outline/detail approval gate was not performed before merge — only TL1/TL2 coverage exists (15 sub-tests in tests/fix-1133-1148-approval-gate/). Accepted as risk per user decision on 2026-07-28; full clarify-intent -> make-outline-plan -> make-detail-plan E2E run through a real session was not executed.;run_tests marked complete with 12/26 selected tests failing. All 12 failures reproduced identically on a clean pre-branch baseline worktree (detached at HEAD 8b197f02, before any #1133/#1148 changes) — confirmed not a regression from this branch. Root causes already tracked: #1619, #1620, #1629, #1632, #1633. <!-- compose-doc-append-sentinel: branch=fix/1133-1148-approval-gate pr=#1676 -->

### FEATURE: PR #1675 — feature/issue-1672-backdate (2026-07-28, bc602fbca7961a4a7465cf38a03c35d5c261b2f8, #1675)
Background: feat(#1672): add --allow-backdate to doc-append and batch backfill script
Changes: Security review of the backfill path found two defects in the new code, both fixed before merge. issue-to-history.sh cd's into AGENTS_CONFIG_DIR, so its duplicate-entry grep resolved docs/history.md there while the append honored --target; since the batch path always passes --target, a re-run appended a duplicate into an append-only record, and --allow-backdate had removed the ascending-date guard that was catching this by accident. The check now unions the canonical pair with the target and its sibling archive. Separately, backfill-batch.sh resolved sort-history.py / doc-rotate.py from the caller-supplied --repo-dir while the append step pinned AGENTS_CONFIG_DIR; both now run from the config dir with the absolute target path.;doc-append rejected every /issue-reconcile backfill entry: its ascending-date guard blocks any entry dated more than 7 days before the stream tail, and a backfill entry is older than the tail by definition. Added `--allow-backdate` to lift that guard alone, forwarded it plus `--no-auto-rotate` through issue-to-history.sh, and added skills/issue-reconcile/scripts/backfill-batch.sh so a batch appends once and then sorts and rotates exactly once rather than rotating per entry. doc-append re-sorts pre-existing entries but writes each new one at the tail, so the trailing sort-history.py run is what actually places backdated entries. <!-- compose-doc-append-sentinel: branch=feature/issue-1672-backdate pr=#1675 -->

### FEATURE: PR #1683 — feature/1640-measurement-infra (2026-07-29, 3798cc1, #1683)
Background: feat(#1640): lightweight measurement infrastructure
Changes: #1640: added lightweight measurement infrastructure covering exactly three metrics, with no LLM calls on any path. `bin/measure-norm-docs` reports normative-doc size against the `rules/coding/file-split.md` thresholds, `bin/count-subagents` counts subagent invocations from the session JSONL, and a new `hooks/workflow-state/step-timestamps.js` module records a per-step `started_at` in workflow state, wired into `state-io.js` `markStep` and `hooks/workflow-mark/reset-handler.js`. Until now these three quantities could only be estimated by hand, so prompt growth, subagent fan-out, and step duration were invisible when deciding whether a split or a refactor was warranted. The timestamp recorder is gated behind `RECORD_STEP_TIMESTAMPS` (default `off`) so existing sessions pay nothing for it, and both CLIs are read-only. During review the repo owner asked for `hooks/lib/workflow-state{.js,/}` to be relocated to `hooks/workflow-state{.js,/}` so the barrel plus sibling-folder pair matches the existing `hooks/workflow-mark.js` + `hooks/workflow-mark/` shape; that repo-wide move landed in the same PR, and the pre-merge audit's objection that the shared-barrel case is not structurally symmetric with the entrypoint-private cases was recorded as #1696 rather than resolved here. <!-- compose-doc-append-sentinel: branch=feature/1640-measurement-infra pr=#1683 -->

### FEATURE: PR #1700 — refactor/recover-runtl4-decoupling (2026-07-29, e3d19884d10eb2342aa34b8aba95182e8870ff7a, #1700)
Background: refactor(#1586): decouple verification-gate ask from RUN_TL3; add RUN_TL4
Changes: #1586: Split the two responsibilities RUN_TL3 had been carrying. It gated both TL3 test execution (via bin/select-tests.sh) and the manual verification-gate AskUserQuestion raised during the WORKFLOW_USER_VERIFIED preflight. The ask was originally wired to RUN_E2E as a placeholder and was carried forward unchanged when #1581 renamed RUN_E2E to RUN_TL3. The risk categories the dialog asks about (hook-registration, pwsh-required, installer, skill-orchestration) require whole-pipeline real-environment verification, which is TL4 territory, so the ask now reads a dedicated RUN_TL4 toggle (default off) in skills/_shared/user-verified.md. RUN_TL3 keeps test execution and selection only; bin/check-verification-gate.sh stays a pure classifier with no env-var reads, and bin/select-tests.sh is untouched. TL4 automated tests remain unimplemented — this wires the flag to the manual dialog only. tests/feature-1405-run-e2e-verification-gate.sh was renamed to tests/feat-verification-gate-toggles.sh so the permanent coverage is no longer exposed to the feature-<N> retire policy, and reworked to 20 cases asserting both toggles orthogonally. The feat- prefix follows the TL1/TL2 subsystem naming convention in rules/test.md; feature- is the legacy issue-numbered form. .env.example states that automated TL4 tests do not exist yet, so enabling RUN_TL4 runs no tests and only restores the manual confirmation prompt. <!-- compose-doc-append-sentinel: branch=refactor/recover-runtl4-decoupling pr=#1700 -->

### FEATURE: PR #1704 — fix/fix-1679 (2026-07-29, 59e2b59, #1704)
Background: fix(#1679): enforce-worktree write-detection false-positive fixes
Changes: FIX(#1679): Four enforce-worktree write-detection false positives that blocked sanctioned non-local-write commands from the main worktree. Fixed `isExoticExecWriteIR` (eval/$() unconditional flag), `isInterpreterCWriteIR` (bash -c wrapper false-positive on quoted prose containing shell syntax), `isCommandSubstWriteIR` ($(...) in gh --body), and heredoc WRITE_PATTERNS (raw-regex scan matching <<'EOF' prose in quoted args). Replaced each with quote-span-aware IR-based checks; `cat <<EOF` with a safe body now classifies as "read"; `stripDoubleQuotedContent` is now SQ-aware (prevents phantom DQ spans from `"` inside `'...'` spans) while still stripping SQ body content so `<<EOF` inside bash -c body args is not mistaken for a real heredoc opener. `finalize-worker-overlay.js` run-initial.sh arg contract updated to accept 2-or-3 args with repo-slug type. <!-- compose-doc-append-sentinel: branch=fix/fix-1679 pr=#1704 -->

### FEATURE: PR #1703 — feature/issue-1701 (2026-07-29, 202b363, #1703)
Background: feat(#1701): block HARD file-size limit in workflow-gate.js (Gate 2)
Changes: Added Gate 2 to `hooks/workflow-gate.js`: staged code files exceeding the 500-line HARD limit now block commits at pre-commit time. `bin/review-code-size --staged` (new `--staged` mode) is the SSOT for index-blob line counting; the gate module `hooks/workflow-gate/code-size-gate.js` maps exit codes to block/ok verdicts. Fail-closed on infrastructure errors; fail-open on timeout only. Resolves silent bypass of `rules/coding/file-split.md` Pattern A. <!-- compose-doc-append-sentinel: branch=feature/issue-1701 pr=#1703 -->

### FEATURE: PR #1702 — fix/wf-state-derivation (2026-07-29, dfdb0faed201e66ca9dcbb8443cde7c447ea61b1, #1702)
Background: fix(#1305,#1681,#1091,#1619,#1648,#1674): Approach B read-time state derivation — veto de-skip, stale-state guard, early-gate security hardening
Changes: #1305 (FEATURE): session-start.js — new session no longer inherits stale state from an abandoned prior session; inheritance scan now skips sessions where every step is pending (never did real work), collecting all session IDs from SessionStart+PostCompact transcript entries before selecting.;#1681 (FEATURE): effective-state.js — reconcileEffectiveState de-skips outline/detail at read time when skip_verdict.verdict==="veto", preventing the step from being permanently pinned to "skipped" with no way to reach make-detail-plan.;#1619 (FEATURE): session-start.js — additionalContext now reliably injects the "Current workflow session_id: <sid>" line; TL3-hook-session-start.sh SS-E2 test passes.;#1648 (FEATURE): companion to #1619 — same session-start additionalContext fix; test/main-workflow-state-machine/state-inheritance.sh covers the cross-session inheritance contract.;#1674 (FEATURE): workflow-gate.js — early gate (Tier 1/Tier 2) reads effective state via reconcileEffectiveState instead of raw state; fail-closed: snapshot error falls back to raw state instead of treating all steps as complete; removed resolveAll+repoDir to prevent unvalidated git exec in early tier.;#1091 (FEATURE): introduced hooks/lib/workflow-state/effective-state.js (reconcileEffectiveState) as the single SSOT for derived step status; Approach B replaces the #1094 write-back self-repair pattern — derivation is always read-only, state file remains the canonical raw record. <!-- compose-doc-append-sentinel: branch=fix/wf-state-derivation pr=#1702 -->

### FEATURE: PR #1707 — fix/fix-1161 (2026-07-29, 39eb3b17cba3031fa010060b28ea3feaef5a81bd, #1707)
Background: fix(#1161): next-step post-merge guard — skip user_verification when reset_reason=post-merge
Changes: BUGFIX #1161: next-step (bin/workflow/next-step) — after gh pr merge, workflow-mark.js resets user_verification to pending with reset_reason: "post-merge" via PostToolUse. The currentStep walk loop only reads snapStatus() which lacks reset_reason, so next-step misidentified user_verification as the current step and re-prompted /issue-close-stage after WE-8 was already complete. Added a post-merge guard in the walk loop (mirroring post-compact.js lines 66-70) to skip user_verification when reset_reason=post-merge, treating it as effectively complete. Also appended a clarifying note to STEP_HINT.user_verification explaining that re-emitting the sentinel is unnecessary after gh pr merge. <!-- compose-doc-append-sentinel: branch=fix/fix-1161 pr=#1707 -->

### FEATURE: PR #1708 — fix/issue-1706-pr-window-open-twice (2026-07-29, 9e985c4, #1708)
Background: fix(#1706): remove duplicate openInBrowser call from show-user-verified-context.js
Changes: fix(#1706): Removed `openInBrowser(prUrl)` call from `hooks/show-user-verified-context.js` — `hooks/pr-created-open.js` is now the sole owner of browser-open responsibility, preventing the PR URL from opening twice in the browser before the USER_VERIFIED permission dialog. The "Open PR: <url>" text line in the context dialog is preserved. Updated `tests/feature-show-user-verified.sh` (U14a/b/c: inverted browser-spawn assertion, added whitebox source guard; removed vacuous U15/U16) and `tests/feature-pr-created-open.sh` (T6: NO_BROWSER opt-out, T7: non-http URL rejection, T8: positive-path regression anchor confirming pr-created-open.js remains the sole opener). <!-- compose-doc-append-sentinel: branch=fix/issue-1706-pr-window-open-twice pr=#1708 -->

### FEATURE: PR #1711 — feature/worker-dispatch (2026-07-30, a29ad7889d11aea3d9b33acba678d3eb0f049bce, #1711)
Background: feat(#1643): replace six LLM worker subagents with a deterministic dispatcher
Changes: #1643 FEATURE — Background: six worker subagents (test-runner, worktree-copy, worktree-backup, doc-append, issue-reconcile, session-close-gate) carried no judgement; every dispatch spent an LLM context on mechanical work. Changes: added `bin/worker-dispatch.js` as the single entrypoint with per-worker modules under `bin/worker-dispatch/`, `hooks/lib/worker-dispatch-registry.js` as the worker/capability SSOT, and `bin/worker-dispatch-paths` to emit the three absolute-path literals the guard's canonical form needs. `hooks/enforce-worktree/main-worktree-allows/worker-dispatch-overlay.js` sanctions exactly one command shape (5 tokens, bare `node`, quoted literals, no `$VAR`/chaining/redirect) under three locks: script-derived root == marker-validated `AGENTS_CONFIG_DIR`, argv main-root == the repo under judgement, and that root ∈ trusted main worktrees. `bin/worker-dispatch/emit.js` is the sole stdout writer and rescans every render for WORKFLOW sentinels before writing. The six caller skills now dispatch via `skills/_shared/worker-dispatch.md`, and all six `agents/*-worker.md` files were deleted. Delivered as one PR in two stages, test-runner first as canary.;#1643 follow-up within the same change: the new dispatcher's own defects were fixed before landing — the YAML renderer emitted `status: failed`, which matches no branch of the documented `pass|fail|timeout|runner-error` contract (now `runner-error`); a worker that *returned* a degenerate value instead of throwing crashed the dispatcher with an uncaught TypeError and a stack trace carrying absolute host paths (now rendered as a failure at exit 0); and `session-close-gate` read an absent and a corrupt supervisor-state file identically, so an unreadable state file silently produced `gate_action: proceed` — fail-open on the gate whose whole purpose is to withhold the Final Report while a review is outstanding (now fails closed with `yield` plus a warning finding).;#1643 follow-up: the scriptified `test-runner` was verifying the wrong tree. Its `runAll` script was anchored to `main-root`, and `tests/run-all.sh` derives its test directory from `BASH_SOURCE` rather than from the process CWD — so the payload's `cwd` had no effect on which suite ran, and a dispatch from a linked worktree silently ran main's tests while reporting success. The agent-based worker it replaced ran in the given cwd, so this was a regression introduced by the scriptification. Fixed by adding a third script anchor, `family-worktree`, which resolves against the payload's target worktree; the widening stays bounded because that root is the path already validated by `assertCwdInFamily`, never an arbitrary payload path. `run()` now validates `cwd` before constructing argv, so a family-anchored script can never resolve against an unproven root. Separately, the `timeout_seconds` ceiling was raised 3600 → 21600: 3600 was below the real cost of a full `--all` sweep with `RUN_TL3=on`, so the only way to request one was a request the dispatcher would kill.;#1643 follow-up: the post-implementation security review found one class of defect in the new dispatcher — a payload value that becomes a path or an argv element was not always bounded by the scope it was supposed to stay inside — and it was fixed as a class rather than case by case. (a) `branch` was validated by charset alone, so `../../../x` survived; `derived-backup-dir` joins that onto `<main-root>/.worktree-backup`, `path.join` normalized the traversal away, and the result became an authorized fsguard write scope for the worker that copies gitignored files — `.env` included. `branch` now also passes a structural check (no `.`/`..` segment, no leading or trailing `/`, no `//`, no `-`-initial segment, all of which git ref rules already forbid), and the join result is asserted to be under the backup root regardless. (b) `test_args` was typed `text[]`, but the elements become argv for `tests/run-all.sh`, which word-splits each one and runs whatever `.sh` it lands on — so an absolute path or a `../` climb selected a script outside the validated family worktree. It is now `rel-path-arg[]`, a new capability type that proves containment structurally for values that cannot be resolved in advance (a glob may match nothing yet). (c) `GH_TOKEN`/`GITHUB_TOKEN` sat in the global child-env allowlist and so reached every worker's children — including, through the family-worktree script anchor, unreviewed code from the branch under review; they now belong to `issue-reconcile`, the only worker that authenticates. (d) sentinel neutralization covered stdout but not the artifact file the calling skill reads back, so a GitHub issue title could carry a live `<<WORKFLOW_...>>` into the transcript by the second route; `fsguard.writeFile` now applies the same redaction, reusing emit.js's function so the two cannot drift. (e) `.worktree-backup/` was excluded only via `.git/info/exclude`, which is machine-local — any other clone would stage the backed-up `.env` files on `git add -A`; it is now in the tracked `.gitignore`. <!-- compose-doc-append-sentinel: branch=feature/worker-dispatch pr=#1711 -->

### FEATURE: PR #1730 — docs/reconcile-1672-en-translation (2026-07-30, 6297f19b413ee67aa669cd5203981ea51c5e6c10, #1730)
Background: fix(#1672): translate Japanese history.md entries to English
Changes: Discovered that the #1672 bulk history backfill (PR #1722) had copied Japanese-language issue content verbatim into the English-only docs/history.md (2 entries) and docs/history/2026.md (59 entries), violating rules/github-issues.md. Reconciled and translated all 61 entries into 5 scratch batch files, verified against an authoritative direct-scan of the source files (zero duplicates, zero missing). Filed #1725 (propose WORKFLOW_OFF override for hooks/block-history-direct.js, which currently blocks all direct writes to the history/changelog family with no bypass) and #1726 (several docs/history/2026.md entries have truncated/corrupted Changes fields from the original backfill) to track follow-up fixes separately from this reconciliation pass. <!-- compose-doc-append-sentinel: branch=docs/reconcile-1672-en-translation pr=#1730 -->

### FEATURE: PR #1729 — feature/1721-parallel-subagent-dispatch (2026-07-30, 6d48bbeb32acbc0673113ec2a296be5daa32de05, #1729)
Background: feat(#1721): centralize subagent parallel-dispatch protocol
Changes: feat(#1721): add `skills/_shared/subagent-concurrency.md` (SC-P/SC-S/SC-W protocol) and reduce workflow-init/review-code-security to reference it; annotate worktree-end and issue-close-finalize with SC-S serial-by-dependency reasons; add SC-W wait-guidance to codex-review-loop.md and write-tests/SKILL.md. <!-- compose-doc-append-sentinel: branch=feature/1721-parallel-subagent-dispatch pr=#1729 -->

### FEATURE: PR #1737 — fix/block-history-workflow-off (2026-07-30, bfe534c678602ec7fc7336158c9f44cf4ed91507, #1737)
Background: fix(#1725): honor WORKFLOW_OFF / EMERGENCY OFF override in block-history-direct.js
Changes: #1725: `block-history-direct.js` now honors the session's `WORKFLOW_ENFORCE_WORKFLOW_OFF` / `WORKFLOW_ENFORCE_WORKFLOW_OFF_EMERGENCY` override — a would-be-blocked append-only doc write (`docs/history.md`, `CHANGELOG.md`, and their rotated archives) approves instead of blocking when the calling session has an active `isWorkflowOff(sid)` marker, matching the existing pattern already used by `enforce-worktree.js` and `enforce-issue-close.js`. No new marker file was introduced — the existing marker already covers both sentinels.;#1724: Closed as superseded (not implemented) — the root problem (no way to fix `docs/history.md` mistakes from inside a session) is resolved by #1725's bypass; a dedicated auditable amend path for `doc-append.py` was judged unnecessary for now and can be re-filed separately if a real need for it arises. <!-- compose-doc-append-sentinel: branch=fix/block-history-workflow-off pr=#1737 -->

### FEATURE: PR #1752 — fix/review-plan-codex-stderr-fix (2026-07-31, 5fb8c8930edf2072212b746d1eb65d9f420ce805, #1752)
Background: fix(#1734): route plan-truncation warning to stderr, not stdout
Changes: FIX #1734: `bin/review-plan-codex`'s plan-truncation warning was printed to stdout, where `bin/run-codex-review-loop`'s `awk 'NF{print; exit}'` status-header parser misread it as the header whenever a plan/draft exceeded `MAX_PLAN_LINES=5000`, causing a false "unrecognized status header" `die()` → exit 4 (HALT) that broke the review loop. Fixed by redirecting the warning to stderr (line 155, `>&2`). Regression test `tests/feature-1734-plan-truncation-stderr.sh` covers direct invocation, the full wrapper pipeline, and the exact 5000/5001-line boundary. <!-- compose-doc-append-sentinel: branch=fix/review-plan-codex-stderr-fix pr=#1752 -->

### FEATURE: PR #1751 — fix/fix-review-tests-token-deletions (2026-07-31, f475025d9d89c05ba05d68e1b9a709c461c7c251, #1751)
Background: fix(#1068): exclude staged deletions from compute-staged-tests-token fingerprint
Changes: fix(#1068): `computeStagedTestsToken` in `hooks/workflow-gate/review-tests-evidence.js` returned `null` (fail-open, bypassing the stale-token gate) whenever any staged `tests/**`/`test/**` path was a deletion, because `git rev-parse :<path>` has no index entry for a deleted path and throws. Added `--diff-filter=d` to the staged-path enumeration (matching the exclude-deletions idiom already used in `hooks/pre-commit`), so deleted paths are excluded before the OID lookup while surviving staged test paths still produce a valid fingerprint. All-deletion staging sets intentionally continue to return `null` per the existing fail-open convention (scope confirmed against issue #1068 and PR #1065). Added `tests/fix-1068-compute-staged-tests-token-deletion.sh` (dispatcher) + 5 sibling test files covering deletion/mixed/rename/error-path/status-combination cases. <!-- compose-doc-append-sentinel: branch=fix/fix-review-tests-token-deletions pr=#1751 -->

### FEATURE: PR #1772 — feature/wf-init (2026-07-31, 40afb38565e98fcaa73510d71d40fdacfc3967d8, #1772)
Background: feat(#1743): add /wf-init alias for /workflow-init
Changes: FEATURE (#1743): Add `/wf-init` as a short alias for `/workflow-init`, since an unowned built-in command (`/workflow-launch-exec`) outranks `/workflow-init` in Claude Code's autocomplete. Implemented as an installer-created directory symlink (`skills/wf-init` → `skills/workflow-init`) added to both `install/win/dotfileslink.ps1` and `install/linux/dotfileslink.sh`, plus a `.gitignore` entry for the symlink path. Filed a separate mid-workflow issue (#1771) for structural retarget-deletion risk and win/posix failure-handling asymmetry found across the whole `$links`/`_link_one` mechanism during review — out of scope for this change. <!-- compose-doc-append-sentinel: branch=feature/wf-init pr=#1772 -->

### FEATURE: PR #1774 — feature/session-sync (2026-08-01, 86a3dc224b78d185c9e091c3d8a6f93345a1d3f4, #1774)
Background: feat(#1747): add SESSION_SYNC toggle (default off) + fixes for #1218 #1564 #1214 #1739
Changes: #1747: Automatic Claude Code session sync was always on with no way to turn it off, so every shell startup ran a `git fetch` and every `codes` invocation ran a push whether or not the user wanted sync. Added a `SESSION_SYNC` toggle in `.env` (`on` / `off` only, default `off`) and gated the two automatic systems across four call sites — shell-startup fetch and `codes` push, in both `profile-snippet.sh` and `profile-snippet.ps1`. Resolution is fail-safe OFF: unset, empty, unrecognized values, config-read failure, and a missing node all keep sync off; only an explicit `on` enables it. Manual `session-sync push/pull/status/reset` is deliberately never gated, and installer bootstrap (`session-sync-init.sh` / `.ps1`) stays unconditional so manual sync works immediately after a fresh install regardless of the toggle. This is a backward-incompatible default change: existing users must set `SESSION_SYNC=on` to keep the previous behavior.;#1218: `bin/session-sync.sh reset` and `bin/cc-session-mtime` fed a timestamp parsed out of untrusted JSONL content straight into `touch -d "$ts" "$f"`. Reproduction against the unmodified code showed the value is consumed as `-d`'s operand, so the reported option-injection path did not actually trigger — the change is defensive hardening, not a fix for an exploitable defect. Both call sites now reject option-looking (`-*`) timestamps before they reach `touch` and pass `--` before the filename so a leading-dash filename cannot be read as an option. Applied symmetrically to both scripts per CPR-5.;#1564: The `[sync] Push with no changes` case in `tests/main-session-sync.sh` hung because the suite never controlled `WORKFLOW_PLANS_DIR`, so session-sync resolved the real `~/.workflow-plans` — meaning pull/reset cases could also write into the developer's actual plans directory. Pinned `WORKFLOW_PLANS_DIR` suite-wide to a temporary directory, repaired the plans section, and split the file into `tests/main-session-sync/` case files so each section is independently readable.;#1214: The `session-sync.ps1 reset` Describe block in `tests/main-session-sync.Tests.ps1` failed because its fixture repo was left on an unborn branch — no initial commit — so reset had no ref to operate on. The fixture now creates an initial commit on `main` before the reset cases run.;#1739: The `* Warning: --agents is deprecated, ignoring.` line printed on every session-sync fetch was originally recorded as a Claude Code internal git-remote-helper warning requiring no local action, and the issue was closed on that basis. Re-investigation in this session found that attribution wrong: the surrounding `git fetch Claude session sync ...` text is emitted by this repository's own `profile-snippet.{sh,ps1}`, not by Claude Code. The root source of the deprecation warning itself (SSH / git / a credential helper) could not be reproduced or identified here and remains unattributed — it must not be stated as fact. The re-investigation did surface a genuine, independent defect: the `profile-snippet.ps1` auto-fetch path was the only one of the three sibling call sites not suppressing stderr (`profile-snippet.sh` uses `2>/dev/null`, `bin/session-sync.ps1` uses `2>&1 | Out-Null`), so it alone leaked raw git/SSH diagnostics into an interactive shell startup. That asymmetry is fixed in this PR. As a side effect of #1747, automatic fetch no longer runs by default, so the warning stops appearing automatically — it can still appear on a manual `session-sync pull/push`. <!-- compose-doc-append-sentinel: branch=feature/session-sync pr=#1774 -->

### FEATURE: PR #1781 — feature/code-lang-exclude (2026-08-01, 8458bafe98d79e02a17586381d0e58b0bceac11e, #1781)
Background: feat(#1741): add CODE_LANG_EXCLUDE repo-level opt-out for language check
Changes: FEATURE(#1741): Added `CODE_LANG_EXCLUDE` — a repo-granularity opt-out for the `CODE_LANG` commit-time language check, matched via the shared `ENFORCE_WORKTREE_EXCLUDE` glob/path matcher (`hooks/lib/glob-match.js` / `hooks/lib/path-coverage-match.js`). `pre-commit` now emits a one-line stderr audit trace when a commit is skipped via this exclusion. Also hardened `glob-match.js` against catastrophic-backtracking (CWE-1333) patterns with a pattern-complexity cap, protecting every consumer of the shared matcher (`ENFORCE_WORKTREE_EXCLUDE`, `CODE_LANG_EXCLUDE`). <!-- compose-doc-append-sentinel: branch=feature/code-lang-exclude pr=#1781 -->

### FEATURE: PR #1784 — feature/1769-sweep-issues (2026-08-01, e55fc5e110f2eae8c3ce4bd2c1df9acaa9196cac, #1784)
Background: feat(#1769): add /sweep-issues skill; flip /sweep family to apply-by-default
Changes: #1769 FEATURE — Background: the `/sweep` family covered branches, worktrees, plans and tests, but open GitHub issues had no member, so stale issues accumulated with no triage path. Changes: added `/sweep-issues` plus `bin/sweep-issues.sh` and the detectors under `bin/sweep-issues/` (`scan-stale-paths.js`, `list-band.sh`, `meta-parent-scan.sh`, `verify-candidate.sh`, `close-batch.sh`, `summary.sh`), registered as SW-5 in the `/sweep` hub. Detection is two-tier: tier 1 auto-closes meta parents whose sub-issues are all closed, tier 2 nominates candidates whose referenced `tests/*.sh` path tokens no longer exist and (under `--deep`) verifies each by executing the referenced test. Delivered together with the family-wide write-mode inversion below, because `/sweep-issues` had to be born into whichever convention the family settled on rather than into the one it was replacing. Security review before merge fixed the defects the new bulk-close surface introduced: `verify-candidate.sh` executed `bash "$token"` under a `tests/*.sh` glob alone, so the `..`-rejection and charset rule enforced in `scan-stale-paths.js` were never re-applied at the point of use and `tests/../../../x.sh` reached execution — token validation is now a single authority (`scan-stale-paths.js --check-tokens`) applied at both ends, backed by a `realpath`-under-`$REPO_ROOT` containment check; issue numbers taken from TSV rows were passed unvalidated to `gh`, where a leading `-` is consumed as a flag; rationale comments posted via `gh issue comment` bypassed `hooks/scan-outbound.js` entirely (the hook matches on the Bash-tool command string, never on a subprocess) and so opened a new bulk-capable private-info leak channel, now routed through a fail-closed `bin/lib/gh-outbound-guard.sh`; `--band-size`/`--band-index` were validated only inside `list-band.sh`, whose failure collapsed into `[]` and let the run proceed to close tier-1 issues, so a mistyped selection argument widened the blast radius; band fetch, stale scanning and meta-parent listing were each wrapped in `|| []` / `|| true` with a hard-coded `"errors":0` in the CI JSON, rendering auth failures and partial closes indistinguishable from a clean empty sweep; `--dry-run` still executed candidate tests in pass 2, contradicting its own contract; and `--repo` was never cross-checked against the working tree, so pointing it at another repository marked nearly every path token missing and produced a maximal false candidate set.;#847 BUGFIX — Background: `bin/sweep-plans.sh` grouped planning artifacts by filename prefix, and the empty-prefix bucket matched any hyphen-prefixed file in the plans directory, including files that were never workflow artifacts. Changes: introduced `EMPTY_PREFIX_ALLOW_RE` as a single allowlist constant applied both at the grouping site and at the TOCTOU re-check before deletion (the re-check previously re-matched on a looser pattern than the one that selected the file), and added a `files_skipped_unrecognized` counter to all three output paths so a file the allowlist declines is reported rather than silently ignored. Test gap: no test asserted which filenames the empty-prefix bucket was allowed to claim, so the over-match was invisible — covered by `tests/fix-847-sweep-plans-empty-prefix.sh`. <!-- compose-doc-append-sentinel: branch=feature/1769-sweep-issues pr=#1784 -->

### FEATURE: PR #1786 — feature/1673 (2026-08-01, a071e131b0c166764cc4d1298710f4bd3e3e357f, #1786)
Background: feat(#1673): replace close-path LLM subagents with deterministic worker scripts
Changes: FEATURE: Replaced six LLM-subagent workers (commit-push, issue-close-stage, issue-close-finalize families) with deterministic scripts under `bin/worker-dispatch.js`, closing #1673. <!-- compose-doc-append-sentinel: branch=feature/1673 pr=#1786 -->

### FEATURE: PR #1801 — fix/fix-1782-normalize-token-glob (2026-08-01, 43ef0a72ab0587388375b2063d62d03628ca1828, #1801)
Background: fix(#1782): normalize_token() no longer glob-expands or accepts root-equivalent tokens
Changes: #1782: `bin/lib/test-frontmatter-fix.sh`'s `normalize_token()` used an unquoted `for word in $pre`, causing shell glob/pathname expansion when a `# Tests:` header token contained `*`/`?`; fixed by switching to `read -r -a` word-splitting. Also added `_is_root_like_token()` so root-equivalent tokens (`/`, `.`, `..`, `./`, `../`) are no longer accepted as valid existing paths via `[[ -e "$word" ]]` (previously always true for these), across both `classify_tests_header()` and `_rebuild_tests_value()`'s direct-match branches. Covered by 16 new test cases (TC11-TC26) in `tests/fix-1576-audit-tests-fix-headers.sh`, split into 4 sourced fragment files to satisfy the file-split HARD limit. <!-- compose-doc-append-sentinel: branch=fix/fix-1782-normalize-token-glob pr=#1801 -->

### FEATURE: PR #1792 — fix/1756-settled-status (2026-08-01, 88461db9637d9d4c5bdc4294b3735038aeba80e4, #1792)
Background: fix(#1756): recognise complete as settled in next-step's fail-open re-invoke
Changes: FEATURE (#1756): `bin/workflow/next-step`'s fail-open terminal loop treated only `skipped` as settled, so a session whose `write_tests` was `complete` still matched and re-emitted `ACTION=invoke / NEXT_SKILL=write-tests` forever after every step had finished — a permanently false verdict at session close. Root cause was a duplicated inline definition of "settled": three sites spelled it `!== "complete" && !== "skipped"` while the terminal loop spelled it `=== "skipped"` alone, and the two drifted. Fixed at the class level rather than at the one line (CPR-2 / CPR-4): `isSettledStatus()` now lives once in `hooks/workflow-state/state-io/core.js`, is re-exported through the `state-io.js` barrel and reaches `hooks/workflow-state.js` unchanged via spread composition, and all four call sites use it. `tests/feature-speculative-skip-complete/l2-gate.sh` G1/G4 had been pinning the buggy verdict and were corrected to `ACTION=done`. In the same session `bin/workflow/next-step` was split (554 lines, over the 500-line HARD limit) into a 21-line dispatcher plus seven modules under `bin/workflow/lib/next-step/`; the split is mechanical code movement apart from five lazy-require depth adjustments and three `__filename` → `ENTRYPOINT_PATH` substitutions, and `entrypoint-path.js` is now the single sanctioned place a module may derive its own location. 39 dependent tests gained a `bin/workflow/lib/next-step/` frontmatter token so Tier 2 selection still reaches them after the split (38 in the split commit plus `l2-gate.sh` in the fix commit; `tests/feature-534-stop-final-report-guard/g28-trigger.sh` is deliberately excluded because staging that pre-existing 540-line file would trip the code-size gate, and its dispatcher carries the token). <!-- compose-doc-append-sentinel: branch=fix/1756-settled-status pr=#1792 -->

### FEATURE: PR #1808 — feature/1733-state-event-log (2026-08-01, e9ce5c81c17874f86afa0ce2b82ba934db64f01d, #1808)
Background: feat(#1733): migrate workflow state file to an append-only event stream
Changes: FEATURE #1733: Migrated the workflow session state file from a keyed `steps` map to an append-only event stream (schema v2). Background: the keyed map overwrote `updated_at` in place, so a step's history was destroyed on every transition and per-step elapsed time could not be reconstructed — `RECORD_STEP_TIMESTAMPS`/`started_at` existed only to work around that loss, and even then covered a single attempt. Changes: `events` is now the sole source of truth (CPR-2), each event carrying gap-free `seq`, `kind`, `at`, `provenance`, and `origin`; `current` is a projection folded by `projectState()` and rewritten on every write, keeping the legacy `steps` shape so every pre-existing consumer (`workflow-gate.js`, `next-step`, `session-start.js`) reads unchanged. `computeIntervals()` derives per-step durations from the stream, flagging `estimated` rows built on reconstructed timestamps and refusing to report negative durations. Migration from v1 (`migrateV1ToV2`) is a pure function so concurrent processes agree byte-for-byte, and it sanitizes out-of-vocabulary steps/statuses because it is the one producer that bypasses `validateEvent`. Reads never write: `readState` normalizes v1 in memory only, since the workflow directory is shared and a foreign session may still be running a release that cannot read v2; writers migrate under the state lock instead. `RECORD_STEP_TIMESTAMPS` and `started_at` (#1640) are retired. Security review of the new writer paths also replaced `execSync` with `execFileSync` for git calls taking tool-supplied paths, made the temp-file write symlink-safe (`wx`), and stopped a foreign session's out-of-vocabulary approval key from aborting state inheritance. <!-- compose-doc-append-sentinel: branch=feature/1733-state-event-log pr=#1808 -->

### FEATURE: PR #1826 — fix/1779-zero-commit-select-tests (2026-08-02, 80faa6c0326230a282e8a74ef21a50734baceffd, #1826)
Background: fix(#1779): detect zero-commit branches in resolve-merge-base and fall back to working-tree diff
Changes: Fixed `bin/select-tests.sh --auto` returning an empty test selection on a branch with zero commits (all work staged/unstaged/untracked), where `merge-base HEAD` resolves to `HEAD` itself and starves the `<merge-base>...HEAD` diff range. `bin/resolve-merge-base.sh --format kv` now reports `base_is_head`, `uncommitted_lines`, `uncommitted_files`, `untracked_files` as observational fields; `select-tests.sh` and `skills/run-tests/SKILL.md` RNT-3 fall back to diffing the working tree directly when `base_is_head=true`. Closes #1779.;Fixed the same zero-commit blind spot in Tier 2 semantic test matching (RNT-3), which shared the identical `<merge-base>...HEAD` range as Tier 1 and was equally starved on a zero-commit branch. Closes #1331. <!-- compose-doc-append-sentinel: branch=fix/1779-zero-commit-select-tests pr=#1826 -->

### FEATURE: PR #1843 — feature/issue-create-verdict (2026-08-02, dd15dc6f8bb77304f0c5cfc274cfb6cb69811b38, #1843)
Background: feat(#1761,#1763): codex worth_filing reviewer replaces regex-based issue-creation provenance
Changes: #1761: Deleted the regex-based "provenance" mechanism (~42 files) that `/issue-create`'s Phase 2 survey used to justify reopen/sub-of/make-parent verdicts, replacing it with a codex-review `worth_filing` judgment. The regex approach matched fixed keyword/pattern lists against issue text and was fragile and hard to extend as new duplicate/relation shapes appeared; the codex-review verdict is semantically driven and generalizes without pattern-list maintenance.;#1763: Added a severity + review-verdict based pre-file confirm gate driven by the new `worth_filing` provenance, and made the `/issue-create` reopen and make-parent survey verdicts more assertive — reducing unnecessary confirmation prompts when the codex review's judgment is already high-confidence while still gating on severity for lower-confidence or high-impact cases. <!-- compose-doc-append-sentinel: branch=feature/issue-create-verdict pr=#1843 -->

### FEATURE: PR #1854 — feature/preuse-auto-approve (2026-08-02, 212533a987c5ceeaa9e7033a0a0a36ba5b8147f4, #1854)
Background: feat(#1849): add preuse-auto-approve hook for Monitor and EnterWorktree
Changes: FEATURE (#1849): Add `hooks/preuse-auto-approve.js` — PreToolUse hook (matcher: `Monitor|EnterWorktree`) that auto-approves Monitor tool calls unconditionally and EnterWorktree calls when the target path is within `WORKTREE_BASE_DIR`. Eliminates per-call user confirmation dialogs for these two tools. `AUTO_APPROVE_TOOLS=off` in `.env` disables the hook instantly (restores existing dialog behavior). Rollback: remove the `Monitor|EnterWorktree` PreToolUse entry from `settings.json` and delete the hook file. Transitional design — to be removed when CC supports native allow/deny rules for Monitor and EnterWorktree. <!-- compose-doc-append-sentinel: branch=feature/preuse-auto-approve pr=#1854 -->

### FEATURE: PR #1853 — feature/1642-prompt-extraction-gate (2026-08-02, 678ab8d16661bd1451c4580bd77a1c41320d0310, #1853)
Background: feat(#1642): single-authority prompt-extraction CLI + blocking gate wiring
Changes: Added `bin/check-prompt-extraction` as single authority (SSOT) for §1.5 code-fence and §1.3 inline-procedure violations, replacing scattered detection logic;Wired as workflow-gate.js Gate 3 (blocking, before user_verification) and pre-commit backstop (defense-in-depth);Converted `bin/check-inline-procedures` to thin advisory adapter (always exits 0, delegates to new engine);Committed `.prompt-extraction-allowlist` ratchet (TOTAL=81, WILDCARD=0, ENTRIES=63) — freeze existing violations before stricter gate enforcement;Split `hooks/workflow-gate.js` (782→487 lines) into `hooks/workflow-gate/supervisor-check.js` + `hooks/workflow-gate/early-gate.js` to satisfy 500-line HARD limit;Security fixes: H1 fail-closed infra errors in targets.js, H2 ENOBUFS distinct from ETIMEDOUT in prompt-extraction-gate.js, M3 pre-commit rc=2/126/127 fail-closed, M4 NUL bytes in cli.js replaced with printable separator <!-- compose-doc-append-sentinel: branch=feature/1642-prompt-extraction-gate pr=#1853 -->

### SECURITY: PR #1857 -- fix/off-clearance-1780 (2026-08-03, 6b6c875, #1857)
Background: Hardened the OFF-clearance token lifecycle across five related issues (#1658, #1709, #1780, #1625, #1626): a mint/claim race in bin/request-off-clearance, cwd-tracking bypasses of the OFF-clearance write guard, an unprotected mint-lock temp file, a silent-allow gap in the main-worktree write guard, and a silent-death failure mode in WORKFLOW_DIR resolution.
Changes: #1658/#1780: closed the bin/request-off-clearance mint/claim race (concurrent mints could both succeed), added a dir-fsync after token write, clarified nonce wording in emitted guidance; #1780: fixed sibling cd-tracking gaps in hooks/block-off-clearance-write/bash-target-context.js (popd/Pop-Location, cd -/set-location -/sl -/chdir - OLDPWD swaps, pushd -n non-cd disqualification, command/builtin prefix unwrapping), closing bypasses of the OFF-clearance write guard via cwd-tracking blind spots; #1780: added .off-clearance.mint.lock.tmp to OFF_CLEARANCE_TOKEN_SUFFIXES in hooks/lib/protected-basenames.js so the SID-scoped exclusive mint-lock file is protected by the same write/delete guard as other token-suffix forms; #1780: extracted the shared acquireMintLock/releaseMintLock primitive into hooks/lib/off-clearance-mint-lock.js and wired hooks/supervisor-off-proposal-shim.js's claim lifecycle through it, re-reading the bare token only after lock acquisition, closing a race against bin/request-off-clearance's concurrent mint/rename/stale-claim-sweep transition -- on lock timeout the shim now fails closed with a lock-busy reason; #1625/#1626: reordered bin/request-off-clearance's WORKFLOW_DIR resolution so a resolution failure now emits an UNAVAILABLE audit record plus emergency guidance and exits 1 instead of dying silently under set -e; also gated the git -C fallback in hooks/enforce-worktree/handle-bash-write.js on isGitWriteIR(ir), closing a silent-allow-from-main path for write-detected Bash commands whose target extraction came up empty.

### FEATURE: PR #1860 — fix/fix-1833-audit-tests-survival-first (2026-08-07, 6405170fd227b808a992fc47caded1da600a26f7, #1860)
Background: fix(#1833): audit-tests.sh primary filter is now target survival, not issue-closed staleness
Changes: FEATURE #1833: Inverted `bin/audit-tests.sh`'s primary candidate filter from "issue CLOSED + stale ≥3 months" to "target-path survival verdict" (`trp_survival_verdict` in new `bin/lib/test-retire-predicate.sh`), demoting issue-closed-state to a delete-time safety check only. Extracted duplicated logic shared with `bin/audit-tests-common.sh` into the new predicate module. Removed the `--offline` early-continue so offline runs still report survival candidates. Fixed `skills/sweep-tests/SKILL.md`'s zero-result wording. Security review found HIGH/HARD issues (ambiguous-branch issue-ref regex in `trp_issue_ref`, missing non-numeric guard in `trp_compute_cutoff`, a bare `python3 -c` in a test file) — all fixed and re-verified; MEDIUM/LOW findings (e.g. `trp_json_escape`'s control-character escape coverage) were accepted as known limitations per user decision. <!-- compose-doc-append-sentinel: branch=fix/fix-1833-audit-tests-survival-first pr=#1860 -->

### FEATURE: PR #1866 — refactor/cpr-semantic-ids-1858 (2026-08-07, 650b2812f7cf4c3557ded58ff4a4bcb8ba34a2f1, #1866)
Background: refactor(#1858): replace CPR-N numeric IDs with semantic short codes, add CPR-WPH
Changes: #1858: Replaced the numeric `CPR-<N>` identifiers in `rules/core-principles.md` with semantic mnemonic short codes and reordered the principles. Background: the numeric IDs carried no meaning, so every reference required a lookup, and inserting a principle forced a renumber that invalidated every existing citation. New canonical order and codes: CPR-UO (User-Obsessed) → CPR-WPH (Why Precedes How, newly added) → CPR-SC (Separate the Concerns) → CPR-SSOT (Single Source of Truth) → CPR-E2C (Elevate to the Class) → CPR-ORTH (Orthogonality) → CPR-E2E (End-to-End Integrity) → CPR-NRS (Name Reflects Substance) → CPR-UNV (Universality, renamed from "Universality First"). Old→new map: 1→UO, 2→SSOT, 3→SC, 4→E2C, 5→ORTH, 6→E2E, 7→NRS, 8→UNV. CPR-WPH is a new principle with no numeric predecessor; it and CPR-UO are independent (deliberately not chained or ordered against each other). Every stale `CPR-<N>` reference across `rules/`, `agents/`, `skills/`, `hooks/`, `bin/`, `docs/architecture/`, `install/` and `tests/` was swept; append-only `docs/history*.md` and `CHANGELOG.md` are excluded by policy, and lines that must retain a legacy ID as test data are exempted with the new line-scoped `[CPR-LEGACY-ID-OK]` marker. Two structural changes rode along: `hooks/lib/bash-write-targets.js` (518 lines) was split per `rules/coding/file-split.md` Pattern A into sibling modules under `hooks/lib/bash-write-targets/`, and five over-500-line test files were split the same way. Security review of the split found that the extracted `bash-write-targets/exotic-exec.js` took a new `deps` parameter, creating a fail-open seam — `hooks/enforce-worktree.js` does not wrap the predicate in try/catch, so a direct single-arg call would throw and yield no verdict on exactly the eval/xargs/find commands the predicate exists to block; the module now fails closed (returns "is a write") when `deps` is missing or malformed. Test gap: no test asserted that a CPR reference resolves to an existing heading, so the numeric IDs could drift silently; the refactor test suite now pins all nine headings by exact line and asserts the old→new mapping. <!-- compose-doc-append-sentinel: branch=refactor/cpr-semantic-ids-1858 pr=#1866 -->

### FEATURE: PR #1855 — feature/stop-guard-exemptions (2026-08-07, ba69610dcb92aa54669bd65660f02d159b106e2b, #1855)
Background: feat(#1794): add Stop-guard exemption sentinels for background work a...
Changes: #1794 FEATURE: The Stop hook and EM Supervisor intervened before `workflow-init`, so self-contained skill runs were interrupted by a guard that had nothing to guard yet. Added a Stop-guard exemption mechanism: a session-scoped `<sid>.background-work` marker declared via sentinel keeps the premature-stop guard quiet while genuine background work is in flight, and an `isWorkflowStarted` gate suppresses the supervisor C2 scheduled review when no workflow state file exists. The marker authorizes on file existence alone, so `.background-work` was registered in `hooks/lib/protected-basenames.js` — the SSOT that `block-clearance-token-write.js` and enforce-worktree's marker-gate derive their denylists from — to stop a tool-issued write from forging it. An AWAITING_USER companion sentinel was drafted and then removed before merge as unnecessary surface area. Documented in `rules/stop-guard-exemptions.md`.;#1692 FEATURE: `supervisor-guard`'s private modules were split across `hooks/lib/supervisor-guard/` and `hooks/supervisor-guard/`, with colliding names between the two — a file-split Pattern A violation that made it ambiguous which copy a `require` resolved. Consolidated the modules into a single directory and updated the `require` paths; the moved files (`arbitrate.js`, `format-integrated.js`) are byte-identical renames, and `collect-audit-triggers.js` differs only by its adjusted require path. Regression coverage in `tests/refactor-1692-hooks-lib-orphan-dirs.sh`, whose stale-path sweep was also switched from `grep -r` to `git grep` so untracked and gitignored scratch files can no longer fail the case.;#1665 FEATURE: The `next-step` tool did not recognise `write_code` as an independent workflow step, leaving a structural gap in the 15-step plan where the implementation step could not be addressed or reset on its own. Closed the gap so `write_code` is tracked like its sibling steps.;#1319 FEATURE: `resolveSessionId` accepted `sessionIdFromInput` without validation, leaving a latent path-traversal in `block-memory-direct.js` — a crafted session id could escape the intended marker directory. Validation is now applied centrally inside `resolveSessionId`, so every caller inherits it rather than each hook re-implementing its own check. Covered by `tests/harden-1319-session-id-central-validation.sh`. <!-- compose-doc-append-sentinel: branch=feature/stop-guard-exemptions pr=#1855 -->

### FEATURE: PR #1867 — feature/notes-promotion (2026-08-07, 0db2f533e1cc9b19686ddca2485fc64a3d4001c5, #1867)
Background: feat(#530,#688,#1805,#1766,#1825,#1758,#1693): unconditional WORKTREE...
Changes: FEATURE (#530): worktree-end and issue-close-finalize now auto-run /issue-create for un-promoted WORKTREE_NOTES.md entries (BugsFound/RelatedTasks/NextTasks) immediately at Step WE-11 instead of deferring — deferral language is no longer permitted. Added `skills/_shared/notes-promotion.md` as the shared promotion protocol and `bin/worktree-notes-triage.js` (plus the new `bin/worktree-notes-triage/resolve.js` subcommand) to track promoted entries via inline markers.;FEATURE (#688): Improved worktree-end Step 5.5(a.5) and the docs-completion gate prompt UX — callsite conditions in `hooks/lib/worktree-notes.js` and the WE-11/SC-8/issue-close-finalize call sites now surface clearer status and skip-reason text to the user during notes promotion.;BUGFIX (#1805): worktree-end SKILL.md's WE-9 and WE-21 steps referenced Agent subagent names (`worktree-backup`, `doc-append`) that do not exist; corrected to describe the actual dispatch mechanism.;DOCS (#1766): Clarified worktree-end SKILL.md so WE-7's premise — that the user merged the PR independently via the GitHub web UI — is stated explicitly in the step body instead of being left implicit.;DOCS (#1825): Brought `rules/workflow-off.md` and `rules/worktree.md` into symmetric documentation for their equivalent OFF mechanisms (WORKFLOW_OFF vs WORKTREE_OFF), so both sentinel pairs are documented to the same depth.;BUGFIX (#1758): `detect-restart.sh`'s "(cascaded into CLAUDE.md)" wording for `rules/` file changes was corrected — it previously read as if `CLAUDE.md` itself had been edited when only a `rules/` file changed.;BUGFIX (#1693): `migration-from-todo.md` Step 4 wording was ambiguous about `--from-step N` silently skipping intermediate steps; clarified the docs and added `bin/github-issues/migration/skipped-step-warning.sh`, sourced by `orchestrate.sh`, to warn when a skip actually occurs. <!-- compose-doc-append-sentinel: branch=feature/notes-promotion pr=#1867 -->

### FEATURE: PR #1887 — fix/1862-gh-state-case (2026-08-08, d693525972b2ae91259541fb353fabf06288f82e, #1887)
Background: fix: normalize gh issue state casing in validate-review-verdict.js
Changes: #1862: Made candidate `state` comparison in `bin/github-issues/lib/validate-review-verdict.js` case-insensitive, so it correctly accepts the uppercase `OPEN`/`CLOSED` returned by `gh`. Added a matching schema note to `agents/issue-create-survey-worker.md`. Added regression tests first (TDD: confirmed red before the fix, green after). <!-- compose-doc-append-sentinel: branch=fix/1862-gh-state-case pr=#1887 -->

### FEATURE: PR #1890 — fix/final-report-issue-1614-1870 (2026-08-08, 8533539f25d0acfef2df6f2f8e32879ba3f145b4, #1890)
Background: fix: correct issueNumber field mismatch in Final Report and remove dead forFinalReport option
Changes: FIX #1614: `render-final-report.js`'s Closed Issue Outcomes section rendered `#undefined` instead of the issue number. `hooks/lib/final-report-schema.js`'s `closedIssueOutcomeLines()` read `it.number`, but `bin/issue-close-write-outcome.js` writes the field as `issueNumber`. Fixed the field-name mismatch (one-line change) and added regression test K8 in `tests/feature-405-final-report.sh` (confirmed failing before the fix, passing after).;FIX #1870: `hooks/lib/supervisor-findings-render.js`'s `forFinalReport` option in `formatLayer2Findings()` had zero production callers — the real `supervisorFindingsDetail()` path always calls with `summaryOnly: true`. Removed the dead option and its branches; retired the corresponding dead tests (K7 in `tests/feature-405-final-report.sh`, R9-R12 in `tests/feature-1027-findings-render.sh`, T6a-T6d in `tests/feature-supervisor-findings-render.sh`) and added K9 to cover the real `summaryOnly` production branch.;MAINT (bundled, no separate issue): split two test files that crossed the `rules/coding/file-split.md` 500-line hard limit and were blocking this PR's commit-push gate — `tests/feature-405-final-report.sh` (739→54 lines, pre-existing on main, out of scope for #1614/#1870) and `tests/feature-1463-session-close-scriptify.sh` (541→52 lines, newly crossed by this session's own additions). Pure file reorganization into sibling `<name>/` folders, no behavior change. Recorded here per supervisor pre-merge audit WARN (812e3d29-14ff-4cf4-9ebe-3778c27967cd session) so the squash-merge history entry reflects the full diff. <!-- compose-doc-append-sentinel: branch=fix/final-report-issue-1614-1870 pr=#1890 -->

### FEATURE: PR #1897 — fix/1305-workflow-inherit-lineage (2026-08-08, 70eabf359791f3c8c05057a588c50b40c3d348e4, #1897)
Background: fix: replace cwd+branch session-start inheritance with lineage-based key (#1305)
Changes: BUGFIX #1305: replaced session-start's cwd+branch context-scan inheritance key with a <!-- compose-doc-append-sentinel: branch=fix/1305-workflow-inherit-lineage pr=#1897 -->

### FEATURE: PR #1893 — fix/1794-guard-adoption (2026-08-08, f34bf921d38b38d23f6e93e9c46036f39866529b, #1893)
Background: fix(workflow): close gaps in Stop-guard/EM-Supervisor workflow-started detection (#1794)
Changes: #1794 FEATURE: The Stop-guard/EM-Supervisor "has the workflow started?" adoption predicate — the `ADOPTION_ORIGINS` allow-list, the raw-event-stream rewrite, `isGenuineProvenance`, and the 14 new `i-*` regression cases in `hooks/workflow-state/lifecycle.js` — is new work introduced by this PR (#1794 / PR #1893), not a carry-over from #1855: #1855 shipped only the exemption sentinels, not this predicate. This PR closes two gaps surfaced by RCS-2 Codex review during the regression fix: added `reset-sentinel` to the allow-list, since `WORKFLOW_RESET_FROM_{step}` requires explicit ask-gated user approval and is exactly as genuine as a direct `mark-step` call; gave `hooks/workflow-run-tests.js`'s automated PostToolUse `markStep` call an explicit `workflow-run-tests-auto-detect` origin override so a pattern-matched test run in an unrelated self-contained task no longer silently satisfies the adoption predicate via the default `mark-step` origin. Documented the full `ADOPTION_ORIGINS` allow-list rationale in `docs/architecture/claude-code/workflow.md` under a new `## Exemptions` subsection. Covered by `tests/feature-1794-stop-guard-exemptions/i-adoption-predicate.sh` plus direct regression runs of `tests/feature-1794-stop-guard-exemptions.sh` and `tests/main-workflow-run-tests.sh`. <!-- compose-doc-append-sentinel: branch=fix/1794-guard-adoption pr=#1893 -->

### FEATURE: PR #1903 — fix/final-report-severity-compress (2026-08-08, 3ef8d806d580a8f390c54f632f58bf3066475b9f, #1903)
Background: fix: compress Final Report Bugs Found/Related/Next Tasks sections
Changes: Background: #1886 — Final Report's Bugs Found/Related Tasks/Next Tasks sections mechanically dumped WORKTREE_NOTES.md verbatim, producing CPR-UO-violating long output, and CONV_LANG adherence in the Final Report was inconsistent. Changes: added an optional `<!-- severity: high -->` marker written at record time (new Mode B on `bin/worktree-notes-append.js`, backed by `hooks/lib/worktree-notes-sections.js`'s new `scanSection`/`entryBody`/`SEVERITY_MARKER_RE`); `bin/render-final-report/notes.js` now compresses non-severity:high entries to a title line plus a count/pointer summary while keeping severity:high entries verbatim, with `sanitizeTokens()` guarding against Stop-hook guard-token spoofing; `rules/mid-workflow-findings.md` and `skills/worktree-end/SKILL.md` Step WE-10 now point at the append CLI as the primary path; `skills/_shared/final-report-emission.md` is the new SSOT for Final Report verbatim-vs-CONV_LANG output scope, referenced from `skills/session-close/SKILL.md` SC-6. <!-- compose-doc-append-sentinel: branch=fix/final-report-severity-compress pr=#1903 -->

### FEATURE: PR #1907 — refactor/issue-1904 (2026-08-08, 060216f36454d67506aab7006351a02dc2e2feb2, #1907)
Background: cleanup(workflow-state): remove unused WORKFLOW_START_STEP constant + add MEDIUM-1 inheritance-origin regression test (#1904)
Changes: Removed unused `WORKFLOW_START_STEP` constant (and its comment and export) from `hooks/workflow-state/lifecycle.js`; dead code since PR #1893/#1794 rewrote `isWorkflowStarted()` to use event-stream + `ADOPTION_ORIGINS` allow-list. Added `tests/feature-1904-inheritance-origin-stamp.sh` regression test locking the `applyInheritance()` invariant: every inherited event is stamped `origin:"session-inherit"` regardless of the donor's own origin, including v1-migrated donors whose events carry `origin:"migration-v1-to-v2"` (closes #1904). <!-- compose-doc-append-sentinel: branch=refactor/issue-1904 pr=#1907 -->

### FEATURE: PR #1909 — fix/fix-488-ensure-project-ready-link (2026-08-08, 704bae54e3c035c047a4d4b7e68f960425f72b89, #1909)
Background: fix(issue-setup): link Projects v2 board to repo in ensure-project-re...
Changes: FIX: `ensure-project-ready.sh` (the `/issue-setup` path) did not link the Projects v2 board to its repository — the same regression `create-project.sh` had before PR #496. Now calls the shared `link_project_to_repo()` helper on both the create and reuse paths, non-fatally (warns on failure). Closes #488. <!-- compose-doc-append-sentinel: branch=fix/fix-488-ensure-project-ready-link pr=#1909 -->

### FEATURE: PR #1906 — feature/roundtrip-reduction (2026-08-08, 3a3bd19ed6945552340deaa7515d0ff60038be47, #1906)
Background: feat: reduce workflow roundtrips (types 1-4) + symmetrize related SKI...
Changes: #1644 FEATURE: Reduced unnecessary workflow round-trip steps by addressing waste types 1-4 of a 5-type taxonomy across the workflow orchestration (skip-judgment recording, `--advance` step-completion transactionality, sentinel dispatch, and symmetrized related SKILL.md files); type 5 was already fixed by PR #1711.;#1767 FEATURE: Separated the outline/detail skip judgment's complexity risk-category signals (S1-S6, which justify test/review depth) from the plan-stage-necessity judgment (so_c1/so_c2, sd_c1-sd_c3) that decides whether outline/detail can be skipped, and propagated `outline-planner`'s `SINGLE_APPROACH_JUSTIFIED` verdict into the detail-skip decision — self-evident changes with a single obvious approach no longer trigger both planning stages solely because they touch multiple files or an installer.;#1795 FEATURE: `/write-code`'s WCD-4 subagent prompt now requires a self-check (CPR-5 sibling sweep) — re-applying the same finding class that motivated a fix to its own diff before reporting completion — for finding-driven fix tasks (review_security / review-plan-security / codex-review fixes), removing a structural extra review round observed to cost 2-3 hours in a prior session.;#1796 FEATURE: `/write-code`'s WCD-4 subagent prompt now requires grepping `tests/` for literal pins on any contract it changed (new steps, branches, status values, registry keys, schema shapes) and reporting likely-stale tests in its final summary, so a `/write-tests` repair round no longer needs a separate discovery pass to find what broke. <!-- compose-doc-append-sentinel: branch=feature/roundtrip-reduction pr=#1906 -->

### FEATURE: PR #1928 — fix/1616-gh-stdout-leak (2026-08-10, 37806f8c868567583371edf51962b8dec83dd9a5, #1928)
Background: fix: suppress gh CLI stdout leaks in issue-close-stage helper scripts
Changes: BUGFIX (#1616): `gh issue edit` stdout leaked out of three github-issues wrapper scripts. Background: `clarify-commit-scope.sh` (label step), `parent-body-update.sh`, and `skills/workflow-init/scripts/path-a-label-and-board.sh` each suppressed only stderr (`2>/dev/null`) or nothing at all, so the issue URL `gh` prints on success reached the caller's stdout. Callers that capture stdout to detect a completion token then saw the URL as the token. Changes: all three call sites now suppress both streams (`>/dev/null 2>&1`); `parent-body-update.sh` additionally checks the `gh issue edit` exit status and fails with a stderr WARN plus `exit 1` instead of silently continuing, and `path-a-label-and-board.sh` keeps its fail-closed abort-marker path unchanged. Test gap: no test asserted that these scripts produce empty stdout on the success path — three new regression tests now pin the stdout contract, the `RC2` / `CREATED:<N>` token paths, and the failure-injection behavior, and the shared `gh` mock was upgraded to print a URL on `issue edit --add-label` success so the leak is actually reproducible in-test.;BUGFIX (#1684): `run-completion.sh` mis-detected the clarify-intent completion token because `ensure-board-card.sh` stdout was not suppressed. Background: `clarify-commit-scope.sh` invoked `ensure-board-card.sh ... 2>/dev/null || true`, silencing stderr only, so the board-card URL it prints went to stdout. `run-completion.sh` captures that stdout into `SCOPE_OUT` and treats any non-empty value as a `CREATED:<N>` token, exiting immediately — so a normal multi-issue Path B run never reached the Step 6 guard-loop that should return `PROCEED`, and CI-C0's dispatch table received unrecognized board-card URLs instead. Changes: the `ensure-board-card.sh` call now redirects stdout to `/dev/null` as well. Test gap: the Path B success path had no assertion that stdout is empty — the new `tests/fix-1616-clarify-commit-scope-stdout.sh` covers it alongside a regression guard that the `CREATED:<N>` token path still emits its token. <!-- compose-doc-append-sentinel: branch=fix/1616-gh-stdout-leak pr=#1928 -->

### FEATURE: PR #1902 — fix/1273-run-tests-classifier (2026-08-10, 2aebfc8a830b48014c6acd41cc8371fe973bc02f, #1902)
Background: fix: rewrite run_tests workflow-gate classifier to close emitter-ambiguity and stdout byte-attribution gaps
Changes: #1273: Rewrote the `run_tests` workflow-gate classifier in `hooks/workflow-run-tests.js` — split exec-position classification and emitter-provenance resolution into dedicated modules (`hooks/workflow-run-tests/exec-model.js`, `hooks/workflow-run-tests/provenance-identity.js`), fixed contract output positioning/formatting, and closed emitter-ambiguity and stdout byte-attribution gaps found across two follow-up security-review rounds (`hooks/lib/output-sanitize.js`, `bin/workflow/read-step-status` added). A residual structural gap (in-band stdout verification has no true out-of-band verdict channel) was filed separately as #1901 and is not fixed by this change.;#1798: Companion classifier-behavior fixes bundled with the #1273 rewrite (see PR diff — same `hooks/workflow-run-tests.js` / `hooks/lib/command-ir.js` changes).;#1378: Worker-dispatch YAML contract round-trip and log-tail integrity fixes bundled with the #1273 rewrite (`bin/worker-dispatch/emit.js`, `bin/worker-dispatch/workers/test-runner.js`). <!-- compose-doc-append-sentinel: branch=fix/1273-run-tests-classifier pr=#1902 -->

### FEATURE: PR #1929 — fix/1912-verdict-criterion (2026-08-10, 137629767423053e06d8abc0080f3d8e3c970330, #1929)
Background: fix(issue-create): unify the identity criterion on same_fix and make make-parent produce a real meta parent
Changes: #1912: the survey worker and the codex verdict reviewer were judging issue identity on different unstated criteria, so the same candidate pair could come back `sub-of` from one and `sibling` from the other. The criterion is now the single question "does one fix resolve both?", carried explicitly as a `same_fix` field in the verdict artifact (schema v3). `SAME_FIX_BY_VERDICT` in `bin/github-issues/lib/validate-review-verdict.js` is the SSOT, with a prose twin in `skills/_shared/issue-verdict-cascade.md`; validation now rejects a verdict whose `same_fix` value contradicts its verdict, so the two judges cannot drift apart silently.;#1699: the `make-parent` verdict created an ordinary issue as the parent, contradicting the meta/sub-issue model in `rules/github-issues.md` where a parent carries no implementation and closes via `admin_close_path`. The parent is now built by the new `bin/github-issues/lib/meta-parent-body.sh` with the `meta` label and the `Group: ` title prefix and no sub-issue completion state in the body, and the new fail-closed guard `bin/github-issues/lib/require-meta-parent.sh` refuses to attach siblings to a parent that is not a meta issue. The guard redacts its own diagnostic output so a `GH_DEBUG=api` trace cannot carry an `Authorization` header into stderr or the on-disk artifact. <!-- compose-doc-append-sentinel: branch=fix/1912-verdict-criterion pr=#1929 -->

### FEATURE: PR #1950 — fix/1719-gh-config-env (2026-08-10, a8cf3b58606ac38c48fd6316d955c17841c75cfa, #1950)
Background: fix(worker-dispatch): add XDG_CONFIG_HOME and GH_CONFIG_DIR to CHILD_ENV_ALLOWLIST
Changes: worker-dispatch rebuilds every child's env from `CHILD_ENV_ALLOWLIST`, and the variables naming WHERE a tool reads its configuration were missing from it, so a dispatched `gh` could not reach `hosts.yml` and every gh-driven worker failed — observed for real in session d846c1b8 as a `doc-append` worker failure during `/worktree-end` WE-21. Two earlier repairs had already landed on main without a PR number or a history entry and are recorded here together: `652fde2e` (2026-08-01, `APPDATA`) and `4b4b380c` (2026-08-07, `ProgramData`/`PROGRAMDATA` — a separate root cause: Windows OpenSSH resolving its `ssh_known_hosts2` path). This session adds `XDG_CONFIG_HOME` and `GH_CONFIG_DIR`; measured on gh 2.87.3 / Windows the resolution order is `GH_CONFIG_DIR` > `XDG_CONFIG_HOME` > `APPDATA`, so while the two higher-priority names stayed absent the exact failure `APPDATA` fixed still reproduced on any host that relocates the gh config directory. `LOCALAPPDATA` was assessed and deliberately NOT added — it does not appear in gh's resolution chain and no path in this repository needs it. The allowlist's admission rule was tightened to two clauses (names a config location AND only relocates a root already reachable through an existing member), which keeps `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` and `SSH_AUTH_SOCK` out by construction. Test gap: #1643 shipped `CHILD_ENV_ALLOWLIST` with no test pinning its membership at all, which is the direct reason this was missed; closed here with three layers — structural (exact sorted set + cardinality), behavioural (real `buildEnv()` per worker, plus missing-value, edge-string, idempotency and real-subprocess groups), and TL3 (real `gh`, `RUN_TL3`-gated, table-driven arms behind a dispatcher-free gate whose deterministic self-checks can fail the suite even when the gate is unmet). <!-- compose-doc-append-sentinel: branch=fix/1719-gh-config-env pr=#1950 -->

### FEATURE: PR #1945 — feature/1894-comment-block-size (2026-08-10, cc7b829e433df89dbc36ae407102299dca47917b, #1945)
Background: feat: warn at commit time when a staged file's comment blocks grow too large (#1894)
Changes: #1894: A comment block that outgrows the code it describes reads as documentation while hiding the thing being documented, and nothing at commit time noticed one growing — PR #1929 landed a set of `tests/` files whose comment blocks were uniformly oversized before anyone read them. Added `bin/review-comment-block-size`, an independent scanner with two modes: `--staged` compares each staged file against its committed version and reports it only when its comment blocks got longer (a file with no baseline falls back to absolute state), and `--all` walks the tracked + untracked tree as a plain absolute-state inventory. `hooks/pre-commit` gained a WARN-only section that prints those findings and always lets the commit through, guarded by a repo-identity check extracted from the existing prompt-extraction block into a shared `_is_agents_session_repo` helper — a global `core.hooksPath` routes every repo on this machine through the hook, so both repo identity and scanner availability are checked before it runs. `bin/review-code-size` and the HARD-blocking code-size gate were deliberately left untouched: this threshold is advisory by design, with no auto-fix and no commit block. Report lines escape every C0 control byte and 0x7F as `\xHH` (uppercase hex; a literal backslash is data and is never doubled) so a staged path carrying LF or ESC — `git ... -z` emits raw path bytes and ignores `core.quotePath` by design — cannot forge a `WARN:`/`ERROR:`/header line or repaint the reader's terminal; the same escaper covers the `extensions:` summary line, whose value arrives from the environment. The scanner under-detects on purpose: `#`, `//`, and `/* … */` are the only comment forms it knows. Configurable via `COMMENT_BLOCK_WARN_LINES` (default 10) and `COMMENT_BLOCK_WARN`, with the extension list read from the existing `CODE_FILE_EXTENSIONS`; the file-count and byte-size performance guards stayed compiled-in constants, because five environment variables is too wide a configuration surface for an advisory check that never blocks. The CPR-E2C sibling sweep found the same raw-path-bytes defect in `bin/review-code-size`, which feeds a HARD-blocking gate; the approved plan scoped that file out, so it was filed as #1938 instead of fixed here. <!-- compose-doc-append-sentinel: branch=feature/1894-comment-block-size pr=#1945 -->

### FEATURE: PR #1949 — fix/1899-gh-repo-view (2026-08-11, 979da603340fe9dbdbee01a2e698d00794750084, #1949)
Background: fix: resolve repository identity from origin remote instead of gh rep... (#1949)
Changes: #1899: `gh repo view` resolved the wrong repository whenever a checkout carried both `origin` and `upstream`. It asks the forge which repository the checkout belongs to, and on a fork that question has two defensible answers — so any consumer that named a repository this way could act on one the caller never named (a comment posted to the upstream project, a board card written to the wrong repo). Added `hooks/lib/parse-remote-url.js` (JS) and `bin/github-issues/lib/origin-repo.sh` (bash) as the single resolver: read `git remote get-url origin`, parse it once so host and repo id can never diverge, accept it only when it yields a github.com `owner/repo`, and fail closed on anything else rather than falling back to a caller's claim. The origin URL can embed an access token, so `redactUserinfo` strips it before the URL reaches any log or error message. Migrated the two MUST call sites (`board-card.sh`, issue-close-finalize `pre-flight.sh`) plus two high-impact OPTIONAL sites (the `issue-close-stage` worker's identity probe, `workflow-init` `route-decision.js`); `is-private-repo.js` now derives host and repo id from that one parse instead of two independent extractions that could describe different repositories. The cross-check alternative from the issue (resolve both ways, refuse on mismatch) was rejected — it keeps depending on `gh repo view`'s ambiguous resolution. Remaining call sites and doc references were deliberately deferred to #1913 to keep the diff reviewable (1 session = 1 PR). <!-- compose-doc-append-sentinel: branch=fix/1899-gh-repo-view pr=#1949 -->

### FEATURE: PR #1968 — feature/issue-1665-write-code-step (2026-08-11, 605b803d4256e9b11997117f25c036b1b9465ca6, #1968)
Background: feat: insert write_code as a tracked next-step step between review_tests and run_tests
Changes: FEATURE: insert `write_code` as a tracked `next-step` step between `review_tests` and `run_tests` (#1665) — bumps `VALID_STEPS` to 16 entries, adds state schema v3 with a v2→v3 backfill migration for sessions whose state predates the new step (no `write_code` event but a later step already settled), and retires the `WORKFLOW_BACKGROUND_WORK_START`/`_END` sentinel pair in favor of an automatic `write_code`-in-flight Stop-guard exemption (4h TTL) plus `WORKFLOW_NEXT_STEP_PAUSE` for other long-running work outside `write_code`. Also split `hooks/workflow-state/state-io/core.js` (grown to 508 lines) into a `core/context.js` sibling module per the file-split HARD limit, with no behavior change. <!-- compose-doc-append-sentinel: branch=feature/issue-1665-write-code-step pr=#1968 -->

### FEATURE: PR #1974 — feature/rules-injection-scope (2026-08-12, 67a099e31f2f8a1ce6188f9217d1da58879bc0a6, #1974)
Background: feat(rules): scope rules injection by lifecycle and audit what sessions load
Changes: #1651 Scoped rules injection by lifecycle. `rules/test.md`, `rules/docs.md`, and `rules/github-issues.md` were injected into every session regardless of what the session was doing, so their whole content sat in context even for work that would never touch a test, a doc, or an issue. All three now carry the reserved never-match glob `.on-demand-only/never-match` as the sole `paths:` entry plus an `<!-- injection: on-demand-only -->` marker comment, and the ~20 agent and skill prompts that actually need them gained an explicit Read step placed before the first step that depends on the rule. `rules/test/fixture-isolation.md` was split out with real `paths:` frontmatter so fixture-isolation guidance still auto-loads when a test file is edited. Two new pieces enforce the convention rather than leaving it to memory: `hooks/lib/rules-injection-policy.js` is a declaration-only SSOT (the reserved token, the marker regex, the 3 on-demand files, the 14 expected-unconditional files), and `bin/check-on-demand-rules.sh` + `bin/lib/check-on-demand-rules.js` check the tree-wide invariant, wired into `hooks/pre-commit` via `--staged`. The notation is deliberately two halves — the glob alone is unreadable, the marker alone does nothing — and the checker rejects either half appearing without the other. Design: `docs/architecture/claude-code/rules-injection.md`.;#1652 Added an observation-only `InstructionsLoaded` audit hook so a conditional rule silently falling back to unconditional loading is detected instead of discovered months later. `hooks/instructions-loaded-audit.js` classifies each loaded instruction into S-MISSING / S-MALFORMED / S-LEAK and publishes a receipt under `<workflowDir>/<sid>.instructions-loaded/`, with `hooks/lib/instructions-loaded-receipt.js` owning normalization and `reportRulesInjection` added to `hooks/lib/supervisor-emit.js` for the audit trail. The hook is fail-open by contract: always exit 0, always empty stdout, never able to break a session — the guard now covers module-scope `require()` failures too, not just the `main()` body. Security review of the same diff forced three corrections before merge: the audit hook was `require()`ing the contributor-editable policy file (a reviewer merely opening a session on a PR branch would have executed that PR's module body), so `hooks/lib/rules-policy-reader.js` now holds the policy-as-data reader used by the checker, the hook, and the ownership test alike; and `toRulesKey()` matched any path containing a `rules/` segment, which downgraded the `out-of-root:<digest>` redaction to a readable key for paths like `~/.ssh/rules/id_rsa.md` — it is now anchored to the five real rules roots. <!-- compose-doc-append-sentinel: branch=feature/rules-injection-scope pr=#1974 -->

### FEATURE: PR #1991 — feature/1988-reporter-model-named-you-are (2026-08-13, 1c478c063d7bc34cf0476d3499826104deccb545, #1991)
Background: fix(issue-create): extract reporter model from named-less self-report form (#1988)
Changes: fix(issue-create): extract reporter model from named-less self-report form "You are powered by the model <name>." — extended MODEL_NAME_RE in hooks/lib/model-match.js, updated skills/issue-create/SKILL.md docs, added tests (feature-1611-model-match.sh, fix-1579-reporter-model-keyword-scan.sh) <!-- compose-doc-append-sentinel: branch=feature/1988-reporter-model-named-you-are pr=#1991 -->

### FEATURE: PR #2023 — feature/1894-edit-pre-commit (2026-08-16, e8a34cd176b3d4c128b3da2eddc73d307837d82a, #2023)
Background: feat: block comment blocks over threshold at edit time and commit tim...
Changes: Added a gate that blocks comment blocks that grow past the configured threshold, both at Edit time (`hooks/block-comment-block-size.js`) and at commit time (`bin/review-comment-block-size` via pre-commit). Renamed the old warn-only settings (`COMMENT_BLOCK_WARN_LINES`/`COMMENT_BLOCK_WARN`) to `COMMENT_BLOCK_MAX_LINES`/`COMMENT_BLOCK_ENFORCE`, and the gate cannot be bypassed via `WORKFLOW_OFF`/`WORKTREE_OFF` session markers (#1894). <!-- compose-doc-append-sentinel: branch=feature/1894-edit-pre-commit pr=#2023 -->

### FEATURE: PR #2024 — feature/1976-review-code-codex-diff (2026-08-16, 76f4318a591d48f55b61d1143db65e7f8f408fcc, #2024)
Background: fix(review-code-codex): replace flat line-cap truncation with path-pr...
Changes: fix(review-code-codex): stop truncating at a flat 5000-line cap that could review zero of the intended changes (#1976); add path-priority chunk collection with per-path budgets so committed/uncommitted/untracked changes are covered proportionally, with size, exclusion, and cross-reference safeguards;fix(review-code-codex): correct the off-by-one in CODEX_REVIEW_MAX_DIFF_LINES budget accounting so the configured line limit is honored exactly (#1750) <!-- compose-doc-append-sentinel: branch=feature/1976-review-code-codex-diff pr=#2024 -->

### FEATURE: PR #2035 — feature/2013-agent-tool-dispatch-posttooluse-c4 (2026-08-16, ec258b43d64c537561bba165ae923fc322861cc3, #2035)
Background: feat: PostToolUse auto-mark + NEXT_STEP_PAUSE v2 + stall detection (#2013 #1624 #1979 #1997)
Changes: #2013: PostToolUse auto-mark — Agent/Task/Skill dispatch now automatically records the current allowlisted step as `in_progress`, eliminating the need for manual `NEXT_STEP_PAUSE` during delegated work; `STEP_IN_FLIGHT_ALLOWLIST` (`research`, `detail`, `write_tests`, `review_tests`) and 4-hour TTL are the SSOT in `hooks/lib/step-in-flight-policy.js`;#1624: NEXT_STEP_PAUSE v2 — pause markers now carry `for_step` scope, `expires_at` TTL, and a full `audit` object; `writePauseMarker` extracts `[for=<step>]` from the sentinel reason and writes v2 format; `isPauseActive` is fail-closed on missing/expired/out-of-scope markers;#1979: UserPromptSubmit mechanism-check hook — at the next user turn after a stall, `hooks/user-prompt-submit-mechanism-check.js` detects stuck `in_progress` steps via `detectStalledSteps` and surfaces the finding as `additionalContext`; `state-absent` is intentionally excluded (non-workflow sessions are normal);#1997: Mechanism failure visibility — `detectStalledSteps` classifies four stall kinds (`state-absent`, `state-corrupt`, `in-flight-expired`, `invalid-timestamp`); `reportMechanismFailureOnce` reports each finding exactly once per session via `bin/supervisor-report` and the `.stall-reported` ledger; C4 also blocks on stalled steps <!-- compose-doc-append-sentinel: branch=feature/2013-agent-tool-dispatch-posttooluse-c4 pr=#2035 -->

### FEATURE: PR #1944 — fix/1910-worktree-start-auto-naming (2026-08-16, 781ff0ddcd2ddbf8435d3ae16f06af44808a4fd1, #1944)
Background: fix(worktree-start): auto-derive task name and branch type, remove interactive/non-interactive fallback branching
Changes: worktree-start: removed the interactive/non-interactive branch split — `/worktree-start` now always auto-derives the task name and branch type via the new `skills/worktree-start/scripts/derive-worktree-name.sh`, so the standard workflow pipeline (which never passed `--task-name`/`--branch-type`) no longer stalls on an `AskUserQuestion` prompt; the flags and the interactive fallback were removed entirely (#1910) <!-- compose-doc-append-sentinel: branch=fix/1910-worktree-start-auto-naming pr=#1944 -->

### FEATURE: PR #2050 — feature/1982-no-sanctioned-recovery-command-when (2026-08-16, a958112024e0dc86b77f7e929b9239e65dd005fc, #2050)
Background: feat(enforce-worktree): sanctioned git merge --no-edit for diverged main worktree (#1982)
Changes: feat(enforce-worktree): add git merge --no-edit origin/<branch> to isAllowedMainWorktreeCleanup — allows sanctioned divergence-recovery merge from the main worktree when the branch is 1-ahead 1-behind its upstream. C1 guard enforces upstream match (fail-closed); MERGE_ENV_PREFIX_RE + MERGE_LAUNCHER_RE reject env-var and launcher prefixes; --no-edit mandatory to prevent editor hang. Also adds "Diverged Main Worktree Recovery" section to rules/ops.md documenting the 3-option decision path (merge sanctioned / rebase+reset escalate), and updates rules/user-escalation.md and rules/worktree.md with references to the new SSOT. Closes #1982. <!-- compose-doc-append-sentinel: branch=feature/1982-no-sanctioned-recovery-command-when pr=#2050 -->

### FEATURE: PR #2038 — feature/1992-code-tests-concern-ledger (2026-08-17, 43577152e4213d64cdbad6d5f009546c30ce74df, #2038)
Background: feat(concern-ledger): persist review findings across rounds and finalize on non-convergence
Changes: Concern ledger (#1992): introduced a schema-v2 concern ledger (`bin/lib/concern-ledger.sh` + core/finalize/parse/reduce/render modules) so code and test review findings keep a persistent identity across review rounds instead of being renumbered or silently dropped.;Non-convergence finalize (#1996): extended the ledger to detect non-convergence, finalize deterministically, and write unresolved concerns to a machine-readable artifact. <!-- compose-doc-append-sentinel: branch=feature/1992-code-tests-concern-ledger pr=#2038 -->

### FEATURE: PR #2055 — feature/1532-get-config-var-bash-installer (2026-08-17, 42ef5af0ed67dc117c6993aaa4f69a84a3321a01, #2055)
Background: fix(bin): detect node misinvocation in five PATH-exposed bash entry p...
Changes: #1532: Five PATH-exposed extensionless bash entry points (`bin/get-config-var`, `bin/confirm-off`, `bin/resolve-session-id`, `bin/resolve-worktree-path`, `bin/is-github-dotcom-remote`) used to collapse into a bare `SyntaxError` with rc=1 and empty stdout when launched by `node` instead of `bash`, which callers could not distinguish from "the variable was simply unset" — a fail-open reading. Each script now carries an identical polyglot envelope (12 lines after the shebang, 2 lines at EOF) that is simultaneously a bash no-op and valid JavaScript: under `node` it writes a one-line diagnostic naming the correct re-run command to stderr and exits 70, and the bash body is byte-for-byte unchanged. The diagnostic uses `require("fs").writeSync(2, ...)` with bounded EAGAIN retry and sets `process.exitCode = 70` rather than calling `process.exit()`, so the exit code survives even when the write itself fails. Scope was deliberately capped at the five highest-traffic commands rather than all ~145 `bin/` scripts; the permanent syntax reservation this imposes (no block-comment terminator anywhere in those five bodies) and the maintenance contract for editing or extending the set are documented in `docs/agents-repo-dev.md`. The in-file comment is deliberately two lines, stating only what a maintainer standing at that site needs — do not break this, and never write a block-comment terminator here; the mechanism itself is explained in exactly one place, because restating it across five byte-identical files made every revision cost five edits (CPR-SSOT).;#1967: Check #9 of `tests/feature-confirm-flags-static.sh` asserted a literal `get-config-var` string in the installer scripts, but that wiring had already moved to `install/path-exposed-commands.txt`, leaving the check permanently stale. The assertion now delegates to the list file as its single source of truth, with a negative control (`t8b`) proving the pin actually fails when the entry is removed, duplicated, or the list is missing. Seven further assertions in the same file were found to be long-standing RED for unrelated reasons (stale literal checks and stale prose pins); rather than deleting them, an `EXPECTED_FAILURES` ledger now classifies each run as XFAIL / FAIL / XPASS / STALE-LEDGER and exits non-zero only on genuinely new failures, so the suite becomes a working signal again while the seven stay tracked in #2054. Fixing one of them without removing its ledger entry is deliberately an XPASS failure, which is what stops the ledger from rotting. <!-- compose-doc-append-sentinel: branch=feature/1532-get-config-var-bash-installer pr=#2055 -->

### FEATURE: PR #2066 — feature/1836-tests-run-all-sh (2026-08-18, a5ac80e3d7d7df79575bc7fcc4e9098ab12c3cbe, #2066)
Background: feat: stop run-all.sh self-inclusion, add calibrated parallel dispatch (#1836, #1832)
Changes: fix(#1836): tests/run-all.sh --all no longer recursively re-launches itself from inside a suite run (self-inclusion guard, `is_self`).;feat(#1832): tests/run-all.sh gained calibrated parallel dispatch (`-j`/`RUN_ALL_JOBS`, corpus-based auto calibration, deterministic reap/fallback), cutting full-suite wall time. <!-- compose-doc-append-sentinel: branch=feature/1836-tests-run-all-sh pr=#2066 -->

### FEATURE: PR #2062 — feature/2037-rules-2-rules-skill-subagent (2026-08-18, 633935d270ba2b4242d586b4dd67f335fe7c5e54, #2062)
Background: feature/2037 rules 2 rules skill subagent
Changes: #2037: Second-stage rules de-injection — converted 7 remaining unconditional rules (branch, coding, mid-workflow-findings, ops, worktree de-injected to on-demand; stop-guard-exemptions, supervisor-reporting, workflow-off minimized in place) to cut per-subagent-launch re-injection, added a path-containment helper to the on-demand-rules checker, split supervisor-reporting into a minimized rule plus a new skill, and closed 10 Codex-flagged test-coverage gaps. <!-- compose-doc-append-sentinel: branch=feature/2037-rules-2-rules-skill-subagent pr=#2062 -->
