# History (agents)

## Archived
- [2026](1790000180-147063-pr2358-history-staging/2026.md) — 104 entries

### FEATURE: PR #2272 — feature/2262-prompt-cmd-prompt-bash-node (2026-09-09, 147f60f0c49029df7ee08b433526a83904a3c58f, #2272)
Background: feat(#2262): direct prompt-authored command lines through bash/node execution position
Changes: fix(#2262): the permission engine only auto-approves a Bash call when its execution-position token is not an unexpanded shell variable, so a prompt-authored command line like `"$AGENTS_CONFIG_DIR/bin/foo"` asked for approval while `bash "$AGENTS_CONFIG_DIR/bin/foo"` did not; corrected the affected SKILL.md / shared-doc command examples to put `bash`/`node` in execution position and the config-relative entry path in argument position, added the A' convention (two accepted forms: an executable command line vs. a location-only reference without the `$AGENTS_CONFIG_DIR/` prefix) to `docs/architecture/claude-code/settings.md`, and updated `tests/prompt-bash-node-calling-convention/` to pin the convention as a prefix-only check independent of surrounding prose <!-- compose-doc-append-sentinel: branch=feature/2262-prompt-cmd-prompt-bash-node pr=#2272 -->

### FEATURE: PR #2274 — feature/2270-session-id-ssot-llm-env (2026-09-12, aefd1ed579cbcd4a7f90d68349a86ab3415d8484, #2274)
Background: feat(#2270): unify session-id resolution to a single-source-of-truth ...
Changes: Unified session-id resolution across bridge callers into a single source-of-truth resolver (Option C), replacing scattered per-caller SESSION_ID/CLAUDE_SESSION_ID/CLAUDE_CODE_SESSION_ID lookups with one shared fail-closed 4-tier SUPPLY-only chain (#2270);Fixed issue-close-write-outcome silently skipping the outcome JSON write when the session id was unresolvable in a subagent context (#658);Fixed review-tests RT-1 select-staged-files.sh not exporting SESSION_ID, which caused resolve-worktree-path to return an empty string (#1759);Fixed resolve-worktree-path.js's session_worktree fallback being unreachable when state.cwd is absent (#1596) <!-- compose-doc-append-sentinel: branch=feature/2270-session-id-ssot-llm-env pr=#2274 -->

### FEATURE: PR #2275 — feature/746-review-plan-codex-codex-cli (2026-09-12, a5c441f3277f70ca637fe5d0df492808fcc33f62, #2275)
Background: fix(#746): pin codex sandbox_mode/approval_policy via -c overrides for codex-cli 0.153.4
Changes: Fixed #746: bin/review-plan-codex's codex-cli 0.153.4 flag incompatibility (the removed `--sandbox read-only` / `--full-auto` flags caused every codex review to fall back) by replacing them with unconditional `-c approval_policy="never" -c sandbox_mode="read-only"` config overrides; also added a missing-value guard to `--repo-root` (matching its sibling flags) and, as a narrow partial fix for #1742, widened `run-codex-review-loop.sh`'s `arm_terminal_guard()` to treat exit 3 as terminal. <!-- compose-doc-append-sentinel: branch=feature/746-review-plan-codex-codex-cli pr=#2275 -->

### FEATURE: PR #2281 — docs/2278-harden-plan-lang-docs-lang (2026-09-13, 7f57e2654110c3db927cd64e4b674cbbf9adbac6, #2281)
Background: fix(#2278): harden PLAN_LANG/DOCS_LANG enforcement against non-compli...
Changes: FEATURE (#2278): Hardened PLAN_LANG / DOCS_LANG enforcement against non-compliant models. Background: a general-purpose planner that ignored the injected PLAN_LANG directive could write a CJK detail plan, and the post-hoc `check-plan-lang.js` / `check-worktree-notes-lang.js` hooks only complained after the bytes were on disk; `stop-confirm-plan-guard.js` Layer 2 also never ran without a turn marker, so a CONFIRM_DETAIL with no follow-up Skill slipped through. Changes: added PreToolUse gates `hooks/gate-plan-lang.js` and `hooks/gate-worktree-notes-lang.js` (registered in `settings.json`, fail-open, per-fragment lint before the write lands) on shared libs `hooks/lib/pretool-lang-gate.js` and `hooks/lib/plan-artifact-lang.js`; `check-plan-lang.js` and `check-worktree-notes-lang.js` now delegate to those libs (`safeIsPrivateRepo` moved into `hooks/lib/is-private-repo.js`); Layer 2 of `stop-confirm-plan-guard.js` is marker-independent and re-lints the confirmed artifact against PLAN_LANG (`[confirm-plan] Layer 2/plan-lang:` / `Layer 2/follow-up:` prefixes); `subagent-start.js` injects PLAN_LANG for every agent type; `VERBOSE_PROMPT_TEXT` names the five observed failure modes. Follow-up candidate: `block-comment-block-size.js` `applyOne` should reuse `applyEdits`. <!-- compose-doc-append-sentinel: branch=docs/2278-harden-plan-lang-docs-lang pr=#2281 -->

### FEATURE: PR #2282 — feature/1773-session-sync-init-sh-ps1 (2026-09-13, eebeea1d5accff3d1632fc3865df0c68af35d8de, #2282)
Background: fix(#1773,#1757): harden session-sync-init against untrusted remote URL/git-root migration; fix session-sync.ps1 pull error handling
Changes: fix(#1773): session-sync-init.sh/.ps1 accepted an unvalidated remote URL/`--claude-dir` and migrated the legacy git root without checking it was the right repository — both RCE and silent-data-loss risk. Added a remote-URL allowlist, two-stage realpath containment (`$CLAUDE_DIR` under `$HOME`, `$PROJECTS_DIR` under the resolved `$CLAUDE_DIR`), a provenance gate (`--expected-origin`/`-ExpectedOrigin` > allowlisted remote URL > refuse) evaluated before any filesystem write, and a transactional (stage/move/promote + rollback) migration replacing the prior unconditional `rm -rf`. A round-2 security review of this same fix found and closed two further gaps in the new code: the colliding-destination guard did not reject an origin-less destination repo (silently promoted over it), and the post-migration restore re-rooted tracked paths one level too deep by using the wrong git work-tree. Both are fixed in this branch; test fixtures updated to match the corrected contract.;fix(#1757): `bin/session-sync.ps1`'s pull step aborted the whole sync on any git stderr (missing `$ErrorActionPreference`/`$LASTEXITCODE` handling around the pull), mirrored from the already-correct push-block pattern. <!-- compose-doc-append-sentinel: branch=feature/1773-session-sync-init-sh-ps1 pr=#2282 -->

### FEATURE: PR #2285 — feature/2075-write-tests-tests-2075 (2026-09-13, 4bb8c5f9a421e75f22beb1750c16f24e7cdcca75, #2285)
Background: feat(#2075): add append-vs-new destination valve for write-tests
Changes: Implemented an append-vs-new destination valve for write-tests: `bin/find-tests-for-source.sh`, `bin/lib/test-route-destination.sh`, and `bin/lib/test-dup-group.sh` route a newly written test case to an existing test file (append) or a new file, based on source-file/tag/duplicate-group heuristics, replacing the ad hoc per-call heuristics that caused issue #2075's undifferentiated test-file inflow; `write-tests`/`review-tests` skills, `skills/_shared/test-design.md` (with new `test-design/append-vs-new.md`), and `install/settings-allow-commands.txt` were updated accordingly, with `--test-file`/staged-filename arguments treated defensively against path-traversal and shell-metacharacter input (#2075) <!-- compose-doc-append-sentinel: branch=feature/2075-write-tests-tests-2075 pr=#2285 -->

### FEATURE: PR #2298 — fix/2279-fix-resume-session-skill-resume (2026-09-14, 99e696b60b6d72e61850244c613f7b2b34363eee, #2298)
Background: fix(#2279,#2213): stop WI-10 lookahead from poisoning /resume-session's own heir
Changes: fix(#2279): `hooks/workflow-state/inheritance/adopt.js`'s adoption gate now reads `isEffectivelyPendingStep` (new SSOT in `hooks/workflow-state/lifecycle.js`), so a Skill-path `/resume-session --from` on a fresh heir session no longer gets tainted by the WI-10 lookahead mark; `--list`/`--from` adoptability was aligned, `skills/resume-session/SKILL.md`'s procedure order was corrected, and `bin/workflow/handoff-append`'s UX was cleaned up.;fix(#2213): the Stop-guard C4 premature-stop check's pre-workflow-init exemption is now evaluated per finding via `isLookaheadOnlyInFlight` instead of once per session, so a genuinely-stalled step in a pre-init session is no longer over-suppressed alongside the WI-10 lookahead-only step. <!-- compose-doc-append-sentinel: branch=fix/2279-fix-resume-session-skill-resume pr=#2298 -->

### FEATURE: PR #2256 — feature/2256-supervisor-user-verified-sentinel-shift (2026-09-15, 7125989d)
Background: The shift-left of the EM Supervisor pre-merge audit (PR #2253) moved the final decision point to just before USER_VERIFIED. PR #2256 hardens that shift by closing a set of identity-mismatch, bypass, and guard gaps discovered during security review of the implementation. The changes address the session-ID duality (CC UUID effectiveSid vs. workflow-session wsid), a post-sentinel TR5 re-arm gap, and several fail-open bypass conditions in the override / merge-gate / terminal-guard paths.
Changes: C8/C21: supervisor-audit verdict write must pass --session-id <effective-state-sid>; auto-resolve targeted the wsid store (different from the effective state store), causing identity-mismatch rejections on compare-and-set — agents/supervisor-audit.md updated to mandate explicit --session-id.;C12: dual-ID wsid/effectiveSid duality documented in docs/architecture/claude-code/session-id-resolution.md.;C22: user-verified-audit.js override guard now blocks release when a later TR6 BLOCK exists; a TR5 override speaks only to the TR5 run, never to an independent later BLOCK.;C33: workflow-gate.js blocks a merge command co-appearing in the same runCommands as <<WORKFLOW_USER_VERIFIED>> — the merge gate never runs once approve() exits, so the leading-merge form bypassed the freshness backstop.;C36: supervisor-guard.js Phase B now surfaces audit_verdict_summary in preference over audit_cause in the block reason; audit_verdict_summary added to the audit fields table in claude-code.md.;C7: Lifecycle text in claude-code.md updated: 'next stage boundary can re-arm' → 'next arm trigger can re-arm'; terminology aligned with TR1–TR6 nomenclature.;C16: prestaged-pass.sh archive-or-halt: round-1 fresh-cycle start now uses mktemp+mv and exits 4 on failure instead of '|| true', preventing silent reduce into a stale terminal ledger.;C11/C30: run-codex-review-loop.sh (review-code-security and review-plan-security) arm_terminal_guard now breaks on mktemp failure instead of writing to a predictable path.;C4: exit 7 (FINALIZE_FAILED) added to the terminal guard case in review-tests run-codex-review-loop.sh; comment block compressed to stay within the 10-line HARD limit.

### FEATURE: PR #2313 — feature/2256-supervisor-user-verified-sentinel-shift (2026-09-15, 2696d3eea7307ac3b8cf6cdcff97bbc96cbc6cfa, #2313)
Background: feat(supervisor): shift-left pre-merge audit before USER_VERIFIED (#2256, #2276)
Changes: Shift-left the EM Supervisor pre-merge audit gate ahead of the USER_VERIFIED sentinel so user-approved code is no longer amended by post-approval supervisor findings; the final decision point stays immediately before USER_VERIFIED (the single point WE-7 and WE-8 both traverse), and S5 systemic-risk moves to write_code completion. (#2256);Cap the review-code-security round (CAP=2 / MAX_EXTENSIONS=1) by migrating it to the shared codex-default / security-scanner fallback loop, matching review-plan-security; closes the only previously uncapped review. (#2276) <!-- compose-doc-append-sentinel: branch=feature/2256-supervisor-user-verified-sentinel-shift pr=#2313 -->

### FEATURE: feat(#2284): add wait-cc-exit guard to install scripts (2026-09-17, 47d482cf)
Background: Install scripts (claude update / codex update) could corrupt the CC binary when Claude Code was running during an install or update operation, because the installer did not check whether CC was active before proceeding.
Changes: shared install/lib/wait-cc-exit.{sh,ps1} helpers poll for a live Claude Code process (3 s × 10 polls = 30 s max) before running claude update or codex update; all 6 installers (claude-code.sh, codex.sh, dotfileslink.sh on Linux; claude-code.ps1, codex.ps1, dotfileslink.ps1 on Windows) updated; testability via WAIT_CC_POLL_INTERVAL / WAIT_CC_MAX_POLLS / MOCK_PGREP_MODE (bash) / WAIT_CC_PROCESS_OVERRIDE (PS) overrides

### FEATURE: PR #2321 — feature/2307-forge (2026-09-19, 2f8c89992fa30fc142c17addaf654836b4f534bb, #2321)
Background: feat(#2307): forge abstraction layer — 2-axis router (codehost + tracker)
Changes: feat(#2307): forge abstraction layer — 2-axis router (codehost + tracker); forge-router.js descriptor registry, github/gitlab/stub forge modules, glab-flag-vocab.js, detectForgeType in parse-remote-url.js, scan-outbound.js CWD privacy gate for glab (C9/C10/C11/C12 security fixes) <!-- compose-doc-append-sentinel: branch=feature/2307-forge pr=#2321 -->

### FEATURE: PR #2320 — feature/2223-nfr-planner-writer-reviewer (2026-09-19, c99c866413f603a49fefe392b66dc6790385a672, #2320)
Background: feat(#2223): add project NFR injection and complexity-judge unification
Changes: feat(#2223): Add mechanism for project-specific NFR to be referenced as acceptance and severity calibration criteria across all planner, writer, and reviewer agents. Appends severity-calibration instruction to codex_core_project_nfr_block() (Scope 1); adds bin/project-nfr-block CLI and agents/lib/nfr-severity-calibration.md to distribute the NFR block to 5 CC fallback reviewers plus outline-planner, detail-planner, write-tests, and write-code (Scope 2/3); adds complexity-judge subagent and bin/workflow/normalize-judge-signals CLI unifying 4 complexity-dispatch sites (Scope 4). <!-- compose-doc-append-sentinel: branch=feature/2223-nfr-planner-writer-reviewer pr=#2320 -->

### FEATURE: PR #2322 — feature/2319-vs-code-extension-null-cwd (2026-09-19, 7be6b14b959157d9098c198b720cf237c051771b, #2322)
Background: fix(#2319,#2316): null cwd fallback + repo-dir-guard INDETERMINATE fix
Changes: #2319 (BUGFIX): VS Code extension sends `toolInput.cwd` as null. The workflow-gate freshness check and its sibling hooks (show-user-verified-context, supervisor-check, user-verified-audit) now resolve cwd through a shared `resolveInputCwd` (process.cwd() fallback, `hooks/lib/resolve-cwd.js`) instead of infinite-arming the #2256 TR5 gate or fail-closed stopping. The oversized `hooks/workflow-gate.js` was split under the 500-line HARD limit by extracting `hooks/workflow-gate/incomplete-steps-message.js` (behavior-preserving).;#2316 (BUGFIX): `next-step` aborted at the worktree-end → session-close boundary when the worktree directory had been deleted (repo-dir-guard INDETERMINATE verdict). It now fails open for a self-call so session-close proceeds, while an explicit cross-session override still fails fast. <!-- compose-doc-append-sentinel: branch=feature/2319-vs-code-extension-null-cwd pr=#2322 -->

### FEATURE: PR #2312 — feature/1987-migration-code-blocks-19 (2026-09-20, bad454a7dd84afbb4adbf9623a0655b793dff243, #2312)
Background: feat(#1987): 一時 migration code blocks の断捨離（クラスタ A/B/D/E/F、15 ブロック削除 + リンタ追加）
Changes: Removed 15 temporary migration code blocks across clusters A/B/D/E/F; added check-migration-blocks.sh linter and migration-blocks-audit.yml GitHub Actions workflow to prevent future accumulation. Fixes null-cwd supervisor-audit arm loop (hookCwd fallback to process.cwd() per #2319). <!-- compose-doc-append-sentinel: branch=feature/1987-migration-code-blocks-19 pr=#2312 -->

### FEATURE: PR #2330 — feature/2323-agents-config-dir-stale-null (2026-09-20, 091fd0ad0bc46a150c4a347f6cd36d9a633780d6, #2330)
Background: fix(#2323): null freshness_key infinite-arm loop when AGENTS_CONFIG_DIR is stale
Changes: BUGFIX #2323 (2026-09-20): fix(#2323): null freshness_key infinite-arm loop when AGENTS_CONFIG_DIR is stale | Background: When AGENTS_CONFIG_DIR has no protected-branch ancestor (stale/detached/shallow clone), computeFreshnessKey returns freshness_key:null and input_version:null. The TR5 audit gate's Stage 2 then hits `if (!currentFk) return arm(...)` on every WE-8 sentinel emission, creating an infinite arm/block loop that the user cannot escape regardless of recovery attempts. | Changes: Added NON_BLOCK_TERMINAL_VERDICTS=["CONTINUE"] explicit allow-list constant to supervisor-state-schema.js (SSOT; not derived from AUDIT_VERDICT_VALUES to prevent fail-open on WARN/malformed). Added selfRecovering predicate to user-verified-audit.js: when the last TR5 terminal run is CONTINUE, no later BLOCK exists, and input_version is null (code-side uncomputable), Stage 2 approves the sentinel instead of re-arming. Stage 1 receives the symmetric guard (CPR-ORTH; dead code at runtime, documented). supervisor-check.js deny-on-null and deny-on-mismatch are unchanged (fail-closed). Added 28-case self-recovery test section (null-freshness-short-circuit.sh, 7 fail-before-fix) and 8-case computeInputVersion characterization test (null-collapse-characterization.sh, all pass). | Test gap: No TL3 test for a real stale AGENTS_CONFIG_DIR against a live origin; TL2 uses a fixture repo lacking a merge-base ancestor as an equivalent null trigger. <!-- compose-doc-append-sentinel: branch=feature/2323-agents-config-dir-stale-null pr=#2330 -->
Test gap: No TL3 test for a real stale AGENTS_CONFIG_DIR against a live origin; TL2 uses a fixture repo lacking a merge-base ancestor as an equivalent null trigger.

### FEATURE: #2228 #2338 #2339: Normalize plan-artifact heading + section order; plan-schema SSOT; sweep tool (2026-09-21)
Background: #2228: outline.md section order — Adopted approach must come first; Class members removed from outline/detail and consolidated to intent.md per CPR-SSOT. #2338: H2 headings must be canonical English regardless of PLAN_LANG. #2339: no tool existed to normalize existing artifacts.
Changes: (1) New hooks/lib/plan-schema.js — SSOT for CANONICAL_SECTIONS (per artifact type), canonicalizeHeading(), firstBodySection(), CLI flags. (2) lint-plan-lang.js/plan-artifact-lang.js/check-plan-lang.js/gate-plan-lang.js/stop-confirm-plan-guard.js — heading enforcement fires regardless of PLAN_LANG. (3) assemble-mandatory.sh — mandatory 3-to-2; Class members removed; Adopted approach first-section hard check. (4) bin/sweep-plan-headings.js — new CLI; dry-run default, --fix rewrites + reorders outline sections. (5) bin/review-plan-codex — Class members from intent.md via --class-members; CONTEXT_CONTENT delimiter sanitization across all 4 format branches. (6) CPR-E2E: outline-reviewer, detail-reviewer, detail-planner, outline-planner, supervisor-audit, run-codex-review-loop, path-parse.sh updated.
### FEATURE: PR #2341 — feature/2326-rtk-bash (2026-09-21, f58f0702b7e7640e5c30c0779a5d220398d570fd, #2341)
Background: feat(#2326): introduce RTK transparent output-compression hook
Changes: feat(#2326): introduce RTK (Rust Token Killer) transparent output-compression proxy for Bash commands. Adds `hooks/rtk-rewrite.js` (PreToolUse, Bash) with four passthrough guards (G-a isAgentsEmit, G-b isMachineReadable, G-c isComposite, G-d isShellBuiltin); cross-platform installers (`install/linux/rtk.sh`, `install/win/rtk.ps1`) and config-deploy helper (`install/lib/rtk-config-deploy.js`); feature tests under `tests/feature-2326-rtk-rewrite/`. RTK is default OFF (`RTK=off`); set `RTK=on` in `.env` to enable. <!-- compose-doc-append-sentinel: branch=feature/2326-rtk-bash pr=#2341 -->

### FEATURE: PR #2348 — feature/2080-tests-lib-harness-sh (2026-09-21, a2e025bd0e471d52ff13aa9d93e19f9c2df72537, #2348)
Background: feat(#2080): add shared test harness tests/lib/harness.sh
Changes: feat(#2080): add shared test harness tests/lib/harness.sh — 773/913 top-level tests duplicated pass()/fail()/skip() locally; tests/lib/harness.sh provides a single shared implementation (pass, fail, skip, assert_eq, case_begin, case_end, make_tmp, run_with_timeout, harness_isolate, harness_git_init, np) with a re-entry guard so multi-source fragments work safely; bin/check-test-frontmatter.sh extended with harness-source check (pre-commit enforced for new top-level tests); verification test tests/feature-2080-shared-harness.sh (T_PASS=59) <!-- compose-doc-append-sentinel: branch=feature/2080-tests-lib-harness-sh pr=#2348 -->

### FEATURE: PR #2353 — docs/2340-feat-update-docs-readme-md (2026-09-21, 2796503300dfbcebe2a31ac268afe57b5a8d18a6, #2353)
Background: feat(docs): enforce README conciseness rules and line limits (#2340)
Changes: Add review_docs workflow step with bin/review-doc-gates, bin/review-doc-heading-order, bin/review-doc-size CLIs; skills/review-docs/SKILL.md; rules/docs/readme.md README section order rules; hooks/workflow-gate/review-docs-checker.js; tests/docs-2340-feat-update-docs-readme-md/ test suite. Closes #2340. <!-- compose-doc-append-sentinel: branch=docs/2340-feat-update-docs-readme-md pr=#2353 -->

### FEATURE: PR #2358 — feature/2347-rtk-config-deploy-rtk (2026-09-21, 4684483853f603bed302535d86c9ed85644a2b2e, #2358)
Background: fix(#2347): delegate RTK config validation to rtk binary, drop hardco...
Changes: BUGFIX #2347: Delegate RTK config management to the RTK binary instead of a hardcoded schema. Background: `install/lib/rtk-config-deploy.js` hardcoded an RTK config schema that was a second source of truth for a contract RTK itself owns (CPR-SSOT); it drifted from RTK's real contract, and RTK v0.48.0 rejects the deployed config. Changes: Deleted `install/lib/rtk-config-deploy.js`; both installers (`install/win/rtk.ps1`, `install/linux/rtk.sh`) now delegate config management to the RTK binary via `rtk config` / `rtk config --create` (RTK owns its own schema, so no drift). Replaced `tests/feature-2326-rtk-rewrite/test-config-toml-idempotent.sh` with `tests/TL3-installer-rtk-config-create-contract.sh`, a real-binary contract test for the `rtk config` seam (valid/idempotent, unparseable-TOML repair, obsolete-schema migration, absent/empty-file → built-in defaults; skipped on Windows and when the rtk binary is absent). Updated `docs/architecture/claude-code/settings.md`. Test gap: no real-binary contract test pinned the installer's config seam to RTK's actual schema, so hardcoded-schema drift went uncaught until RTK v0.48.0 rejected it. <!-- compose-doc-append-sentinel: branch=feature/2347-rtk-config-deploy-rtk pr=#2358 -->

### FEATURE: PR #2364 — feature/2356-rtk-rewrite-js-rtk-hook (2026-09-22, edb782743b03edfcdebf7627c1db6f9876059c0c, #2364)
Background: feat(#2356,#2345,#2346,#2350): delegate rtk-rewrite to rtk hook claude, add RTK_AUDIT guard log
Changes: #2356: rtk-rewrite.js was bypassing RTK's canonical audit path (`rtk hook claude`). Rewrote `decide()` to delegate to `rtk hook claude` via `spawnSync` so RTK owns the rewrite verdict and all audit records flow through the official path. Added `hooks/lib/rtk-guard-audit.js` (JSONL log with file-lock rotation) and `RTK_AUDIT` toggle so agents-owned guard rejections are recorded independently of RTK's own audit. Fixes #2356.;#2345: `isShellBuiltin` was missing `alias` and `typeset` and did not detect `bash -c` via `basename` comparison. Extended the guard to cover all three forms. Fixes #2345.;#2346: `isAgentsEmit` was not matching when the interpreter was an absolute path (e.g. `/usr/bin/node /path/to/agents-script`). Added absolute-path interpreter detection in `checkTokens`. Fixes #2346.;#2350: RTK guards were not peeling `env`-prefixed commands (`/usr/bin/env VAR=1 git ...`), allowing the inner command to bypass guards when headed by the `env` binary. Added `isEnvHead()` helper and peeling logic to all five guards. Fixes #2350. <!-- compose-doc-append-sentinel: branch=feature/2356-rtk-rewrite-js-rtk-hook pr=#2364 -->

### FEATURE: PR #2366 — feature/2357-review-code-security-terminal-guard (2026-09-22, ff0c74da8a0915048edc30ccbe3dda442244693c, #2366)
Background: fix(#2357): block exit-6 terminal guard bypass with exit 9 gate
Changes: fix(#2357): block fingerprint bypass after exit 6 with new exit 9 gate. Added PREV_RC==6 + missing accept marker → exit 9 guard to all three review-loop wrappers (review-code-security, review-tests, review-plan-security). Introduced accept marker mechanism (session-scoped file) for conscious-accept path. Updated 3 SKILL.md files and 2 shared exit-code docs. TDD: new test fix-2357 (7 cases) + updates to fix-1361 and feature-2068. <!-- compose-doc-append-sentinel: branch=feature/2357-review-code-security-terminal-guard pr=#2366 -->

### FEATURE: PR #2367 — feature/2344-review-code-security-review-tests (2026-09-22, 4c932e71f4d5d1ce101c724caa502325702e2022, #2367)
Background: fix(#2344,#2185): concern-ledger rejected state + DISCRIM carrier + CTX_CONCERNS_LOG wiring
Changes: fix(#2344): Replaced the ROUND>=2 guard with a `have_prior_entries` check and simultaneously relaxed the NEEDS_LEDGER_PASS gate in run-codex-review-loop. Added per-entry FP_SCANNER_REQUIRED gate and round-level `_rnd_scanner_complete` check to reduce.sh so security-code rounds stay fail-closed when the scanner has not completed. Prior concerns now resolve correctly on re-review after fixes.;feat(#2185): Added `rejected` terminal STATE, `cl_reject` CLI, DISCRIM-keyed concern-carrier (`*-concern-carrier.md`), and `render-concerns-log` CLI to concern-ledger. Symmetrically wired CTX_CONCERNS_LOG auto-generation into all five run-codex-review-loop wrappers and updated SKILL.md files so the codex reviewer receives prior rejection history across cleanup_ledger and cl_begin_cycle boundaries. <!-- compose-doc-append-sentinel: branch=feature/2344-review-code-security-review-tests pr=#2367 -->

### FEATURE: PR #2365 — feature/2360-tr5-audit-gate-artifact-side (2026-09-22, f79a2e98876ece5efd4f2f62af4bb96c60378de0, #2365)
Background: fix(#2360): TR5 audit gate infinite re-arm loop on artifact-side null sessions
Changes: fix(#2360): TR5 audit gate re-armed indefinitely on artifact-side null sessions (freshness_key==null with input_version non-null — plan artifact file missing). Added filterNullKeySubChecks to exclude recurrence-patterns from arm sets when freshness_key is null, and inputVersionMatches to approve via selfRecovering when trigger_input_keys.TR5 matches the current input_version. Symmetric fix applied to audit-arm.js buildJudgmentSet (CPR-ORTH). <!-- compose-doc-append-sentinel: branch=feature/2360-tr5-audit-gate-artifact-side pr=#2365 -->

### FEATURE: PR #2368 — docs/2308-gitlab-forge-codehost-cli-wip (2026-09-22, 47043ca76d78e5c62dc39b9cf0f01bfa55ef96f8, #2368)
Background: feat(#2308): add GitLab forge support with detect-forge-type CLI and ...
Changes: feat(#2308): added detect-forge-type CLI and FORGE_GITLAB_HOST env var for self-hosted GitLab auto-detection; implemented codehostGitlab (isPrivateRepo/shouldScanAsPublicTarget/WIP label management) and wip-state/gitlab.sh for MR WIP state; added corresponding tests <!-- compose-doc-append-sentinel: branch=docs/2308-gitlab-forge-codehost-cli-wip pr=#2368 -->

### FEATURE: PR #2371 — feature/2081-retire-refcount-gc-orphan-ps1 (2026-09-22, 8325688ae26d06fddcf1fa49c9eadf7161476bc8, #2371)
Background: fix(#2081,#1864): retire predicate case-unit refcount GC, orphan case removal, and .ps1/.py scan extension
Changes: #2081: retire predicate migrated to case-unit refcount GC; orphan case physical removal (trp_remove_orphan_cases) added; scan extension to .ps1/.py in bin/audit-tests.sh and bin/audit-tests-common.sh. PR #2371 squash-merged 2026-09-22.;#1864: .Tests.ps1 and test_*.py scan gap closed; bin/audit-tests.sh and bin/audit-tests-common.sh now include these extensions in survival audit scope. PR #2371 squash-merged 2026-09-22. <!-- compose-doc-append-sentinel: branch=feature/2081-retire-refcount-gc-orphan-ps1 pr=#2371 -->

### FEATURE: PR #2376 — feature/sweep-issues-20260921141913 (2026-09-23, c8d469f27a5bf4115afce0ab1b1a68bfa3d0a49d, #2376)
Background: feat(#2363): sweep-issues all-bands loop, shared band-loop library, snapshot mechanism
Changes: feat(#2363): sweep-issues all-bands loop — added --all-bands/--max-bands flags, bin/lib/sweep-band-loop.sh SSOT library, snapshot mechanism (single gh issue list fetch shared across all bands), CI contract fields (band_index="all", bands_swept, total_bands) in summary.sh, and 32 tests across two new test files <!-- compose-doc-append-sentinel: branch=feature/sweep-issues-20260921141913 pr=#2376 -->

### FEATURE: PR #2377 — feature/929-agents (2026-09-23, 16be5629499af2c2a51628b230c73179f90b4c77, #2377)
Background: feat(#929): supervisor codex engine + C1/C2/C3 label rename
Changes: #929: Retire C1 critic (`bin/supervisor-review-codex` deleted); introduce shared codex engine `bin/supervisor-findings-codex` (alert + audit) with STATUS-channel output and `hooks/lib/supervisor-codex-parse.js` C4 rejection rules; add `session_start_context.transcript_path` persistence; make audit verdict+findings an atomic CAS write; rename C1/C2/C3 trigger labels to `sentinel-hang`/`scheduled-review`/`off-proposal`. <!-- compose-doc-append-sentinel: branch=feature/929-agents pr=#2377 -->

### FEATURE: PR #2380 — feature/2370-bin-rtk-cmd-wrapper-0 (2026-09-24, 33215b354a90dd94a4e5cc8925f74c125a8461ef, #2380)
Background: feat(#2370): add bin/rtk-cmd opt-in RTK wrapper + adoption convention
Changes: #2370: Added `bin/rtk-cmd`, an opt-in wrapper that runs `exec rtk <cmd> [args...]` when RTK=on and the rtk binary is resolvable (RTK_BIN env var, validated as a regular executable file, then `command -v rtk`), and otherwise `exec <cmd>` passthrough — exit code, stdout, and stderr are propagated transparently in both paths. Fail-safe-OFF on every ambiguous config state (AGENTS_CONFIG_DIR unset, get-config-var exit 2/3/4). A cross-cutting survey (all RTK-target commands times 195 bin/ scripts) found zero adoption candidates — no existing script emits raw human-readable RTK-target output to Claude's stdout — so the wrapper ships as infrastructure plus an adoption convention (documented in docs/architecture/rtk.md) to be used when a suitable call site first appears (option 4b). Covered by TL2 (fake-binary, 19 assertions) and TL3 (real-binary, RUN_TL3-gated) tests. <!-- compose-doc-append-sentinel: branch=feature/2370-bin-rtk-cmd-wrapper-0 pr=#2380 -->

### FEATURE: PR #2382 — feature/2374-off-examiner-spof-emergency-provenance (2026-09-24, 68363f32d5efd4c689e209078835e0ac65fc4cef, #2382)
Background: feat(#2374,#2173): off-clearance examiner SPOF fix + provenance regex...
Changes: #2374: Removed examiner SPOF from `bin/request-off-clearance`. When the codex CLI is unavailable (not-found / timeout / auth-401 / network), the script now falls back to `human_approval_fallback()`. Interactive terminals (/dev/tty) prompt for a human reason and mint a clearance token; non-interactive paths record an UNAVAILABLE audit entry, emit an EMERGENCY hint, and exit 1. `classify_codex_failure()` buckets the failure class in the audit trail. `OFFCLR_APPROVAL_INPUT` added as a test seam for non-interactive environments.;#2173: Split `OFF_SKILL_INVOCATION_RE` into `WRAPPED_RE` (m-flag, any line, `<command-name>`-wrapped form) and `BARE_RE` (no m-flag, first line only). Moved both to `hooks/lib/off-emergency-provenance.js` as the SSOT. `hooks/record-off-skill-invocation.js` now imports and uses `promptInvokesOffSkill()` instead of owning a local regex.;#1660: Verified that `skills/enforce-workflow-off/SKILL.md` already emits the EMERGENCY sentinel directly without an examiner step (introduced in commit 6b6c8753). No new code changes; issue closed as already-implemented. <!-- compose-doc-append-sentinel: branch=feature/2374-off-examiner-spof-emergency-provenance pr=#2382 -->

### FEATURE: PR #2385 — feature/1937-worktree-backup-worker-gitignored-partia (2026-09-26, 620d89a5c71946362cb30bc341b819a064e6bef0, #2385)
Background: fix(#1937): worktree-backup copies gitignored dirs via conditional --directory flag
Changes: BUGFIX: worktree-backup worker fails to copy gitignored directories when dir_expand:true (#1937). Background: On Windows/Git Bash, `git ls-files --others --ignored --exclude-standard` returns individual files inside plain (non-opaque) gitignored directories instead of directory entries, so expandDir() was never called for plain dirs. Changes: Added conditional `--directory` flag to git ls-files when dir_expand:true, trailing-slash normalization to prevent double-slash paths, dir_expand:bool payload field (default false, backward-compatible), resolve-dir-expand.js to read verbose_prompt session state, EISDIR guard in worktree-copy.js copyInclude(), worktree-end WE-9 updated to resolve dir_expand and pass to both backup passes. Test gap: no test for git ls-files --directory behavior difference between plain and opaque gitignored directories on Windows. <!-- compose-doc-append-sentinel: branch=feature/1937-worktree-backup-worker-gitignored-partia pr=#2385 -->
