# History (agents)

## Archived
- [2026](history/2026.md) — 103 entries

### FEATURE: Add /issue-create skill — type:task enforcement + Projects v2 auto-attach (#246) (2026-05-15)
Background: Sessions needed a sanctioned CLI path for creating task issues that enforces type:task and attaches to Projects v2 automatically. Raw gh issue create risked wrong labels and manual project attachment.
Changes: New bin/github-issues/issue-create.sh: wraps gh issue create; unconditionally applies type:task; rejects caller-supplied type:* labels (exit 2); supports --body/--body-file (mutually exclusive); Projects v2 attach non-fatal on failure (ISSUE_CREATE_OWNER/ISSUE_CREATE_PROJECT_NUM env overrides). New skills/issue-create/SKILL.md: user-invocable skill with scope, pre-flight, procedure, label policy. Updated rules/github-issues.md: added Issue creation section. Updated CLAUDE.md: added /issue-create note under Step 1. New tests/feature-issue-create-skill.sh: 15 cases (S1-S11 behavior + D1-D4 docs).

### FEATURE: Switch VS Code detection in show-plan-link.js to TERM_PROGRAM (2026-05-15, pending)
Background: CLAUDE_CODE_ENTRYPOINT is a Claude Code internal variable not guaranteed by Anthropic public API. TERM_PROGRAM=vscode is the POSIX-standard signal set by VS Code's integrated terminal. closes #290.
Changes: hooks/show-plan-link.js: gate on TERM_PROGRAM === 'vscode' instead of CLAUDE_CODE_ENTRYPOINT === 'claude-vscode'; updated NOTE comment. tests/feature-show-plan-link.sh: T16 driven by TERM_PROGRAM=vscode; added T20 (non-VS Code negative), T21 (legacy var regression guard), T22 (fail-open with failing code shim); default unset extended. docs/architecture/claude-code/settings.md: updated description.

### FEATURE: backfill-commit-comments.sh: J-1 + git-log fallback + --canary flag (#300) (2026-05-15, pending)
Background: Closed issues lacking J-1/J-2 comments (issues closed via web UI, 'closes #N' PR keyword, or before /issue-close was in place) had no human-readable 'Resolved by commit' comment or machine-readable sentinel. backfill-commit-comments.sh only posted a J-2 sentinel when a history.md entry existed, and warned+skipped otherwise.
Changes: Enhanced backfill-commit-comments.sh with 3-tier hash discovery (Tier 1: history.md heading bracket hex, Tier 2: git log --all --grep boundary-safe pattern preventing #42 from matching #420, Tier 3: no-hash). Added J-1 'Resolved by commit' posting with has_resolved_by() idempotency check. Added --canary flag (bash 3.2 compatible scalars, 1 issue per class max 3 total). Class-specific J-2 sentinel bodies (backfill / backfill-gitlog / backfill-no-hash). Issues missing from history.md now fall through to git-log then no-hash J-2 instead of warn+skip. Added ops.md self-contained migration runbook. Tests: R4 updated + R5-R12 added (40 total, all pass).

### FEATURE: Add cc-session-title CLI — VS Code session title auto-update from workflow (#299) (2026-05-15, pending)
Background: The VS Code Claude Code extension displays a session title in the sidebar, but it never reflected which issue was being worked on — sessions always showed the auto-generated title. The update mechanism is a custom-title JSONL record appended to ~/.claude/projects/<encoded-cwd>/<session>.jsonl; the extension picks up the latest record on next render. CLAUDE_SESSION_ID is unavailable in Bash subprocesses (Anthropic bug #27987), so the current session file is discovered via mtime.
Changes: New bin/cc-session-title.py: set-issue <N> "<title>" appends a custom-title record to the newest JSONL in the project dir; add-pr and mark-complete are stubbed (Phase 2/3). Manual-rename guard: skips update when latest custom-title does not match ^(#\d+\s|✓). Installers: cc-session-title launcher added to install/linux/dotfileslink.sh; cmd + bash shim added to install/win/dotfileslink.ps1. Skills: workflow-init steps A2.5 and B2.5 call cc-session-title set-issue immediately after fetching issue title; clarify-intent Completion substeps 2 and 3 call cc-session-title set-issue after labelling/creating the issue. Tests: 38 pytest cases in tests/test_feat-cc-session-title.py covering encode_cwd, latest_custom_title, is_workflow_generated, build_record, format_set_issue_title, run_set_issue integration, and main stubs.

### BUGFIX: enforce-worktree CWD issues: cd+worktree-remove, post-cleanup guard, FD redirect, orphan-dir path (#242, #243, #294, #297) (2026-05-15)
Background: Four linked bugs blocked worktree-end cleanup operations and planning workflow commands from the main worktree. Root cause: enforce-worktree.js lacked exemptions for (a) the combined cd+git-worktree-remove pattern needed when VS Code resets Bash CWD between calls (#294), (b) stash/restore/checkout after all linked worktrees are removed (#297), (c) read-only planning commands using 2>&1 or 1>&2 FD-to-FD redirects falsely classified as writes (#243), and (d) cleanup-orphan-dir.js invoked via relative path that breaks when CWD resets to a deleted directory (#242).
Changes: hooks/enforce-worktree.js: added findFirstUnquotedAnd(cmd) — quote-aware && scanner used by new isAllowedCdWorktreeRemove; added isAllowedCdWorktreeRemove(cmd, repoRoot) — allows exactly one unquoted &&, LHS must cd to main worktree, RHS must be git worktree remove|prune with no --force/-f, multiple -C flags rejected; added isAllowedMainWorktreeCleanup(cmd, repoRoot) — allows stash push|pop|apply|drop|clear, restore (no --source), checkout path-restore (requires -- separator, no -b/-B/-f) gated on git worktree list --porcelain returning exactly 1 entry (main-only). hooks/lib/bash-write-patterns.js: posix-redirect regex adds (?!&\d) lookahead after >>? to exclude FD-to-FD redirects (2>&1, 1>&2) from write classification; header comment updated to note the fix. skills/worktree-end/SKILL.md: step 6e changed from relative node hooks/cleanup-orphan-dir.js to absolute node "C:\git\agents/hooks/cleanup-orphan-dir.js"; step 6b.5 note updated to document that the combined cd+worktree-remove form is now permitted via isAllowedCdWorktreeRemove. Three new test files: fix-bash-write-patterns-fd-redirect.sh (7 cases), fix-enforce-worktree-main-cleanup.sh (27 cases), fix-enforce-worktree-cd-worktree-remove.sh (13 cases). Closes #242, #243, #294, #297; parent issue #296 closed after merge.

### BUGFIX: backfill-commit-comments.sh: drop unsupported --paginate flag (#300 hotfix) (2026-05-15, pending)
Background: After PR #301 merged the new backfill-commit-comments.sh and the dry-run was executed, it processed 0 of 175 closed issues. Root cause: gh issue list does not support --paginate (only gh api does); the script's gh issue list --state closed --limit 1000 --paginate ... call printed an error to stderr that was swallowed by 2>/dev/null, leaving ALL_NUMBERS empty. Tests passed because the gh mock dispatch pattern also matched on --paginate, masking the real-CLI incompatibility.
Changes: Drop --paginate from the gh issue list call (--limit 1000 is sufficient for foreseeable closed-issue counts; consumer repos with >1000 closed issues can use gh api workflow). Update gh mock dispatch pattern to match --limit instead of --paginate so tests stay aligned with real-CLI behavior. Tests: 40 pass.

### BUGFIX: tracking: worktree-end x CWD issues parent (#296) (2026-05-15, 396a534, #296)
Background: Parent tracking issue grouping four enforce-worktree CWD bugs (#242 orphan-dir path, #243 FD-redirect false-positive, #294 cd+worktree-remove combined form, #297 post-cleanup stash/restore/checkout) blocking worktree-end and planning workflows from the main worktree.
Changes: All four child issues resolved by PR #304 (squash commit 396a534). See main BUGFIX entry for #242, #243, #294, #297 for implementation details.

### BUGFIX: planning skills: CONFIRM_* checks blocked by FD-redirect false-positive in bash-write-patterns.js (#243) (2026-05-15, 396a534, #243)
Background: Read-only planning commands using 2 and 1 FD-to-FD redirects were falsely classified as writes by bash-write-patterns.js, blocking skill steps such as CONFIRM_* flag checks from the main worktree.
Changes: posix-redirect regex in hooks/lib/bash-write-patterns.js: added negative-lookahead for FD-to-FD redirects so that 2-to-1 and 1-to-2 forms are excluded from write classification. Part of BUGFIX entry for #242, #243, #294, #297.

### BUGFIX: enforce-worktree: allow cd+git-worktree-remove combined form for VS Code CWD-reset behavior (#294) (2026-05-15, 396a534, #294)
Background: enforce-worktree.js lacked an exemption for the cd-main-then-git-worktree-remove pattern, required when VS Code extension host resets the Bash CWD between calls during worktree-end cleanup step 6b.5.
Changes: Added findFirstUnquotedAnd() and isAllowedCdWorktreeRemove() to hooks/enforce-worktree.js. Part of BUGFIX entry for #242, #243, #294, #297.

### CONFIG: Disable 1M context for skill/subagent calls (CLAUDE_CODE_DISABLE_1M_CONTEXT) (2026-05-16)
Background: Skill/agent frontmatter with model: opus or model: sonnet resolves to 1M-context defaults (Opus 4.7 and Sonnet 4.6 are natively 1M), causing frequent 'Extra usage is required for 1M context' API errors when Sonnet 1M is not covered by the active plan. The UI-level non-1M model selection constrains only the main session, not subagents.
Changes: Added CLAUDE_CODE_DISABLE_1M_CONTEXT=1 to the env block in settings.json. This env var (officially added in Claude Code changelog) forces all sessions and subagent invocations to use standard-context (200K) mode, preventing alias resolution from silently upgrading to 1M.

### BUGFIX: fix Tier 1 hash extraction in backfill-commit-comments.sh (2026-05-16, 2749dce, #307)
Background: backfill-commit-comments.sh dry-run showed hash-from-history: 0 out of 161 closed issues. Two independent bugs in discover_hash_from_history(): (1) grep pattern [,)] missing : so INCIDENT: #N: and BUGFIX: #N: headings never matched; (2) tail -n 1 selected only the last paren group, missing hashes in earlier bracket positions.
Changes: Fix A: extend [,)] to [,):] in both grep patterns (lines 46, 48) so category-prefixed INCIDENT/BUGFIX headings match. Fix B: remove tail -n 1 from the bracket-scan pipeline so all paren groups are scanned for a 7-40 hex match. Comment updated from 'last bracket group' to 'any bracket group'. Regression tests R13/R14/R15 added to tests/feature-issue-222.sh covering the archive path and both fixes independently. Closes #307.

### FEATURE: Projects v2 Content Date backfill and issue-create auto-set (2026-05-16)
Background: Projects v2 'Content Date' field was missing for 14 issues (#246, #247, #249, #250, #251, #257, #261, #262, #267, #268, #283, #299, #300, #307) created in the new format. The existing backfill-content-date.sh only handles the old history-entry format.
Changes: One-off backfill: 14/14 issues set via gh project item-edit using createdAt date (node one-liner, no migration script). issue-create.sh: attach call now uses --format json --jq '.id' to capture item_id and calls gh project item-edit --date with createdAt[:10]; ISSUE_CREATE_FIELD_ID / ISSUE_CREATE_PROJECT_ID env-var overrides added. SKILL.md: documented Content Date auto-set and new env vars. tests/feature-issue-create-skill.sh: T1-T4 added.

### FEATURE: #262: plan-time premise verification gate (2026-05-16, 37e5314)
Background: PR #311 implements the plan-time premise verification gate requested in #262. Root cause: planning pipeline ran on stated issue premises without verifying real-tool behavior, leading to wasted work (#250, #261). Added two-axis research verification: (1) /survey-code now extracts and verifies up to 5 behavioral claims from intent.md against live code; (2) new /survey-history skill checks git log + docs/history.md + archived entries + GitHub PRs since the issue opened for changes that contradict issue premises. make-outline-plan Step 0 aggregates both artifacts and emits WORKFLOW_PREMISE_FAIL if contradictions found, prompting user with AskUserQuestion before proceeding.
Changes: Added skills/survey-history/SKILL.md and agents/survey-history.md (new subagent); strengthened skills/survey-code/SKILL.md with Step 0 claim extraction and Step 5 verified-claims output; added WORKFLOW_PREMISE_FAIL/ACK sentinels to hooks/workflow-mark.js and settings.json; added Step 0 aggregation block to skills/make-outline-plan/SKILL.md; added setPremiseContradiction/clearPremiseContradiction/getPremiseContradiction to hooks/lib/workflow-state.js; updated agents/outline-planner.md with sentinel-emission constraint.

### FEATURE: Unify plan-artifact path presentation: SKILL.md and hook now mandate Windows backslash absolute path (2026-05-16, 5988ab1, #314)
Background: After make-outline-plan finished, the orchestrator was generating its own confirmation text (e.g. 'outline.md written: ~/.workflow-plans/...(full path: C:/Users/.../...)') which was neither clickable nor consistently formatted. Root cause: skills/_shared/confirm-plan.md had been aggregated as the single source of truth, but stale '(b) the final clickable link' instructions remained in skills/make-outline-plan/SKILL.md and skills/make-detail-plan/SKILL.md, so the agent kept producing custom strings. Two adjacent gaps were folded in: show-plan-link.js auto-open relied on TERM_PROGRAM only and never fired in the VS Code extension webview, and the WORKFLOW_USER_VERIFIED step gave no visibility into staged files or PR URL.
Changes: (PR1) Removed stale clickable-link instructions from make-outline-plan/SKILL.md and make-detail-plan/SKILL.md, delegating fully to confirm-plan.md. Hardened confirm-plan.md Step 2 with an explicit prohibition list (no duplicate, translate, paraphrase, markdown link, tilde, file:/// URI, dual-path annotation, Japanese sentence-prepend). show-plan-link.js now emits Windows backslash absolute paths (POSIX keeps forward slashes). Added Windows backslash output test in feature-show-plan-link.sh. (PR2) Extracted shouldOpenInVsCode() with layered detection (TERM_PROGRAM=vscode OR CLAUDE_CODE_ENTRYPOINT=claude-vscode) plus SHOW_PLAN_LINK_NO_AUTO_OPEN=1 opt-out; documented VSCODE_IPC_HOOK_CLI rejection rationale; added 5 layer-detection tests and inverted T21. New hooks/show-user-verified-context.js detects WORKFLOW_USER_VERIFIED in tool_input.command and emits a systemMessage listing staged files and the open PR URL; registered the PostToolUse Bash matcher in settings.json; added feature-show-user-verified.sh (11 cases, U5 skipped on Windows due to Node spawnSync limitation with POSIX-format PATH inherited from Git-Bash).

### BUGFIX: fix: cc-session-title set-issue unconditional overwrite (#299) (2026-05-16, 95230ad)
Background: run_set_issue had an is_workflow_generated guard that blocked title writes when Claude Code auto-titler set the session title first. Auto-titler titles never match the workflow regex, causing a permanent no-op that made the entire title chain ineffective.
Changes: Removed is_workflow_generated guard from run_set_issue. set-issue now unconditionally writes the workflow title. add-pr and mark-complete guards are preserved.
### BUGFIX: #326: suppress VS Code spawn in tests via SHOW_PLAN_LINK_NO_SPAWN (2026-05-16, 927d40a)
Background: T16 and T22 in tests/feature-show-plan-link.sh launched real VS Code on Windows during test runs. Root cause: the hook uses spawn(cmd.exe, [/d, /s, /c, code, -r, absPath]) on Windows; cmd.exe resolves code via PATHEXT (.COM;.EXE;.BAT;.CMD) and ignores extensionless bash shims placed at the front of PATH, falling back to the real code.cmd and opening abc-detail.md in the user VS Code window.
Changes: Added SHOW_PLAN_LINK_NO_SPAWN=1 test-only env var bypass to openInVsCode() in hooks/show-plan-link.js. When set, the real code -r spawn is skipped; optional SHOW_PLAN_LINK_MARKER_FILE env var records invocation via a marker file for test assertion. Updated T16 and T22 to use SHOW_PLAN_LINK_NO_SPAWN=1 instead of PATH shim tricks, added T23 verifying the bypass block is not entered when only SHOW_PLAN_LINK_MARKER_FILE is set without SHOW_PLAN_LINK_NO_SPAWN. Closes #326.

### REFACTOR: #327: shift survey-code/survey-history left into workflow-init (2026-05-16, pending)
Background: The clarify-intent interview ran without codebase or history context — survey-code and survey-history only executed after the interview completed (clarify-intent Step 6 / Completion Step 3). This meant CC could not detect wrong premises or surface 'already done' facts during the interview, causing rework. Industry analysis (Devin/Cursor/Cline/Aider/OpenHands/SWE-agent) confirmed that code survey before or interleaved with interview is the canonical pattern.
Changes: workflow-init now writes a context.md fact-sheet (session-id, path, issue body sentinel-stripped, user prompt, issue metadata, top-20 keywords) and launches survey-code + survey-history as parallel Agent subagents on all Paths A/B/C before the interview. survey-code/SKILL.md Step 0: accepts context.md as fallback input when intent.md is not yet available (intent.md preferred post-clarify-intent). survey-history/SKILL.md Step 1: same input precedence; new Step 2.5 keyword-only mode (DEGRADED MODE header, all verdicts indeterminate, --since=1 year ago, skip gh pr list) when no issue number is available. clarify-intent Steps 6 and Completion 3: removed survey invocations; replaced with artifact-existence check — falls back to invoking missing surveys if workflow-init agents failed. New agents/survey-code.md (symmetric to agents/survey-history.md). Closes #327.

### FEATURE: Session-scoped ENFORCE_WORKTREE escape hatch (2026-05-17, 678016c, #336)
Background: ENFORCE_WORKTREE=on (default) blocks main-worktree writes. Maintenance scenarios such as worktree-end CWD-lock recovery (#296) sometimes need a temporary off, but editing .env globally leaks to other sessions and disrupts their worktree-start/end behavior.
Changes: Added two sentinels: WORKFLOW_ENFORCE_WORKTREE_OFF[: <reason>] writes a per-session marker (<workflowDir>/<sessionId>.worktree-off, atomic tmp+rename), and WORKFLOW_ENFORCE_WORKTREE_ON deletes it (idempotent, ENOENT no-op). workflow-mark.js (PostToolUse) handles ingestion; enforce-worktree.js (PreToolUse) checks the marker after isEnforceWorktreeOn() and allows writes when present. Session IDs validated with /^[A-Za-z0-9_-]+$/ before any fs operation; session-ID resolution via resolveSessionId() bypasses Anthropic bug #27987 (CLAUDE_SESSION_ID not propagated to Bash subprocesses). 25 tests across ingestion (A1-A11), guard consumption (B1-B5), round-trip (C1-C3), and security (SEC1-SEC5: path traversal, shell-metachar injection, env-file traversal).

### SECURITY: Require ask for WORKFLOW_ENFORCE_WORKTREE_OFF sentinel (security hotfix) (2026-05-17, pending, #336)
Background: Initial #336 implementation shipped WORKFLOW_ENFORCE_WORKTREE_OFF without an ask rule in settings.json. The sentinel fell through the generic echo allow path, so any CC session could silently lift main-worktree enforcement and bypass ENFORCE_WORKTREE=on without user confirmation. Discovered immediately after #336 merge when a CC session (this one) used the sentinel to commit docs/history.md from main with zero user prompt.
Changes: settings.json permissions.ask: added Bash(echo "<<WORKFLOW_ENFORCE_WORKTREE_OFF*>>"). ON sentinel left as auto-allow since restoring enforcement is the safe direction.

### BUGFIX: Orthogonalize ask sentinel rules — bare+reason coverage (#341) (2026-05-17)
Background: ask sentinel rules for ENFORCE_WORKTREE_OFF had a glob pattern (OFF*>>) that did not match the colon-containing reason form, so no ask prompt fired. Orthogonality audit revealed all sentinels in the ask array had inconsistent bare/reason coverage.
Changes: Split every ask entry into bare and reason forms; extended USER_VERIFIED and ENFORCE_WORKTREE_ON regexes to accept optional reason; fixed show-user-verified-context.js detection regex.

### BUGFIX: Tier 1.5 bulk-import commit blacklist in backfill-commit-comments.sh (#344) (2026-05-17, pending)
Background: Tier 1.5 (`git log --all --reverse -S "<title>"`) returns the oldest commit that introduced a history.md entry. For issues whose entries were bulk-imported in a single commit (`3969773 = feat(agents-split): add 39 tests from dotfiles (step 11)`), all affected issues resolved to the same import commit, causing false attribution. Discovered during M1 dry-run analysis; `3969773` accounted for 12–39 incorrectly attributed issues.
Changes: Added `TIER15_BLACKLIST="3969773"` constant and a prefix-match loop in `discover_hash_from_history_introducer()` — when `git log -S` resolves to a blacklisted hash, the function returns 1 and the classifier falls through to Tier 2 or no-hash. Added R26 (blacklist → no-hash) and R29 (blacklist + Tier 2 fallback) to `tests/feature-issue-222.sh`. 57/57 tests green. Proper Tier 1.5 refinement (collision detection, heuristic) tracked in #344.

### FEATURE: user-verified: PreToolUse hook migration so context surfaces before permission dialog (#318) (2026-05-17, da113d6)
Background: Under ENFORCE_WORKTREE=off step 8, when the user denied the sentinel approval, fixed the diff, and re-approved, the relevant URL had often scrolled out of view. Directly emitting the sentinel without an AskUserQuestion also made the link between the URL and the user's approval intent weak.
Changes: Migrated show-user-verified-context.js from PostToolUse to PreToolUse so staged files and Open PR URL are surfaced as a systemMessage immediately before the permission dialog. Added skills/_shared/user-verified.md as the shared protocol and made CLAUDE.md step 8 and worktree-end steps 3b/4 delegate to it. Dropped documentation of the bare <<WORKFLOW_USER_VERIFIED>> form — only the reason-bearing form is documented now. Rejected the AskUserQuestion approach: the permission dialog itself remains the load-bearing wall (tool-enforced, not protocol-only).

### FEATURE: Update README workflow mermaid chart (#348) (2026-05-17, 49294c1)
Background: The mermaid flowchart in README.md ('Hook-enforced end-to-end workflow') had not been updated since commit 76d4f8b (2026-05-10). Five workflow changes introduced by subsequent commits were missing from the chart.
Changes: Added S0 (workflow-init) as Step 0 before clarify-intent (DocCheck -- No --> S0 --> S1). Relabeled P2a as fallback since surveys now run in parallel inside workflow-init. Added S6d (review-skill-size) as a fourth parallel lane to the Review subgraph. Added IC (/issue-close --from-session) as a convergence node after all three Cleanup paths. Updated S5 label to reflect both ENFORCE_WORKTREE modes (on: Edit direct / off: diff to approve).

### FEATURE: WORKFLOW_PLANS_DIR: skills hardcode path, ignore env var (2026-05-17, d294414, #349)
Background: Skills hardcoded ~/.workflow-plans/ or used ${WORKFLOW_PLANS_DIR:-...} shell expansion, but LLM tool args (Read/Write/Edit/subagent prompts) are never piped through a shell — the brace expansion never fired and the override was silently ignored. JS hooks (loadDefaultEnv) and session-sync scripts (bin/workflow-plans-dir) honored the override, creating split behavior across the codebase.
Changes: Adopted orchestrator-injects pattern: each consuming SKILL.md inlines a Step 0 preamble that resolves PLANS_DIR once via bash bin/workflow-plans-dir, then substitutes the literal absolute path for every <PLANS_DIR> placeholder in subsequent tool args and subagent prompts. Added skills/_shared/resolve-plans-dir.md (canonical reference, inlined verbatim — no auto-load dependency). Added tests/feature-skills-plans-dir-drift-guard.sh and extended tests/feature-workflow-plans-dir.sh (I4/I5/I6). 8 SKILL.md files updated: workflow-init, clarify-intent, survey-code, survey-history, make-outline-plan, make-detail-plan, worktree-end, issue-close.

### REFACTOR: Rename plan-principles → core-principles, add §4 SSOT, tighten to macroscopic form (2026-05-17, 2e9ad7d b453ce4, #352)
Background: rules/plan-principles.md (PR #343) had three gaps: §1 read as symmetric-coverage only (no goal-shortest-path framing); no cross-cutting SSOT principle; Codex adversarial reviewers and reviewer agents (outline-reviewer, detail-reviewer) had no reference to these principles, making the planner/reviewer reference graph asymmetric. Reference sites also restated the principle TOC at every call — violating the very SSOT principle being added.
Changes: Renamed rules/plan-principles.md to rules/core-principles.md. Rewrote §1 to lead with "Solve from the class, not from the immediate task" — root/abstract/parent class identification first, then optional merge/replace/restructure of the class itself, then sibling application. Added §4 Single Source of Truth (reference master / no duplication / extract shared part; excludes summaries/snapshots/caches/stream records). Tightened §1–§4 to macroscopic form: dropped Apply when / How / Checks organizational headers; removed concrete example enumerations; §2 compressed to its principle (orthogonality = §1 specialized to symmetric pairs). Added core-principles reference bullets to agents/outline-reviewer.md and agents/detail-reviewer.md. Extended bin/review-plan-codex to accept multiple --context flags. Wired --context rules/core-principles.md into all Codex call sites: skills/make-outline-plan/SKILL.md, skills/make-detail-plan/SKILL.md. Applied SSOT to references to this file itself: every reference site (CLAUDE.md, planners, reviewers, SKILLs, rules/coding.md) reduced to bare "rules/core-principles.md" — no TOC restatement. Tests: added B8-B13 and test-30 (multi-context).

### BUGFIX: backfill: has_appended_sentinel idempotency broken — jq 'm' flag does not anchor '^' to line starts (Oniguruma semantics) (2026-05-17, #364)
Background: jq's 'm' flag (Oniguruma) is multi-line dot mode, not PCRE line-anchor mode — ^ still anchors to string start. has_appended_sentinel() used test('^...appended'; 'm') which failed to detect merged-format sentinels where the sentinel appears on line 2 of the comment body. Root cause: R32 mock dispatched on 'm' substring presence rather than exercising real jq behavior.
Changes: Replace regex with (^|\n)<!-- issue-close-sentinel: appended (no 'm' flag). Update R32 mock dispatch and R33 grep regression guard in tests/feature-issue-222.sh and tests/fixtures/gh-mock/gh to match new pattern via (^|\n) substring check.

### FEATURE: Split /issue-close into Phase 1 (worktree) and Phase 2 (main) skills (2026-05-17, 3934325, #325)
Background: /issue-close ran from the main worktree to append docs/history.md, forcing the user to toggle ENFORCE_WORKTREE off/on each invocation (with rollback risk if forgotten). Discovered most acutely during #262's close, which compounded with a CRLF/UU rebase artifact (#277 thread). PR #323 attempted a path-based bypass via .env.example default but was closed after Codex review surfaced cross-repo suffix collision and lack of append-only enforcement.
Changes: Split /issue-close into /issue-close-stage (Phase 1, runs INSIDE the linked worktree before PR merge — sub-issue gate, pending sentinel, doc-append + commit, promote sentinel, parent body update) and /issue-close-finalize (Phase 2, runs from main worktree AFTER merge — API-only on the normal path: close issue, post resolved-by + appended sentinels). Both new skills carry user-invocable: false so they only fire from CLAUDE.md Steps 8.5 / 10b. /commit-push pre-flights Phase 1 completion via check-phase1-complete.sh and embeds <!-- issue-close-pr-of: N --> in the PR body; /issue-close-finalize resolves the merge SHA via find-pr-by-marker.sh. Triage logic was extracted into issue-close-triage-lib.sh (SSOT). Legacy /issue-close skill deleted. Follow-up gaps filed as #360 (pending-sentinel rollback), #361 (PR/SHA early-resolve masking errors), #366 (gate-skip when parent already CLOSED).

### REFACTOR: Remove cc-session-title — VS Code auto-titler overwrites every turn (#299) (2026-05-17, #299)
Background: PRs #303/#313/#331 added a workflow-driven session-title chain (set-issue / add-pr / mark-complete) that wrote custom-title JSONL records into ~/.claude/projects/<encoded-cwd>/<session>.jsonl. The VS Code Claude Code extension's auto-titler runs after every Stop hook and unconditionally overwrites custom-title, making the chain a permanent no-op regardless of when (Stop, SessionStart, or any other hook) the workflow rewrites the title. No last-writer-wins variant can win against per-turn overwrite. User decided to abandon the feature.
Changes: Deleted bin/cc-session-title.py and its two test files (test_feat-cc-session-title.py, test_fix-session-title-set-issue-guard.py). Removed cc-session-title invocations from skills/workflow-init, clarify-intent, commit-push, worktree-end. Removed launcher-generation blocks from install/linux/dotfileslink.sh and install/win/dotfileslink.ps1, and added idempotent BEGIN/END temporary cleanup blocks to actively remove ~/.local/bin/cc-session-title{,.cmd} on next dotfileslink run. Dropped A9 regression assertion from tests/feature-workflow-init-survey-shift-left.sh.

### REFACTOR: Add .py to cc-session-title installer cleanup list (#299) (2026-05-17)
Background: Initial removal commit only purged ~/.local/bin/cc-session-title{,.cmd}; the .py file (deployed by an earlier iteration of the launcher) was missed.
Changes: Extended cleanup arrays in install/linux/dotfileslink.sh and install/win/dotfileslink.ps1 to also remove ~/.local/bin/cc-session-title.py on next dotfileslink run.

### FEATURE: Phase 1: gate gh operations by remote host (2026-05-17, #367)
Background: agents workflow assumed GitHub remote implicitly. On non-GitHub hosts (generic git hosting) all gh CLI calls failed. Phase 1 auto-detects non-GitHub remote and skips gh operations to allow the rest of the workflow (plan/test/commit/push) to function.
Changes: Add bin/is-github-dotcom-remote helper (exit 0=GitHub, 1=non-GitHub, 2=unknown). Gate workflow-init, clarify-intent, commit-push, issue-close-stage, issue-close-finalize against non-GitHub remotes. Add tests and manual review checklist.

### BUGFIX: issue-close-finalize: skip sub-issue gate on auto_close_path when parent already CLOSED (#366) (2026-05-18)
Background: auto_close_path route (issues closed via 'closes #N' without Phase 1) included Step B (sub-issue gate) in NEXT_STEPS. The gate's intent is pre-close protection; once the parent is CLOSED it only stalls bookkeeping behind long-lived tracker sub-issues indefinitely.
Changes: Drop B from auto_close_path NEXT_STEPS in issue-close-finalize-triage.sh. Update SKILL.md Step B header to clarify Phase 1 only scope. Add FT5b trap test with closed_no_sentinel_open_subissue fixture.

### BUGFIX: issue-close-finalize: defer PR/SHA resolution to after triage, guarded by J-step membership (#361) (2026-05-18)
Background: find-pr-by-marker.sh was invoked unconditionally in the SKILL.md Pre-flight section before triage ran. Recovery routes (resume_h, stuck_sentinel_only) that do not include Step J would abort with 'PR not found' rather than surfacing the real diagnostic.
Changes: Remove find-pr-by-marker.sh from Pre-flight. Add Step A.5 with [[ ,, == *,J,* ]] guard so PR/SHA resolution only runs when the resolved-by sentinel step is scheduled. Add static contract test feature-361-finalize-pr-resolution-order.sh verifying ordering and guard presence.

### FEATURE: Add WIP signaling via Projects v2 Status field (#362) (2026-05-18, #362)
Background: GitHub Issues has only open/closed states; parallel Claude Code sessions have no way to signal one session is actively working an issue. Projects v2 Status and a session-fingerprint text field (sha256(session_id:N)[:8]) provide the signaling layer.
Changes: New bin/github-issues/wip-state.sh helper with set/check/clear/setup verbs. Integrated into clarify-intent Completion (set after intent:clarified label), workflow-init Step 3 (check on OPEN issue; AskUserQuestion on conflict), and issue-close-finalize Step K (clear on close). Updated issue-close-finalize-triage.sh to include K in all NEXT_STEPS paths. Added tests/feature-wip-state.sh (31 cases), feature-issue-close-finalize-wip-clear.sh (6 cases), and WIP assertions to feature-clarify-intent.sh and feature-workflow-init-routing.sh.

### FEATURE: /issue-create: auto-survey existing issues before creating (2026-05-18, #378)
Background: Every time the user creates a new issue via /issue-create, they must manually instruct survey/reopen/sub-issue/sibling steps. This automates all five outcomes inside the skill using a new dispatch wrapper script.
Changes: New bin/github-issues/issue-create-dispatch.sh dispatch script handles five verdicts (none/reopen/sub-of/make-parent/sibling). skills/issue-create/SKILL.md rewritten to 4-phase procedure: Gather/Survey/Confirm/Dispatch. rules/github-issues.md updated with new survey behavior and sub-issue node-id clarification.

### FEATURE: wip-state.sh setup: auto-create session-fingerprint field if missing (2026-05-19, #383)
Background: cmd_setup hard-errored when the session-fingerprint Projects v2 text field did not exist, requiring manual UI creation. Broke zero-touch onboarding for new repos and bulk issue migration (#247).
Changes: Added ensure_field helper inside cmd_setup: auto-creates missing session-fingerprint field via createProjectV2Field mutation, re-discovers it after creation, idempotent no-op when field exists. Added project-scope hard-gate (gh auth status check) before mutation.

### FEATURE: issue-migration: reusable helper scripts for history/todo migration to other repos (2026-05-19, #247)
Background: nirecom/agents の移行（history.md → closed issues, todo.md → open issues, Projects v2 Content Date backfill）は repo 固有のスクリプトで ad-hoc に実施されていた。他 repo への横展開のために、bin/github-issues/migration/ 配下に再利用可能な helper 群が必要。
Changes: bin/github-issues/migration/ 配下に state.sh / orchestrate.sh / create-project.sh を新規追加。既存の migrate-history.sh / migrate-todo.sh / backfill-content-date.sh / preview-history.sh / backfill-commit-comments.sh を REPO_DIR 引数化。Canary パターン（1→確認→2→確認→全件）と state machine（.migration-state.json）で resumable に。migrate-todo.sh の todo.md → ID index 書き換えバグも修正。/migrate-repo skill (skills/migrate-repo/SKILL.md) で全体を一括起動。実装言語は元の issue 本文では .py を想定していたが、隣接スクリプトの慣例に合わせて .sh を採用。

### FEATURE: worktree-end: deferred-resume cleanup for Windows EPERM (closes #251 and #357) (2026-05-19, #251, #357)
Background: Step 6c (git worktree remove) of /worktree-end fails with EPERM on Windows + VS Code. The previous in-session workarounds (#294: 'cd <main>' chain; #333: Reload Window) could not release the session-level CWD lock held by the VS Code extension host when the session had entered the worktree via the EnterWorktree tool. The variant resurfaced as #357 and could only be recovered by a full Claude Code restart.
Changes: Introduced a deferred-resume escape path. When Step 6c fails with EPERM/busy/not-empty, /worktree-end writes a 3-line marker at ~/.workflow-plans/worktree-end/pending-cwd-unlock-<repo-id>--<branch> (branch / worktree-path / 'pre-remove' stage) and exits gracefully with instructions to run /worktree-end --resume in a new session. The new entry point bin/worktree-end-resume-load.js validates the marker (WORKTREE_BASE_DIR path-traversal guard, KNOWN_STAGES whitelist) and replays the remaining cleanup (worktree remove, prune, orphan-dir, branch -D, fetch + pull). hooks/enforce-worktree.js is generalized to support multiple marker prefixes via a MARKER_PREFIXES constant plus prefix-loop dispatch in isMarkerFilePath / isAllowedMarkerDelete (PR1). 52 tests pass; security review PASS.

### FEATURE: sentinel chain guard + rules/git.md write commands expansion (#382) (2026-05-20)
Background: Sentinel echo commands chained with && could bypass hook classification in workflow-gate.js (the sentinel echo dropped silently while the chained non-sentinel command ran unguarded). Separately, rules/git.md only covered git add/commit/push, leaving other write subcommands (fetch, pull, merge, rebase, worktree, stash, etc.) undocumented as chain-prohibited.
Changes: Extracted shared sentinel recognition primitives to hooks/lib/sentinel-patterns.js (SSOT refactor; workflow-mark.js now imports from it). Added sentinel chain guard to hooks/workflow-gate.js blocking echo <<WORKFLOW_*>> && <non-sentinel> forms. Expanded rules/git.md Write Commands section to enumerate all git write subcommands. Added test suite tests/feature-sentinel-and-chain-gate.sh (15 cases, all passing).

### INCIDENT: #1: Subagent autonomously ran winget install (jq) without user approval (2026-05-20, #384)
Cause: A general-purpose subagent launched by write-tests detected missing jq and autonomously ran winget install --id jqlang.jq --silent without user approval. rules/user-escalation.md Rule 1 (Autonomy-First) had no carve-out for system-state-altering commands, and rules/ops.md did not list package manager commands as requiring approval.
Fix: Short-term: user manually ran winget uninstall --id jqlang.jq. Long-term: added hooks/enforce-system-ops.js PreToolUse hook blocking categories A-F (pkg install, power, service stop/disable/mask, user/group mgmt, reg/boot, disk ops). Updated rules/installer.md, rules/ops.md, and rules/user-escalation.md. Installer scripts bypass via inherited SYSTEM_OPS_APPROVED=1 env var.

### FEATURE: Review the CC↔Codex plan-review loop: rounds overrun the cap and quality plateaus (#329) (2026-05-20, #329)
Background: Sessions using make-detail-plan experienced cap overruns (rounds 3-4) and quality plateau: codex raised 5 new concerns each round with no cumulative improvement, re-raising already-resolved items. Accepted design decisions (tradeoffs) were not being propagated to codex, causing repeated misreads. Extension budget was unbounded in practice.
Changes: Added SSOT functions to bin/lib/codex-core.sh: severity vocab, round-log (jq), hard-cap check, validator, jq prereq guard. Added bin/extract-accepted-tradeoffs (awk-based markdown extractor) and bin/review-loop-cap-menu (JSON-emitting cap decision helper, exit 42 auto-extend). Rewrote bin/review-plan-codex with --cap/--max-extensions/--extensions-used/--session-id/--log-dir/--accepted-tradeoffs args; fail-closed log-before-emit contract. Updated skills/clarify-intent, make-outline-plan, make-detail-plan SKILL.md and agents/detail-planner.md, outline-planner.md, outline-reviewer.md: accepted-tradeoffs carry-forward, round-log, planner-response trailer. Extracted symmetric codex review loop + cap-menu dispatch into skills/_shared/ (SSOT, removes ~150 lines of SKILL.md duplication). Added jq prereq docs to README.md. Added tests/feature-329-*.sh (58 new cases).

### FEATURE: Group: Better way to allow /issue-close's docs/history.md append from main (2026-05-20, 78d0796, #325)
Background: Phase 1 (issue-close-stage) wrote history.md inside the linked worktree and committed it on the feature branch, causing extra history-only commits, rebase conflicts on parallel branches (#277), staged-but-uncommitted leftovers (#280), and a gap where auto_close_path could not write history.md from the main worktree under ENFORCE_WORKTREE=on.
Changes: Moved doc-append + git commit from Phase 1 to Phase 2 (issue-close-finalize). Added isAllowedHistoryWriteViaIssueCloseSkill bypass to enforce-worktree.js (three-axis AND: ISSUE_CLOSE_SKILL=1 prefix + docs/history path + git-add/commit shape). Relaxed check-phase1-complete.sh to sentinel-only. Updated issue-close-stage-triage.sh routing to B,D,F,G. Side-effects: #277 and #280 resolved; auto_close_path gap closed.

### FEATURE: workflow-gate: mode-aware user_verification prompt + resolveRepoDir payload-cwd tier (2026-05-20, #380, #394)
Background: When git commit ran without -C from a linked worktree, resolveRepoDir() fell back to the main worktree CWD, causing isWorktreeContext() to return false. This pushed user_verification into incomplete[], and SKILL_MAP[user_verification]'s unconditional 'run immediately' wording caused Claude to override CLAUDE.md Step 8's 'ENFORCE_WORKTREE=on: No action here' rule and emit the sentinel unnecessarily.
Changes: Added isLinkedWorktree() predicate to workflow-gate.js (narrower than isWorktreeContext — checks only common-dir != git-dir). Extended resolveRepoDir() with a Tier 3 that trusts hook payload input.cwd when it resolves to a linked worktree, preventing CWD drift on Windows VS Code (CC #27343). Updated SKILL_MAP[user_verification] text with explicit two-branch structure using pipe separator (ENFORCE_WORKTREE=on+linked-worktree → SKIP | otherwise → emit). Added C17b (positive) and C17c (negative) regression tests to feature-commit-gate-redesign.sh.

### FEATURE: workflow-gate: resolveRepoDir CWD fallback breaks worktree commit skip when git -C is omitted (2026-05-20, #394, #380)
Background: Sub-issue of #225 pattern 4. When git commit ran without -C from a linked worktree, resolveRepoDir() returned the main worktree CWD (process.cwd() drift on Windows VS Code, CC #27343). isWorktreeContext() then received the main worktree path, returned false, and incorrectly pushed user_verification into the incomplete list — blocking commits that should have been approved.
Changes: Root cause addressed as part of #380 fix: added isLinkedWorktree() predicate and resolveRepoDir() Tier 3 that trusts hook payload input.cwd when it resolves to a linked worktree. See #380 history entry for full changeset.

### FEATURE: refactor-prompts skill + prompt-criteria.md SSOT (#395) (2026-05-20)
Background: rules/*.md (ops.md, git.md, shell-commands.md) were redundantly enumerating the same dangerous command literals that hooks/lib/bash-write-patterns.js and settings.json deny already block. Prompt-quality criteria (directives-not-prose, one-line-per-directive, few-examples) were also scattered across multiple files with no SSOT. Issue #395 introduced the tooling to fix this systematically.
Changes: Added rules/prompt-criteria.md as SSOT for prompt-content quality criteria (Form, Examples discipline, SSOT specialization) — scoped via frontmatter globs to rules/skills/agents. Added /refactor-prompts skill (SKILL.md 25 lines) with 2-phase scan: lexical scan via bin/refactor-prompts/ CLI (extract-keywords.js reads bash-write-patterns.js + settings.permissions.deny + enforce-system-ops.js; scan-prompts.js matches hot regions in rules/*.md / skills/*/SKILL.md / agents/*.md with word-boundary regex and whitespace normalization) + LLM judge (agents/refactor-prompts-judge.md owns the verdict semantics: delete/category-rewrite/keep-trigger/keep-context/defer). Defer regions surface in PR body only. Updated bin/review-skill-size to reference the SSOT. 25 tests (extract 10, scan 9, skill 6 incl. handoff contract). Closes #395.

### FEATURE: fix(migration): create-project.sh uses non-existent `gh -C` flag (2026-05-20, 3040e1d8a5bfc13b00f45009274d88c620bc8d99, #406)
Background: `bin/github-issues/migration/create-project.sh` lines 32-33 use `gh -C "$REPO_DIR" repo view ...`, but the `gh` CLI does not support a `-C` flag (that pattern belongs to `git`). As a result, Step 4 (Projects v2 + Content Date backfill) of `/migrate-repo` always fails with: ``` unknown shorthand flag: 'C' in -C Usage:  gh repo view [<repository>] [flags] ``` Reproduced today while migrating my-private-repo. Steps 1-3 and 5 of `/migrate-repo` work; only Step 4 is broken.
Changes: - Replace `gh -C "$REPO_DIR" repo view ...` with `(cd "$REPO_DIR" && gh repo view ...)` on lines 32-33. - Verify no other scripts under `bin/github-issues/` use the same incorrect pattern. Repro: ``` bash bin/github-issues/migration/orchestrate.sh <fresh-repo> ``` Related to #247 (the parent issue that introduced these scripts).

### FEATURE: #404: Enforce reason on WORKFLOW sentinels; ENFORCE_WORKTREE_ON to allow (2026-05-21, dba1018,5434853)
Background: Issue #404: bare (reason-less) sentinel forms for WORKFLOW_USER_VERIFIED, WORKFLOW_ENFORCE_WORKTREE_OFF, and WORKFLOW_ENFORCE_WORKTREE_ON created audit gaps and gave LLM no self-reflection cue. ENFORCE_WORKTREE_ON (restoring enforcement) required an unnecessary ask dialog despite being low-risk.
Changes: Made reason field mandatory at both layers (settings.json and sentinel-patterns.js regex). Bare forms now hard-rejected via LOOKSLIKE handler in workflow-mark.js. ENFORCE_WORKTREE_ON: * moved from ask to allow. Added repo-wide grep audit gate (feature-bare-sentinel-audit.sh). Fixed pre-existing claude-global path bug in 2 test files. Soft-validation preserved: validateSkipReason warns but applies for these 3 sentinels.

### INCIDENT: #2: #415: canary gate bypass via yes y | pipe during /migrate-repo (2026-05-21, 46a990a4)
Cause: orchestrate.sh chained canary-1 -> confirm() -> canary-2 -> confirm() -> full within a single process. confirm() read from stdin via read -rp, which yes y | bypasses entirely. TTY detection ([ -t 0 ]) is defeated by PTY wrappers, so behavioral guards were ineffective.
Fix: Approach B (structural separation): added --stage canary-1|canary-2|full flag to orchestrate.sh. Each stage runs as one process invocation and exits, making cross-stage bypass structurally impossible. Added schema v2 to state.sh (advanced{} blocks per kind) with state_set_advanced/state_get_advanced helpers. PR #422 (merge 46a990a4).

### FEATURE: Skillize code phase (Step 5 subagent delegation + per-language guidance) (2026-05-21, aa7ecdc, #411)
Background: CLAUDE.md Step 5 (code phase) was inlined; main-context token cost and the lack of language-specific guidance motivated extracting it as a subagent-delegated skill, mirroring /run-tests.
Changes: Added /write-code skill (subagent-delegated edit + lint/typecheck/self-repair, CONFIRM_CODE gate). Moved skills/judge-task-complexity/SKILL.md to skills/_shared/judge-task-complexity.md (read-only rubric); updated path references in make-detail-plan, write-tests, write-code. Added rules/coding/python.md and rules/coding/nodejs.md as B-layer language essence. Added tests/feature-write-code-skill-static.sh.

### FEATURE: find-pr-by-marker.sh: stale marker incorrectly treated as closing PR (2026-05-21, 293b88b, #418)
Background: find-pr-by-marker.sh used the issue-close-pr-of body marker as primary PR source. Stale markers from abandoned branches were trusted, causing wrong merge commit and PR number in resolved-by sentinels.
Changes: Swapped primary/fallback: closedByPullRequestsReferences (most-recently merged) is now primary, marker is fallback. Added validate_sentinel_freshness() to triage-lib.sh (ISSUE_CLOSE_STALE_DAYS env, default 30d); stale pending sentinel is treated as Phase 1 not run.

### FEATURE: Phase 2 history.md write: non-GitHub remote gap and issue body insufficiency (2026-05-21, 293b88b, #412)
Background: Phase 2 issue-to-history.sh assumed gh issue view, breaking on non-GitHub remotes. Issue body alone is also insufficient for history.md because implementation notes (rejected alternatives, sharp edges) are only available inside the worktree during the PR. CLOSED:appended triage path omitted Step E entirely.
Changes: Added History Notes section to WORKTREE_NOTES.md template. issue-to-history.sh now accepts --history-notes-file, --non-github-mode, --title, --body-file, --closed-date. CLOSED:appended triage NEXT_STEPS changed from J,K to E,J,K.

### FEATURE: Long-lived pending sentinel has no rollback or expiry when Phase 1 branch is abandoned (2026-05-21, 293b88b, #360)
Background: Phase 1 posted an issue-close-sentinel comment with no TTL. If the branch was abandoned or rebased, the stale sentinel confused triage on re-attempt, routing through recovery instead of running Phase 1 cleanly.
Changes: Added validate_sentinel_freshness() in issue-close-triage-lib.sh using node-based ISO8601 diff. Controlled by ISSUE_CLOSE_STALE_DAYS env var (default 30d); fail-open on malformed/future dates. Stale pending sentinel is treated as absent, triggering clean Phase 1 re-run.

### FEATURE: fix(migration): backfill-content-date.sh fails on Windows due to CRLF in jq output (2026-05-22, adda42a, #427)
Background: On Windows, jq.exe opens stdout in text mode and emits CRLF-terminated lines. backfill-content-date.sh captured the migrated issue numbers via $(jq -r ...) and fed them through 'while read', leaving \r attached to each number; subsequent 'gh issue view 3\r' then failed with 'invalid issue format'. The same latent CRLF risk existed in every scalar-capture site across the migration scripts. MSYS Git Bash's $(...) strips trailing CRLF, masking the bug on developer laptops.
Changes: Added jq_text() SSOT helper in bin/github-issues/migration/state.sh that wraps 'jq -r | tr -d \r' inside a subshell with pipefail so jq's exit code propagates through the tr stage. Routed all confirmed scalar-capture jq calls in state.sh, backfill-content-date.sh, create-project.sh, and orchestrate.sh through jq_text. Replaced create-project.sh's 'jq ... | head -1' lookup with a SIGPIPE-safe jq array-index pattern (.[0] // empty) to avoid regressing under set -euo pipefail. Added tests/fix-fix-427-crlf-jq-migration.sh covering CRLF input handling and the SIGPIPE pattern.

### FEATURE: forge write scan: extend PreToolUse hook to scan gh issue/pr write commands (2026-05-22)
Background: hooks/scan-outbound.js Bash branch fast-pathed all non-git-commit commands to approve, so gh issue create/edit/comment and gh pr create/edit/comment/review bypassed private-info scanning entirely. Private info in --body, --title, --body-file, and heredoc arguments could be posted to GitHub Issues and PRs without triggering the blocklist.
Changes: Added hooks/lib/forge-write-extract.js (isForgeScanTarget + extractTexts) to classify forge write commands and extract scannable text from --body/--title (quoted, equals-form, unquoted token), --body-file (with Windows path normalization and 1 MiB size cap), and heredoc (arbitrary delimiter). Extended hooks/scan-outbound.js Bash branch to run extractTexts before the git-commit check; forge targets with extracted content flow to bin/scan-outbound.sh --stdin; forge commands with no extractable body (e.g. close-only subcommands) are approved. Updated docs/scan-outbound.md with forge write checkpoint row and v1 known-false-negatives (gh repo *, gh api). Tests: feature-forge-write-scan-extract.sh (27 unit cases), feature-forge-write-scan-pretooluse.sh (15 integration cases including Windows .cmd stub, private-repo skip, --body-file scan, non-EOF heredoc).

### FEATURE: settings.json SSH-path deny pattern false-positive on command body text (2026-05-22, 58cb407daccf7b393261363e1e525f33bd8bb9c7, #424)
Background: 2026-05-21 のセッションで agents repo (cwd) から `gh issue create --repo nirecom/my-private-repo` を発行した際、ユーザーへの permission ダイアログ表示なしで `Permission to use Bash ... has been denied.` が返却された。 原因調査の結果、本 repo の `settings.json` line 204 にある以下の deny パターンが false-positive を起こしていることが判明: ``` "Bash(*~/.ssh/*)" ``` このパターンは substring glob のため、コマンド文字列全体 (`--body` argument や heredoc の中身を含む) に対して一致判定が行われる。issue body に `~/.ssh/config` 等の文字列が **説明用途で** 含まれているだけで巻き添え auto-deny される。 ## Symptoms / Evidence セッションログ `0f58b662-ee5d-4bbf-bd31-6ea0699ae16b.jsonl` の line 405〜413 のタイムスタンプ: | Time (UTC) | Event | |---|---| | 14:16:06.631 | assistant tool_use 発行 (`gh issue create --repo nirecom/my-private-repo --body "$(cat <<'EOF'...`) | | 14:16:06.797 | hook_success: `scan-outbound.js` | | 14:16:06.821 | hook_success: `block-dotenv.js` | | 14:16:06.822 | hook_success: `enforce-worktree.js` | | 14:16:06.822 | hook_success: `enforce-issue-close.js` | | 14:16:06.824 | hook_success: `enforce-system-ops.js` | | 14:16:06.832 | tool_result: `has been denied` | フック通過から ~8ms 後の deny。**ユーザーがダイアログで Deny を押す時間スケールではない**ため、Claude Code built-in の `permissions.deny` ルール照合による auto-deny で確定。 ## Root cause 意図された保護対象は `cat ~/.ssh/id_rsa` のような **SSH 秘密鍵への直接 read** だが、実装はコマンド文字列への substring 一致なので、`~/.ssh/` というリテラルがコマンド本文中にどんなコンテキストで含まれていてもマッチしてしまう。実際の file アクセス vs 単なる文字列言及を区別できない。 既存の以下 2 ルールは tool-input file path への一致なので適切に動作し、影響を受けない (本 issue の scope 外): ``` "Read(~/.ssh/**)" "Edit(~/.ssh/**)" ``` ## Proposed fixes ### 案 A (推奨): read コマンドに絞ったパターン群に置換 substring 一致は維持するが、対象を file-read/write 系コマンドの先頭に限定する。 置換前: ``` "Bash(*~/.ssh/*)" ``` 置換後 (例): ``` "Bash(cat *~/.ssh/*)" "Bash(less *~/.ssh/*)" "Bash(more *~/.ssh/*)" "Bash(head *~/.ssh/*)" "Bash(tail *~/.ssh/*)" "Bash(cp *~/.ssh/*)" "Bash(mv *~/.ssh/*)" "Bash(scp *~/.ssh/*)" "Bash(Get-Content *~/.ssh/*)" "Bash(Copy-Item *~/.ssh/*)" ``` - 利点: 既存の Claude Code built-in deny で完結、フック追加不要 - 欠点: パターン数が増える。redirect (`* < ~/.ssh/*`) や pipe 経由のアクセスは別パターンが必要 ### 案 B: フック側で context-aware 判定 `block-dotenv.js` と同じ quote-aware tokenizer 方式を採用した `block-ssh-private-key.js` PreToolUse フックを新設し、コマンドを実 token 列に分解した上で「実 file アクセスかどうか」を判定する。`settings.json` の `Bash(*~/.ssh/*)` deny rule は削除。 - 利点: 1 箇所で正確な判定ができる。`gh pr create --body` 内の言及など false positive を構造的に除外可能 - 欠点: フック追加 + 既存 deny 削除の連動。`block-dotenv.js` の実装パターンをそのまま流用できるので工数は小 ### 評価軸 | 観点 | 案 A | 案 B | |---|---|---| | 実装規模 | 小 (settings.json 編集のみ) | 中 (フック新設 + 既存 deny 削除) | | 抜け漏れ耐性 | パターン enumeration 漏れリスク | tokenizer ベースで構造的に網羅 | | 既存パターン整合性 | 周辺の Bash deny と同方式 | `block-dotenv.js` と同方式 | | メンテナンス | 新しい read コマンドが出るたび追加 | tokenizer 改良で対応 | `block-dotenv.js` という precedent があるので、本件も同じ方式 (案 B) のほうが consistent。 ## Out of scope - SSH 秘密鍵保護の意図は維持する (loosen ではなく precise にする) - `Read(~/.ssh/**)` / `Edit(~/.ssh/**)` deny rule は既に precise なため変更不要 - 他の similar substring パターン (`Bash(*aws * delete*)` 等) の同種 false-positive の網羅調査は別 issue (本 issue で発見した代表例のみを直す) ## Notes - 発見セッション: `20260521-221915` (clarify-intent stage 中、別件で my-private-repo に issue を作成しようとした副作用) - 当該 my-private-repo issue は最終的にユーザー手動実行で作成済: nirecom/my-private-repo#42 - 過去 sessions で同種 deny に遭遇していなかったのは、issue body に `~/.ssh/` リテラルを含めなかった偶然と推測
Changes: Removed the substring-glob deny rule Bash(*~/.ssh/*) from settings.json. Added hooks/block-ssh-private-key.js (PreToolUse), backed by hooks/lib/command-parser.js extracted from block-dotenv.js — tokenize argv, walk path-bearing positions, ignore text-flag values and quoted message bodies. Coverage gains over the old substring rule: runInTerminal/runCommands/editFiles tools, attached redirect forms (>~/.ssh/, <~/.ssh/, 2>~/.ssh/), --flag=value form for path-bearing flags, path normalization (~/./.ssh/, $USERPROFILE/.ssh/, /root/.ssh/), and defense-in-depth wrapper for Read/Grep/Glob/Edit/Write/MultiEdit. block-dotenv.js refactored to share the same engine (core-principles §2 orthogonality). Tests: tests/feature-424-command-parser.sh, tests/fix-issue-424-ssh-deny-hook.sh, tests/lib/test-command-parser.js, tests/main-block-dotenv.sh additions. Related to #253

### FEATURE: worktree-end Step 5.5 should prompt /issue-create for WORKTREE_NOTES.md entries before backup/delete (2026-05-22, 4ee1134e3b95b7aae4a91e1cf2a18d5d64e20744, #429)
Background: `/worktree-end` Step 5.5 currently does: 1. Last-chance findings review (instruction to append more to `WORKTREE_NOTES.md`) 2. Capture PR metadata 3. Copy `WORKTREE_NOTES.md` to backup destination 4. Cache branch/path/date for Final Report The Final Report includes the backup path of `WORKTREE_NOTES.md`, but **no step prompts the user to convert pending BugsFound / RelatedTasks / NextTasks entries into GitHub issues before the worktree is removed**. As a result, entries get archived under `.worktree-backup/<branch>/WORKTREE_NOTES.md` and are effectively forgotten — they never surface back into the active GitHub Issues task list (the SSOT per `rules/github-issues.md`). ## Observed in Session for #423 (commit a5c3c5e). After bundling the `review-code-codex` fallback fix into #423, `WORKTREE_NOTES.md` still had 2 unresolved BugsFound entries and 2 RelatedTasks entries. None would have been issue-ized had the user not explicitly noticed mid-skill. ## Proposed direction In `skills/worktree-end/SKILL.md` Step 5.5, between sub-step (a) and (c): - (a.5) Parse `WORKTREE_NOTES.md`. For each non-empty entry under `## BugsFound`, `## RelatedTasks`, `## NextTasks` (excluding entries already marked resolved/closed):   - Present a brief list to the user   - Ask: which ones to `/issue-create` now? (multi-select)   - For selected entries, invoke `/issue-create` with a body draft derived from the bullet text   - Mark the converted entries in-place (e.g. append `→ #<N>`) so the backup copy reflects the linkage ## Scope notes - Out of scope: auto-deciding which entries are issue-worthy (user judgment required) - Out of scope: closing the loop on entries already converted in prior sessions (idempotent re-parse should detect existing `→ #<N>` markers and skip)
Changes:  (History Notes: Added sub-step (a.5) to worktree-end Step 5.5: parse WORKTREE_NOTES.md BugsFound/RelatedTasks/NextTasks entries, present unpromoted entries via AskUserQuestion multi-select, invoke /issue-create per selected entry, annotate with <!-- promoted: #N --> before backup. Extracted shared lib to hooks/lib/worktree-notes-sections.js, added bin/worktree-notes-triage.js CLI, compacted skills/issue-create/SKILL.md with stdout URL contract.)

### FEATURE: review-code-codex: diff coverage gaps in BASE...HEAD fallback (untracked files; non-empty BASE...HEAD masks fresh edits) (2026-05-22, 1f1d3ac329c91d33c4cf27e5cd1baf2df7267262, #430)
Background: `bin/review-code-codex`'s fallback to `git diff HEAD` (introduced by PR #426) has two diff coverage gaps. Gap 1: when `BASE...HEAD` is empty, untracked files are silently skipped because `git diff HEAD` does not include them, so brand-new files created by `/write-code` get no review. Gap 2: when `BASE...HEAD` is non-empty, the one-way gate causes uncommitted working-tree edits (staged + unstaged) to be entirely ignored, so fresh fixup edits during a second implementation pass are silently missed.
Changes: Extended the diff collection block in `bin/review-code-codex` inline (Approach A from the outline). Added per-file untracked enumeration via `git ls-files --others --exclude-standard -z` + `git diff --no-index /dev/null <file>` pseudo-diff synthesis for Gap 1. Switched committed/uncommitted from a one-way gate to concatenation with `[COMMITTED DIFF (BASE...HEAD)]` / `[UNCOMMITTED CHANGES (working tree vs HEAD)]` separators for Gap 2. Truncation via `MAX_DIFF_LINES=5000` now applies to the combined payload. SKIPPED guard message updated to `"no committed changes vs <base>, no uncommitted changes, no untracked files"`. Added tests 16 (Gap 1 untracked-only) and 17 (Gap 2 committed + uncommitted + untracked combined) to `tests/feature-review-code-codex.sh`. ## Context PR #426 (commit a5c3c5e, bundled into #423) replaced the `git diff --cached` fallback in `bin/review-code-codex` with `git diff HEAD` so workflow Step 6 reviews unstaged working-tree changes (the common state after `/write-code`). This addressed #391's chicken-and-egg case. Codex review of that change surfaced **two further coverage gaps** that the new fallback does not close: ## Gap 1 — untracked files `git diff HEAD` does **not** include untracked files. If `/write-code` creates a brand-new file and leaves it unstaged, `review-code-codex` runs but its prompt has no awareness of that file's content — the review silently skips what is arguably the highest-risk class of change (new code with no prior baseline). ### Repro 1. Fresh feature branch, no commits past base. 2. Create `new-module.ts` (unstaged, untracked). 3. Run `review-code-codex --base main`. 4. Observe: fallback fires ("reviewing uncommitted changes…") but `git diff HEAD` output is empty → SKIPPED with no review. ### Proposed direction In the fallback branch only (when BASE...HEAD is empty): - Enumerate untracked files via `git ls-files --others --exclude-standard -z` - For each, synthesize a pseudo-diff (e.g. `git diff --no-index /dev/null <file>`) - Append to the diff payload sent to codex - Honor the existing `MAX_DIFF_LINES` truncation ## Gap 2 — non-empty BASE...HEAD masks fresh uncommitted edits The fallback is **one-way**: when `git diff BASE...HEAD` is non-empty, uncommitted working-tree changes (staged + unstaged) are entirely ignored. The original rationale ("avoid duplicate/out-of-order content") is sound, but it creates a blind spot: ### Failure mode - Branch has prior commits past `main` AND fresh uncommitted edits (e.g. mid-implementation fixup in a second pass). - `review-code-codex` reviews only the **old committed** content — silently misses the fresh edits the user is actually about to commit. ### Proposed direction Concatenate (committed diff) + (uncommitted diff) with a clear separator like: ``` [COMMITTED DIFF (BASE...HEAD)] ... [UNCOMMITTED CHANGES (working tree vs HEAD)] ... ``` Then truncate the combined payload to `MAX_DIFF_LINES`. The duplicate-content concern from the original comment is overstated: `BASE...HEAD` and `HEAD` (working tree) are disjoint by definition. ## Out of scope - Workflow-gate ordering changes (covered by #225) - Test gaps (the existing test for the bundled fix asserts PERFORMED but doesn't validate the actual prompt content includes the diff — file separately if pursued)

### FEATURE: wip-state.sh: add CLAUDE_SESSION_ID fallback and suppress spurious clear warning (2026-05-22, c3bd6ec, #440)
Background: VS Code Claude Code does not propagate CLAUDE_ENV_FILE to Bash subprocesses, so resolve_session_id() in wip-state.sh always returned rc=2 in that environment. WIP signaling (session fingerprint writes and parallel-session collision detection) was silently broken for all VS Code sessions.
Changes: Added CLAUDE_SESSION_ID direct-env fallback to resolve_session_id() (file-first, env-fallback order). Suppressed spurious fingerprint-clear warning when gh returns rc=1 + 'no changes to make' on an already-empty field. Added T-new-6/7/8 to feature-wip-state.sh covering the new resolution paths.

### FEATURE: issue-close-finalize: fail-soft Step E so J/K always run (2026-05-22, c3bd6ec, #439)
Background: When Step E (doc-append + commit) failed due to a gh/network error, issue-close-finalize would abort before Step H (issue close), J (resolved-by sentinel), and K (WIP clear), leaving the issue in an inconsistent open state.
Changes: Step E failure now emits a warning and proceeds to Step G/H/J/K. End report includes Step E outcome line with /issue-reconcile backfill hint. Steps H/J/K remain mandatory regardless of Step E result.

### FEATURE: workflow-init Path A1 template: add Accepted Tradeoffs section and fix Issue omission (2026-05-22, c3bd6ec, #431)
Background: The Path A1 intent.md template lacked the ## Accepted Tradeoffs section required by the detail-planner Approved Scope gate. The ## Issue section was conditionally omitted when title was unavailable, causing inconsistency with downstream planner expectations.
Changes: Added ## Accepted Tradeoffs placeholder to Path A1 template. ## Issue section is now always included; when title is unavailable, uses '#<N>: (title unavailable)' rather than omitting the section.

### FEATURE: issue-create: enforce canonical Background/Changes (or Cause/Fix) schema at issue creation time (2026-05-22, b035f98a7717d6504d14d86c1760da7512413b7b, #443)
Background: `/issue-close-finalize` の Step E は `bin/github-issues/issue-to-history.sh` 経由で `doc-append` を呼び、`bin/github-issues/lib/extract-field.sh` が issue body から canonical な 4 フィールド (`Background`, `Changes`, `Cause`, `Fix`) のみを抽出する。FEATURE/REFACTOR/BUGFIX 系は `Background` + `Changes` を、INCIDENT 系は `Cause` + `Fix` を必須とする。これらが空だと `doc-append` がエラーで停止し、issue close の Step E がブロックされる。 しかし `/issue-create` は issue 作成時にこの canonical schema を検証・強制しない。結果として close 直前まで schema 不一致に気付けず、close 中に retroactive な `gh issue edit` が必要になるケースが発生する。 これは #401 (受信側 `issue-to-history.sh` で graceful fallback) の対称解。送信側の予防策として独立に検討する価値があり、両方採用すれば 2 重防御になる。
Changes: - `/issue-create` 実行時に body をパースして canonical フィールドの有無を検査する - type ラベル (`type:task` / `type:incident`) に応じて要求フィールドセットを切り替える:   - `type:task` (FEATURE/REFACTOR/BUGFIX): `## Background` と `## Changes` 必須   - `type:incident`: `## Cause` と `## Fix` 必須 - 不在時は (a) hard fail で作成中止 / (b) warn + 自動補完テンプレ挿入 / (c) AskUserQuestion で確認、のいずれかをポリシーとして決める - `extract-field.sh` と同じ正規表現 (H2/H3/inline、case-insensitive) で検証することで SSOT を守る ## Repro 1. 既存 issue #430 は `## Context` / `## Gap 1` / `## Gap 2` / `## Out of scope` のみで作成されていた 2. PR マージ後 `/issue-close-finalize --from-session` → Step E で `Error: requires --background and --changes` で停止 3. `gh issue edit 430 --body-file` で `## Background` + `## Changes` を retroactive に追加して復旧 ## Out of scope - `extract-field.sh` 側の挙動変更 — #401 で別途検討 - close path のフォールバック (canonical 不在時に `# H1 title` を使う等) — root cause は作成時の検証不在なのでそちらで直す - `history.md` への retroactive 整合性チェック (既存 entry の品質監査) — 別 issue 候補 Related to #401

### FEATURE: reduce clicks: CONFIRM_*=off suppresses VS Code file open; Proceed closes plan tab (2026-05-22, 10bac0b58bb1de31707ad0e1de89ae61dff5d250, #445)
Background: ワークフローの planning pipeline（intent/outline/detail.md 確認フロー）で 2 点のクリック数削減が求められている。 ## Problem 1: Proceed 後にプランファイルを VS Code で自動クローズ - 「進んでよろしいですか」ダイアログで **Proceed** を選択した後、VS Code に開かれたプランファイル（intent/outline/detail.md）を手動で閉じる必要がある。 - Revise 選択時は閉じない（修正中にファイルを参照できるようにする）。 - VS Code CLI にはファイルを閉じるコマンドが存在しないため、実現手段を outline-plan 段階で検討する。 ## Problem 2: `CONFIRM_*=off` のとき VS Code でファイルを開かない - `CONFIRM_DETAIL=off` にしているのに detail.md が VS Code に開かれてしまう（同様に CONFIRM_OUTLINE / CONFIRM_INTENT も）。 - 修正対象: `hooks/show-plan-link.js` で対応する `CONFIRM_*` フラグを読み取り、off なら `openInVsCode()` をスキップする。 - チャット内の systemMessage ブレッドクラム（「Plan file written: ...」）は引き続き表示してよい。 ## Constraints - VS Code CLI 制約: 問題1 の実現手段は調査が必要。 - `skills/_shared/confirm-plan.md` の Steps 1+2 は `CONFIRM_*` に関わらず必須とされているが、問題2 では Step 2 の VS Code open 部分のみ条件付きにする。 - #425 (open) と同クラスの問題: 衝突確認が必要。 <!-- auto-created by clarify-intent -->
Changes: ワークフローの planning pipeline（intent/outline/detail.md 確認フロー）で 2 点のクリック数削減が求められている。

### FEATURE: Split docs/history.md into its own commit even when closes_issues is empty; use WORKTREE_NOTES.md as input source (2026-05-22, 4375d5b0197d06cada4214fe6ccdf17f9f978255, #436)
Background: Split docs/history.md into its own commit even when closes_issues is empty; use WORKTREE_NOTES.md as input source
Changes: ENFORCE_WORKTREE=on の下では、`closes_issues` が空のセッション(= GitHub Issue を閉じない通常タスク)は次の path を通る:

### FEATURE: feat(migrate-repo): commit + push Step 1/3 artifacts as part of the migration flow (2026-05-22, 599bc63ccc7c0d0a96bf33b6b692088b683f0cb7, #446)
Background: feat(migrate-repo): commit + push Step 1/3 artifacts as part of the migration flow
Changes: `/migrate-repo` produces five generated/modified files during Step 1 and Step 3, but leaves them uncommitted:

### FEATURE: feat: session-scoped WORKFLOW_ENFORCE_WORKFLOW_OFF/ON sentinel (2026-05-22, 6a802594d2a8f3eb539cbfd9ffc42833e6287748, #453)
Background: feat: session-scoped WORKFLOW_ENFORCE_WORKFLOW_OFF/ON sentinel
Changes: 軽微なメンテ（private repo の .env commit、1ファイル変更など）で、10段階 workflow の全センチネルを立てる必要があり、かつ block-dotenv.js / scan-outbound.js 等

### FEATURE: workflow-init: #N 不在時に issue 自動作成が走らないことがある (2026-05-22, 0e800c4c43c3c47a022caa250ab83599dfcb060c, #449)
Background: `/workflow-init` はユーザー入力に `#N` が含まれない場合、Path C にルーティングされ `/clarify-intent` を起動する。`/clarify-intent` の Completion Step 3 は `closes_issues: (empty)` の場合に `gh issue create` を実行して tracking issue を自動作成する設計になっている。 ## Observed セッション `20260522-193351`（feature/cc-restart-required → PR #447）で、ユーザー入力に `#N` が含まれず Path C にルーティングされたが、`gh issue create` が走らず `<session-id>-intent.md` の `## closes_issues` が `(empty)` のまま workflow が完走、PR #447 が tracking issue なしでマージされた。 ## Expected ユーザー入力に `#N` が含まれないあらゆる workflow 開始で、tracking issue が自動作成され `closes_issues` に反映される。手動介入なしに「全 workflow が必ず issue を持つ」状態を担保する。 ## Related - PR #447 (cc-restart-required, merged) — このギャップが顕在化したセッション - `skills/workflow-init/SKILL.md` Path C - `skills/clarify-intent/SKILL.md` Completion Step 3
Changes: `/workflow-init` はユーザー入力に `#N` が含まれない場合、Path C にルーティングされ `/clarify-intent` を起動する。`/clarify-intent` の Completion Step 3 は `closes_iss

### FEATURE: Label auto-management setup for non-agents repos (2026-05-22, 32c404995e53609f50c433111fd3ca19ee3aae18, #283)
Background: Label auto-management setup for non-agents repos
Changes: Spin-off from #279 (label auto-sync added to agents repo).

### FEATURE: backfill-commit-comments.sh: refine Tier 1.5 to avoid bulk-import-commit collision (sub-issue of #222) (2026-05-22, 32c404995e53609f50c433111fd3ca19ee3aae18, #344)
Background: #316 で Tier 1.5 (`git log --all --reverse -S "<title>" -- docs/history.md docs/history/`) を導入したことで M1 dry-run の hash discovery カバレッジは 3.3% → 100% に到達した（161/188 backfill対象 + 27 skipped, no-hash ゼロ）。 しかし dry-run 結果を精査すると **同じ hash に多数の issue が attribute される collision** が発生している: | Hash | 件数 | 正体 | |---|---|---| | `3969773` | 12〜39件（再計測で揺れあり） | `feat(agents-split): add 39 tests and docs from dotfiles (step 11)` — dotfiles から history.md を bulk import した commit | | `feedbac` | 5件 | （要確認） | | その他 2件ペア | 数組 | （要確認） | 原因: Tier 1.5 は `git log --all --reverse -S "<needle>"` で最古の commit を返す。bulk import commit には 60+ 件分の history entry が一度に追加されるため、これらすべての issue について同じ import commit に解決されてしまう。 各 issue の **実際の修正 commit** ではないため、`Resolved by commit 3969773` が posted されると誤った帰属となる。 ## Scope `bin/github-issues/backfill-commit-comments.sh` の Tier 1.5 を以下のいずれか/組合せで改良: ### 案 A: import commit ブラックリスト - 既知の bulk import commit（`3969773` 他）をハードコードで除外 - マッチした場合は次の最古 commit にフォールバック、なければ next tier - **Pro**: 実装最小、即効性高 - **Con**: 将来の bulk import commit を都度追加する必要あり ### 案 B: needle 精緻化 - 現状の `sed -E 's/^[A-Z]+: //'` で除いた後の title が短い/汎用的すぎる場合のフォールバック - entry 全体（heading + Background の1行目）を needle に使うことで一意性を高める - **Pro**: より精度高い - **Con**: heading 自体が import 時から変わっていれば miss する可能性 ### 案 C: 「複数 issue に同じ hash が attribute された場合」を検出して no-hash に降格 - dry-run pass で hash → issue list を集計 - 閾値（例: 3件以上が同じ hash を持つ）超過なら、その hash を信用しない - **Pro**: 自動検出、blacklist 不要 - **Con**: 2-pass 構成になり実装複雑度↑ ### 案 D: heuristic「同 commit で N entries 追加なら import」 - `git show --stat <commit> -- docs/history.md docs/history/` で entry 追加数を測定 - 5件以上 = import commit と判定し除外 - **Pro**: 自動、汎用 - **Con**: rotation commit などとの誤判定可能性 ## Immediate hotfix (already applied in this session via separate PR) 判別ロジック改良までの繋ぎとして、`3969773` をハードコードで blacklist する小修正を別 PR で先行投入予定（本 issue とは別、即座の M2/M3 進行のため）。本 issue ではより根本的な改良を扱う。 ## Acceptance Criteria - 任意 1案以上を実装 - M1 dry-run で hash collision が大幅減少（同 hash 3件以上が無くなる、または明示的に正当な共有のみ） - 既存 R-series テスト緑、新規 collision-detection テスト追加 ## Out of Scope - M2 canary / M3 full run（hotfix 入手段階で実施済みの想定） - import commit のハードコード追加（hotfix 側で対応） ## References - 親 issue: #222 (GitHub migration) - 前段: #316 (Tier 0a/0b/1.5 導入) — closed at bec10e1 - 実装ファイル: `bin/github-issues/backfill-commit-comments.sh` - 観察された collision sample: `3969773` (12〜39件), `feedbac` (5件)
Changes: しかし dry-run 結果を精査すると **同じ hash に多数の issue が attribute される collision** が発生している:

### FEATURE: workflow: clarify-intent / outline 段階に 'class enumeration' を必須化する (2026-05-23, da5e41f5ee19b35b24dc6aca8067e41b920ed598, #462)
Background: `/clarify-intent` と `/make-outline-plan` の survey 段階は現状「症状ベース」で、修正対象の単一インスタンスに focus する。しかし症状が属する **クラス全体の sibling 列挙** が手順として組み込まれていないため、core-principles.md §1 "Elevate Perspective" の anti-pattern (case A を fix して symmetric B/C/D を放置) を踏むことが頻発する。 ### 直近の実例 (PR #452 / #371) 「gh issue create が main worktree から block される」(#369) を解決しようとして、class = **classify() の false-positive 群** を構成する sibling が連鎖発覚した: - `gh issue create --body "##..."` (heredoc/quoted body) → #371 で fix (in scope) - `bash <wrapper>.sh ...` (skill 経由の wrapper コマンド) → #460 残存 (Group A override の網から漏れた) - `grep -nE "...>..." file` (generic command quoted regex) → #460 残存 - `$(... 2>/dev/null)` (command substitution closing paren) → 既存 #359 — sibling として認識されていたが scope に入れず - `<<WORKFLOW_ENFORCE_WORKTREE_OFF>>` sentinel marker → #461 残存 (sanctioned escape hatch 自体の劣化) - multi-line `gh issue create \` (line-continuation 形式) → 本 issue 起票時に発覚した **未起票の追加 sibling**: Group A override が multi-line cmd で機能しない可能性 #369 観測 (1) 修正時点で class の sibling 全列挙を行っていれば、scope 判断 (fix in scope / track separately / wontfix) を user と事前合意でき、PR merged 後の後発発覚と段階的な芋づる露呈を防げた。 ### Root cause - survey-code は「コードの構造」を見るが「症状のクラス」を網羅しない - survey-history は git/issue 履歴を見るが「クラスのメンバ」として整理しない - clarify-intent には class 識別 + sibling 列挙の問いが組み込まれていない - outline-planner subagent prompt にも sibling 列挙の要求がない - intent.md / outline.md の Output Schema に "Class members" 欄がない
Changes: - `skills/clarify-intent/SKILL.md` に "Class identification" 質問を追加:   - 「このバグ/タスクは何のクラスのインスタンスか?」   - 「そのクラスの他のメンバはどこに存在しうるか? (実装側 dispatch site grep + 入力空間)」   - 「既存 issue/PR に類似クラスの記述があるか? (クラス名でのキーワード検索)」 - `skills/make-outline-plan/SKILL.md` の outline-planner subagent prompt に "list class siblings" 要求を追加 - intent.md の Output Schema に `## Class members` セクションを必須化。各メンバごとに `fix in scope / track separately / wontfix` を user と合意して記録 - outline.md の Output Schema にも同等の引き継ぎ欄を追加 (Considered alternatives と並ぶ位置) - 影響範囲:   - `skills/clarify-intent/SKILL.md`   - `skills/make-outline-plan/SKILL.md`   - `agents/outline-planner.md`   - `tests/feature-clarify-intent-*.sh`, `tests/feature-make-outline-plan-*.sh`   - `rules/core-principles.md` (§1 の手順化を skill 連動で記述) ## Non-goals - 既存の全 skill/CLI を遡及 audit する (本 issue は intent/outline stage の procedure 整備のみ) - detail-planner 段階での重複 check (本 issue scope 外、必要なら別 issue) Related to: - #457 (sibling: 実装時の universality 規約 — 本 issue は planning 時の class enumeration を補完) - #369 (parent incident — 観測 (1) のみで scope 設定した実例) - #371 (Group A fix — class の 1 member のみ修正) - #460 (class の他 member 残存) - #461 (sanctioned escape hatch の class 漏れ)

### FEATURE: install: dotfileslink misses review-loop-cap-menu and review-skill-size launchers (2026-05-23, e9b156b8e91fe3bad7a0b4a07df0718d4159cf3c, #471)
Background: During `/make-detail-plan` cap-menu dispatch, `review-loop-cap-menu` failed with `command not found` (exit 127). The binary exists at `bin/review-loop-cap-menu` in the agents repo but is not on PATH. Root cause: neither `install/win/dotfileslink.ps1` nor `install/linux/dotfileslink.sh` generates a launcher for `review-loop-cap-menu` or `review-skill-size`. They only handle `doc-append`, `doc-append-plain`, `review-code-codex`, `review-plan-codex`. Callers invoke them bare (not via `$AGENTS_CONFIG_DIR/bin/...`): - `skills/_shared/cap-menu-dispatch.md:30` — `menu_json=$(review-loop-cap-menu ...)` - `CLAUDE.md:34` (Step 6) — `review-skill-size --base <merge-base>` So any user without manual PATH augmentation hits exit 127 in the cap-menu dispatch / size-check workflow steps. Related to #244 (same install-launcher gap category; doc-append-plain POSIX side).
Changes: - Add launcher generation for `review-loop-cap-menu` and `review-skill-size` in both `install/win/dotfileslink.ps1` and `install/linux/dotfileslink.sh`, mirroring the `review-code-codex` / `review-plan-codex` pattern (cmd + bash shim on Windows; symlink on Linux). - Audit other `bin/` entries against bare-name invocations under `skills/` and `CLAUDE.md` so every bare-called bin has an installer launcher (orthogonality — close the class, not just the case). ## Repro 1. Fresh checkout of agents repo, run `install/win/dotfileslink.ps1`. 2. New shell: `which review-loop-cap-menu` → not found. 3. Run a `/make-*-plan` flow that reaches cap-menu dispatch → exit 127. Related to #244

### FEATURE: Skill 整理: /boost 削除と /resume-session 新設 (2026-05-23, d48ff7ce8f1066a1da36ae862f86c61542d61fbe, #468)
Background: Skill 整理: /boost 削除と /resume-session 新設
Changes: skill 系の小さい改良 2 件:

### FEATURE: workflow-gate: docs step should recognize WORKTREE_NOTES.md ## History Notes / ## Changelog Notes as docs evidence (post-#436 residual gap) (2026-05-23, 912da07601d7f2621948dc7c02db7ff15b8f30cd, #484)
Background: Under `ENFORCE_WORKTREE=on`, `/update-docs` stages history/changelog entries into `WORKTREE_NOTES.md` `## History Notes` / `## Changelog Notes` (gitignored) instead of writing `docs/history.md` / `CHANGELOG.md` directly. The deferred write happens during `/worktree-end` Step 6i via `bin/compose-doc-append-entry` (introduced by #436 / PR #474). However, `workflow-gate.js` (and `workflow-mark.js` around line 527) still requires staged `docs/*` or `*.md` evidence to pass the docs gate, and explicitly rejects `MARK_STEP_docs_complete`. Result: installer-only or code-only fixes that legitimately have no `docs/` or root-level `*.md` change cannot satisfy the docs gate under worktree mode, even though `/update-docs` did its job by staging into `WORKTREE_NOTES.md`. Repro: PR #483 (fix #471, merged 2026-05-23) was an installer-only fix; `/update-docs` staged bullets into `WORKTREE_NOTES.md`; commit blocked at docs gate. Workaround applied: artificial 1-line `README.md` update. This is the natural next residual gap after #436 — the new staging surface is invisible to the gate.
Changes: Under `ENFORCE_WORKTREE=on`, teach `workflow-gate.js` to accept a non-empty `## History Notes` or `## Changelog Notes` section in `<worktree>/WORKTREE_NOTES.md` as valid docs evidence. Read-only check (no staging required, since the file is gitignored). Acceptance: - Installer/code-only fixes that update `WORKTREE_NOTES.md` notes sections pass the docs gate without an artificial doc edit. - Existing `docs/*` / `*.md`-staged evidence still passes. - Empty notes sections (default placeholder bullet) do not satisfy the gate. - `ENFORCE_WORKTREE=off` path is unchanged (still uses `doc-append` directly).

### FEATURE: fix: restore code plan-file open with --folder-uri workspace targeting (counter-patch #454, closes #291) (2026-05-23, 866fec01a7be9fd3f0a9a283efee70d8db95ff09, #486)
Background: PR #454 (merged 2026-05-22, closes #445) removed `code -r` auto-open from `hooks/show-plan-link.js` unconditionally to satisfy #445's intent of suppressing VS Code file open when `CONFIRM_*=off`. The removal was over-broad — even when `CONFIRM_*=on` (default), intent/outline/detail.md no longer appear in any VS Code window; only the URL breadcrumb shows in the main conversation. The user has no convenient way to view the plan file before Proceed/Revise. In parallel, #291 (OPEN since 2026-05-14) describes that `code -r` without workspace targeting routes to the most recently focused VS Code window — not the originating workspace — when multiple VS Code windows run workflow sessions. Since #454 removed `code -r` entirely, #291 is temporarily moot, but any restoration of `code` invocation must address #291 at the same time or the bug returns.
Changes: Restore VS Code file open as a forward-patch (not a revert of #454), addressing #291 at the same time. ### Modified - `hooks/show-plan-link.js`: restore `code` spawn for VS Code file open, gated by `CONFIRM_*=on` (preserves #445's intent that off-mode does not open files), VS Code environment detection (`TERM_PROGRAM === "vscode"` or `CLAUDE_CODE_ENTRYPOINT === "claude-vscode"`), and `SHOW_PLAN_LINK_NO_AUTO_OPEN=1` opt-out. - Pass `--folder-uri` (or equivalent workspace-targeting argument) to address #291 — open in the VS Code window for the originating workspace, not the most recently focused window. - Apply to all 3 plan artifacts (intent.md / outline.md / detail.md — sibling class members handled by the same hook). - Windows CVE-2024-27980 workaround: `spawn("cmd.exe", ["/d","/s","/c","code",...])` on Windows. - `SHOW_PLAN_LINK_NO_SPAWN=1` + `SHOW_PLAN_LINK_MARKER_FILE` test bypass to be restored. - `skills/_shared/confirm-plan.md`, `.env.example`, `docs/architecture/claude-code/settings.md` updated. - Tests in `tests/feature-show-plan-link.sh`: code spawn + window-targeting cases. ### Explicitly NOT changed - `hooks/show-diff.js` diff suppression for `CONFIRM_*=off` (introduced in #454) — kept as-is to minimize the counter-patch. - `hooks/lib/plan-confirm-flag.js` (introduced in #454) — reused as-is for `CONFIRM_*` SSOT. - No `git revert` of #454 — forward-patch only, so downstream history (#462 etc.) is not disturbed. ## Closes - #291 — multi-VS-Code-window routing addressed by `--folder-uri` ## Related - #445 (closed by #454) — original `CONFIRM_*=off` suppression goal, preserved - #425 (open) — write-tests `CONFIRM_TESTS=OFF` (not in scope) <!-- auto-created by clarify-intent — session 20260523-133235 -->

### FEATURE: bug: outline opens in wrong VS Code window when multiple windows run workflow (2026-05-23, 866fec01a7be9fd3f0a9a283efee70d8db95ff09, #291)
Background: When two VS Code windows are open simultaneously and each runs a workflow session (for different repositories), the plan file (e.g. outline) meant for one window opens in the other. The `code` command routes to the most recently focused VS Code instance rather than the workspace that originated the request. ## Steps to Reproduce 1. Open VS Code window A (repo: `agents`) 2. Open VS Code window B (repo: another repo) 3. Start a workflow session in both windows simultaneously 4. Observe: window A may receive the outline file intended for window B ## Desired Behavior Each workflow session's plan files open in the VS Code window that corresponds to the originating workspace / repository. ## Notes - Root cause likely: `code` without `--folder-uri` or `--reuse-window` picks the most recently active window - Fix candidates: pass `--folder-uri "$PWD"` to target the correct workspace, or use `code -r` with the workspace path - Related: the environment detection issue (#290) addresses the non-VS Code case; this issue focuses on multi-window VS Code disambiguation --- *Auto-created by clarify-intent — session 20260515-021319*
Changes: When two VS Code windows are open simultaneously and each runs a workflow session (for different repositories), the plan file (e.g. outline) meant for one window opens in the other. The `code` command

### FEATURE: fix(migration): create-project.sh が Projects v2 を repo に link しない (2026-05-23, b8d71bb7dc6885b60e3fc8d9f2f274e38d1c21fb, #488)
Background: `/migrate-repo` 実行後、対象 repo の `/projects` ページに作成済みの Projects v2 ボードが表示されない。 具体例: - my-private-repo migration board (Project #2, `PVT_kwHOAMF_jc4BYZ4b`) は user-scoped (https://github.com/users/nirecom/projects/2) として作成されている - `gh api graphql '{ repository(owner:"nirecom",name:"my-private-repo"){ projectsV2(first:10){ nodes{ number title } } } }'` は空配列を返す → repo link 0 件 - 結果として https://github.com/nirecom/my-private-repo/projects には何も表示されない - agents repo の Project #1 も同じ状態 原因: [bin/github-issues/migration/create-project.sh](../blob/main/bin/github-issues/migration/create-project.sh) は `createProjectV2` mutation で user-scoped board を作成するが、`linkProjectV2ToRepository` mutation を呼んでいない。GitHub の Projects v2 は user/org-scoped であり、repo の `/projects` ページに表示するには明示的な link mutation が必要。
Changes: 1. `create-project.sh` の Project 作成/再利用直後に `linkProjectV2ToRepository` mutation を追加    - 新規作成パス・既存再利用パスの両方で実行 (idempotent: 既に link 済みなら no-op で済む)    - link 先 repo の node id は `gh api repos/<owner>/<repo> --jq .node_id` で取得 2. 既存の 2 つの board に対して 1 回限りの backfill を実行    - agents #1 → nirecom/agents    - my-private-repo #2 → nirecom/my-private-repo 3. `.migration-state.json` に link 済みフラグを persist して再実行時のスキップ判定に使う (検討) ## Related - #295 — gh CLI scope audit (関連: Projects v2 周辺の setup 漏れ) - #406 — create-project.sh の別バグ (closed; 参考)

### FEATURE: workflow: granular plan-skip sentinels (WORKFLOW_OUTLINE_NOT_NEEDED / WORKFLOW_DETAIL_NOT_NEEDED) for trivial fixes (2026-05-23, 5aecc52f1fc14b513ec9a4b9d11966262a62ffb2, #485)
Background: The current workflow has a single coarse-grained plan-skip sentinel: `WORKFLOW_PLAN_NOT_NEEDED`. For trivial fixes (1-file installer additions, typo fixes, copy-paste-style changes), running the full three-stage planning pipeline (intent → outline → detail) is disproportionate overhead — but skipping it entirely with `WORKFLOW_PLAN_NOT_NEEDED` is also wrong, because intent capture still adds value (it records `closes_issues`, scope, and acceptance criteria). In practice CC ends up running the full pipeline even for trivial fixes because there is no fine-grained skip handle.
Changes: Introduce sub-stage skip sentinels: - `WORKFLOW_OUTLINE_NOT_NEEDED: <reason>` — skip `/make-outline-plan` when only one obvious approach exists. - `WORKFLOW_DETAIL_NOT_NEEDED: <reason>` — skip `/make-detail-plan` when the outline already pins down file-level changes. Both must be **ask-gated** (require user approval, not auto-allow) so CC cannot self-emit them silently. Same gating posture as `WORKFLOW_PLAN_NOT_NEEDED`. Acceptance: - Each sub-stage sentinel passes its own gate independently; emitting one does not implicitly skip the others. - Reason field is mandatory (`: <reason>`); bare form rejected. - Telemetry / on-disk audit records which sub-stage was skipped and why. - `/clarify-intent` still runs in all cases (intent capture is non-negotiable). - Existing `WORKFLOW_PLAN_NOT_NEEDED` continues to skip both sub-stages together (backwards-compatible). ## Related - Use case: PR #483 (fix #471) was a 6-line-installer change where intent.md, outline.md, and detail.md were all generated for a one-line conclusion.

### FEATURE: workflow: support N issues per session — N history entries, 1 PR (revise one-issue-per-session principle) (2026-05-23, 40dab6945a86a69171c49e74aca051a8f51fdc30, #444)
Background: workflow: support N issues per session — N history entries, 1 PR (revise one-issue-per-session principle)
Changes: ワークフローの「1 session = 1 issue = 1 commit history = 1 PR」原則は、現状 \`skills/clarify-intent/SKILL.md\` step 1a に \"One issue per session\" として埋め込まれているの

### FEATURE: Group: worktree-end x CWD issues (2026-05-23, d70e2085c3e1e668c03efc33f294e026a3f65a8e, #296)
Background: Group: worktree-end x CWD issues
Changes: `worktree-end` の cleanup フェーズで、linked worktree のディレクトリを `git worktree remove` で物理削除しようとすると **Windows 環境で EPERM (Permission denied)** で失æ

### FEATURE: /resume-session: findWorktreeEndMarker fails to detect existing pending-cwd-unlock marker from main worktree CWD (2026-05-23, d70e2085c3e1e668c03efc33f294e026a3f65a8e, #489)
Background: PR #487 (/resume-session 新設) リリース直後に実地検証。`/worktree-end` が Windows EPERM で deferred-cleanup パスに入り、`~/.workflow-plans/worktree-end/` に有効な `pending-cwd-unlock-3ad563dbb2e32ad9--feature%2Fresume-session-468` および `...--feature%2F436-compose-doc-append` の 2 つの marker が書かれた状態で、新セッションを起動し agents main worktree (`/c/git/agents`) から `/resume-session` を実行した。 期待:`bin/resume-session-detect` が `{type:"worktree-end-resume", marker_path:...}` を返し、skill が `/worktree-end --resume` にディスパッチ。 実際: ```json {"type":"none","reason":"session_id_unresolved"} ``` `detect()` は `findWorktreeEndMarker()` を最初に走らせ、hit すれば即 `worktree-end-resume` を返す構造。`session_id_unresolved` まで落ちたということは marker scan が null を返している。 repo-id 3ad563dbb2e32ad9 は直前の `/worktree-end` セッションで `getRepoId("C:/git/agents")` から計算され、ファイル名と一致している。CWD は `/c/git/agents` (MinGW 表記の同じ main worktree)。 ## 疑わしい原因 1. **path 表記差による repo-id 不一致**(本命) — marker 書き込み時は backslash 化された `C:\git\agents` ベースで hash、detect 実行時の `process.cwd()` は `/c/git/agents` から `path.resolve` 経由で hash に渡されており、hash 入力が異なる可能性。既存 memory `feedback_worktree_end_marker_repoid.md` で「`path.resolve` が Windows で backslash を返すため repoId は `node -e "path.resolve(...)" | sha256sum` で計算」と既知の落とし穴。 2. `readdirSync` は成功したが case-insensitive 比較が miss — 双方 `.toLowerCase()` 通過するので unlikely。 3. `getWorkflowPlansDir()` が想定外の dir を返している — ありえなくはない。 #293 (CLOSED, branch-delete marker での同型 mismatch: writer が raw relative path、validator が resolved absolute path をハッシュ) と同根の可能性が高い。
Changes: - `bin/resume-session-detect` の `findWorktreeEndMarker()` での `getRepoId(process.cwd())` 呼び出しを、marker 書き込み側 (`skills/worktree-end` Step 6b の `getRepoId(<absolute-main-root>)`) と同じ path 正規化に揃える。 - もしくは marker storage スキーマを変更:hash 化された repo-id ではなく marker content (Line 2 の absolute worktree path) からの reverse-match で resolve する方向。後者は #293 系の構造的回避にもなる。 - 回帰テスト追加:Windows / Linux / macOS で marker を書いてから `process.cwd()` 由来の path で resolve できることを確認するテストを `tests/feature-resume-session-468.sh` に追加 (現在は `worktree-end-resume` パスは smoke only)。 - `feedback_worktree_end_marker_repoid.md` で示唆された normalization rule を `hooks/lib/` に共通関数として抽出し、SSOT 化することも検討。 ## Repro 1. Windows + VS Code + Claude Code、`ENFORCE_WORKTREE=on`。 2. `/worktree-start` で linked worktree を作成、何か edit して `/worktree-end`。 3. Step 6c で EPERM(VS Code 拡張ホストが worktree CWD を握っていれば容易に再現)→ deferred marker 書き込み。 4. VS Code を Reload せず別の Claude Code セッションを起動、agents main worktree から `/resume-session`。 5. detect 出力が `type:"worktree-end-resume"` ではなく `type:"none", reason:"session_id_unresolved"` になることを観察。

### FEATURE: skills: stage-1 context:fork + user-invocable:false (2026-05-23, 8248ef92519bdaa431a3593986e6cd186642ad02, #501)
Background: main conversation の context window が複雑な workflow で枯渇するのが早い。公式 frontmatter の `context: fork` (experimental, `CLAUDE_CODE_FORK_SUBAGENT=1` 必要) を活用し、main から見えなくてよい処理を fork 先に逃がしたい。同時に、user が直接叩く必要のない workflow 内部専用 skill には `user-invocable: false` を付与し、`/` 一覧から隠す。 frontmatter 編集のみで両 dimension を同一 PR で適用する。
Changes: ### `user-invocable: false` を付与 (workflow 内部呼び出しのみ) - `skills/write-tests/SKILL.md` - `skills/write-code/SKILL.md` - `skills/run-tests/SKILL.md` - `skills/update-docs/SKILL.md` (既存の false 4 件 — worktree-start, worktree-end, issue-close-stage, issue-close-finalize — は変更不要。`commit-push` は ENFORCE_WORKTREE=off で user が直接叩く運用のため例外的に true 維持。) ### `context: fork` を付与 — 段階 1 (非 sentinel / 非 interactive) A. 現在無指定で独立タスク型: - `skills/review-plan-security/SKILL.md` - `skills/create-key/SKILL.md` - `skills/migrate-repo/SKILL.md` - `skills/save-research/SKILL.md` (本文確認後) - `skills/update-infrastructure/SKILL.md` (本文確認後) - `skills/aws-scan/SKILL.md` / `aws-scan-apps` / `aws-scan-cost` / `aws-scan-resources` / `aws-scan-security` (本文確認後) B. 既に内部で subagent を spawn する形: - `skills/review-tests/SKILL.md` - `skills/refactor-prompts/SKILL.md` ## Out of scope - 段階 2 (sentinel emit 系: deep-research, run-tests, write-tests) は fork 内 Bash echo が main の workflow-gate hook に届くかの検証が未了のため別 issue - fork 不可 (AskUserQuestion / sentinel 中核): workflow-init, clarify-intent, make-outline-plan, make-detail-plan, worktree-*, issue-create, write-code, commit-push 等 - 既に Agent / Bash 化済み: review-code-security, review-code-codex, review-plan-codex ## Constraints - 修正は **frontmatter のみ** — Procedure / 本文は変更しない - 適用前に各 SKILL.md 本文を再確認し、sentinel emit / AskUserQuestion の見落としがないことを最終チェック --- *Auto-created by `/clarify-intent` (Path C) — session 75b0ee5b-9996-4f3e-bfae-6968d7066700*

### FEATURE: workflow-init: subagent artifact write failure & post-check verifies existence only (2026-05-23, 8b4e43bee4d0c37c4d4e2c73593e26f75ac631a7, #497)
Background: `workflow-init` Step 6 で並列起動する Agent subagent (`survey-code` / `survey-history` 等) が、SKILL.md 必須の artifact 書き込みを行わず、内容を agent return text として返してしまうパターンが繰り返し発生している。下流の `clarify-intent` / `make-outline-plan` は artifact を Read することを前提に設計されているため、planner が不完全な情報で判断を下す。 これは旧 #419 が報告した「subagent が `~/.workflow-plans/` への書き込みを断念しテキスト返却する」と同一現象。本 issue は #419 を統合し、post-check の検出漏れも合わせてスコープ化する (#419 はこの issue 作成後 `status:migrated` でクローズ依頼)。 ### 観測事実 (session-id: `75b0ee5b-9996-4f3e-bfae-6968d7066700`, 2026-05-23) | ファイル | 状態 | 中身 | |---|---|---| | `75b0ee5b-...-context.md` | ✅ 存在 | 正常 (orchestrator が書いた) | | `75b0ee5b-...-survey-code.md` | ✅ 存在 | **`test` 1行のみ** (プローブ書き込み) | | `75b0ee5b-...-survey-history.md` | ❌ 不在 | Write 呼び出しが一度も発火していない | 両 subagent の return text には**本来 artifact に書かれるべき調査結果がフルに含まれている**ため、内容生成ではなく**書き込み手段の判断ミス**が問題。 ### 根本原因 (推定) 両 subagent の出力を読むと: - `enforce-worktree.js` が Bash の書き込み系コマンドを全てブロックする挙動を認識 - Write ツールが `C:\Users\nire\.workflow-plans\` (= git repo 外) に対しては通る、という判定に**自信が持てず**ファイル書き込みを諦め、agent return text に本文を載せる代替モードに切り替えた - survey-code はプローブ目的で `test` だけ Write 試行 (成功) したが本体で上書きしないまま終了 - survey-history は Write 呼び出し自体に至らず `agents/survey-code.md` / `agents/survey-history.md` は Bash write block 時のフォールバック挙動が**明文化されていない**。 ### 副次バグ: post-check が中身を見ない `skills/workflow-init/SKILL.md` Step 6.5 の post-check は**ファイル存在のみ**を確認する仕様で、`test` 1行の survey-code.md は PASS してしまう。root cause が修正されても post-check の中身検証ギャップは独立して残る。
Changes: 修正は frontmatter ではなく本文修正のため、別途プランニング: 1. **subagent SKILL.md / agent.md の明文化** (`agents/survey-code.md` / `agents/survey-history.md` 等):    - Bash write block 時のフォールバック手順 = 「Write ツールで `artifact_path` に書く。git repo 外のパス (`~/.workflow-plans/` 等) は enforce-worktree を通すので疑わず実行する」を Rules セクションに明記 2. **post-check の中身検証** (`skills/workflow-init/SKILL.md` Step 6.5):    - ファイルサイズ最小値 (例: 200 bytes) **または** 必須セクション (`## Verified Claims`) の grep 存在チェックを追加    - 失敗時は `<<WORKFLOW_SURVEY_AGENT_FAILED: <name>>` を発行 (既存仕様と同じ fall-through) 3. **(オプション) orchestrator 側のリカバリ** (`bin/` 配下):    - 内容が薄いと判定したら subagent の return text から復元する CLI ### 関連メモリ - `feedback_cc_tool_env_var_handling.md` — subagent の実行制約に関する既知の落とし穴 - `feedback_verify_before_assert_or_act.md` — fact 主張前の検証 (今回エージェントが "write blocked" と判断する前の検証を怠った) - `reference_claude_protected_paths.md` — `~/.workflow-plans/` は全モードで安全 ### Supersedes - #419 (この issue で統合; 本 issue 作成後 `status:migrated` でクローズ依頼)

### FEATURE: show-plan-link: --folder-uri breaks file open in VS Code (regression from #490) (2026-05-23, 7b073d551ac254efaa08b3f7e8157a3a21c6a6d2, #506)
Background: Workflow を実行すると `Plan file written: ...` のブレッドクラムは表示されるが、intent/outline/detail.md のタブが VS Code に開かない。PR #490 (`fix(#486): restore code --folder-uri plan-file open; fix #291 multi-window routing`) で `--folder-uri` をマルチウィンドウルーティング対策として導入したが、ファイル open 自体が機能しなくなった。意図したケース（VS Code 環境 + `CONFIRM_*=on`）でブレッドクラム以外の効果がゼロになっている。 ### Reproduction (Windows 11, VS Code 1.121.0, `code .` from `C:\git\agents`) | # | Command | Result | |---|---|---| | 1 | `code --folder-uri file:///c:/git/agents -r <file>` | does not open | | 2 | `code -r <file>` | opens | | 3 | `code --folder-uri X --file-uri Y -r` | does not open | | 4 | `code --file-uri Y -r` | opens | `--folder-uri` を付けると VS Code CLI は folder open のみ処理し、ファイル open（positional file も `--file-uri` も）を黙って捨てる。引数順入替・ドライブ文字大小は無関係。 ### Affected hook `hooks/show-plan-link.js:67` `buildCodeArgs()` が `--folder-uri` を常に先頭に挿入。 ### Test gap `tests/fix-restore-code-folder-uri.sh` は `SHOW_PLAN_LINK_NO_SPAWN=1` で spawn 引数の文字列のみ検証しており、実際に VS Code が file を開くかを検証していない。E2E で「タブが実際に開く」確認が抜けている。
Changes: `hooks/show-plan-link.js` の `--folder-uri` 戦略を見直し、file open が機能する形に変更する。 ### Fix direction candidates (plan 段階で決定) - (A) `--folder-uri` を撤回して `code -r <file>` 単体に戻す。`#291` のマルチウィンドウ問題は別アプローチで再検討（#291 reopen）。 - (B) 2段階 spawn: 先に `code --folder-uri X`（フォーカス）、続けて `code -r <file>`（open）。 - (C) `-r` のみで last active window に開く方針で確定し、マルチウィンドウは別 issue 化。 ### Related work - #486 — PR #490 が close したが本件で実質未解決 - #291 — マルチウィンドウルーティング、PR #490 で同時 close したが (A)/(C) 採用時は再オープン候補 - #492 — `workspaceFolderUriFrom` URI encoding（同じ `--folder-uri` 経路。本件で `--folder-uri` を撤回する場合は wontfix の可能性） Related to #492 Related to #492

### FEATURE: show-plan-link: workspaceFolderUriFrom lacks URI encoding (2026-05-23, 7b073d551ac254efaa08b3f7e8157a3a21c6a6d2, #492)
Background: Surfaced by codex review on PR #490 (follow-up to #486 / #291). `hooks/show-plan-link.js` `workspaceFolderUriFrom()` builds `file://` URIs by raw string concatenation: ```js const fwd = cwd.replace(/\/g, "/"); return fwd.startsWith("/") ? "file://" + fwd : "file:///" + fwd; ``` Paths containing spaces, `#`, `%`, `?`, non-ASCII characters, or UNC/network forms (`\server\share`) produce invalid or misinterpreted `file://` URIs. This breaks the `--folder-uri` workspace matching introduced in PR #490, which was supposed to fix multi-window mis-routing (#291).
Changes: - Fix `workspaceFolderUriFrom()` to URI-encode the path portion (per-segment `encodeURIComponent` preferred — `encodeURI` leaves `?`/`#` unescaped, both valid in filenames but URI-reserved). - Handle UNC paths (`\server\share` → conventional `file://server/share/...` form, not the current 4-slash output). - Add test coverage in `tests/feature-show-plan-link.sh` for: spaces, `#`, `%`, non-ASCII, UNC. Current PR #490 tests cover only ASCII forward-slash paths.

### INCIDENT: #3: gh issue create が main worktree から block される (memory との不整合) (2026-05-23, a44c8c9423200ac242a85dd30157d98045d6b676, #369)
Cause: hooks/lib/bash-write-patterns.js は Group A の gh 調整コマンド (pr/issue/repo create/edit/close/comment) を quoting-only パターン (here-doc/here-string/pwsh-here) のみがマッチしたとき read として override する。しかし body 内に posix-redirect 相当 (例: '>' '>>') や他の非 quoting パターンに当たる文字列が含まれると override 条件 matchedNames.every(QUOTING_ONLY) が false になり、classify が write を返す。enforce-worktree.js 1330 行で main worktree からの write は block される。memory entry project_enforce_worktree_gh_scope.md は「gh issue create / comment / edit は main worktree から実行可」と記載していたが、body content によっては実際は block される。
Fix: いずれかの方向で要決定 (本 issue では実装せず別 session で対処): (1) Group A コマンドの override 条件を緩和して body content のパターンを無視する (--title/--body などの flag 引数として認識した token を stripped にする stripGhArgs) (2) ドキュメント・memory を実態に合わせて「body 内容次第で block される」と修正 (3) Bash 直接呼び出しでなく gh API 呼び出し用 wrapper script を main worktree 用に提供。再現条件: 今 session で issue #368 を作成しようとした際、body に Markdown 表記/コード例を含んでいた gh issue create が ENFORCE_WORKTREE block された (`<<WORKFLOW_ENFORCE_WORKTREE_OFF: ...>>` 発出で回避)。関連: #368 (autonomous /loop incident — 本 issue は #368 の作成過程で発覚した副次問題)。 --- Fix task: #371 (sub-issue) — 具体的修正案 (heredoc body を Group A コマンドで strip して write-pattern 検査から除外する) と root cause の精緻化。

### FEATURE: WIP flag: extend set/check to all closes_issues (2026-05-23, 23d893bec4b6312de03b0ae8dcac3921308a8b45, #508)
Background: N-issues-per-session support (PR #498) intentionally limited WIP flag to the primary issue only. However this creates an asymmetry: `intent:clarified` label is applied to all `closes_issues`, while WIP fingerprint is keyed to the primary only. ## Scope Extend WIP flag operations to cover all `closes_issues`: - `skills/clarify-intent/SKILL.md` Completion: loop `wip-state set` over all `closes_issues` (not just `[0]`) - `skills/workflow-init/SKILL.md` Step 3a:   - WIP check (conflict detection): check all N, show single prompt listing conflicted issues   - Resume path (WIP==none + intent:clarified): set WIP for all N   - Conflict-Continue path: set WIP for all N - `rules/github-issues.md`: update WIP primary-only description ## Constraints - Error handling: best-effort per-N (same as label application) - WIP check conflict: if any N has "other", single AskUserQuestion listing all conflicted N; Continue overrides all; Abort emits WORKFLOW_ABORTED_WIP_CONFLICT - No change needed: `clarify-intent` Path C (always single N), `issue-close-finalize` Step K (already loops all N via --from-session) - Tests: update W1-W4 (clarify-intent) and C13a-C13d (workflow-init) <!-- auto-created by clarify-intent -->
Changes: N-issues-per-session support (PR #498) intentionally limited WIP flag to the primary issue only. However this creates an asymmetry: `intent:clarified` label is applied to all `closes_issues`, while WI

### FEATURE: bug: write-tests skill ignores CONFIRM_TESTS=OFF and still pauses for review at step 7 (2026-05-23, 03cde1f03d7e289aaa58da61e0ecce3813da976f, #425)
Background: bug: write-tests skill ignores CONFIRM_TESTS=OFF and still pauses for review at step 7
Changes: `write-tests` skill が `CONFIRM_TESTS=OFF` 設定下でも step 7 (Present final test file content for review) でユーザー承認待ちのポーズを入れてしまう。CONFIRM_TESTS フラグã

### FEATURE: bug: write-code skill ignores CONFIRM_CODE=off and pauses after step 6 (2026-05-23, 03cde1f03d7e289aaa58da61e0ecce3813da976f, #472)
Background: After completing the `/write-code` skill with `CONFIRM_CODE=off`, Claude stops and emits a completion summary instead of automatically proceeding to Step 6 (run-tests + security reviews). User had to ask 'ここで止まれと言われた?' to surface the behavioral error. ### Observed in session session-id: 20260523-015103 (issue #436 implementation) After 10 source files were edited successfully by the write-code subagent, Claude posted a completion summary listing the edited files and paused — instead of auto-proceeding to Step 6. This is the same pause-when-CONFIRM-off pattern as #425 (write-tests skill, Step 7), but in /write-code Step 6. #425's '関連 skill' section explicitly predicted write-code as one of the likely recurrences. ### Why this is wrong `skills/write-code/SKILL.md` does not instruct Claude to wait or to announce completion as a stop point: - Step 2 (CONFIRM_CODE gate): explicit OFF path proceeds without confirmation. - Step 6: 'Present the final edited file list + skipped-check notes + scope-expansion notes to the user.' - Completion section: 'None.' CLAUDE.md workflow Step 5 → Step 6 implies auto-progression. The user has a standing preference (recorded in memory) that CONFIRM_*=off must auto-proceed at every review point without pausing. ### Root cause hypothesis Step 6's 'Present the final edited file list to the user' is ambiguous — same wording-pattern problem as #425's analysis of write-tests Step 7. 'Present' is being read as 'pause for review' rather than 'emit summary and proceed'.
Changes: Tighten Step 6 wording, parallel to the #425 proposal for write-tests: - 'When CONFIRM_CODE=on, present the edited files for review and wait for approval. When CONFIRM_CODE=off, emit a one-line summary and auto-proceed to Step 6 (run-tests + security reviews) without waiting.' Alternatively, replace 'Present' with 'Emit summary and proceed' to eliminate the pause ambiguity entirely. A shared helper (`skills/_shared/pause-if-confirm-on.md`, mentioned as option 2 in #425) would address both this issue and #425 together. ## Related - #425 — same root cause in write-tests skill (Step 7); this issue is the empirical confirmation of #425's '関連 skill' prediction for write-code Related to #425 Related to #425

### FEATURE: make-outline-plan: approach 間の差分が delivery topology のみのとき自動選択する (2026-05-23, 03cde1f03d7e289aaa58da61e0ecce3813da976f, #420)
Background: make-outline-plan: approach 間の差分が delivery topology のみのとき自動選択する
Changes: `make-outline-plan` の Step 7 ([skills/make-outline-plan/SKILL.md:138-154](skills/make-outline-plan/SKILL.md)) は、`CONFIRM_OUTLINE=ON` のとき outline-planner が提案した複数 approach を 

### FEATURE: worktree-end Step 7 Final Report が表示されない事象の根本対策 (2026-05-24, 5c1016c6ca0e416fd07b50f33c47b3da47b048cf, #504)
Background: #405 で `bin/worktree-final-report.js` を導入し worktree-end Step 7 のテンプレ化を行ったが、実運用では Final Report がテンプレ通り出力されないケースが頻発している。 ### 確認された原因 1. **環境変数の引き継ぎ失敗(最頻)**    - Step 7 の renderer 起動は 10個の env var (`PR_NUMBER` / `PR_TITLE` / `PR_URL` / `PR_STATE` / `BRANCH` / `WORKTREE_PATH` / `CREATED_DATE` / `BACKUP_MANIFEST_PATH` / `BRANCH_DELETED` / `CLAUDE_CODE_RESTART_REQUIRED`) を1コマンド前置で渡す前提    - Windows の Bash tool は CWD と env がコマンド間でリセットされることがある(既知; `cc_tool_env_var_handling`)    - Step 5 → 5.5 → 6 → 7 の長い経路で1つでも欠けると renderer 出力は `(none)` だらけになる    - 結果として model は手書き Markdown にフォールバックすることが多い 2. **CLI 強制が無い(silent fallback)**    - `/run-tests` には `<<WORKFLOW_MARK_STEP_run_tests_complete>>` のような sentinel があるが Step 7 には無い    - env 欠落・MODULE_NOT_FOUND 等で renderer が落ちても誰も検知しない 3. **Step 7 に到達しない経路**    - Step 6c.1 deferred-fork(Windows CWD lock)→ Stop で Step 7 未実行    - `--resume` パスが Step 7 を再実行する保証が SKILL.md に無い 4. **main 未取得**    - PR merge 直後の main worktree が未 pull で `bin/worktree-final-report.js` が未配置のケース → MODULE_NOT_FOUND
Changes: 改善案(優先順): | 案 | 効果 | コスト | |---|---|---| | A. renderer stdout 末尾に `<<WORKFLOW_MARK_STEP_final_report_complete>>` を出させ、skill 側で sentinel 未出 = fail とみなす | silent fallback を検出可 | 小 | | B. Step 5/5.5 末尾で env を `<plans>/<sid>-final-report-env.json` に書き、Step 7 はそれを読むだけにする | env 散逸を構造的に排除 | 中 | | C. `--resume` / deferred-fork 復帰時にも Step 7 を必ず走らせる分岐追加 | 抜け穴を塞ぐ | 中 | | D. Step 7 冒頭で renderer 未配置を検出し `git pull --ff-only` 1回試行 | main 未取得問題を救済 | 小 | ### 提案 (A) + (B) を組み合わせて構造的に対処するのが本筋。(C)(D) は別 issue に分割可能。 ### 関連 - #405 (Final Report テンプレ化の本体) - #494 (--resume 経路で Step 6i/7 が再生されない問題と隣接) Related to #494

### FEATURE: workflow-mark: session-scoped ENFORCE_WORKTREE_OFF sentinel produces no marker file (2026-05-24, 9d69f6bb1259f0e56d90458c8c0a1e4e84518230, #461)
Background: `<<WORKFLOW_ENFORCE_WORKTREE_OFF: <reason>>>` sentinel は worktree.md でセッションスコープの enforcement bypass として sanctioned されているが、実環境で marker file (`~/.workflow-plans/<session-id>.worktree-off`) が作られず、後続の write が引き続き block される。 ### 再現 main worktree から: ``` echo '<<WORKFLOW_ENFORCE_WORKTREE_OFF: temporary off for X>>>' ls ~/.workflow-plans/*.worktree-off   # 期待: marker が見える / 実際: No such file # 続けて write 系コマンド → 依然 ENFORCE_WORKTREE で block される ``` ### 環境 - session-id: 8f27a0e9-30af-4ae5-a294-ae8ba8797888 (本セッション) - VS Code 拡張版 Claude Code on Windows - agents repo: PR #452 merged main 時点 - `echo $CLAUDE_ENV_FILE` → empty (Bash tool context) ### 仮説 (Root cause) `hooks/workflow-mark.js` L640 付近: ``` if (enforceOffMatch) {     if (!sessionId) {       messages.push("workflow-mark: could not resolve session_id — ENFORCE_WORKTREE override NOT applied.");       continue;     } ``` `sessionId` の解決元は: 1. `input.session_id` — hook stdin JSON 2. `resolveSessionId()` — `CLAUDE_ENV_FILE` から `CLAUDE_SESSION_ID=` を読む (`hooks/lib/workflow-state.js`) このセッションでは Bash tool context で `$CLAUDE_ENV_FILE` が empty。hook context でも同様に empty なら `resolveSessionId()` は null を返す。Hook stdin の `input.session_id` も渡されていない (or 渡されているが他の条件で skip されている)。 結果として L640 で `continue`、marker file は書かれない。stderr の `messages` は user/Claude の Bash tool result に surfaced されないため、failure はサイレント。 ### Impact - worktree.md で sanctioned されている「セッションスコープ escape hatch」が機能しない - 本来の sanctioned path (issue 起票や緊急復旧用 main worktree write) が hook の本問題によって塞がれたとき、`.env` 編集 / 別 worktree 作成しか手段がない - 直近事例: #460 起票時に `/issue-create` の sanctioned path が `>` 文字 false-positive で block (#460 の症状そのもの) → sentinel で迂回を試みたが、本 issue により sentinel も機能不全 → user に web UI 起票を依頼 (sanctioned path の二段階劣化) - Silent failure である点も問題: marker 作成失敗時に user / Claude に可視のエラーが返らない
Changes: - 原因切り分け:   - `hooks/workflow-mark.js` の sentinel handler 内 `sessionId` 解決ロジックを debug log で確認 (Hook stdin JSON 内の session_id propagation、`CLAUDE_ENV_FILE` 解決経路)   - `resolveSessionId()` の fallback (e.g. `transcript_path` から session id を導出) を追加検討 - 失敗時の可視化:   - `messages` を stderr に最終 flush するだけでなく、hook が hard fail で stop に分岐する閾値 (例: marker 書き込みのみ失敗時) を `additionalContext` 形式で surface   - sentinel emit 直後に Claude が marker 存在を polling できる sanctioned API を導入してもよい (worktree.md にドキュメント化) - 影響範囲: `hooks/workflow-mark.js`、`hooks/lib/workflow-state.js` (resolveSessionId 強化)、`tests/feature-workflow-mark.sh` 系、`rules/worktree.md` (失敗時の手順を明文化) Related to: - #369 (observed during follow-up issue creation for #369 obs (2)) - #460 (the very bug that required this sentinel to be functional) - Anthropic bug #27987 (`$CLAUDE_SESSION_ID` propagation issue — the root motivation for the marker-based bypass)

### FEATURE: worktree-end: retire pending-branch-delete- marker — replace with direct git worktree list check (2026-05-24, 79c96d54fe1c6dfd4c948954d266388790ab6785, #503)
Background: `pending-branch-delete-` markers (written by `worktree-end` Step 6b, read by `enforce-worktree.js` `isAllowedBranchDeleteViaMarker`) gate `git branch -D <branch>` from the main worktree. PR #499 introduced `/sweep-worktrees` to reclaim zombie worktrees and their branches via the sweeper, making the marker's purpose partially redundant. Codex review (PR #499) flagged a HIGH-severity cross-repo deletion risk: `sweep-worktrees.sh`'s orphan-marker pass decodes only the branch suffix from the marker filename and ignores the embedded repo-id. A stale marker from repo A can authorize `git branch -D` in repo B if they share $HOME/.workflow-plans. The correct fix: check `git worktree list --porcelain` directly to confirm a branch is not checked out in any worktree. No marker needed. This also makes #226 (session-binding enhancement for the same marker gate) obsolete. The `pending-cwd-unlock-` marker family was retired in PR #499. `pending-branch-delete-` is the remaining marker family. ### Additional scope: orphan-directory sweep PR #499 マージ直後の実地検証で発覚：Windows の `git worktree remove` は非アトミックで、以下の中間状態が残ることがある — registered=false (registry から除名済み) かつ物理ディレクトリは残存 (空またはほぼ空)。 シーケンス: 1. `git worktree remove` が worktree 内 `.git` テキストファイルを先に削除 2. ディレクトリ本体削除が EPERM 失敗 (VS Code 拡張ホストの CWD lock 等) 3. `/worktree-end` Step 6c.1 が WARN 出して 6d 以降をスキップ 4. その後の `git fetch --prune` 等の implicit prune が、無効化済み admin 領域を回収 → registry から除名 結果: `git worktree list` には現れないが、`WORKTREE_BASE_DIR` 配下に物理ディレクトリだけ残る zombie。現在の `sweep-worktrees.sh` は `git worktree list --porcelain` のみを起点に走査するため、この zombie は完全に不可視。 実例: PR #499 自身の worktree `C:/git/worktrees/sweep-worktrees-pr1/agents` が、マージ直後にこの状態で残った。
Changes: - Remove `worktree-end` Step 6b (marker write) and Step 6g (marker delete); remove MARKER_PREFIXES.BRANCH_DELETE / getMarkerPath branch-delete usage - Add `isAllowedBranchDeleteWhenNotCheckedOut(cmd, repoRoot)` predicate to `enforce-worktree.js`: runs `git worktree list --porcelain` and allows `git branch -D <branch>` only when no worktree lists that branch as HEAD - Remove `isAllowedBranchDeleteViaMarker`, `isAllowedMarkerDelete`, `isMarkerFilePath` predicates from `enforce-worktree.js` - Remove orphan-marker 2nd pass from `sweep-worktrees.sh` (targets `pending-branch-delete-` only; now handled inline by the new predicate) - Update `skills/worktree-end/SKILL.md` to remove Steps 6b/6g - Update tests: replace marker-gate tests with worktree-list-based tests ### Changes (additional scope: orphan-directory sweep) - `sweep-worktrees.sh` に orphan-dir scan pass を追加: `WORKTREE_BASE_DIR` 配下の `<task-name>/<repo-name>/` パターンに合致するディレクトリを列挙し、`git worktree list --porcelain` に存在しないものを orphan 候補とする - orphan 候補に対する 4-AND safety gate:   1. パスが `WORKTREE_BASE_DIR` 配下に正規化解決される (path traversal 防御)   2. ディレクトリ内に `.git` ファイル/ディレクトリが存在しない (生きた worktree との誤認防止)   3. mtime > `--min-age-hours` 閾値 (短時間内に作られた手動ディレクトリ誤削除防止)   4. 中身が空、または `WORKTREE_NOTES.md` のみ (未コミット作業ファイルなし) - 既存の `hooks/cleanup-orphan-dir.js` が orphan-dir 削除のロジックを既に持っているため、sweeper から再利用 (重複実装しない) - `--apply` 時のみ実削除、dry-run では `would remove orphan dir: <path>` を出力 - summary に `orphan_dirs_removed: N` カウンタを追加 - Codex review HIGH (PR #499) 指摘の cross-repo 安全性: orphan-dir には repo-id 情報がないため、`WORKTREE_BASE_DIR` 配下 + `<repo-name>` が現在の repo 名と一致する場合のみ対象とする (誤って他リポジトリのものを掃除しない) 本拡張は marker 機構廃止と直交。marker 廃止後も「worktree 撤去後に物理ディレクトリだけ残る Windows ケース」は引き続き発生するため、orphan-dir sweep は独立した安全網として残る。 Related to #226 (supersedes its session-binding enhancement — marker removal makes the gate itself unnecessary)

### FEATURE: subagent: detail-planner / survey-code still attempt Bash heredoc writes to ~/.workflow-plans/ despite #507 (2026-05-24, bb653ece88bbca846c915f92769e177b297d85be, #516)
Background: PR #510 セッション (#369/#460/#359 修正) 進行中に観測。detail-planner および survey-code subagent が plan artifact / survey artifact を ~/.workflow-plans/ 配下に書き込む際、Write ツールではなく Bash heredoc / redirect (cat heredoc-then-redirect-to-file / printf-quoted-then-redirect-to-file) を使用し、enforce-worktree.js (main worktree からの Bash write block) にブロックされた。 PR #507 (#497 を close) で subagent prompt に Write ツール使用の明示が入ったはずだが、本セッションでは fix 後も同一現象が再発した。 ## 影響 - subagent の artifact 書き込みが完了しない → orchestrator が代行する必要があり摩擦 - 失敗ケースでは artifact 不在 / stub 内容のまま downstream skill に渡される (#497 の root cause と同じパターン) - enforce-worktree.js のブロック自体は正しい動作 (Bash redirect は main worktree write; ~/.workflow-plans/ も Bash 経由では同様)。issue は subagent 側の行動指示不足にある ## 想定される原因 1. PR #507 の明示が detail-planner.md / survey-code.md agent ファイルまで届いていない (workflow-init や clarify-intent の SKILL.md にのみ追加された可能性) 2. subagent prompt の指示が抽象的で、Bash heredoc が暗黙のデフォルトになっている 3. ~/.workflow-plans/ の書き込み可否について subagent が誤った安全側判定 (Bash 経由を試して fallback) を行っている memory feedback_cc_tool_env_var_handling.md / feedback_verify_before_assert_or_act.md でも触れられている既知パターン。
Changes: 調査範囲: 1. agents/detail-planner.md, agents/survey-code.md, agents/survey-history.md, agents/outline-planner.md 等の subagent 定義ファイルに、PR #507 で追加された Write tool 必須指示が全て反映されているか確認 2. 不足している箇所に明示的な Rule を追記 (Write ツールを使う / Bash heredoc は block される / artifact 書き込み不能ならエスカレーション) 3. 回帰テスト: subagent が Write ツールを実際に呼ぶことを確認する E2E テスト ## Acceptance - detail-planner / survey-code / survey-history 含む全 plan-pipeline subagent が ~/.workflow-plans/ への書き込みに Write ツールを使う - Bash heredoc / redirect を試行しない (テストで検証) - orchestrator が artifact 書き込みを代行する必要がなくなる ## Related - #419 (CLOSED, status:migrated to #497) - #497 (CLOSED, fixed by #507) - PR #507 (subagent artifact write 指示追加; 本 issue は fix 後の再発を扱う) - memory feedback_cc_tool_env_var_handling.md, feedback_verify_before_assert_or_act.md, reference_claude_protected_paths.md

### FEATURE: mid-workflow /issue-create を主フロー化し WORKTREE_NOTES 遅延起票を廃止 (2026-05-24, 96eb6b92508de34ba01880655339c24a2a7b26c8, #521)
Background: WORKTREE_NOTES.md への遅延起票パターンは INCIDENT #3（#369）で `gh issue create` が linked worktree からブロックされていた時期に設計された。PR #371・#460 により技術的ブロックは解消済みだが、CLAUDE.md に矛盾が残っている: - L6: "Mid-workflow follow-up issues: use `/issue-create`"（即時） - L112-125: "append a bullet to `WORKTREE_NOTES.md`"（遅延） セッション実行時間の増大により、mid-workflow で見つかった課題が worktree-end まで起票できず即時性が失われる。 #376（mid-workflow 呼び出し時の warning）のスコープも本 issue に統合。 ## Scope 1. **CLAUDE.md L112-125** — mid-workflow finding capture の主フローを即時 `/issue-create` に変更。WORKTREE_NOTES.md は fallback として維持 2. **worktree-end Step 5.5(a.5)** — "fallback path" と明記 3. **issue-create SKILL.md**（#376 統合）— mid-workflow コンテキスト検出 + `closes_issues` 非追加の警告ダイアログ追加 ## Constraints - 1 session = 1 issue 原則（rules/github-issues.md）不変: mid-workflow 起票 issue は closes_issues に追加しない - WORKTREE_NOTES.md sections および worktree-end Step 5.5 は fallback として維持 - 非インタラクティブ（subagent/loop）では warning のみ表示して続行 ## Related - Closes #376 — mid-workflow /issue-create warning（スコープ統合） - #369 — INCIDENT #3（技術的ブロック、修正済み） <!-- auto-created by clarify-intent -->
Changes: WORKTREE_NOTES.md への遅延起票パターンは INCIDENT #3（#369）で `gh issue create` が linked worktree からブロックされていた時期に設計された。PR #371・#460 によりæ

### FEATURE: /issue-create: mid-workflow 呼び出し時に warning を出す (2026-05-24, 96eb6b92508de34ba01880655339c24a2a7b26c8, #376)
Background: /issue-create: mid-workflow 呼び出し時に warning を出す
Changes: 現セッションに intent.md が既に存在する状態(= mid-workflow)で `/issue-create` を呼ぶと、新規 issue は作成されるが現セッションの `closes_issues` には追加され

### FEATURE: worktree-end Final Report: restart/install 要否を分類して常に定型表示 (2026-05-24, 880cb0d3d6a9c686a36352131f1aeaaf44211551, #518)
Background: worktree-end Step 7 の Final Report では、現状 `CLAUDE_CODE_RESTART_REQUIRED` という単一フラグのみが `### Claude Code Restart Required` セクションに表示される(#405 / #504 で導入)。 実運用上 2 つの課題がある: ### 課題 1 — 表示が抜ける/自由形式にフォールバックする #504 で sentinel (`<<WORKFLOW_MARK_STEP_final_report_complete>>`) と env-file 化によって構造的対策が打たれたが、それでもなお: - restart フラグが内部的には立っているのに、ユーザーへの最終出力に restart セクションが現れない - ユーザーが「再起動必要?」と明示的に聞くと初めて「はい、必要です」と答える というケースが頻発する。**最終回答メッセージで必ず定型ブロックを出す**ことを構造的に強制したい。 ### 課題 2 — 「再起動」が一段では不十分 現状は "Claude Code restart" 単一軸だが、実際には粒度・対象の異なる複数の「再適用が必要な操作」が混在する。それぞれ**独立した required / not required フラグ**を持つべきで、互いに包含関係や深刻度の順序があるわけではない。 例として現時点で思いつくものを挙げる(網羅でも順序でもない — 設計時に**整理して増減する**前提): - **VS Code の reload / restart** — VS Code 拡張・keybindings.json・VS Code 側 settings の変更時 - **Claude Code の restart** — CLAUDE.md / rules/ / settings.json の `model`/`outputStyle` 等(現状の `CLAUDE_CODE_RESTART_REQUIRED`) - **OS reboot** — registry 変更、システム PATH 変更等 - **対象 repo の installer 再実行** — agents repo の `./install.ps1` / `./install.sh`(settings.json 再構成・symlink 再作成等を伴う) 上記 4 つは「いずれかが必要」ではなく**それぞれ独立**で、たとえば「VS Code reload は必要だが Claude Code restart は不要」「installer 再実行のみ必要」といった組合せが起こりうる。 また 4 つに限定する意図はない: - 他にも該当する操作カテゴリがあるかもしれない(例: docker compose の `up -d --build`、外部サービスの再認証 等) - 逆に整理した結果カテゴリが減るかもしれない(例: VS Code reload と Claude Code restart を「セッション層 restart」に統合する等)
Changes: 方向性: - Final Report 上に「実施後に必要な追加操作」を**カテゴリごとに独立した required / not required フラグで一覧表示する**セクションを設ける - セクションは**全カテゴリ no の場合でも省略せず常に表示**("何も必要ない" ことが明示される) - 各カテゴリの判定は `skills/worktree-end/lib/detect-restart.sh`(または分割した複数 lib)で行い、`yes` 時には「なぜ」(検出されたファイル/設定)を併記 - 何か 1 つでも `yes` の場合は Final Report 直後に**強調表示の定型サマリブロック**を model が必ず復唱するよう SKILL.md に明示 具体的なカテゴリ分けと flag 命名は**設計フェーズで確定**する。出発点は上記 4 例を叩き台とするが、設計時に下記の観点で再整理してよい: - カテゴリ統合(本質的に同じ層の restart を 1 つに束ねる) - カテゴリ追加(現時点で漏れている操作) - カテゴリ削除(検出も意味付けも困難なものは外す) 最終的には「ユーザーが merge 後に何をすればよいか」が定型ブロックを読むだけで完全に判断できる状態を目指す。 ## Constraints - 既存 sentinel `<<WORKFLOW_MARK_STEP_final_report_complete>>` と env-file 経路は維持 - detect-restart.sh は fail-safe(gh 失敗時は "no")を維持 - 出力フォーマットを変える場合、`tests/fix-worktree-end-git-pull.sh` や renderer のテストに追随が必要 - 既存の `CLAUDE_CODE_RESTART_REQUIRED` env var との後方互換方針は設計時に決める(deprecate / マッピング / 廃止のいずれか) ## Accepted Tradeoffs - 自動判定が困難なカテゴリ(OS reboot 等)は手動 env のみで対応し、自動判定は将来課題とする選択肢を許容 - 「常に全カテゴリ表示」により全 no のケースでも数行のノイズが増えるが、"何もしなくてよい" ことの明示価値を優先 ## Related - #405 (Final Report テンプレ導入) - #504 (Final Report 不表示の根本対策 — env-file + sentinel 化)

### FEATURE: cleanup(settings.json): audit and remove dead path-based allow/deny rules (2026-05-25, af0aff113272a1f828f87faade90409fa3118855, #254)
Background: Empirical results (Phase 1 diagnostic, 2026-05-13) and the 2026-05-04 history entry show that `settings.json` path-based allow/deny rules for `Read|Write|Edit|Grep|Glob` **do not reliably work in the VS Code Claude Code extension**. Terminal CLI (Mac/Linux) behaviour is unverified, but the hook-based mechanism (`block-dotenv.js`) works portably on all clients. The `settings.json` file therefore contains many path-based rules that may be either: - (a) Dead everywhere (already covered by a hook), or - (b) Effective on some clients but not VS Code, or - (c) Effective everywhere (Bash-pattern rules) ## Scope ### Audit table Enumerate every path-bearing `Read|Write|Edit|Grep|Glob(<glob>)` rule in `settings.json` (allow + deny) and classify each: - Hook coverage status (which hook, if any, enforces it) - VS Code extension status (effective / dead / unknown) - Terminal CLI status (effective / dead / unknown) - Action: remove / migrate to hook / keep with note Initial enumeration (non-exhaustive): | Rule | Hook coverage | VS Code | Term CLI | Action | |---|---|---|---|---| | `Read(**/.env.example)` allow (× 4 safe variants) | `block-dotenv.js` (silent for safe variants) | dead | likely effective | TBD: migrate safe-variant approval into hook? | | `Grep(**/.env.example)` allow (× 4 safe variants) | same | dead | likely effective | TBD | | `Read(**/.env)` deny (× 8 .env*) | `block-dotenv.js` blocks | redundant | redundant | remove | | `Grep(**/.env)` deny (× 8) | `block-dotenv.js` blocks | redundant | redundant | remove | | `Edit(**/.env)` deny (× 8) | `block-dotenv.js` blocks | redundant | redundant | remove | | `Write(**/.env)` deny (× 8) | `block-dotenv.js` blocks | redundant | redundant | remove | | `Read(**/.claude/plans/**)` allow | none | dead | likely effective | TBD | | `Write(**/tests/**)` allow + Edit | `block-tests-direct.js` (gatekeeper) | dead | likely effective | TBD | | `Write(~/.claude/plans/drafts/**)` allow (× 6 variants) | none → after #253 PR, covered by `approve-plan-writes.js` | dead | likely effective | remove after #253 PR ships | | `Read(~/.ssh/**)` deny | **none** | likely dead | likely effective | **CRITICAL** — add `block-credentials.js` hook? | | `Read(~/.gnupg/**)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.aws/**)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.azure/**)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.config/gh/**)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.git-credentials)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.docker/config.json)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.kube/**)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.npmrc)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.pypirc)` deny | none | likely dead | likely effective | **CRITICAL** | | `Read(~/.gem/credentials)` deny | none | likely dead | likely effective | **CRITICAL** | | `Edit(~/.bashrc)` deny (× 5 shell configs) | none | likely dead | likely effective | **CRITICAL** | | `Edit(~/.claude/projects/workflow/**)` deny + Write | none | likely dead | likely effective | **CRITICAL** — workflow state integrity | | `Edit(**/history.md)` deny + Write | none | likely dead | likely effective | **CRITICAL** — append-only enforcement | | `Write(**/.git/info/pending-branch-delete)` allow | none | dead (per Phase 1) | likely effective | TBD: check who actually writes this file | ### Verification protocol Before removing any deny rule, verify (manually or via E2E test): - On VS Code Windows: confirm the deny is currently NOT being enforced (Read/Edit/Write succeeds, possibly with a prompt). - On terminal CLI: confirm the deny IS being enforced (Read/Edit/Write fails outright). ### Migration plan For each `CRITICAL` row above, decide between: 1. **Add a `block-<category>.js` PreToolUse hook** (preferred for security-critical rules) 2. **Accept VS Code unprotected state, document the gap** (last resort) ## Why follow-up (not bundled with #253's PR) - Security review needed for any deny removal — out of immediate scope. - Some rules may need new hooks (`block-credentials.js`?) — separate design work. - Removing dead allow rules is mechanical but should be batched after hook coverage is in place. ## Non-goals - Do NOT remove `Bash(...)` allow/deny rules (different mechanism, known to work). - Do NOT remove `WebFetch(domain:...)` / `WebSearch` / tool-name allowlist (different mechanism).
Changes: Parent: #253

### FEATURE: refactor(hooks): extract path-match lib for shared path-normalization logic (2026-05-25, af0aff113272a1f828f87faade90409fa3118855, #252)
Background: Three PreToolUse hooks currently duplicate path normalization (backslash → forward slash, split into segments, basename extraction): - `hooks/block-dotenv.js` (~360 lines) — security-critical: blocks `.env*` access with Bash-injection-aware command parsing. Uses inline `filePath.replace(/\/g, "/").split("/")...` at least 5 times. - `hooks/block-tests-direct.js` (~50 lines) — **gatekeeper** (not pure approve): when `write_tests` workflow step is `pending`, blocks main-conversation writes to `tests/` to force `/write-tests` invocation; otherwise approves. Uses inline path normalization at line 47. - `hooks/approve-plan-writes.js` (new in #253's PR, ~20 lines) — UX: approves Write/Edit under `~/.claude/plans/`. Uses inline normalization initially. Codex single-pass architecture review (2026-05-13) concluded that the right pattern is **shared lib + separate hooks**, not consolidation: > Keep `block-dotenv.js` separate because its threat model and complexity are materially different from the UX-only approve hooks; mixing credential-protection logic with convenience approvals would make review harder and raise the blast radius of future changes. ## Scope 1. **Extract shared lib** — new `hooks/lib/path-match.js` containing:    - Path separator normalization (backslash → forward slash, win32-safe)    - Basename extraction    - Path segment iteration helper (`getPathSegments`)    - Home directory expansion (`~` → `os.homedir()`)    - Optional: case-folding helpers for Windows drive letters 2. **Refactor existing hooks to use the lib**:    - `hooks/block-dotenv.js` — replace inline basename/path normalization (5+ sites) with lib calls. Keep all Bash parsing (TEXT_FLAGS, PATH_FLAGS, shell wrapper recursion, command substitution) untouched. Security-critical — diff review required.    - `hooks/block-tests-direct.js` — replace `isUnderTestsDir` inline normalization with lib `getPathSegments`. Keep gatekeeper logic (workflow state check) untouched. ## Why follow-up (not bundled with #253's PR) - Keeps the immediate fix focused on the user-visible bug (plans/ dialogs on Windows). - `block-dotenv.js` is security-critical — refactor deserves its own review window with full test coverage verification. - The lib API can evolve based on usage in `approve-plan-writes.js` first before broader migration. ## Non-goals - Do NOT consolidate Bash parsing into the lib (too dotenv-specific; security-critical and complex). - Do NOT merge approve hooks into a single dispatcher (Codex verdict explicitly rejected). - Do NOT rename `block-tests-direct.js` — original assumption that it was a pure approve hook was wrong; the file's gatekeeper role justifies the existing `block-` prefix.
Changes: Parent: #253

### FEATURE: fix: unify Projects board title to <repo> — Issue Timeline (2026-05-25, 19f025b842f925817e320fd6ed8e0db589c37c0c, #529)
Background: When migrating repos with `/migrate-repo`, `create-project.sh` creates GitHub Projects v2 boards with the title pattern `"$REPO_NAME migration"` (e.g. `my-private-repo migration`). This diverges from the agents repo convention of `agents — Issue Timeline`. ## Scope - `bin/github-issues/migration/create-project.sh` line 43 (search) and line 62 (create): change `"$REPO_NAME migration"` → `"$REPO_NAME — Issue Timeline"` - `tests/fixtures/migration/gh-mock.sh` line 101: change `"mockrepo migration"` → `"mockrepo — Issue Timeline"` - Rename existing project #2 (`my-private-repo migration` → `my-private-repo — Issue Timeline`) via GitHub GraphQL API ## Constraints - em dash is U+2014 (`—`) — not an ASCII hyphen <!-- auto-created by clarify-intent -->
Changes: When migrating repos with `/migrate-repo`, `create-project.sh` creates GitHub Projects v2 boards with the title pattern `"$REPO_NAME migration"` (e.g. `my-private-repo migration`). This diverges from

### FEATURE: wip-state.sh: CLAUDE_SESSION_ID が Bash subprocess に伝播せず WIP signaling が再び全停止 (regression of #440) (2026-05-25, 3e23f64c8d7a088faa3391a833f49ebd1b08db4b, #519)
Background: ### Symptom Projects v2 view (https://github.com/users/nirecom/projects/1/views/1) に `Status: In Progress` の issue が 0 件。実際は複数の作業中 issue (例: #503, #516) があるはずだが、いずれも `Status: Todo` のまま。 ### 観測 (2026-05-24, VS Code Claude Code セッション 058c78b2-906f-4866-8c1d-90f7d22314d8) `bash -x $AGENTS_CONFIG_DIR/bin/github-issues/wip-state.sh check 503` のトレース： ``` + load_env_file + . 'C:\git\agents/.env'           # WIP_STATE_*_ID は正常にロード + preflight_field_ids               # OK ++ resolve_session_id ++ '[' -n '' ']'                    # $CLAUDE_ENV_FILE 空 ++ '[' -n '' ']'                    # $CLAUDE_SESSION_ID も空 ++ echo 'Error: CLAUDE_SESSION_ID not resolvable (neither $CLAUDE_ENV_FILE nor $CLAUDE_SESSION_ID is usable)' ++ return 2 + exit 2 ``` Bash tool の直接実行と子プロセスでの env 比較： ``` # Bash tool 直接実行 CLAUDE_ENV_FILE= CLAUDE_SESSION_ID=058c78b2-906f-4866-8c1d-90f7d22314d8 # bash -c での子プロセス SUBSHELL CLAUDE_SESSION_ID= ``` → Bash tool の shell では `CLAUDE_SESSION_ID` は**変数として設定されているが export されていない**。`bash wip-state.sh` のような子プロセス呼び出しでは見えない。 ### Root cause #440 (CLOSED 2026-05-22) の修正は `resolve_session_id` に `${CLAUDE_SESSION_ID:-}` フォールバックを追加するものだった。当時の前提は「VS Code 拡張版 Claude Code は Bash subprocess に `CLAUDE_SESSION_ID` 環境変数は直接 export する」(#440 issue 本文より)。 しかし現在の claude-vscode バージョンでは `CLAUDE_SESSION_ID` は Bash tool の shell 変数としてセットされるのみで、export されないため子プロセスに伝播しない。結果として #440 のフォールバックも到達できず、`resolve_session_id` は rc=2 で失敗する。 `clarify-intent/SKILL.md:96-98` で "Exit 2 → warn and continue" と定義されているため、workflow は中断せず**全 session で WIP set が silent fail する**。 ### 影響 - `workflow-init` Step 3(a) の WIP check も同じ理由で rc=2 → 「advisory; warn-and-continue」扱いで並行 session 衝突検出が機能しない - `clarify-intent` Completion の WIP set 失敗で Projects v2 Status が `Todo` のまま - `issue-close-finalize` Step K の `clear` でも fingerprint clear で `no changes to make` 警告（fingerprint がそもそも書かれていないため） - **#508 (multi-N WIP loop, 2026-05-23 マージ済) の実装自体は正しいが、wip-state.sh が動かないため効果が出ない**
Changes: ### 修正方針 (案) `CLAUDE_SESSION_ID` の伝播に頼らない session-id 解決経路を追加する。候補： 1. `~/.claude/projects/<encoded-cwd>/` 配下の最新 `.jsonl` の basename を session-id として読む (#482 の recovery 手順と同じ経路) 2. Claude Code が必ず書き込む別のファイル/環境変数経由 3. Bash tool の wrapper で `export CLAUDE_SESSION_ID` を強制 (settings.json レベル？ — 要調査) 副次：`clear` の `--text ""` が `no changes to make` を返すケースを正常終了扱いにする (#440 にも提案あり、未実装)。 ### Test plan - `bash -c 'echo $CLAUDE_SESSION_ID'` が空を返すことを確認 (現状再現) - 修正後: 同じ Bash tool 経由で `wip-state.sh set <N>` が成功し、`gh issue view <N>` の Projects v2 Status が `In Progress` に遷移すること - 回帰: 従来の CLI Claude Code 経路 (`CLAUDE_ENV_FILE` only) でも動作継続 ### 関連 - #440 — CLAUDE_SESSION_ID フォールバック追加 (CLOSED — 今回のリグレッションの原因) - #482 — recovery 手順を docs/ops.md に追加 (OPEN — 本 issue 修正後は不要になる可能性あり) - #508 — multi-N WIP loop (CLOSED 2026-05-23 — 正しく実装されているが本バグのため効果が出ない) - #451 — clarify-intent SKILL.md の error hint 改善 (OPEN — 隣接トピック) - Upstream: anthropics/claude-code#27987 Related to #482 Related to #482

### FEATURE: ops: WIP state recovery procedure when CLAUDE_SESSION_ID propagation fails (2026-05-25, 3e23f64c8d7a088faa3391a833f49ebd1b08db4b, #482)
Background: `bin/github-issues/wip-state.sh set` can silently fail during `/clarify-intent` Completion when neither `$CLAUDE_ENV_FILE` nor `$CLAUDE_SESSION_ID` is available in the Bash subprocess env. The skill is documented to warn and continue (it does not abort the workflow), but the practical effect is that the Projects v2 Status remains `Todo` rather than flipping to `In Progress`, and no fingerprint is written — so subsequent `check` returns `none` and other sessions cannot detect the lock. Confirmed reproduction: session 20260523-102502 working on #444 — `clarify-intent` emitted the warning `[clarify-intent: wip-state set failed — Projects v2 Status not updated (exit 2: CLAUDE_SESSION_ID not resolvable)]`. After completing the planning stages, the issue's Projects v2 Status was still `Todo` even though `intent:clarified` was correctly applied. Root cause is the VS Code Claude Code extension environment failing to propagate `$CLAUDE_SESSION_ID` to Bash subprocesses (Anthropic bug anthropics/claude-code#27987 / locally tracked as #440). This issue is **not** about fixing the propagation bug itself (that is #440 / upstream anthropics/claude-code#27987, both already closed/being-addressed). It is about documenting the **manual recovery procedure** so that a user who notices the WIP flag is missing can repair the state without re-running the workflow. Related: #451 covers updating the in-skill error hint to mention the fallback — this issue covers the docs/ops surface. ## Symptom User observation: issue is being actively worked in a session but `gh issue view <N> --json projectItems --jq '[.projectItems[].status.name]'` returns `["Todo"]` instead of `["In Progress"]`. Search history.md / session warnings for the literal substring `wip-state set failed` or `CLAUDE_SESSION_ID not resolvable` to confirm this is the cause (vs. an unclarified intent). ## Recovery procedure (verified) 1. Locate the Claude Code session id. In VS Code extension environments this is the basename of the active session transcript at `~/.claude/projects/<encoded-cwd>/<session-id>.jsonl` (no extension). The encoded-cwd uses `--` in place of `:\` and `\` (e.g. `c--git-agents`). 2. Run `wip-state.sh set` with the session id inlined:    ```    CLAUDE_SESSION_ID=<session-id> bash "$AGENTS_CONFIG_DIR/bin/github-issues/wip-state.sh" set <N>    ``` 3. Verify with:    ```    gh issue view <N> --json projectItems --jq '[.projectItems[] | {title, status: .status.name}]'    CLAUDE_SESSION_ID=<session-id> bash "$AGENTS_CONFIG_DIR/bin/github-issues/wip-state.sh" check <N>    ```    Expected: Status `In Progress`; `check` prints `same`. ## Why this works `wip-state.sh` already supports `$CLAUDE_SESSION_ID` as a fallback when `$CLAUDE_ENV_FILE` is unreadable (see `resolve_session_id`, `bin/github-issues/wip-state.sh:102`). The inline form bypasses the propagation bug by injecting the variable directly into the child process environment.
Changes: - Add a new `## WIP state recovery` section to `docs/ops.md` containing the 3-step procedure above and a one-line pointer back to this issue. Place near other operational runbooks; do not duplicate into multiple files. - Cross-link: from this section, link out to #440 (root cause) and #451 (error-hint discoverability) so a reader following any chain ends up here for the recovery procedure. - Do **not** modify `wip-state.sh` itself — the propagation bug fix already shipped with #440. This issue is docs/ops only. - Do **not** modify `clarify-intent/SKILL.md` here — that surface belongs to #451. ## Related - #440 — wip-state.sh: VS Code Claude Code で CLAUDE_ENV_FILE 未伝播により session-id 解決が失敗 (CLOSED — added the CLAUDE_SESSION_ID fallback this procedure depends on) - #451 — clarify-intent/SKILL.md: CLAUDE_SESSION_ID fallback not mentioned in session-id error hint (OPEN — sibling, error-hint surface) - Upstream: anthropics/claude-code#27987 Related to #440 Related to #451

### FEATURE: /update-docs: auto-detect repo visibility and enforce language for WORKTREE_NOTES.md history/changelog entries (2026-05-25, d1c82d6ea2c9764f7cb2e49168d7aef17cde6309, #528)
Background: /update-docs (Path A, ENFORCE_WORKTREE=on) stages entries into WORKTREE_NOTES.md `## History Notes` / `## Changelog Notes` before /worktree-end appends them to docs/history.md and CHANGELOG.md. Currently the model is expected to self-police the language rule (history.md always English; public repo docs always English; private repo docs may be Japanese). In practice, Japanese entries appear in public repo WORKTREE_NOTES.md history/changelog sections frequently, only surfacing as a mismatch at /worktree-end Step 6i. rules/language.md spec: - history.md entries: always English regardless of repo visibility - public repo docs: always English - private repo docs: Japanese allowed (except history.md)
Changes: Add a repo-visibility check inside the /update-docs skill, triggered before any write to WORKTREE_NOTES.md ## History Notes or ## Changelog Notes. If the check returns public (or fails closed), enforce English. Enforcement candidates: (a) /update-docs skill self-check: call repo-visibility, fix draft language before writing (b) bin/compose-doc-append-entry lint: detect non-ASCII / Japanese characters in history/changelog sections and exit non-zero with a clear error message

### FEATURE: harness: enforce confirm-plan protocol via hook/code rather than prompt instructions (2026-05-25, ddcd7c4f0e7d194d00e4bc6224f9a61690ee3355, #524)
Background: `skills/_shared/confirm-plan.md` Step 2 specifies that after writing a plan artifact (intent.md / outline.md / detail.md), the **orchestrator MUST NOT emit any path representation** — no markdown link, no fullpath, no `file:///` URI, no relative/tilde path, no path appended to a Japanese sentence. The `show-plan-link.js` PostToolUse hook is the sole authoritative path surface and also routes the artifact to a matching VS Code window (#291). Step 3 specifies that under `CONFIRM_<STEP>=ON`, the orchestrator MUST call `AskUserQuestion` to ask "Proceed with this, or revise?" — not a plain-text question. Parent: #328 (URL/パス提示プロトコル umbrella). ## Observed violations (session 20260524-220446, clarify-intent for #254/#252) Recorded honestly without blame, to capture failure modes that prompt-level instructions did not prevent: 1. **Path representation emission** — after Write of intent.md, the orchestrator output both a markdown link `[20260524-220446-intent.md](...)` and the fullpath as plain text in the same response. Two distinct prohibitions in Step 2 violated simultaneously. 2. **AskUserQuestion bypass** — Step 3 confirmation was issued as a plain-text question ("承認 / 修正") instead of via the `AskUserQuestion` tool, breaking the structured-approval contract. Additionally, the VS Code preview did not open as expected, suggesting the `show-plan-link.js` hook may not have fired or its routing target was ambiguous (a separate `20260524-225717-intent.md` from another session appeared in the IDE instead). ## Hypothesis Prompt-level instructions in SKILL.md / shared protocols are insufficient to prevent these violations reliably. The protocol is correctly written and the LLM had access to it, yet the violations occurred. Stale memory entries (e.g. older `feedback_workflow_plans_link_not_clickable.md` advising fullpath text emission, which predated #291) compound the risk.
Changes: Move the enforcement surface from prompt instructions to code/hook so it is structurally impossible for the orchestrator to violate: 1. **Prevent path emission at the hook layer**: extend `show-plan-link.js` (or add a sibling PostToolUse hook) to inject the breadcrumb as the only path surface. Optionally, add a UserPromptSubmit / Stop hook that scans the assistant's pending response for absolute paths matching `<PLANS_DIR>/.*-(intent|outline|detail)\.md` and strips/warns when the show-plan-link breadcrumb is already present in the same turn. 2. **Force AskUserQuestion via tool gating**: when `CONFIRM_<STEP>=ON` is active and the artifact was just written, a hook could set a turn-scoped marker; the next assistant turn that ends without an `AskUserQuestion` call could be flagged via a Stop hook. 3. **Diagnose show-plan-link.js routing**: confirm the hook fires for every Write to `<PLANS_DIR>/<session-id>-*.md` and routes to the correct VS Code window when multiple sessions are open concurrently (relates to #291 and #486 history). ## Out of scope - Modifying upstream Claude Code permission matcher. - Changing the confirm-plan protocol itself (the protocol is correctly written — the gap is enforcement). ## Refs - `skills/_shared/confirm-plan.md` Steps 2 + 3 - `hooks/show-plan-link.js` (#275, #291, #486) - `hooks/show-diff.js` (PreToolUse diff preview) - Sibling closed issues under #328: #275, #291, #314, #318, #326, #486 - Parent architecture migration: #253
