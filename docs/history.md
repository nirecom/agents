# History (agents)

## Archived
- [2026](history/2026.md) — 103 entries

## Archived
- [2026](history/2026.md) — 105 entries

### FEATURE: Multi-N session: related issues should get equal treatment in Projects v2 and intent.md (2026-05-26, 5069d8cf343c9e6f7f21bb941343a67d3ae1d939, #548)
Background: The 1 session = N issues extension (#444) introduced a primary/related distinction that creates asymmetric treatment in two surface areas: - **Projects v2 card**: only the primary is registered via `gh project item-add`. Related issues are skipped. - **intent.md `## Issue` section**: only the primary appears here. Related issues are only listed under `## closes_issues`.
Changes: 1. **Projects v2 parity**: add every issue in `closes_issues` to the Projects v2 board, not just the primary. Include a bulk-registration path for issues that were added as related in past sessions and currently lack a board card. 2. **intent.md `## Issue` section parity**: surface all `closes_issues` entries under `## Issue` (or merge the two sections so `closes_issues` is the canonical list and every entry gets equal prominence). ## Related - N-issues extension: #444 (closed — this issue tracks the residual surface gaps)

### FEATURE: check-japanese-in-docs.js: false-positive when 'doc-append' appears in arguments of other commands (2026-05-26, 854f70699e1e1a2481b8afd28da02d83b332ffd0, #334)
Background: `hooks/check-japanese-in-docs.js` は doc-append の引数に日本語が含まれていないかをチェックする PreToolUse hook だが、scope 判定が `\bdoc-append\b` の正規表現で **Bash コマンド全体** を検査しているため、別コマンドの引数内に literal token `doc-append` が含まれるだけで誤発火する。 ## 再現例 (2026-05-16, #280 へのコメント投稿時) ```bash gh issue comment 280 --body "...skill 内で `doc-append` → `git commit` まで一気通貫で実行可能..." ``` このコマンドはコメント本文に `doc-append` という文字列を含むため、hook が「doc-append コマンドである」と判定し、本文中の日本語を検出してブロックした。実際には `gh issue comment` であり、`doc-append` CLI ではない。 ## Root cause `hooks/check-japanese-in-docs.js:27`: ```javascript if (!/\bdoc-append\b/.test(command)) approve(); ``` `\bdoc-append\b` はコマンド名の word-boundary マッチを想定したものだが、コマンド全体に対して検査されるため、引数内に同一トークンがあると hit する。 ## Scope - hook の judging を「コマンド自体が `doc-append` で始まる (もしくは `uv run` 経由で `bin/doc-append.py` を呼ぶ)」に限定する - 候補:   1. **コマンド先頭判定**: `command.trim().split(/\s+/)[0]` が `doc-append` か `uv` (かつ後続に `doc-append`) か判定する   2. **shell parse**: 軽量 tokenizer で実コマンド部分のみ抽出 (quoted arg は除外)   3. **環境変数前置を許容**: `FOO=bar doc-append ...` のような prefix を考慮 (現行の `strip-quoted-args.js` 系の知見を流用) ## Test coverage gap `tests/main-check-japanese-in-docs.sh` には word-boundary テスト (`some-other-command with Japanese`) はあるが、「別コマンドの **引数内** に `doc-append` 文字列が混入するケース」は未カバー。fix と同時に以下のような expect_approve ケース追加が必要: ```bash expect_approve "gh issue comment with 'doc-append' in body" \     '{"tool_name":"Bash","tool_input":{"command":"gh issue comment 1 --body \"see doc-append docs\" カタカナ"}}' expect_approve "echo with 'doc-append' string and Japanese" \     '{"tool_name":"Bash","tool_input":{"command":"echo \"doc-append のテスト\""}}' ``` ## Acceptance criteria - `gh issue comment` / `gh issue create` / `echo` / `git commit -m "..."` 等のコマンドが `doc-append` を引数内に含んでいても、日本語の有無に関わらず approve される - 実際の `doc-append` / `uv run bin/doc-append.py` 呼び出しに対する Japanese 検出は引き続き機能する (既存 14 ケースの retention) - テストに上記 false-positive ケースが追加されている ## Workaround (until fix) 引数内に `doc-append` を含む日本語コマンドを Bash 経由で実行する場合、`--body-file` 等で本文をファイル経由に逃がす: ```bash echo "本文..." > /tmp/body.md gh issue comment N --body-file /tmp/body.md ``` ## Refs - 観測: #280 への sub-issue 登録コメント投稿時 (2026-05-16) - 関連 hook: `hooks/check-japanese-in-docs.js` - 関連テスト: `tests/main-check-japanese-in-docs.sh` - 起源: #101 (origin of this hook)
Changes: `hooks/check-japanese-in-docs.js` は doc-append の引数に日本語が含まれていないかをチェックする PreToolUse hook だが、scope 判定が `\bdoc-append\b` の正規表現で **Ba

### FEATURE: make-outline-plan / make-detail-plan: pass survey artifacts & source to review-plan-codex (2026-05-26, 695aa7cdde647131ff745e6e5f8d57a3102547cd, #553)
Background: `bin/review-plan-codex` に渡される情報は現在: - `PLAN_CONTENT` (outline.md / detail.md) - `CONTEXT` (core-principles.md 等) - `ACCEPTED_TRADEOFFS_BLOCK` (intent.md の `## Accepted Tradeoffs` + `## Class members` を "SETTLED だから触れるな" と注釈付きで) 不足: - `survey-code.md` / `survey-history.md` (コード調査の根拠) - 実際のフックソースコード その結果、Codex はプランナーの「コード調査の結果」系の主張を検証できず、クラスメンバーの公称スコープだけを拠って "全て対等に扱え" と繰り返す傾向がある。 具体例: issue #334 outline レビューで、Planner の「5 hook 中 2 hook のみアンチパターン有」検出に対し、Codex が「全 5 hook を統一コントラクトに移行せよ」と `MISSING_ALTERNATIVE` を返し続けた。 ## Goal Codex がプランナーのコード調査系論拠を検証できるだけのコンテキストを渡す。ただし CC 側も Codex 側も context window を無駄にしない設計にする。
Changes: Approach candidates (詳細は /clarify-intent で議論): A. **Evidence-cited claims** — プランナーが outline.md に「Claim → 証拠(ファイル:行範囲)」の引用をつけ、その範囲だけ Codex に積んで渡す B. **survey を --context に追加** — `review-plan-codex` が outline 時に survey-code.md / survey-history.md も読んで連結 C. **Progressive disclosure** — Codex にファイルリストだけ渡し、必要な問を Claude 側に投げ返すラウンドトリップ構造 (設計コスト大) ## Acceptance criteria - review-plan-codex がコードベースの根拠にアクセスできる - プランナーが「コード調査で X だと判明」と書いた場合、Codex がその根拠を参照して verify できる - 追加のコンテキスト出入りが context window に悪影響を与えない (Outline レビュー 1 回に付き多くて 10 KB 程度目安) ## Related Issue #334 のワークフロー中に発見 Related to #470

### FEATURE: make-detail-plan: codex review context.md missing outline section (Section 2) (2026-05-26, 695aa7cdde647131ff745e6e5f8d57a3102547cd, #470)
Background: `skills/_shared/codex-review-loop.md` L39-41 specifies that the per-session context.md passed to codex must contain both intent.md (Section 1) and outline.md (Section 2) when both exist, separated by `---`. The builder was originally introduced in #200 (planner drafts persistence, intent+outline context merge). In a recent session (2026-05-22, PR #466 / issue #453), the actual `~/.claude/plans/drafts/<session-id>-context.md` contained Section 1 (intent) only — Section 2 (outline) was missing. As a result, codex received the detail-plan diff without the outline-approved scope context. **Concrete consequence in that session**: outline.md approved a 1-PR implementation, but detail.md unilaterally split the work into 3 PRs. Without the outline section in context.md, codex could not detect this outline-vs-detail deviation. The user had to catch it manually and revert to 1 PR.
Changes: - Audit the context.md builder (in `skills/_shared/codex-review-loop.md` and/or its implementation) to identify why Section 2 was missing despite the spec. - Fix the builder so context.md reliably contains both sections when both files exist, with the `---` separator as specified at L39-41. - Add regression tests: given intent.md + outline.md both present, the generated context.md contains both sections; given intent.md only, only Section 1 is included. - Cross-reference #200 in the implementation commit.

### FEATURE: update-docs: proposal step does not remind author to use English for history/changelog entries (2026-05-26, 48bef5a41c3a03c8d9bd23a03a463b822119bca2, #571)
Background: The `/update-docs` skill procedure (Step 4 "Propose updates") does not explicitly remind the model to write history/changelog entries in English for public repos. The skill just says "Follow `rules/docs-convention.md`", but `DOCS_LANG_HISTORY=english` and `DOCS_LANG_CHANGELOG_PUBLIC=english` are defined in `rules/language.md` (the `docs-lang` block), which the skill does not call out directly. ## Observed failure Session 20260526-134342: `/update-docs` proposal presented `## History Notes` content in Japanese. The language rule was not flagged at proposal time. The user had to manually catch it. Note: issue #528 added a `check-worktree-notes-lang.js` PostToolUse hook that enforces language at *write* time. The gap here is earlier — the skill's *proposal* step does not remind the author, so the user sees a Japanese draft and has to manually request a correction before approval.
Changes: - Add an explicit language-check step to the update-docs skill procedure, before proposing entries: "History Notes and Changelog Notes must be English for public repos (`DOCS_LANG_HISTORY=english`, `DOCS_LANG_CHANGELOG_PUBLIC=english` per `rules/language.md`). Confirm each proposed entry is in English before presenting to the user." - Alternatively, have the proposal step read `rules/language.md` (or the `docs-lang` block) and enforce inline, rather than delegating entirely to the PostToolUse hook layer. Related to #528

### FEATURE: enforce-worktree blocks memory directory writes from main worktree (2026-05-26, 61699cdf7a59ef2b6f37afa940211c0898f9e913, #573)
Background: `~/.claude/projects/<repo>/memory/` is Claude Code's auto-memory directory (per the global `# auto memory` instructions). It lives outside any git repo and is conceptually a per-user state store. It should be freely readable and writable regardless of which worktree or branch is currently checked out. Currently `hooks/enforce-worktree.js` blocks Bash write operations against this directory whenever the session CWD is the main worktree, because the guard is CWD/branch-based and does not exempt paths outside the agents repo. This is structurally the same class of bug as #419 / #497 (`~/.workflow-plans/` blocked despite being outside the repo). Filed separately because the memory path was never explicitly covered and the asymmetry below adds a new failure mode. ### Example (encountered 2026-05-26) After /worktree-end finished and CWD was switched to the main worktree (`C:/git/agents` on branch 'main'), Claude wrote a feedback memory file via the Write tool (which is path-classified differently and succeeded). The user then said 'don't write to memory'. Claude tried to delete it with: ``` rm "C:/Users/nire/.claude/projects/c--git-agents/memory/feedback_final_report_surface_restart.md" ``` enforce-worktree blocked the rm with: ``` ENFORCE_WORKTREE: write blocked. Reason: main worktree (branch 'main'). ``` Workarounds available to Claude were all heavy: - ask user to delete manually via PowerShell - ask user to approve `<<WORKFLOW_ENFORCE_WORKFLOW_OFF>>` just to delete one file - /sweep-worktrees does not cover memory files (out of scope) **Asymmetry**: Write tool can create memory files from the main worktree, but Bash rm cannot delete them. So Claude can dig itself into a hole it cannot exit without escalation.
Changes: - Exempt the memory directory tree (`~/.claude/projects/*/memory/**` or equivalent platform path) from enforce-worktree's write-block, regardless of CWD/branch. - Apply the exemption uniformly across Bash write operations (rm, mv, cp, redirect, etc.) so behavior matches the Write tool's existing pass-through. - Verify enforce-worktree path resolution handles Windows backslash / mixed-separator paths correctly for this exemption. - Consider whether a general 'non-repo path exemption' (per §1/§2 of core-principles) would cover both `~/.claude/projects/*/memory/` and the already-fixed `~/.workflow-plans/` from one place, rather than per-path patches. - Add a test covering: from main worktree CWD, both Write-tool create and Bash rm against a memory file succeed. Related to #419 Related to #369

### FEATURE: Add §5 Serve Actual Audience to core-principles; formalize agents-repo behavior rule (2026-05-27, a83acb791d7ecd13bf5bc9119c6c1949a7d0a2b3, #576)
Background: When developing with the agents repo workflow, two cases arise where Claude Code may encounter behavioral issues: 1. **User investigates**: "Expected behavior X is not working — is it in a prompt, enforced by a hook, or not implemented at all?" 2. **Claude Code discovers**: CC itself finds a workflow contradiction or inconsistency. In both cases, if the issue is a genuine workflow problem, it should be filed as a GitHub issue against the agents repo itself. Currently, no rule in `rules/` or `CLAUDE.md` mandates this — leading to the common mistake of writing behavioral findings to memory instead. Additionally, `rules/core-principles.md` lacks a foundational principle that "the user" differs by repo visibility: in a private repo the audience is the repo's own users; in a public repo the audience is all GitHub users. This principle underlies the existing public/private branches throughout the rules (language.md, changelog.md, workflow-off.md, etc.) but has never been stated explicitly. ## Scope 1. Add §5 "Serve the Actual Audience" to `rules/core-principles.md` (abstract principle, no file/skill references):    - Private repo: target audience is the repo's own users only    - Public repo: target audience is all GitHub users → requirements may vary by user (e.g., language.md requirements) 2. Add explicit rule to `rules/github-issues.md`: agents repo behavioral issues must be filed via `/issue-create` — writing to memory is prohibited for this class of finding 3. Delete `memory/feedback_agents_repo_behavior_use_issue_create.md` and remove its entry from `memory/MEMORY.md` — SSOT moves to `rules/` ## Constraints - `rules/core-principles.md` constraint: abstract principles only — no specific skill names, file paths, or step-by-step procedures - `rules/prompt-criteria.md` standards: directives not prose, one line per directive - §5 must be written in English (public repo documentation) <!-- intent:clarified -->
Changes: When developing with the agents repo workflow, two cases arise where Claude Code may encounter behavioral issues:

### FEATURE: worktree-end Final Report: Stop hook で定型ブロックの最終 assistant message 検証/注入 (PR2 of #518) (2026-05-27, 32eb4180e309dc725bdc06d0487fda4889ce9cfa, #534)
Background: #518 (PR #533) で Final Report の `### Post-Merge Actions Required` ブロックを常時表示する renderer 強化を行った (Approach B PR1)。 `stop-cleanup-reminder.js` と同パターンの Stop hook を追加し、最終 assistant message に定型ブロックが含まれることを検証する。欠落時は block + reason で再描画を促す。
Changes: - Stop hook 新設: session state (Step 7 完了 + env-file JSON 存在) をトリガーに発火 - env-file JSON から定型 ALL-CATEGORIES ブロックを再構築して reason に載せる - `stop-cleanup-reminder.js` と同じ「状態を読む → reason で block」パターン ## Acceptance criteria - PR1 (#518) を本番で運用して Final Report 漏れがゼロでない場合のみ実装する - 漏れがゼロなら本 issue は close/cancelled ## Related - #518 (PR1: renderer 強化) - `hooks/stop-cleanup-reminder.js` (参考パターン)

### FEATURE: commit gate の「commit 前ステップが commit 後の状態に依存する」設計矛盾 (2026-05-27, d4d81558f533303422b51825ab2f345389426c80, #225)
Background: commit gate の「commit 前ステップが commit 後の状態に依存する」設計矛盾
Changes: **根本原因**: workflow-gate は commit 前に全ステップ（review → user_verification）の完了を要求する。しかし一部のステップは commit 後の状態に依存するためã

### FEATURE: clarify-intent: text proposal + 2-choice AskUserQuestion (replace multiSelect) (2026-05-27, 6eb2e1687036dfdbf5f432cd34484c21adcc0fe0, #556)
Background: 親 issue #554 の構成要素。`clarify-intent` の UI フローを multiSelect 中心から text proposal + 合意確認型に変更する。 現状 (`skills/clarify-intent/SKILL.md` Step 3): - 候補が 5 個以上あると上位 3 + "Other" にフォールバックし、視覚的に俯瞰できない - multiSelect のチェックボックス UI が「全部やって」というユーザー心理バイアスを誘発
Changes: - CC が survey-code / survey-history の結果から **おすすめ class members 案** を生成 (sub-issue #(TBD: CC propose) と協調) - main conversation に text 形式で全候補リストを列挙 (各メンバーに proposed triage flag MUST/OPTIONAL/NA 付き) - AskUserQuestion は **2-3 択** に簡素化:   - "Accept proposal (recommended)"   - "Modify"  (Other 経由でユーザーが triage / リスト自体を修正) - multiSelect は廃止 - ユーザーが Modify を選んだ場合のフォローアップフロー (free text で修正指示を受け、CC が intent.md に反映) ## Acceptance criteria - 候補が 10 個でも text 列挙で全て見える - ユーザーが triage flag を修正できる - リストそのもの (メンバーの追加/削除) も修正できる - AskUserQuestion 1 回で合意可能なケースが大半 (Accept proposal で完結) ## Parent #554

### FEATURE: intent.md schema: add triage flag (MUST/OPTIONAL/NA) to ## Class members (2026-05-27, 6eb2e1687036dfdbf5f432cd34484c21adcc0fe0, #555)
Background: 親 issue #554 の構成要素。`intent.md` の `## Class members` セクションのスキーマを triage flag に対応させる。 現在のスキーマ (`skills/clarify-intent/SKILL.md` Step 4): ``` - <name>: <description> — disposition: <fix in scope | track separately> ``` `disposition` が二値のため、「detail まで踏み込んだ上で対応がリーズナブルなら入れる」のような中間状態が表せない。
Changes: - スキーマを以下に変更:   ```   - <name>: <description> — triage: <MUST | OPTIONAL | NA>   ```   - MUST: 必須対応 (= 現在の `fix in scope` の強い版)   - OPTIONAL: detail まで踏み込んで判断、合理的であれば対応   - NA: 対象外 (= 現在の `track separately`) - 後方互換: 旧 `disposition: fix in scope` → `MUST`、`disposition: track separately` → `NA` の変換ロジック (intent.md 読み出し側) - `hooks/lib/parse-closes-issues.js` 等の intent.md パーサに影響あるか確認 - `assemble-mandatory.sh` が intent.md → outline.md / detail.md に carry-forward する際の triage flag 保持を検証 ## Acceptance criteria - 新スキーマで書かれた intent.md が assemble-mandatory.sh で正しく carry-forward される - 旧スキーマの intent.md (既存セッション) が読み出し時に新形式に変換される (もしくは reject されない) - スキーマ違反 (enum 外の値) を検出する validation がある ## Parent #554

### FEATURE: clarify-intent: CC auto-proposes triage flags from survey-code/history results (2026-05-27, 6eb2e1687036dfdbf5f432cd34484c21adcc0fe0, #557)
Background: 親 issue #554 の構成要素。CC が `clarify-intent` 内で survey-code.md / survey-history.md の結果から自動的に class members 候補と triage flag (MUST/OPTIONAL/NA) を提案するロジックを追加する。 現状: `skills/clarify-intent/SKILL.md` Step 2a は `## Candidate class members` を survey 出力から集約するだけで、triage flag の判断は user に丸投げ。
Changes: - CC が survey 結果の各候補について以下を評価:   - **MUST**: primary subject と同一の anti-pattern を明確に持つ   - **OPTIONAL**: 関連はあるが anti-pattern の有無はコード調査で確認が必要 / detail で判定   - **NA**: 異なる目的の hook で同じ class に属さない (例: scan-outbound.js は content scanner であり command-head detector ではない) - 判断根拠を 1-2 行で text proposal に併記 (ユーザーが triage 修正の判断材料にできる) - 自動 triage は best-effort であり、最終決定権はユーザーにある ## Acceptance criteria - issue #334 ケースで CC が自動的に: `check-japanese-in-docs.js: MUST`, `enforce-issue-close.js: MUST`, `scan-outbound.js: NA`, `block-dotenv.js: NA`, `enforce-worktree.js: NA` 相当を提案できる - 判断根拠が main conversation に表示される - ユーザーが提案を Accept するか Modify するかを 1 回の AskUserQuestion で選択できる (sub-issue UI flow と協調) ## Parent #554 ## Related #553 (CC/Codex が survey/source にアクセスする道筋。本 issue の前提条件にはならないが、CC が triage を提案する根拠を共有する点で関連)

### FEATURE: clarify-intent triage redesign: downstream planner/reviewer/codex/survey-format updates (2026-05-27, 6eb2e1687036dfdbf5f432cd34484c21adcc0fe0, #582)
Background: #554 グループの下流コンポーネント。上流 (#555 スキーマ変更, #556 UI 変更, #557 CC 自動提案) が確定した後、triage flag (MUST/OPTIONAL/NA) を読み込む全下流 consumer を更新する。 #558 がこの内容を実装していたが、clarify-intent 部分 (2-stage multiSelect) が #556 (multiSelect 廃止) と衝突したため cancel された。clarify-intent 以外の下流変更はそのまま有効であり、本 issue で継承する。
Changes: - `agents/outline-planner.md`: `disposition: fix in scope / track separately` → `triage: MUST / OPTIONAL / NA` 対応 - `agents/outline-reviewer.md`: 同上 - `agents/detail-planner.md`: 同上 - `agents/detail-reviewer.md`: 同上 - `bin/review-plan-codex`: `ACCEPTED_TRADEOFFS_BLOCK` を MUST/OPTIONAL/NA-aware に更新 - `skills/survey-code/SKILL.md`: `## Candidate class members` 出力フォーマットに `proposed triage: MUST/OPTIONAL/NA + rationale` を追加 - `skills/survey-history/SKILL.md`: 同上 (survey-code と対称) - `tests/feature-clarify-intent.sh`: N11 (multiSelect assert) / N12a/N12b (disposition assert) 更新 - backward-compat: `disposition: fix in scope` → `MUST`、`track separately` → `NA` の読み替えをプランナープロンプトに注記 ## Non-goals - assemble-mandatory.sh の変更 (verbatim コピー、スキーマ非依存) - parse-closes-issues.js の変更 (## Issues のみ解析) ## Parent #554 ## Related - #555: intent.md スキーマ変更 (前提条件) - #556: clarify-intent UI 変更 (前提条件) - #557: CC auto-propose ロジック (前提条件) - #558: cancel 済み (clarify-intent 部分のみ衝突; 本 issue はそれ以外を継承)

### FEATURE: wip-state.sh: CLAUDE_SESSION_ID が Bash subprocess に伝播せず WIP signaling が再び全停止 (regression of #440) (2026-05-27, c4fa643848bb50b5711b3f1aa12ad5c09da39aee, #519)
Background: ### Symptom Projects v2 view (https://github.com/users/nirecom/projects/1/views/1) に `Status: In Progress` の issue が 0 件。実際は複数の作業中 issue (例: #503, #516) があるはずだが、いずれも `Status: Todo` のまま。 ### 観測 (2026-05-24, VS Code Claude Code セッション 058c78b2-906f-4866-8c1d-90f7d22314d8) `bash -x $AGENTS_CONFIG_DIR/bin/github-issues/wip-state.sh check 503` のトレース： ``` + load_env_file + . 'C:\git\agents/.env'           # WIP_STATE_*_ID は正常にロード + preflight_field_ids               # OK ++ resolve_session_id ++ '[' -n '' ']'                    # $CLAUDE_ENV_FILE 空 ++ '[' -n '' ']'                    # $CLAUDE_SESSION_ID も空 ++ echo 'Error: CLAUDE_SESSION_ID not resolvable (neither $CLAUDE_ENV_FILE nor $CLAUDE_SESSION_ID is usable)' ++ return 2 + exit 2 ``` Bash tool の直接実行と子プロセスでの env 比較： ``` # Bash tool 直接実行 CLAUDE_ENV_FILE= CLAUDE_SESSION_ID=058c78b2-906f-4866-8c1d-90f7d22314d8 # bash -c での子プロセス SUBSHELL CLAUDE_SESSION_ID= ``` → Bash tool の shell では `CLAUDE_SESSION_ID` は**変数として設定されているが export されていない**。`bash wip-state.sh` のような子プロセス呼び出しでは見えない。 ### Root cause #440 (CLOSED 2026-05-22) の修正は `resolve_session_id` に `${CLAUDE_SESSION_ID:-}` フォールバックを追加するものだった。当時の前提は「VS Code 拡張版 Claude Code は Bash subprocess に `CLAUDE_SESSION_ID` 環境変数は直接 export する」(#440 issue 本文より)。 しかし現在の claude-vscode バージョンでは `CLAUDE_SESSION_ID` は Bash tool の shell 変数としてセットされるのみで、export されないため子プロセスに伝播しない。結果として #440 のフォールバックも到達できず、`resolve_session_id` は rc=2 で失敗する。 `clarify-intent/SKILL.md:96-98` で "Exit 2 → warn and continue" と定義されているため、workflow は中断せず**全 session で WIP set が silent fail する**。 ### 影響 - `workflow-init` Step 3(a) の WIP check も同じ理由で rc=2 → 「advisory; warn-and-continue」扱いで並行 session 衝突検出が機能しない - `clarify-intent` Completion の WIP set 失敗で Projects v2 Status が `Todo` のまま - `issue-close-finalize` Step K の `clear` でも fingerprint clear で `no changes to make` 警告（fingerprint がそもそも書かれていないため） - **#508 (multi-N WIP loop, 2026-05-23 マージ済) の実装自体は正しいが、wip-state.sh が動かないため効果が出ない**
Changes: ### 修正方針 (案) `CLAUDE_SESSION_ID` の伝播に頼らない session-id 解決経路を追加する。候補： 1. `~/.claude/projects/<encoded-cwd>/` 配下の最新 `.jsonl` の basename を session-id として読む (#482 の recovery 手順と同じ経路) 2. Claude Code が必ず書き込む別のファイル/環境変数経由 3. Bash tool の wrapper で `export CLAUDE_SESSION_ID` を強制 (settings.json レベル？ — 要調査) 副次：`clear` の `--text ""` が `no changes to make` を返すケースを正常終了扱いにする (#440 にも提案あり、未実装)。 ### Test plan - `bash -c 'echo $CLAUDE_SESSION_ID'` が空を返すことを確認 (現状再現) - 修正後: 同じ Bash tool 経由で `wip-state.sh set <N>` が成功し、`gh issue view <N>` の Projects v2 Status が `In Progress` に遷移すること - 回帰: 従来の CLI Claude Code 経路 (`CLAUDE_ENV_FILE` only) でも動作継続 ### 関連 - #440 — CLAUDE_SESSION_ID フォールバック追加 (CLOSED — 今回のリグレッションの原因) - #482 — recovery 手順を docs/ops.md に追加 (OPEN — 本 issue 修正後は不要になる可能性あり) - #508 — multi-N WIP loop (CLOSED 2026-05-23 — 正しく実装されているが本バグのため効果が出ない) - #451 — clarify-intent SKILL.md の error hint 改善 (OPEN — 隣接トピック) - Upstream: anthropics/claude-code#27987 Related to #482 Related to #482

### FEATURE: clarify-intent: text proposal + 2-choice AskUserQuestion (replace multiSelect) (2026-05-27, 6eb2e1687036dfdbf5f432cd34484c21adcc0fe0, #556)
Background: 親 issue #554 の構成要素。`clarify-intent` の UI フローを multiSelect 中心から text proposal + 合意確認型に変更する。 現状 (`skills/clarify-intent/SKILL.md` Step 3): - 候補が 5 個以上あると上位 3 + "Other" にフォールバックし、視覚的に俯瞰できない - multiSelect のチェックボックス UI が「全部やって」というユーザー心理バイアスを誘発
Changes: - CC が survey-code / survey-history の結果から **おすすめ class members 案** を生成 (sub-issue #(TBD: CC propose) と協調) - main conversation に text 形式で全候補リストを列挙 (各メンバーに proposed triage flag MUST/OPTIONAL/NA 付き) - AskUserQuestion は **2-3 択** に簡素化:   - "Accept proposal (recommended)"   - "Modify"  (Other 経由でユーザーが triage / リスト自体を修正) - multiSelect は廃止 - ユーザーが Modify を選んだ場合のフォローアップフロー (free text で修正指示を受け、CC が intent.md に反映) ## Acceptance criteria - 候補が 10 個でも text 列挙で全て見える - ユーザーが triage flag を修正できる - リストそのもの (メンバーの追加/削除) も修正できる - AskUserQuestion 1 回で合意可能なケースが大半 (Accept proposal で完結) ## Parent #554

### FEATURE: intent.md schema: add triage flag (MUST/OPTIONAL/NA) to ## Class members (2026-05-27, 6eb2e1687036dfdbf5f432cd34484c21adcc0fe0, #555)
Background: 親 issue #554 の構成要素。`intent.md` の `## Class members` セクションのスキーマを triage flag に対応させる。 現在のスキーマ (`skills/clarify-intent/SKILL.md` Step 4): ``` - <name>: <description> — disposition: <fix in scope | track separately> ``` `disposition` が二値のため、「detail まで踏み込んだ上で対応がリーズナブルなら入れる」のような中間状態が表せない。
Changes: - スキーマを以下に変更:   ```   - <name>: <description> — triage: <MUST | OPTIONAL | NA>   ```   - MUST: 必須対応 (= 現在の `fix in scope` の強い版)   - OPTIONAL: detail まで踏み込んで判断、合理的であれば対応   - NA: 対象外 (= 現在の `track separately`) - 後方互換: 旧 `disposition: fix in scope` → `MUST`、`disposition: track separately` → `NA` の変換ロジック (intent.md 読み出し側) - `hooks/lib/parse-closes-issues.js` 等の intent.md パーサに影響あるか確認 - `assemble-mandatory.sh` が intent.md → outline.md / detail.md に carry-forward する際の triage flag 保持を検証 ## Acceptance criteria - 新スキーマで書かれた intent.md が assemble-mandatory.sh で正しく carry-forward される - 旧スキーマの intent.md (既存セッション) が読み出し時に新形式に変換される (もしくは reject されない) - スキーマ違反 (enum 外の値) を検出する validation がある ## Parent #554

### FEATURE: feat: per-context language config (CONV_LANG/ASK_LANG/PLAN_LANG) + migrate DOCS_LANG_* to .env (2026-05-27, e72179e8d924424e4c09f080437ed219b7b33637, #581)
Background: `rules/language.md` defines language policy, but there is no per-user, per-context customization mechanism. Observed failures: - AskUserQuestion language is inconsistent (Japanese but drifts to English or Chinese-like wording) - Planning artifacts (intent/outline/detail.md) are sometimes written in English - Existing DOCS_LANG_* settings live in the `docs-lang` fenced block in `rules/language.md`, separate from `.env` config (CONFIRM_* etc.), reducing consistency ## Scope 1. **Migrate all language settings to `.env`**    - Existing: `DOCS_LANG_HISTORY`, `DOCS_LANG_CHANGELOG_PUBLIC`, `DOCS_LANG_CHANGELOG_PRIVATE` — migrate from docs-lang fenced block to `.env`    - New: `CONV_LANG`, `ASK_LANG`, `PLAN_LANG` — add to `.env`    - Document all 6 keys in `.env.example` 2. **Update `hooks/lib/docs-lang-config.js`** to read from `.env` via `load-env.js` instead of parsing the docs-lang fenced block 3. **Update `rules/language.md`**: remove docs-lang fenced block; update Conversation/Planning Artifacts sections to reference new `.env` keys; fix stale path `~/.claude/plans/` → `~/.workflow-plans/` 4. **New hook `hooks/check-plan-lang.js`** (PostToolUse): enforce PLAN_LANG when Write targets `*-intent.md` / `*-outline.md` / `*-detail.md` — mirrors existing `check-worktree-notes-lang.js` pattern 5. **New hook `hooks/check-ask-lang.js`** (PreToolUse): enforce ASK_LANG on AskUserQuestion tool calls — CJK detection logic TBD at detail stage 6. **CONV_LANG**: prompt instruction only (main conversation text cannot be hook-enforced) 7. **Related fixes**: remove hard-coded "Use English terms only" from `agents/detail-planner.md:30`; update `skills/make-outline-plan/SKILL.md:114` to reference PLAN_LANG ## Constraints - `rules/language.md` is gitignored (symlink from my-private-repo) — removing the docs-lang block requires coordinated update across agents repo (public) and my-private-repo (private) - ASK_LANG CJK detection logic is non-trivial; hook may start as warn-only - `check-worktree-notes-lang.js` and `bin/compose-doc-append-entry` depend on `docs-lang-config.js` — backward compatibility required during migration <!-- workflow-session: 20260526-223459 -->
Changes: `rules/language.md` defines language policy, but there is no per-user, per-context customization mechanism. Observed failures:

### FEATURE: show-plan-link.js: assemble-mandatory.sh の Bash 書き込みで PostToolUse フックが発火しない (2026-05-27, bb40d690ff219063e763cee72df0dcc940dc78b0, #574)
Background: `assemble-mandatory.sh` は Bash ツール経由で `outline.md` を書き出す。PostToolUse フック (`show-plan-link.js`) は Write ツールの完了時にのみ発火するため、Bash 経由の書き込みではフックが起動せず、VS Code preview リンクがユーザーに届かない。 再現条件: `make-outline-plan` が `assemble-mandatory.sh` を Bash ツールで呼び出した後、確認ダイアログを出す前にフックが発火していない（VS Code にプレビューリンクが表示されない）。 期待動作: `outline.md` 書き込み後に `show-plan-link.js` が発火し、VS Code の Plan file リンクがユーザーに表示される。 Related to #563
Changes: - `assemble-mandatory.sh` の後に orchestrator が Write ツールで outline.md を再書き込みしてフックを強制発火させる（現在の workaround）、もしくは - make-outline-plan SKILL.md を改訂し、assemble-mandatory.sh 後に Write ツールで outline.md を書くよう orchestrator に明示指示する Related to #563

### FEATURE: stop-confirm-plan-guard.js: activate path-emission guard also when CONFIRM_<STEP>=off (2026-05-27, bb40d690ff219063e763cee72df0dcc940dc78b0, #563)
Background: confirm-plan.md Step 2 unconditionally prohibits the orchestrator from emitting any path representation (markdown link / fullpath / file:/// all forbidden). The show-plan-link.js PostToolUse hook is the sole authorized surface. However, stop-confirm-plan-guard.js (Gap 1, merged in #524) is currently only active when CONFIRM_<STEP>=on. In off-mode, no structural guard exists to catch path emission violations. ## Evidence In session ffc14513 (issue #334), the orchestrator violated the path-emission prohibition twice — once at outline stage and once at detail stage — both times in off-mode (CONFIRM_OUTLINE=off / CONFIRM_DETAIL=off). The violations were caught only via user review, not by the guard.
Changes: Extend stop-confirm-plan-guard.js to activate the path-emission check regardless of CONFIRM_<STEP> value: - Remove the CONFIRM_<STEP>=on guard that currently short-circuits the check - The path-emission block (markdown link / fullpath / file:/// detection) fires in both on-mode and off-mode - The AskUserQuestion enforcement branch (CONFIRM=on-only) remains unchanged ## Constraints - Implement AFTER #524 is already merged (stop-confirm-plan-guard.js exists) - Do NOT change the AskUserQuestion enforcement (CONFIRM=on-only is correct) - Maintain fail-open semantics for marker-missing edge cases ## Relationship to #534 and #545 #534 targets worktree-end Final Report (different rule). #545 (Gap 2) enforces AskUserQuestion in on-mode. This issue extends Gap 1's path-emission guard to off-mode. All three are orthogonal. Related to #545 Related to #545

### FEATURE: feat: add CONFIRM_DOCS flag to /update-docs to skip proposal confirmation (2026-05-27, 0a3e19086726a2d1a9be91fe281440d458adb4e3, #598)
Background: /update-docs requires user approval before writing History/Changelog entries to WORKTREE_NOTES.md (the gather → propose → confirm → apply cycle, Step 5). In workflows where the user trusts the model's draft (or when running with CONFIRM_CODE=off / CONFIRM_TESTS=off), this confirmation wait is unnecessary friction and breaks the flow. The existing CONFIRM_* family (CONFIRM_CODE, CONFIRM_TESTS, CONFIRM_OUTLINE, CONFIRM_DETAIL) provides a consistent escape hatch for each workflow stage. /update-docs has no equivalent.
Changes: - Add CONFIRM_DOCS config var (default on) to the update-docs skill SKILL.md. - When CONFIRM_DOCS=off: skip the 'Apply after confirmation' AskUserQuestion (Step 5) and write entries directly to WORKTREE_NOTES.md. - Apply the same gate to the post-action review step if present (mirror the CONFIRM_CODE double-gate pattern). - Document CONFIRM_DOCS in .env.example with the standard 3-point format (what you can do / what you can't / format + example). - Add a test to feature-robust-workflow-settings.sh (or a new dedicated test) verifying CONFIRM_DOCS is present in .env.example with the correct format.

### FEATURE: workflow prompt 短縮：refactor-prompts 修正・review-skill-size 拡張・上位4 SKILL.md 縮小＋ステップ番号リナンバー (2026-05-27, 06c9b01a7b9fc7ec90ca9da903d36ff80b7d99a5, #602)
Background: SKILL.md ファイルの肥大化が継続しており、3つの問題が表面化した。 (1) /refactor-prompts が Windows で /tmp が存在しないため失敗 + worktree-start の AskUserQuestion と context:fork の自己矛盾でハング。 (2) bin/review-skill-size が差分ファイルのみをスキャンするため、既存の違反ファイル（worktree-end 422行等）を検出しない。 (3) worktree-end/SKILL.md が 422行に達し、issue-close-finalize (248), make-detail-plan (182), workflow-init (174) も過大。加えて SKILL.md 内に長い node -e / bash インライン埋め込みがあり、ハルシネーションを誘発するリスクがある。
Changes: 1. refactor-prompts: Windows /tmp バグ修正・worktree-start AskUserQuestion ハング解消 2. review-skill-size: 既存ファイル全スキャンモード追加（差分スキャンは保持） 3. 上位4 SKILL.md (worktree-end 422行, issue-close-finalize 248行, make-detail-plan 182行, workflow-init 174行) を縮小（目標: 各 150行以下） 4. SKILL.md 内の長い bash/node -e インライン埋め込みを bin/ スクリプトに切り出し Related to #396 Related to #513 Related to #396 Related to #513

### FEATURE: issue-create: dispatch script blocked from main worktree when body contains write-pattern strings (2026-05-28, 5b52897fa77de35e0ef99d4fcf34f52e142c49f3, #596)
Background: /issue-create Phase 4 calls `bash issue-create-dispatch.sh --body "..."`. When the body text contains write-pattern strings (e.g. `git commit`, `git push`), enforce-worktree.js blocks the call from the main worktree. #371 fixed this for direct `gh issue create` commands via the Group A override in bash-write-patterns.js: when the outer command is a Group A gh command, write-pattern matches inside the body are suppressed. However the dispatch script is invoked as `bash dispatch.sh`, not as a direct gh command, so the Group A override does not apply. As a result, issue bodies that document workflow commands (e.g. acceptance criteria containing `ISSUE_CLOSE_SKILL=1 git commit`) cause the entire dispatch call to be blocked. Workaround: use `gh issue create` directly, or sanitize the body to remove write-pattern strings. Related: #595 (ISSUE_CLOSE_SKILL=1 git commit bypass broken — same session, possibly same regression). Root cause analysis in: #371 (CLOSED — fixed for direct gh, not for bash wrappers).
Changes: - Extend the Group A write-pattern body suppression to `bash` calls to recognized dispatch scripts (issue-create-dispatch.sh, similar wrappers) - OR strip body/title arguments before write-pattern scanning when the outer command is a known safe script - Add regression test: issue-create from main worktree with body containing `git commit` text succeeds Related to #595

### FEATURE: issue-create-dispatch.sh: sub_issue_id schema is stale (GitHub API now requires integer databaseId, not GraphQL node id) (2026-05-28, 5b52897fa77de35e0ef99d4fcf34f52e142c49f3, #432)
Background: `bin/github-issues/issue-create-dispatch.sh` (and the SKILL.md behavioral note) currently document the sub-issue attach API as: > The dispatcher uses `POST /repos/{owner}/{repo}/issues/{N}/sub_issues` with `sub_issue_id` set to the **child's GraphQL node id** (`gh issue view <child> --json id`). But GitHub's sub-issue API now rejects GraphQL node ids: ``` gh: Invalid request. Invalid property /sub_issue_id: `"I_kwDOSLnZ9c8AAAABC_4xhw"` is not of type `integer`. (HTTP 422) ``` The API now requires the **integer databaseId** (`gh api graphql ... databaseId` or `gh issue view <N> --json number` for the simple case where it equals the issue number's databaseId). ## Observed in This session: dispatching `/issue-create` for #430 with `--verdict sub-of --parent 225` succeeded at creating #430 but failed at the sub-issue attach step. Manual retry with the databaseId (`4496175495`) succeeded. ## Repro ```bash CHILD_ID=$(gh issue view <child> --json id --jq .id)  # GraphQL node id e.g. I_kwDO... gh api -X POST repos/<owner>/<repo>/issues/<parent>/sub_issues -F sub_issue_id="$CHILD_ID" # → HTTP 422: not of type integer ``` Working form: ```bash DB_ID=$(gh api graphql -f query='query{repository(owner:"<o>",name:"<r>"){issue(number:<child>){databaseId}}}' --jq '.data.repository.issue.databaseId') gh api -X POST repos/<o>/<r>/issues/<parent>/sub_issues -F sub_issue_id=$DB_ID ``` ## Proposed fix In `bin/github-issues/issue-create-dispatch.sh` sub-issue attach path: - Replace the `gh issue view <child> --json id --jq .id` call with a `databaseId` lookup via GraphQL (or read it from the `gh issue create` response if available). - Use `-F` (not `-f`) to pass the value as an integer. - Update SKILL.md and `rules/github-issues.md` to reflect the integer requirement. ## Severity Low — the issue is still created, only the parent attachment fails. Manual retry succeeds, and the error message includes the retry command. But every `sub-of` invocation hits this until the script is updated. ## Origin WORKTREE_NOTES.md of feat/issue-header-423 worktree, encountered while creating #430.
Changes: `bin/github-issues/issue-create-dispatch.sh` (and the SKILL.md behavioral note) currently document the sub-issue attach API as:

### FEATURE: core-principles.md: add Scenario Sweep section (temporal extension of Orthogonality) (2026-05-29, 989cac1db672476efceb0364541f00c5a5d8c886, #606)
Background: `§1 Elevate Perspective` raises the view to the parent class. `§2 Orthogonality` enforces uniform treatment across current siblings. But neither covers the **time dimension**: a fix correct for today's A-1 may leave tomorrow's B, B-1, B-2, B-3 exposed to the same class of problem. Observed in: #596 was introduced because PR #452 fixed the Group A heredoc form but did not walk forward to consider that future invocation forms (inline `--body`, bash wrappers) would face the same write-pattern scanning issue.
Changes: Add a new section to `rules/core-principles.md` — tentatively **§6 Scenario Sweep** (English expression TBD): > When applying a fix to A-1, walk the full time-ordered scenario of the class: A → A-1, A-2, A-3 (current siblings) → B, B-1, B-2, B-3 (future members yet to exist). A fix that closes A-1 but leaves the same vulnerability reachable via a future B-1 is incomplete. Enumerate the expected future members of the class and verify the fix architecture handles them, or document why they are out of scope. Relation to existing sections: - `§1 Elevate Perspective`: raise view to the parent class (A) - `§2 Orthogonality`: treat current siblings uniformly (A-1, A-2, A-3) - **§6 Scenario Sweep (new)**: trace the full timeline; verify the fix holds for future class members (B, B-1, B-2, B-3) Scope: - Add §6 to `rules/core-principles.md` - Update any review skill prompts that reference core-principles.md to surface the new section - English section name to be finalized before implementation (candidates: Scenario Sweep, Lifecycle Coverage, Forward Parity, Temporal Closure)

### FEATURE: review process: add sibling-sweep step to surface class members (complement workflow-init) (2026-05-29, 989cac1db672476efceb0364541f00c5a5d8c886, #607)
Background: `clarify-intent` の class members sweep が #452 の時点で機能していれば #596 は生まれなかった。しかし class member を列挙する責務は workflow-init / clarify-intent だけに限定すべきではない。 現状: class members の列挙は clarify-intent (intent 段階) に集中しており、後半の review プロセス（review-code-codex、review-plan-security、review-code-security）には sibling を列挙して考えさせる機能がない。 問題: PR #452 のように workflow-init を通さず直接コミットされた fix や、intent 段階では見えていなかった sibling が実装後に浮上した場合に、review 段階でのセーフティネットがない。列挙が過剰かどうかの判断は planner / orchestrator が行えばよく、review プロセスが「列挙しない」のは損失。
Changes: - `review-code-codex` のレビュープロンプトに「このコード変更が触れている関数・ロジック・パターンの sibling（同じクラスの他メンバー）を列挙し、同じ修正が必要でないか確認せよ」という directive を追加 - `review-plan-security` / `review-code-security` にも同様の sibling-sweep directive を追加（セキュリティゲートへの例外追加や上書きを含む変更の場合） - 列挙結果の取り扱い: planner / orchestrator が MUST / OPTIONAL / NA をその場で判断し、不要なら明示的にスキップ（現在の class members triage と同じ流れ） Related to #606 (Scenario Sweep section — temporal complement of the same improvement) Related to #596 (post-mortem: the sibling `bash wrapper` form was not surfaced during review of PR #452) Related to #606

### FEATURE: lang config: support arbitrary language codes (not just english/japanese/any) (2026-05-29, 27ed9058b02039189112ced577aca0a813a07d41, #594)
Background: #581 で PLAN_LANG / ASK_LANG / CONV_LANG / DOCS_LANG_* を導入したが、受け入れ値が english | japanese | any の三択に固定されている。global audience を想定すると、spanish / french / chinese / korean / arabic など任意の BCP-47 または自然言語名を設定できるべき。 現状の問題: - lang-config.js の loadLangConfig が ['english', 'japanese', 'any'] に対してのみ validate し、それ以外は 'any' にフォールバックする (fail-open だが設定ミスが無言で無視される) - lint-plan-lang.js の lintPlanLang は policy === 'english' と policy === 'japanese' のハードコード分岐のみで、それ以外の言語は実質 'any' 扱い - check-ask-lang.js も同様に english / japanese のみ対応
Changes: - lang-config.js: 三択 validate を廃止し、任意の文字列を受け入れる（ただし空文字・空白のみは 'any' 扱い） - lint-plan-lang.js: policy が english / japanese 以外のときは hasCJK ベースの粗い判定に代えて、設定言語名を Claude の言語ヒントとして additionalContext に含める warn-only モードへ切り替え（または言語コードから script-range を推定する拡張設計を検討） - check-ask-lang.js / check-plan-lang.js: 同上の lintPlanLang 変更に追従 - .env.example と language.md.example: 有効値を 'english | japanese | any | <任意の言語名>' と更新 - テスト: G6–G11 の既存テストを維持しつつ、任意言語コードのパスを G12 として追加 Related to #581 Related to #581

### FEATURE: PR2: worktree-end/SKILL.md 短縮 + bin/worktree-end/ スクリプト抽出 (2026-05-29, 7760677d8c99161089902bd4ad7561cf0ac78ecf, #611)
Background: worktree-end/SKILL.md は現在 422 行あり、インライン bash/node が多数含まれる。 親 issue: #610
Changes: - bin/worktree-end/ ディレクトリ作成: capture-env.sh, write-env-json.js, extract-pr-fields.js, read-notes-path.js, render-final-report.sh, compose-doc-append.sh - SKILL.md を ≤200 行に短縮（CLI 呼び出しに置換） - tests/feature-worktree-end-no-inline-js.sh 追加（node -e 禁止アサート）

### FEATURE: lang-config: remove docs-lang fenced-block fallback; consolidate DOCS_LANG_* entirely in .env (2026-05-29, 5504a48548599102183076cb9aaff023224321cc, #619)
Background: #594 migrated `DOCS_LANG_*` settings from the `docs-lang` fenced block in `rules/language.md` to `.env`. However, `loadDocsLangConfig` in `hooks/lib/lang-config.js` still reads `rules/language.md` as a fallback (fenced-block parsing) and overrides with `.env` values. Now that `.env` is the canonical source, the fallback is dead weight.
Changes: 1. `hooks/lib/lang-config.js`: remove `extractDocsLangBlock`, `parseDocsLangBody`, and the `langFilePath` parameter from `loadDocsLangConfig`. Read only from `.env` via `loadDefaultEnv()`. 2. `hooks/check-worktree-notes-lang.js`: drop the `langFile` path construction and the `loadDocsLangConfig(langFile)` call; replace with the simpler `.env`-only loader. 3. `my-private-repo/agents/rules/language.md`: remove the `docs-lang` fenced block (manual edit — out of scope for the agents repo PR, but must be coordinated). 4. Update any tests that exercise the fenced-block parsing path. ## Non-goals - Changing the `.env` key names or hook behavior. - Removing `rules/language.md` itself (the non-`docs-lang` sections — Conversation, Commit Messages, Code — are still prompt-only guidance). ## Related - #594 (this issue is a follow-up cleanup)

### FEATURE: worktree-end Final Report: primary-path /issue-create findings not reflected in Related/Next Tasks sections (2026-05-29, 196bbe20f502d28a6e6c3d4e36669832b1f20c8e, #622)
Background: The Final Report (`## Final Report`) reads its Bugs Found / Related Tasks / Next Tasks sections from `WORKTREE_NOTES.md`. The fallback path (`CLAUDE.md ## Mid-workflow finding capture`) writes entries to WORKTREE_NOTES.md, which then appear in the Final Report via `worktree-final-report.js`. However, the **primary path** — invoking `/issue-create` directly from the linked worktree — creates the GitHub issue but does NOT write any entry to WORKTREE_NOTES.md. As a result, issues created via the primary path are invisible in the Final Report.
Changes: - After a primary-path `/issue-create` completes, record the created issue number in the appropriate WORKTREE_NOTES.md section (`## RelatedTasks` or `## NextTasks` depending on category), annotated with the GitHub issue URL. - `worktree-final-report.js` (or the WORKTREE_NOTES.md format) should surface these primary-path findings in the same sections as fallback-path entries. - Ensure `worktree-notes-triage.js` does not re-promote already-primary-path-created issues during Step 5.5(a.5). ## Related - Observed during session b89f61e5 (issue #594): `/issue-create` for #619 was invoked via the primary path but did not appear in the Final Report's Related Tasks. Related to #530 Related to #530

### FEATURE: Group: GitHub Issues 移行 — フェーズ状況 (2026-05-29, 9e6f5edecc3dc3214cc9cb3a1dd48208fc7b02ba, #222)
Background: Group: GitHub Issues 移行 — フェーズ状況
Changes: detail plan: `~/.claude/plans/20260510-todo-to-github-issues-detail.md`

### FEATURE: PR3: issue-close-finalize/SKILL.md 短縮 + bin/ 抽出 + eval 除去 (2026-05-29, 1c1e83d78dff34e39e3a6fee459c9d7307af15ad, #612)
Background: issue-close-finalize/SKILL.md は約 220 行でインライン bash が多い。eval 呼び出しも含む。 親 issue: #610
Changes: - bin/issue-close-finalize/ 作成: pre-flight.sh, step-e.sh, step-g5-loop.sh - SKILL.md を ≤130 行に短縮 - eval 呼び出しをすべて除去 - tests/feature-issue-close-finalize-no-eval.sh 追加

### FEATURE: codex-review-loop: prompt 頼みを排除し hook/helper で review-plan-codex 呼び出しを強制 (2026-05-29, 64bb3be3f0934b48477fcae6b2aa34f3ced4f006, #603)
Background: `codex-review-loop.md` のプロトコルは「まず `review-plan-codex` を呼び、SKIPPED/FAILED 時のみ `detail-reviewer` / `outline-reviewer` にフォールバック」を要求しているが、この呼び出し順はプロンプト指示のみで強制されており、Claude orchestrator が誤った手順を踏んでも検出・ブロックする仕組みがない。 具体的な事故例 (#602 実装セッション): `make-detail-plan` の codex-review-loop ステップで `review-plan-codex` を呼ばずに `detail-reviewer` subagent を2ラウンド直接起動してしまった。`review-plan-codex` は intent + outline の context を受け取り assembler の挙動を考慮した判定ができるが、subagent はその context なしに動作するため「mandatory sections 欠如」を HIGH として誤報し続け、ラウンドキャップを無駄に消費した。
Changes: `bin/run-codex-review-loop` (または `bin/codex-review-loop`) ヘルパースクリプトを導入し、make-detail-plan / make-outline-plan の codex-review-loop ステップをスクリプト呼び出しに変える。スクリプトが以下を一括担う: 1. context ファイルの構築 (`build-codex-context`) 2. `review-plan-codex` の呼び出しと stdout パース 3. SKIPPED/FAILED 判定 → `REVIEWER_AGENT` subagent への自動フォールバック 4. 結果 (APPROVED / NON_APPROVED_VERDICT / cap) の exit code での通知 orchestrator SKILL.md は 1 行のスクリプト呼び出し (`review-loop-run`) のみを記述し、内部フローを prompt 指示で記述しない。 Related to #578 (codex-review-loop SSOT) Related to #578

### FEATURE: Group: Residual gaps under the new history/changelog workflow (post-#436) (2026-05-29, #480)
Background: #436 (PR #474, merged 2026-05-23) introduced bin/compose-doc-append-entry, which writes docs/history.md and CHANGELOG.md in separate commits from the main worktree post-PR-merge. This supersedes the original assumption that history.md is appended from the linked worktree (or from main with ENFORCE_WORKTREE toggling), which #325 was created to address. #325 is now closed as status:migrated since its main premise no longer holds: history.md append from main is sanctioned via COMPOSE_DOC_APPEND_SKILL=1, and the closes_issues path commits history from the linked worktree in Phase 1. However, three open sub-issues of #325 remain active because their scope is broader than the close-from-main problem.
Changes: This Group re-parents the three open sub-issues from #325 for continued tracking under the post-#436 workflow: - #277 — doc-append: rebase conflicts when both branches append to history.md / CHANGELOG.md (blast radius reduced post-#436: cross-branch race window narrowed because main-side appends are sequential; concurrent worktree-end races and manual doc-append conflicts remain) - #225 — commit gate ordering paradox (patterns 1/4 touch history-append; remaining patterns are general workflow-gate concerns) - #226 — enforce-worktree marker forgery resistance (marker is used by /worktree-end branch-delete authorization; same enforcement family as the history append sanctioned path) ## Related - Predecessor: #325 (closed status:migrated) - Parent fix: #436 (PR #474 merged 2026-05-23) — bin/compose-doc-append-entry split - Related parallel close: #464 (closed status:migrated to #436)

### FEATURE: fix(#612): issue-close-finalize/SKILL.md tmpfile pattern blocked from main worktree (2026-05-29, 9b0909e5819da9fefc603223a57b0458d79f2121, #636)
Background: PR #632 (#612) replaced `eval` with tmpfile+dot-source in `skills/issue-close-finalize/SKILL.md`. However, `issue-close-finalize` is designed to run from the **main worktree** (Step 10b). With `ENFORCE_WORKTREE=on`, `mktemp` and `> redirect` are write operations blocked by `enforce-worktree.js`. The skill becomes unusable from the main worktree — regression introduced in #612.
Changes: - Revert 6 tmpfile+dot-source blocks in `skills/issue-close-finalize/SKILL.md` back to `eval "$(bash ...)"` pattern: Pre-flight, Step A, Step A.5, Step E, Step G.5-1, Step G.5-3 - Add missing CHANGELOG.md entry for PR #632 (commit 11be81b was on the refactor branch, not on main)

### FEATURE: PR approval: hook approval phrase + direct sentinel emit (2026-05-29, c6ce71b313da4c19f18a16029e02cf417ff6e4c7, #635)
Background: PR approval フロー（ENFORCE_WORKTREE=on）で余分な往復が発生: commit-push Step 7a が PR URL を chat に表示して停止し、ユーザーがテキストでゴーサインを返信してから worktree-end Step 4 で WORKFLOW_USER_VERIFIED sentinel を発行する。 WORKFLOW_USER_VERIFIED sentinel 自体が ask 権限 dialog の最終承認メカニズムであり、show-user-verified-context.js が sentinel 発行直前に PR URL + staged files を dialog context に自動表示する。テキスト返信は構造的に不要。sentinel 直前の説明文もプロンプト依存でランダムになる。 show-user-verified-context.js / show-plan-link.js と同様に hook で定型的に出す手法がすでに存在し、それを適用すれば良い。
Changes: - `hooks/show-user-verified-context.js`: systemMessage に定型承認フレーズ追加（script-based） - `skills/_shared/user-verified.md`: Protocol 節を "narrative 不要; hook が context + 承認案内を表示" に更新 - `CLAUDE.md` Step 9: sentinel 直接発行の指示に更新 - `skills/worktree-end/SKILL.md` Step 4/3b: narrative 省略を明示

### FEATURE: PR #625 — fix/fix-608-session-close (2026-05-30, 20f9544, #625)
Background: feat(#608): add /session-close skill — Final Report after issue-close-finalize
Changes: Background: `/worktree-end` Step 7 emitted the Final Report before CLAUDE.md Step 10b ran `/issue-close-finalize --from-session`, so the report lacked history.md append status, resolved-by/appended sentinel posting, and WIP-clear status per issue. Under `ENFORCE_WORKTREE=off`, no Final Report was emitted at all (asymmetry).;Changes: New `/session-close` orchestration skill replaces CLAUDE.md Steps 10/10b — drives Phase 2 issue close via `/issue-close-finalize` and emits the Final Report on both worktree and branch/main paths. Removed Step 7 from `/worktree-end`. Added `closed_issue_outcomes` section to Final Report schema with section-scoped `stop-final-report-guard.js` validation (heading + bullet presence). Added Step L to `/issue-close-finalize` writing per-issue outcome JSON to `<PLANS_DIR>/<session-id>-issue-close-outcome.json`. Extended `bin/worktree-final-report.js` with `--outcome-file` arg and `closedIssueOutcomeLines` ctx. Updated golden fixture and existing test files. <!-- compose-doc-append-sentinel: branch=fix/fix-608-session-close pr=#625 -->

### FEATURE: fix(#581): remove CJK reject hook, deprecate ASK_LANG → CONV_LANG (2026-05-30, a1014efebfedad12e8f2a9cfff7d29cbfd84511a, #645)
Background: PR #593（issue #581）で導入した `check-ask-lang.js` / `detect-cjk.js` は： - 日本語・中国語漢字が同一 Unicode ブロックを共有するため区別不可能 - デフォルト `ASK_LANG=any` では hook が noop で実質何も機能していない AskUserQuestion の日本語表示「不自然さ」は解消されていない。
Changes: /deep-research 結果次第で条件分岐： **ケース A：hook に意味がない場合** - `hooks/check-ask-lang.js` 削除 - `hooks/lib/detect-cjk.js` 削除 - `.env` / `.env.example` から `ASK_LANG` 削除 - `settings.json` フック登録削除 - `hooks/lib/lang-config.js` から `ask` サーフェス削除 - 関連テスト削除 - CONV_LANG はプロンプト指示のまま **ケース B：hook に意味がある場合** - `check-ask-lang.js` を CONV_LANG 読み取りに変更 - ASK_LANG 廃止・統合 非ゴール：PLAN_LANG 変更は今回スコープ外。CONV_LANG への hook 強制追加は行わない。 Related to #581

### FEATURE: fix(worktree-end): capture-env.sh session-id resolution fails when CLAUDE_ENV_FILE is empty after VS Code restart or context compaction (2026-05-30, db3818062b89a70cbf9d33fbadcfc7909077cdcd, #642)
Background: During /worktree-end Step 5.5(b), capture-env.sh is called with the session-id as the 4th argument. The session-id is supposed to be read from $CLAUDE_ENV_FILE. However, after a VS Code restart or Claude Code context compaction+resumption, $CLAUDE_ENV_FILE is empty in Bash tool subprocesses (env vars set in the parent shell are not propagated into Bash tool calls after reconnect). Reproduction steps: 1. Start a workflow session via /workflow-init #N → write code → reach /worktree-end 2. During /worktree-end (or any point in the workflow), VS Code restarts or context is compacted and the session resumes from summary 3. Continue with /worktree-end Step 5.5(b): try to resolve session-id via $CLAUDE_ENV_FILE → empty 4. Fall back to listing $PLANS_DIR/*-intent.md and picking the last UUID-format filename → picks a random previous session's intent.md (e.g. #334 instead of the current #636) 5. capture-env.sh writes final-report-env.json keyed to the wrong session-id 6. Final Report shows wrong Closed Issues (#334 instead of #636) Root cause: no durable session-id recovery path exists when CLAUDE_ENV_FILE is unavailable.
Changes: - worktree-start should write the session-id into WORKTREE_NOTES.md (or a dedicated .worktree-session file) at creation time, so worktree-end can recover it without relying on CLAUDE_ENV_FILE - capture-env.sh should accept the session-id from WORKTREE_NOTES.md as a fallback when the 4th argument cannot be resolved from env - The fallback heuristic of listing intent files and picking the last UUID must NOT be used — it silently picks the wrong session

### FEATURE: Reduce context window via Agent delegation in workflow skills (2026-05-30, 99820f10b6248258e1d5604082ac5789c2e338c1, #644)
Background: メインコンテキストのウィンドウ節約のため、ワークフロースキルの重い処理を「internal Agent 委譲」アプローチで分離する。 `context: fork` タグではなく、SKILL.md が Skill ツール経由でメインコンテキストから実行されたまま、内部の重い処理を Agent 呼び出しに委譲する。sentinel は必ずメインコンテキスト側（SKILL.md ラッパー）が emit するため、sentinel reach 問題および AskUserQuestion hang バグ（#602）は発生しない。 ## Scope ### 委譲度: subagent化（完全委譲） - `issue-close-stage`: GH API 呼び出し全体 - `issue-close-finalize`: GH API + doc-append 全体 ### 委譲度: fork（部分委譲） - `update-docs`: doc-append 操作 - `worktree-end`: Step 5（バックアップ）+ Step 6i（doc-append） - `deep-research`: リサーチ処理本体 - `run-tests`: テスト実行本体 - `write-tests`: テスト作成処理本体 - `review-code-security`: セキュリティ調査処理本体 - `commit-push`（OPTIONAL）: git/gh サブ操作のみ（merge 確認はメインに残す） ## Constraints - `context: fork` タグは使用しない（sentinel reach 問題 + AskUserQuestion hang #602 回避） - sentinel は必ずメインコンテキスト側（SKILL.md ラッパー）が emit - AskUserQuestion 機能は保持 - `commit-push` の merge 確認・`worktree-end` の core インタラクションはメインに残す <!-- auto-created by clarify-intent -->
Changes: メインコンテキストのウィンドウ節約のため、ワークフロースキルの重い処理を「internal Agent 委譲」アプローチで分離する。

### FEATURE: enforce-worktree: ISSUE_CLOSE_SKILL=1 bypass broken for git commit from main worktree (2026-05-30, #595)
Background: During `/issue-close-finalize` (session 20260526-235949), `ISSUE_CLOSE_SKILL=1 git commit -m 'docs(history): record issue #N'` was blocked by enforce-worktree.js with the standard 'write blocked — main worktree' error. The same symptom was observed for `WORKTREE_END_SKILL=1 git worktree remove` during the same session. Previously working sessions (e.g. PR #584, #585, #586, #587) ran issue-close-finalize from main worktree successfully, so this is a regression. ## What was blocked - `ISSUE_CLOSE_SKILL=1 git commit -m 'docs(history): record issue #556'` → blocked - `ISSUE_CLOSE_SKILL=1 git add docs/history.md` → allowed (git add works) - `WORKTREE_END_SKILL=1 git worktree remove <worktree-path>` → blocked ## Impact - 4 history.md entries staged but not committed (#556 #555 #557 #582) - Worktree feature/triage-redesign not removed (deferred to /sweep-worktrees) ## Suspected cause PR #587 (fix(#577): block premature WORKFLOW_USER_VERIFIED emit) may have changed the hook in a way that affected the ISSUE_CLOSE_SKILL/WORKTREE_END_SKILL bypass predicates. ## Workaround Run manually from the main worktree: ``` git commit -m 'docs(history): record issues #556 #555 #557 #582' git push origin main ``` ## Acceptance criteria - `ISSUE_CLOSE_SKILL=1 git commit -m 'docs(history): record issue #N'` from main worktree succeeds - `WORKTREE_END_SKILL=1 git worktree remove <path>` from main worktree succeeds - Regression test added
Changes: During `/issue-close-finalize` (session 20260526-235949), `ISSUE_CLOSE_SKILL=1 git commit -m 'docs(history): record issue #N'` was blocked by enforce-worktree.js with the standard 'write blocked — m

### FEATURE: sweep-worktrees: gh pr list fails with --jq --arg (unknown arguments), all worktrees skipped (2026-05-30, 1981ea565f267a3b961f9d936c05a2ff1a828e89, #650)
Background: `bin/sweep-worktrees.sh` calls `gh pr list` with `--jq --arg branch "$branch" '...'` to check whether a branch's PR is merged. However, `gh pr list`'s `-q/--jq` flag does not pass-through jq's `--arg` option — the CLI treats `--arg`, the branch name, and the jq expression as three unknown positional arguments, exits 1, and the script emits "WARN: gh pr list failed" and skips every worktree. As a result, no zombie worktrees are ever reclaimed by `/sweep-worktrees` regardless of their state. Broken invocation: ```bash gh pr list --state merged --search "head:${branch}"     --json headRefName,number --limit 5     --jq --arg branch "$branch"     'map(select(.headRefName==$branch)) | length > 0' ```
Changes: Replace with `gh pr list -H "$branch" --state merged --json number --jq 'length > 0'`, which uses the built-in `-H` head-branch filter and a simple `length > 0` expression — no variable binding needed. Manually verified: ``` $ gh pr list -H "refactor/612-icf-refactor" --state merged --json number --jq 'length > 0' true  (exit 0) ```

### FEATURE: ISSUE_CREATE_PROJECT_NUM のデフォルト 1 が agents 固定で、他 repo の Project v2 自動 attach が失敗する (2026-05-30, 5f23d515e2a44a701696966abbeeb968abeb11cd, #641)
Background: `/issue-create` 経由で作成される issue は `bin/github-issues/issue-create.sh` により Projects v2 に自動 attach される設計だが、project number のデフォルトが agents repo 用の `1` 固定になっている。 ``` # bin/github-issues/issue-create.sh:17 PROJECT_NUM="${ISSUE_CREATE_PROJECT_NUM:-1}" ``` my-private-repo では Project v2 は #2 (`PVT_kwHOAMF_jc4BYZ4b` / "my-private-repo — Issue Timeline") であり、`ISSUE_CREATE_PROJECT_NUM=2` が環境に設定されていない状態で `/issue-create` を実行すると、#2 に attach されない (agents Project #1 に誤 attach される)。 実害: my-private-repo でのポスト migration issue (#41, #42, #44) が my-private-repo の Project v2 に存在せず、Project ビューでの一覧管理から漏れている。Migration (Steps 1–5) 自体は完了済み (#3〜#40 の 38 件は正常 attach)。 設計上の課題 (`rules/core-principles.md` §1 SSOT): - repo と Project v2 の対応付けが SSOT 化されていない — 各 repo の `.env` に `ISSUE_CREATE_PROJECT_NUM` を手動設定しなければ誤 attach が発生する - デフォルト値が agents 固定のため、複数 repo を扱う構成では silent failure になりやすい
Changes: 短期対応 (my-private-repo 側): - `agents/.env` に `ISSUE_CREATE_PROJECT_NUM=2` を追加 - 未 attach issue (#41, #42, #44) を `gh project item-add 2 --owner nirecom --url <url>` で手動補正 中期改善 (このファイル): - `PROJECT_NUM` のデフォルト値を廃止 → 未設定時に warn して skip、または repo 名から Project を解決する仕組みを導入 - attach 失敗時の warn を強化 (どの project number に attach しようとしたかを明示)

### FEATURE: SKILL.md: inline code block 禁止 — skills/<name>/scripts/ に切り出すルール化 (2026-05-30, 5d42647ab1a36df6d56c749ca37deb82d8963a5b, #630)
Background: #603 で `bin/run-codex-review-loop` を抽出したが、`make-detail-plan/SKILL.md` と `make-outline-plan/SKILL.md` の Step 5 には依然としてラッパー呼び出しのコードブロック（~15行）がインラインで残っている。SKILL.md 内のコードブロックはオーケストレーターのコンテキストウィンドウを圧迫する。 より一般的な問題として、スキルを追加・編集する際にコードブロックを SKILL.md に書くパターンが繰り返され、それが蓄積されている。
Changes: 1. **即時対応（make-detail-plan / make-outline-plan）**: Step 5 のラッパー呼び出しブロックを `skills/make-detail-plan/scripts/` / `skills/make-outline-plan/scripts/` 配下のスクリプトに切り出し、SKILL.md からの参照に置き換える 2. **ルール化**: `rules/prompt-criteria.md` または新規 `rules/skill-authoring.md` に「SKILL.md 内にコードブロックを書いてはならない — 3行以上のコードは `skills/<name>/scripts/` に切り出し、SKILL.md はそのパスへの参照のみ記載する」というディレクティブを追加 3. **適用範囲**: 今後あらゆる skills に何か追加・編集する際にこのルールを適用する Related to #513 Related to #513

### FEATURE: check-plan-lang: ## Issues 行の issue タイトルを PLAN_LANG チェックから除外 (2026-05-30, 667021a6fab54851b1251188d1e9d0ef0cbb9a50, #629)
Background: lint-plan-lang.js は PLAN_LANG=japanese のとき「CJK なし + 英単語4語以上連続」で違反を報告するが、intent.md の ## Issues セクションに書く issue タイトルは GitHub の英語タイトルをそのまま参照する形式（`- #N: <title>`）のため、英語タイトルの issue を扱うと必ず違反になる。現状の回避策はバッククォート wrap だが、参照値（固有名）と日本語散文を区別しない設計上の問題。
Changes: lint-plan-lang.js / lintPlanLang() に除外ルールを追加する: - `- #\d+:` で始まる行（## Issues セクションの issue タイトル参照行）は PLAN_LANG チェックをスキップ。あるいはより汎用的に: 行のうち issue タイトル参照部分（`#N:` 以降）を any 相当で扱い、残りの非タイトル部分のみを policy 対象にする。

### FEATURE: issue-create: Survey のキーワード不一致で同一現象の類似 issue を見落とす (2026-05-30, 712a3095d3c3c51e7ccff971a07faf1eb2eb8d1e, #661)
Background: /issue-create の Phase 2 Survey はキーワード検索ベースで重複を検出するが、同一現象を異なるファイル名・用語で表現した issue を見落とす。 実例: #562（detect-restart.sh / cc_restart reason 文字列）と #660（capture-env.sh / CC_RESTART_REQUIRED 誤検知）は同一の根本現象を指していたが、タイトルのキーワードに重複が少なく（#562 には "CC_RESTART_REQUIRED" も "誤検知" も含まれない）、#660 作成時の Survey で #562 を候補として返せなかった。結果として重複 issue が作成され、今回のセッションで手動で整合する必要が生じた。
Changes: - Phase 2 Survey のキーワード抽出を、タイトルトークンだけでなく現象の機能領域（スクリプト名から親ディレクトリ・feature 名など）に広げて retry pass を追加する。 - または Survey で 0 件が続いた場合、Claude が issue の Background 説明から別語彙セットを抽出して再検索するフォールバックを追加する。 - 検索がタイトルのみでなく body トークンも含む表現で実施されているか確認し、必要なら修正する。
