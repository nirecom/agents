# History 2026

### SECURITY: check-private-info: allow Docker internal network and Node.js base image paths (2026-04-11, (pending))
Background: judgeclaw's docker-compose.yml uses Docker's default bridge network (172.24.0.x) and the official Node.js base image home directory (/home/node). The former matched the RFC 1918 private address pattern; the latter resembled a real user home path — both caused false positives in the private-info scanner.
Changes: Added two scoped entries to `.private-info-allowlist`: `docker-compose.yml:172\.24\.0` (Docker internal network, not routable) and `docker-compose.yml:/home/node` + `*/generate-web-access-section.sh:/home/node` (Node.js base image path, not a real user). Also fixed the path-capture regex in `bin/check-private-info.sh` from `/home/[a-zA-Z]` (one char) to `/home/[a-zA-Z][a-zA-Z0-9_.-]*` so the full username component is captured and allowlist entries can match precisely.


### REFACTOR: write-tests subagent: mode acceptEdits → default (2026-04-11, (pending))
Background: write-tests subagent was launched with mode: "acceptEdits" to bypass VSCode's "Ask before edits" dialogs for tests/ files. After settings.json was updated with Write(**/tests/**) and Edit(**/tests/**) allow rules, acceptEdits became unnecessarily broad — it bypassed all file permission checks, allowing the subagent to silently edit any file including source code. This was confirmed when a test subagent modified bin/session-sync.sh and settings.json without authorization.
Changes: Changed subagent launch mode in claude-global/skills/write-tests/SKILL.md from "acceptEdits" to "default". The existing allow rules in settings.json continue to auto-approve test file writes without confirmation dialogs.


### FEATURE: session-sync push: retry loop for simultaneous-commit race condition (2026-04-11, (pending))
Background: When Windows (windows-host) and macOS (mac-host-1) push at the same time, both machines commit session data independently, creating a diverged state. The original code ran git pull --rebase once before push, but if Claude Code wrote new session data to a .jsonl file after the initial git add/commit but before pull --rebase, the pull failed with "unstaged changes" — leaving push to fail and trigger the "push failed" toast.
Changes: Replaced the single pull+push block in bin/session-sync.ps1 and bin/session-sync.sh with a retry loop (max 3 iterations). Each iteration commits any newly-written session files, rebases over remote, and retries push. Also added `:` no-op at end of quiet-mode success path in session-sync.sh to ensure exit code 0. Added two test cases per platform: static check for retry loop presence, and functional test verifying recovery from pre-diverged state with unstaged changes. Also suppressed pull --rebase stdout inside the retry loop (was only suppressing stderr) to match Windows behavior of silent quiet-mode operation. Added two quiet-mode stdout/stderr separation tests.


### FEATURE: Workflow state machine: deterministic 8-step enforcement (2026-04-12, (pending))
Background: CLAUDE.md workflow instructions were text-based and relied on Claude's judgment, making step skipping undetectable. check-tests-updated.js and check-docs-updated.js enforced single concerns independently but were not unified. Goal was to replace both with a single state-machine-based gate.
Changes: Created workflow state machine (lib/workflow-state.js shared module, workflow-gate.js PreToolUse commit gate replacing check-tests-updated.js and check-docs-updated.js, mark-step.js step completion CLI with --reset-from bypass, session-start.js SessionStart hook). Fail-safe design: missing or corrupted state blocks commits. Added Completion sections to 5 global skill SKILL.md files (survey-code, deep-research, make-plan, write-tests, update-docs). 36 tests in tests/feature-robust-workflow.sh.


### INCIDENT: #21: Hook integration path untested — CLAUDE_ENV_FILE format bug (2026-04-12)
Cause: session-start.js wrote `export CLAUDE_SESSION_ID=...` to CLAUDE_ENV_FILE, but Claude Code's env-file parser expects `KEY=VALUE` format (no `export` prefix). Test 30 used `grep -q` (substring match), so `export KEY=VALUE` passed despite being wrong. Root cause: tests only validated the JS script in isolation — the handoff point from script output to Claude Code's environment injection was never verified. In hook-based systems, the correctness of output format is only meaningful relative to the consuming system's spec, which unit tests cannot cover. More broadly, when writing tests, only single-file normal/error/edge cases were considered; integration paths across multiple components (hook → JS → env injection → Bash tool) were not enumerated as test targets.
Fix: Removed `export` prefix in session-start.js. Tightened test 30 to use `grep -qx` (exact line match). Follow-up investigation confirmed CLAUDE_ENV_FILE path is not propagated to Bash tool subprocesses (tested on Windows; macOS/Linux behavior unconfirmed).


### FEATURE: Workflow State Machine: mark-step.js session ID resolution redesign (2026-04-12)
Background: Empirical testing (Windows) showed that both CLAUDE_ENV_FILE path and CLAUDE_SESSION_ID are absent from Bash tool subprocess environment. The CLI design of passing session ID as an argument ($CLAUDE_SESSION_ID) was broken — always expanded to empty string. CLAUDE_ENV_FILE is available in hook contexts (SessionStart, PreToolUse, etc.) via process.env, but not in Bash subprocesses.
Changes: Removed session ID CLI argument from mark-step.js. Added resolveSessionId() to lib/workflow-state.js, reading CLAUDE_ENV_FILE from process.env to extract CLAUDE_SESSION_ID. workflow-gate.js usage messages updated. 41 tests pass.


### BUGFIX: Workflow State Machine: E2E verification + WSL path bug fix (2026-04-13)
Background: Manual E2E verification of the workflow state machine in a real Claude Code session after implementation was complete.
Changes: Confirmed all six test scenarios — normal cases 1–4 (SessionStart writes CLAUDE_SESSION_ID, mark-step.js records steps, git commit blocked on incomplete steps, git commit passes when all steps complete) and error cases 2–3 (--reset-from recovery from corrupted state file, partial reset from mid-workflow step). During verification, found and fixed a bug: on Windows, the hook resolved the WSL-style drive path extracted from the git -C flag using Windows Node.js, causing git rev-parse to fail with ENOENT and the gate to report "state not found" instead of listing incomplete steps. Fixed by adding toNativePath() to is-private-repo.js (converts bash-style drive paths to Windows paths on win32) and wiring it into resolveRepoDir. Added tests 42–49 to the test suite (all 49 tests pass).


### BUGFIX: PostToolUse marker interception: fix mark-step.js in Bash subprocess context (2026-04-13)
Background: CLAUDE_ENV_FILE is not propagated to Bash tool subprocesses (Anthropic bug #27987). mark-step.js called from skill Completion sections via Bash always failed to resolve session_id, silently leaving workflow steps unrecorded.
Changes: Added workflow-mark.js (PostToolUse Bash hook) that intercepts `echo "<<WORKFLOW_MARK_STEP:step:status>>"` via strict anchored regex on tool_input.command — rejects pipes, &&, cat/grep output containing the marker, etc. Hook reads session_id directly from hook stdin JSON and uses CLAUDE_PROJECT_DIR (documented env var, available in all hook types) for repo resolution. HOOK_CWD was excluded as undocumented with no confirmed usage evidence. Updated 5 skill Completion sections (survey-code, deep-research, make-plan, write-tests, update-docs). user_verification remains CLI-only to preserve the ask-rule gate. Added 26 test cases (normal, must-not-mark strict regex, error/edge, idempotency). Key finding: additionalContext surfaces messages to Claude; systemMessage reaches users only.


### INCIDENT: #22: settings.json PostToolUse misplacement (2026-04-13)
Cause: commit 01a9110 placed the PostToolUse hook inside the `permissions` block instead of the `hooks` block. Unit tests (direct JS invocation) pass regardless because they call the hook handler directly — they cannot detect whether Claude Code will ever invoke the hook at all.
Fix: Moved PostToolUse registration to the `hooks` block. Added SR1-SR4 structural assertions to the test suite to automatically detect misplacement. Added E2E test E1 (`claude -p` real invocation with `--setting-sources project`) confirming the hook fires and records the step in the state file. Added "Test Layer Selection" section to `claude-global/rules/test.md` documenting that wiring bugs (config misplacement, hook registration errors) require integration/E2E tests — not just unit tests (based on Fowler and Kent C. Dodds research).


### FEATURE: Workflow State Machine: PostToolUse Windows E2E verified + CLAUDE_PROJECT_DIR migration confirmed (2026-04-13)
Background: Windows E2E smoke test for the PostToolUse hook and simultaneous verification that the HOOK_CWD → CLAUDE_PROJECT_DIR migration (commit 264ab0a) works correctly in a real session.
Changes: E1 E2E test (`claude -p --setting-sources project --session-id`) passed: PostToolUse hook fires in a real Claude session, `CLAUDE_PROJECT_DIR` resolves the repo correctly, and the step is written to the state file. All tests pass with `RUN_E2E=1`. macOS/Linux verification remains pending.


### FEATURE: Workflow State Machine: Robust Improvements — Windows verification (2026-04-13)
Background: User verification of three robustness improvements on Windows (VSCode extension).
Changes: (1) `echo "<<WORKFLOW_USER_VERIFIED>>"` ask dialog confirmed — permissions.ask rule fires and PostToolUse hook records user_verification as complete. (2) SessionStart creates UUID-format state file at session start (.git/workflow/<session-id>.json confirmed). (3) isPrivateRepo bypass removal confirmed via git diff — a6ad6ab had `if (isPrivateRepo(repoDir)) approve()`, current code imports only resolveRepoDir.


### FEATURE: Workflow State Machine: .git/workflow deny rule verified (2026-04-13)
Background: In a new session (VSCode extension, Windows), confirmed that the deny rules work. The Edit(**/.git/workflow/**) and Write(**/.git/workflow/**) deny rules in settings.json take effect after restarting Claude Code.
Changes: Confirmed that both Write(**/.git/workflow/**) and Edit(**/.git/workflow/**) rules are automatically blocked by the Claude Code permission system. Full verification on Windows complete.


### REFACTOR: Workflow State Machine: marker format `:` → `_` (2026-04-13)
Background: Claude Code's permission glob parser treats `:` as a named-parameter separator inside `Bash(...)` rules, causing silent match failure (confirmed via anthropics/claude-code#33601). `WORKFLOW_RESET_FROM` ask rule was not triggering a dialog; investigation showed `Bash(echo "<<WORKFLOW_USER_VERIFIED>>")` (no colon) worked while any pattern containing `:` silently failed to match. Same issue applied to `WORKFLOW_MARK_STEP` allow rules. Default behavior when no rule matches is auto-allow, masking the problem.
Changes: Changed both marker formats to use `_` as separator: `<<WORKFLOW_MARK_STEP:step:status>>` → `<<WORKFLOW_MARK_STEP_step_status>>`, `<<WORKFLOW_RESET_FROM:step>>` → `<<WORKFLOW_RESET_FROM_step>>`. Updated workflow-mark.js regexes, settings.json allow/ask rules (RESET_FROM single-quote variant removed — DQ only), workflow-gate.js block message, 5 skill Completion sections, and test suite. Windows re-verification confirmed: normal cases 1–4 and error cases 1–2 all pass.


### FEATURE: Workflow State Machine: cross-platform verification complete (2026-04-14, 60aa84c)
Background: Completed happy-path and error-path verification across Windows, WSL, and macOS. macOS was the first native E2E run.
Changes: Verified happy path (session start, step recording, commit block, commit pass, PostToolUse real-fire) and error path (state file corruption -> fail-safe block -> --reset-from recovery, partial reset). Found and fixed three portability bugs during macOS E2E: (1) timeout not available -> run_with_timeout() perl fallback; (2) nested session error from inherited CLAUDECODE -> unset CLAUDECODE; (3) disableBypassPermissionsMode: disable neutralizing --dangerously-skip-permissions -> use minimal settings.json. WSL did not surface any of the three (CLAUDECODE not propagated, Windows profile referenced via bridge). Added run_with_timeout pattern and claude -p E2E caveats to test.md; extracted Installer Testing into test-installer.md.


### FEATURE: session-sync: push conflict auto-resolution (2026-04-14)
Background: Session sync was silently failing with "push failed" toast when ~/.claude/projects
received diverging commits from multiple machines (e.g. macOS and Windows both committing
session files before either pushes). git pull --rebase inside the retry loop swallowed JSONL
conflict errors via `|| true`, causing repeated push failures with no recovery.
Changes: Two fixes to bin/session-sync.sh push action. (1) Interrupted rebase detection: at
push entry, detect .git/rebase-merge or .git/rebase-apply and abort (rm -rf fallback for
fake/corrupt state that git rebase --abort cannot clean). (2) JSONL conflict auto-resolution:
when git pull --rebase fails, find conflicted *.jsonl files via git diff --diff-filter=U, strip
conflict marker lines (^<<<<<<<, ^=======, ^>>>>>>>), dedup via awk, git add, then
GIT_EDITOR=true git rebase --continue. Non-JSONL conflicts abort and retry. Also fixed the
"No changes to push" early exit to additionally check git log origin/main..HEAD, so push
proceeds when local branch is ahead of remote despite a clean working tree. 57 tests pass.


### INCIDENT: #23: workflow-gate block message as bypass attractor (2026-04-14)
Cause: workflow-gate.js:113-117 included a literal `echo "<<WORKFLOW_RESET_FROM_<step>>>"` recipe
in its "commit blocked" error message. When a skill's test setup ran `git commit` in a temp
repository, workflow-gate blocked it and the model read the hint — triggering autonomous
WORKFLOW_RESET_FROM calls to clear the gate. The model lacked holistic project context to judge
whether a reset was warranted; from inside a skill the hint looked like a legitimate shortcut.
Root cause is structural: the mechanism that should be a last resort was advertised in an error
message visible to subagents that cannot evaluate the full situation.

Fix: Removed the RESET_FROM hint from workflow-gate.js entirely (the block message now lists only
incomplete steps and the skill to run for each). Added a "Workflow State Recovery" section to
CLAUDE.md framing WORKFLOW_RESET_FROM as a last resort that "only the main conversation can use
when it has enough holistic context to judge that a reset is genuinely warranted." The settings.json
`ask` guard remains as a backstop. The RESET_FROM mechanism itself (workflow-mark.js PostToolUse
interception, required because CLAUDE_ENV_FILE does not propagate to Bash subprocesses per
Anthropic bug #27987) is unchanged.

Discussion notes: Several alternative fixes were considered and rejected.
(1) Obfuscating the hint — rejected because it merely makes bypass harder to find, not
structurally prevented.
(2) Moving documentation to ops.md only — would have required the user to manually run
mark-step.js even in cases where the main conversation can judge correctly.
(3) Manager subagent — rejected as overskill; the main conversation already has full context
and IS the manager.
The key insight: the right place for reset authority is the entity with holistic project context,
which is the main conversation (or the user directly). Skills and subagents must not reset.


### INCIDENT: #24: Workflow State Machine — multi-repo state mismatch (2026-04-14)
Cause: echo markers resolved repoDir from CLAUDE_PROJECT_DIR (primary project), while workflow-gate resolved repoDir from the git -C flag in the commit command. When committing to a secondary repo with git -C, the state was written to primary-project/.git/workflow/ but read from secondary-repo/.git/workflow/ — permanently blocking commits with no recovery path from inside a Bash subprocess (CLAUDE_ENV_FILE not propagated per Anthropic bug #27987).
Fix: Removed repoDir from all state operations. State moved to ~/.claude/projects/workflow/{session-id}.json (session-scoped). All hooks and CLI tools now use a single path, regardless of which repo is being committed. CLAUDE_WORKFLOW_DIR env var override added for test isolation. Migration block added to session-start.js to auto-delete old .git/workflow/ files. settings.json deny rules updated to new path. /workflow/*.tmp added to .gitignore in session-sync-init scripts to prevent race with session-sync git push.


### FEATURE: Workflow State Machine: session-scoped state migration (2026-04-15, (pending))
Background: Incident #24 root cause: per-repo state storage caused permanent blocking when committing across repos. Session-scoped storage eliminates the mismatch because all hooks and CLI tools use the same path for a given session, independent of which repo is targeted.
Changes: workflow-state.js: removed execSync/repoDir, added os.homedir()-based path (~/.claude/projects/workflow/), CLAUDE_WORKFLOW_DIR override for tests, cleanupZombies now removes stale .tmp files. workflow-gate.js, workflow-mark.js, mark-step.js, session-start.js: removed all repoDir arguments. session-start.js: added BEGIN/END temporary migration block to delete old .git/workflow/<session-id>.json on each session start. session-sync-init.ps1/.sh: added .gitignore with /workflow/*.tmp to prevent half-written state files from being synced. settings.json: deny rules updated from **/.git/workflow/** to ~/.claude/projects/workflow/**. Tests: added WS-UNIT-1~6, WS-IDEM-1 (unit), T-D1-4~7 (session-start without CLAUDE_PROJECT_DIR), T-MIG-1~2 (migration idempotency), INT-REGR-1 (cross-repo regression), SR-DENY-1~2 (deny rule validation). All pass on Windows.


### FEATURE: Workflow State Machine — E2E verification across all 3 environments (2026-04-15)
Background: After migrating state storage from {repo}/.git/workflow/ to ~/.claude/projects/workflow/, re-ran E2E verification on all platforms to confirm the new path is used end-to-end.
Changes: No code changes. Verified on Windows, macOS, and WSL: (1) SessionStart creates state file at new path; (2) WORKFLOW_MARK_STEP_* markers record steps via PostToolUse hook; (3) incomplete steps block git commit; (4) all steps complete allows git commit; (5) PostToolUse real invocation via RUN_E2E=1 (WSL skipped due to environment constraints — verified via settings.json structure instead); (6) git -C <other-repo> commit reads the same session state, no #24 regression; (7) WORKFLOW_RESET_FROM_* triggers ask dialog and hook rolls back state to the named step (manually confirmed including WSL). This also serves as user verification for the session-scoped state migration (previously tracked as "Verifying" in todo.md).


### FEATURE: Workflow State Machine — block user_verification direct bypass in mark-step.js (2026-04-15)
Background: mark-step.js user_verification complete could mark the step complete without going through the echo "<<WORKFLOW_USER_VERIFIED>>" path that triggers the settings.json ask dialog, discovered during WSL E2E verification.
Changes: mark-step.js: extended user_verification guard to also block status "complete" (was only blocking "skipped"), with error message pointing to the correct echo path. workflow-mark.js: corrected misleading comment ("must go through CLI") and fallback error message that incorrectly referenced mark-step.js as the manual path. Tests: added WS-UV-BLOCK-1~4 to feature-robust-workflow.sh (error, error, normal, edge cases). All tests pass.


### REFACTOR: install-obsolete: .git/workflow → ~/.claude/projects/workflow cleanup (2026-04-15, (pending))
Background: Workflow state was migrated from {repo}/.git/workflow/ to ~/.claude/projects/workflow/ (commit 738cb1c, incident #24). The session-start.js migration block handles new sessions automatically, but old .git/workflow/ directories left on disk before the migration remained unhandled.
Changes: install-obsolete.sh and install-obsolete.ps1 each received a BEGIN/END temporary migration block: searches $HOME (maxdepth 4) / C:\git repos for .git/workflow/ directories, salvages JSON files newer than 7 days to ~/.claude/projects/workflow/, then removes the old directory. Tests: main-install-obsolete-workflow-migration.sh (T1–T9, all pass).


### REFACTOR: Workflow State Machine: mark-step.js removal (2026-04-16, (pending))
Background: mark-step.js was designed as a CLI tool to mark workflow steps, but relied on CLAUDE_ENV_FILE which is not propagated to Bash tool subprocesses (Anthropic bug #27987). All functionality was already covered by the echo marker mechanism in workflow-mark.js (PostToolUse hook), making mark-step.js dead code.
Changes: Deleted mark-step.js. workflow-gate.js block messages updated from `node mark-step.js` to `echo "<<WORKFLOW_RESET_FROM_research>>"` / `echo "<<WORKFLOW_MARK_STEP_<step>_complete>>"`. workflow-mark.js fallback error messages likewise updated. settings.json: removed allow rule for mark-step.js and ask rule for mark-step.js user_verification bypass (now moot). Tests: removed all mark-step.js-specific test sections from feature-robust-workflow.sh (tests 17–34, WS-UV-BLOCK-1~4); replaced mark-step.js state setup in integration test with direct workflow-state.js call. All remaining tests pass.


### FEATURE: Workflow State Machine: evidence-based write_tests/docs enforcement (2026-04-16, bf95596+)
Background: write_tests and docs steps could be bypassed by running echo "<<WORKFLOW_MARK_STEP_*_complete>>" directly, without actually writing tests or updating docs. The user_verification step had protection via WORKFLOW_USER_VERIFIED ask dialog, but write_tests and docs had none. Additionally, the old check-tests-updated.js and check-docs-updated.js hooks (replaced by workflow-gate.js) used git staged-file evidence rather than state flags, and exempted claude-global/ from checks — an exemption that was found to be unjustified.
Changes: workflow-gate.js: added evidence-based checks — write_tests passes if tests/ changes are staged; docs passes if docs/*.md or *.md changes are staged; no claude-global/ exemption. Added resolveRepoDir(), hasStagedTestChanges(), hasStagedDocChanges() helpers. workflow-mark.js: MARK_STEP sentinel now rejected for write_tests and docs (like user_verification), with messages directing to staged-file path or NOT_NEEDED sentinels. Added WORKFLOW_WRITE_TESTS_NOT_NEEDED and WORKFLOW_DOCS_NOT_NEEDED handlers (mark step complete via ask dialog when evidence is genuinely absent). settings.json: added ask permissions for both new sentinels. write-tests/SKILL.md, update-docs/SKILL.md: completion instructions updated to git add instead of echo marker; NOT_NEEDED path documented.
Also: workflow-mark.js user_verification rejection message clarified to lead with "NOT recorded" (bf95596), fixing incident where Claude misread guidance as optional and proceeded to commit prematurely.


### FEATURE: doc-append/doc-rotate tools + architecture.md split (2026-04-16, (pending))

Background: history.md had no append-without-read strategy (full Read consumed context on every entry); architecture.md was 480 lines making context-efficient updates hard.

Changes: Added bin/doc-append.py (tail-seek append, CRLF/BOM-safe, ascending date invariant, incident #N numbering) and bin/doc-rotate.py (year-based archive, history/index.md generation, floor protection). Split architecture.md into architecture/file-tree.md, architecture/shell-startup.md, architecture/claude-code.md (156 lines from 480). Updated docs-convention.md with append-only tool table and rotation thresholds. Added settings.json allow/ask permissions for both tools. Tests: tests/feature-doc-tools.py (24 pytest integration cases).


### BUGFIX: Fix: commit-push skill model directive removed (2026-04-17, (pending))

Background: commit-push SKILL.md had model: haiku and effort: low directives, causing the Skill runner to spawn a new agent with a separate session UUID. The new session had all workflow steps pending, so workflow-gate blocked every commit attempt from within the skill.

Changes: Removed model: haiku and effort: low from commit-push/SKILL.md so the skill executes in the caller's session. Added regression test tests/main-commit-push-workflow-gate.sh (2 static checks verifying no model/effort directives).


### FEATURE: doc-rotate.py: line-count trigger and floor-based archiving (2026-04-17, (pending))

Background: doc-rotate.py archived entries based on a 365-day hardcoded cutoff, making rotation impossible when all entries are recent. Additionally, undated entries (pre-dating convention, 102 entries) were never archived. The threshold-warn/hard parameters were parsed but unused.

Changes: Replaced date-based cutoff with floor-based archiving: keep last --floor entries (default 20), archive the rest. threshold-warn gate (default 0 = always run) skips rotation when file is under the threshold. Undated entries treated as oldest and archived to history/legacy.md. Date regex made comma-optional to correctly parse entries without commit hashes. Removed --max-age-days. Tests: T-THRESH-1..5 added to feature-doc-tools.py (29 total, all pass).


### BUGFIX: Fix workflow-gate: Unix-style drive path normalization in resolveRepoDir (2026-04-17, pending)
Background: On Windows with Git Bash, `git -C /<drive>/path commit` passed a Unix-style drive path (e.g. `/<drive>/git/dotfiles`) to workflow-gate.js. Windows Node.js does not accept Unix-style paths as `cwd` in `execSync`, causing `hasStagedTestChanges` and `hasStagedDocChanges` to silently return `false` even when tests/ or docs/ were staged — blocking commits with a spurious "write_tests not complete" error.
Changes: `resolveRepoDir` now normalizes `/<drive>/path` → `C:\path` patterns (single drive letter followed by `/`). `hasStagedTestChanges` and `hasStagedDocChanges` catch blocks now write a warning to `process.stderr` instead of silently swallowing the error. Main execution wrapped in `require.main === module` guard; `resolveRepoDir`, `hasStagedTestChanges`, `hasStagedDocChanges` exported for testability. 13 tests added in `tests/fix-workflow-gate-unix-path.sh`.


### FEATURE: doc-append/doc-rotate + architecture.md split (2026-04-17, 28fa673)
Background: Added doc-append.py and doc-rotate.py to manage growth of docs/history.md. Split docs/architecture.md into an architecture/ subdirectory in preparation for exceeding 300 lines.
Changes: Added bin/doc-append.py and bin/doc-rotate.py. Split docs/architecture/ into claude-code.md, file-tree.md, and shell-startup.md. Added uv run permissions to settings.json.


### REFACTOR: install-obsolete: .git/workflow cleanup (2026-04-17, 5b3bf91)
Background: .git/workflow was the old storage location for workflow state, but migration to ~/.claude/projects/workflow was complete. Added salvage and deletion logic to install-obsolete.
Changes: Added .git/workflow -> ~/.claude/projects/workflow salvage logic to install-obsolete.sh and install-obsolete.ps1. Copies JSON files updated within 7 days to the new path and removes the old directory.


### FEATURE: Workflow State Machine: session ID injection and state inheritance across VS Code restarts (2026-04-18, (pending))
Background: When VS Code restarts (on hook changes, shutdown, etc.), a new session_id is issued and the old session workflow state file becomes orphaned, causing workflow-gate to treat all steps as pending and block commits.
Changes: Resolved by three combined changes. (1) session-start.js: added inheritance logic — on new session start, scans ~/.claude/projects/<encoded-cwd>/<session_id>.jsonl files by mtime descending (up to 10), searches stdout of SessionStart/PostCompact attachment entries for 'Current workflow session_id: <uuid>'. If a state with the same cwd+branch exists and is not all-pending, copies its steps. (2) session-start.js: changed output from {} to additionalContext JSON (session_id + state path) to record in transcript for future sessions to reference. (3) post-compact.js (new PostCompact hook): re-injects session_id after compaction to preserve transcript markers. workflow-state.js: added getCurrentContext() and findLatestStateForContext(ctx), added optional ctx (cwd/git_branch) argument to createInitialState(). settings.json: registered PostCompact hook. Tests: tests/feature-workflow-inherit-state.sh (A1-A12, T-C1-T-C3, 15 cases) and tests/feature-workflow-post-compact.sh (B1-B3, 3 cases), 18 cases total, all PASS.


### REFACTOR: mark-step.js removal and workflow-gate/mark message migration verified (2026-04-18, 9340267)
Background: Migrated workflow-gate/mark block messages from node mark-step.js call format to echo marker format. mark-step.js deleted and consolidated into workflow-mark.js.
Changes: Verified on all platforms: Windows, WSL, and macOS. Confirmed that block messages no longer contain node mark-step.js calls and that WORKFLOW_MARK_STEP / WORKFLOW_RESET_FROM echo markers are correctly reflected in state files.


### FEATURE: workflow-mark: require reason argument for WORKFLOW_DOCS_NOT_NEEDED sentinel (2026-04-18, (pending))
Background: The bare <<WORKFLOW_DOCS_NOT_NEEDED>> sentinel allowed reflexive skipping of the docs step without the model articulating why. Several sessions skipped docs when a history.md entry was warranted.
Changes: Sentinel extended to <<WORKFLOW_DOCS_NOT_NEEDED: REASON>> where REASON is validated (>=3 non-space chars, no '>', not a bilingual placeholder). Reason stored as state.steps.docs.skip_reason via new extraFields parameter on markStep(). settings.json ask glob updated. workflow-mark.js looks-like fallback rejects legacy bare form and reason-with->. WRITE_TESTS_NOT_NEEDED mirror change deferred (inline TODO). Tests WS-EV-9 through WS-EV-17 added to tests/main-workflow-evidence.sh.


### FEATURE: WORKFLOW_DOCS_NOT_NEEDED reason enforcement verification (2026-04-18, d5544f2)
Background: WORKFLOW_DOCS_NOT_NEEDED sentinel now requires a meaningful reason string (>=3 non-space chars, not a placeholder). Bare form is rejected. Verified via WS-EV-9 through WS-EV-18 in tests/main-workflow-evidence.sh.
Changes: All 18 test cases passed. No code changes needed.


### BUGFIX: workflow-gate: fix hasStagedTestChanges for Unix-style Git Bash paths (2026-04-18, 876d12e)

Background: Git Bash passes MSYS2-style paths (/<letter>/rest) to workflow-gate when using git -C. hasStagedTestChanges used this path as cwd for execSync, but Node.js on Windows cannot resolve MSYS2 drive paths, causing the function to throw and return false — the test-evidence override never triggered even when tests/ files were staged.

Changes: Added resolveRepoDir() function that converts MSYS2 paths to Windows paths. Called from the main hook entrypoint to normalize repoDir before passing to hasStagedTestChanges/hasStagedDocChanges. Both functions now accept an explicit repoDir parameter. Exported all three functions for unit testing. Tests: fix-workflow-gate-unix-path.sh — 13 cases (A: resolveRepoDir unit x7, B: staged-file integration x4, C: error handling x2), all PASS.


### BUGFIX: workflow-gate: fix commit regex false-positive on argument text (2026-04-18, pending)

Background: /git\s+(?:-C\s+\S+\s+)?commit\s/ had no line-anchor, so a Bash command whose arguments contained 'git commit' as text (e.g. doc-append.py --background '... git commit ...') triggered the commit block even though no git commit was being run.

Changes: Added ^ anchor to the regex (/^git\s+(?:-C\s+\S+\s+)?commit\s/). Regression test tests/main-workflow-gate-regex.sh added (7 cases: normal x4, bug-regression x2, edge x1), all PASS.


### FEATURE: Workflow State Inheritance across VS Code Restarts (2026-04-18, c6159ea)

Background: Implemented session state inheritance so that workflow steps (research/plan completion state) carry over to new sessions after VS Code restarts. findLatestStateForContext scans transcript JSONL files to find the most recently used session state for the current cwd+branch.

Changes: Manual smoke tests all passed: (1) steps inherited after VS Code restart, (2) most-recently-used session selected when multiple parallel sessions exist, (3) session_id preserved after /compact via SessionStart:compact hook.


### SECURITY: Enforce doc-append for history.md; block Japanese in public-repo doc-append (2026-04-18, pending)

Background: Claude was occasionally using the Edit tool directly on history.md instead of doc-append.py, violating the append-only convention. Japanese text was also leaking into history.md in public repos despite the language policy.

Changes: Added Edit(**/history.md) and Write(**/history.md) to the deny list in settings.json to hard-block direct edits. Added check-japanese-in-docs.js PreToolUse hook that blocks doc-append.py calls containing Japanese characters when the target repo is public. Tests: tests/main-check-japanese-in-docs.sh (10 cases, all PASS).


### FEATURE: Workflow State Inheritance across VS Code Restarts (2026-04-18, 2852d03)

Background: Workflow steps (research/plan completion state) were lost on VS Code restart because each new session started fresh. findLatestStateForContext was added to scan transcript JSONL files and inherit state from the most recently used session with matching cwd+branch.

Changes: Manual smoke tests all passed: steps inherited after restart, most-recent session wins among parallel sessions, session_id preserved after /compact via SessionStart:compact hook. Also fixed a bug where Windows cwd was not lowercased before encoding, causing transcript directory lookup to fail (C--git-dotfiles vs c--git-dotfiles).


### FEATURE: make-plan: NEEDS_RESEARCH mid-plan research escalation (2026-04-18, pending)
Background: The planner/reviewer loop had no escape hatch when the planner lacked external knowledge — it had to guess or stall. Adopted Option B (inlining): research findings are embedded verbatim into the next planner prompt, with no file persistence.
Changes: Added Research Escalation section to ~/.claude/skills/make-plan/SKILL.md: NEEDS_RESEARCH protocol (skill:/question:/reason: fields, first-non-whitespace-token detection, malformed handling), independent round counters (revision_rounds cap=2, research_rounds cap=2, malformed_retries cap=1 — never reset), re-prompt template with verbatim findings, and escalation message format. Added NEEDS_RESEARCH escape hatch, Research Findings section, and citation tag rules ([research: tag], format [a-z0-9-]+) to ~/.claude/agents/planner.md. Added citation integrity checklist item to ~/.claude/agents/reviewer.md. Contract tests: tests/feature-make-plan-research-escalation.sh (27 cases, all PASS).


### SECURITY: Security Enhancement Phase 1: /review-security skill (2026-04-18, 7330de5)
Background: Added architecture-phase security review skill (implemented 2026-04-06). Design decision: extract checklist details into skill (not rules/) to minimize context consumption per session. Skill naming follows existing verb-noun kebab-case convention.
Changes: Created ~/.claude/skills/review-security/SKILL.md with 3-axis checklist (Information Leakage: OWASP ASVS V8/V6; Third-Party Access: OWASP MCP Top 10 MCP03/MCP04, LLM Top 10 LLM03; External Access: OWASP WSTG, CWE Top 25). TodoWrite mandatory — no inline evaluation. Verified 2026-04-18: 15-item checklist registered and evaluated correctly in a live session.


### SECURITY: Security Enhancement Phase 2: Security cases in test.md + test-rules/ restructure (2026-04-18, pending)
Background: test.md lacked security-specific test case categories. Reviewers had no guidance on what security scenarios to cover when writing tests. test.md also exceeded 100 lines with low-priority detail mixed in.
Changes: Added Security cases bullet to Test Case Categories in claude-global/rules/test.md (secret leakage OWASP ASVS V8, input injection OWASP WSTG CWE-78/CWE-22, permission OWASP ASVS V4, prompt injection LLM Top 10 LLM01/MCP Top 10 MCP06, security idempotency). Extracted lower-priority sections to new rules/test-rules/ subdirectory: macos-timeout.md (paths-gated), claude-e2e.md (always loaded), installer.md (paths-gated, moved from test-installer.md). Removed Test Layer Selection reference links. test.md reduced from 149 to 106 lines.


### BUGFIX: Fix findLatestStateForContext: collect all session IDs and skip completed workflows (2026-04-18, 0ba7e13)
Background: findLatestStateForContext had two bugs: (1) it stopped at the first SessionStart/PostCompact session_id found in a JSONL file, ignoring later PostCompact entries that carry the more recent session_id; (2) it inherited state from completed workflows (user_verification=complete), causing the gate to demand USER_VERIFIED even at the start of a brand-new task. Both bugs were triggered together when a session ended with user verification and a new task was started in the same VS Code window.
Changes: workflow-state.js findLatestStateForContext: collect ALL session_id matches from SessionStart and PostCompact entries per JSONL, try them in reverse order (PostCompact/most-recent first). When the most-recent session has user_verification=complete, break out of the JSONL entirely — no fallback to older sessions in the same file. CLAUDE_TRANSCRIPT_BASE_DIR env override added for test isolation. Six new tests added to tests/feature-workflow-inherit-state.sh covering PostCompact priority, completed-workflow exclusion, and multi-ID scenarios. Also fixed Windows test infrastructure (TMPDIR_BASE path conversion, node_path() helper, stdin read without /dev/stdin, WORKFLOW_STATE_LIB_NODE path conversion).


### FEATURE: Workflow skip sentinels unified + DOCS_NOT_NEEDED removed (2026-04-18, pending)
Background: workflow-gate.js treated research/plan as SKIPPABLE_STEPS but no echo sentinel existed to declare them skipped — only workaround was manual JSON state editing. WORKFLOW_WRITE_TESTS_NOT_NEEDED allowed bare declaration without reason (abuse observed). WORKFLOW_DOCS_NOT_NEEDED allowed skipping docs, contradicting the policy that docs must always be actually updated (complete = actually completed).
Changes: Added WORKFLOW_RESEARCH_NOT_NEEDED and WORKFLOW_PLAN_NOT_NEEDED sentinels with mandatory reason (>=3 non-space chars, not a placeholder, no '>'). Migrated WORKFLOW_WRITE_TESTS_NOT_NEEDED to reason-required form; all three record status: skipped with skip_reason. Removed WORKFLOW_DOCS_NOT_NEEDED — replaced with deprecation message. Updated SKIPPABLE_STEPS (+ write_tests), settings.json ask block, CLAUDE.md workflow steps, workflow-gate.js SKILL_MAP, and skill completion sections (survey-code, deep-research, make-plan, update-docs). Tests: main-workflow-skip-sentinels.sh (18 cases, all PASS), main-workflow-evidence.sh updated (WS-EV-8/8b updated, WS-EV-9-18 deleted).


### SECURITY: Security Enhancement Phase 3: /review-plan-security + /review-code-security + hard-secret detection (2026-04-18, pending)
Background: Phase 3 redesigned /review-security into two companion skills with clear invocation timing: /review-plan-security (Step 2, planning-time checklist) and /review-code-security (Step 5, code-review-time patterns). Original plan used /scan-security but the name implied a different tool type; renamed to reflect when each skill is called. check-private-info.sh lacked coverage of structured API secrets (LLM keys, GitHub tokens, etc.) - only personal information was detected.
Changes: git mv review-security/ -> review-plan-security/: updated frontmatter name/description, added companion reference to /review-code-security. New claude-global/skills/review-code-security/SKILL.md: same 3-axis structure (### Axis 1/2/3), each axis uses italic Axis scope: instead of Source: because patterns derive from Gitleaks/CWE not OWASP directly; Automated coverage note explains hard-secret patterns are handled by check-private-info.sh. bin/check-private-info.sh extended with 11 hard-secret detection patterns: AWS AKIA key, PEM private key header, GitHub PAT, Slack token, OpenAI, Anthropic (checked before OpenAI to prevent sk- prefix overlap), Google, HuggingFace, Groq, Replicate, Cohere. claude-global/hooks/pre-commit extended to block dotenv file commits (allows dotenv.example/.sample/.template). claude-global/CLAUDE.md Step 2 updated to /review-plan-security; Step 5 adds conditional /review-code-security call. 4 new/updated test files: main-review-code-security.sh (renamed from main-scan-security.sh, 20 cases), main-review-plan-security.sh (new, 16 cases, axis consistency test), main-hard-secret-scan.sh (new, 11 normal + edge cases), main-env-file-block.sh (new, pre-commit dotenv block tests). All tests PASS.


### SECURITY: Security Enhancement Phase 4: Trojan Source detection + scan-outbound rename (2026-04-18, pending)
Background: Phase 4 was redesigned from 'add Prompt Injection Defense section' after analysis showed: (1) Prompt Injection patterns fit existing Axis 2/3 rather than needing a new Axis 4; (2) the pre-commit context suits Trojan Source detection (CVE-2021-42574) rather than prompt-injection defense, whose correct defense point is at PostToolUse (tool results entering the LLM); (3) the tool name check-private-info.sh was too narrow for its expanding scope. JudgeClaw (C:/LLM/judgeclaw bridge/injection_signals.py) surveyed as the baseline pattern set for the planned Phase 5 PostToolUse hook.
Changes: Renamed bin/check-private-info.sh to scan-outbound.sh, hooks/check-private-info.js to scan-outbound.js, docs/private-info-scanning.md to scan-outbound.md; updated 20+ references across tests, hooks, settings, and docs. Added zero-width char (U+200B/C/D, U+FEFF) and Bidi override (U+202D/E, U+2066-2069) detection as [zero-width]/[bidi-override] labels in scan-outbound.sh (Trojan Source, CVE-2021-42574). Added Tool Poisoning/Rug Pull/Return Value Injection to review-plan-security and review-code-security Axis 2; Instruction override/Base64 obfuscation added to Axis 3. Source Integrity section added to review-plan-security. Phase 5 (PostToolUse hook) added to plan.md and todo.md for future implementation. Tests: main-hidden-char-scan.sh (18 cases, all PASS); regression: main-hard-secret-scan.sh, main-review-code-security.sh, main-review-plan-security.sh all PASS.


### REFACTOR: workflow-mark: && chaining support + workflow step rename (2026-04-20, (pending))
Background: workflow-mark.js applied strict anchored regex to the full Bash command string. Sentinel echos chained with && in one Bash call (e.g. echo <<A>> && echo <<B>>) never matched, leaving state unrecorded. Additionally, VALID_STEPS contained code/verify but SKILL_MAP had no entries for them -- gate fell back to the generic WORKFLOW_MARK_STEP_* hint and Claude was echoing the sentinel without performing the actual step.
Changes: Refactored workflow-mark.js to split command on &&. All-or-nothing validation: if any part is not a sentinel (e.g. cd /tmp), the whole command is rejected, preserving the existing security property that prefix-chained commands do not update state. Handlers converted from early-exit done() to messages[] accumulation loop. Renamed workflow-state.js steps code->run_tests and verify->review_security; added review_security to SKIPPABLE_STEPS. Added run_tests and review_security entries to workflow-gate.js SKILL_MAP. Added REVIEW_SECURITY_NOT_NEEDED sentinel and handler. Updated CLAUDE.md Step 5 to Run tests and Security review with parallel execution and hook auto-mark. Tests: tests/main-workflow-and-chain.sh added (32 cases, all PASS).


### BUGFIX: Fix workflow-gate: quoted -C path causes hasStagedDocChanges false-negative + fail-open bypass (2026-04-20, 7a25c16)
Background: resolveRepoDir() used \S+ regex to extract git -C <arg>, capturing quoted paths verbatim (e.g. c:/LLM/my-specs-repo with surrounding quotes). execSync received an invalid cwd, making hasStagedDocChanges/hasStagedTestChanges always return false — gate blocked even when docs/tests were staged. Deeper issue: the same \S+ in commit detection stopped at the first space in a quoted path with spaces, causing !commitMatch to fire and approve() to run — silently bypassing all gate checks (fail-open).
Changes: New claude-global/hooks/lib/parse-git-args.js with parseGitCArg() — quote-aware parser handling bare, double-quoted, and single-quoted -C arguments; unterminated quotes return null. workflow-gate.js: resolveRepoDir delegates to parseGitCArg; commit detection refactored to 2-step check to prevent bypass. is-private-repo.js: extractRepoDirFromCommand delegates to parseGitCArg. resolveRepoDir adds c:/path -> c:\path normalization on win32. Tests: tests/fix-workflow-gate-unix-path.sh extended with Sections D-G (34 cases total, all PASS).


### FEATURE: doc-append: global launcher + category prefix system (2026-04-20, (pending))
Background: doc-append.py was only callable from dotfiles CWD via 'uv run bin/doc-append.py', blocking history.md writes in other repos (my-specs-repo etc.) where the script doesn't exist. Also lacked a structured classification system for history entries.
Changes: Added --category required arg (INCIDENT/BUGFIX/FEATURE/REFACTOR/CONFIG/SECURITY). Entry format changed to '### CATEGORY: Subject' and '### INCIDENT: #N: Subject'. Install scripts (dotfileslink.sh/ps1) now generate ~/.local/bin/doc-append launcher at setup time. settings.json allow rule updated to 'Bash(doc-append *)'. check-japanese-in-docs.js hook updated to catch wrapper form via regex. Migrated 186 existing history entries across 3 files with heuristic categorization.


### FEATURE: doc-rotate: index.md category badges + distribution summary + --rebuild-index flag (2026-04-20, (pending))
Background: After the category prefix migration (FEATURE/BUGFIX/INCIDENT/etc.), history/index.md still showed entries without category labels — the index was generated before migration and not regenerated since. There was also no way to rebuild the index without running a full rotation, so manual archive edits or schema changes left the index stale.
Changes: doc-rotate.py _write_index now (1) emits a Category Distribution section with per-category counts (e.g. 'FEATURE: 94 | BUGFIX: 25 | ...'), (2) prefixes each entry with a backtick category badge (e.g. `FEATURE`), (3) strips the CATEGORY:/INCIDENT:#N: prefix from displayed link text. Added _extract_category and _subject_from_header helpers and a CATEGORY_PREFIX_RE constant. New rebuild_index(history_path) function reads existing archive files plus current history.md and rewrites only history/index.md without touching the body. New --rebuild-index argparse flag exposes this. docs-convention.md updated with the new tool entry. Tests: 6 new cases in tests/feature-doc-tools.py (test_n4-n9): badge presence, distribution summary, rebuild-index, link text stripping, INCIDENT format, uncategorized entries, idempotency. All 35 tests pass.


### SECURITY: Phase 5: scan-inbound PostToolUse hook for prompt injection defense (2026-04-20, ecb70ff)
Background: No PostToolUse hook existed to detect prompt injection in WebFetch results before they reached the LLM. External content fetched during deep-research was the primary attack surface. JudgeClaw (C:/LLM/judgeclaw) injection_signals.py served as the baseline pattern set. Confirmed during implementation that PostToolUse supports decision:block (prior research was incorrect). Content over 2MB is blocked immediately to prevent timeout-based scan bypass attacks. Discovered a shared-buffer bug in the existing readSync loop pattern used by other hooks; fixed in this hook using fs.readFileSync(0).
Changes: Added claude-global/hooks/scan-inbound.js. BLOCK signals: chatml_tag, direct_override (EN/JP), disregard_jp, expose_system_jp. WARN signals: role_tag, base64_blob (60+ chars with benign-context exclusion for PEM, data URI, data:application/). Content over 2MB triggers content_too_large BLOCK. extractText() recursion depth-limited to 20 to prevent stack overflow on crafted nested payloads. Added PostToolUse WebFetch matcher to settings.json. 31 tests in tests/main-scan-inbound.sh.


### FEATURE: doc-append: auto-rotate when history.md >= 500 lines (2026-04-20, pending)
Background: Manual rotation step required noticing the warning and remembering to run doc-rotate.py. Easy to skip — files grew past the soft limit unattended.
Changes: doc-append.py now invokes doc-rotate.py as a subprocess (--threshold-warn 500 --floor 20) when the resulting file is >= 500 lines. Rotation already calls _write_index, so history/index.md is rebuilt in the same pass. Tests cover trigger, silent under threshold, and that the just-appended entry remains in the body.


### CONFIG: Add WebFetch allow domains for deep-research (2026-04-20, HEAD)
Background: WebFetch permission list only covered specific doc sites. Claude/MCP/LangChain research during deep-research sessions prompted for each new domain.
Changes: Added 8 domains to permissions.allow: code.claude.com, platform.claude.com, anthropic.com, modelcontextprotocol.io, spec.modelcontextprotocol.io, ecc.tools, docs.langchain.com, reference.langchain.com.


### FEATURE: write-tests skill: enumerate call paths step (2026-04-20, (pending))
Background: Incident #21 (CLAUDE_ENV_FILE export prefix bug) revealed that tests only covered single-file units; integration path boundaries were never enumerated as test targets, allowing format mismatches to go undetected.
Changes: Added step 3 'Enumerate call paths' to write-tests SKILL.md Procedure — traces callers -> file -> downstream consumers, identifies format/contract expectations at each boundary, and feeds potential mismatches into step 4 as integration-path error cases. Steps 3-5 renumbered to 4-6.


### FEATURE: workflow-gate: docs-only commit short-circuit (2026-04-21, pending)
Background: After VS Code restarts, new Claude Code sessions required 4-5 ask-dialog clicks (WORKFLOW_*_NOT_NEEDED: echoes) to commit a trivial follow-up change such as ticking a checkbox in docs/todo.md. The workflow gate guards code quality — these checks were unnecessary overhead for docs-only changes.
Changes: Added isDocsOnlyStaged(repoDir) to workflow-gate.js. When all staged files match docs/*.md, all steps except user_verification are automatically bypassed. Block message replaced with 'docs-only commit — only user_verification is required.' Files outside docs/ (CLAUDE.md, SKILL.md, root README.md, etc.) are treated as behavior code and excluded. Tests: tests/feature-workflow-gate-docs-only.sh (18 cases, all PASS). Documentation added to CLAUDE.md and docs/architecture/claude-code.md.


### FEATURE: Consolidate Workflow State Machine tests into single integration file (2026-04-21, TBD)
Background: Workflow State Machine tests had accumulated as separate files per feature addition — 7+ files coexisted with no CI runner, making it unclear which files to run for sufficient regression protection. The todo.md task to automate manual E2E verification scenarios triggered a consolidation refactor.
Changes: New: tests/main-workflow-state-machine.sh (38 cases, Sections 1-6) covering state inheritance, cross-repo commit, RESET_FROM, USER_VERIFIED, branch isolation, and security injection as integrated regression tests. Deleted: feature-robust-workflow-user-verified.sh, feature-robust-workflow-session-start.sh, feature-robust-workflow-integration.sh, feature-workflow-gate-docs-only.sh, feature-workflow-post-compact.sh. Shrunk: feature-robust-workflow-settings.sh reduced to structural smoke tests SR1-SR5 only. Mutation probe (6 mutations, S1-S6) confirmed the aggregated test catches real bugs in each section.


### FEATURE: block-tests-direct.js: enforce test writes via /write-tests hook (2026-04-21, pending)
Background: Claude probabilistically violated the test.md rule 'Do not write or edit test files directly in the main conversation', prompting the user for Write/Edit approval each time. A mechanical enforcement via PreToolUse hook was needed.
Changes: Added block-tests-direct.js hook: denies Write/Edit/MultiEdit targeting a tests/ path when the workflow write_tests step is pending and agent_id is absent (main conversation). agent_id reliably distinguishes main from subagent — verified empirically on Claude Code sonnet-4-6 (agent_id absent in main, populated in subagent). Added Write|Edit|MultiEdit PreToolUse matcher to settings.json. Directory name configurable via CLAUDE_BLOCK_TESTS_DIR_NAMES env var (default: tests). Fail-open on any error path.


### FEATURE: workflow-gate: extend docs-only short-circuit to root human-facing .md files (2026-04-22, pending)
Background: The 2026-04-21 short-circuit design treated root README.md as behavior code (excluded from docs-only bypass). In practice README.md is human-facing documentation (per rules/docs-convention.md: 'External-facing overview', 'Compact'), and standalone README catchup commits (e.g. d4b0f55) required navigating the full 6-step workflow gate — unnecessary overhead for non-code changes. User confirmed that root-level human-facing .md files (README/CHANGELOG/CONTRIBUTING/LICENSE) warrant the same short-circuit treatment as docs/*.md, while CLAUDE.md/SKILL.md remain behavior/prompt code.
Changes: workflow-gate.js: replaced the isDocsOnlyStaged predicate from startsWith('docs/') && endsWith('.md') to a named constant DOCS_ONLY_ALLOWLIST = /^(docs\/.+\.md|(README|CHANGELOG|CONTRIBUTING|LICENSE)\.md)$/i. Subdirectory README.md and CLAUDE.md remain excluded. claude-global/CLAUDE.md, docs/architecture/claude-code.md (x2), and README.md updated to reflect new allowlist semantics. Tests: added L2-c2..c5 to main-workflow-state-machine.sh (root README.md → short-circuit, docs+README mix → short-circuit, root CLAUDE.md → full gate, subdir README.md → full gate).


### FEATURE: update-docs skill: structure README.md into routine (step 2/3/Completion) (2026-04-22, pending)
Background: update-docs/SKILL.md listed README.md as a target in the header (line 11) but step 2 (Read current docs), step 3 (Identify gaps), and the Completion git add command did not reinforce it. In practice the README was read-passed only when Claude remembered to, leading to standalone catchup commits like d4b0f55. The skill also lacked a clear statement of what a README should achieve.
Changes: step 2: added explicit note to always include root README.md in the read pass. step 3: added README.md goal ('make the reader think I want to use this — key features in crisp abstract terms') and update trigger criteria. Completion: git add changed from 'git add docs/' to 'git add docs/ README.md' with a note to omit the path when no README exists.


### FEATURE: docs/history.md classification -- history-classification.md + split-history.py (2026-04-25, pending)

Background: Step 6 of the agents repo split. Classified all 71 history.md entries into @claude (agents repo), @dotfiles (keep in dotfiles repo), or @both, then built a machine-driven split script to produce the two output files.

Changes: Created docs/history-classification.md (71 entries: @claude=58, @dotfiles=13, @both=0). Created bin/split-history.py: reads history.md + classification table, writes history-agents.md (@claude+@both) and history-dotfiles.md (@dotfiles+@both). Normalization: strips trailing date suffix like (2026-04-12, abc1234); normalizes INCIDENT: #N: -> INCIDENT #N: for fuzzy match. 0 unmatched entries on current history.md. Added 11 tests (N11-N15, E4-E6, I1, ER1-ER2) to tests/feature-agents-repo-split.sh.


### FEATURE: agents-split step 2: introduce AGENTS_CONFIG_DIR abstraction (2026-04-25, ca3ceec)

Background: settings.json had 11 hardcoded $DOTFILES_DIR/claude-global/hooks/*.js paths. To enable the agents repo split, these needed to be abstracted behind a $AGENTS_CONFIG_DIR env var so the same settings.json works whether the hooks live in dotfiles or a standalone agents repo.

Changes: Updated settings.json 11 hook paths from $DOTFILES_DIR/claude-global/hooks/* to $AGENTS_CONFIG_DIR/hooks/*. Added $AGENTS_CONFIG_DIR and $AGENTS_DIR (with BEGIN/END temporary compat defaults pointing to claude-global) to .profile_common and install/win/profile.ps1.


### FEATURE: agents-split step 3: abstract hook scanner path via AGENTS_CONFIG_DIR (2026-04-25, e79cb06)

Background: pre-commit and commit-msg hooks used relative paths (../../bin/scan-outbound.sh), which would break when the hook directory moves to the agents repo. scan-outbound.sh used a hardcoded my-private-repo sibling path that would not exist in a standalone agents install.

Changes: Updated pre-commit and commit-msg hooks to use $AGENTS_CONFIG_DIR/../bin/scan-outbound.sh. Updated scan-outbound.sh sibling path to use ${DOTFILES_PRIVATE_DIR:-$DOTFILES_DIR/../my-private-repo} optional fallback — warns and continues with empty allowlist if unset.


### FEATURE: agents-split step 4: abstract session-sync path via AGENTS_DIR (2026-04-25, 08bd2c4)

Background: .profile_common referenced $DOTFILES_DIR/bin/session-sync.sh directly. After the split, session-sync.sh will live in the agents repo bin/, so the path needed to be driven by $AGENTS_DIR.

Changes: Updated .profile_common session-sync calls to use ${AGENTS_DIR}/bin/session-sync.sh.


### FEATURE: agents-split step 5: classify 76 tests for repo split (tests/split-plan.md) (2026-04-25, dd61833)

Background: 76 test files in tests/ cover both dotfiles-specific and claude-framework-specific functionality. A classification table was needed before the split to ensure each test lands in the right repo.

Changes: Created tests/split-plan.md with a per-file classification table (@claude/@dotfiles/@both) covering all 76 test files. Rationale is recorded for ambiguous entries.


### REFACTOR: Fix hook paths + absorb 4 test files from dotfiles (step 13) (2026-04-25, a87fabf)
Background: Step 13 cleanup on agents side: hooks still had temporary migration block referencing AGENTS_CONFIG_DIR which pointed to old dotfiles/claude-global path.
Changes: Remove temporary migration block from hooks/pre-commit and hooks/commit-msg; resolve scanner path from hook's own location instead of env var. Update SETTINGS path to settings.json. Absorb 4 test files from dotfiles (main-sort-history.sh, main-translate-history.sh, main-convert-history-table.sh, test_main-sort-history.py) whose bin/ tools live in agents. Fix feature-doc-append-wrapper-permissions-static.sh variable naming. Add .private-info-allowlist with RFC 2606 example.com exemption.


### FEATURE: Step 16: sibling-static profile-snippet pattern (Option B) (2026-04-25, )
Background: Step 16 of dotfiles→agents repo split: replaced ~/.agents_profile{.ps1} generated-file approach with sibling-static-source pattern. Motivation: install-not-required for dotfiles+agents mode, git history tracking of repair logic, no generated file management cost.
Changes: Added profile-snippet.ps1 and profile-snippet.sh to agents root — set AGENTS_CONFIG_DIR/AGENTS_DIR via PSScriptRoot/BASH_SOURCE (no hardcoded paths), include CLAUDE.md/settings.json symlink repair logic. Removed generated ~/.agents_profile{.ps1} from dotfileslink scripts. Updated install.{ps1,sh} to idempotently append profile-snippet sourcing to user shell rc (agents-only mode, BEGIN/END marker). dotfiles: profile.ps1 compat block removed, $AgentsDir hardcode replaced with sibling detection, $symlinkFiles pruned to dotfiles-only items. E2E tests added (E2E-1 through E2E-6): isolated temp-dir tests for agents-only, dotfiles-only, and dotfiles+agents scenarios.


### CONFIG: Exclude rules/language.md from tracking (private symlink) (2026-04-25, pending)
Background: language.md is a private-repo symlink installed by my-private-repo/install. After agents-split, the symlink target lands at c:/git/agents/rules/language.md. Without a gitignore entry it would appear as untracked in the public agents repo.
Changes: Added rules/language.md to .gitignore.


### BUGFIX: block-dotenv: extend to Edit/Write/MultiEdit tools (2026-04-26, pending)
Background: block-dotenv hook only covered Bash/Read/Grep/Glob tools, leaving Edit and Write tools able to overwrite dotenv files. Also, git commit message stripping regex did not handle 'git -C <path> commit' form, causing hook to self-block when commit messages mentioned dotenv.
Changes: Extended block-dotenv.js to handle Edit, Write, MultiEdit tools via isDotenvPath() on file_path. Fixed commit stripping regex to match 'git -C <path> commit'. Added Edit/Write deny rules for dotenv files in settings.json. Updated hook matcher. Fixed stale HOOK path in test file. Added test coverage.


### FEATURE: GitHub Copilot support: shared instructions, hooks, and prompt files (2026-04-26, (pending))

Background: agents repo was Claude Code-only. Goal was to extend CLAUDE.md, rules/, hooks, and skills to GitHub Copilot (VS Code) without duplicating the SSOT. Research found that Copilot natively reads CLAUDE.md and ~/.claude/rules/ via chat.useClaudeMdFile, shares the same hooks JSON protocol as Claude Code and reads ~/.claude/settings.json directly, and supports .prompt.md slash commands. Subagent delegation (Agent tool) has no Copilot equivalent.

Changes: Added copilot/prompts/ with 8 core skills ported to .prompt.md (commit-push, update-docs, review-code-security, review-plan-security, survey-code, write-tests, make-plan, deep-research). make-plan rewritten as self-review single-pass (draft -> critique -> revise). Added install/win/vscode-settings.ps1 and install/linux/vscode-settings.sh to merge 8 required VS Code settings keys non-destructively with .bak safety. install.ps1 and install.sh call the new subscripts. settings.json hook matchers extended with Copilot tool names (runInTerminal, runCommands, editFiles) via OR patterns. README updated; docs/architecture/copilot.md added.


### CONFIG: Deny agent writes to .private-info-allowlist (2026-04-26, )

Background: Prevent accidental allowlist expansion: .private-info-allowlist must only be edited by humans with clear intent, not by the AI agent.

Changes: Add deny rules for Edit and Write on .private-info-allowlist to settings.json permissions.


### BUGFIX: workflow-gate: junction-aware docs evidence detection (2026-04-26, pending)

Background: workflow-gate hasStagedDocChanges() assumed docs/ lives in the same git repository as the source code. In repos where docs/ is a junction pointing to an external git repo, docs/ is gitignored in the source repo, so git diff --cached never shows staged doc changes there — making the docs gate permanently impassable.

Changes: Added resolveExternalDocsRepo() to workflow-gate.js: runs git rev-parse --show-toplevel from within docs/, compares to source repo root, returns the external repo root if they differ (null otherwise). Refactored hasStagedDocChanges() to check the source repo first, then fall back to the external repo if a junction is detected. Added missing path require(). Updated update-docs.prompt.md Completion section to document staging in the junction target repo. Added L2-k/L2-l tests to main-workflow-state-machine.sh covering junction-present+staged->approve and junction-present+unstaged->block.


### CONFIG: Allow WebFetch for code.visualstudio.com; raise effortLevel to high (2026-04-26, pending)

Background: VS Code Copilot documentation (code.visualstudio.com/docs/copilot/) is frequently referenced. effortLevel raised from medium to high.

Changes: Added WebFetch(domain:code.visualstudio.com) to permissions.allow. Set effortLevel to "high".


### CONFIG: Add dotfiles/my-private-repo/agents to additionalDirectories (2026-04-26, pending)

Background: Cross-repo visibility: when working from any of dotfiles/my-private-repo/agents, the other two should be visible as additional directories. Confirmed via testing that additionalDirectories relative paths are CWD-relative.

Changes: Added ../dotfiles, ../my-private-repo, ../agents to additionalDirectories in settings.json alongside existing ../my-specs-repo.


### CONFIG: Allow WebFetch for github.com and langfuse.com (2026-04-26, pending)

Background: Added github.com and langfuse.com to WebFetch allowlist for langfuse project research.

Changes: Added WebFetch(domain:github.com) and WebFetch(domain:langfuse.com) to permissions.allow in settings.json


### FEATURE: Add Claude Code and Codex install to agents installer (2026-04-26, (pending))

Background: installer only checked for claude; did not install it or Codex. Goal was to consolidate both AI CLI tools into agents/install.ps1 and install.sh, loosely coupled from dotfiles.

Changes: Added install/win/claude-code.ps1 (@anthropic-ai/claude-code via npm) and install/win/codex.ps1 (@openai/codex via npm). Added install/linux/claude-code.sh (native installer) and install/linux/codex.sh (npm/nvm). install.ps1: checks fnm — if missing, installs via winget then aborts with restart prompt; if present, runs both scripts then session-sync. install.sh: same pattern with nvm.


### FEATURE: 3-stage planning pipeline: clarify-intent / design-approach / make-detail-plan (2026-04-26, pending)
Background: The make-plan planner/reviewer loop worked well as an implementation-planning tool but had three structural gaps: (1) no background explanation surfaced to the user — the orchestrator suppressed all transcripts until APPROVED; (2) the planner/reviewer debate focused on file-level details before any high-level alignment, because the reviewer checklist had no direction-validity axis; (3) no requirement-clarification stage existed. External frameworks (grill-me by Matt Pocock, GitHub Spec Kit /specify->plan->tasks, Kiro IDE requirements/design/tasks, Aider architect/editor split) all separate the three altitudes: requirements, high-level approach, implementation detail.
Changes: Split the Plan step into three sequential skills inside the existing workflow plan atom (workflow-state.js VALID_STEPS unchanged). (1) /clarify-intent: main Claude interviews the user via AskUserQuestion in decision-tree style with a recommended answer per question (max 5 rounds); writes session-id-intent.md to ~/.claude/plans/. Mandatory; hard-fails in non-interactive contexts. (2) /design-approach: approach-designer subagent proposes 2-3 mutually-exclusive high-level approaches (file paths and steps forbidden), approach-reviewer checks direction only (APPROVED / MISSING_ALTERNATIVE verdicts, max 2 rounds); writes session-id-approach.md. (3) /make-detail-plan (renamed from make-plan): existing planner/reviewer loop, now reads intent.md and approach.md as inputs; planner Background section replaces Goal. New agents: approach-designer (opus, 2-3 approaches required, file paths prohibited, SINGLE_APPROACH_JUSTIFIED escape, NEEDS_RESEARCH escape), approach-reviewer (opus, rigid 2-verdict format). rules/plan-skip.md centralizes skip conditions. CLAUDE.md Workflow step 2 expanded to 2a/2b/2c. workflow-gate.js SKILL_MAP.plan updated to show 3-skill chain.



### CONFIG: hooksPath の書き込み先を ~/.gitconfig に変更 (2026-04-26, d8b7ee7)
Background: agents インストーラーが core.hooksPath を config.local に書いていたが、config.local が my-private-repo の config.local.linux への symlink になっているため tracked なファイルに絶対パスが書き込まれてしまった。--global に切り替えたが XDG 優先により ~/.config/git/config（dotfiles 管理・tracked）に書かれる問題も発生。どちらの tracked ファイルも汚さないよう明示的に ~/.gitconfig を指定することにした。
Changes: install/linux/dotfileslink.sh と install/win/dotfileslink.ps1 で git config --file $HOME/.gitconfig core.hooksPath に変更。~/.gitconfig はどのリポジトリにも追跡されない per-user のローカル設定ファイルであり、hooksPath の置き場として適切。


### REFACTOR: Rebrand to Nirecode (2026-04-26, (pending))

Background: The README h1 was the generic name '# agents', making the framework hard to identify. To establish an independent brand tied to the GitHub handle 'nirecom', /deep-research verified naming conventions across 10 prominent Claude Code framework repositories from primary sources.

Changes: Changed README h1 to 'Nirecode: The self-driving agent coding framework — secure by default, reliable by design'. Added a <sub> line with pronunciation hint and supported tools, plus two feature bullets (self-driving state machine + OWASP-scoped test/security). Repository name 'agents' is kept unchanged for URL compatibility.


### FEATURE: Add cross-provider adversarial review via Codex CLI at workflow step 5 (2026-04-26, pending)

Background: The step 5 security review previously relied solely on Claude (claude-sonnet subagent via /review-code-security), leaving model-specific blind spots unchecked. Adding a second-provider review with OpenAI Codex CLI gives adversarial cross-model coverage — no single model's biases dominate.

Changes: Added bin/review-code-codex (bash script): detects codex CLI, runs adversarial diff review, always emits ## Codex Review: PERFORMED|SKIPPED|FAILED status line via Bash tool (user-visible without relying on Claude's summary), appends JSONL audit log to ~/.claude/projects/codex-review/. Added skills/review-code-codex/SKILL.md as thin wrapper. Updated CLAUDE.md step 5 to include review-code-codex as a third parallel call alongside test suite and /review-code-security. Added symlink installation in dotfileslink.sh (Linux) and review-code-codex.cmd launcher in dotfileslink.ps1 (Windows). Added tests/feature-review-code-codex.sh with 16 test cases covering SKIPPED/PERFORMED/FAILED status labels, visibility invariant, JSONL logging, timeout handling, shell injection safety, and idempotency.


### CONFIG: install.sh: add ANSI color output (2026-04-27, pending)

Background: On macOS/Linux, install.sh used plain echo with no color formatting, while the Windows install.ps1 already used Write-Host -ForegroundColor. The visual gap made the macOS installer harder to follow at a glance.

Changes: Added TTY-detected ANSI color variables (C_CYAN, C_GREEN, C_YELLOW, C_BOLD, C_RESET) to install.sh. === headers print in cyan, --- section markers in bold, === Done === in green, error/warning messages in yellow. No escape codes emitted when stdout is not a terminal (pipe/log safe).


### BUGFIX: profile-snippet.sh: BASH_SOURCE[0] undefined in zsh causes AGENTS_CONFIG_DIR=HOME (2026-04-27, 1a0c112)

Background: In zsh, BASH_SOURCE[0] is undefined; dirname received an empty string, resolving to CWD (typically $HOME) via cd ., so AGENTS_CONFIG_DIR was set to $HOME. All hooks resolved to $HOME/hooks/*.js, node could not find the files, and every hook silently fail-opened (approved everything).

Changes: Added ZSH_VERSION branch to profile-snippet.sh: use ${(%):-%x} in zsh to obtain the script own path instead of BASH_SOURCE[0].


### BUGFIX: install.sh incompatible with macOS zsh and symlinked rc files (2026-04-27, 9be47fd)

Background: install.sh hardcoded ~/.bashrc as the rc file target, so profile-snippet sourcing was never written for zsh users on macOS. Additionally, sed -i '' failed on symlinked rc files (macOS sed refuses in-place edit on symlinks). A separate bug caused an unbound variable error by referencing _rc_file after unset. vscode-settings.sh used GNU-only realpath -m which does not exist on macOS.

Changes: Detect default shell via $SHELL to write profile sourcing to .zshrc / .bashrc / .profile. Replace sed -i '' with perl -i to support symlinked rc files. Save _rc_file before unset to avoid unbound variable in the final message. Drop realpath -m in vscode-settings.sh; fall back to plain realpath with error suppression.


### BUGFIX: install.sh/ps1: restart message shown on every run; printf errors on non-tty (2026-04-27, staged)

Background: The restart-your-shell message was printed unconditionally at the end of install.sh and install.ps1, even when profile sourcing was already present and no restart was needed. Additionally, printf format strings starting with --- triggered an invalid option error in bash when color variables were empty (non-tty stdout, e.g. when piped).

Changes: Added _need_restart/_needRestart flag; only print the restart message when sourcing is newly added. Fixed all printf format strings that start with --- to use printf -- to prevent bash from interpreting --- as an option flag.

### BUGFIX: Fix HIGH findings in review-code-codex from Codex self-review (2026-04-27, pending)

Background: Running review-code-codex on its own diff revealed three HIGH-severity issues: (1) --base without argument caused unbound-variable crash before emitting status line, violating output contract; (2) git diff failures were silently treated as empty diff (SKIPPED) instead of FAILED, creating false coverage; (3) prompt was passed as command-line argument instead of stdin, risking OS arg length limits on large diffs. Also: PATH-filtering approach in tests broke once codex was actually installed (fnm path not filtered).

Changes: bin/review-code-codex: check $# before accessing $2 for --base; capture git diff stderr and emit FAILED on non-zero exit; pass prompt via stdin ('-') instead of command-line arg. tests/feature-review-code-codex.sh: add cases for --base-missing-arg and git-diff-fail; replace PATH-filtering with MINIMAL_PATH=/usr/local/bin:/usr/bin:/bin to reliably hide codex without removing bash/git.


### CONFIG: installer: gray color for already-done messages on Linux/WSL (2026-04-28, pending)

Background: On Windows, already-handled items use Write-Host -ForegroundColor DarkGray. On Linux/WSL, these were plain white echo — making re-runs harder to scan.

Changes: install.sh: added C_GRAY to color block, exported all color vars for child script inheritance. dotfileslink.sh: Symlinks created/core.hooksPath/Generated in green, backups in yellow. claude-code.sh and codex.sh: already installed in gray, installed in green. session-sync-init.sh: already exists in gray, initialized in green, Remote set to unchanged. Profile sourcing messages in green. check-cross-platform.js error message updated to specify --short hash.



### FEATURE: Worktree safety: rules, skills, and deny hardening (2026-04-28, pending)
Background: INCIDENT #2 (worktree deletion silently destroyed a gitignored dotenv file, making Langfuse volume passwords unrecoverable) revealed no skill-based procedure existed for worktree entry/exit. This adds structured guardrails: a rule for when to use worktrees, two skills encapsulating the entry/exit procedure, URL-safe secret generation guidance, and a risky-operations decision path requiring recovery options before destructive proposals.
Changes: Added rules/worktree.md (skill-only entry/exit policy + fit criteria; skill notice moved to top). Added skills/worktree-start (worktree setup + gitignored file classification and copy). Added skills/worktree-end (inventory, backup manifest, Docker bind-mount detection, dry-run summary, and safe removal). Added skills/create-key (URL-safe password generation: hex / base64url / percent-encode; opaque secret guidance; handling and storage rules). Added rules/ops.md (Risky Operations Decision Path: enumerate recovery options before any destructive command). Added two deny entries to settings.json blocking Remove-Item -Recurse -Force (both arg orders).


### FEATURE: Codex-first plan review: integrate Codex as primary plan reviewer (2026-04-28, pending)
Background: review-code-codex at Step 5 showed good results. Manually feeding plan-mode output to Codex surfaced different perspectives from Claude reviewer. Goal: integrate Codex as the primary reviewer in make-detail-plan and design-approach loops to improve plan quality and load-balance Claude token costs.
Changes: Added bin/lib/codex-core.sh: shared library (codex_core_init, codex_core_adversarial_preamble, codex_core_check_cli, codex_core_run, codex_core_log, codex_core_emit_failed). Added bin/review-plan-codex: CLI with --input <file> --format {detail-plan|approach}; emits Codex Plan Review: PERFORMED/SKIPPED/FAILED; always exits 0. Refactored bin/review-code-codex to source codex-core.sh (public interface unchanged); added adversarial preamble to prompt. Updated skills/make-detail-plan and skills/design-approach to codex-first review loop each round; fall back to Claude reviewer subagent on SKIPPED/FAILED/malformed with explicit user-visible message. Added fallback-role notes to agents/reviewer.md and agents/approach-reviewer.md. Added review-plan-codex launchers to install/linux and install/win. Added tests/feature-review-plan-codex.sh (22 cases); added preamble assertion to feature-review-code-codex.sh. Fixed symlink source path bug in both bin scripts (dirname realpath).


### BUGFIX: workflow-gate: auto-detect repo via additionalDirectories (2026-04-28, pending)
Background: When git commit ran without -C from a dotfiles-primary session, resolveRepoDir used CLAUDE_PROJECT_DIR (dotfiles), so evidence checks looked for staged tests/docs in the wrong repo.
Changes: resolveRepoDir now checks CLAUDE_PROJECT_DIR for staged changes first; if none, scans additionalDirectories from settings.json via __dirname. Falls back when nothing is staged. Fixed stale path in fix-workflow-gate-unix-path.sh; added H1-H3 detection tests.


### REFACTOR: Rename design-approach to make-outline-plan (2026-04-28, pending)
Background: The design-approach skill name caused confusion with UI design work and lacked symmetry with make-detail-plan. Renaming to make-outline-plan (outline vs detail) makes the pairing clear.
Changes: Renamed skill design-approach to make-outline-plan (directory + SKILL.md), subagents approach-designer to outline-planner and approach-reviewer to outline-reviewer, completion marker WORKFLOW_DESIGN_APPROACH_COMPLETE to WORKFLOW_OUTLINE_PLAN_COMPLETE, and test feature-design-approach.sh to feature-make-outline-plan.sh. The --format approach CLI flag and <session-id>-approach.md output filename were left unchanged (internal artifact labels). Updated 17 files across agents and my-private-repo repos.


### FEATURE: Add next-step transition hints to SessionStart and workflow-mark (2026-04-28, pending)
Background: SessionStart and workflow-mark only emitted session_id in additionalContext, so when users gave instructions without naming a skill (e.g. 'start langchain phase 5'), Claude would skip clarify-intent and jump straight to implementation. The only enforcement point was the commit-time workflow-gate.js with no guidance during the session.
Changes: hooks/lib/workflow-state.js: added NEXT_STEP_HINT table (step to next-skill instruction string) and nextStepHint(step) function, both exported. hooks/session-start.js: added buildWorkflowStatus() that outputs all 9 step statuses and a NEXT ACTION line in additionalContext on every SessionStart. hooks/workflow-mark.js: after each successful markStep() call in all sentinel handlers, appends nextStepHint() result to additionalContext as a transition hint. 8 SKILL.md files (clarify-intent, make-outline-plan, make-detail-plan, survey-code, deep-research, write-tests, review-code-security, update-docs): Completion sections converted to numbered lists with explicit next-skill Skill tool invocation step after the sentinel echo. tests/feature-workflow-transition-hint.sh: 19 test cases (SS-1 to SS-4, WM-5 to WM-12).


### FEATURE: session-sync: sync plans/ across machines (2026-04-29, (pending))
Background: Plan files written to ~/.claude/plans/ (e.g. *-intent.md generated by clarify-intent, make-outline-plan, make-detail-plan) were not included in session-sync. When continuing work on another machine, previously created plan files were unavailable.
Changes: Added plans sync to push/pull/reset in bin/session-sync.sh and bin/session-sync.ps1. Push: copies $CLAUDE_DIR/plans/ into $PROJECTS_DIR/plans/ before git add. Pull/reset: copies files from $PROJECTS_DIR/plans/ into $CLAUDE_DIR/plans/ (merge — local-only files are preserved). Skips silently when plans/ is absent on either side. Added 10 assertions (8 test cases) to tests/main-session-sync.sh and 4 Pester tests to tests/main-session-sync.Tests.ps1. All new plans tests pass.


### REFACTOR: Subagent model/effort optimization + planner rename + outline.md unification (2026-04-29, pending)
Background: Token consumption was reaching 80-100% of Opus session budget in a single workflow run. Community research (wshobson/agents, VoltAgent, davepoon) confirmed the industry pattern: Opus for critical review only, Sonnet for standard work, effort:high reserved for heavy decision-making roles (doctor/triage equivalents). This refactor aligns agent configuration with that practice.
Changes: model: sonnet — clarify-intent, make-outline-plan (orch), make-detail-plan (orch), detail-planner (ex planner), write-tests. model: opus / effort removed — outline-planner, outline-reviewer. model: opus / effort:high kept — detail-reviewer (ex reviewer). model: opus / effort:high added — review-code-security (security audit is the heaviest decision). effort:low removed — survey-code, update-docs. Progressive disclosure added to outline-planner and detail-planner: start with docs/architecture.md + docs/todo.md, Grep before Read, max 8 source files, do not re-read rules/ (already in system prompt). Renamed planner.md -> detail-planner.md and reviewer.md -> detail-reviewer.md for explicit role distinction. Renamed session artifact approach.md -> outline.md throughout. Split clarify-intent output into intent.md (Background/Scope/Constraints only, passed downstream) and intent-log.md (Q&A transcript, not passed downstream) to reduce context copied into each subagent prompt.


### FEATURE: document plan mode incompatibility (2026-04-29, pending)
Background: Plan mode restricts Skill tool invocations, causing the mandatory workflow to be silently skipped. A SessionStart hook approach was investigated but hook additionalContext is not surfaced to Claude in plan mode, making it ineffective.
Changes: Added plan mode incompatibility notes to README.md (before Docs-only short-circuit bullet) and CLAUDE.md (new section before Docs-only Short-circuit).


### BUGFIX: Fix cleanup STEP_HINT sentinel format (2026-04-29, pending)
Background: STEP_HINT for cleanup used ':main' suffix in the sentinel, not matched by workflow-mark.js regex (WORKFLOW_MARK_STEP_cleanup_skipped: main).
Changes: Corrected to echo '<<WORKFLOW_MARK_STEP_cleanup_skipped>>' without suffix.


### FEATURE: Add show-diff.js: display-only PreToolUse hook for code diffs (2026-04-29, pending)
Background: require-diff-approval.js was a blocking PreToolUse hook requiring /tmp/diff-approved-<HASH> token files before each Edit/Write. It was never registered in settings.json so diff display never worked, causing frequent incidents where code changes were made without showing diffs in chat.
Changes: Replaced require-diff-approval.js with show-diff.js: a display-only PreToolUse hook that outputs systemMessage with diff preview (no decision field, no blocking). Registered in ~/.claude/settings.json PreToolUse with timeout 10.


### BUGFIX: fix: suppress create/delete mode output in session-sync on macOS (BSD grep compat) (2026-04-29)
Background: macOS (BSD grep) does not support \s in ERE mode (-E flag). The pull action filter '^\s*(create|delete) mode ' used grep -Ev which silently failed to match on macOS, letting all create/delete mode lines through. Additionally, git commit -q on macOS can still emit create/delete mode lines to stdout when new files are staged.
Changes: bin/session-sync.sh pull: replaced \s with [[:space:]] (POSIX character class, works on both BSD and GNU grep). bin/session-sync.sh push: added >/dev/null to initial commit to suppress stdout create/delete mode output on macOS git.


### BUGFIX: session-sync push failure on UD conflict + hooksPath always applied (2026-04-29, pending)
Background: session-sync push failed silently after 3 retries when rebase encountered a delete/modify conflict (file deleted by remote, still present locally). The case block only handled *.jsonl, leaving *.json workflow files unresolved → rebase --abort → push failure. Separately, session-sync-init only set core.hooksPath=/dev/null at first git init; if agents was reinstalled (writing ~/.gitconfig) without re-running session-sync-init, the projects repo inherited the agents hooks and blocked rebase --continue commits.
Changes: bin/session-sync.sh: added *) catch-all to conflict case (git rm to accept remote deletion); rebase --continue now falls back to rebase --abort on failure. install/linux/session-sync-init.sh and install/win/session-sync-init.ps1: moved core.hooksPath setting outside the init-only block so it is always applied on every run.


### CONFIG: settings: fix Write allow glob for intent-log.md (2026-04-29, pending)
Background: Added a Write allow rule to settings.json to suppress the permission dialog when clarify-intent writes intent-log.md. The initial pattern ~/.claude/plans/*-intent-log.md did not match because tilde is not expanded to the full absolute path at match time.
Changes: Changed allow rule to Write(**/.claude/plans/*-intent-log.md) so it matches the full absolute path.


### CONFIG: Move personal WebFetch domains to settings.local.json (2026-04-29, pending)
Background: Some WebFetch domains in the settings.json allow list were personal-workflow-specific or framework-specific and not appropriate to keep in a public repo.
Changes: Removed WebFetch(domain:ecc.tools), WebFetch(domain:langfuse.com), WebFetch(domain:docs.langchain.com), and WebFetch(domain:reference.langchain.com) from settings.json. These four entries are moved to settings.local.json (gitignored), whose source file lives in my-private-repo and is deployed via dotfileslink symlink.


### CONFIG: settings: cover both intent-log.md filename formats in allow pattern (2026-04-29, pending)
Background: Two filename formats exist for intent-log.md: UUID-based sessions produce <uuid>-intent-log.md, while the timestamp fallback produces intent-YYYYMMDD-HHMMSS-log.md. The previous fix (intent-*-log.md only) missed the UUID format.
Changes: Added both Write(**/.claude/plans/*-intent-log.md) and Write(**/.claude/plans/intent-*-log.md) to cover both filename formats.


### FEATURE: installer platform guards for install.sh/install.ps1 (2026-05-01, pending)
Background: install.sh had no guard against running in Git Bash/MSYS2/Cygwin on Windows. install.ps1 had no guard against running on Linux/macOS via PowerShell Core 7+.
Changes: install.sh: added uname -s check blocking MINGW*/MSYS*/CYGWIN* with printf %s format injection safety, placed before AGENTS_ROOT setup. install.ps1: added $IsWindows -eq $false guard before Set-StrictMode. Profile sourcing already-present message changed Green to DarkGray in both scripts.


### BUGFIX: installer idempotency: gray display for already-done operations (2026-05-01, pending)
Background: Second run of install.ps1 triggered unnecessary secret injection (the secrets CLI ran even when the injected output file already existed in my-private-repo), showed incorrect symlink backup warnings (path comparison used raw Target string without normalization), always set core.hooksPath green, always regenerated launcher files green, always showed Session sync initialized green, and always wrote VS Code settings even when unchanged.
Changes: dotfileslink.ps1: added Write-Launcher helper for idempotent launcher writes (DarkGray if unchanged); fixed symlink path comparison with GetFullPath() normalization; added current-value check for core.hooksPath (DarkGray if unchanged). session-sync-init.ps1: added _changed tracking, remote URL check before set-url, DarkGray already-up-to-date message when no-op. vscode-settings.ps1: compare each patch key before writing; skip write and backup when all keys already match. session-sync-init.sh: same idempotency fixes as PS version.


### BUGFIX: doc-append: initialize tail_text to suppress Pylance possibly-unbound warning (2026-05-01, pending)
Background: tail_text was assigned inside a for loop, so Pylance flagged it as possibly unbound at usage on line 152.
Changes: Added tail_text = "" initializer before the loop.


### FEATURE: Add repo-visibility tool for gh-based public/private detection (2026-05-01, pending)
Background: Needed a reliable way to check repo visibility (public/private) from Claude Code without per-repo CLAUDE.md maintenance. Direct gh command failed due to MSYS2 path mangling and lack of -C flag support in gh.
Changes: Added bin/repo-visibility.py: accepts Windows (c:/git/...) or WSL (/mnt/<drive>/...) path formats, normalizes per platform, runs gh repo view to output 'public' or 'private'. Added launchers in dotfileslink.sh (bash wrapper) and dotfileslink.ps1 (.cmd wrapper). Language policy updated in my-private-repo rules/language.md to use repo-visibility command.


### FEATURE: Assemble ~/.claude/settings.json from base + private extension (2026-05-02, pending)
Background: hooks (e.g. fornix-agent) require entries in settings.json but settings.json was a public-repo symlink, leaking private paths. settings.local.json does not support hooks. Solution: replace the symlink with an assembled real file. agents/settings.json stays public (base); agents/settings-extension.json (gitignored, symlinked from my-private-repo) holds private additions. assemble-settings.js performs schema-aware merge (hooks/permissions arrays concatenated; env/attribution object-merged; other keys extension-overrides base) and writes ~/.claude/settings.json on every install.
Changes: Add install/assemble-settings.js. dotfileslink.ps1/sh: remove settings.json symlink, add stale-symlink cleanup, call assembler. workflow-gate.js: read additionalDirectories from assembled ~/.claude/settings.json with fallback. .gitignore: add settings-extension.json. my-private-repo/install.{ps1,sh}: move agents installer to last (Step 6) so assembly runs after fornix hooks are injected into the extension file.


### FEATURE: Reset user_verification after git push; delete obsolete privacy.md (2026-05-02, pending)
Background: Once user_verification was marked complete it persisted for the rest of the session, allowing follow-up pushes without re-verification. This created a window where post-push bug fixes could be committed and pushed without user sign-off. rules/privacy.md contained obsolete .context-private/ instructions superseded by rules/private-info.md.
Changes: hooks/workflow-mark.js: hoist toolResponse/exitCode/sessionId; add push-detection block that resets user_verification to pending on successful git push; markStep failure emits FAILED message instead of silently succeeding. rules/privacy.md: deleted (obsolete).


### BUGFIX: profile-snippet.ps1: remove settings.json from symlink watchlist (2026-05-02, pending)
Background: profile-snippet.ps1 listed settings.json alongside CLAUDE.md as a symlink to monitor for breakage. However, settings.json is now an assembled file (written by assemble-settings.js), never a symlink. This caused the broken-symlink check to always fire, triggering dotfileslink.ps1 on every shell start even when nothing was broken.
Changes: Removed settings.json from the _agentSymlinks watchlist in profile-snippet.ps1. Only CLAUDE.md is now monitored.


### CONFIG: codex-core: increase review timeout 60s to 180s (2026-05-02, pending)
Background: Large plans (187+ lines) were timing out at 60s in review-plan-codex and review-code-codex. Diagnosed via ~/.claude/projects/codex-review logs — a 187-line plan hit the wall exactly at 60s.
Changes: bin/lib/codex-core.sh: changed timeout 60 to timeout 180; updated FAILED message to 'timeout (180s)'.


### BUGFIX: workflow-gate: expand env vars in resolveRepoDir (2026-05-03, pending)
Background: PreToolUse hook receives the command string before Bash expansion, so parseGitCArg returned the literal $FORNIX_DIR string. resolveRepoDir treated it as a path, causing hasStagedDocChanges to check the wrong directory and incorrectly block commits with git -C "$FORNIX_DIR" commit. Discovered during fornix-stream salvage bulk import (2026-05-02).
Changes: Expanded env vars ($VAR / ${VAR}) in resolveRepoDir via process.env lookup after parseGitCArg returns. Undefined vars resolve to empty string (falsy), falling through to the staged-changes fallback. Added test cases N5/N6/N7 to fix-workflow-gate-unix-path.sh.


### FEATURE: AWS infrastructure access Phase 1a: scan skills + settings.json guards (2026-05-03, pending)
Background: Phase 1a of AWS account access infrastructure. Goal: auto-collect full account configuration into AWS_STATE_DIR without opening the console. Claude Code gets AWS operation guards via settings.json and ready-to-use scan skill stubs.
Changes: settings.json: added 18 deny patterns (delete/terminate/remove/detach/release/destroy/etc.) and 14 ask patterns (create/put/update/start/stop/etc.) for aws CLI operations. New SKILL.md stubs: aws-scan (orchestrator), aws-scan-resources (EC2/S3/RDS/VPC/IAM), aws-scan-security (IAM posture/public exposure/security services), aws-scan-cost (Cost Explorer top-5), aws-scan-apps (ECS/Lambda/ALB/API GW topology). docs/architecture/claude-code/settings.md: AWS IAM Posture section added. docs/todo.md: IAM follow-up tasks added.


### FEATURE: judge-task-complexity + boost: complexity routing for subagent model selection (2026-05-03, pending)
Background: Sonnet weekly token consumption was outpacing Opus. VSCode + Claude Code cannot switch models mid-thread, so the solution uses the Agent tool model: override at subagent launch time. Two mechanisms: manual per-task Opus delegation (/boost) and automatic complexity-based routing (judge-task-complexity integrated into make-detail-plan and write-tests).
Changes: Added skills/judge-task-complexity/SKILL.md: dedicated complexity evaluator returning VERDICT: opus|sonnet | <signal IDs>. Six signals: S1-multi-file, S2-architecture, S3-security, S4-installer, S5-breaking, S6-long-plan. 1+ signal triggers opus; parse failure defaults to opus. Added skills/boost/SKILL.md: per-task Opus delegation via Agent tool (subagent_type: general-purpose, model: opus, mode: auto); empty-args guard included. Modified skills/make-detail-plan/SKILL.md: inserted step 2 (judge call + Model selected: text output), renumbered downstream steps, fixed missing step 5 gap; Skip Conditions updated to say skip judge-task-complexity explicitly. Modified skills/write-tests/SKILL.md: inserted step 5 (judge call + model display), renumbered old 5->6 and 6->7; subagent launch now passes model: param. Added tests/main-task-complexity-routing.sh: 39 structural grep tests, all passing.


### FEATURE: Submit nirecom/agents to awesome-lists (2026-05-03, )
Background: awesome-lists submission for nirecom/agents (agents repo split project follow-up)
Changes: Submitted PR #363 to rohitg00/awesome-claude-code-toolkit adding nirecom/agents to the All Plugins section. hesreallyhim/awesome-claude-code requires manual issue submission via GitHub UI (CLI submissions are blocked by repo policy — user guided with URL and field values). travisvn/awesome-claude-skills on hold until repo reaches 10 stars (currently 1). VoltAgent/awesome-agent-skills deferred.


### REFACTOR: Make session-sync standalone (no dotfiles dependency) (2026-05-03, pending)
Background: Session-sync auto-fetch and the codes function lived in dotfiles' install/win/profile.ps1 and .profile_common, so machines without dotfiles installed had no automatic sync.
Changes: Added SessionDir auto-pull and codes function to profile-snippet.{ps1,sh}. Moved bin/wait-vscode-window.{ps1,sh} from dotfiles. Moved tests/main-wait-vscode-window.* and tests/main-profile-codes.* and updated path references to agents repo. Updated docs/architecture/claude-code/session-sync.md.


### FEATURE: CHANGELOG.md workflow: doc-append --commits optional + update-docs integration (2026-05-04, 2ac70af)
Background: No user-facing changelog existed for the public agents repo. /update-docs had no mechanism to write one simultaneously with history.md.
Changes: doc-append: --commits made optional (omitted produces date-only header). update-docs SKILL.md: Completion now calls doc-append twice — docs/history.md (internal, with --commits) and CHANGELOG.md (user-facing, without --commits). docs-convention.md: CHANGELOG.md added to Standard Files and Append-Only Tools. tests/feature-doc-append-wrapper.sh: 2 test cases for --commits omitted.


### FEATURE: Gemini CLI + mmdc integration; draw-mermaid skill; installer flags (2026-05-04, pending)
Background: Wanted Mermaid diagram generation (for README workflow chart) and Gemini CLI as a third AI provider. Codex/Gemini were previously installed unconditionally; made optional behind --develop flag to lower the default install bar.
Changes: Added skills/draw-mermaid/SKILL.md: context-efficient subagent-delegated Mermaid diagram generation with embedded dark-mode conventions (classDef palette, br/ line breaks, WCAG 2.1 AA). Added bin/draw-diagram (mmdc SVG renderer) and bin/draw-diagram-gemini (Gemini API image generation). Added install/linux/gemini.sh and install/win/gemini.ps1 to install Gemini CLI and mmdc via npm. Added draw-diagram + draw-diagram-gemini launchers to dotfileslink.{sh,ps1}. install.ps1/-Develop and install.sh/--develop now gate both Codex and Gemini+mmdc install (default: Claude Code only). my-private-repo install.{ps1,sh} pass flags through to agents installer. Added workflow Mermaid flowchart to README.md. Updated README.md install commands to document -Develop option. doc-append --commits made optional for CHANGELOG.md use case.


### SECURITY: Block CC writes to .private-info-allowlist via PreToolUse hook (2026-05-04, 27afc41)
Background: permissions.deny for Edit/Write is bypassed in VSCode ask-before-edits mode, allowing CC to freely append to the scan-outbound allowlist and effectively whitelist private patterns without user approval.
Changes: Added isAllowlistPath check to block-dotenv.js PreToolUse hook to block Edit/Write/MultiEdit on .private-info-allowlist. Removed dead permissions.deny rules for the same file from settings.json. Added write-protection note to docs/scan-outbound.md Allowlist section.


### REFACTOR: Symlink .private-info-allowlist to my-private-repo; keep .example as public template (2026-05-04)
Background: Adding host/project-specific allowlist entries (e.g. CHAT_API_KEY_*) directly to agents/.private-info-allowlist would leak private info names through the public repo. Mirror the existing blocklist pattern.
Changes: Removed agents/.private-info-allowlist from tracking (now gitignored). Added .private-info-allowlist.example as the public template. Added rules/claude-config-source.md note: docker compose --force-recreate is required when only environment values change.


### REFACTOR: Rename private-info-blocklist.example to .private-info-blocklist.example for dotfile naming consistency (2026-05-04)
Background: After introducing .private-info-allowlist.example, the existing private-info-blocklist.example (without leading dot) became inconsistent. Convention research confirms .env.example pattern (leading dot preserved) is the de facto standard.
Changes: Renamed private-info-blocklist.example to .private-info-blocklist.example. Updated README.md, docs/scan-outbound.md, and the allowlist example glob path.


### FEATURE: PreToolUse early gate enforcing clarify_intent before Edit/Write (2026-05-04)
Background: CLAUDE.md instructions and the SessionStart additionalContext NEXT-ACTION hint were both advisory and routinely ignored — Claude would dive straight into investigation/edits without running /clarify-intent. CC ecosystem research confirmed only PreToolUse + exit-code-2 hard blocking achieves reliable enforcement. The TodoWrite-first mandate had also been over-firing for trivial tasks.
Changes: Extended hooks/workflow-gate.js with an earlyGate dispatch that fires before the existing git-commit gate. While clarify_intent is pending, blocks Edit/Write/MultiEdit/editFiles/NotebookEdit unless the Write target resolves under ~/.claude/plans/ (skill output allowlist with path.resolve hardening). Read/Grep/Glob/Bash are intentionally NOT in the matcher so the clarify-intent skill itself can do codebase exploration without deadlocking. settings.json matcher widened: Bash|Edit|Write|MultiEdit|editFiles|NotebookEdit|runInTerminal|runCommands. CLAUDE.md line 5 TodoWrite mandate replaced with clarify-intent-first directive that defers checklist creation to the skill. clarify-intent SKILL.md Completion section now creates the 9-step TodoWrite checklist after emitting CLARIFY_INTENT_COMPLETE. Added tests/feature-clarify-intent-gate.sh: 13 tests covering pending-block, complete-approve, fail-open, plans-path allowlist, regression for commit gate, idempotency.


### FEATURE: scan-outbound: add warn: prefix for soft-block patterns (2026-05-04)
Background: Hard-only blocklist forced a binary trade-off between noisy false positives and weakened patterns. Suspicious-but-uncertain combinations (e.g. two common keywords colocated) had no middle option.
Changes: Add warn: prefix in .private-info-blocklist for soft-block patterns. Scanner exit codes extended to 0/1/2/3 (warn-only=2, usage error moved 2 to 3 — non-backward-compatible). PreToolUse JS hook returns block + reason 'Ask the user' on warn (no TTY use). Bash git hooks prompt y/N via /dev/tty when interactive, auto-block when not. Empty warn: lines are skipped with stderr warning. Includes 4 new tests and security review (no CRITICAL/HIGH).


### FEATURE: Multi-session safety — AGENT_AUTO_BRANCH enforcement (replaces heartbeat sibling detection) (2026-05-05)
Background: Multiple concurrent CC sessions on the same repo can race when committing to the default branch. The session began with a heartbeat-based detection design (per-repo .git/agent-state/<sid>.json with mtime TTL, scanned by pre-commit/pre-push). Implementation completed and tested before the user identified that heartbeats were unsuitable for multi-root VSCode workspaces — CC's cwd is the workspace's first folder, so heartbeats land in repo X while real commits go to repo Y, leaving the guard blind. The 'twisted relationship' was deemed accident-prone. Three alternatives explored: touch-based heartbeats (PostToolUse refreshes per-repo with a manifest), centralized heartbeats keyed by repo-hash, and reusing CC's transcript files as natural heartbeats. All observe a proxy rather than enforce the actual invariant. The user proposed a fourth path: eliminate the race by construction — agents never write to the default branch by default. Codex independently endorsed this strongly: comparable to GitHub's protected branches, but enforced at edit-time rather than push-time. Hard pivot. Detailed design exploration in my-specs-repo research-results/multi-session-design-pivot.md.
Changes: Removed heartbeat infrastructure: hooks/lib/heartbeat.js, hooks/lib/check-sibling-heartbeat.sh, hooks/session-end.js, hooks/pre-push, hooks/user-prompt-submit.js, tests/feature-multi-session-coordination.sh; reverted heartbeat blocks in session-start.js, workflow-mark.js, pre-commit. Cleaned legacy agent-state heartbeat directories. Added AUTO_BRANCH enforcement: hooks/auto-branch-guard.js (PreToolUse Edit|Write|MultiEdit blocks default-branch edits), bash defense in pre-commit (commit-time mirror). Default-branch detection chain: origin/HEAD then local main/master then init.defaultBranch then fallback main; AGENT_DEFAULT_BRANCHES env override. Truthy on|1|true|yes|enabled (default ON); falsy off|0|false|no|disabled. Allows unborn HEAD, detached HEAD, non-git paths. Added push-boundary detection: workflow-mark.js records last_pushed_sha into workflow state on push; hooks/post-push-workflow-reset.js (UserPromptSubmit) resets branching_decision to pending when HEAD matches last_pushed_sha. Added helpers: hooks/lib/load-env.js parses AGENTS_CONFIG_DIR per-user environment file into process.env (existing env wins); hooks/lib/path-normalize.js exposes normalizeCwd (Windows POSIX drive-letter conversion) and resolveRepoCwd (5-level priority: dash-C path then CLAUDE_PROJECT_DIR then input.cwd then state.cwd then process.cwd()). Updated CLAUDE.md Step 3 (Branch/Worktree decision to creation) with explicit AGENT_AUTO_BRANCH on/off case-split; rules/branch.md and rules/worktree.md updated. Tests: tests/feature-auto-branch.sh 37 cases. Bug-fix sequence (R1, F1-F4) found by external verifier session: R1 environment file not loaded; F1 UserPromptSubmit envelope contract mismatch (needs hookSpecificOutput); F2 workflow-mark missing cwd option (read wrong repo HEAD); F3 Windows POSIX-style path rejected by execSync; F4 PostToolUse stdin lacks cwd, fell back to wrong process.cwd(). Each fix added a regression test reproducing the production failure.


### REFACTOR: clarify-intent: drop summary-confirmation AskUserQuestion (2026-05-05)
Background: User feedback: the skill asked twice — once at intent.md Write (reviewable diff), then again via AskUserQuestion on the summary. The second prompt added friction without new information; users can interject freely on the summary text.
Changes: Removed step 5 (AskUserQuestion summary gate) from skills/clarify-intent/SKILL.md. Step 4 now notes that no confirmation gate is needed and that later planning stages provide revision opportunities.

### BUGFIX: workflow-gate: fix readStdin shared-buffer corruption causing large-payload fail-safe blocks (2026-05-06)
Background: workflow-gate.js readStdin used Buffer.alloc(4096) with buf.slice() in a loop. buf.slice() returns a view into the shared buffer, so subsequent loop iterations overwrote previous chunks, corrupting the accumulated input. Large Write/Edit/Bash heredoc payloads exceeded 4096 bytes and triggered JSON.parse failure, causing fail-safe block on every large content operation. User applied a temporary workaround in main; fix now landed in worktree.
Changes: hooks/workflow-gate.js readStdin: replaced chunk-accumulation loop with fs.readFileSync(0, 'utf8') — reads all stdin atomically, eliminating buffer aliasing and chunk-boundary issues.


### FEATURE: Parallel sessions worktree workflow: enforce-worktree, path layout, mandatory PR (2026-05-06)
Background: Three failure modes in multi-session parallel work: (1) main-branch leak — agents writing directly to the main checkout on any branch; (2) inconsistent worktree path schemes across sessions; (3) no mandatory PR + cleanup discipline after merge. auto-branch-guard only blocked protected-branch commits, not main-checkout writes from non-protected branches.
Changes: Renamed auto-branch-guard.js -> hooks/enforce-worktree.js; extended to detect main checkout via --git-common-dir==--git-dir (blocks all writes from main checkout regardless of branch). Added hooks/lib/bash-write-patterns.js: 46-pattern write classifier (POSIX redirects, here-docs, PowerShell cmdlets/aliases/encoded, file ops including npm/pip/cargo/go install, git/gh mutating subcommands). Added hooks/lib/worktree-config.js: WORKTREE_BASE_DIR env, validateTaskName/validateRepoName (injection-safe via [a-zA-Z0-9_-]+ allowlist), buildWorktreePath. Added install/linux/global-gitignore.sh and install/win/global-gitignore.ps1: idempotent XDG gitignore block manager for WORKTREE_NOTES.md; integrated into install.sh and install.ps1. Renamed env vars: AGENT_AUTO_BRANCH -> ENFORCE_WORKTREE, AGENT_DEFAULT_BRANCHES -> DEFAULT_BRANCHES; added AUTO_MERGE_PR, WORKTREE_BASE_DIR; migration fallback with deprecation warnings wrapped in BEGIN/END temporary markers. Rewrote skills worktree-start/worktree-end/commit-push: standardized two-level path layout <base>/<task>/<repo>, mandatory PR flow with AskUserQuestion merge prompt, gitignored state inventory before worktree removal. Added docs/parallel-sessions.md. Updated settings.json hook matcher Bash|Edit|Write|MultiEdit. Updated rules/branch.md (ENFORCE_WORKTREE note), rules/worktree.md (removed When-to-Use table — now controlled by env var). 5 TDD test files, 211 cases.


### BUGFIX: global-gitignore.ps1: .Count error on empty Where-Object result (2026-05-06, TBD)
Background: install.ps1 errored at line 50 of global-gitignore.ps1 with 'The property Count cannot be found on this object' on first run. PowerShell unwraps single/null pipeline results, so Where-Object returning 0 items produced $null; .Count then failed. Test coverage was bash-only (tests/feature-parallel-sessions-worktree-installer-ignore.sh tests global-gitignore.sh); the PS sibling had zero coverage.
Changes: Wrap Where-Object pipelines in @() at lines 50-51 so the result is always an array. PS-side test coverage tracked in todo.md.


### FEATURE: commit-gate redesign: merge boundary enforcement + staged diff fallback (2026-05-07, pending)
Background: Three design issues in the commit gate: (1) user_verification gate fired on feature-branch commits inside linked worktrees. (2) workflow-mark.js reset user_verification after any git push including feature-branch pushes. (3) review-code-codex could not use staged diff when no commits existed past base.
Changes: hooks/lib/merge-detect.js new module classifies gh-pr-merge / git-push-protected commands. hooks/lib/parse-git-args.js added parseGitGlobalOptions. hooks/workflow-gate.js added merge gate and worktree commit skip. hooks/workflow-mark.js narrowed push reset to protected-branch push or gh pr merge. bin/review-code-codex added staged diff fallback. skills/commit-push/SKILL.md removed AUTO_MERGE_PR env check. Tests: 24 cases in feature-commit-gate-redesign.sh, 26 in main-merge-detect.sh.


### BUGFIX: enforce-worktree: allow worktree lifecycle commands from main checkout (2026-05-07)
Background: git worktree add/remove/prune and the PowerShell directory-creation cmdlet are meta-operations that by design must run from the main checkout — they write .git/worktrees/ metadata and create the worktree directory. enforce-worktree.js was classifying these as write operations and blocking them, creating a circular bootstrap dependency: the fix requires creating a worktree, but that is exactly what was blocked.
Changes: Added isAllowedWorktreeCommand() and isAllowedNewItemDirectory() helpers in enforce-worktree.js. Both functions reject shell-chained commands (pipe, semicolon, ampersand in unquoted segments) and reject target paths that resolve inside the main repo root. The directory-creation helper is fail-closed when no path argument can be resolved. The -ItemType flag detection avoids a leading word-boundary anchor because the flag starts with a dash (non-word char). Added helpers stripQuotedSegments() and hasShellChaining() for safe chain detection across single and double-quoted segments. New test file tests/refactor-branching-complete-rename-worktree.sh: 19 cases (WL-1..12 for git worktree variants, NI-1..7 for directory creation variants).


### REFACTOR: rename workflow step branching_decision → branching_complete and sentinel WORKFLOW_BRANCHING_DECIDED → WORKFLOW_BRANCHING_COMPLETE (2026-05-07)
Background: The step name "branching_decision" implied a deliberate choice, but with ENFORCE_WORKTREE=on a worktree is mandatory — no decision is made. "branching_complete" aligns with the _COMPLETE naming used by all other sentinels (CLARIFY_INTENT_COMPLETE, WORKFLOW_OUTLINE_PLAN_COMPLETE, etc.) and with the new sentinel name. The CLAUDE.md label had already been updated to "Branch/Worktree creation" in a prior commit.
Changes: Renamed step key across hooks/lib/workflow-state.js (VALID_STEPS, STEP_HINT), hooks/workflow-mark.js (new BRANCHING_COMPLETE_RE_DQ/LOOKSLIKE_RE regexes, handler updated), hooks/workflow-gate.js (SKILL_MAP entry), hooks/post-push-workflow-reset.js (markStep target and message text), hooks/stop-cleanup-reminder.js (state key reference). Updated CLAUDE.md record line and skills/make-detail-plan/SKILL.md Completion section. Backward compat: old sentinel WORKFLOW_BRANCHING_DECIDED still accepted (both DECIDED and COMPLETE regexes are OR-chained in isSentinel() and the match handler); readState() migrates old state JSON with branching_decision key to branching_complete on read. Updated architecture docs (workflow.md, settings.md). Updated all test state JSON fixtures; fixed AGENTS_DIR hardcoding in tests/feature-auto-branch.sh to use script-relative path so worktree tests use worktree hooks.


### BUGFIX: bash-write-patterns: here-doc regex false-positive on workflow sentinel echo (2026-05-07)
Background: The here-doc pattern had no position anchor, so echo "<<WORKFLOW_...>>" was classified as a write command and blocked by enforce-worktree.js from the main checkout. Real heredocs always follow a command with preceding whitespace; sentinel strings appear inside quoted arguments with a quote character immediately before <<, which the old pattern did not distinguish.
Changes: Added position anchor matching the posix-redirect pattern's anchor style. Quotation marks no longer satisfy the anchor, so quoted strings containing << are no longer misclassified. Added tests/fix-heredoc-false-positive.sh: 20 cases (FP-1..5 sentinel echo variants, HD-1..5 heredoc regression, EDGE-1..4 compound commands, IDEM-1..2, SEC-1..3). Existing 101-case suite also verified clean.


### BUGFIX: enforce-worktree: gh command whitelist refactor (Group A/B + session-scope) (2026-05-07, pending)

Background: enforce-worktree.js was falsely blocking coordination commands (gh pr create/edit/close/comment/review, gh issue create/edit/close/comment, gh repo create/edit/rename/archive) in VS Code multi-root workspaces. Root cause: bash-write-patterns.js catch-all gh patterns classified both safe metadata-only commands and destructive ones as write, subjecting all gh commands to worktree enforcement. Additionally, findRepoRootForBash() fell back to AGENTS_CONFIG_DIR (agents main checkout) instead of process.cwd(), causing cross-repo operations to resolve to the wrong repo.

Changes: Split gh WRITE_PATTERNS into Group A (coordination/metadata only, classified "read" — guard never fires) and Group B (content-modifying or destructive, classified "write" with session-scope check). Group A: gh pr create/edit/close/comment/review, gh issue create/edit/close/comment, gh repo create/edit/rename/archive. Group B: gh pr merge, gh issue delete, gh repo delete, gh release create/edit/delete/upload, gh api with POST/PUT/PATCH/DELETE in any flag form (-X, -XVERB, -X=VERB, --method, --method=). Added session-scope whitelist: Group B commands verify target repo is in CWD repo + ENFORCE_WORKTREE_EXTRA_REPOS (comma-separated env var, new in .env.example). Changed findRepoRootForBash() fallback from AGENTS_CONFIG_DIR to process.cwd(). Added 72 integration tests in tests/fix-enforce-worktree-gh-whitelist.sh; updated feature-parallel-sessions-worktree-bash-patterns.sh and feature-parallel-sessions-worktree-guard.sh.


### FEATURE: enforce-worktree: EXTRA_REPOS directory-scan fallback (depth 1) (2026-05-07)
Background: Users with many repos under a common root (e.g. C:\git) had to enumerate every repo path individually in ENFORCE_WORKTREE_EXTRA_REPOS. No mechanism existed to specify a parent directory and have its children discovered automatically.
Changes: getSessionRepoRoots() now falls back to a depth-1 directory scan when an EXTRA_REPOS entry is not itself a git repo root: fs.readdirSync finds immediate subdirs and resolveRepoRoot() tests each one. Mixed individual+parent entries supported; spaces around commas trimmed. gh write commands (Group B) now call done() after the session-scope check passes instead of falling through to the main/worktree check — gh operates on GitHub, not local files, so the worktree-locality restriction is inapplicable. Added tests/fix-extra-repos-dir-scan.sh: 22 cases (N1-N5 dir-scan normal, E1-E4 error/edge, IDEM1 idempotency, SEC1 metachar injection, INT1-INT2 integration).


### BUGFIX: isPathOutsideRepo: normalize POSIX drive-letter paths before path.resolve (2026-05-07)
Background: On Windows + Git Bash, git rev-parse --show-toplevel returns POSIX-style paths like /c/git/agents. When these were passed directly to path.resolve() in Node.js on Windows, the drive letter was misread as a directory component (e.g. /c/git/agents -> C:\c\git\agents), causing isPathOutsideRepo to return the wrong result and falsely blocking git worktree add for targets that are actually outside the repo.
Changes: Applied normalizeCwd() to both targetPath and repoRoot before path.resolve() in isPathOutsideRepo. normalizeCwd converts /c/foo -> C:\foo on Windows and is a no-op elsewhere; || fallback guards against undefined return on non-string input. Also fixed run_hook test helper to cd into MAIN_REPO so New-Item tests pass consistently when run from a linked worktree. Added to_unix_style_path helper and WL-13/WL-14 regression tests covering POSIX drive-letter external and in-repo path cases.


### BUGFIX: bash-write-patterns: /dev/null null-sink misclassified as write (2026-05-08)
Background: posix-redirect pattern (regex /(?:^|[\s;|&])(?:\d*)>>?(?!>|\d)/) classified any redirect token as write regardless of destination. Common read-only suppression patterns like `git status 2>/dev/null` and `ls >/dev/null` were blocked by enforce-worktree.js from the main checkout, blocking the workflow on every error-suppressing command.
Changes: Added negative lookahead `(?!\s*\/dev\/null(?=\s|[;|&]|$))` to posix-redirect regex. The `(?=\s|[;|&]|$)` terminator (not `\b`) ensures `/dev/null/foo` (a real file under a directory called null) remains a write — `\b` matched at l/ boundary and would have wrongly excluded subpaths. Coverage: 4 READ_CASES additions (`2>/dev/null`, `>/dev/null`, `&>/dev/null`, `grep ... 2>/dev/null`), new test_dev_null_compound function with 5 cases (compound + rm, compound + read, append form, /dev/null/foo subpath, `>/dev/null 2>&1` documented FP preserved). Windows NUL intentionally not excluded (POSIX pattern only; pwsh null-sink uses Out-Null).


### FEATURE: ENFORCE_WORKTREE_EXCLUDE bypass + doc-append-plain + EXTRA_REPOS separator migration (2026-05-08)
Background: The ENFORCE_WORKTREE gate blocked all commits from the main checkout, including low-risk append-only updates to files like todo.md that span multiple repos. Agents needing to record a quick backlog item in a shared todo had to create a per-task worktree just for the note. ENFORCE_WORKTREE_EXTRA_REPOS also used comma as its separator, inconsistent with the emerging semicolon convention.
Changes: Added ENFORCE_WORKTREE_EXCLUDE env var accepting semicolon-separated glob patterns (hooks/lib/glob-match.js: parseExcludePatterns, pathMatchesGlob, matchesAnyExcludePattern). When ALL staged files match at least one pattern the pre-commit main-checkout and protected-branch gates are skipped; the private-info scanner still runs. AGENTS_CONFIG_DIR fallback ensures the matcher works in normal hook installs without the env var set; matcher failures emit a diagnostic and fall back to enforce (fail-closed). Added bin/doc-append-plain.py: append-only plaintext write CLI for doc files. Migrated ENFORCE_WORKTREE_EXTRA_REPOS separator from comma to semicolon (enforce-worktree.js); .env.example updated with migration note. Added 3 test files: feature-enforce-worktree-exclude-glob.sh (38 cases), feature-enforce-worktree-exclude.sh (9 integration cases), test_feature-enforce-worktree-exclude-doc-append.py (14 pytest cases). Cross-platform launchers added to install/win/dotfileslink.ps1 and install/linux/dotfileslink.sh.


### BUGFIX: fix worktree-to-main flow: 6 hook bugs + security review hardening (2026-05-08)
Background: ENFORCE_WORKTREE introduction blocked the worktree to main merge workflow (PR create / merge / cleanup) via several PreToolUse hook bugs. Six known issues from todo.md plus security review findings from Codex and Claude reviewers were addressed in one PR.
Changes: bash-write-patterns.js: tightened git-branch-mutate regex to flag-position only (kills branch-name false-positives like agents-env-consolidate); reclassified git branch -d as read; added Group A heredoc/here-string override in classify so gh pr create --body has no false-positive; added git-update-ref to WRITE_PATTERNS. enforce-worktree.js: added isAllowedFastForwardMerge predicate so git pull/merge --ff-only is allowed from main checkout; wrapped Main block in require.main guard and exported the predicate for unit testing; extended hasShellChaining to detect command substitutions (necessary defense after security review). block-dotenv.js: replaced checkBashCommand with path-position parser (stripSubstitutions / tokenizeSegment / splitSegments / segmentBlocksDotenv); recurses into command substitutions before stripping (closes gh pr create --body $\(cat .env\) bypass); detects redirect targets before TEXT_CMDS short-circuit (closes echo > .env / printf >> .env bypass); recurses through bash -lc / sh -ic combined short flags; dropped -l/-a/-r short forms from TEXT_FLAGS to avoid Unix-flag collisions (closes wc -l .env / ls -l .env / cp -r .env bypass). Added 4 test files / 1 new file (fix-enforce-worktree-merge-gate.sh) covering all 6 changes plus security regression cases — 187 PASS / 0 new failures.


### BUGFIX: enforce-worktree Bundle A — EXCLUDE JS-side, session-scope Edit/Bash, git-commit regex (2026-05-09, c9f3c6a)
Background: Three daily-workflow blockers: (1) ENFORCE_WORKTREE_EXCLUDE was only honoured by pre-commit (shell), not by enforce-worktree.js PreToolUse — files matching EXCLUDE were blocked at the hook level even though pre-commit would have allowed them. (2) Edit/Write/MultiEdit and Bash branches lacked session-scope filtering — writes to non-session repos (e.g. ~/.claude/projects/.../memory/*.md) were blocked because findRepoRoot() and findRepoRootForBash() fell back to the session CWD repo. (3) git-commit regex matched any token containing 'commit', so git log -- hooks/pre-commit was classified as a write command.
Changes: bash-write-patterns.js: tightened git-commit regex to require 'commit' at subcommand position. New module bash-write-targets.js: extractRedirectTargets (POSIX > >>, skips FD-to-FD and /dev/null), extractTeeTargets, extractPwshWriteTargets (quote-aware tokenizePwsh, Move/Copy destination semantics), extractStagedFiles. enforce-worktree.js: added getExcludePatterns/isExcluded (gitignore basename semantics), collectBashWriteTargets, areAllBashTargetsOutsideSessionScope, isWriteTargetAllExcluded; hasCommandSequencing guard on compound commands (;/&&/||) to prevent session-scope allow bypass; Edit/Write/MultiEdit branch checks EXCLUDE then session-scope; Bash branch checks all extracted targets against session scope + EXCLUDE. Security: compound-command bypass (echo x > /tmp/out; rm README.md) blocked; PS quoted-path bypass blocked via tokenizePwsh. Tests: bash-write-patterns.sh +4 read-cases; new fix-enforce-worktree-bundle-a-targets.sh 37 unit cases; new fix-enforce-worktree-bundle-a.sh 31 integration cases.


### FEATURE: planner drafts persistence, intent+outline context merge, show-diff suppression (2026-05-10)
Background: Planner draft files written to OS temp (/tmp or %TEMP%) were lost to OS cleanup or context compaction, requiring planner subagent restart. review-plan-codex lacked intent.md context in the outline-plan branch and had no structured merge for detail-plan, causing out-of-scope reviewer concerns. show-diff.js previewed every draft revision to the user, interrupting the planner/reviewer loop. clarify-intent fallback session IDs used an intent- prefix that produced confusing filenames like intent-20260510-001819-intent.md.
Changes: make-outline-plan/make-detail-plan: drafts now written to ~/.claude/plans/drafts/; context file built from intent+outline sections and passed as --context. review-plan-codex outline-plan branch: added CONTEXT_BLOCK/CONTEXT_NOTE mirroring the detail-plan branch. show-diff.js: added isPlanDraftFile() to suppress previews for ~/.claude/plans/drafts/ only (final artifacts in ~/.claude/plans/*.md still show diffs). clarify-intent: fallback session ID changed from intent-YYYYMMDD-HHMMSS to YYYYMMDD-HHMMSS. docs/ops.md: new file with one-time migration runbook for renaming legacy intent-*.md files. Tests: +7 cases in feature-review-plan-codex.sh; new fix-planner-drafts-context.sh covering show-diff.js isPlanDraftFile suppression (4 cases).


### REFACTOR: marker-gated branch deletion + main worktree terminology cleanup (2026-05-10)
Background: PR #20 had reclassified `git branch -d/-D` as a read operation so the worktree-only-write hook would not block it, but this was location-axis thinking dressed up as taxonomy: the right axis is the target branch's worktree binding, not the current working tree. Combined with `Bash(*git branch -d/-D*)` deny rules in settings.json that intercepted the command before any hook could run, `/worktree-end` was unable to autonomously delete the local branch after squash-merge cleanup — the user had to drop to a terminal each time. Separately, the codebase was riddled with the project-internal jargon "main checkout" where Git's official term is "**main worktree**" (vs "linked worktree"), drifting away from upstream documentation.
Changes:
1. **Marker-file-gated branch deletion** (the autonomy fix). `bash-write-patterns.js` now classifies `git branch -d/-D` as write (alongside `-m/-M`/`-c/-C`). `enforce-worktree.js` exposes a single authorised path: a marker file at `<git-common-dir>/info/pending-branch-delete` containing the target branch name and the worktree path being removed. The hook (`isAllowedBranchDeleteViaMarker`) allows the command iff the marker matches the target AND the recorded path resolves under `WORKTREE_BASE_DIR`. Direct ad-hoc `git branch -d/-D` from any worktree (main or linked) is rejected — no settings.json deny needed. `/worktree-end` SKILL: step 6 reordered to write the marker before `git worktree remove`, switch from `-d` to `-D` (squash-merge produces a non-fully-merged local branch that `-d` refuses), and unconditionally remove the marker on both success and failure paths to prevent stale authorisation.
2. **Terminology cleanup**: replaced "main checkout" → "main worktree" across `enforce-worktree.js` (comments + reason strings), `pre-commit`, `workflow-gate.js`, `rules/branch.md`, `rules/worktree.md`, `skills/worktree-end/SKILL.md`, `CLAUDE.md`, `README.md`, `.env.example`, `docs/parallel-sessions.md`, `docs/architecture/claude-code/settings.md`, `docs/plan.md`, `docs/todo.md`, and 8 test files. `docs/history.md` and `CHANGELOG.md` historical entries left intact (records of past wording at the time).
3. **Settings.json deny removal**: dropped `Bash(*git branch -d*)` and `Bash(*git branch -D*)` from `permissions.deny` so the marker-gated path can actually execute.
Tests: new `fix-unblock-branch-d.sh` (26 cases) covering classify reclassification, marker match/mismatch, path-outside-base rejection, missing marker, chained-command rejection, CRLF acceptance, linked-worktree readability via shared `.git`, and end-to-end hook block-then-allow. Updated `feature-parallel-sessions-worktree-bash-patterns.sh` to assert `-d/-D` are write (replaces previous PR #20 read assertions). Pre-existing failures in `feature-parallel-sessions-worktree-guard.sh`, `fix-enforce-worktree-gh-whitelist.sh`, `refactor-branching-complete-rename-worktree.sh` (related to PR #18's session-scope semantics) are unchanged — verified identical to main.


### CONFIG: correct .env.example EXCLUDE comment about PreToolUse bypass (2026-05-10)
Background: The .env.example comment for ENFORCE_WORKTREE_EXCLUDE still claimed 'Edit/Write/MultiEdit/Bash tool gating is NOT bypassed', which has been false since PR #18 added EXCLUDE bypass to the PreToolUse hook for those tools. The stale comment caused at least one unnecessary worktree creation in a docs-only edit task.
Changes: .env.example: rewrote the comment to state that the PreToolUse hook ALSO bypasses Edit/Write/MultiEdit and Bash file-target writes when every target matches EXCLUDE, with bin/doc-append-plain remaining as the append-only convenience wrapper.


### CONFIG: rewrite .env.example EXCLUDE comment qualitatively (no PR refs) (2026-05-10)
Background: The .env.example entry added in the prior commit referenced PR #18 directly. Code-review feedback: .env.example is end-user config documentation and should describe what a setting does and doesn't do qualitatively, not reference internal change history. PR refs belong in history.md, not in user-facing config.
Changes: .env.example: replaced the prior 'PR #18 added X' phrasing with a structural breakdown listing what is bypassed (PreToolUse Edit/Write/MultiEdit + Bash file-target writes; pre-commit gate when all staged files match) and what is NOT bypassed (block-dotenv, scan-outbound, partial-staged commits). bin/doc-append-plain still called out as the convenience wrapper when EXCLUDE is unset.


### CONFIG: tighten .env.example to user-perspective + codify rule in docs-convention.md (2026-05-10)
Background: The qualitative rewrite of ENFORCE_WORKTREE_EXCLUDE in the prior commit still referenced internal hook names (block-dotenv, scan-outbound, PreToolUse) and tool-class breakdowns (Edit/Write/MultiEdit) that an end user reading .env does not know. Code-review feedback: .env.example should document only what the user can do, what they can't do, and the value format — strictly from a user-perspective.
Changes: Two changes: (1) .env.example: rewrote the ENFORCE_WORKTREE_EXCLUDE block to drop all internal hook/module names and tool-class jargon, replacing them with user-visible phrasing (e.g. 'secret protection' instead of 'block-dotenv'). Trimmed from ~17 lines to ~14 lines. (2) rules/docs-convention.md: added a new content rule for .env.example codifying the three required items (what you can do / what you can't do / format) and the explicit don't-include list (internal names, PR refs, implementation details), so future entries inherit the same lens.


### CONFIG: trim verbose 'do not include' subsection from .env.example convention (2026-05-10)
Background: The newly added .env.example content rule in docs-convention.md included a long 'Do NOT include in .env.example' subsection (4 bullets + a recap paragraph). Per code-review feedback, this was over-specified: the three required items already imply what is in scope, so the negative list was redundant noise.
Changes: rules/docs-convention.md: removed the 'Do NOT include' bullet list and the 'Use plain functional language' recap paragraph from the .env.example entry. The three positive requirements (what you can do / what you can't do / format) remain as the entire rule.


### CONFIG: retrofit CHANGELOG for PRs 19-22 + add CHANGELOG content rule + record write-pattern false-positive (2026-05-10)
Background: PRs 19-22 had updated docs/history.md but skipped CHANGELOG.md because the merging session called bare doc-append on history.md instead of routing through /update-docs (which writes both files for public repos). User flagged the gap. Same session also hit a write-pattern false-positive: POSIX file-copy and file-move tokens appeared in a doc-append subject argument, were classified as a write op, and blocked the command. This echoes the isBranchDeleteCommand subcommand-position regex issue logged earlier.
Changes: CHANGELOG.md: appended four user-facing entries summarising PR 19 (planner draft persistence + cross-stage context propagation), PR 20 (POSIX file-copy/move destination extraction + PowerShell migration runbook), PR 21 (branch deletion now requires /worktree-end marker + main worktree terminology alignment), PR 22 (ENFORCE_WORKTREE_EXCLUDE comment rewrite + .env.example content rule). PR 20 reverted git-branch read-classification deliberately omitted from CHANGELOG to avoid confusing readers upgrading past PR 21. rules/docs-convention.md: added a new content rule for CHANGELOG.md (write only user-felt changes: critical fixes, regressions, behaviour changes, configuration changes; do not include internal function or module or hook names). docs/todo.md: appended a new entry generalising the file-op-token substring false-positive cluster.


### FEATURE: global-gitignore.ps1: add Pester integration tests (2026-05-10)
Background: PS version of global-gitignore installer had zero test coverage; .Count null-ref bug first detected in production. Bash version was already covered by feature-parallel-sessions-worktree-installer-ignore.sh (15 cases).
Changes: Added tests/feature-parallel-sessions-worktree-installer-ignore.Tests.ps1 (Pester 5, 15 cases matching bash version): T01-T14 pass; T15 (unwritable parent dir via icacls) skips on TestDrive. Uses pwsh -File child process + TestDrive XDG isolation.


### FEATURE: worktree-start: .worktreeinclude gitignored-state automated copy (B-Hybrid Phase 1) (2026-05-10)
Background: worktree-start required manual file-copy for gitignored state (.env etc.), but enforce-worktree blocked the operation and Write tool blocked dotenv writes. Implemented B-Hybrid Phase 1: a Node.js CLI reads JSON from stdin and copies gitignored files matched by .worktreeinclude while enforcing .worktreecopyexclude denylist. Claude never reads file contents (blackbox copy guarantee).
Changes: Added bin/worktree-copy-include.js (stdin JSON -> copyInclude -> stdout JSON). Added hooks/lib/worktree-copy.js: enumerates gitignored files via git ls-files, applies ignore-package matchers for allowlist and denylist, path-traversal guard on source and destination, symlink rejection, mainRoot existence check. Added hooks/lib/worktree-include-match.js: thin ignore@5.3.2 wrapper. Added .worktreeinclude (default allowlist: .env/.env.local/.env.development/.env.test/.private-info-allowlist) and .worktreecopyexclude (denylist: .env.production/*.pem etc.). Updated skills/worktree-start/SKILL.md step 9 to use automated node pipeline. Added package.json with ignore@5.3.2. Security: destination bound check, symlink rejection, includeFile traversal guard, denylist negation stripping.


### FEATURE: user-escalation rule: autonomy-first, one-command-at-a-time, script-first (2026-05-11)
Background: CC had no rule governing when to ask the user to run a shell command. Two failure modes: (1) escalating to the user for operations CC could run itself via tools; (2) presenting multiple commands in batch, creating cascade risk if an intermediate command fails and the next one still runs against production state.
Changes: New rules/user-escalation.md with Decision Flow table (destructive check, CC-can-execute check, step count), Rule 1 Autonomy-First (attempt tools first, escalate only when genuinely blocked, no repeat asks), Rule 2 One-Command-at-a-Time (one command per ask, wait for result before next), Rule 3 Script-First (2 or more non-destructive steps bundled into a single fail-fast script). Precedence: ops.md beats Rules 1-3; Rule 1 beats Rules 2-3; Rule 2 beats Rule 3. New tests/feature-user-escalation.sh validates file structure, required headings, and Decision Flow ordering.


### FEATURE: GitHub Issues migration foundation: /issue-close, sub-issue gate, reconcile (2026-05-11, pending)
Background: todo.md files operated as flat open-issue lists with no ticket lifecycle. This PR adds the foundation layer for migrating per-repo work tracking to GitHub Issues: issue templates and label schema aligned with the history.md FEATURE/INCIDENT format, and a transaction-safe close pipeline ensuring every closed issue is atomically appended to docs/history.md before the ticket is shut.
Changes: rules/issues.md: ID-index format (- [ ] #N title), label schema (type:task/incident/priority:*), /issue-close as the sole authorised close path. .github/ISSUE_TEMPLATE/task.yml + incident.yml: issue forms with Background/Changes and Cause/Fix fields matching history.md entry format. .github/labels.yml + bin/sync-labels.sh: label definitions and idempotent gh-based sync. bin/issue-to-history.sh: fetches issue JSON via gh, parses all fields with a single node call (ASCII RS separator, no shell interpolation), calls doc-append; idempotent via ^### #N: anchor check. bin/issue-close-gate.sh: validates open sub-issues via gh api --paginate with multi-page awk sum; blocks parent close if any child is open. hooks/enforce-issue-close.js: PreToolUse hook blocking bare gh issue close commands not routed through the skill; bypass via ISSUE_CLOSE_SKILL=1 env var. skills/issue-close/SKILL.md: 9-step transaction-safe flow: state triage across 6 OPEN/CLOSED x sentinel combinations, pending->appended 2-phase sentinel commit on issue comments, sub-issue gate, doc-append, parent checkbox flip, gh issue close, todo.md cleanup. skills/issue-reconcile/SKILL.md: backfill skill scanning issues closed outside /issue-close (web UI, mobile, CLI). settings.json: enforce-issue-close.js PreToolUse hook entry added.


### FEATURE: Add CONFIRM_OUTLINE/DETAIL/WORKTREE/TESTS flags + get-config-var helper (2026-05-11, 5b530d0 56a2c7f)
Background: Confirmation prompts in make-outline-plan, make-detail-plan, worktree-start, and write-tests were unconditional, interrupting the workflow for users who wanted lower friction. Per-round planner/reviewer summaries also cluttered chat output unnecessarily.
Changes: New bin/get-config-var (POSIX bash) and bin/get-config-var.ps1 read AGENTS_CONFIG_DIR/.env via hooks/lib/load-env.js. The --is-off subcommand exits 0 when value is off/0/false/no/disabled (case-insensitive, no trim), mirroring enforce-worktree.js:69-72 semantics. Four CONFIRM_* flags (default on) added to .env.example: CONFIRM_OUTLINE, CONFIRM_DETAIL, CONFIRM_WORKTREE, CONFIRM_TESTS. Each gated skill checks the flag via bash -c get-config-var --is-off; when off the final result is shown and execution continues without AskUserQuestion. Per-round planner/reviewer chat noise suppressed: orchestrator emits only one status line per round; codex fallback messages go to session-id-{outline,detail}-debug.log instead of chat. Installer wiring: linux symlink and Windows .cmd/bash shim via dotfileslink. Tests: feature-confirm-flags-helper.sh (--is-off matrix, env precedence, path traversal, metachars, idempotency) and feature-confirm-flags-static.sh (skill wiring, installer presence, preserved instructions, removed legacy emit patterns).


### BUGFIX: strip-quoted-args: fix false positives in classify() and isBranchDeleteCommand (2026-05-11)
Background: isBranchDeleteCommand fired when a commit message contains branch -d (marker gate confused message text for a branch delete subcommand). classify() also fired on file-op terms (cp/mv/rm/touch) inside quoted shell arguments (e.g. doc-append subject values).
Changes: New hooks/lib/strip-quoted-args.js strips double-, single-, and ANSI-C-quoted content. classify() applies stripping only to file-op kind patterns via STRIP_KINDS. isBranchDeleteCommand() strips quoted args before branch-delete regex. Added interpreter-c write pattern to preserve write classification for bash -c and sh -c invocations.


### REFACTOR: rules: progressive disclosure — scope large rules to globs: (2026-05-12, pending)
Background: Session startup loaded all rules globally (~35,300 chars / ~8,800 tokens). test.md (107 lines), docs-convention.md (94 lines), coding.md (43 lines) loaded even when irrelevant. paths: frontmatter was silently ignored in user-level rules/ (Issue #21858); correct key is globs:.
Changes: Group A: fixed paths: YAML list to globs: string in 4 files. Group B: split test.md/docs-convention.md/coding.md into thin pointers plus scoped sub-files under rules/docs-convention/, rules/coding/; moved test design rules (categories, naming, layers) to skills/test-design-shared/reference.md loaded only by write-tests/review-tests. Group C: merged feedback_deep_research_source_quality and feedback_neutral_tone into feedback_third_party_references; cleaned feedback_force_push_policy; reindexed MEMORY.md. Tests: 31/31 PASS.


### FEATURE: delivery plan visible at outline stage; importance-first plan sections (2026-05-12)
Background: PR #26 (GitHub Issues migration) had a delivery plan variant buried at line 171 of detail.md without upstream agreement. The delivery plan (triage priority, execution order, split policy) is a higher-priority decision than implementation content, and leaving it implicit until detail.md was a structural gap in the planning workflow.
Changes: outline-planner.md: added required Delivery plan field to each approach Output Format; extended SINGLE_APPROACH_JUSTIFIED to two-line format with mandatory DELIVERY_PLAN: line. make-outline-plan/SKILL.md: added prose rationale preamble before AskUserQuestion; added DELIVERY_PLAN: parsing for single-approach path; updated Output Schema to include Delivery plan field. detail-planner.md: reordered plan sections to importance-first (Delivery plan -> Background -> Files to modify -> Steps -> Risks -> Out of scope). make-detail-plan/SKILL.md: inserted step 2 to surface outline delivery plan to main conversation before planner runs. clarify-intent/SKILL.md: added CONFIRM_OUTLINE flag check — when off, captures delivery plan direction one stage upstream. rules/docs-convention.md: appended Planning Pipeline Artifacts section. New test: tests/feature-delivery-plan-visibility.sh (10 static convention assertions).


### BUGFIX: settings.json push allow-rule + worktree-end marker auto-delete (2026-05-12)
Background: Two gaps blocked /worktree-end from completing cleanly: (1) git push -u origin <branch> from inside a linked worktree lacked a settings.json allow rule; (2) step 6g could not delete the pending-branch-delete marker because the deletion command is write-classified and blocked by enforce-worktree.js from main worktree CWD. Also make-outline-plan/make-detail-plan wrapped get-config-var in bash -c (also write-classified) and both had a defensive mkdir that the Write tool makes redundant.
Changes: settings.json: added Bash(git push -u origin *), Write(**/.git/info/pending-branch-delete), and two marker-delete allow rules. hooks/enforce-worktree.js: added isAllowedMarkerDelete predicate that allows deleting the exact marker path only when the recorded branch is gone (git show-ref exit 1); blocks on chaining, recursive flags, -Path wildcard, multi-target, branch still present, git fatal. Added extractRemoveItemPositionals helper. skills/worktree-end/SKILL.md: documented gate in step 6g. skills/make-outline-plan and make-detail-plan: removed bash -c wrapper from get-config-var and removed defensive mkdir. tests/fix-settings-marker-cleanup.sh: 23 cases covering predicate positive/negative, e2e hook, classifier stability, settings patterns.


### FEATURE: add planning skill allow rules: bash -c get-config-var, get-config-var, Read .claude/plans, printf debug log (2026-05-12)
Background: Planning workflow and write-tests/worktree-start skills invoked Bash commands and Read operations not in permissions.allow, causing permission prompts. Specifically: bash -c 'get-config-var' (used by clarify-intent/worktree-start/write-tests), get-config-var (used directly by make-outline-plan/make-detail-plan post-b7d6a97), Read /**/.claude/plans/** (skills read prior-stage artifacts), printf >> */.claude/plans/* (debug log for malformed codex output). review-plan-codex excluded as it has never prompted. mkdir rule omitted — Write tool auto-creates directories per clarify-intent/SKILL.md.
Changes: settings.json: added 4 allow rules (Bash(bash -c 'get-config-var *'), Bash(get-config-var *), Read(**/.claude/plans/**), Bash(printf * >> */.claude/plans/*)). New tests/feature-allow-rules-planning.sh: static test verifying presence, uniqueness, and placement of each rule. docs/todo.md: added follow-up items for npm install gap in worktree-start and .env/.private end-to-end copy verification.


### BUGFIX: worktree-end Step 6h: add git pull --ff-only after fetch (2026-05-12)
Background: After PR squash-merge, the local main branch was not fast-forwarded to origin/main, leaving it stale for subsequent work. The fix had to remain safe under concurrent sessions editing the same files.
Changes: skills/worktree-end/SKILL.md Step 6h now runs git -C <main> pull --ff-only immediately after git -C <main> fetch --prune origin. --ff-only halts on divergence rather than silently merging, which keeps concurrent-session setups safe. tests/fix-worktree-end-git-pull.sh adds four regression cases (literal string present, order after fetch, located within Step 6 section, no bare pull without --ff-only).


### BUGFIX: settings.json glob fix and worktree-CWD-safe get-config-var calls (2026-05-12)
Background: Two workflow-blocking bugs: (1) settings.json lines 86-87 used single * which cannot cross path separators, causing a permission prompt on every /worktree-end marker cleanup; (2) the 5 SKILL.md gates for CONFIRM_* flags called get-config-var from a linked worktree CWD where .env is absent. The helper fell back to defaults, so off-mode flags were silently ignored in worktrees.
Changes: settings.json: changed * to ** in two Bash allow rules for pending-branch-delete deletion. Added narrow allow rule for cd-prefixed get-config-var form (Bash(bash -c 'cd AGENTS_CONFIG_DIR && get-config-var *')). Wrapped each get-config-var --is-off invocation in 5 SKILL.md files (clarify-intent, make-outline-plan, make-detail-plan, worktree-start, write-tests) with bash -c 'cd AGENTS_CONFIG_DIR && ...' so load-env.js reliably locates .env regardless of worktree CWD. Also added a chaining/pipe caveat note to worktree-start SKILL.md step 5-6 documenting that enforce-worktree.js exemptions only apply to isolated commands.

### FEATURE: planning pipeline UX — narrow plans/drafts allow rules + show-diff Write preview (2026-05-12, pending)
Background: Two friction points: (1) settings.json had broad Write(**/.claude/plans/**) and Edit(**/.claude/plans/**) rules that do not match Windows backslash paths, causing VS Code permission dialogs for intermediate draft files written during the planner/reviewer loop. Final plan artifacts should still show dialogs — only drafts under plans/drafts/ should be silent. (2) show-diff.js Write branch displayed a plain header without a diff, and the editFiles tool was misrouted to the Write branch (content-less), producing empty previews.
Changes: settings.json: replaced two broad plans/** rules with six narrow drafts-only rules covering tilde, forward-slash, and Windows-backslash path forms. show-diff.js: Write branch reads existing file (1 MiB cap + isFile() symlink guard) and generates real diff -u for overwrite, or uses /dev/null as the old-side label for new files; makeDiff extended with opts.oldLabel/newLabel; tmpfile names use pid + 8-byte crypto random suffix; editFiles now correctly routes to the MultiEdit branch. Tests: feature-allow-rules-planning.sh extended with NARROW_RULES array, c2 placement check, d negative check; feature-show-diff-write-preview.sh added (7 cases).


### FEATURE: settings.json: permission update for --force-with-lease operations (2026-05-12)
Background: Deny rules blocked --force-with-lease by matching it as a substring of --force via glob, preventing auto-execution of rebase-cleanup on feature branches despite the policy permitting it.
Changes: settings.json: 3 deny patterns replaced with 6 anchored variants; 2 allow rules added for lease-based force flag. rules/git.md: Force section added. docs/todo.md: Policy section removed. docs/security-policy.md + architecture/settings.md: updated.


### BUGFIX: enforce-worktree: allow Write/Edit and no-op rm for pending-branch-delete marker (2026-05-12, 5de9026)
Background: The pending-branch-delete marker lives at <git-common-dir>/info/pending-branch-delete and is written by /worktree-end from the main worktree before authorising git branch -d. The enforce-worktree hook blocked Write/Edit to this path because it rejects all writes from the main worktree. Additionally, rm of a non-existent marker (stale cleanup after an aborted /worktree-end run) returned false fail-closed, causing an unnecessary permission prompt. The settings.json already had a Write allow rule for this path but it was ineffective because the hook fires before settings.json is consulted.
Changes: Added isMarkerFilePath helper to resolve the marker path via git rev-parse --git-common-dir and compare against the tool input path with cross-platform normalisation. Added Write/Edit exception in the main-checkout block gated by isMarkerFilePath. Fixed isAllowedMarkerDelete to return true on ENOENT (no-op delete is safe) and false on all other errors (fail-closed). Exported isMarkerFilePath. Updated tests: T5 renamed to marker_delete_allowed_no_marker (expected value flipped to true); added T22-T27 covering isMarkerFilePath matching and Write/Edit hook exceptions.


### BUGFIX: enforce-worktree: allow read-only CONFIRM_* probe from main worktree (2026-05-12, pending)
Background: Planning skills use bash -c to probe CONFIRM_* flags via get-config-var --is-off, but enforce-worktree.js classified all bash -c as writes and blocked the probe from the main worktree. CONFIRM_OUTLINE=off and CONFIRM_DETAIL=off were silently ignored in any session started from main.
Changes: Added isAllowedReadOnlyConfigCheck() to enforce-worktree.js: structurally validates three clauses (cd AGENTS_CONFIG_DIR, get-config-var --is-off KEY on|off, echo OFF with optional || echo ON), rejects backtick/dollar-paren/redirect/semicolon/bare-pipe variants. Call site added after isAllowedMarkerDelete. 15 test cases in tests/feature-parallel-sessions-worktree-guard.sh (4 positive, 11 negative). Coupling risk documented in docs/architecture/claude-code/workflow.md.


### FEATURE: workflow-gate: add --wip signal to skip user_verification for fixup commits (2026-05-12, pending)
Background: After the main implementation commit, small fixup commits triggered a full user_verification gate re-block, requiring repeated user approval for low-risk intermediate commits. The UX cost was disproportionate to the risk.
Changes: Added parseGitConfigValues() to hooks/lib/parse-git-args.js (quote-aware, pre-subcommand only). workflow-gate.js detects git -c workflow.wip=1 and skips only the user_verification step; run_tests, review_security, and docs gates still fire. State not mutated in the WIP path so user_verification stays pending and the next non-WIP commit re-blocks (idempotent). commit-push/SKILL.md: added ## WIP mode (--wip) section. WIP signal contract and coupling risk documented in docs/architecture/claude-code/workflow.md. 12 test cases in tests/feature-commit-gate-redesign.sh (Section F).


### FEATURE: Add test-runner subagent for Workflow Step 6 (2026-05-13, pending)
Background: Verbose test output ran in the main context, competing for token budget and obscuring planning context. Step 6 ran tests directly via Bash with a PostToolUse hook auto-marking run_tests, creating tight coupling between test output volume and context quality.
Changes: New agents/test-runner.md agent definition (tools: Bash/Read/Grep/Glob; model: sonnet): runs the test suite, returns a structured YAML summary (status/exit_code/duration/failing_tests/log_tail), never modifies files. CLAUDE.md Step 6 rewritten: three parallel calls (Agent test-runner + Agent security + Bash codex), then a sequential sentinel: run_tests_complete on pass, run_tests_pending on fail/timeout/runner-error. run_tests_pending overwrites stale complete state to prevent a failing build from passing the commit gate. STEP_HINT and workflow-gate.js SKILL_MAP hint updated. docs/architecture/claude-code/workflow.md table row updated. Direct Bash test path retained as backward-compatible fallback.

### BUGFIX: enforce-worktree: three main-worktree unblock fixes — push-from-main, interpreter-c read-only, orphan-dir cleanup (2026-05-13, pending)
Background: Three write-guard over-blocking cases after ENFORCE_WORKTREE hardening. (1) git push origin main was blocked even when all outgoing commit files were ENFORCE_WORKTREE_EXCLUDE-covered (docs-only PRs blocked). (2) bash -c read-only invocations were unconditionally write-classified, blocking planning skill config checks from the main worktree. (3) Orphan directory removal after git worktree remove was blocked by both the hook and settings.json deny rules; no safe alternative existed.
Changes: Fix 1: hooks/enforce-worktree.js: added resolveUpstream and isAllowedPushAllExcluded (fail-closed refspec parser using git log --name-only <upstream>..HEAD; pre-resolves file paths against repoRoot to guard against hook CWD != repoRoot). Fix 2: hooks/lib/bash-write-patterns.js: added isReadOnlyInterpreterC (guards: ANSI-C escapes, here-strings, here-docs, backtick subst, outer chaining, embedded newlines, depth-1 nested interpreter; segment-split classify-each); classify returns read when interpreter-c is the sole non-quoting match and body passes all guards. Fix 3: hooks/cleanup-orphan-dir.js NEW standalone script with 9-step validation pipeline (WORKTREE_BASE_DIR check, path.relative traversal guard, symlink rejection, registered-worktree check via git worktree list --porcelain, non-empty dir rejection, floor check); uses fs.rmdirSync. settings.json: two allow rules added for node hooks/cleanup-orphan-dir.js (POSIX + Windows forms). skills/worktree-end/SKILL.md step 6e: rewritten to use cleanup-orphan-dir.js instead of direct recursive delete from main. Tests: 33 cases across three new scripts — fix-enforce-worktree-push-fix-range.sh (14), fix-enforce-worktree-push-fix-interp.sh (11), fix-enforce-worktree-push-fix-cleanup.sh (8); all pass.


### FEATURE: GitHub Issues full migration — docs/todo.md to thin ID index (2026-05-13, pending)
Background: Direct commits to docs/todo.md caused merge conflicts across sessions and blocked push and worktree-end flows. GitHub Issues adopted as SSOT; todo.md reduced to a number index to eliminate conflicts.
Changes: Created 157 closed issues (#65-#221) from completed history entries (bin/github-issues/migration/migrate-history.sh). Created 24 open issues (#222-#245) from todo.md task sections (bin/github-issues/migration/migrate-todo.sh). Established Projects v2 'agents — Issue Timeline' with Content Date custom field; backfilled all 181 items (bin/github-issues/migration/backfill-content-date.sh). Rewrote docs/todo.md as a thin '- [ ] #N title' ID index. Added bin/github-issues/migration/preview-history.sh for dry-run preview. Reorganised issue scripts under bin/github-issues/ (ongoing) and bin/github-issues/migration/ (one-time).


### BUGFIX: show-diff.js: extend plan-file suppression from plans/drafts/ to all plans/ (2026-05-13, pending)
Background: The 2026-05-12 planning pipeline UX PR suppressed diff previews only for plans/drafts/, leaving final plan artifacts (*-intent.md, *-outline.md, *-detail.md) still triggering previews.
Changes: Renamed isPlanDraftFile to isPlanFile; widened path check from /.claude/plans/drafts/ to /.claude/plans/. Added tests/feature-show-diff-plan-file.sh (9 cases: T1-T9). skills/worktree-start/SKILL.md step 9e: added note that 'Symlink source rejected: .env' is expected when .env is a symlink in dotfiles setups.


### BUGFIX: pre-commit dotenv order fix + enforce-worktree hooks bypass block (2026-05-13, pending)
Background: Fix 1: hooks/pre-commit dotenv-add block ran before private-repo check, blocking .env commits on private repos. Fix 2: hooks/enforce-worktree.js lacked detection of git hooks bypass patterns.
Changes: Fix 1: swapped block order in hooks/pre-commit so private-repo check runs first. Fix 2: added hasGitHooksBypass() to hooks/enforce-worktree.js detecting core.hooksPath bypass via -c, --config-env, GIT_CONFIG_PARAMETERS, GIT_CONFIG_KEY_N. Tests: fix-pre-commit-dotenv-order.sh (7) + fix-enforce-worktree-hooks-bypass.sh (53); all pass.


### FEATURE: approve-plan-writes.js: auto-approve planning skill writes via PreToolUse hook (2026-05-13)
Background: VS Code Claude Code extension's ask-before-edits mode bypasses settings.json permissions.allow/deny path-based glob rules for Edit/Write tools entirely — confirmed empirically (7 candidate glob patterns, all BLOCK; also documented in 2026-05-04 SECURITY history entry). Planning skills (clarify-intent, make-outline-plan, make-detail-plan) write artifacts to ~/.claude/plans/, triggering repeated permission dialogs. The only reliable fix is a PreToolUse hook returning {"decision":"approve"} before the VS Code permission system fires.
Changes: Added hooks/lib/path-match.js: shared path-normalization library (normalizeSlashes, getBasename, getPathSegments, expandHome, isUnderPath; cross-platform, case-insensitive on win32). Added hooks/approve-plan-writes.js: PreToolUse hook that approves Write/Edit/MultiEdit targeting ~/.claude/plans/ by calling isUnderPath. Registered in settings.json PreToolUse after show-diff.js and before block-tests-direct.js. Created GitHub Issues #252/#253/#254; #252 and #254 as sub-issues of #253. Added tests/fix-windows-permission-glob.sh (40 cases).


### CONFIG: settings.json: remove 6 narrow drafts allow rules superseded by approve-plan-writes.js (2026-05-13)
Background: The 6 Write/Edit rules for ~/.claude/plans/drafts/** were confirmed dead on VS Code (ask-before-edits bypasses them) and are now fully covered by the approve-plan-writes.js PreToolUse hook.
Changes: Removed Write(~/.claude/plans/drafts/**), Edit(~/.claude/plans/drafts/**), and their **/ and Windows-backslash variants from settings.json permissions.allow. Updated tests/feature-allow-rules-planning.sh: removed NARROW_RULES presence assertions; added negative assertions confirming all 6 rules are absent.


### FEATURE: Migrate planning artifacts from ~/.claude/plans/ to ~/.workflow-plans/ (2026-05-14)
Background: ~/.claude/plans/ is a Claude Code protected path — VS Code ask-before-edits mode always prompts for Write/Edit calls under .claude/ regardless of PreToolUse hooks or settings.json allow rules (confirmed in prior session). The approve-plan-writes.js workaround could not fully suppress the dialogs. Moving artifacts to ~/.workflow-plans/ (outside the protected .claude/ tree) eliminates the root cause: all modes write silently, no hook needed.
Changes: New hooks/lib/workflow-plans-dir.js: getWorkflowPlansDir() reads WORKFLOW_PLANS_DIR env var (must be absolute; tilde/relative rejected), defaults to ~/.workflow-plans/. New bin/workflow-plans-dir bash bridge (cygpath-aware for Windows). New bin/migrate-plans-dir.js: two-pass content-aware migration (conflict detect → copy → verify → delete src); DST-in-SRC guard; run once on this machine. Deleted hooks/approve-plan-writes.js (no longer needed). Removed ~/.claude/plans/ allow rules from settings.json. Updated hooks/show-diff.js and hooks/workflow-gate.js to call getWorkflowPlansDir(). Updated bin/session-sync.sh and bin/session-sync.ps1 to resolve plans dir via helper. Updated skills/clarify-intent, make-outline-plan, make-detail-plan SKILL.md path references. Updated 15+ test files (path fixtures, removed approve-plan-writes integration tests). New tests: feature-workflow-plans-dir.sh (9 cases), feature-migrate-plans-dir.sh (6 cases).


### FEATURE: confirm-plan protocol + show-diff suppression narrowed to final artifacts only (2026-05-14, caa898d)
Background: Three CONFIRM_* steps (CONFIRM_INTENT, CONFIRM_OUTLINE, CONFIRM_DETAIL) each duplicated the same show-diff preview / flag-check / AskUserQuestion loop inline. show-diff.js suppressed all files under ~/.workflow-plans/ including final plan artifacts, preventing users from seeing intent.md, outline.md, and detail.md diffs in VS Code.
Changes: Added skills/_shared/confirm-plan.md as a shared protocol reference for all CONFIRM_* steps: step 1 (show-diff auto-preview fires on Write), step 2 (absolute path link), step 3 (flag check — OFF: proceed with summary; ON: AskUserQuestion with revise loop). skills/clarify-intent, make-outline-plan, make-detail-plan SKILL.md updated to reference the shared protocol instead of duplicating it. show-diff.js isPlanFile narrowed from all files under workflow-plans/ to only files under workflow-plans/drafts/; final artifacts now show diffs.


### REFACTOR: skills/_shared/: consolidate confirm-plan and test-design shared references (2026-05-14, 9593dce)
Background: confirm-plan-artifact/reference.md and test-design-shared/reference.md were separate directories with no consistent naming convention. The _archived/ precedent suggested using underscore-prefixed directories for non-executable shared content.
Changes: Moved skills/confirm-plan-artifact/reference.md to skills/_shared/confirm-plan.md and skills/test-design-shared/reference.md to skills/_shared/test-design.md. Updated references in skills/clarify-intent, make-outline-plan, make-detail-plan, write-tests, review-tests SKILL.md files and tests/refactor-rules-progressive-disclosure.sh. Deleted old directories.


### BUGFIX: issue-to-history.sh: fix --date missing, --issue removed, --commits injection, FEATURE idempotency (2026-05-14, #261)
Background: issue-to-history.sh had four bugs: (1) --date was not passed to doc-append, so header date fell back to doc-append's internal today instead of the issue's closedAt; (2) --issue N was passed but doc-append does not accept that flag, causing a silent parse-skip; (3) --commit (singular) was used but doc-append only recognises --commits; (4) the FEATURE idempotency regex only matched the legacy ### #N: prefix, missing entries with the new parenthetical (date, #N) format. Also, closedAt was not requested from the GitHub API.
Changes: Added closedAt to gh issue view --json field list and derived CLOSED_DATE from it. Removed --issue from ARGS; added --date "$CLOSED_DATE". Renamed --commit to --commits and injected #N as "${COMMIT}, #${ISSUE_NUM}" or "#${ISSUE_NUM}" so the issue number is embedded in the history header without modifying doc-append. Updated idempotency regex to OR-match both legacy ### #N: and new ### [^(]+ (..., #N) forms. Fixed skip message. Updated tests/fixtures/gh-mock/doc-append to parse --date; updated tests/feature-issue-migration-issue-close.sh (N1/I1/I2 updated + N5-N9 added; 15/15 pass).


### FEATURE: clarify-intent: auto-detect closes_issues; CLAUDE.md: Step 10 issue-close reminder (2026-05-14, #257)
Background: clarify-intent had no mechanism to record which GitHub issue a session was closing, so /issue-close had to be invoked manually after the workflow without session context. CLAUDE.md Step 10 had no reminder to call /issue-close.
Changes: skills/clarify-intent/SKILL.md: added step 1a to auto-detect #N tokens in the user's initial message; records the issue number as closes_issues in intent.md without asking when unambiguous (single #N + clear context), asks during the interview when ambiguous, sets empty list when none found. Added closes_issues field to intent.md Output Schema with serialization examples. CLAUDE.md: added paragraph after Step 10 listing closes_issues usage and /issue-close <N> invocation pattern.

### BUGFIX: confirm-plan protocol: enforce mandatory Steps 1+2 and fix make-outline-plan double-write (2026-05-14, 87fc8de)
Background: Agents executing the planning workflow would sometimes skip the clickable artifact link (confirm-plan protocol Step 2) in OFF mode, because the shared protocol's three-step requirement was under-specified. In make-outline-plan, the file write was split across Step 7 (OFF path) and Step 8 with no protocol coverage in the SINGLE_APPROACH branch. The Rule section said 'One AskUserQuestion per run', which was incorrect for ON mode where approach-selection (Step 7) and artifact-review (Step 8) are distinct touchpoints.
Changes: skills/_shared/confirm-plan.md rewritten: three mandatory steps labelled explicitly; Step 2 marked MANDATORY with 'before any chat output that follows the write' ordering (tool calls allowed between); Step 3 OFF no longer duplicates the link. skills/make-outline-plan/SKILL.md: SINGLE_APPROACH branch now applies full confirm-plan protocol Steps 1+2+3; file write moved from Step 7 to Step 8; Step 8 applies full protocol; Rules section updated to 'Two AskUserQuestion calls per run in ON mode' with rationale. skills/make-detail-plan/SKILL.md: OFF path note updated to reference protocol Steps 2+3.


### BUGFIX: enforce-worktree: move /worktree-end marker out of .git/ to eliminate protected-path permission prompts (2026-05-14, #270)
Background: The pending-branch-delete marker written by /worktree-end was stored at <git-common-dir>/info/pending-branch-delete — inside .git/, a Claude Code protected path. In default and acceptEdits modes, writes always trigger a permission prompt regardless of settings.json allow rules. The existing Write/**/.git/info/pending-branch-delete allow rule was therefore ineffective. Issue #270. Same root cause as the .claude/plans/ → ~/.workflow-plans/ fix in PR #256.
Changes: Moved marker to ~/.workflow-plans/worktree-end/pending-branch-delete-<sha256-16>--<encoded-branch> (WORKFLOW_PLANS_DIR customisable). Added getWorktreeEndDir(), getRepoId() (sha256 .slice(0,16), 64-bit repo ID from git-common-dir), and getMarkerPath() helpers to enforce-worktree.js. Rewrote isAllowedBranchDeleteViaMarker (uses getMarkerPath directly), isAllowedMarkerDelete (validates directory + sha256-16 repo-id prefix + git show-ref branch-gone check; fs.readFileSync(nTarget) for path consistency), and isMarkerFilePath (validates directory + sha256-16 prefix + non-empty branch suffix). Security follow-up: sha1 → sha256, readFileSync(target) → readFileSync(nTarget). Updated skills/worktree-end/SKILL.md Steps 6b (repo-id computation, encoded-branch, marker-path storage) and 6g (reuse <marker-path>, not re-derive from WORKFLOW_PLANS_DIR). Deleted 3 dead allow rules for .git/info path from settings.json. Updated tests/fix-settings-marker-cleanup.sh: marker_path_for(repo,branch) computes sha256-16 path, per-test WORKFLOW_PLANS_DIR isolation, T19-T21 removed (no longer relevant), T23 renamed to isMarkerFilePath_wrong_repo_id; 24 numbered tests pass.


### FEATURE: issue-fix-history cross-reference — closes #N PR flow, auto_close_path, backfill (#222) (2026-05-14)
Background: Issues closed via GitHub's closes #N merge keyword left no history.md entry and no resolved-by sentinel comment. /issue-close aborted on CLOSED+no-sentinel state instead of recovering gracefully. No mechanism existed to retroactively link past closed issues to their commit hashes.
Changes: New bin/github-issues/issue-close-triage.sh encodes all state×sentinel routing (OPEN/CLOSED × none/pending/appended → NEXT_STEPS); CLOSED+no-sentinel maps to auto_close_path (B,E,G,J). New bin/github-issues/post-close-sentinels.sh: J-1 resolved-by hash comment (when --commit provided), J-2 appended sentinel — both idempotent via gh --jq. New bin/github-issues/parent-body-update.sh: flips - [ ] #N → - [x] #N in parent body, no-op when no parent. New bin/github-issues/backfill-commit-comments.sh: --dry-run mode; posts appended sentinel with optional commit hash extracted from history.md for all closed issues lacking one. skills/issue-close/SKILL.md rewritten to 103 lines (was 177); all state routing delegated to triage script; Step J added. rules/github-issues.md: dual-write period ended; docs/todo.md is now a pointer only; two close flows documented. skills/commit-push/SKILL.md: PR step instructs inclusion of Closes #N in --body. CLAUDE.md: noted that /issue-close handles CLOSED+(none) state after PR auto-close.

### FEATURE: Add /workflow-init routing skill — issue-aware workflow entry point (Path A/B/C) (2026-05-14, pending)
Background: clarify-intent was always the mandatory first step, but GitHub issue integration required routing at session start: sessions with an issue marked intent:clarified should skip the interview entirely; sessions with an issue but no label should pre-fill the interview; sessions with no issue should run the full interview and auto-create a tracking issue at completion.
Changes: Added skills/workflow-init/SKILL.md: three routing paths (A: intent:clarified label -> write intent.md + skip interview; B: issue present, no label -> write pre-fill file + delegate to clarify-intent; C: no issue -> delegate to clarify-intent as before). Added workflow_init as first element of VALID_STEPS in hooks/lib/workflow-state.js; migration backfills legacy sessions (ci complete/skipped -> wi complete; ci absent or pending -> wi pending). Added STEP_HINT entry for workflow_init. workflow-gate.js: replaced single-tier early gate with two-tier check (Tier 1: workflow_init, Tier 2: clarify_intent); plans-path allowlist moved outside both tiers; SKILL_MAP updated. hooks/session-start.js: fallback NEXT_ACTION changed from clarify_intent to workflow_init hint. skills/clarify-intent/SKILL.md: added step 1a auto-detect closes_issues note that pre-fill satisfies issue detection; added step 1b pre-fill detection from ~/.workflow-plans/drafts/<sid>-issue-prefill.md; Completion section updated with issue reconciliation (label N -> add intent:clarified; empty -> gh issue create + update closes_issues); TodoWrite checklist now marks workflow_init + clarify_intent completed. CLAUDE.md Step 1 replaced with /workflow-init routing description. .github/labels.yml: appended intent:clarified label (color 1d76db). tests/feature-workflow-init-routing.sh: 22 cases covering migration (M1-M4), early gate (G5-G8), sentinel (S9), and content checks (C10-C12).

### FEATURE: show-plan-link.js: PostToolUse hook to emit plan file path breadcrumb (2026-05-15)
Background: confirm-plan.md Step 2 instructed the LLM to print a clickable absolute-path link after each plan artifact write, but execution was unreliable — tildes unexpanded, Windows backslashes used, and VS Code extension webview upstream bug (issues #53919, #16056) preventing clickable links on C: drive paths. Moving this to a deterministic PostToolUse hook ensures the breadcrumb always appears.
Changes: New hooks/show-plan-link.js: fires on Write tool for files directly under ~/.workflow-plans/ matching *-(intent|outline|detail).md (not drafts/); emits {systemMessage:"Plan file written: <abs-path>"}; when CLAUDE_CODE_ENTRYPOINT==="claude-vscode" also spawns code -r <path> fire-and-forget via cmd.exe on Windows (CVE-2024-27980 workaround) or directly on POSIX. Registered in settings.json as PostToolUse Write matcher. skills/_shared/confirm-plan.md Step 2 rewritten: hook handles breadcrumb; fallback to plain-text if hook not deployed; removed markdown link instructions. New tests/feature-show-plan-link.sh: 19 cases (T1-T19) covering artifact detection, exclusions, tool filtering, Windows backslash normalization, VS Code shim, custom WORKFLOW_PLANS_DIR, malformed stdin, and failed-write detection.


### BUGFIX: enforce-issue-close: inline ISSUE_CLOSE_SKILL=1 bypass + worktree dead-CWD fail-open + /worktree-end CWD step (#267, #268, #251) (2026-05-15, pending)
Background: Three linked bugs. (1) #267: enforce-issue-close.js only checked process.env.ISSUE_CLOSE_SKILL — missed inline VAR=val cmd prefix form used by /issue-close skill. (2) #268: after /worktree-end removed the linked worktree, subsequent hook invocations ran with a dead CWD, breaking enforce-worktree.js for the rest of the session. (3) #251: Windows OS CWD lock caused git worktree remove EPERM when session CWD was inside the worktree being removed.
Changes: (1) Added INLINE_SKILL_RE strict-shape regex to enforce-issue-close.js; matches only the exact canonical invocation shape the skill generates (ISSUE_CLOSE_SKILL=1 as inline prefix, digits-only issue id, --reason completed required, end-anchored, horizontal whitespace only). Tests N11-N21 added to feature-issue-migration-enforce-hook.sh. (2) Added dead-CWD fail-open to enforce-worktree.js: process.cwd() ENOENT catch-and-done + fs.existsSync false branch-and-done; both paths emit audit stderr. Tests C1-C5 in new fix-enforce-worktree-cwd-fallback.sh. (3) Inserted step 6b.5 in skills/worktree-end/SKILL.md: standalone cd to main-worktree-root as a separate Bash call before step 6c worktree remove (chained form blocked by enforce-worktree.js isolated-command rule).


### CONFIG: Auto-sync labels.yml to GitHub via Actions (2026-05-15)
Background: labels.yml is the SSOT for repo labels but manual sync was required; intent:clarified missed remote until manual run.
Changes: Added .github/workflows/sync-labels.yml — triggers on push to main touching labels.yml (plus workflow_dispatch). Shortened intent:clarified description to fit GitHub's 100-char limit. Closes #278.


### BUGFIX: #267: enforce-issue-close.js ignores inline ISSUE_CLOSE_SKILL=1 prefix (2026-05-15, 9321e9e)
Background: PreToolUse hook checked only process.env.ISSUE_CLOSE_SKILL, missing inline VAR=val cmd prefix used by /issue-close skill.
Changes: See the BUGFIX entry above titled enforce-issue-close: inline ISSUE_CLOSE_SKILL=1 bypass for full implementation details (#267, #268, #251 fixed together in PR #276).


### BUGFIX: #268: enforce-worktree breaks after worktree removal (dead CWD) (2026-05-15, 9321e9e)
Background: After /worktree-end removed the linked worktree, subsequent hook invocations ran with a dead CWD, breaking enforce-worktree.js for the rest of the session.
Changes: See the BUGFIX entry above titled enforce-issue-close: inline ISSUE_CLOSE_SKILL=1 bypass for full implementation details (#267, #268, #251 fixed together in PR #276).


### BUGFIX: #251: worktree-end worktree-removal fails with EPERM on Windows (CWD lock) (2026-05-15, 9321e9e)
Background: Windows OS held a CWD lock on the linked worktree path when session CWD was inside it, causing the removal step to fail with EPERM.
Changes: See the BUGFIX entry above for full implementation details (#267, #268, #251 fixed together in PR #276).


### FEATURE: worktree-start / worktree-end hidden from slash-command autocomplete (#281) (2026-05-15, pending)
Background: /workflow-init required typing 'workf' (5 chars) to uniquely complete because worktree-start and worktree-end shared the 'work' prefix. Users never invoke these skills directly — they are workflow-internal only.
Changes: Added user-invocable: false to SKILL.md frontmatter of skills/worktree-start and skills/worktree-end. Claude Code's documented frontmatter field hides the skill from the / autocomplete menu while keeping Skill-tool invocation intact. Result: 'wo' + Enter now uniquely completes to /workflow-init.


### FEATURE: worktree-start / worktree-end hidden from slash-command autocomplete (#281) (2026-05-15, pending)
Background: /workflow-init required typing 'workf' (5 chars) to uniquely complete because worktree-start and worktree-end shared the 'work' prefix. Users never invoke these skills directly — they are workflow-internal only.
Changes: Added user-invocable: false to SKILL.md frontmatter of skills/worktree-start and skills/worktree-end. Claude Code's documented frontmatter field hides the skill from the / autocomplete menu while keeping Skill-tool invocation intact. Result: 'wo' + Enter now uniquely completes to /workflow-init.


### FEATURE: review-skill-size — Step 6 skill size/quality review script (#284) (2026-05-15)
Background: CC tends to write verbose skill definitions. Added an automated size/quality check to Step 6 so that any session editing a SKILL.md file gets a non-blocking review.
Changes: Added bin/review-skill-size — a bash script that detects changed skills/*/SKILL.md files (excluding _archived/), warns when a file exceeds 100 lines, and always prints a qualitative checklist for checks 2/3/4 (remove verbose prose, extract complex logic to external scripts, split overscoped skills). Integrated into CLAUDE.md Step 6 as a parallel Bash call (review-skill-size --base <merge-base>). Added tests/feature-review-skill-size.sh with 7 smoke test cases. Closes #284.


### FEATURE: review-skill-size — Step 6 skill size/quality review script (#284) (2026-05-15)
Background: CC tends to write verbose skill definitions. Added an automated size/quality check to Step 6 so that any session editing a SKILL.md file gets a non-blocking review.
Changes: Added bin/review-skill-size — a bash script that detects changed skills/*/SKILL.md files (excluding _archived/), warns when a file exceeds 100 lines, and always prints a qualitative checklist for checks 2/3/4 (remove verbose prose, extract complex logic to external scripts, split overscoped skills). Integrated into CLAUDE.md Step 6 as a parallel Bash call (review-skill-size --base <merge-base>). Added tests/feature-review-skill-size.sh with 7 smoke test cases. Closes #284.

